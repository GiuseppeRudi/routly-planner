;; ============================================================
;;  DOMAIN: road-network
;;  Features: tl_cong-pddl-dynamic_llm_macro
;;    traversal             : compiled_duration
;;    state representation  : node_based
;;    action generation     : compiled
;;    traffic_lights  : True
;;    congestion      : enabled=True, mode=pddl, type=dynamic
;;    llm_events      : True
;;    fuel            : enabled=False, replanning=False, consumption=continuous
;; ============================================================

(define (domain road-network)
  (:requirements :typing :numeric-fluents :time)
  (:types
    vehicle location road time-window - object
    loc_type_loc_0000 loc_type_loc_0001 loc_type_loc_0002 loc_type_loc_0003 loc_type_loc_0004 loc_type_loc_0005 loc_type_loc_0006 loc_type_loc_0007 loc_type_loc_0008 loc_type_loc_0009 loc_type_loc_0010 loc_type_loc_0011 loc_type_loc_0012 loc_type_loc_0013 loc_type_loc_0014 loc_type_loc_0015 loc_type_loc_0016 loc_type_loc_0017 loc_type_loc_0018 loc_type_loc_0019 loc_type_loc_0020 loc_type_loc_0021 loc_type_loc_0022 loc_type_loc_0023 loc_type_loc_0024 loc_type_loc_0025 loc_type_loc_0026 loc_type_loc_0027 loc_type_loc_0028 loc_type_loc_0029 loc_type_loc_0030 loc_type_loc_0031 loc_type_loc_0032 loc_type_loc_0033 loc_type_loc_0034 loc_type_loc_0035 loc_type_loc_0036 loc_type_loc_0037 loc_type_loc_0038 loc_type_loc_0039 loc_type_loc_0040 loc_type_loc_0041 loc_type_loc_0043 loc_type_loc_0044 loc_type_loc_0045 loc_type_loc_0046 loc_type_loc_0047 loc_type_loc_0048 loc_type_loc_0049 loc_type_loc_0050 loc_type_loc_0051 loc_type_loc_0052 loc_type_loc_0053 loc_type_loc_0054 loc_type_loc_0055 loc_type_loc_0056 loc_type_loc_0057 loc_type_loc_0058 loc_type_loc_0059 loc_type_loc_0060 loc_type_loc_0061 loc_type_loc_0062 loc_type_loc_0063 loc_type_loc_0064 loc_type_loc_0065 loc_type_loc_0066 loc_type_loc_0067 loc_type_loc_0068 loc_type_loc_0069 loc_type_loc_0070 loc_type_loc_0071 loc_type_loc_0072 loc_type_loc_0073 loc_type_loc_0076 loc_type_loc_0077 loc_type_loc_0078 loc_type_loc_0079 loc_type_loc_0080 loc_type_loc_0081 loc_type_loc_0082 loc_type_loc_0083 loc_type_loc_0084 loc_type_loc_0085 loc_type_loc_0086 loc_type_loc_0087 loc_type_loc_0088 loc_type_loc_0089 loc_type_loc_0091 loc_type_loc_0092 loc_type_loc_0093 loc_type_loc_0094 loc_type_loc_0095 loc_type_loc_0096 loc_type_loc_0097 loc_type_loc_0098 loc_type_loc_0099
    - location
    road_type_macro_0000 road_type_macro_0001 road_type_macro_0002 road_type_macro_0003 road_type_macro_0004 road_type_macro_0005 road_type_macro_0006 road_type_macro_0007 road_type_macro_0008 road_type_macro_0009 road_type_macro_0010 road_type_macro_0011 road_type_macro_0012 road_type_macro_0013 road_type_macro_0014 road_type_macro_0015 road_type_macro_0016 road_type_macro_0017 road_type_road_0000 road_type_road_0001 road_type_road_0002 road_type_road_0003 road_type_road_0004 road_type_road_0008 road_type_road_0009 road_type_road_0010 road_type_road_0011 road_type_road_0012 road_type_road_0013 road_type_road_0014 road_type_road_0015 road_type_road_0017 road_type_road_0018 road_type_road_0019 road_type_road_0020 road_type_road_0021 road_type_road_0022 road_type_road_0023 road_type_road_0024 road_type_road_0025 road_type_road_0026 road_type_road_0027 road_type_road_0028 road_type_road_0029 road_type_road_0030 road_type_road_0031 road_type_road_0032 road_type_road_0033 road_type_road_0034 road_type_road_0035 road_type_road_0036 road_type_road_0037 road_type_road_0038 road_type_road_0039 road_type_road_0040 road_type_road_0041 road_type_road_0042 road_type_road_0043 road_type_road_0044 road_type_road_0045 road_type_road_0046 road_type_road_0047 road_type_road_0049 road_type_road_0050 road_type_road_0051 road_type_road_0052 road_type_road_0054 road_type_road_0056 road_type_road_0057 road_type_road_0058 road_type_road_0059 road_type_road_0060 road_type_road_0061 road_type_road_0063 road_type_road_0064 road_type_road_0065 road_type_road_0066 road_type_road_0067 road_type_road_0069 road_type_road_0070 road_type_road_0071 road_type_road_0072 road_type_road_0073 road_type_road_0075 road_type_road_0076 road_type_road_0077 road_type_road_0078 road_type_road_0079 road_type_road_0080 road_type_road_0083 road_type_road_0084 road_type_road_0085 road_type_road_0086 road_type_road_0088 road_type_road_0089 road_type_road_0090 road_type_road_0092 road_type_road_0093 road_type_road_0094 road_type_road_0095 road_type_road_0096 road_type_road_0098 road_type_road_0100 road_type_road_0101 road_type_road_0102 road_type_road_0103 road_type_road_0105 road_type_road_0106 road_type_road_0108 road_type_road_0109 road_type_road_0110 road_type_road_0111 road_type_road_0112 road_type_road_0113 road_type_road_0114 road_type_road_0116 road_type_road_0117 road_type_road_0118 road_type_road_0122 road_type_road_0123 road_type_road_0124 road_type_road_0125 road_type_road_0126 road_type_road_0127 road_type_road_0128 road_type_road_0129 road_type_road_0130 road_type_road_0131 road_type_road_0133 road_type_road_0134 road_type_road_0135 road_type_road_0137 road_type_road_0138 road_type_road_0139 road_type_road_0140 road_type_road_0141 road_type_road_0142 road_type_road_0143 road_type_road_0144 road_type_road_0146 road_type_road_0147 road_type_road_0148 road_type_road_0149 road_type_road_0150 road_type_road_0151 road_type_road_0152 road_type_road_0154 road_type_road_0156 road_type_road_0157 road_type_road_0158 road_type_road_0159 road_type_road_0160 road_type_road_0161
    - road
    window_type_tw_00000 window_type_tw_00030 window_type_tw_00060 window_type_tw_00090 window_type_tw_00120 window_type_tw_00150 window_type_tw_00180 window_type_tw_00210 window_type_tw_00240 window_type_tw_00270 window_type_tw_00300 window_type_tw_00330 window_type_tw_00360 window_type_tw_00390 window_type_tw_00420 window_type_tw_00450
    - time-window
  )

  (:predicates
    (connects  ?r - road  ?from - location  ?to - location)
    (road-open ?r - road)
    (has-traffic-light ?l - location)
    (static-road  ?r - road)
    (dynamic-road ?r - road)
    (current-window ?w - time-window)
    (next-window ?from - time-window ?to - time-window)
    (road-blocked ?r - road)   ;; set by LLM event generator
    (location-blocked ?l - location)   ;; derived from blocked roads
    (at       ?v - vehicle  ?l - location)
  )

  (:functions
    (travel-duration      ?r - road)  ;; precomputed seconds to traverse
    (travel-time          ?v - vehicle)
    (congestion-factor    ?r - road)  ;; 1.0=free, 2.0=half speed
    (sim-time)
    (window-start         ?w - time-window)
    (travel-duration-window ?r - road ?w - time-window)
  )

  (:action traverse-road-dynamic-macro_0000-tw_00000
    :parameters (?v - vehicle ?r - road_type_macro_0000 ?from - loc_type_loc_0002 ?to - loc_type_loc_0081 ?w - window_type_tw_00000)
    :precondition (and
      (at ?v ?from)
      (connects ?r ?from ?to)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
      (not (location-blocked ?from))
      (not (location-blocked ?to))
    )
    :effect (and
      (not (at ?v ?from))
      (at ?v ?to)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-macro_0000-tw_00030
    :parameters (?v - vehicle ?r - road_type_macro_0000 ?from - loc_type_loc_0002 ?to - loc_type_loc_0081 ?w - window_type_tw_00030)
    :precondition (and
      (at ?v ?from)
      (connects ?r ?from ?to)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
      (not (location-blocked ?from))
      (not (location-blocked ?to))
    )
    :effect (and
      (not (at ?v ?from))
      (at ?v ?to)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-macro_0000-tw_00060
    :parameters (?v - vehicle ?r - road_type_macro_0000 ?from - loc_type_loc_0002 ?to - loc_type_loc_0081 ?w - window_type_tw_00060)
    :precondition (and
      (at ?v ?from)
      (connects ?r ?from ?to)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
      (not (location-blocked ?from))
      (not (location-blocked ?to))
    )
    :effect (and
      (not (at ?v ?from))
      (at ?v ?to)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-macro_0000-tw_00090
    :parameters (?v - vehicle ?r - road_type_macro_0000 ?from - loc_type_loc_0002 ?to - loc_type_loc_0081 ?w - window_type_tw_00090)
    :precondition (and
      (at ?v ?from)
      (connects ?r ?from ?to)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
      (not (location-blocked ?from))
      (not (location-blocked ?to))
    )
    :effect (and
      (not (at ?v ?from))
      (at ?v ?to)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-macro_0000-tw_00120
    :parameters (?v - vehicle ?r - road_type_macro_0000 ?from - loc_type_loc_0002 ?to - loc_type_loc_0081 ?w - window_type_tw_00120)
    :precondition (and
      (at ?v ?from)
      (connects ?r ?from ?to)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
      (not (location-blocked ?from))
      (not (location-blocked ?to))
    )
    :effect (and
      (not (at ?v ?from))
      (at ?v ?to)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-macro_0000-tw_00150
    :parameters (?v - vehicle ?r - road_type_macro_0000 ?from - loc_type_loc_0002 ?to - loc_type_loc_0081 ?w - window_type_tw_00150)
    :precondition (and
      (at ?v ?from)
      (connects ?r ?from ?to)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
      (not (location-blocked ?from))
      (not (location-blocked ?to))
    )
    :effect (and
      (not (at ?v ?from))
      (at ?v ?to)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-macro_0000-tw_00180
    :parameters (?v - vehicle ?r - road_type_macro_0000 ?from - loc_type_loc_0002 ?to - loc_type_loc_0081 ?w - window_type_tw_00180)
    :precondition (and
      (at ?v ?from)
      (connects ?r ?from ?to)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
      (not (location-blocked ?from))
      (not (location-blocked ?to))
    )
    :effect (and
      (not (at ?v ?from))
      (at ?v ?to)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-macro_0000-tw_00210
    :parameters (?v - vehicle ?r - road_type_macro_0000 ?from - loc_type_loc_0002 ?to - loc_type_loc_0081 ?w - window_type_tw_00210)
    :precondition (and
      (at ?v ?from)
      (connects ?r ?from ?to)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
      (not (location-blocked ?from))
      (not (location-blocked ?to))
    )
    :effect (and
      (not (at ?v ?from))
      (at ?v ?to)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-macro_0000-tw_00240
    :parameters (?v - vehicle ?r - road_type_macro_0000 ?from - loc_type_loc_0002 ?to - loc_type_loc_0081 ?w - window_type_tw_00240)
    :precondition (and
      (at ?v ?from)
      (connects ?r ?from ?to)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
      (not (location-blocked ?from))
      (not (location-blocked ?to))
    )
    :effect (and
      (not (at ?v ?from))
      (at ?v ?to)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-macro_0000-tw_00270
    :parameters (?v - vehicle ?r - road_type_macro_0000 ?from - loc_type_loc_0002 ?to - loc_type_loc_0081 ?w - window_type_tw_00270)
    :precondition (and
      (at ?v ?from)
      (connects ?r ?from ?to)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
      (not (location-blocked ?from))
      (not (location-blocked ?to))
    )
    :effect (and
      (not (at ?v ?from))
      (at ?v ?to)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-macro_0000-tw_00300
    :parameters (?v - vehicle ?r - road_type_macro_0000 ?from - loc_type_loc_0002 ?to - loc_type_loc_0081 ?w - window_type_tw_00300)
    :precondition (and
      (at ?v ?from)
      (connects ?r ?from ?to)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
      (not (location-blocked ?from))
      (not (location-blocked ?to))
    )
    :effect (and
      (not (at ?v ?from))
      (at ?v ?to)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-macro_0000-tw_00330
    :parameters (?v - vehicle ?r - road_type_macro_0000 ?from - loc_type_loc_0002 ?to - loc_type_loc_0081 ?w - window_type_tw_00330)
    :precondition (and
      (at ?v ?from)
      (connects ?r ?from ?to)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
      (not (location-blocked ?from))
      (not (location-blocked ?to))
    )
    :effect (and
      (not (at ?v ?from))
      (at ?v ?to)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-macro_0000-tw_00360
    :parameters (?v - vehicle ?r - road_type_macro_0000 ?from - loc_type_loc_0002 ?to - loc_type_loc_0081 ?w - window_type_tw_00360)
    :precondition (and
      (at ?v ?from)
      (connects ?r ?from ?to)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
      (not (location-blocked ?from))
      (not (location-blocked ?to))
    )
    :effect (and
      (not (at ?v ?from))
      (at ?v ?to)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-macro_0000-tw_00390
    :parameters (?v - vehicle ?r - road_type_macro_0000 ?from - loc_type_loc_0002 ?to - loc_type_loc_0081 ?w - window_type_tw_00390)
    :precondition (and
      (at ?v ?from)
      (connects ?r ?from ?to)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
      (not (location-blocked ?from))
      (not (location-blocked ?to))
    )
    :effect (and
      (not (at ?v ?from))
      (at ?v ?to)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-macro_0000-tw_00420
    :parameters (?v - vehicle ?r - road_type_macro_0000 ?from - loc_type_loc_0002 ?to - loc_type_loc_0081 ?w - window_type_tw_00420)
    :precondition (and
      (at ?v ?from)
      (connects ?r ?from ?to)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
      (not (location-blocked ?from))
      (not (location-blocked ?to))
    )
    :effect (and
      (not (at ?v ?from))
      (at ?v ?to)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-macro_0000-tw_00450
    :parameters (?v - vehicle ?r - road_type_macro_0000 ?from - loc_type_loc_0002 ?to - loc_type_loc_0081 ?w - window_type_tw_00450)
    :precondition (and
      (at ?v ?from)
      (connects ?r ?from ?to)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
      (not (location-blocked ?from))
      (not (location-blocked ?to))
    )
    :effect (and
      (not (at ?v ?from))
      (at ?v ?to)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-macro_0001-tw_00000
    :parameters (?v - vehicle ?r - road_type_macro_0001 ?from - loc_type_loc_0003 ?to - loc_type_loc_0031 ?w - window_type_tw_00000)
    :precondition (and
      (at ?v ?from)
      (connects ?r ?from ?to)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
      (not (location-blocked ?from))
      (not (location-blocked ?to))
    )
    :effect (and
      (not (at ?v ?from))
      (at ?v ?to)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-macro_0001-tw_00030
    :parameters (?v - vehicle ?r - road_type_macro_0001 ?from - loc_type_loc_0003 ?to - loc_type_loc_0031 ?w - window_type_tw_00030)
    :precondition (and
      (at ?v ?from)
      (connects ?r ?from ?to)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
      (not (location-blocked ?from))
      (not (location-blocked ?to))
    )
    :effect (and
      (not (at ?v ?from))
      (at ?v ?to)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-macro_0001-tw_00060
    :parameters (?v - vehicle ?r - road_type_macro_0001 ?from - loc_type_loc_0003 ?to - loc_type_loc_0031 ?w - window_type_tw_00060)
    :precondition (and
      (at ?v ?from)
      (connects ?r ?from ?to)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
      (not (location-blocked ?from))
      (not (location-blocked ?to))
    )
    :effect (and
      (not (at ?v ?from))
      (at ?v ?to)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-macro_0001-tw_00090
    :parameters (?v - vehicle ?r - road_type_macro_0001 ?from - loc_type_loc_0003 ?to - loc_type_loc_0031 ?w - window_type_tw_00090)
    :precondition (and
      (at ?v ?from)
      (connects ?r ?from ?to)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
      (not (location-blocked ?from))
      (not (location-blocked ?to))
    )
    :effect (and
      (not (at ?v ?from))
      (at ?v ?to)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-macro_0001-tw_00120
    :parameters (?v - vehicle ?r - road_type_macro_0001 ?from - loc_type_loc_0003 ?to - loc_type_loc_0031 ?w - window_type_tw_00120)
    :precondition (and
      (at ?v ?from)
      (connects ?r ?from ?to)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
      (not (location-blocked ?from))
      (not (location-blocked ?to))
    )
    :effect (and
      (not (at ?v ?from))
      (at ?v ?to)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-macro_0001-tw_00150
    :parameters (?v - vehicle ?r - road_type_macro_0001 ?from - loc_type_loc_0003 ?to - loc_type_loc_0031 ?w - window_type_tw_00150)
    :precondition (and
      (at ?v ?from)
      (connects ?r ?from ?to)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
      (not (location-blocked ?from))
      (not (location-blocked ?to))
    )
    :effect (and
      (not (at ?v ?from))
      (at ?v ?to)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-macro_0001-tw_00180
    :parameters (?v - vehicle ?r - road_type_macro_0001 ?from - loc_type_loc_0003 ?to - loc_type_loc_0031 ?w - window_type_tw_00180)
    :precondition (and
      (at ?v ?from)
      (connects ?r ?from ?to)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
      (not (location-blocked ?from))
      (not (location-blocked ?to))
    )
    :effect (and
      (not (at ?v ?from))
      (at ?v ?to)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-macro_0001-tw_00210
    :parameters (?v - vehicle ?r - road_type_macro_0001 ?from - loc_type_loc_0003 ?to - loc_type_loc_0031 ?w - window_type_tw_00210)
    :precondition (and
      (at ?v ?from)
      (connects ?r ?from ?to)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
      (not (location-blocked ?from))
      (not (location-blocked ?to))
    )
    :effect (and
      (not (at ?v ?from))
      (at ?v ?to)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-macro_0001-tw_00240
    :parameters (?v - vehicle ?r - road_type_macro_0001 ?from - loc_type_loc_0003 ?to - loc_type_loc_0031 ?w - window_type_tw_00240)
    :precondition (and
      (at ?v ?from)
      (connects ?r ?from ?to)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
      (not (location-blocked ?from))
      (not (location-blocked ?to))
    )
    :effect (and
      (not (at ?v ?from))
      (at ?v ?to)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-macro_0001-tw_00270
    :parameters (?v - vehicle ?r - road_type_macro_0001 ?from - loc_type_loc_0003 ?to - loc_type_loc_0031 ?w - window_type_tw_00270)
    :precondition (and
      (at ?v ?from)
      (connects ?r ?from ?to)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
      (not (location-blocked ?from))
      (not (location-blocked ?to))
    )
    :effect (and
      (not (at ?v ?from))
      (at ?v ?to)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-macro_0001-tw_00300
    :parameters (?v - vehicle ?r - road_type_macro_0001 ?from - loc_type_loc_0003 ?to - loc_type_loc_0031 ?w - window_type_tw_00300)
    :precondition (and
      (at ?v ?from)
      (connects ?r ?from ?to)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
      (not (location-blocked ?from))
      (not (location-blocked ?to))
    )
    :effect (and
      (not (at ?v ?from))
      (at ?v ?to)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-macro_0001-tw_00330
    :parameters (?v - vehicle ?r - road_type_macro_0001 ?from - loc_type_loc_0003 ?to - loc_type_loc_0031 ?w - window_type_tw_00330)
    :precondition (and
      (at ?v ?from)
      (connects ?r ?from ?to)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
      (not (location-blocked ?from))
      (not (location-blocked ?to))
    )
    :effect (and
      (not (at ?v ?from))
      (at ?v ?to)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-macro_0001-tw_00360
    :parameters (?v - vehicle ?r - road_type_macro_0001 ?from - loc_type_loc_0003 ?to - loc_type_loc_0031 ?w - window_type_tw_00360)
    :precondition (and
      (at ?v ?from)
      (connects ?r ?from ?to)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
      (not (location-blocked ?from))
      (not (location-blocked ?to))
    )
    :effect (and
      (not (at ?v ?from))
      (at ?v ?to)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-macro_0001-tw_00390
    :parameters (?v - vehicle ?r - road_type_macro_0001 ?from - loc_type_loc_0003 ?to - loc_type_loc_0031 ?w - window_type_tw_00390)
    :precondition (and
      (at ?v ?from)
      (connects ?r ?from ?to)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
      (not (location-blocked ?from))
      (not (location-blocked ?to))
    )
    :effect (and
      (not (at ?v ?from))
      (at ?v ?to)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-macro_0001-tw_00420
    :parameters (?v - vehicle ?r - road_type_macro_0001 ?from - loc_type_loc_0003 ?to - loc_type_loc_0031 ?w - window_type_tw_00420)
    :precondition (and
      (at ?v ?from)
      (connects ?r ?from ?to)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
      (not (location-blocked ?from))
      (not (location-blocked ?to))
    )
    :effect (and
      (not (at ?v ?from))
      (at ?v ?to)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-macro_0001-tw_00450
    :parameters (?v - vehicle ?r - road_type_macro_0001 ?from - loc_type_loc_0003 ?to - loc_type_loc_0031 ?w - window_type_tw_00450)
    :precondition (and
      (at ?v ?from)
      (connects ?r ?from ?to)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
      (not (location-blocked ?from))
      (not (location-blocked ?to))
    )
    :effect (and
      (not (at ?v ?from))
      (at ?v ?to)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-macro_0002-tw_00000
    :parameters (?v - vehicle ?r - road_type_macro_0002 ?from - loc_type_loc_0011 ?to - loc_type_loc_0039 ?w - window_type_tw_00000)
    :precondition (and
      (at ?v ?from)
      (connects ?r ?from ?to)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
      (not (location-blocked ?from))
      (not (location-blocked ?to))
    )
    :effect (and
      (not (at ?v ?from))
      (at ?v ?to)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-macro_0002-tw_00030
    :parameters (?v - vehicle ?r - road_type_macro_0002 ?from - loc_type_loc_0011 ?to - loc_type_loc_0039 ?w - window_type_tw_00030)
    :precondition (and
      (at ?v ?from)
      (connects ?r ?from ?to)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
      (not (location-blocked ?from))
      (not (location-blocked ?to))
    )
    :effect (and
      (not (at ?v ?from))
      (at ?v ?to)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-macro_0002-tw_00060
    :parameters (?v - vehicle ?r - road_type_macro_0002 ?from - loc_type_loc_0011 ?to - loc_type_loc_0039 ?w - window_type_tw_00060)
    :precondition (and
      (at ?v ?from)
      (connects ?r ?from ?to)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
      (not (location-blocked ?from))
      (not (location-blocked ?to))
    )
    :effect (and
      (not (at ?v ?from))
      (at ?v ?to)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-macro_0002-tw_00090
    :parameters (?v - vehicle ?r - road_type_macro_0002 ?from - loc_type_loc_0011 ?to - loc_type_loc_0039 ?w - window_type_tw_00090)
    :precondition (and
      (at ?v ?from)
      (connects ?r ?from ?to)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
      (not (location-blocked ?from))
      (not (location-blocked ?to))
    )
    :effect (and
      (not (at ?v ?from))
      (at ?v ?to)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-macro_0002-tw_00120
    :parameters (?v - vehicle ?r - road_type_macro_0002 ?from - loc_type_loc_0011 ?to - loc_type_loc_0039 ?w - window_type_tw_00120)
    :precondition (and
      (at ?v ?from)
      (connects ?r ?from ?to)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
      (not (location-blocked ?from))
      (not (location-blocked ?to))
    )
    :effect (and
      (not (at ?v ?from))
      (at ?v ?to)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-macro_0002-tw_00150
    :parameters (?v - vehicle ?r - road_type_macro_0002 ?from - loc_type_loc_0011 ?to - loc_type_loc_0039 ?w - window_type_tw_00150)
    :precondition (and
      (at ?v ?from)
      (connects ?r ?from ?to)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
      (not (location-blocked ?from))
      (not (location-blocked ?to))
    )
    :effect (and
      (not (at ?v ?from))
      (at ?v ?to)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-macro_0002-tw_00180
    :parameters (?v - vehicle ?r - road_type_macro_0002 ?from - loc_type_loc_0011 ?to - loc_type_loc_0039 ?w - window_type_tw_00180)
    :precondition (and
      (at ?v ?from)
      (connects ?r ?from ?to)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
      (not (location-blocked ?from))
      (not (location-blocked ?to))
    )
    :effect (and
      (not (at ?v ?from))
      (at ?v ?to)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-macro_0002-tw_00210
    :parameters (?v - vehicle ?r - road_type_macro_0002 ?from - loc_type_loc_0011 ?to - loc_type_loc_0039 ?w - window_type_tw_00210)
    :precondition (and
      (at ?v ?from)
      (connects ?r ?from ?to)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
      (not (location-blocked ?from))
      (not (location-blocked ?to))
    )
    :effect (and
      (not (at ?v ?from))
      (at ?v ?to)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-macro_0002-tw_00240
    :parameters (?v - vehicle ?r - road_type_macro_0002 ?from - loc_type_loc_0011 ?to - loc_type_loc_0039 ?w - window_type_tw_00240)
    :precondition (and
      (at ?v ?from)
      (connects ?r ?from ?to)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
      (not (location-blocked ?from))
      (not (location-blocked ?to))
    )
    :effect (and
      (not (at ?v ?from))
      (at ?v ?to)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-macro_0002-tw_00270
    :parameters (?v - vehicle ?r - road_type_macro_0002 ?from - loc_type_loc_0011 ?to - loc_type_loc_0039 ?w - window_type_tw_00270)
    :precondition (and
      (at ?v ?from)
      (connects ?r ?from ?to)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
      (not (location-blocked ?from))
      (not (location-blocked ?to))
    )
    :effect (and
      (not (at ?v ?from))
      (at ?v ?to)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-macro_0002-tw_00300
    :parameters (?v - vehicle ?r - road_type_macro_0002 ?from - loc_type_loc_0011 ?to - loc_type_loc_0039 ?w - window_type_tw_00300)
    :precondition (and
      (at ?v ?from)
      (connects ?r ?from ?to)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
      (not (location-blocked ?from))
      (not (location-blocked ?to))
    )
    :effect (and
      (not (at ?v ?from))
      (at ?v ?to)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-macro_0002-tw_00330
    :parameters (?v - vehicle ?r - road_type_macro_0002 ?from - loc_type_loc_0011 ?to - loc_type_loc_0039 ?w - window_type_tw_00330)
    :precondition (and
      (at ?v ?from)
      (connects ?r ?from ?to)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
      (not (location-blocked ?from))
      (not (location-blocked ?to))
    )
    :effect (and
      (not (at ?v ?from))
      (at ?v ?to)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-macro_0002-tw_00360
    :parameters (?v - vehicle ?r - road_type_macro_0002 ?from - loc_type_loc_0011 ?to - loc_type_loc_0039 ?w - window_type_tw_00360)
    :precondition (and
      (at ?v ?from)
      (connects ?r ?from ?to)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
      (not (location-blocked ?from))
      (not (location-blocked ?to))
    )
    :effect (and
      (not (at ?v ?from))
      (at ?v ?to)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-macro_0002-tw_00390
    :parameters (?v - vehicle ?r - road_type_macro_0002 ?from - loc_type_loc_0011 ?to - loc_type_loc_0039 ?w - window_type_tw_00390)
    :precondition (and
      (at ?v ?from)
      (connects ?r ?from ?to)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
      (not (location-blocked ?from))
      (not (location-blocked ?to))
    )
    :effect (and
      (not (at ?v ?from))
      (at ?v ?to)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-macro_0002-tw_00420
    :parameters (?v - vehicle ?r - road_type_macro_0002 ?from - loc_type_loc_0011 ?to - loc_type_loc_0039 ?w - window_type_tw_00420)
    :precondition (and
      (at ?v ?from)
      (connects ?r ?from ?to)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
      (not (location-blocked ?from))
      (not (location-blocked ?to))
    )
    :effect (and
      (not (at ?v ?from))
      (at ?v ?to)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-macro_0002-tw_00450
    :parameters (?v - vehicle ?r - road_type_macro_0002 ?from - loc_type_loc_0011 ?to - loc_type_loc_0039 ?w - window_type_tw_00450)
    :precondition (and
      (at ?v ?from)
      (connects ?r ?from ?to)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
      (not (location-blocked ?from))
      (not (location-blocked ?to))
    )
    :effect (and
      (not (at ?v ?from))
      (at ?v ?to)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-macro_0003-tw_00000
    :parameters (?v - vehicle ?r - road_type_macro_0003 ?from - loc_type_loc_0031 ?to - loc_type_loc_0003 ?w - window_type_tw_00000)
    :precondition (and
      (at ?v ?from)
      (connects ?r ?from ?to)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
      (not (location-blocked ?from))
      (not (location-blocked ?to))
    )
    :effect (and
      (not (at ?v ?from))
      (at ?v ?to)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-macro_0003-tw_00030
    :parameters (?v - vehicle ?r - road_type_macro_0003 ?from - loc_type_loc_0031 ?to - loc_type_loc_0003 ?w - window_type_tw_00030)
    :precondition (and
      (at ?v ?from)
      (connects ?r ?from ?to)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
      (not (location-blocked ?from))
      (not (location-blocked ?to))
    )
    :effect (and
      (not (at ?v ?from))
      (at ?v ?to)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-macro_0003-tw_00060
    :parameters (?v - vehicle ?r - road_type_macro_0003 ?from - loc_type_loc_0031 ?to - loc_type_loc_0003 ?w - window_type_tw_00060)
    :precondition (and
      (at ?v ?from)
      (connects ?r ?from ?to)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
      (not (location-blocked ?from))
      (not (location-blocked ?to))
    )
    :effect (and
      (not (at ?v ?from))
      (at ?v ?to)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-macro_0003-tw_00090
    :parameters (?v - vehicle ?r - road_type_macro_0003 ?from - loc_type_loc_0031 ?to - loc_type_loc_0003 ?w - window_type_tw_00090)
    :precondition (and
      (at ?v ?from)
      (connects ?r ?from ?to)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
      (not (location-blocked ?from))
      (not (location-blocked ?to))
    )
    :effect (and
      (not (at ?v ?from))
      (at ?v ?to)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-macro_0003-tw_00120
    :parameters (?v - vehicle ?r - road_type_macro_0003 ?from - loc_type_loc_0031 ?to - loc_type_loc_0003 ?w - window_type_tw_00120)
    :precondition (and
      (at ?v ?from)
      (connects ?r ?from ?to)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
      (not (location-blocked ?from))
      (not (location-blocked ?to))
    )
    :effect (and
      (not (at ?v ?from))
      (at ?v ?to)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-macro_0003-tw_00150
    :parameters (?v - vehicle ?r - road_type_macro_0003 ?from - loc_type_loc_0031 ?to - loc_type_loc_0003 ?w - window_type_tw_00150)
    :precondition (and
      (at ?v ?from)
      (connects ?r ?from ?to)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
      (not (location-blocked ?from))
      (not (location-blocked ?to))
    )
    :effect (and
      (not (at ?v ?from))
      (at ?v ?to)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-macro_0003-tw_00180
    :parameters (?v - vehicle ?r - road_type_macro_0003 ?from - loc_type_loc_0031 ?to - loc_type_loc_0003 ?w - window_type_tw_00180)
    :precondition (and
      (at ?v ?from)
      (connects ?r ?from ?to)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
      (not (location-blocked ?from))
      (not (location-blocked ?to))
    )
    :effect (and
      (not (at ?v ?from))
      (at ?v ?to)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-macro_0003-tw_00210
    :parameters (?v - vehicle ?r - road_type_macro_0003 ?from - loc_type_loc_0031 ?to - loc_type_loc_0003 ?w - window_type_tw_00210)
    :precondition (and
      (at ?v ?from)
      (connects ?r ?from ?to)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
      (not (location-blocked ?from))
      (not (location-blocked ?to))
    )
    :effect (and
      (not (at ?v ?from))
      (at ?v ?to)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-macro_0003-tw_00240
    :parameters (?v - vehicle ?r - road_type_macro_0003 ?from - loc_type_loc_0031 ?to - loc_type_loc_0003 ?w - window_type_tw_00240)
    :precondition (and
      (at ?v ?from)
      (connects ?r ?from ?to)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
      (not (location-blocked ?from))
      (not (location-blocked ?to))
    )
    :effect (and
      (not (at ?v ?from))
      (at ?v ?to)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-macro_0003-tw_00270
    :parameters (?v - vehicle ?r - road_type_macro_0003 ?from - loc_type_loc_0031 ?to - loc_type_loc_0003 ?w - window_type_tw_00270)
    :precondition (and
      (at ?v ?from)
      (connects ?r ?from ?to)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
      (not (location-blocked ?from))
      (not (location-blocked ?to))
    )
    :effect (and
      (not (at ?v ?from))
      (at ?v ?to)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-macro_0003-tw_00300
    :parameters (?v - vehicle ?r - road_type_macro_0003 ?from - loc_type_loc_0031 ?to - loc_type_loc_0003 ?w - window_type_tw_00300)
    :precondition (and
      (at ?v ?from)
      (connects ?r ?from ?to)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
      (not (location-blocked ?from))
      (not (location-blocked ?to))
    )
    :effect (and
      (not (at ?v ?from))
      (at ?v ?to)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-macro_0003-tw_00330
    :parameters (?v - vehicle ?r - road_type_macro_0003 ?from - loc_type_loc_0031 ?to - loc_type_loc_0003 ?w - window_type_tw_00330)
    :precondition (and
      (at ?v ?from)
      (connects ?r ?from ?to)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
      (not (location-blocked ?from))
      (not (location-blocked ?to))
    )
    :effect (and
      (not (at ?v ?from))
      (at ?v ?to)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-macro_0003-tw_00360
    :parameters (?v - vehicle ?r - road_type_macro_0003 ?from - loc_type_loc_0031 ?to - loc_type_loc_0003 ?w - window_type_tw_00360)
    :precondition (and
      (at ?v ?from)
      (connects ?r ?from ?to)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
      (not (location-blocked ?from))
      (not (location-blocked ?to))
    )
    :effect (and
      (not (at ?v ?from))
      (at ?v ?to)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-macro_0003-tw_00390
    :parameters (?v - vehicle ?r - road_type_macro_0003 ?from - loc_type_loc_0031 ?to - loc_type_loc_0003 ?w - window_type_tw_00390)
    :precondition (and
      (at ?v ?from)
      (connects ?r ?from ?to)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
      (not (location-blocked ?from))
      (not (location-blocked ?to))
    )
    :effect (and
      (not (at ?v ?from))
      (at ?v ?to)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-macro_0003-tw_00420
    :parameters (?v - vehicle ?r - road_type_macro_0003 ?from - loc_type_loc_0031 ?to - loc_type_loc_0003 ?w - window_type_tw_00420)
    :precondition (and
      (at ?v ?from)
      (connects ?r ?from ?to)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
      (not (location-blocked ?from))
      (not (location-blocked ?to))
    )
    :effect (and
      (not (at ?v ?from))
      (at ?v ?to)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-macro_0003-tw_00450
    :parameters (?v - vehicle ?r - road_type_macro_0003 ?from - loc_type_loc_0031 ?to - loc_type_loc_0003 ?w - window_type_tw_00450)
    :precondition (and
      (at ?v ?from)
      (connects ?r ?from ?to)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
      (not (location-blocked ?from))
      (not (location-blocked ?to))
    )
    :effect (and
      (not (at ?v ?from))
      (at ?v ?to)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-macro_0004-tw_00000
    :parameters (?v - vehicle ?r - road_type_macro_0004 ?from - loc_type_loc_0036 ?to - loc_type_loc_0031 ?w - window_type_tw_00000)
    :precondition (and
      (at ?v ?from)
      (connects ?r ?from ?to)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
      (not (location-blocked ?from))
      (not (location-blocked ?to))
    )
    :effect (and
      (not (at ?v ?from))
      (at ?v ?to)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-macro_0004-tw_00030
    :parameters (?v - vehicle ?r - road_type_macro_0004 ?from - loc_type_loc_0036 ?to - loc_type_loc_0031 ?w - window_type_tw_00030)
    :precondition (and
      (at ?v ?from)
      (connects ?r ?from ?to)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
      (not (location-blocked ?from))
      (not (location-blocked ?to))
    )
    :effect (and
      (not (at ?v ?from))
      (at ?v ?to)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-macro_0004-tw_00060
    :parameters (?v - vehicle ?r - road_type_macro_0004 ?from - loc_type_loc_0036 ?to - loc_type_loc_0031 ?w - window_type_tw_00060)
    :precondition (and
      (at ?v ?from)
      (connects ?r ?from ?to)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
      (not (location-blocked ?from))
      (not (location-blocked ?to))
    )
    :effect (and
      (not (at ?v ?from))
      (at ?v ?to)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-macro_0004-tw_00090
    :parameters (?v - vehicle ?r - road_type_macro_0004 ?from - loc_type_loc_0036 ?to - loc_type_loc_0031 ?w - window_type_tw_00090)
    :precondition (and
      (at ?v ?from)
      (connects ?r ?from ?to)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
      (not (location-blocked ?from))
      (not (location-blocked ?to))
    )
    :effect (and
      (not (at ?v ?from))
      (at ?v ?to)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-macro_0004-tw_00120
    :parameters (?v - vehicle ?r - road_type_macro_0004 ?from - loc_type_loc_0036 ?to - loc_type_loc_0031 ?w - window_type_tw_00120)
    :precondition (and
      (at ?v ?from)
      (connects ?r ?from ?to)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
      (not (location-blocked ?from))
      (not (location-blocked ?to))
    )
    :effect (and
      (not (at ?v ?from))
      (at ?v ?to)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-macro_0004-tw_00150
    :parameters (?v - vehicle ?r - road_type_macro_0004 ?from - loc_type_loc_0036 ?to - loc_type_loc_0031 ?w - window_type_tw_00150)
    :precondition (and
      (at ?v ?from)
      (connects ?r ?from ?to)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
      (not (location-blocked ?from))
      (not (location-blocked ?to))
    )
    :effect (and
      (not (at ?v ?from))
      (at ?v ?to)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-macro_0004-tw_00180
    :parameters (?v - vehicle ?r - road_type_macro_0004 ?from - loc_type_loc_0036 ?to - loc_type_loc_0031 ?w - window_type_tw_00180)
    :precondition (and
      (at ?v ?from)
      (connects ?r ?from ?to)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
      (not (location-blocked ?from))
      (not (location-blocked ?to))
    )
    :effect (and
      (not (at ?v ?from))
      (at ?v ?to)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-macro_0004-tw_00210
    :parameters (?v - vehicle ?r - road_type_macro_0004 ?from - loc_type_loc_0036 ?to - loc_type_loc_0031 ?w - window_type_tw_00210)
    :precondition (and
      (at ?v ?from)
      (connects ?r ?from ?to)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
      (not (location-blocked ?from))
      (not (location-blocked ?to))
    )
    :effect (and
      (not (at ?v ?from))
      (at ?v ?to)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-macro_0004-tw_00240
    :parameters (?v - vehicle ?r - road_type_macro_0004 ?from - loc_type_loc_0036 ?to - loc_type_loc_0031 ?w - window_type_tw_00240)
    :precondition (and
      (at ?v ?from)
      (connects ?r ?from ?to)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
      (not (location-blocked ?from))
      (not (location-blocked ?to))
    )
    :effect (and
      (not (at ?v ?from))
      (at ?v ?to)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-macro_0004-tw_00270
    :parameters (?v - vehicle ?r - road_type_macro_0004 ?from - loc_type_loc_0036 ?to - loc_type_loc_0031 ?w - window_type_tw_00270)
    :precondition (and
      (at ?v ?from)
      (connects ?r ?from ?to)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
      (not (location-blocked ?from))
      (not (location-blocked ?to))
    )
    :effect (and
      (not (at ?v ?from))
      (at ?v ?to)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-macro_0004-tw_00300
    :parameters (?v - vehicle ?r - road_type_macro_0004 ?from - loc_type_loc_0036 ?to - loc_type_loc_0031 ?w - window_type_tw_00300)
    :precondition (and
      (at ?v ?from)
      (connects ?r ?from ?to)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
      (not (location-blocked ?from))
      (not (location-blocked ?to))
    )
    :effect (and
      (not (at ?v ?from))
      (at ?v ?to)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-macro_0004-tw_00330
    :parameters (?v - vehicle ?r - road_type_macro_0004 ?from - loc_type_loc_0036 ?to - loc_type_loc_0031 ?w - window_type_tw_00330)
    :precondition (and
      (at ?v ?from)
      (connects ?r ?from ?to)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
      (not (location-blocked ?from))
      (not (location-blocked ?to))
    )
    :effect (and
      (not (at ?v ?from))
      (at ?v ?to)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-macro_0004-tw_00360
    :parameters (?v - vehicle ?r - road_type_macro_0004 ?from - loc_type_loc_0036 ?to - loc_type_loc_0031 ?w - window_type_tw_00360)
    :precondition (and
      (at ?v ?from)
      (connects ?r ?from ?to)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
      (not (location-blocked ?from))
      (not (location-blocked ?to))
    )
    :effect (and
      (not (at ?v ?from))
      (at ?v ?to)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-macro_0004-tw_00390
    :parameters (?v - vehicle ?r - road_type_macro_0004 ?from - loc_type_loc_0036 ?to - loc_type_loc_0031 ?w - window_type_tw_00390)
    :precondition (and
      (at ?v ?from)
      (connects ?r ?from ?to)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
      (not (location-blocked ?from))
      (not (location-blocked ?to))
    )
    :effect (and
      (not (at ?v ?from))
      (at ?v ?to)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-macro_0004-tw_00420
    :parameters (?v - vehicle ?r - road_type_macro_0004 ?from - loc_type_loc_0036 ?to - loc_type_loc_0031 ?w - window_type_tw_00420)
    :precondition (and
      (at ?v ?from)
      (connects ?r ?from ?to)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
      (not (location-blocked ?from))
      (not (location-blocked ?to))
    )
    :effect (and
      (not (at ?v ?from))
      (at ?v ?to)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-macro_0004-tw_00450
    :parameters (?v - vehicle ?r - road_type_macro_0004 ?from - loc_type_loc_0036 ?to - loc_type_loc_0031 ?w - window_type_tw_00450)
    :precondition (and
      (at ?v ?from)
      (connects ?r ?from ?to)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
      (not (location-blocked ?from))
      (not (location-blocked ?to))
    )
    :effect (and
      (not (at ?v ?from))
      (at ?v ?to)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-macro_0005-tw_00000
    :parameters (?v - vehicle ?r - road_type_macro_0005 ?from - loc_type_loc_0037 ?to - loc_type_loc_0072 ?w - window_type_tw_00000)
    :precondition (and
      (at ?v ?from)
      (connects ?r ?from ?to)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
      (not (location-blocked ?from))
      (not (location-blocked ?to))
    )
    :effect (and
      (not (at ?v ?from))
      (at ?v ?to)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-macro_0005-tw_00030
    :parameters (?v - vehicle ?r - road_type_macro_0005 ?from - loc_type_loc_0037 ?to - loc_type_loc_0072 ?w - window_type_tw_00030)
    :precondition (and
      (at ?v ?from)
      (connects ?r ?from ?to)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
      (not (location-blocked ?from))
      (not (location-blocked ?to))
    )
    :effect (and
      (not (at ?v ?from))
      (at ?v ?to)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-macro_0005-tw_00060
    :parameters (?v - vehicle ?r - road_type_macro_0005 ?from - loc_type_loc_0037 ?to - loc_type_loc_0072 ?w - window_type_tw_00060)
    :precondition (and
      (at ?v ?from)
      (connects ?r ?from ?to)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
      (not (location-blocked ?from))
      (not (location-blocked ?to))
    )
    :effect (and
      (not (at ?v ?from))
      (at ?v ?to)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-macro_0005-tw_00090
    :parameters (?v - vehicle ?r - road_type_macro_0005 ?from - loc_type_loc_0037 ?to - loc_type_loc_0072 ?w - window_type_tw_00090)
    :precondition (and
      (at ?v ?from)
      (connects ?r ?from ?to)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
      (not (location-blocked ?from))
      (not (location-blocked ?to))
    )
    :effect (and
      (not (at ?v ?from))
      (at ?v ?to)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-macro_0005-tw_00120
    :parameters (?v - vehicle ?r - road_type_macro_0005 ?from - loc_type_loc_0037 ?to - loc_type_loc_0072 ?w - window_type_tw_00120)
    :precondition (and
      (at ?v ?from)
      (connects ?r ?from ?to)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
      (not (location-blocked ?from))
      (not (location-blocked ?to))
    )
    :effect (and
      (not (at ?v ?from))
      (at ?v ?to)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-macro_0005-tw_00150
    :parameters (?v - vehicle ?r - road_type_macro_0005 ?from - loc_type_loc_0037 ?to - loc_type_loc_0072 ?w - window_type_tw_00150)
    :precondition (and
      (at ?v ?from)
      (connects ?r ?from ?to)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
      (not (location-blocked ?from))
      (not (location-blocked ?to))
    )
    :effect (and
      (not (at ?v ?from))
      (at ?v ?to)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-macro_0005-tw_00180
    :parameters (?v - vehicle ?r - road_type_macro_0005 ?from - loc_type_loc_0037 ?to - loc_type_loc_0072 ?w - window_type_tw_00180)
    :precondition (and
      (at ?v ?from)
      (connects ?r ?from ?to)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
      (not (location-blocked ?from))
      (not (location-blocked ?to))
    )
    :effect (and
      (not (at ?v ?from))
      (at ?v ?to)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-macro_0005-tw_00210
    :parameters (?v - vehicle ?r - road_type_macro_0005 ?from - loc_type_loc_0037 ?to - loc_type_loc_0072 ?w - window_type_tw_00210)
    :precondition (and
      (at ?v ?from)
      (connects ?r ?from ?to)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
      (not (location-blocked ?from))
      (not (location-blocked ?to))
    )
    :effect (and
      (not (at ?v ?from))
      (at ?v ?to)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-macro_0005-tw_00240
    :parameters (?v - vehicle ?r - road_type_macro_0005 ?from - loc_type_loc_0037 ?to - loc_type_loc_0072 ?w - window_type_tw_00240)
    :precondition (and
      (at ?v ?from)
      (connects ?r ?from ?to)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
      (not (location-blocked ?from))
      (not (location-blocked ?to))
    )
    :effect (and
      (not (at ?v ?from))
      (at ?v ?to)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-macro_0005-tw_00270
    :parameters (?v - vehicle ?r - road_type_macro_0005 ?from - loc_type_loc_0037 ?to - loc_type_loc_0072 ?w - window_type_tw_00270)
    :precondition (and
      (at ?v ?from)
      (connects ?r ?from ?to)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
      (not (location-blocked ?from))
      (not (location-blocked ?to))
    )
    :effect (and
      (not (at ?v ?from))
      (at ?v ?to)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-macro_0005-tw_00300
    :parameters (?v - vehicle ?r - road_type_macro_0005 ?from - loc_type_loc_0037 ?to - loc_type_loc_0072 ?w - window_type_tw_00300)
    :precondition (and
      (at ?v ?from)
      (connects ?r ?from ?to)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
      (not (location-blocked ?from))
      (not (location-blocked ?to))
    )
    :effect (and
      (not (at ?v ?from))
      (at ?v ?to)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-macro_0005-tw_00330
    :parameters (?v - vehicle ?r - road_type_macro_0005 ?from - loc_type_loc_0037 ?to - loc_type_loc_0072 ?w - window_type_tw_00330)
    :precondition (and
      (at ?v ?from)
      (connects ?r ?from ?to)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
      (not (location-blocked ?from))
      (not (location-blocked ?to))
    )
    :effect (and
      (not (at ?v ?from))
      (at ?v ?to)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-macro_0005-tw_00360
    :parameters (?v - vehicle ?r - road_type_macro_0005 ?from - loc_type_loc_0037 ?to - loc_type_loc_0072 ?w - window_type_tw_00360)
    :precondition (and
      (at ?v ?from)
      (connects ?r ?from ?to)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
      (not (location-blocked ?from))
      (not (location-blocked ?to))
    )
    :effect (and
      (not (at ?v ?from))
      (at ?v ?to)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-macro_0005-tw_00390
    :parameters (?v - vehicle ?r - road_type_macro_0005 ?from - loc_type_loc_0037 ?to - loc_type_loc_0072 ?w - window_type_tw_00390)
    :precondition (and
      (at ?v ?from)
      (connects ?r ?from ?to)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
      (not (location-blocked ?from))
      (not (location-blocked ?to))
    )
    :effect (and
      (not (at ?v ?from))
      (at ?v ?to)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-macro_0005-tw_00420
    :parameters (?v - vehicle ?r - road_type_macro_0005 ?from - loc_type_loc_0037 ?to - loc_type_loc_0072 ?w - window_type_tw_00420)
    :precondition (and
      (at ?v ?from)
      (connects ?r ?from ?to)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
      (not (location-blocked ?from))
      (not (location-blocked ?to))
    )
    :effect (and
      (not (at ?v ?from))
      (at ?v ?to)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-macro_0005-tw_00450
    :parameters (?v - vehicle ?r - road_type_macro_0005 ?from - loc_type_loc_0037 ?to - loc_type_loc_0072 ?w - window_type_tw_00450)
    :precondition (and
      (at ?v ?from)
      (connects ?r ?from ?to)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
      (not (location-blocked ?from))
      (not (location-blocked ?to))
    )
    :effect (and
      (not (at ?v ?from))
      (at ?v ?to)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-macro_0006-tw_00000
    :parameters (?v - vehicle ?r - road_type_macro_0006 ?from - loc_type_loc_0047 ?to - loc_type_loc_0018 ?w - window_type_tw_00000)
    :precondition (and
      (at ?v ?from)
      (connects ?r ?from ?to)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
      (not (location-blocked ?from))
      (not (location-blocked ?to))
    )
    :effect (and
      (not (at ?v ?from))
      (at ?v ?to)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-macro_0006-tw_00030
    :parameters (?v - vehicle ?r - road_type_macro_0006 ?from - loc_type_loc_0047 ?to - loc_type_loc_0018 ?w - window_type_tw_00030)
    :precondition (and
      (at ?v ?from)
      (connects ?r ?from ?to)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
      (not (location-blocked ?from))
      (not (location-blocked ?to))
    )
    :effect (and
      (not (at ?v ?from))
      (at ?v ?to)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-macro_0006-tw_00060
    :parameters (?v - vehicle ?r - road_type_macro_0006 ?from - loc_type_loc_0047 ?to - loc_type_loc_0018 ?w - window_type_tw_00060)
    :precondition (and
      (at ?v ?from)
      (connects ?r ?from ?to)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
      (not (location-blocked ?from))
      (not (location-blocked ?to))
    )
    :effect (and
      (not (at ?v ?from))
      (at ?v ?to)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-macro_0006-tw_00090
    :parameters (?v - vehicle ?r - road_type_macro_0006 ?from - loc_type_loc_0047 ?to - loc_type_loc_0018 ?w - window_type_tw_00090)
    :precondition (and
      (at ?v ?from)
      (connects ?r ?from ?to)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
      (not (location-blocked ?from))
      (not (location-blocked ?to))
    )
    :effect (and
      (not (at ?v ?from))
      (at ?v ?to)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-macro_0006-tw_00120
    :parameters (?v - vehicle ?r - road_type_macro_0006 ?from - loc_type_loc_0047 ?to - loc_type_loc_0018 ?w - window_type_tw_00120)
    :precondition (and
      (at ?v ?from)
      (connects ?r ?from ?to)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
      (not (location-blocked ?from))
      (not (location-blocked ?to))
    )
    :effect (and
      (not (at ?v ?from))
      (at ?v ?to)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-macro_0006-tw_00150
    :parameters (?v - vehicle ?r - road_type_macro_0006 ?from - loc_type_loc_0047 ?to - loc_type_loc_0018 ?w - window_type_tw_00150)
    :precondition (and
      (at ?v ?from)
      (connects ?r ?from ?to)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
      (not (location-blocked ?from))
      (not (location-blocked ?to))
    )
    :effect (and
      (not (at ?v ?from))
      (at ?v ?to)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-macro_0006-tw_00180
    :parameters (?v - vehicle ?r - road_type_macro_0006 ?from - loc_type_loc_0047 ?to - loc_type_loc_0018 ?w - window_type_tw_00180)
    :precondition (and
      (at ?v ?from)
      (connects ?r ?from ?to)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
      (not (location-blocked ?from))
      (not (location-blocked ?to))
    )
    :effect (and
      (not (at ?v ?from))
      (at ?v ?to)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-macro_0006-tw_00210
    :parameters (?v - vehicle ?r - road_type_macro_0006 ?from - loc_type_loc_0047 ?to - loc_type_loc_0018 ?w - window_type_tw_00210)
    :precondition (and
      (at ?v ?from)
      (connects ?r ?from ?to)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
      (not (location-blocked ?from))
      (not (location-blocked ?to))
    )
    :effect (and
      (not (at ?v ?from))
      (at ?v ?to)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-macro_0006-tw_00240
    :parameters (?v - vehicle ?r - road_type_macro_0006 ?from - loc_type_loc_0047 ?to - loc_type_loc_0018 ?w - window_type_tw_00240)
    :precondition (and
      (at ?v ?from)
      (connects ?r ?from ?to)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
      (not (location-blocked ?from))
      (not (location-blocked ?to))
    )
    :effect (and
      (not (at ?v ?from))
      (at ?v ?to)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-macro_0006-tw_00270
    :parameters (?v - vehicle ?r - road_type_macro_0006 ?from - loc_type_loc_0047 ?to - loc_type_loc_0018 ?w - window_type_tw_00270)
    :precondition (and
      (at ?v ?from)
      (connects ?r ?from ?to)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
      (not (location-blocked ?from))
      (not (location-blocked ?to))
    )
    :effect (and
      (not (at ?v ?from))
      (at ?v ?to)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-macro_0006-tw_00300
    :parameters (?v - vehicle ?r - road_type_macro_0006 ?from - loc_type_loc_0047 ?to - loc_type_loc_0018 ?w - window_type_tw_00300)
    :precondition (and
      (at ?v ?from)
      (connects ?r ?from ?to)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
      (not (location-blocked ?from))
      (not (location-blocked ?to))
    )
    :effect (and
      (not (at ?v ?from))
      (at ?v ?to)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-macro_0006-tw_00330
    :parameters (?v - vehicle ?r - road_type_macro_0006 ?from - loc_type_loc_0047 ?to - loc_type_loc_0018 ?w - window_type_tw_00330)
    :precondition (and
      (at ?v ?from)
      (connects ?r ?from ?to)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
      (not (location-blocked ?from))
      (not (location-blocked ?to))
    )
    :effect (and
      (not (at ?v ?from))
      (at ?v ?to)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-macro_0006-tw_00360
    :parameters (?v - vehicle ?r - road_type_macro_0006 ?from - loc_type_loc_0047 ?to - loc_type_loc_0018 ?w - window_type_tw_00360)
    :precondition (and
      (at ?v ?from)
      (connects ?r ?from ?to)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
      (not (location-blocked ?from))
      (not (location-blocked ?to))
    )
    :effect (and
      (not (at ?v ?from))
      (at ?v ?to)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-macro_0006-tw_00390
    :parameters (?v - vehicle ?r - road_type_macro_0006 ?from - loc_type_loc_0047 ?to - loc_type_loc_0018 ?w - window_type_tw_00390)
    :precondition (and
      (at ?v ?from)
      (connects ?r ?from ?to)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
      (not (location-blocked ?from))
      (not (location-blocked ?to))
    )
    :effect (and
      (not (at ?v ?from))
      (at ?v ?to)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-macro_0006-tw_00420
    :parameters (?v - vehicle ?r - road_type_macro_0006 ?from - loc_type_loc_0047 ?to - loc_type_loc_0018 ?w - window_type_tw_00420)
    :precondition (and
      (at ?v ?from)
      (connects ?r ?from ?to)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
      (not (location-blocked ?from))
      (not (location-blocked ?to))
    )
    :effect (and
      (not (at ?v ?from))
      (at ?v ?to)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-macro_0006-tw_00450
    :parameters (?v - vehicle ?r - road_type_macro_0006 ?from - loc_type_loc_0047 ?to - loc_type_loc_0018 ?w - window_type_tw_00450)
    :precondition (and
      (at ?v ?from)
      (connects ?r ?from ?to)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
      (not (location-blocked ?from))
      (not (location-blocked ?to))
    )
    :effect (and
      (not (at ?v ?from))
      (at ?v ?to)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-macro_0007-tw_00000
    :parameters (?v - vehicle ?r - road_type_macro_0007 ?from - loc_type_loc_0050 ?to - loc_type_loc_0038 ?w - window_type_tw_00000)
    :precondition (and
      (at ?v ?from)
      (connects ?r ?from ?to)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
      (not (location-blocked ?from))
      (not (location-blocked ?to))
    )
    :effect (and
      (not (at ?v ?from))
      (at ?v ?to)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-macro_0007-tw_00030
    :parameters (?v - vehicle ?r - road_type_macro_0007 ?from - loc_type_loc_0050 ?to - loc_type_loc_0038 ?w - window_type_tw_00030)
    :precondition (and
      (at ?v ?from)
      (connects ?r ?from ?to)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
      (not (location-blocked ?from))
      (not (location-blocked ?to))
    )
    :effect (and
      (not (at ?v ?from))
      (at ?v ?to)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-macro_0007-tw_00060
    :parameters (?v - vehicle ?r - road_type_macro_0007 ?from - loc_type_loc_0050 ?to - loc_type_loc_0038 ?w - window_type_tw_00060)
    :precondition (and
      (at ?v ?from)
      (connects ?r ?from ?to)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
      (not (location-blocked ?from))
      (not (location-blocked ?to))
    )
    :effect (and
      (not (at ?v ?from))
      (at ?v ?to)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-macro_0007-tw_00090
    :parameters (?v - vehicle ?r - road_type_macro_0007 ?from - loc_type_loc_0050 ?to - loc_type_loc_0038 ?w - window_type_tw_00090)
    :precondition (and
      (at ?v ?from)
      (connects ?r ?from ?to)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
      (not (location-blocked ?from))
      (not (location-blocked ?to))
    )
    :effect (and
      (not (at ?v ?from))
      (at ?v ?to)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-macro_0007-tw_00120
    :parameters (?v - vehicle ?r - road_type_macro_0007 ?from - loc_type_loc_0050 ?to - loc_type_loc_0038 ?w - window_type_tw_00120)
    :precondition (and
      (at ?v ?from)
      (connects ?r ?from ?to)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
      (not (location-blocked ?from))
      (not (location-blocked ?to))
    )
    :effect (and
      (not (at ?v ?from))
      (at ?v ?to)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-macro_0007-tw_00150
    :parameters (?v - vehicle ?r - road_type_macro_0007 ?from - loc_type_loc_0050 ?to - loc_type_loc_0038 ?w - window_type_tw_00150)
    :precondition (and
      (at ?v ?from)
      (connects ?r ?from ?to)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
      (not (location-blocked ?from))
      (not (location-blocked ?to))
    )
    :effect (and
      (not (at ?v ?from))
      (at ?v ?to)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-macro_0007-tw_00180
    :parameters (?v - vehicle ?r - road_type_macro_0007 ?from - loc_type_loc_0050 ?to - loc_type_loc_0038 ?w - window_type_tw_00180)
    :precondition (and
      (at ?v ?from)
      (connects ?r ?from ?to)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
      (not (location-blocked ?from))
      (not (location-blocked ?to))
    )
    :effect (and
      (not (at ?v ?from))
      (at ?v ?to)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-macro_0007-tw_00210
    :parameters (?v - vehicle ?r - road_type_macro_0007 ?from - loc_type_loc_0050 ?to - loc_type_loc_0038 ?w - window_type_tw_00210)
    :precondition (and
      (at ?v ?from)
      (connects ?r ?from ?to)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
      (not (location-blocked ?from))
      (not (location-blocked ?to))
    )
    :effect (and
      (not (at ?v ?from))
      (at ?v ?to)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-macro_0007-tw_00240
    :parameters (?v - vehicle ?r - road_type_macro_0007 ?from - loc_type_loc_0050 ?to - loc_type_loc_0038 ?w - window_type_tw_00240)
    :precondition (and
      (at ?v ?from)
      (connects ?r ?from ?to)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
      (not (location-blocked ?from))
      (not (location-blocked ?to))
    )
    :effect (and
      (not (at ?v ?from))
      (at ?v ?to)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-macro_0007-tw_00270
    :parameters (?v - vehicle ?r - road_type_macro_0007 ?from - loc_type_loc_0050 ?to - loc_type_loc_0038 ?w - window_type_tw_00270)
    :precondition (and
      (at ?v ?from)
      (connects ?r ?from ?to)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
      (not (location-blocked ?from))
      (not (location-blocked ?to))
    )
    :effect (and
      (not (at ?v ?from))
      (at ?v ?to)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-macro_0007-tw_00300
    :parameters (?v - vehicle ?r - road_type_macro_0007 ?from - loc_type_loc_0050 ?to - loc_type_loc_0038 ?w - window_type_tw_00300)
    :precondition (and
      (at ?v ?from)
      (connects ?r ?from ?to)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
      (not (location-blocked ?from))
      (not (location-blocked ?to))
    )
    :effect (and
      (not (at ?v ?from))
      (at ?v ?to)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-macro_0007-tw_00330
    :parameters (?v - vehicle ?r - road_type_macro_0007 ?from - loc_type_loc_0050 ?to - loc_type_loc_0038 ?w - window_type_tw_00330)
    :precondition (and
      (at ?v ?from)
      (connects ?r ?from ?to)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
      (not (location-blocked ?from))
      (not (location-blocked ?to))
    )
    :effect (and
      (not (at ?v ?from))
      (at ?v ?to)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-macro_0007-tw_00360
    :parameters (?v - vehicle ?r - road_type_macro_0007 ?from - loc_type_loc_0050 ?to - loc_type_loc_0038 ?w - window_type_tw_00360)
    :precondition (and
      (at ?v ?from)
      (connects ?r ?from ?to)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
      (not (location-blocked ?from))
      (not (location-blocked ?to))
    )
    :effect (and
      (not (at ?v ?from))
      (at ?v ?to)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-macro_0007-tw_00390
    :parameters (?v - vehicle ?r - road_type_macro_0007 ?from - loc_type_loc_0050 ?to - loc_type_loc_0038 ?w - window_type_tw_00390)
    :precondition (and
      (at ?v ?from)
      (connects ?r ?from ?to)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
      (not (location-blocked ?from))
      (not (location-blocked ?to))
    )
    :effect (and
      (not (at ?v ?from))
      (at ?v ?to)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-macro_0007-tw_00420
    :parameters (?v - vehicle ?r - road_type_macro_0007 ?from - loc_type_loc_0050 ?to - loc_type_loc_0038 ?w - window_type_tw_00420)
    :precondition (and
      (at ?v ?from)
      (connects ?r ?from ?to)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
      (not (location-blocked ?from))
      (not (location-blocked ?to))
    )
    :effect (and
      (not (at ?v ?from))
      (at ?v ?to)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-macro_0007-tw_00450
    :parameters (?v - vehicle ?r - road_type_macro_0007 ?from - loc_type_loc_0050 ?to - loc_type_loc_0038 ?w - window_type_tw_00450)
    :precondition (and
      (at ?v ?from)
      (connects ?r ?from ?to)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
      (not (location-blocked ?from))
      (not (location-blocked ?to))
    )
    :effect (and
      (not (at ?v ?from))
      (at ?v ?to)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-macro_0008-tw_00000
    :parameters (?v - vehicle ?r - road_type_macro_0008 ?from - loc_type_loc_0054 ?to - loc_type_loc_0036 ?w - window_type_tw_00000)
    :precondition (and
      (at ?v ?from)
      (connects ?r ?from ?to)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
      (not (location-blocked ?from))
      (not (location-blocked ?to))
    )
    :effect (and
      (not (at ?v ?from))
      (at ?v ?to)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-macro_0008-tw_00030
    :parameters (?v - vehicle ?r - road_type_macro_0008 ?from - loc_type_loc_0054 ?to - loc_type_loc_0036 ?w - window_type_tw_00030)
    :precondition (and
      (at ?v ?from)
      (connects ?r ?from ?to)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
      (not (location-blocked ?from))
      (not (location-blocked ?to))
    )
    :effect (and
      (not (at ?v ?from))
      (at ?v ?to)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-macro_0008-tw_00060
    :parameters (?v - vehicle ?r - road_type_macro_0008 ?from - loc_type_loc_0054 ?to - loc_type_loc_0036 ?w - window_type_tw_00060)
    :precondition (and
      (at ?v ?from)
      (connects ?r ?from ?to)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
      (not (location-blocked ?from))
      (not (location-blocked ?to))
    )
    :effect (and
      (not (at ?v ?from))
      (at ?v ?to)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-macro_0008-tw_00090
    :parameters (?v - vehicle ?r - road_type_macro_0008 ?from - loc_type_loc_0054 ?to - loc_type_loc_0036 ?w - window_type_tw_00090)
    :precondition (and
      (at ?v ?from)
      (connects ?r ?from ?to)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
      (not (location-blocked ?from))
      (not (location-blocked ?to))
    )
    :effect (and
      (not (at ?v ?from))
      (at ?v ?to)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-macro_0008-tw_00120
    :parameters (?v - vehicle ?r - road_type_macro_0008 ?from - loc_type_loc_0054 ?to - loc_type_loc_0036 ?w - window_type_tw_00120)
    :precondition (and
      (at ?v ?from)
      (connects ?r ?from ?to)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
      (not (location-blocked ?from))
      (not (location-blocked ?to))
    )
    :effect (and
      (not (at ?v ?from))
      (at ?v ?to)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-macro_0008-tw_00150
    :parameters (?v - vehicle ?r - road_type_macro_0008 ?from - loc_type_loc_0054 ?to - loc_type_loc_0036 ?w - window_type_tw_00150)
    :precondition (and
      (at ?v ?from)
      (connects ?r ?from ?to)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
      (not (location-blocked ?from))
      (not (location-blocked ?to))
    )
    :effect (and
      (not (at ?v ?from))
      (at ?v ?to)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-macro_0008-tw_00180
    :parameters (?v - vehicle ?r - road_type_macro_0008 ?from - loc_type_loc_0054 ?to - loc_type_loc_0036 ?w - window_type_tw_00180)
    :precondition (and
      (at ?v ?from)
      (connects ?r ?from ?to)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
      (not (location-blocked ?from))
      (not (location-blocked ?to))
    )
    :effect (and
      (not (at ?v ?from))
      (at ?v ?to)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-macro_0008-tw_00210
    :parameters (?v - vehicle ?r - road_type_macro_0008 ?from - loc_type_loc_0054 ?to - loc_type_loc_0036 ?w - window_type_tw_00210)
    :precondition (and
      (at ?v ?from)
      (connects ?r ?from ?to)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
      (not (location-blocked ?from))
      (not (location-blocked ?to))
    )
    :effect (and
      (not (at ?v ?from))
      (at ?v ?to)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-macro_0008-tw_00240
    :parameters (?v - vehicle ?r - road_type_macro_0008 ?from - loc_type_loc_0054 ?to - loc_type_loc_0036 ?w - window_type_tw_00240)
    :precondition (and
      (at ?v ?from)
      (connects ?r ?from ?to)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
      (not (location-blocked ?from))
      (not (location-blocked ?to))
    )
    :effect (and
      (not (at ?v ?from))
      (at ?v ?to)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-macro_0008-tw_00270
    :parameters (?v - vehicle ?r - road_type_macro_0008 ?from - loc_type_loc_0054 ?to - loc_type_loc_0036 ?w - window_type_tw_00270)
    :precondition (and
      (at ?v ?from)
      (connects ?r ?from ?to)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
      (not (location-blocked ?from))
      (not (location-blocked ?to))
    )
    :effect (and
      (not (at ?v ?from))
      (at ?v ?to)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-macro_0008-tw_00300
    :parameters (?v - vehicle ?r - road_type_macro_0008 ?from - loc_type_loc_0054 ?to - loc_type_loc_0036 ?w - window_type_tw_00300)
    :precondition (and
      (at ?v ?from)
      (connects ?r ?from ?to)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
      (not (location-blocked ?from))
      (not (location-blocked ?to))
    )
    :effect (and
      (not (at ?v ?from))
      (at ?v ?to)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-macro_0008-tw_00330
    :parameters (?v - vehicle ?r - road_type_macro_0008 ?from - loc_type_loc_0054 ?to - loc_type_loc_0036 ?w - window_type_tw_00330)
    :precondition (and
      (at ?v ?from)
      (connects ?r ?from ?to)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
      (not (location-blocked ?from))
      (not (location-blocked ?to))
    )
    :effect (and
      (not (at ?v ?from))
      (at ?v ?to)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-macro_0008-tw_00360
    :parameters (?v - vehicle ?r - road_type_macro_0008 ?from - loc_type_loc_0054 ?to - loc_type_loc_0036 ?w - window_type_tw_00360)
    :precondition (and
      (at ?v ?from)
      (connects ?r ?from ?to)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
      (not (location-blocked ?from))
      (not (location-blocked ?to))
    )
    :effect (and
      (not (at ?v ?from))
      (at ?v ?to)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-macro_0008-tw_00390
    :parameters (?v - vehicle ?r - road_type_macro_0008 ?from - loc_type_loc_0054 ?to - loc_type_loc_0036 ?w - window_type_tw_00390)
    :precondition (and
      (at ?v ?from)
      (connects ?r ?from ?to)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
      (not (location-blocked ?from))
      (not (location-blocked ?to))
    )
    :effect (and
      (not (at ?v ?from))
      (at ?v ?to)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-macro_0008-tw_00420
    :parameters (?v - vehicle ?r - road_type_macro_0008 ?from - loc_type_loc_0054 ?to - loc_type_loc_0036 ?w - window_type_tw_00420)
    :precondition (and
      (at ?v ?from)
      (connects ?r ?from ?to)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
      (not (location-blocked ?from))
      (not (location-blocked ?to))
    )
    :effect (and
      (not (at ?v ?from))
      (at ?v ?to)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-macro_0008-tw_00450
    :parameters (?v - vehicle ?r - road_type_macro_0008 ?from - loc_type_loc_0054 ?to - loc_type_loc_0036 ?w - window_type_tw_00450)
    :precondition (and
      (at ?v ?from)
      (connects ?r ?from ?to)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
      (not (location-blocked ?from))
      (not (location-blocked ?to))
    )
    :effect (and
      (not (at ?v ?from))
      (at ?v ?to)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-macro_0009-tw_00000
    :parameters (?v - vehicle ?r - road_type_macro_0009 ?from - loc_type_loc_0054 ?to - loc_type_loc_0073 ?w - window_type_tw_00000)
    :precondition (and
      (at ?v ?from)
      (connects ?r ?from ?to)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
      (not (location-blocked ?from))
      (not (location-blocked ?to))
    )
    :effect (and
      (not (at ?v ?from))
      (at ?v ?to)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-macro_0009-tw_00030
    :parameters (?v - vehicle ?r - road_type_macro_0009 ?from - loc_type_loc_0054 ?to - loc_type_loc_0073 ?w - window_type_tw_00030)
    :precondition (and
      (at ?v ?from)
      (connects ?r ?from ?to)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
      (not (location-blocked ?from))
      (not (location-blocked ?to))
    )
    :effect (and
      (not (at ?v ?from))
      (at ?v ?to)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-macro_0009-tw_00060
    :parameters (?v - vehicle ?r - road_type_macro_0009 ?from - loc_type_loc_0054 ?to - loc_type_loc_0073 ?w - window_type_tw_00060)
    :precondition (and
      (at ?v ?from)
      (connects ?r ?from ?to)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
      (not (location-blocked ?from))
      (not (location-blocked ?to))
    )
    :effect (and
      (not (at ?v ?from))
      (at ?v ?to)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-macro_0009-tw_00090
    :parameters (?v - vehicle ?r - road_type_macro_0009 ?from - loc_type_loc_0054 ?to - loc_type_loc_0073 ?w - window_type_tw_00090)
    :precondition (and
      (at ?v ?from)
      (connects ?r ?from ?to)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
      (not (location-blocked ?from))
      (not (location-blocked ?to))
    )
    :effect (and
      (not (at ?v ?from))
      (at ?v ?to)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-macro_0009-tw_00120
    :parameters (?v - vehicle ?r - road_type_macro_0009 ?from - loc_type_loc_0054 ?to - loc_type_loc_0073 ?w - window_type_tw_00120)
    :precondition (and
      (at ?v ?from)
      (connects ?r ?from ?to)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
      (not (location-blocked ?from))
      (not (location-blocked ?to))
    )
    :effect (and
      (not (at ?v ?from))
      (at ?v ?to)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-macro_0009-tw_00150
    :parameters (?v - vehicle ?r - road_type_macro_0009 ?from - loc_type_loc_0054 ?to - loc_type_loc_0073 ?w - window_type_tw_00150)
    :precondition (and
      (at ?v ?from)
      (connects ?r ?from ?to)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
      (not (location-blocked ?from))
      (not (location-blocked ?to))
    )
    :effect (and
      (not (at ?v ?from))
      (at ?v ?to)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-macro_0009-tw_00180
    :parameters (?v - vehicle ?r - road_type_macro_0009 ?from - loc_type_loc_0054 ?to - loc_type_loc_0073 ?w - window_type_tw_00180)
    :precondition (and
      (at ?v ?from)
      (connects ?r ?from ?to)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
      (not (location-blocked ?from))
      (not (location-blocked ?to))
    )
    :effect (and
      (not (at ?v ?from))
      (at ?v ?to)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-macro_0009-tw_00210
    :parameters (?v - vehicle ?r - road_type_macro_0009 ?from - loc_type_loc_0054 ?to - loc_type_loc_0073 ?w - window_type_tw_00210)
    :precondition (and
      (at ?v ?from)
      (connects ?r ?from ?to)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
      (not (location-blocked ?from))
      (not (location-blocked ?to))
    )
    :effect (and
      (not (at ?v ?from))
      (at ?v ?to)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-macro_0009-tw_00240
    :parameters (?v - vehicle ?r - road_type_macro_0009 ?from - loc_type_loc_0054 ?to - loc_type_loc_0073 ?w - window_type_tw_00240)
    :precondition (and
      (at ?v ?from)
      (connects ?r ?from ?to)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
      (not (location-blocked ?from))
      (not (location-blocked ?to))
    )
    :effect (and
      (not (at ?v ?from))
      (at ?v ?to)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-macro_0009-tw_00270
    :parameters (?v - vehicle ?r - road_type_macro_0009 ?from - loc_type_loc_0054 ?to - loc_type_loc_0073 ?w - window_type_tw_00270)
    :precondition (and
      (at ?v ?from)
      (connects ?r ?from ?to)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
      (not (location-blocked ?from))
      (not (location-blocked ?to))
    )
    :effect (and
      (not (at ?v ?from))
      (at ?v ?to)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-macro_0009-tw_00300
    :parameters (?v - vehicle ?r - road_type_macro_0009 ?from - loc_type_loc_0054 ?to - loc_type_loc_0073 ?w - window_type_tw_00300)
    :precondition (and
      (at ?v ?from)
      (connects ?r ?from ?to)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
      (not (location-blocked ?from))
      (not (location-blocked ?to))
    )
    :effect (and
      (not (at ?v ?from))
      (at ?v ?to)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-macro_0009-tw_00330
    :parameters (?v - vehicle ?r - road_type_macro_0009 ?from - loc_type_loc_0054 ?to - loc_type_loc_0073 ?w - window_type_tw_00330)
    :precondition (and
      (at ?v ?from)
      (connects ?r ?from ?to)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
      (not (location-blocked ?from))
      (not (location-blocked ?to))
    )
    :effect (and
      (not (at ?v ?from))
      (at ?v ?to)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-macro_0009-tw_00360
    :parameters (?v - vehicle ?r - road_type_macro_0009 ?from - loc_type_loc_0054 ?to - loc_type_loc_0073 ?w - window_type_tw_00360)
    :precondition (and
      (at ?v ?from)
      (connects ?r ?from ?to)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
      (not (location-blocked ?from))
      (not (location-blocked ?to))
    )
    :effect (and
      (not (at ?v ?from))
      (at ?v ?to)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-macro_0009-tw_00390
    :parameters (?v - vehicle ?r - road_type_macro_0009 ?from - loc_type_loc_0054 ?to - loc_type_loc_0073 ?w - window_type_tw_00390)
    :precondition (and
      (at ?v ?from)
      (connects ?r ?from ?to)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
      (not (location-blocked ?from))
      (not (location-blocked ?to))
    )
    :effect (and
      (not (at ?v ?from))
      (at ?v ?to)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-macro_0009-tw_00420
    :parameters (?v - vehicle ?r - road_type_macro_0009 ?from - loc_type_loc_0054 ?to - loc_type_loc_0073 ?w - window_type_tw_00420)
    :precondition (and
      (at ?v ?from)
      (connects ?r ?from ?to)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
      (not (location-blocked ?from))
      (not (location-blocked ?to))
    )
    :effect (and
      (not (at ?v ?from))
      (at ?v ?to)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-macro_0009-tw_00450
    :parameters (?v - vehicle ?r - road_type_macro_0009 ?from - loc_type_loc_0054 ?to - loc_type_loc_0073 ?w - window_type_tw_00450)
    :precondition (and
      (at ?v ?from)
      (connects ?r ?from ?to)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
      (not (location-blocked ?from))
      (not (location-blocked ?to))
    )
    :effect (and
      (not (at ?v ?from))
      (at ?v ?to)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-macro_0010-tw_00000
    :parameters (?v - vehicle ?r - road_type_macro_0010 ?from - loc_type_loc_0056 ?to - loc_type_loc_0060 ?w - window_type_tw_00000)
    :precondition (and
      (at ?v ?from)
      (connects ?r ?from ?to)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
      (not (location-blocked ?from))
      (not (location-blocked ?to))
    )
    :effect (and
      (not (at ?v ?from))
      (at ?v ?to)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-macro_0010-tw_00030
    :parameters (?v - vehicle ?r - road_type_macro_0010 ?from - loc_type_loc_0056 ?to - loc_type_loc_0060 ?w - window_type_tw_00030)
    :precondition (and
      (at ?v ?from)
      (connects ?r ?from ?to)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
      (not (location-blocked ?from))
      (not (location-blocked ?to))
    )
    :effect (and
      (not (at ?v ?from))
      (at ?v ?to)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-macro_0010-tw_00060
    :parameters (?v - vehicle ?r - road_type_macro_0010 ?from - loc_type_loc_0056 ?to - loc_type_loc_0060 ?w - window_type_tw_00060)
    :precondition (and
      (at ?v ?from)
      (connects ?r ?from ?to)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
      (not (location-blocked ?from))
      (not (location-blocked ?to))
    )
    :effect (and
      (not (at ?v ?from))
      (at ?v ?to)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-macro_0010-tw_00090
    :parameters (?v - vehicle ?r - road_type_macro_0010 ?from - loc_type_loc_0056 ?to - loc_type_loc_0060 ?w - window_type_tw_00090)
    :precondition (and
      (at ?v ?from)
      (connects ?r ?from ?to)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
      (not (location-blocked ?from))
      (not (location-blocked ?to))
    )
    :effect (and
      (not (at ?v ?from))
      (at ?v ?to)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-macro_0010-tw_00120
    :parameters (?v - vehicle ?r - road_type_macro_0010 ?from - loc_type_loc_0056 ?to - loc_type_loc_0060 ?w - window_type_tw_00120)
    :precondition (and
      (at ?v ?from)
      (connects ?r ?from ?to)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
      (not (location-blocked ?from))
      (not (location-blocked ?to))
    )
    :effect (and
      (not (at ?v ?from))
      (at ?v ?to)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-macro_0010-tw_00150
    :parameters (?v - vehicle ?r - road_type_macro_0010 ?from - loc_type_loc_0056 ?to - loc_type_loc_0060 ?w - window_type_tw_00150)
    :precondition (and
      (at ?v ?from)
      (connects ?r ?from ?to)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
      (not (location-blocked ?from))
      (not (location-blocked ?to))
    )
    :effect (and
      (not (at ?v ?from))
      (at ?v ?to)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-macro_0010-tw_00180
    :parameters (?v - vehicle ?r - road_type_macro_0010 ?from - loc_type_loc_0056 ?to - loc_type_loc_0060 ?w - window_type_tw_00180)
    :precondition (and
      (at ?v ?from)
      (connects ?r ?from ?to)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
      (not (location-blocked ?from))
      (not (location-blocked ?to))
    )
    :effect (and
      (not (at ?v ?from))
      (at ?v ?to)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-macro_0010-tw_00210
    :parameters (?v - vehicle ?r - road_type_macro_0010 ?from - loc_type_loc_0056 ?to - loc_type_loc_0060 ?w - window_type_tw_00210)
    :precondition (and
      (at ?v ?from)
      (connects ?r ?from ?to)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
      (not (location-blocked ?from))
      (not (location-blocked ?to))
    )
    :effect (and
      (not (at ?v ?from))
      (at ?v ?to)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-macro_0010-tw_00240
    :parameters (?v - vehicle ?r - road_type_macro_0010 ?from - loc_type_loc_0056 ?to - loc_type_loc_0060 ?w - window_type_tw_00240)
    :precondition (and
      (at ?v ?from)
      (connects ?r ?from ?to)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
      (not (location-blocked ?from))
      (not (location-blocked ?to))
    )
    :effect (and
      (not (at ?v ?from))
      (at ?v ?to)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-macro_0010-tw_00270
    :parameters (?v - vehicle ?r - road_type_macro_0010 ?from - loc_type_loc_0056 ?to - loc_type_loc_0060 ?w - window_type_tw_00270)
    :precondition (and
      (at ?v ?from)
      (connects ?r ?from ?to)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
      (not (location-blocked ?from))
      (not (location-blocked ?to))
    )
    :effect (and
      (not (at ?v ?from))
      (at ?v ?to)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-macro_0010-tw_00300
    :parameters (?v - vehicle ?r - road_type_macro_0010 ?from - loc_type_loc_0056 ?to - loc_type_loc_0060 ?w - window_type_tw_00300)
    :precondition (and
      (at ?v ?from)
      (connects ?r ?from ?to)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
      (not (location-blocked ?from))
      (not (location-blocked ?to))
    )
    :effect (and
      (not (at ?v ?from))
      (at ?v ?to)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-macro_0010-tw_00330
    :parameters (?v - vehicle ?r - road_type_macro_0010 ?from - loc_type_loc_0056 ?to - loc_type_loc_0060 ?w - window_type_tw_00330)
    :precondition (and
      (at ?v ?from)
      (connects ?r ?from ?to)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
      (not (location-blocked ?from))
      (not (location-blocked ?to))
    )
    :effect (and
      (not (at ?v ?from))
      (at ?v ?to)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-macro_0010-tw_00360
    :parameters (?v - vehicle ?r - road_type_macro_0010 ?from - loc_type_loc_0056 ?to - loc_type_loc_0060 ?w - window_type_tw_00360)
    :precondition (and
      (at ?v ?from)
      (connects ?r ?from ?to)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
      (not (location-blocked ?from))
      (not (location-blocked ?to))
    )
    :effect (and
      (not (at ?v ?from))
      (at ?v ?to)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-macro_0010-tw_00390
    :parameters (?v - vehicle ?r - road_type_macro_0010 ?from - loc_type_loc_0056 ?to - loc_type_loc_0060 ?w - window_type_tw_00390)
    :precondition (and
      (at ?v ?from)
      (connects ?r ?from ?to)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
      (not (location-blocked ?from))
      (not (location-blocked ?to))
    )
    :effect (and
      (not (at ?v ?from))
      (at ?v ?to)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-macro_0010-tw_00420
    :parameters (?v - vehicle ?r - road_type_macro_0010 ?from - loc_type_loc_0056 ?to - loc_type_loc_0060 ?w - window_type_tw_00420)
    :precondition (and
      (at ?v ?from)
      (connects ?r ?from ?to)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
      (not (location-blocked ?from))
      (not (location-blocked ?to))
    )
    :effect (and
      (not (at ?v ?from))
      (at ?v ?to)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-macro_0010-tw_00450
    :parameters (?v - vehicle ?r - road_type_macro_0010 ?from - loc_type_loc_0056 ?to - loc_type_loc_0060 ?w - window_type_tw_00450)
    :precondition (and
      (at ?v ?from)
      (connects ?r ?from ?to)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
      (not (location-blocked ?from))
      (not (location-blocked ?to))
    )
    :effect (and
      (not (at ?v ?from))
      (at ?v ?to)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-macro_0011-tw_00000
    :parameters (?v - vehicle ?r - road_type_macro_0011 ?from - loc_type_loc_0058 ?to - loc_type_loc_0026 ?w - window_type_tw_00000)
    :precondition (and
      (at ?v ?from)
      (connects ?r ?from ?to)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
      (not (location-blocked ?from))
      (not (location-blocked ?to))
    )
    :effect (and
      (not (at ?v ?from))
      (at ?v ?to)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-macro_0011-tw_00030
    :parameters (?v - vehicle ?r - road_type_macro_0011 ?from - loc_type_loc_0058 ?to - loc_type_loc_0026 ?w - window_type_tw_00030)
    :precondition (and
      (at ?v ?from)
      (connects ?r ?from ?to)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
      (not (location-blocked ?from))
      (not (location-blocked ?to))
    )
    :effect (and
      (not (at ?v ?from))
      (at ?v ?to)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-macro_0011-tw_00060
    :parameters (?v - vehicle ?r - road_type_macro_0011 ?from - loc_type_loc_0058 ?to - loc_type_loc_0026 ?w - window_type_tw_00060)
    :precondition (and
      (at ?v ?from)
      (connects ?r ?from ?to)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
      (not (location-blocked ?from))
      (not (location-blocked ?to))
    )
    :effect (and
      (not (at ?v ?from))
      (at ?v ?to)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-macro_0011-tw_00090
    :parameters (?v - vehicle ?r - road_type_macro_0011 ?from - loc_type_loc_0058 ?to - loc_type_loc_0026 ?w - window_type_tw_00090)
    :precondition (and
      (at ?v ?from)
      (connects ?r ?from ?to)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
      (not (location-blocked ?from))
      (not (location-blocked ?to))
    )
    :effect (and
      (not (at ?v ?from))
      (at ?v ?to)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-macro_0011-tw_00120
    :parameters (?v - vehicle ?r - road_type_macro_0011 ?from - loc_type_loc_0058 ?to - loc_type_loc_0026 ?w - window_type_tw_00120)
    :precondition (and
      (at ?v ?from)
      (connects ?r ?from ?to)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
      (not (location-blocked ?from))
      (not (location-blocked ?to))
    )
    :effect (and
      (not (at ?v ?from))
      (at ?v ?to)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-macro_0011-tw_00150
    :parameters (?v - vehicle ?r - road_type_macro_0011 ?from - loc_type_loc_0058 ?to - loc_type_loc_0026 ?w - window_type_tw_00150)
    :precondition (and
      (at ?v ?from)
      (connects ?r ?from ?to)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
      (not (location-blocked ?from))
      (not (location-blocked ?to))
    )
    :effect (and
      (not (at ?v ?from))
      (at ?v ?to)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-macro_0011-tw_00180
    :parameters (?v - vehicle ?r - road_type_macro_0011 ?from - loc_type_loc_0058 ?to - loc_type_loc_0026 ?w - window_type_tw_00180)
    :precondition (and
      (at ?v ?from)
      (connects ?r ?from ?to)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
      (not (location-blocked ?from))
      (not (location-blocked ?to))
    )
    :effect (and
      (not (at ?v ?from))
      (at ?v ?to)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-macro_0011-tw_00210
    :parameters (?v - vehicle ?r - road_type_macro_0011 ?from - loc_type_loc_0058 ?to - loc_type_loc_0026 ?w - window_type_tw_00210)
    :precondition (and
      (at ?v ?from)
      (connects ?r ?from ?to)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
      (not (location-blocked ?from))
      (not (location-blocked ?to))
    )
    :effect (and
      (not (at ?v ?from))
      (at ?v ?to)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-macro_0011-tw_00240
    :parameters (?v - vehicle ?r - road_type_macro_0011 ?from - loc_type_loc_0058 ?to - loc_type_loc_0026 ?w - window_type_tw_00240)
    :precondition (and
      (at ?v ?from)
      (connects ?r ?from ?to)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
      (not (location-blocked ?from))
      (not (location-blocked ?to))
    )
    :effect (and
      (not (at ?v ?from))
      (at ?v ?to)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-macro_0011-tw_00270
    :parameters (?v - vehicle ?r - road_type_macro_0011 ?from - loc_type_loc_0058 ?to - loc_type_loc_0026 ?w - window_type_tw_00270)
    :precondition (and
      (at ?v ?from)
      (connects ?r ?from ?to)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
      (not (location-blocked ?from))
      (not (location-blocked ?to))
    )
    :effect (and
      (not (at ?v ?from))
      (at ?v ?to)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-macro_0011-tw_00300
    :parameters (?v - vehicle ?r - road_type_macro_0011 ?from - loc_type_loc_0058 ?to - loc_type_loc_0026 ?w - window_type_tw_00300)
    :precondition (and
      (at ?v ?from)
      (connects ?r ?from ?to)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
      (not (location-blocked ?from))
      (not (location-blocked ?to))
    )
    :effect (and
      (not (at ?v ?from))
      (at ?v ?to)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-macro_0011-tw_00330
    :parameters (?v - vehicle ?r - road_type_macro_0011 ?from - loc_type_loc_0058 ?to - loc_type_loc_0026 ?w - window_type_tw_00330)
    :precondition (and
      (at ?v ?from)
      (connects ?r ?from ?to)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
      (not (location-blocked ?from))
      (not (location-blocked ?to))
    )
    :effect (and
      (not (at ?v ?from))
      (at ?v ?to)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-macro_0011-tw_00360
    :parameters (?v - vehicle ?r - road_type_macro_0011 ?from - loc_type_loc_0058 ?to - loc_type_loc_0026 ?w - window_type_tw_00360)
    :precondition (and
      (at ?v ?from)
      (connects ?r ?from ?to)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
      (not (location-blocked ?from))
      (not (location-blocked ?to))
    )
    :effect (and
      (not (at ?v ?from))
      (at ?v ?to)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-macro_0011-tw_00390
    :parameters (?v - vehicle ?r - road_type_macro_0011 ?from - loc_type_loc_0058 ?to - loc_type_loc_0026 ?w - window_type_tw_00390)
    :precondition (and
      (at ?v ?from)
      (connects ?r ?from ?to)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
      (not (location-blocked ?from))
      (not (location-blocked ?to))
    )
    :effect (and
      (not (at ?v ?from))
      (at ?v ?to)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-macro_0011-tw_00420
    :parameters (?v - vehicle ?r - road_type_macro_0011 ?from - loc_type_loc_0058 ?to - loc_type_loc_0026 ?w - window_type_tw_00420)
    :precondition (and
      (at ?v ?from)
      (connects ?r ?from ?to)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
      (not (location-blocked ?from))
      (not (location-blocked ?to))
    )
    :effect (and
      (not (at ?v ?from))
      (at ?v ?to)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-macro_0011-tw_00450
    :parameters (?v - vehicle ?r - road_type_macro_0011 ?from - loc_type_loc_0058 ?to - loc_type_loc_0026 ?w - window_type_tw_00450)
    :precondition (and
      (at ?v ?from)
      (connects ?r ?from ?to)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
      (not (location-blocked ?from))
      (not (location-blocked ?to))
    )
    :effect (and
      (not (at ?v ?from))
      (at ?v ?to)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-macro_0012-tw_00000
    :parameters (?v - vehicle ?r - road_type_macro_0012 ?from - loc_type_loc_0062 ?to - loc_type_loc_0019 ?w - window_type_tw_00000)
    :precondition (and
      (at ?v ?from)
      (connects ?r ?from ?to)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
      (not (location-blocked ?from))
      (not (location-blocked ?to))
    )
    :effect (and
      (not (at ?v ?from))
      (at ?v ?to)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-macro_0012-tw_00030
    :parameters (?v - vehicle ?r - road_type_macro_0012 ?from - loc_type_loc_0062 ?to - loc_type_loc_0019 ?w - window_type_tw_00030)
    :precondition (and
      (at ?v ?from)
      (connects ?r ?from ?to)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
      (not (location-blocked ?from))
      (not (location-blocked ?to))
    )
    :effect (and
      (not (at ?v ?from))
      (at ?v ?to)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-macro_0012-tw_00060
    :parameters (?v - vehicle ?r - road_type_macro_0012 ?from - loc_type_loc_0062 ?to - loc_type_loc_0019 ?w - window_type_tw_00060)
    :precondition (and
      (at ?v ?from)
      (connects ?r ?from ?to)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
      (not (location-blocked ?from))
      (not (location-blocked ?to))
    )
    :effect (and
      (not (at ?v ?from))
      (at ?v ?to)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-macro_0012-tw_00090
    :parameters (?v - vehicle ?r - road_type_macro_0012 ?from - loc_type_loc_0062 ?to - loc_type_loc_0019 ?w - window_type_tw_00090)
    :precondition (and
      (at ?v ?from)
      (connects ?r ?from ?to)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
      (not (location-blocked ?from))
      (not (location-blocked ?to))
    )
    :effect (and
      (not (at ?v ?from))
      (at ?v ?to)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-macro_0012-tw_00120
    :parameters (?v - vehicle ?r - road_type_macro_0012 ?from - loc_type_loc_0062 ?to - loc_type_loc_0019 ?w - window_type_tw_00120)
    :precondition (and
      (at ?v ?from)
      (connects ?r ?from ?to)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
      (not (location-blocked ?from))
      (not (location-blocked ?to))
    )
    :effect (and
      (not (at ?v ?from))
      (at ?v ?to)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-macro_0012-tw_00150
    :parameters (?v - vehicle ?r - road_type_macro_0012 ?from - loc_type_loc_0062 ?to - loc_type_loc_0019 ?w - window_type_tw_00150)
    :precondition (and
      (at ?v ?from)
      (connects ?r ?from ?to)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
      (not (location-blocked ?from))
      (not (location-blocked ?to))
    )
    :effect (and
      (not (at ?v ?from))
      (at ?v ?to)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-macro_0012-tw_00180
    :parameters (?v - vehicle ?r - road_type_macro_0012 ?from - loc_type_loc_0062 ?to - loc_type_loc_0019 ?w - window_type_tw_00180)
    :precondition (and
      (at ?v ?from)
      (connects ?r ?from ?to)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
      (not (location-blocked ?from))
      (not (location-blocked ?to))
    )
    :effect (and
      (not (at ?v ?from))
      (at ?v ?to)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-macro_0012-tw_00210
    :parameters (?v - vehicle ?r - road_type_macro_0012 ?from - loc_type_loc_0062 ?to - loc_type_loc_0019 ?w - window_type_tw_00210)
    :precondition (and
      (at ?v ?from)
      (connects ?r ?from ?to)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
      (not (location-blocked ?from))
      (not (location-blocked ?to))
    )
    :effect (and
      (not (at ?v ?from))
      (at ?v ?to)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-macro_0012-tw_00240
    :parameters (?v - vehicle ?r - road_type_macro_0012 ?from - loc_type_loc_0062 ?to - loc_type_loc_0019 ?w - window_type_tw_00240)
    :precondition (and
      (at ?v ?from)
      (connects ?r ?from ?to)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
      (not (location-blocked ?from))
      (not (location-blocked ?to))
    )
    :effect (and
      (not (at ?v ?from))
      (at ?v ?to)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-macro_0012-tw_00270
    :parameters (?v - vehicle ?r - road_type_macro_0012 ?from - loc_type_loc_0062 ?to - loc_type_loc_0019 ?w - window_type_tw_00270)
    :precondition (and
      (at ?v ?from)
      (connects ?r ?from ?to)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
      (not (location-blocked ?from))
      (not (location-blocked ?to))
    )
    :effect (and
      (not (at ?v ?from))
      (at ?v ?to)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-macro_0012-tw_00300
    :parameters (?v - vehicle ?r - road_type_macro_0012 ?from - loc_type_loc_0062 ?to - loc_type_loc_0019 ?w - window_type_tw_00300)
    :precondition (and
      (at ?v ?from)
      (connects ?r ?from ?to)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
      (not (location-blocked ?from))
      (not (location-blocked ?to))
    )
    :effect (and
      (not (at ?v ?from))
      (at ?v ?to)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-macro_0012-tw_00330
    :parameters (?v - vehicle ?r - road_type_macro_0012 ?from - loc_type_loc_0062 ?to - loc_type_loc_0019 ?w - window_type_tw_00330)
    :precondition (and
      (at ?v ?from)
      (connects ?r ?from ?to)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
      (not (location-blocked ?from))
      (not (location-blocked ?to))
    )
    :effect (and
      (not (at ?v ?from))
      (at ?v ?to)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-macro_0012-tw_00360
    :parameters (?v - vehicle ?r - road_type_macro_0012 ?from - loc_type_loc_0062 ?to - loc_type_loc_0019 ?w - window_type_tw_00360)
    :precondition (and
      (at ?v ?from)
      (connects ?r ?from ?to)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
      (not (location-blocked ?from))
      (not (location-blocked ?to))
    )
    :effect (and
      (not (at ?v ?from))
      (at ?v ?to)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-macro_0012-tw_00390
    :parameters (?v - vehicle ?r - road_type_macro_0012 ?from - loc_type_loc_0062 ?to - loc_type_loc_0019 ?w - window_type_tw_00390)
    :precondition (and
      (at ?v ?from)
      (connects ?r ?from ?to)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
      (not (location-blocked ?from))
      (not (location-blocked ?to))
    )
    :effect (and
      (not (at ?v ?from))
      (at ?v ?to)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-macro_0012-tw_00420
    :parameters (?v - vehicle ?r - road_type_macro_0012 ?from - loc_type_loc_0062 ?to - loc_type_loc_0019 ?w - window_type_tw_00420)
    :precondition (and
      (at ?v ?from)
      (connects ?r ?from ?to)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
      (not (location-blocked ?from))
      (not (location-blocked ?to))
    )
    :effect (and
      (not (at ?v ?from))
      (at ?v ?to)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-macro_0012-tw_00450
    :parameters (?v - vehicle ?r - road_type_macro_0012 ?from - loc_type_loc_0062 ?to - loc_type_loc_0019 ?w - window_type_tw_00450)
    :precondition (and
      (at ?v ?from)
      (connects ?r ?from ?to)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
      (not (location-blocked ?from))
      (not (location-blocked ?to))
    )
    :effect (and
      (not (at ?v ?from))
      (at ?v ?to)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-macro_0013-tw_00000
    :parameters (?v - vehicle ?r - road_type_macro_0013 ?from - loc_type_loc_0064 ?to - loc_type_loc_0037 ?w - window_type_tw_00000)
    :precondition (and
      (at ?v ?from)
      (connects ?r ?from ?to)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
      (not (location-blocked ?from))
      (not (location-blocked ?to))
    )
    :effect (and
      (not (at ?v ?from))
      (at ?v ?to)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-macro_0013-tw_00030
    :parameters (?v - vehicle ?r - road_type_macro_0013 ?from - loc_type_loc_0064 ?to - loc_type_loc_0037 ?w - window_type_tw_00030)
    :precondition (and
      (at ?v ?from)
      (connects ?r ?from ?to)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
      (not (location-blocked ?from))
      (not (location-blocked ?to))
    )
    :effect (and
      (not (at ?v ?from))
      (at ?v ?to)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-macro_0013-tw_00060
    :parameters (?v - vehicle ?r - road_type_macro_0013 ?from - loc_type_loc_0064 ?to - loc_type_loc_0037 ?w - window_type_tw_00060)
    :precondition (and
      (at ?v ?from)
      (connects ?r ?from ?to)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
      (not (location-blocked ?from))
      (not (location-blocked ?to))
    )
    :effect (and
      (not (at ?v ?from))
      (at ?v ?to)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-macro_0013-tw_00090
    :parameters (?v - vehicle ?r - road_type_macro_0013 ?from - loc_type_loc_0064 ?to - loc_type_loc_0037 ?w - window_type_tw_00090)
    :precondition (and
      (at ?v ?from)
      (connects ?r ?from ?to)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
      (not (location-blocked ?from))
      (not (location-blocked ?to))
    )
    :effect (and
      (not (at ?v ?from))
      (at ?v ?to)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-macro_0013-tw_00120
    :parameters (?v - vehicle ?r - road_type_macro_0013 ?from - loc_type_loc_0064 ?to - loc_type_loc_0037 ?w - window_type_tw_00120)
    :precondition (and
      (at ?v ?from)
      (connects ?r ?from ?to)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
      (not (location-blocked ?from))
      (not (location-blocked ?to))
    )
    :effect (and
      (not (at ?v ?from))
      (at ?v ?to)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-macro_0013-tw_00150
    :parameters (?v - vehicle ?r - road_type_macro_0013 ?from - loc_type_loc_0064 ?to - loc_type_loc_0037 ?w - window_type_tw_00150)
    :precondition (and
      (at ?v ?from)
      (connects ?r ?from ?to)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
      (not (location-blocked ?from))
      (not (location-blocked ?to))
    )
    :effect (and
      (not (at ?v ?from))
      (at ?v ?to)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-macro_0013-tw_00180
    :parameters (?v - vehicle ?r - road_type_macro_0013 ?from - loc_type_loc_0064 ?to - loc_type_loc_0037 ?w - window_type_tw_00180)
    :precondition (and
      (at ?v ?from)
      (connects ?r ?from ?to)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
      (not (location-blocked ?from))
      (not (location-blocked ?to))
    )
    :effect (and
      (not (at ?v ?from))
      (at ?v ?to)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-macro_0013-tw_00210
    :parameters (?v - vehicle ?r - road_type_macro_0013 ?from - loc_type_loc_0064 ?to - loc_type_loc_0037 ?w - window_type_tw_00210)
    :precondition (and
      (at ?v ?from)
      (connects ?r ?from ?to)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
      (not (location-blocked ?from))
      (not (location-blocked ?to))
    )
    :effect (and
      (not (at ?v ?from))
      (at ?v ?to)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-macro_0013-tw_00240
    :parameters (?v - vehicle ?r - road_type_macro_0013 ?from - loc_type_loc_0064 ?to - loc_type_loc_0037 ?w - window_type_tw_00240)
    :precondition (and
      (at ?v ?from)
      (connects ?r ?from ?to)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
      (not (location-blocked ?from))
      (not (location-blocked ?to))
    )
    :effect (and
      (not (at ?v ?from))
      (at ?v ?to)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-macro_0013-tw_00270
    :parameters (?v - vehicle ?r - road_type_macro_0013 ?from - loc_type_loc_0064 ?to - loc_type_loc_0037 ?w - window_type_tw_00270)
    :precondition (and
      (at ?v ?from)
      (connects ?r ?from ?to)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
      (not (location-blocked ?from))
      (not (location-blocked ?to))
    )
    :effect (and
      (not (at ?v ?from))
      (at ?v ?to)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-macro_0013-tw_00300
    :parameters (?v - vehicle ?r - road_type_macro_0013 ?from - loc_type_loc_0064 ?to - loc_type_loc_0037 ?w - window_type_tw_00300)
    :precondition (and
      (at ?v ?from)
      (connects ?r ?from ?to)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
      (not (location-blocked ?from))
      (not (location-blocked ?to))
    )
    :effect (and
      (not (at ?v ?from))
      (at ?v ?to)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-macro_0013-tw_00330
    :parameters (?v - vehicle ?r - road_type_macro_0013 ?from - loc_type_loc_0064 ?to - loc_type_loc_0037 ?w - window_type_tw_00330)
    :precondition (and
      (at ?v ?from)
      (connects ?r ?from ?to)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
      (not (location-blocked ?from))
      (not (location-blocked ?to))
    )
    :effect (and
      (not (at ?v ?from))
      (at ?v ?to)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-macro_0013-tw_00360
    :parameters (?v - vehicle ?r - road_type_macro_0013 ?from - loc_type_loc_0064 ?to - loc_type_loc_0037 ?w - window_type_tw_00360)
    :precondition (and
      (at ?v ?from)
      (connects ?r ?from ?to)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
      (not (location-blocked ?from))
      (not (location-blocked ?to))
    )
    :effect (and
      (not (at ?v ?from))
      (at ?v ?to)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-macro_0013-tw_00390
    :parameters (?v - vehicle ?r - road_type_macro_0013 ?from - loc_type_loc_0064 ?to - loc_type_loc_0037 ?w - window_type_tw_00390)
    :precondition (and
      (at ?v ?from)
      (connects ?r ?from ?to)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
      (not (location-blocked ?from))
      (not (location-blocked ?to))
    )
    :effect (and
      (not (at ?v ?from))
      (at ?v ?to)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-macro_0013-tw_00420
    :parameters (?v - vehicle ?r - road_type_macro_0013 ?from - loc_type_loc_0064 ?to - loc_type_loc_0037 ?w - window_type_tw_00420)
    :precondition (and
      (at ?v ?from)
      (connects ?r ?from ?to)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
      (not (location-blocked ?from))
      (not (location-blocked ?to))
    )
    :effect (and
      (not (at ?v ?from))
      (at ?v ?to)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-macro_0013-tw_00450
    :parameters (?v - vehicle ?r - road_type_macro_0013 ?from - loc_type_loc_0064 ?to - loc_type_loc_0037 ?w - window_type_tw_00450)
    :precondition (and
      (at ?v ?from)
      (connects ?r ?from ?to)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
      (not (location-blocked ?from))
      (not (location-blocked ?to))
    )
    :effect (and
      (not (at ?v ?from))
      (at ?v ?to)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-macro_0014-tw_00000
    :parameters (?v - vehicle ?r - road_type_macro_0014 ?from - loc_type_loc_0081 ?to - loc_type_loc_0002 ?w - window_type_tw_00000)
    :precondition (and
      (at ?v ?from)
      (connects ?r ?from ?to)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
      (not (location-blocked ?from))
      (not (location-blocked ?to))
    )
    :effect (and
      (not (at ?v ?from))
      (at ?v ?to)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-macro_0014-tw_00030
    :parameters (?v - vehicle ?r - road_type_macro_0014 ?from - loc_type_loc_0081 ?to - loc_type_loc_0002 ?w - window_type_tw_00030)
    :precondition (and
      (at ?v ?from)
      (connects ?r ?from ?to)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
      (not (location-blocked ?from))
      (not (location-blocked ?to))
    )
    :effect (and
      (not (at ?v ?from))
      (at ?v ?to)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-macro_0014-tw_00060
    :parameters (?v - vehicle ?r - road_type_macro_0014 ?from - loc_type_loc_0081 ?to - loc_type_loc_0002 ?w - window_type_tw_00060)
    :precondition (and
      (at ?v ?from)
      (connects ?r ?from ?to)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
      (not (location-blocked ?from))
      (not (location-blocked ?to))
    )
    :effect (and
      (not (at ?v ?from))
      (at ?v ?to)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-macro_0014-tw_00090
    :parameters (?v - vehicle ?r - road_type_macro_0014 ?from - loc_type_loc_0081 ?to - loc_type_loc_0002 ?w - window_type_tw_00090)
    :precondition (and
      (at ?v ?from)
      (connects ?r ?from ?to)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
      (not (location-blocked ?from))
      (not (location-blocked ?to))
    )
    :effect (and
      (not (at ?v ?from))
      (at ?v ?to)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-macro_0014-tw_00120
    :parameters (?v - vehicle ?r - road_type_macro_0014 ?from - loc_type_loc_0081 ?to - loc_type_loc_0002 ?w - window_type_tw_00120)
    :precondition (and
      (at ?v ?from)
      (connects ?r ?from ?to)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
      (not (location-blocked ?from))
      (not (location-blocked ?to))
    )
    :effect (and
      (not (at ?v ?from))
      (at ?v ?to)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-macro_0014-tw_00150
    :parameters (?v - vehicle ?r - road_type_macro_0014 ?from - loc_type_loc_0081 ?to - loc_type_loc_0002 ?w - window_type_tw_00150)
    :precondition (and
      (at ?v ?from)
      (connects ?r ?from ?to)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
      (not (location-blocked ?from))
      (not (location-blocked ?to))
    )
    :effect (and
      (not (at ?v ?from))
      (at ?v ?to)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-macro_0014-tw_00180
    :parameters (?v - vehicle ?r - road_type_macro_0014 ?from - loc_type_loc_0081 ?to - loc_type_loc_0002 ?w - window_type_tw_00180)
    :precondition (and
      (at ?v ?from)
      (connects ?r ?from ?to)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
      (not (location-blocked ?from))
      (not (location-blocked ?to))
    )
    :effect (and
      (not (at ?v ?from))
      (at ?v ?to)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-macro_0014-tw_00210
    :parameters (?v - vehicle ?r - road_type_macro_0014 ?from - loc_type_loc_0081 ?to - loc_type_loc_0002 ?w - window_type_tw_00210)
    :precondition (and
      (at ?v ?from)
      (connects ?r ?from ?to)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
      (not (location-blocked ?from))
      (not (location-blocked ?to))
    )
    :effect (and
      (not (at ?v ?from))
      (at ?v ?to)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-macro_0014-tw_00240
    :parameters (?v - vehicle ?r - road_type_macro_0014 ?from - loc_type_loc_0081 ?to - loc_type_loc_0002 ?w - window_type_tw_00240)
    :precondition (and
      (at ?v ?from)
      (connects ?r ?from ?to)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
      (not (location-blocked ?from))
      (not (location-blocked ?to))
    )
    :effect (and
      (not (at ?v ?from))
      (at ?v ?to)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-macro_0014-tw_00270
    :parameters (?v - vehicle ?r - road_type_macro_0014 ?from - loc_type_loc_0081 ?to - loc_type_loc_0002 ?w - window_type_tw_00270)
    :precondition (and
      (at ?v ?from)
      (connects ?r ?from ?to)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
      (not (location-blocked ?from))
      (not (location-blocked ?to))
    )
    :effect (and
      (not (at ?v ?from))
      (at ?v ?to)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-macro_0014-tw_00300
    :parameters (?v - vehicle ?r - road_type_macro_0014 ?from - loc_type_loc_0081 ?to - loc_type_loc_0002 ?w - window_type_tw_00300)
    :precondition (and
      (at ?v ?from)
      (connects ?r ?from ?to)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
      (not (location-blocked ?from))
      (not (location-blocked ?to))
    )
    :effect (and
      (not (at ?v ?from))
      (at ?v ?to)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-macro_0014-tw_00330
    :parameters (?v - vehicle ?r - road_type_macro_0014 ?from - loc_type_loc_0081 ?to - loc_type_loc_0002 ?w - window_type_tw_00330)
    :precondition (and
      (at ?v ?from)
      (connects ?r ?from ?to)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
      (not (location-blocked ?from))
      (not (location-blocked ?to))
    )
    :effect (and
      (not (at ?v ?from))
      (at ?v ?to)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-macro_0014-tw_00360
    :parameters (?v - vehicle ?r - road_type_macro_0014 ?from - loc_type_loc_0081 ?to - loc_type_loc_0002 ?w - window_type_tw_00360)
    :precondition (and
      (at ?v ?from)
      (connects ?r ?from ?to)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
      (not (location-blocked ?from))
      (not (location-blocked ?to))
    )
    :effect (and
      (not (at ?v ?from))
      (at ?v ?to)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-macro_0014-tw_00390
    :parameters (?v - vehicle ?r - road_type_macro_0014 ?from - loc_type_loc_0081 ?to - loc_type_loc_0002 ?w - window_type_tw_00390)
    :precondition (and
      (at ?v ?from)
      (connects ?r ?from ?to)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
      (not (location-blocked ?from))
      (not (location-blocked ?to))
    )
    :effect (and
      (not (at ?v ?from))
      (at ?v ?to)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-macro_0014-tw_00420
    :parameters (?v - vehicle ?r - road_type_macro_0014 ?from - loc_type_loc_0081 ?to - loc_type_loc_0002 ?w - window_type_tw_00420)
    :precondition (and
      (at ?v ?from)
      (connects ?r ?from ?to)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
      (not (location-blocked ?from))
      (not (location-blocked ?to))
    )
    :effect (and
      (not (at ?v ?from))
      (at ?v ?to)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-macro_0014-tw_00450
    :parameters (?v - vehicle ?r - road_type_macro_0014 ?from - loc_type_loc_0081 ?to - loc_type_loc_0002 ?w - window_type_tw_00450)
    :precondition (and
      (at ?v ?from)
      (connects ?r ?from ?to)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
      (not (location-blocked ?from))
      (not (location-blocked ?to))
    )
    :effect (and
      (not (at ?v ?from))
      (at ?v ?to)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-macro_0015-tw_00000
    :parameters (?v - vehicle ?r - road_type_macro_0015 ?from - loc_type_loc_0083 ?to - loc_type_loc_0084 ?w - window_type_tw_00000)
    :precondition (and
      (at ?v ?from)
      (connects ?r ?from ?to)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
      (not (location-blocked ?from))
      (not (location-blocked ?to))
    )
    :effect (and
      (not (at ?v ?from))
      (at ?v ?to)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-macro_0015-tw_00030
    :parameters (?v - vehicle ?r - road_type_macro_0015 ?from - loc_type_loc_0083 ?to - loc_type_loc_0084 ?w - window_type_tw_00030)
    :precondition (and
      (at ?v ?from)
      (connects ?r ?from ?to)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
      (not (location-blocked ?from))
      (not (location-blocked ?to))
    )
    :effect (and
      (not (at ?v ?from))
      (at ?v ?to)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-macro_0015-tw_00060
    :parameters (?v - vehicle ?r - road_type_macro_0015 ?from - loc_type_loc_0083 ?to - loc_type_loc_0084 ?w - window_type_tw_00060)
    :precondition (and
      (at ?v ?from)
      (connects ?r ?from ?to)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
      (not (location-blocked ?from))
      (not (location-blocked ?to))
    )
    :effect (and
      (not (at ?v ?from))
      (at ?v ?to)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-macro_0015-tw_00090
    :parameters (?v - vehicle ?r - road_type_macro_0015 ?from - loc_type_loc_0083 ?to - loc_type_loc_0084 ?w - window_type_tw_00090)
    :precondition (and
      (at ?v ?from)
      (connects ?r ?from ?to)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
      (not (location-blocked ?from))
      (not (location-blocked ?to))
    )
    :effect (and
      (not (at ?v ?from))
      (at ?v ?to)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-macro_0015-tw_00120
    :parameters (?v - vehicle ?r - road_type_macro_0015 ?from - loc_type_loc_0083 ?to - loc_type_loc_0084 ?w - window_type_tw_00120)
    :precondition (and
      (at ?v ?from)
      (connects ?r ?from ?to)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
      (not (location-blocked ?from))
      (not (location-blocked ?to))
    )
    :effect (and
      (not (at ?v ?from))
      (at ?v ?to)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-macro_0015-tw_00150
    :parameters (?v - vehicle ?r - road_type_macro_0015 ?from - loc_type_loc_0083 ?to - loc_type_loc_0084 ?w - window_type_tw_00150)
    :precondition (and
      (at ?v ?from)
      (connects ?r ?from ?to)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
      (not (location-blocked ?from))
      (not (location-blocked ?to))
    )
    :effect (and
      (not (at ?v ?from))
      (at ?v ?to)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-macro_0015-tw_00180
    :parameters (?v - vehicle ?r - road_type_macro_0015 ?from - loc_type_loc_0083 ?to - loc_type_loc_0084 ?w - window_type_tw_00180)
    :precondition (and
      (at ?v ?from)
      (connects ?r ?from ?to)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
      (not (location-blocked ?from))
      (not (location-blocked ?to))
    )
    :effect (and
      (not (at ?v ?from))
      (at ?v ?to)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-macro_0015-tw_00210
    :parameters (?v - vehicle ?r - road_type_macro_0015 ?from - loc_type_loc_0083 ?to - loc_type_loc_0084 ?w - window_type_tw_00210)
    :precondition (and
      (at ?v ?from)
      (connects ?r ?from ?to)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
      (not (location-blocked ?from))
      (not (location-blocked ?to))
    )
    :effect (and
      (not (at ?v ?from))
      (at ?v ?to)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-macro_0015-tw_00240
    :parameters (?v - vehicle ?r - road_type_macro_0015 ?from - loc_type_loc_0083 ?to - loc_type_loc_0084 ?w - window_type_tw_00240)
    :precondition (and
      (at ?v ?from)
      (connects ?r ?from ?to)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
      (not (location-blocked ?from))
      (not (location-blocked ?to))
    )
    :effect (and
      (not (at ?v ?from))
      (at ?v ?to)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-macro_0015-tw_00270
    :parameters (?v - vehicle ?r - road_type_macro_0015 ?from - loc_type_loc_0083 ?to - loc_type_loc_0084 ?w - window_type_tw_00270)
    :precondition (and
      (at ?v ?from)
      (connects ?r ?from ?to)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
      (not (location-blocked ?from))
      (not (location-blocked ?to))
    )
    :effect (and
      (not (at ?v ?from))
      (at ?v ?to)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-macro_0015-tw_00300
    :parameters (?v - vehicle ?r - road_type_macro_0015 ?from - loc_type_loc_0083 ?to - loc_type_loc_0084 ?w - window_type_tw_00300)
    :precondition (and
      (at ?v ?from)
      (connects ?r ?from ?to)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
      (not (location-blocked ?from))
      (not (location-blocked ?to))
    )
    :effect (and
      (not (at ?v ?from))
      (at ?v ?to)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-macro_0015-tw_00330
    :parameters (?v - vehicle ?r - road_type_macro_0015 ?from - loc_type_loc_0083 ?to - loc_type_loc_0084 ?w - window_type_tw_00330)
    :precondition (and
      (at ?v ?from)
      (connects ?r ?from ?to)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
      (not (location-blocked ?from))
      (not (location-blocked ?to))
    )
    :effect (and
      (not (at ?v ?from))
      (at ?v ?to)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-macro_0015-tw_00360
    :parameters (?v - vehicle ?r - road_type_macro_0015 ?from - loc_type_loc_0083 ?to - loc_type_loc_0084 ?w - window_type_tw_00360)
    :precondition (and
      (at ?v ?from)
      (connects ?r ?from ?to)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
      (not (location-blocked ?from))
      (not (location-blocked ?to))
    )
    :effect (and
      (not (at ?v ?from))
      (at ?v ?to)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-macro_0015-tw_00390
    :parameters (?v - vehicle ?r - road_type_macro_0015 ?from - loc_type_loc_0083 ?to - loc_type_loc_0084 ?w - window_type_tw_00390)
    :precondition (and
      (at ?v ?from)
      (connects ?r ?from ?to)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
      (not (location-blocked ?from))
      (not (location-blocked ?to))
    )
    :effect (and
      (not (at ?v ?from))
      (at ?v ?to)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-macro_0015-tw_00420
    :parameters (?v - vehicle ?r - road_type_macro_0015 ?from - loc_type_loc_0083 ?to - loc_type_loc_0084 ?w - window_type_tw_00420)
    :precondition (and
      (at ?v ?from)
      (connects ?r ?from ?to)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
      (not (location-blocked ?from))
      (not (location-blocked ?to))
    )
    :effect (and
      (not (at ?v ?from))
      (at ?v ?to)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-macro_0015-tw_00450
    :parameters (?v - vehicle ?r - road_type_macro_0015 ?from - loc_type_loc_0083 ?to - loc_type_loc_0084 ?w - window_type_tw_00450)
    :precondition (and
      (at ?v ?from)
      (connects ?r ?from ?to)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
      (not (location-blocked ?from))
      (not (location-blocked ?to))
    )
    :effect (and
      (not (at ?v ?from))
      (at ?v ?to)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-macro_0016-tw_00000
    :parameters (?v - vehicle ?r - road_type_macro_0016 ?from - loc_type_loc_0094 ?to - loc_type_loc_0011 ?w - window_type_tw_00000)
    :precondition (and
      (at ?v ?from)
      (connects ?r ?from ?to)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
      (not (location-blocked ?from))
      (not (location-blocked ?to))
    )
    :effect (and
      (not (at ?v ?from))
      (at ?v ?to)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-macro_0016-tw_00030
    :parameters (?v - vehicle ?r - road_type_macro_0016 ?from - loc_type_loc_0094 ?to - loc_type_loc_0011 ?w - window_type_tw_00030)
    :precondition (and
      (at ?v ?from)
      (connects ?r ?from ?to)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
      (not (location-blocked ?from))
      (not (location-blocked ?to))
    )
    :effect (and
      (not (at ?v ?from))
      (at ?v ?to)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-macro_0016-tw_00060
    :parameters (?v - vehicle ?r - road_type_macro_0016 ?from - loc_type_loc_0094 ?to - loc_type_loc_0011 ?w - window_type_tw_00060)
    :precondition (and
      (at ?v ?from)
      (connects ?r ?from ?to)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
      (not (location-blocked ?from))
      (not (location-blocked ?to))
    )
    :effect (and
      (not (at ?v ?from))
      (at ?v ?to)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-macro_0016-tw_00090
    :parameters (?v - vehicle ?r - road_type_macro_0016 ?from - loc_type_loc_0094 ?to - loc_type_loc_0011 ?w - window_type_tw_00090)
    :precondition (and
      (at ?v ?from)
      (connects ?r ?from ?to)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
      (not (location-blocked ?from))
      (not (location-blocked ?to))
    )
    :effect (and
      (not (at ?v ?from))
      (at ?v ?to)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-macro_0016-tw_00120
    :parameters (?v - vehicle ?r - road_type_macro_0016 ?from - loc_type_loc_0094 ?to - loc_type_loc_0011 ?w - window_type_tw_00120)
    :precondition (and
      (at ?v ?from)
      (connects ?r ?from ?to)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
      (not (location-blocked ?from))
      (not (location-blocked ?to))
    )
    :effect (and
      (not (at ?v ?from))
      (at ?v ?to)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-macro_0016-tw_00150
    :parameters (?v - vehicle ?r - road_type_macro_0016 ?from - loc_type_loc_0094 ?to - loc_type_loc_0011 ?w - window_type_tw_00150)
    :precondition (and
      (at ?v ?from)
      (connects ?r ?from ?to)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
      (not (location-blocked ?from))
      (not (location-blocked ?to))
    )
    :effect (and
      (not (at ?v ?from))
      (at ?v ?to)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-macro_0016-tw_00180
    :parameters (?v - vehicle ?r - road_type_macro_0016 ?from - loc_type_loc_0094 ?to - loc_type_loc_0011 ?w - window_type_tw_00180)
    :precondition (and
      (at ?v ?from)
      (connects ?r ?from ?to)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
      (not (location-blocked ?from))
      (not (location-blocked ?to))
    )
    :effect (and
      (not (at ?v ?from))
      (at ?v ?to)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-macro_0016-tw_00210
    :parameters (?v - vehicle ?r - road_type_macro_0016 ?from - loc_type_loc_0094 ?to - loc_type_loc_0011 ?w - window_type_tw_00210)
    :precondition (and
      (at ?v ?from)
      (connects ?r ?from ?to)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
      (not (location-blocked ?from))
      (not (location-blocked ?to))
    )
    :effect (and
      (not (at ?v ?from))
      (at ?v ?to)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-macro_0016-tw_00240
    :parameters (?v - vehicle ?r - road_type_macro_0016 ?from - loc_type_loc_0094 ?to - loc_type_loc_0011 ?w - window_type_tw_00240)
    :precondition (and
      (at ?v ?from)
      (connects ?r ?from ?to)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
      (not (location-blocked ?from))
      (not (location-blocked ?to))
    )
    :effect (and
      (not (at ?v ?from))
      (at ?v ?to)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-macro_0016-tw_00270
    :parameters (?v - vehicle ?r - road_type_macro_0016 ?from - loc_type_loc_0094 ?to - loc_type_loc_0011 ?w - window_type_tw_00270)
    :precondition (and
      (at ?v ?from)
      (connects ?r ?from ?to)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
      (not (location-blocked ?from))
      (not (location-blocked ?to))
    )
    :effect (and
      (not (at ?v ?from))
      (at ?v ?to)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-macro_0016-tw_00300
    :parameters (?v - vehicle ?r - road_type_macro_0016 ?from - loc_type_loc_0094 ?to - loc_type_loc_0011 ?w - window_type_tw_00300)
    :precondition (and
      (at ?v ?from)
      (connects ?r ?from ?to)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
      (not (location-blocked ?from))
      (not (location-blocked ?to))
    )
    :effect (and
      (not (at ?v ?from))
      (at ?v ?to)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-macro_0016-tw_00330
    :parameters (?v - vehicle ?r - road_type_macro_0016 ?from - loc_type_loc_0094 ?to - loc_type_loc_0011 ?w - window_type_tw_00330)
    :precondition (and
      (at ?v ?from)
      (connects ?r ?from ?to)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
      (not (location-blocked ?from))
      (not (location-blocked ?to))
    )
    :effect (and
      (not (at ?v ?from))
      (at ?v ?to)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-macro_0016-tw_00360
    :parameters (?v - vehicle ?r - road_type_macro_0016 ?from - loc_type_loc_0094 ?to - loc_type_loc_0011 ?w - window_type_tw_00360)
    :precondition (and
      (at ?v ?from)
      (connects ?r ?from ?to)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
      (not (location-blocked ?from))
      (not (location-blocked ?to))
    )
    :effect (and
      (not (at ?v ?from))
      (at ?v ?to)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-macro_0016-tw_00390
    :parameters (?v - vehicle ?r - road_type_macro_0016 ?from - loc_type_loc_0094 ?to - loc_type_loc_0011 ?w - window_type_tw_00390)
    :precondition (and
      (at ?v ?from)
      (connects ?r ?from ?to)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
      (not (location-blocked ?from))
      (not (location-blocked ?to))
    )
    :effect (and
      (not (at ?v ?from))
      (at ?v ?to)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-macro_0016-tw_00420
    :parameters (?v - vehicle ?r - road_type_macro_0016 ?from - loc_type_loc_0094 ?to - loc_type_loc_0011 ?w - window_type_tw_00420)
    :precondition (and
      (at ?v ?from)
      (connects ?r ?from ?to)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
      (not (location-blocked ?from))
      (not (location-blocked ?to))
    )
    :effect (and
      (not (at ?v ?from))
      (at ?v ?to)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-macro_0016-tw_00450
    :parameters (?v - vehicle ?r - road_type_macro_0016 ?from - loc_type_loc_0094 ?to - loc_type_loc_0011 ?w - window_type_tw_00450)
    :precondition (and
      (at ?v ?from)
      (connects ?r ?from ?to)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
      (not (location-blocked ?from))
      (not (location-blocked ?to))
    )
    :effect (and
      (not (at ?v ?from))
      (at ?v ?to)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-macro_0017-tw_00000
    :parameters (?v - vehicle ?r - road_type_macro_0017 ?from - loc_type_loc_0096 ?to - loc_type_loc_0010 ?w - window_type_tw_00000)
    :precondition (and
      (at ?v ?from)
      (connects ?r ?from ?to)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
      (not (location-blocked ?from))
      (not (location-blocked ?to))
    )
    :effect (and
      (not (at ?v ?from))
      (at ?v ?to)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-macro_0017-tw_00030
    :parameters (?v - vehicle ?r - road_type_macro_0017 ?from - loc_type_loc_0096 ?to - loc_type_loc_0010 ?w - window_type_tw_00030)
    :precondition (and
      (at ?v ?from)
      (connects ?r ?from ?to)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
      (not (location-blocked ?from))
      (not (location-blocked ?to))
    )
    :effect (and
      (not (at ?v ?from))
      (at ?v ?to)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-macro_0017-tw_00060
    :parameters (?v - vehicle ?r - road_type_macro_0017 ?from - loc_type_loc_0096 ?to - loc_type_loc_0010 ?w - window_type_tw_00060)
    :precondition (and
      (at ?v ?from)
      (connects ?r ?from ?to)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
      (not (location-blocked ?from))
      (not (location-blocked ?to))
    )
    :effect (and
      (not (at ?v ?from))
      (at ?v ?to)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-macro_0017-tw_00090
    :parameters (?v - vehicle ?r - road_type_macro_0017 ?from - loc_type_loc_0096 ?to - loc_type_loc_0010 ?w - window_type_tw_00090)
    :precondition (and
      (at ?v ?from)
      (connects ?r ?from ?to)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
      (not (location-blocked ?from))
      (not (location-blocked ?to))
    )
    :effect (and
      (not (at ?v ?from))
      (at ?v ?to)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-macro_0017-tw_00120
    :parameters (?v - vehicle ?r - road_type_macro_0017 ?from - loc_type_loc_0096 ?to - loc_type_loc_0010 ?w - window_type_tw_00120)
    :precondition (and
      (at ?v ?from)
      (connects ?r ?from ?to)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
      (not (location-blocked ?from))
      (not (location-blocked ?to))
    )
    :effect (and
      (not (at ?v ?from))
      (at ?v ?to)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-macro_0017-tw_00150
    :parameters (?v - vehicle ?r - road_type_macro_0017 ?from - loc_type_loc_0096 ?to - loc_type_loc_0010 ?w - window_type_tw_00150)
    :precondition (and
      (at ?v ?from)
      (connects ?r ?from ?to)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
      (not (location-blocked ?from))
      (not (location-blocked ?to))
    )
    :effect (and
      (not (at ?v ?from))
      (at ?v ?to)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-macro_0017-tw_00180
    :parameters (?v - vehicle ?r - road_type_macro_0017 ?from - loc_type_loc_0096 ?to - loc_type_loc_0010 ?w - window_type_tw_00180)
    :precondition (and
      (at ?v ?from)
      (connects ?r ?from ?to)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
      (not (location-blocked ?from))
      (not (location-blocked ?to))
    )
    :effect (and
      (not (at ?v ?from))
      (at ?v ?to)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-macro_0017-tw_00210
    :parameters (?v - vehicle ?r - road_type_macro_0017 ?from - loc_type_loc_0096 ?to - loc_type_loc_0010 ?w - window_type_tw_00210)
    :precondition (and
      (at ?v ?from)
      (connects ?r ?from ?to)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
      (not (location-blocked ?from))
      (not (location-blocked ?to))
    )
    :effect (and
      (not (at ?v ?from))
      (at ?v ?to)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-macro_0017-tw_00240
    :parameters (?v - vehicle ?r - road_type_macro_0017 ?from - loc_type_loc_0096 ?to - loc_type_loc_0010 ?w - window_type_tw_00240)
    :precondition (and
      (at ?v ?from)
      (connects ?r ?from ?to)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
      (not (location-blocked ?from))
      (not (location-blocked ?to))
    )
    :effect (and
      (not (at ?v ?from))
      (at ?v ?to)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-macro_0017-tw_00270
    :parameters (?v - vehicle ?r - road_type_macro_0017 ?from - loc_type_loc_0096 ?to - loc_type_loc_0010 ?w - window_type_tw_00270)
    :precondition (and
      (at ?v ?from)
      (connects ?r ?from ?to)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
      (not (location-blocked ?from))
      (not (location-blocked ?to))
    )
    :effect (and
      (not (at ?v ?from))
      (at ?v ?to)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-macro_0017-tw_00300
    :parameters (?v - vehicle ?r - road_type_macro_0017 ?from - loc_type_loc_0096 ?to - loc_type_loc_0010 ?w - window_type_tw_00300)
    :precondition (and
      (at ?v ?from)
      (connects ?r ?from ?to)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
      (not (location-blocked ?from))
      (not (location-blocked ?to))
    )
    :effect (and
      (not (at ?v ?from))
      (at ?v ?to)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-macro_0017-tw_00330
    :parameters (?v - vehicle ?r - road_type_macro_0017 ?from - loc_type_loc_0096 ?to - loc_type_loc_0010 ?w - window_type_tw_00330)
    :precondition (and
      (at ?v ?from)
      (connects ?r ?from ?to)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
      (not (location-blocked ?from))
      (not (location-blocked ?to))
    )
    :effect (and
      (not (at ?v ?from))
      (at ?v ?to)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-macro_0017-tw_00360
    :parameters (?v - vehicle ?r - road_type_macro_0017 ?from - loc_type_loc_0096 ?to - loc_type_loc_0010 ?w - window_type_tw_00360)
    :precondition (and
      (at ?v ?from)
      (connects ?r ?from ?to)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
      (not (location-blocked ?from))
      (not (location-blocked ?to))
    )
    :effect (and
      (not (at ?v ?from))
      (at ?v ?to)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-macro_0017-tw_00390
    :parameters (?v - vehicle ?r - road_type_macro_0017 ?from - loc_type_loc_0096 ?to - loc_type_loc_0010 ?w - window_type_tw_00390)
    :precondition (and
      (at ?v ?from)
      (connects ?r ?from ?to)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
      (not (location-blocked ?from))
      (not (location-blocked ?to))
    )
    :effect (and
      (not (at ?v ?from))
      (at ?v ?to)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-macro_0017-tw_00420
    :parameters (?v - vehicle ?r - road_type_macro_0017 ?from - loc_type_loc_0096 ?to - loc_type_loc_0010 ?w - window_type_tw_00420)
    :precondition (and
      (at ?v ?from)
      (connects ?r ?from ?to)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
      (not (location-blocked ?from))
      (not (location-blocked ?to))
    )
    :effect (and
      (not (at ?v ?from))
      (at ?v ?to)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-macro_0017-tw_00450
    :parameters (?v - vehicle ?r - road_type_macro_0017 ?from - loc_type_loc_0096 ?to - loc_type_loc_0010 ?w - window_type_tw_00450)
    :precondition (and
      (at ?v ?from)
      (connects ?r ?from ?to)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
      (not (location-blocked ?from))
      (not (location-blocked ?to))
    )
    :effect (and
      (not (at ?v ?from))
      (at ?v ?to)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-road_0000-tw_00000
    :parameters (?v - vehicle ?r - road_type_road_0000 ?from - loc_type_loc_0000 ?to - loc_type_loc_0087 ?w - window_type_tw_00000)
    :precondition (and
      (at ?v ?from)
      (connects ?r ?from ?to)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
      (not (location-blocked ?from))
      (not (location-blocked ?to))
    )
    :effect (and
      (not (at ?v ?from))
      (at ?v ?to)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-road_0000-tw_00030
    :parameters (?v - vehicle ?r - road_type_road_0000 ?from - loc_type_loc_0000 ?to - loc_type_loc_0087 ?w - window_type_tw_00030)
    :precondition (and
      (at ?v ?from)
      (connects ?r ?from ?to)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
      (not (location-blocked ?from))
      (not (location-blocked ?to))
    )
    :effect (and
      (not (at ?v ?from))
      (at ?v ?to)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-road_0000-tw_00060
    :parameters (?v - vehicle ?r - road_type_road_0000 ?from - loc_type_loc_0000 ?to - loc_type_loc_0087 ?w - window_type_tw_00060)
    :precondition (and
      (at ?v ?from)
      (connects ?r ?from ?to)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
      (not (location-blocked ?from))
      (not (location-blocked ?to))
    )
    :effect (and
      (not (at ?v ?from))
      (at ?v ?to)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-road_0000-tw_00090
    :parameters (?v - vehicle ?r - road_type_road_0000 ?from - loc_type_loc_0000 ?to - loc_type_loc_0087 ?w - window_type_tw_00090)
    :precondition (and
      (at ?v ?from)
      (connects ?r ?from ?to)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
      (not (location-blocked ?from))
      (not (location-blocked ?to))
    )
    :effect (and
      (not (at ?v ?from))
      (at ?v ?to)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-road_0000-tw_00120
    :parameters (?v - vehicle ?r - road_type_road_0000 ?from - loc_type_loc_0000 ?to - loc_type_loc_0087 ?w - window_type_tw_00120)
    :precondition (and
      (at ?v ?from)
      (connects ?r ?from ?to)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
      (not (location-blocked ?from))
      (not (location-blocked ?to))
    )
    :effect (and
      (not (at ?v ?from))
      (at ?v ?to)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-road_0000-tw_00150
    :parameters (?v - vehicle ?r - road_type_road_0000 ?from - loc_type_loc_0000 ?to - loc_type_loc_0087 ?w - window_type_tw_00150)
    :precondition (and
      (at ?v ?from)
      (connects ?r ?from ?to)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
      (not (location-blocked ?from))
      (not (location-blocked ?to))
    )
    :effect (and
      (not (at ?v ?from))
      (at ?v ?to)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-road_0000-tw_00180
    :parameters (?v - vehicle ?r - road_type_road_0000 ?from - loc_type_loc_0000 ?to - loc_type_loc_0087 ?w - window_type_tw_00180)
    :precondition (and
      (at ?v ?from)
      (connects ?r ?from ?to)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
      (not (location-blocked ?from))
      (not (location-blocked ?to))
    )
    :effect (and
      (not (at ?v ?from))
      (at ?v ?to)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-road_0000-tw_00210
    :parameters (?v - vehicle ?r - road_type_road_0000 ?from - loc_type_loc_0000 ?to - loc_type_loc_0087 ?w - window_type_tw_00210)
    :precondition (and
      (at ?v ?from)
      (connects ?r ?from ?to)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
      (not (location-blocked ?from))
      (not (location-blocked ?to))
    )
    :effect (and
      (not (at ?v ?from))
      (at ?v ?to)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-road_0000-tw_00240
    :parameters (?v - vehicle ?r - road_type_road_0000 ?from - loc_type_loc_0000 ?to - loc_type_loc_0087 ?w - window_type_tw_00240)
    :precondition (and
      (at ?v ?from)
      (connects ?r ?from ?to)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
      (not (location-blocked ?from))
      (not (location-blocked ?to))
    )
    :effect (and
      (not (at ?v ?from))
      (at ?v ?to)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-road_0000-tw_00270
    :parameters (?v - vehicle ?r - road_type_road_0000 ?from - loc_type_loc_0000 ?to - loc_type_loc_0087 ?w - window_type_tw_00270)
    :precondition (and
      (at ?v ?from)
      (connects ?r ?from ?to)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
      (not (location-blocked ?from))
      (not (location-blocked ?to))
    )
    :effect (and
      (not (at ?v ?from))
      (at ?v ?to)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-road_0000-tw_00300
    :parameters (?v - vehicle ?r - road_type_road_0000 ?from - loc_type_loc_0000 ?to - loc_type_loc_0087 ?w - window_type_tw_00300)
    :precondition (and
      (at ?v ?from)
      (connects ?r ?from ?to)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
      (not (location-blocked ?from))
      (not (location-blocked ?to))
    )
    :effect (and
      (not (at ?v ?from))
      (at ?v ?to)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-road_0000-tw_00330
    :parameters (?v - vehicle ?r - road_type_road_0000 ?from - loc_type_loc_0000 ?to - loc_type_loc_0087 ?w - window_type_tw_00330)
    :precondition (and
      (at ?v ?from)
      (connects ?r ?from ?to)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
      (not (location-blocked ?from))
      (not (location-blocked ?to))
    )
    :effect (and
      (not (at ?v ?from))
      (at ?v ?to)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-road_0000-tw_00360
    :parameters (?v - vehicle ?r - road_type_road_0000 ?from - loc_type_loc_0000 ?to - loc_type_loc_0087 ?w - window_type_tw_00360)
    :precondition (and
      (at ?v ?from)
      (connects ?r ?from ?to)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
      (not (location-blocked ?from))
      (not (location-blocked ?to))
    )
    :effect (and
      (not (at ?v ?from))
      (at ?v ?to)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-road_0000-tw_00390
    :parameters (?v - vehicle ?r - road_type_road_0000 ?from - loc_type_loc_0000 ?to - loc_type_loc_0087 ?w - window_type_tw_00390)
    :precondition (and
      (at ?v ?from)
      (connects ?r ?from ?to)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
      (not (location-blocked ?from))
      (not (location-blocked ?to))
    )
    :effect (and
      (not (at ?v ?from))
      (at ?v ?to)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-road_0000-tw_00420
    :parameters (?v - vehicle ?r - road_type_road_0000 ?from - loc_type_loc_0000 ?to - loc_type_loc_0087 ?w - window_type_tw_00420)
    :precondition (and
      (at ?v ?from)
      (connects ?r ?from ?to)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
      (not (location-blocked ?from))
      (not (location-blocked ?to))
    )
    :effect (and
      (not (at ?v ?from))
      (at ?v ?to)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-road_0000-tw_00450
    :parameters (?v - vehicle ?r - road_type_road_0000 ?from - loc_type_loc_0000 ?to - loc_type_loc_0087 ?w - window_type_tw_00450)
    :precondition (and
      (at ?v ?from)
      (connects ?r ?from ?to)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
      (not (location-blocked ?from))
      (not (location-blocked ?to))
    )
    :effect (and
      (not (at ?v ?from))
      (at ?v ?to)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-road_0001-tw_00000
    :parameters (?v - vehicle ?r - road_type_road_0001 ?from - loc_type_loc_0001 ?to - loc_type_loc_0003 ?w - window_type_tw_00000)
    :precondition (and
      (at ?v ?from)
      (connects ?r ?from ?to)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
      (not (location-blocked ?from))
      (not (location-blocked ?to))
    )
    :effect (and
      (not (at ?v ?from))
      (at ?v ?to)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-road_0001-tw_00030
    :parameters (?v - vehicle ?r - road_type_road_0001 ?from - loc_type_loc_0001 ?to - loc_type_loc_0003 ?w - window_type_tw_00030)
    :precondition (and
      (at ?v ?from)
      (connects ?r ?from ?to)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
      (not (location-blocked ?from))
      (not (location-blocked ?to))
    )
    :effect (and
      (not (at ?v ?from))
      (at ?v ?to)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-road_0001-tw_00060
    :parameters (?v - vehicle ?r - road_type_road_0001 ?from - loc_type_loc_0001 ?to - loc_type_loc_0003 ?w - window_type_tw_00060)
    :precondition (and
      (at ?v ?from)
      (connects ?r ?from ?to)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
      (not (location-blocked ?from))
      (not (location-blocked ?to))
    )
    :effect (and
      (not (at ?v ?from))
      (at ?v ?to)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-road_0001-tw_00090
    :parameters (?v - vehicle ?r - road_type_road_0001 ?from - loc_type_loc_0001 ?to - loc_type_loc_0003 ?w - window_type_tw_00090)
    :precondition (and
      (at ?v ?from)
      (connects ?r ?from ?to)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
      (not (location-blocked ?from))
      (not (location-blocked ?to))
    )
    :effect (and
      (not (at ?v ?from))
      (at ?v ?to)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-road_0001-tw_00120
    :parameters (?v - vehicle ?r - road_type_road_0001 ?from - loc_type_loc_0001 ?to - loc_type_loc_0003 ?w - window_type_tw_00120)
    :precondition (and
      (at ?v ?from)
      (connects ?r ?from ?to)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
      (not (location-blocked ?from))
      (not (location-blocked ?to))
    )
    :effect (and
      (not (at ?v ?from))
      (at ?v ?to)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-road_0001-tw_00150
    :parameters (?v - vehicle ?r - road_type_road_0001 ?from - loc_type_loc_0001 ?to - loc_type_loc_0003 ?w - window_type_tw_00150)
    :precondition (and
      (at ?v ?from)
      (connects ?r ?from ?to)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
      (not (location-blocked ?from))
      (not (location-blocked ?to))
    )
    :effect (and
      (not (at ?v ?from))
      (at ?v ?to)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-road_0001-tw_00180
    :parameters (?v - vehicle ?r - road_type_road_0001 ?from - loc_type_loc_0001 ?to - loc_type_loc_0003 ?w - window_type_tw_00180)
    :precondition (and
      (at ?v ?from)
      (connects ?r ?from ?to)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
      (not (location-blocked ?from))
      (not (location-blocked ?to))
    )
    :effect (and
      (not (at ?v ?from))
      (at ?v ?to)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-road_0001-tw_00210
    :parameters (?v - vehicle ?r - road_type_road_0001 ?from - loc_type_loc_0001 ?to - loc_type_loc_0003 ?w - window_type_tw_00210)
    :precondition (and
      (at ?v ?from)
      (connects ?r ?from ?to)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
      (not (location-blocked ?from))
      (not (location-blocked ?to))
    )
    :effect (and
      (not (at ?v ?from))
      (at ?v ?to)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-road_0001-tw_00240
    :parameters (?v - vehicle ?r - road_type_road_0001 ?from - loc_type_loc_0001 ?to - loc_type_loc_0003 ?w - window_type_tw_00240)
    :precondition (and
      (at ?v ?from)
      (connects ?r ?from ?to)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
      (not (location-blocked ?from))
      (not (location-blocked ?to))
    )
    :effect (and
      (not (at ?v ?from))
      (at ?v ?to)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-road_0001-tw_00270
    :parameters (?v - vehicle ?r - road_type_road_0001 ?from - loc_type_loc_0001 ?to - loc_type_loc_0003 ?w - window_type_tw_00270)
    :precondition (and
      (at ?v ?from)
      (connects ?r ?from ?to)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
      (not (location-blocked ?from))
      (not (location-blocked ?to))
    )
    :effect (and
      (not (at ?v ?from))
      (at ?v ?to)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-road_0001-tw_00300
    :parameters (?v - vehicle ?r - road_type_road_0001 ?from - loc_type_loc_0001 ?to - loc_type_loc_0003 ?w - window_type_tw_00300)
    :precondition (and
      (at ?v ?from)
      (connects ?r ?from ?to)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
      (not (location-blocked ?from))
      (not (location-blocked ?to))
    )
    :effect (and
      (not (at ?v ?from))
      (at ?v ?to)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-road_0001-tw_00330
    :parameters (?v - vehicle ?r - road_type_road_0001 ?from - loc_type_loc_0001 ?to - loc_type_loc_0003 ?w - window_type_tw_00330)
    :precondition (and
      (at ?v ?from)
      (connects ?r ?from ?to)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
      (not (location-blocked ?from))
      (not (location-blocked ?to))
    )
    :effect (and
      (not (at ?v ?from))
      (at ?v ?to)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-road_0001-tw_00360
    :parameters (?v - vehicle ?r - road_type_road_0001 ?from - loc_type_loc_0001 ?to - loc_type_loc_0003 ?w - window_type_tw_00360)
    :precondition (and
      (at ?v ?from)
      (connects ?r ?from ?to)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
      (not (location-blocked ?from))
      (not (location-blocked ?to))
    )
    :effect (and
      (not (at ?v ?from))
      (at ?v ?to)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-road_0001-tw_00390
    :parameters (?v - vehicle ?r - road_type_road_0001 ?from - loc_type_loc_0001 ?to - loc_type_loc_0003 ?w - window_type_tw_00390)
    :precondition (and
      (at ?v ?from)
      (connects ?r ?from ?to)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
      (not (location-blocked ?from))
      (not (location-blocked ?to))
    )
    :effect (and
      (not (at ?v ?from))
      (at ?v ?to)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-road_0001-tw_00420
    :parameters (?v - vehicle ?r - road_type_road_0001 ?from - loc_type_loc_0001 ?to - loc_type_loc_0003 ?w - window_type_tw_00420)
    :precondition (and
      (at ?v ?from)
      (connects ?r ?from ?to)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
      (not (location-blocked ?from))
      (not (location-blocked ?to))
    )
    :effect (and
      (not (at ?v ?from))
      (at ?v ?to)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-road_0001-tw_00450
    :parameters (?v - vehicle ?r - road_type_road_0001 ?from - loc_type_loc_0001 ?to - loc_type_loc_0003 ?w - window_type_tw_00450)
    :precondition (and
      (at ?v ?from)
      (connects ?r ?from ?to)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
      (not (location-blocked ?from))
      (not (location-blocked ?to))
    )
    :effect (and
      (not (at ?v ?from))
      (at ?v ?to)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-road_0002-tw_00000
    :parameters (?v - vehicle ?r - road_type_road_0002 ?from - loc_type_loc_0001 ?to - loc_type_loc_0031 ?w - window_type_tw_00000)
    :precondition (and
      (at ?v ?from)
      (connects ?r ?from ?to)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
      (not (location-blocked ?from))
      (not (location-blocked ?to))
    )
    :effect (and
      (not (at ?v ?from))
      (at ?v ?to)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-road_0002-tw_00030
    :parameters (?v - vehicle ?r - road_type_road_0002 ?from - loc_type_loc_0001 ?to - loc_type_loc_0031 ?w - window_type_tw_00030)
    :precondition (and
      (at ?v ?from)
      (connects ?r ?from ?to)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
      (not (location-blocked ?from))
      (not (location-blocked ?to))
    )
    :effect (and
      (not (at ?v ?from))
      (at ?v ?to)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-road_0002-tw_00060
    :parameters (?v - vehicle ?r - road_type_road_0002 ?from - loc_type_loc_0001 ?to - loc_type_loc_0031 ?w - window_type_tw_00060)
    :precondition (and
      (at ?v ?from)
      (connects ?r ?from ?to)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
      (not (location-blocked ?from))
      (not (location-blocked ?to))
    )
    :effect (and
      (not (at ?v ?from))
      (at ?v ?to)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-road_0002-tw_00090
    :parameters (?v - vehicle ?r - road_type_road_0002 ?from - loc_type_loc_0001 ?to - loc_type_loc_0031 ?w - window_type_tw_00090)
    :precondition (and
      (at ?v ?from)
      (connects ?r ?from ?to)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
      (not (location-blocked ?from))
      (not (location-blocked ?to))
    )
    :effect (and
      (not (at ?v ?from))
      (at ?v ?to)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-road_0002-tw_00120
    :parameters (?v - vehicle ?r - road_type_road_0002 ?from - loc_type_loc_0001 ?to - loc_type_loc_0031 ?w - window_type_tw_00120)
    :precondition (and
      (at ?v ?from)
      (connects ?r ?from ?to)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
      (not (location-blocked ?from))
      (not (location-blocked ?to))
    )
    :effect (and
      (not (at ?v ?from))
      (at ?v ?to)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-road_0002-tw_00150
    :parameters (?v - vehicle ?r - road_type_road_0002 ?from - loc_type_loc_0001 ?to - loc_type_loc_0031 ?w - window_type_tw_00150)
    :precondition (and
      (at ?v ?from)
      (connects ?r ?from ?to)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
      (not (location-blocked ?from))
      (not (location-blocked ?to))
    )
    :effect (and
      (not (at ?v ?from))
      (at ?v ?to)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-road_0002-tw_00180
    :parameters (?v - vehicle ?r - road_type_road_0002 ?from - loc_type_loc_0001 ?to - loc_type_loc_0031 ?w - window_type_tw_00180)
    :precondition (and
      (at ?v ?from)
      (connects ?r ?from ?to)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
      (not (location-blocked ?from))
      (not (location-blocked ?to))
    )
    :effect (and
      (not (at ?v ?from))
      (at ?v ?to)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-road_0002-tw_00210
    :parameters (?v - vehicle ?r - road_type_road_0002 ?from - loc_type_loc_0001 ?to - loc_type_loc_0031 ?w - window_type_tw_00210)
    :precondition (and
      (at ?v ?from)
      (connects ?r ?from ?to)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
      (not (location-blocked ?from))
      (not (location-blocked ?to))
    )
    :effect (and
      (not (at ?v ?from))
      (at ?v ?to)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-road_0002-tw_00240
    :parameters (?v - vehicle ?r - road_type_road_0002 ?from - loc_type_loc_0001 ?to - loc_type_loc_0031 ?w - window_type_tw_00240)
    :precondition (and
      (at ?v ?from)
      (connects ?r ?from ?to)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
      (not (location-blocked ?from))
      (not (location-blocked ?to))
    )
    :effect (and
      (not (at ?v ?from))
      (at ?v ?to)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-road_0002-tw_00270
    :parameters (?v - vehicle ?r - road_type_road_0002 ?from - loc_type_loc_0001 ?to - loc_type_loc_0031 ?w - window_type_tw_00270)
    :precondition (and
      (at ?v ?from)
      (connects ?r ?from ?to)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
      (not (location-blocked ?from))
      (not (location-blocked ?to))
    )
    :effect (and
      (not (at ?v ?from))
      (at ?v ?to)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-road_0002-tw_00300
    :parameters (?v - vehicle ?r - road_type_road_0002 ?from - loc_type_loc_0001 ?to - loc_type_loc_0031 ?w - window_type_tw_00300)
    :precondition (and
      (at ?v ?from)
      (connects ?r ?from ?to)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
      (not (location-blocked ?from))
      (not (location-blocked ?to))
    )
    :effect (and
      (not (at ?v ?from))
      (at ?v ?to)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-road_0002-tw_00330
    :parameters (?v - vehicle ?r - road_type_road_0002 ?from - loc_type_loc_0001 ?to - loc_type_loc_0031 ?w - window_type_tw_00330)
    :precondition (and
      (at ?v ?from)
      (connects ?r ?from ?to)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
      (not (location-blocked ?from))
      (not (location-blocked ?to))
    )
    :effect (and
      (not (at ?v ?from))
      (at ?v ?to)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-road_0002-tw_00360
    :parameters (?v - vehicle ?r - road_type_road_0002 ?from - loc_type_loc_0001 ?to - loc_type_loc_0031 ?w - window_type_tw_00360)
    :precondition (and
      (at ?v ?from)
      (connects ?r ?from ?to)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
      (not (location-blocked ?from))
      (not (location-blocked ?to))
    )
    :effect (and
      (not (at ?v ?from))
      (at ?v ?to)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-road_0002-tw_00390
    :parameters (?v - vehicle ?r - road_type_road_0002 ?from - loc_type_loc_0001 ?to - loc_type_loc_0031 ?w - window_type_tw_00390)
    :precondition (and
      (at ?v ?from)
      (connects ?r ?from ?to)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
      (not (location-blocked ?from))
      (not (location-blocked ?to))
    )
    :effect (and
      (not (at ?v ?from))
      (at ?v ?to)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-road_0002-tw_00420
    :parameters (?v - vehicle ?r - road_type_road_0002 ?from - loc_type_loc_0001 ?to - loc_type_loc_0031 ?w - window_type_tw_00420)
    :precondition (and
      (at ?v ?from)
      (connects ?r ?from ?to)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
      (not (location-blocked ?from))
      (not (location-blocked ?to))
    )
    :effect (and
      (not (at ?v ?from))
      (at ?v ?to)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-road_0002-tw_00450
    :parameters (?v - vehicle ?r - road_type_road_0002 ?from - loc_type_loc_0001 ?to - loc_type_loc_0031 ?w - window_type_tw_00450)
    :precondition (and
      (at ?v ?from)
      (connects ?r ?from ?to)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
      (not (location-blocked ?from))
      (not (location-blocked ?to))
    )
    :effect (and
      (not (at ?v ?from))
      (at ?v ?to)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-road_0003-tw_00000
    :parameters (?v - vehicle ?r - road_type_road_0003 ?from - loc_type_loc_0002 ?to - loc_type_loc_0064 ?w - window_type_tw_00000)
    :precondition (and
      (at ?v ?from)
      (connects ?r ?from ?to)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
      (not (location-blocked ?from))
      (not (location-blocked ?to))
    )
    :effect (and
      (not (at ?v ?from))
      (at ?v ?to)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-road_0003-tw_00030
    :parameters (?v - vehicle ?r - road_type_road_0003 ?from - loc_type_loc_0002 ?to - loc_type_loc_0064 ?w - window_type_tw_00030)
    :precondition (and
      (at ?v ?from)
      (connects ?r ?from ?to)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
      (not (location-blocked ?from))
      (not (location-blocked ?to))
    )
    :effect (and
      (not (at ?v ?from))
      (at ?v ?to)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-road_0003-tw_00060
    :parameters (?v - vehicle ?r - road_type_road_0003 ?from - loc_type_loc_0002 ?to - loc_type_loc_0064 ?w - window_type_tw_00060)
    :precondition (and
      (at ?v ?from)
      (connects ?r ?from ?to)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
      (not (location-blocked ?from))
      (not (location-blocked ?to))
    )
    :effect (and
      (not (at ?v ?from))
      (at ?v ?to)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-road_0003-tw_00090
    :parameters (?v - vehicle ?r - road_type_road_0003 ?from - loc_type_loc_0002 ?to - loc_type_loc_0064 ?w - window_type_tw_00090)
    :precondition (and
      (at ?v ?from)
      (connects ?r ?from ?to)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
      (not (location-blocked ?from))
      (not (location-blocked ?to))
    )
    :effect (and
      (not (at ?v ?from))
      (at ?v ?to)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-road_0003-tw_00120
    :parameters (?v - vehicle ?r - road_type_road_0003 ?from - loc_type_loc_0002 ?to - loc_type_loc_0064 ?w - window_type_tw_00120)
    :precondition (and
      (at ?v ?from)
      (connects ?r ?from ?to)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
      (not (location-blocked ?from))
      (not (location-blocked ?to))
    )
    :effect (and
      (not (at ?v ?from))
      (at ?v ?to)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-road_0003-tw_00150
    :parameters (?v - vehicle ?r - road_type_road_0003 ?from - loc_type_loc_0002 ?to - loc_type_loc_0064 ?w - window_type_tw_00150)
    :precondition (and
      (at ?v ?from)
      (connects ?r ?from ?to)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
      (not (location-blocked ?from))
      (not (location-blocked ?to))
    )
    :effect (and
      (not (at ?v ?from))
      (at ?v ?to)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-road_0003-tw_00180
    :parameters (?v - vehicle ?r - road_type_road_0003 ?from - loc_type_loc_0002 ?to - loc_type_loc_0064 ?w - window_type_tw_00180)
    :precondition (and
      (at ?v ?from)
      (connects ?r ?from ?to)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
      (not (location-blocked ?from))
      (not (location-blocked ?to))
    )
    :effect (and
      (not (at ?v ?from))
      (at ?v ?to)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-road_0003-tw_00210
    :parameters (?v - vehicle ?r - road_type_road_0003 ?from - loc_type_loc_0002 ?to - loc_type_loc_0064 ?w - window_type_tw_00210)
    :precondition (and
      (at ?v ?from)
      (connects ?r ?from ?to)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
      (not (location-blocked ?from))
      (not (location-blocked ?to))
    )
    :effect (and
      (not (at ?v ?from))
      (at ?v ?to)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-road_0003-tw_00240
    :parameters (?v - vehicle ?r - road_type_road_0003 ?from - loc_type_loc_0002 ?to - loc_type_loc_0064 ?w - window_type_tw_00240)
    :precondition (and
      (at ?v ?from)
      (connects ?r ?from ?to)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
      (not (location-blocked ?from))
      (not (location-blocked ?to))
    )
    :effect (and
      (not (at ?v ?from))
      (at ?v ?to)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-road_0003-tw_00270
    :parameters (?v - vehicle ?r - road_type_road_0003 ?from - loc_type_loc_0002 ?to - loc_type_loc_0064 ?w - window_type_tw_00270)
    :precondition (and
      (at ?v ?from)
      (connects ?r ?from ?to)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
      (not (location-blocked ?from))
      (not (location-blocked ?to))
    )
    :effect (and
      (not (at ?v ?from))
      (at ?v ?to)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-road_0003-tw_00300
    :parameters (?v - vehicle ?r - road_type_road_0003 ?from - loc_type_loc_0002 ?to - loc_type_loc_0064 ?w - window_type_tw_00300)
    :precondition (and
      (at ?v ?from)
      (connects ?r ?from ?to)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
      (not (location-blocked ?from))
      (not (location-blocked ?to))
    )
    :effect (and
      (not (at ?v ?from))
      (at ?v ?to)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-road_0003-tw_00330
    :parameters (?v - vehicle ?r - road_type_road_0003 ?from - loc_type_loc_0002 ?to - loc_type_loc_0064 ?w - window_type_tw_00330)
    :precondition (and
      (at ?v ?from)
      (connects ?r ?from ?to)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
      (not (location-blocked ?from))
      (not (location-blocked ?to))
    )
    :effect (and
      (not (at ?v ?from))
      (at ?v ?to)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-road_0003-tw_00360
    :parameters (?v - vehicle ?r - road_type_road_0003 ?from - loc_type_loc_0002 ?to - loc_type_loc_0064 ?w - window_type_tw_00360)
    :precondition (and
      (at ?v ?from)
      (connects ?r ?from ?to)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
      (not (location-blocked ?from))
      (not (location-blocked ?to))
    )
    :effect (and
      (not (at ?v ?from))
      (at ?v ?to)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-road_0003-tw_00390
    :parameters (?v - vehicle ?r - road_type_road_0003 ?from - loc_type_loc_0002 ?to - loc_type_loc_0064 ?w - window_type_tw_00390)
    :precondition (and
      (at ?v ?from)
      (connects ?r ?from ?to)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
      (not (location-blocked ?from))
      (not (location-blocked ?to))
    )
    :effect (and
      (not (at ?v ?from))
      (at ?v ?to)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-road_0003-tw_00420
    :parameters (?v - vehicle ?r - road_type_road_0003 ?from - loc_type_loc_0002 ?to - loc_type_loc_0064 ?w - window_type_tw_00420)
    :precondition (and
      (at ?v ?from)
      (connects ?r ?from ?to)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
      (not (location-blocked ?from))
      (not (location-blocked ?to))
    )
    :effect (and
      (not (at ?v ?from))
      (at ?v ?to)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-road_0003-tw_00450
    :parameters (?v - vehicle ?r - road_type_road_0003 ?from - loc_type_loc_0002 ?to - loc_type_loc_0064 ?w - window_type_tw_00450)
    :precondition (and
      (at ?v ?from)
      (connects ?r ?from ?to)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
      (not (location-blocked ?from))
      (not (location-blocked ?to))
    )
    :effect (and
      (not (at ?v ?from))
      (at ?v ?to)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-road_0004-tw_00000
    :parameters (?v - vehicle ?r - road_type_road_0004 ?from - loc_type_loc_0002 ?to - loc_type_loc_0025 ?w - window_type_tw_00000)
    :precondition (and
      (at ?v ?from)
      (connects ?r ?from ?to)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
      (not (location-blocked ?from))
      (not (location-blocked ?to))
    )
    :effect (and
      (not (at ?v ?from))
      (at ?v ?to)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-road_0004-tw_00030
    :parameters (?v - vehicle ?r - road_type_road_0004 ?from - loc_type_loc_0002 ?to - loc_type_loc_0025 ?w - window_type_tw_00030)
    :precondition (and
      (at ?v ?from)
      (connects ?r ?from ?to)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
      (not (location-blocked ?from))
      (not (location-blocked ?to))
    )
    :effect (and
      (not (at ?v ?from))
      (at ?v ?to)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-road_0004-tw_00060
    :parameters (?v - vehicle ?r - road_type_road_0004 ?from - loc_type_loc_0002 ?to - loc_type_loc_0025 ?w - window_type_tw_00060)
    :precondition (and
      (at ?v ?from)
      (connects ?r ?from ?to)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
      (not (location-blocked ?from))
      (not (location-blocked ?to))
    )
    :effect (and
      (not (at ?v ?from))
      (at ?v ?to)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-road_0004-tw_00090
    :parameters (?v - vehicle ?r - road_type_road_0004 ?from - loc_type_loc_0002 ?to - loc_type_loc_0025 ?w - window_type_tw_00090)
    :precondition (and
      (at ?v ?from)
      (connects ?r ?from ?to)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
      (not (location-blocked ?from))
      (not (location-blocked ?to))
    )
    :effect (and
      (not (at ?v ?from))
      (at ?v ?to)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-road_0004-tw_00120
    :parameters (?v - vehicle ?r - road_type_road_0004 ?from - loc_type_loc_0002 ?to - loc_type_loc_0025 ?w - window_type_tw_00120)
    :precondition (and
      (at ?v ?from)
      (connects ?r ?from ?to)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
      (not (location-blocked ?from))
      (not (location-blocked ?to))
    )
    :effect (and
      (not (at ?v ?from))
      (at ?v ?to)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-road_0004-tw_00150
    :parameters (?v - vehicle ?r - road_type_road_0004 ?from - loc_type_loc_0002 ?to - loc_type_loc_0025 ?w - window_type_tw_00150)
    :precondition (and
      (at ?v ?from)
      (connects ?r ?from ?to)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
      (not (location-blocked ?from))
      (not (location-blocked ?to))
    )
    :effect (and
      (not (at ?v ?from))
      (at ?v ?to)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-road_0004-tw_00180
    :parameters (?v - vehicle ?r - road_type_road_0004 ?from - loc_type_loc_0002 ?to - loc_type_loc_0025 ?w - window_type_tw_00180)
    :precondition (and
      (at ?v ?from)
      (connects ?r ?from ?to)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
      (not (location-blocked ?from))
      (not (location-blocked ?to))
    )
    :effect (and
      (not (at ?v ?from))
      (at ?v ?to)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-road_0004-tw_00210
    :parameters (?v - vehicle ?r - road_type_road_0004 ?from - loc_type_loc_0002 ?to - loc_type_loc_0025 ?w - window_type_tw_00210)
    :precondition (and
      (at ?v ?from)
      (connects ?r ?from ?to)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
      (not (location-blocked ?from))
      (not (location-blocked ?to))
    )
    :effect (and
      (not (at ?v ?from))
      (at ?v ?to)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-road_0004-tw_00240
    :parameters (?v - vehicle ?r - road_type_road_0004 ?from - loc_type_loc_0002 ?to - loc_type_loc_0025 ?w - window_type_tw_00240)
    :precondition (and
      (at ?v ?from)
      (connects ?r ?from ?to)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
      (not (location-blocked ?from))
      (not (location-blocked ?to))
    )
    :effect (and
      (not (at ?v ?from))
      (at ?v ?to)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-road_0004-tw_00270
    :parameters (?v - vehicle ?r - road_type_road_0004 ?from - loc_type_loc_0002 ?to - loc_type_loc_0025 ?w - window_type_tw_00270)
    :precondition (and
      (at ?v ?from)
      (connects ?r ?from ?to)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
      (not (location-blocked ?from))
      (not (location-blocked ?to))
    )
    :effect (and
      (not (at ?v ?from))
      (at ?v ?to)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-road_0004-tw_00300
    :parameters (?v - vehicle ?r - road_type_road_0004 ?from - loc_type_loc_0002 ?to - loc_type_loc_0025 ?w - window_type_tw_00300)
    :precondition (and
      (at ?v ?from)
      (connects ?r ?from ?to)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
      (not (location-blocked ?from))
      (not (location-blocked ?to))
    )
    :effect (and
      (not (at ?v ?from))
      (at ?v ?to)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-road_0004-tw_00330
    :parameters (?v - vehicle ?r - road_type_road_0004 ?from - loc_type_loc_0002 ?to - loc_type_loc_0025 ?w - window_type_tw_00330)
    :precondition (and
      (at ?v ?from)
      (connects ?r ?from ?to)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
      (not (location-blocked ?from))
      (not (location-blocked ?to))
    )
    :effect (and
      (not (at ?v ?from))
      (at ?v ?to)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-road_0004-tw_00360
    :parameters (?v - vehicle ?r - road_type_road_0004 ?from - loc_type_loc_0002 ?to - loc_type_loc_0025 ?w - window_type_tw_00360)
    :precondition (and
      (at ?v ?from)
      (connects ?r ?from ?to)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
      (not (location-blocked ?from))
      (not (location-blocked ?to))
    )
    :effect (and
      (not (at ?v ?from))
      (at ?v ?to)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-road_0004-tw_00390
    :parameters (?v - vehicle ?r - road_type_road_0004 ?from - loc_type_loc_0002 ?to - loc_type_loc_0025 ?w - window_type_tw_00390)
    :precondition (and
      (at ?v ?from)
      (connects ?r ?from ?to)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
      (not (location-blocked ?from))
      (not (location-blocked ?to))
    )
    :effect (and
      (not (at ?v ?from))
      (at ?v ?to)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-road_0004-tw_00420
    :parameters (?v - vehicle ?r - road_type_road_0004 ?from - loc_type_loc_0002 ?to - loc_type_loc_0025 ?w - window_type_tw_00420)
    :precondition (and
      (at ?v ?from)
      (connects ?r ?from ?to)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
      (not (location-blocked ?from))
      (not (location-blocked ?to))
    )
    :effect (and
      (not (at ?v ?from))
      (at ?v ?to)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-road_0004-tw_00450
    :parameters (?v - vehicle ?r - road_type_road_0004 ?from - loc_type_loc_0002 ?to - loc_type_loc_0025 ?w - window_type_tw_00450)
    :precondition (and
      (at ?v ?from)
      (connects ?r ?from ?to)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
      (not (location-blocked ?from))
      (not (location-blocked ?to))
    )
    :effect (and
      (not (at ?v ?from))
      (at ?v ?to)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-road_0008-tw_00000
    :parameters (?v - vehicle ?r - road_type_road_0008 ?from - loc_type_loc_0004 ?to - loc_type_loc_0002 ?w - window_type_tw_00000)
    :precondition (and
      (at ?v ?from)
      (connects ?r ?from ?to)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
      (not (location-blocked ?from))
      (not (location-blocked ?to))
    )
    :effect (and
      (not (at ?v ?from))
      (at ?v ?to)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-road_0008-tw_00030
    :parameters (?v - vehicle ?r - road_type_road_0008 ?from - loc_type_loc_0004 ?to - loc_type_loc_0002 ?w - window_type_tw_00030)
    :precondition (and
      (at ?v ?from)
      (connects ?r ?from ?to)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
      (not (location-blocked ?from))
      (not (location-blocked ?to))
    )
    :effect (and
      (not (at ?v ?from))
      (at ?v ?to)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-road_0008-tw_00060
    :parameters (?v - vehicle ?r - road_type_road_0008 ?from - loc_type_loc_0004 ?to - loc_type_loc_0002 ?w - window_type_tw_00060)
    :precondition (and
      (at ?v ?from)
      (connects ?r ?from ?to)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
      (not (location-blocked ?from))
      (not (location-blocked ?to))
    )
    :effect (and
      (not (at ?v ?from))
      (at ?v ?to)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-road_0008-tw_00090
    :parameters (?v - vehicle ?r - road_type_road_0008 ?from - loc_type_loc_0004 ?to - loc_type_loc_0002 ?w - window_type_tw_00090)
    :precondition (and
      (at ?v ?from)
      (connects ?r ?from ?to)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
      (not (location-blocked ?from))
      (not (location-blocked ?to))
    )
    :effect (and
      (not (at ?v ?from))
      (at ?v ?to)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-road_0008-tw_00120
    :parameters (?v - vehicle ?r - road_type_road_0008 ?from - loc_type_loc_0004 ?to - loc_type_loc_0002 ?w - window_type_tw_00120)
    :precondition (and
      (at ?v ?from)
      (connects ?r ?from ?to)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
      (not (location-blocked ?from))
      (not (location-blocked ?to))
    )
    :effect (and
      (not (at ?v ?from))
      (at ?v ?to)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-road_0008-tw_00150
    :parameters (?v - vehicle ?r - road_type_road_0008 ?from - loc_type_loc_0004 ?to - loc_type_loc_0002 ?w - window_type_tw_00150)
    :precondition (and
      (at ?v ?from)
      (connects ?r ?from ?to)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
      (not (location-blocked ?from))
      (not (location-blocked ?to))
    )
    :effect (and
      (not (at ?v ?from))
      (at ?v ?to)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-road_0008-tw_00180
    :parameters (?v - vehicle ?r - road_type_road_0008 ?from - loc_type_loc_0004 ?to - loc_type_loc_0002 ?w - window_type_tw_00180)
    :precondition (and
      (at ?v ?from)
      (connects ?r ?from ?to)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
      (not (location-blocked ?from))
      (not (location-blocked ?to))
    )
    :effect (and
      (not (at ?v ?from))
      (at ?v ?to)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-road_0008-tw_00210
    :parameters (?v - vehicle ?r - road_type_road_0008 ?from - loc_type_loc_0004 ?to - loc_type_loc_0002 ?w - window_type_tw_00210)
    :precondition (and
      (at ?v ?from)
      (connects ?r ?from ?to)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
      (not (location-blocked ?from))
      (not (location-blocked ?to))
    )
    :effect (and
      (not (at ?v ?from))
      (at ?v ?to)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-road_0008-tw_00240
    :parameters (?v - vehicle ?r - road_type_road_0008 ?from - loc_type_loc_0004 ?to - loc_type_loc_0002 ?w - window_type_tw_00240)
    :precondition (and
      (at ?v ?from)
      (connects ?r ?from ?to)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
      (not (location-blocked ?from))
      (not (location-blocked ?to))
    )
    :effect (and
      (not (at ?v ?from))
      (at ?v ?to)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-road_0008-tw_00270
    :parameters (?v - vehicle ?r - road_type_road_0008 ?from - loc_type_loc_0004 ?to - loc_type_loc_0002 ?w - window_type_tw_00270)
    :precondition (and
      (at ?v ?from)
      (connects ?r ?from ?to)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
      (not (location-blocked ?from))
      (not (location-blocked ?to))
    )
    :effect (and
      (not (at ?v ?from))
      (at ?v ?to)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-road_0008-tw_00300
    :parameters (?v - vehicle ?r - road_type_road_0008 ?from - loc_type_loc_0004 ?to - loc_type_loc_0002 ?w - window_type_tw_00300)
    :precondition (and
      (at ?v ?from)
      (connects ?r ?from ?to)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
      (not (location-blocked ?from))
      (not (location-blocked ?to))
    )
    :effect (and
      (not (at ?v ?from))
      (at ?v ?to)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-road_0008-tw_00330
    :parameters (?v - vehicle ?r - road_type_road_0008 ?from - loc_type_loc_0004 ?to - loc_type_loc_0002 ?w - window_type_tw_00330)
    :precondition (and
      (at ?v ?from)
      (connects ?r ?from ?to)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
      (not (location-blocked ?from))
      (not (location-blocked ?to))
    )
    :effect (and
      (not (at ?v ?from))
      (at ?v ?to)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-road_0008-tw_00360
    :parameters (?v - vehicle ?r - road_type_road_0008 ?from - loc_type_loc_0004 ?to - loc_type_loc_0002 ?w - window_type_tw_00360)
    :precondition (and
      (at ?v ?from)
      (connects ?r ?from ?to)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
      (not (location-blocked ?from))
      (not (location-blocked ?to))
    )
    :effect (and
      (not (at ?v ?from))
      (at ?v ?to)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-road_0008-tw_00390
    :parameters (?v - vehicle ?r - road_type_road_0008 ?from - loc_type_loc_0004 ?to - loc_type_loc_0002 ?w - window_type_tw_00390)
    :precondition (and
      (at ?v ?from)
      (connects ?r ?from ?to)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
      (not (location-blocked ?from))
      (not (location-blocked ?to))
    )
    :effect (and
      (not (at ?v ?from))
      (at ?v ?to)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-road_0008-tw_00420
    :parameters (?v - vehicle ?r - road_type_road_0008 ?from - loc_type_loc_0004 ?to - loc_type_loc_0002 ?w - window_type_tw_00420)
    :precondition (and
      (at ?v ?from)
      (connects ?r ?from ?to)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
      (not (location-blocked ?from))
      (not (location-blocked ?to))
    )
    :effect (and
      (not (at ?v ?from))
      (at ?v ?to)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-road_0008-tw_00450
    :parameters (?v - vehicle ?r - road_type_road_0008 ?from - loc_type_loc_0004 ?to - loc_type_loc_0002 ?w - window_type_tw_00450)
    :precondition (and
      (at ?v ?from)
      (connects ?r ?from ?to)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
      (not (location-blocked ?from))
      (not (location-blocked ?to))
    )
    :effect (and
      (not (at ?v ?from))
      (at ?v ?to)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-road_0009-tw_00000
    :parameters (?v - vehicle ?r - road_type_road_0009 ?from - loc_type_loc_0005 ?to - loc_type_loc_0050 ?w - window_type_tw_00000)
    :precondition (and
      (at ?v ?from)
      (connects ?r ?from ?to)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
      (not (location-blocked ?from))
      (not (location-blocked ?to))
    )
    :effect (and
      (not (at ?v ?from))
      (at ?v ?to)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-road_0009-tw_00030
    :parameters (?v - vehicle ?r - road_type_road_0009 ?from - loc_type_loc_0005 ?to - loc_type_loc_0050 ?w - window_type_tw_00030)
    :precondition (and
      (at ?v ?from)
      (connects ?r ?from ?to)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
      (not (location-blocked ?from))
      (not (location-blocked ?to))
    )
    :effect (and
      (not (at ?v ?from))
      (at ?v ?to)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-road_0009-tw_00060
    :parameters (?v - vehicle ?r - road_type_road_0009 ?from - loc_type_loc_0005 ?to - loc_type_loc_0050 ?w - window_type_tw_00060)
    :precondition (and
      (at ?v ?from)
      (connects ?r ?from ?to)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
      (not (location-blocked ?from))
      (not (location-blocked ?to))
    )
    :effect (and
      (not (at ?v ?from))
      (at ?v ?to)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-road_0009-tw_00090
    :parameters (?v - vehicle ?r - road_type_road_0009 ?from - loc_type_loc_0005 ?to - loc_type_loc_0050 ?w - window_type_tw_00090)
    :precondition (and
      (at ?v ?from)
      (connects ?r ?from ?to)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
      (not (location-blocked ?from))
      (not (location-blocked ?to))
    )
    :effect (and
      (not (at ?v ?from))
      (at ?v ?to)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-road_0009-tw_00120
    :parameters (?v - vehicle ?r - road_type_road_0009 ?from - loc_type_loc_0005 ?to - loc_type_loc_0050 ?w - window_type_tw_00120)
    :precondition (and
      (at ?v ?from)
      (connects ?r ?from ?to)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
      (not (location-blocked ?from))
      (not (location-blocked ?to))
    )
    :effect (and
      (not (at ?v ?from))
      (at ?v ?to)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-road_0009-tw_00150
    :parameters (?v - vehicle ?r - road_type_road_0009 ?from - loc_type_loc_0005 ?to - loc_type_loc_0050 ?w - window_type_tw_00150)
    :precondition (and
      (at ?v ?from)
      (connects ?r ?from ?to)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
      (not (location-blocked ?from))
      (not (location-blocked ?to))
    )
    :effect (and
      (not (at ?v ?from))
      (at ?v ?to)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-road_0009-tw_00180
    :parameters (?v - vehicle ?r - road_type_road_0009 ?from - loc_type_loc_0005 ?to - loc_type_loc_0050 ?w - window_type_tw_00180)
    :precondition (and
      (at ?v ?from)
      (connects ?r ?from ?to)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
      (not (location-blocked ?from))
      (not (location-blocked ?to))
    )
    :effect (and
      (not (at ?v ?from))
      (at ?v ?to)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-road_0009-tw_00210
    :parameters (?v - vehicle ?r - road_type_road_0009 ?from - loc_type_loc_0005 ?to - loc_type_loc_0050 ?w - window_type_tw_00210)
    :precondition (and
      (at ?v ?from)
      (connects ?r ?from ?to)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
      (not (location-blocked ?from))
      (not (location-blocked ?to))
    )
    :effect (and
      (not (at ?v ?from))
      (at ?v ?to)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-road_0009-tw_00240
    :parameters (?v - vehicle ?r - road_type_road_0009 ?from - loc_type_loc_0005 ?to - loc_type_loc_0050 ?w - window_type_tw_00240)
    :precondition (and
      (at ?v ?from)
      (connects ?r ?from ?to)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
      (not (location-blocked ?from))
      (not (location-blocked ?to))
    )
    :effect (and
      (not (at ?v ?from))
      (at ?v ?to)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-road_0009-tw_00270
    :parameters (?v - vehicle ?r - road_type_road_0009 ?from - loc_type_loc_0005 ?to - loc_type_loc_0050 ?w - window_type_tw_00270)
    :precondition (and
      (at ?v ?from)
      (connects ?r ?from ?to)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
      (not (location-blocked ?from))
      (not (location-blocked ?to))
    )
    :effect (and
      (not (at ?v ?from))
      (at ?v ?to)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-road_0009-tw_00300
    :parameters (?v - vehicle ?r - road_type_road_0009 ?from - loc_type_loc_0005 ?to - loc_type_loc_0050 ?w - window_type_tw_00300)
    :precondition (and
      (at ?v ?from)
      (connects ?r ?from ?to)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
      (not (location-blocked ?from))
      (not (location-blocked ?to))
    )
    :effect (and
      (not (at ?v ?from))
      (at ?v ?to)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-road_0009-tw_00330
    :parameters (?v - vehicle ?r - road_type_road_0009 ?from - loc_type_loc_0005 ?to - loc_type_loc_0050 ?w - window_type_tw_00330)
    :precondition (and
      (at ?v ?from)
      (connects ?r ?from ?to)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
      (not (location-blocked ?from))
      (not (location-blocked ?to))
    )
    :effect (and
      (not (at ?v ?from))
      (at ?v ?to)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-road_0009-tw_00360
    :parameters (?v - vehicle ?r - road_type_road_0009 ?from - loc_type_loc_0005 ?to - loc_type_loc_0050 ?w - window_type_tw_00360)
    :precondition (and
      (at ?v ?from)
      (connects ?r ?from ?to)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
      (not (location-blocked ?from))
      (not (location-blocked ?to))
    )
    :effect (and
      (not (at ?v ?from))
      (at ?v ?to)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-road_0009-tw_00390
    :parameters (?v - vehicle ?r - road_type_road_0009 ?from - loc_type_loc_0005 ?to - loc_type_loc_0050 ?w - window_type_tw_00390)
    :precondition (and
      (at ?v ?from)
      (connects ?r ?from ?to)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
      (not (location-blocked ?from))
      (not (location-blocked ?to))
    )
    :effect (and
      (not (at ?v ?from))
      (at ?v ?to)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-road_0009-tw_00420
    :parameters (?v - vehicle ?r - road_type_road_0009 ?from - loc_type_loc_0005 ?to - loc_type_loc_0050 ?w - window_type_tw_00420)
    :precondition (and
      (at ?v ?from)
      (connects ?r ?from ?to)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
      (not (location-blocked ?from))
      (not (location-blocked ?to))
    )
    :effect (and
      (not (at ?v ?from))
      (at ?v ?to)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-road_0009-tw_00450
    :parameters (?v - vehicle ?r - road_type_road_0009 ?from - loc_type_loc_0005 ?to - loc_type_loc_0050 ?w - window_type_tw_00450)
    :precondition (and
      (at ?v ?from)
      (connects ?r ?from ?to)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
      (not (location-blocked ?from))
      (not (location-blocked ?to))
    )
    :effect (and
      (not (at ?v ?from))
      (at ?v ?to)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-road_0010-tw_00000
    :parameters (?v - vehicle ?r - road_type_road_0010 ?from - loc_type_loc_0007 ?to - loc_type_loc_0092 ?w - window_type_tw_00000)
    :precondition (and
      (at ?v ?from)
      (connects ?r ?from ?to)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
      (not (location-blocked ?from))
      (not (location-blocked ?to))
    )
    :effect (and
      (not (at ?v ?from))
      (at ?v ?to)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-road_0010-tw_00030
    :parameters (?v - vehicle ?r - road_type_road_0010 ?from - loc_type_loc_0007 ?to - loc_type_loc_0092 ?w - window_type_tw_00030)
    :precondition (and
      (at ?v ?from)
      (connects ?r ?from ?to)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
      (not (location-blocked ?from))
      (not (location-blocked ?to))
    )
    :effect (and
      (not (at ?v ?from))
      (at ?v ?to)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-road_0010-tw_00060
    :parameters (?v - vehicle ?r - road_type_road_0010 ?from - loc_type_loc_0007 ?to - loc_type_loc_0092 ?w - window_type_tw_00060)
    :precondition (and
      (at ?v ?from)
      (connects ?r ?from ?to)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
      (not (location-blocked ?from))
      (not (location-blocked ?to))
    )
    :effect (and
      (not (at ?v ?from))
      (at ?v ?to)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-road_0010-tw_00090
    :parameters (?v - vehicle ?r - road_type_road_0010 ?from - loc_type_loc_0007 ?to - loc_type_loc_0092 ?w - window_type_tw_00090)
    :precondition (and
      (at ?v ?from)
      (connects ?r ?from ?to)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
      (not (location-blocked ?from))
      (not (location-blocked ?to))
    )
    :effect (and
      (not (at ?v ?from))
      (at ?v ?to)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-road_0010-tw_00120
    :parameters (?v - vehicle ?r - road_type_road_0010 ?from - loc_type_loc_0007 ?to - loc_type_loc_0092 ?w - window_type_tw_00120)
    :precondition (and
      (at ?v ?from)
      (connects ?r ?from ?to)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
      (not (location-blocked ?from))
      (not (location-blocked ?to))
    )
    :effect (and
      (not (at ?v ?from))
      (at ?v ?to)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-road_0010-tw_00150
    :parameters (?v - vehicle ?r - road_type_road_0010 ?from - loc_type_loc_0007 ?to - loc_type_loc_0092 ?w - window_type_tw_00150)
    :precondition (and
      (at ?v ?from)
      (connects ?r ?from ?to)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
      (not (location-blocked ?from))
      (not (location-blocked ?to))
    )
    :effect (and
      (not (at ?v ?from))
      (at ?v ?to)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-road_0010-tw_00180
    :parameters (?v - vehicle ?r - road_type_road_0010 ?from - loc_type_loc_0007 ?to - loc_type_loc_0092 ?w - window_type_tw_00180)
    :precondition (and
      (at ?v ?from)
      (connects ?r ?from ?to)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
      (not (location-blocked ?from))
      (not (location-blocked ?to))
    )
    :effect (and
      (not (at ?v ?from))
      (at ?v ?to)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-road_0010-tw_00210
    :parameters (?v - vehicle ?r - road_type_road_0010 ?from - loc_type_loc_0007 ?to - loc_type_loc_0092 ?w - window_type_tw_00210)
    :precondition (and
      (at ?v ?from)
      (connects ?r ?from ?to)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
      (not (location-blocked ?from))
      (not (location-blocked ?to))
    )
    :effect (and
      (not (at ?v ?from))
      (at ?v ?to)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-road_0010-tw_00240
    :parameters (?v - vehicle ?r - road_type_road_0010 ?from - loc_type_loc_0007 ?to - loc_type_loc_0092 ?w - window_type_tw_00240)
    :precondition (and
      (at ?v ?from)
      (connects ?r ?from ?to)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
      (not (location-blocked ?from))
      (not (location-blocked ?to))
    )
    :effect (and
      (not (at ?v ?from))
      (at ?v ?to)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-road_0010-tw_00270
    :parameters (?v - vehicle ?r - road_type_road_0010 ?from - loc_type_loc_0007 ?to - loc_type_loc_0092 ?w - window_type_tw_00270)
    :precondition (and
      (at ?v ?from)
      (connects ?r ?from ?to)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
      (not (location-blocked ?from))
      (not (location-blocked ?to))
    )
    :effect (and
      (not (at ?v ?from))
      (at ?v ?to)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-road_0010-tw_00300
    :parameters (?v - vehicle ?r - road_type_road_0010 ?from - loc_type_loc_0007 ?to - loc_type_loc_0092 ?w - window_type_tw_00300)
    :precondition (and
      (at ?v ?from)
      (connects ?r ?from ?to)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
      (not (location-blocked ?from))
      (not (location-blocked ?to))
    )
    :effect (and
      (not (at ?v ?from))
      (at ?v ?to)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-road_0010-tw_00330
    :parameters (?v - vehicle ?r - road_type_road_0010 ?from - loc_type_loc_0007 ?to - loc_type_loc_0092 ?w - window_type_tw_00330)
    :precondition (and
      (at ?v ?from)
      (connects ?r ?from ?to)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
      (not (location-blocked ?from))
      (not (location-blocked ?to))
    )
    :effect (and
      (not (at ?v ?from))
      (at ?v ?to)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-road_0010-tw_00360
    :parameters (?v - vehicle ?r - road_type_road_0010 ?from - loc_type_loc_0007 ?to - loc_type_loc_0092 ?w - window_type_tw_00360)
    :precondition (and
      (at ?v ?from)
      (connects ?r ?from ?to)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
      (not (location-blocked ?from))
      (not (location-blocked ?to))
    )
    :effect (and
      (not (at ?v ?from))
      (at ?v ?to)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-road_0010-tw_00390
    :parameters (?v - vehicle ?r - road_type_road_0010 ?from - loc_type_loc_0007 ?to - loc_type_loc_0092 ?w - window_type_tw_00390)
    :precondition (and
      (at ?v ?from)
      (connects ?r ?from ?to)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
      (not (location-blocked ?from))
      (not (location-blocked ?to))
    )
    :effect (and
      (not (at ?v ?from))
      (at ?v ?to)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-road_0010-tw_00420
    :parameters (?v - vehicle ?r - road_type_road_0010 ?from - loc_type_loc_0007 ?to - loc_type_loc_0092 ?w - window_type_tw_00420)
    :precondition (and
      (at ?v ?from)
      (connects ?r ?from ?to)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
      (not (location-blocked ?from))
      (not (location-blocked ?to))
    )
    :effect (and
      (not (at ?v ?from))
      (at ?v ?to)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-road_0010-tw_00450
    :parameters (?v - vehicle ?r - road_type_road_0010 ?from - loc_type_loc_0007 ?to - loc_type_loc_0092 ?w - window_type_tw_00450)
    :precondition (and
      (at ?v ?from)
      (connects ?r ?from ?to)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
      (not (location-blocked ?from))
      (not (location-blocked ?to))
    )
    :effect (and
      (not (at ?v ?from))
      (at ?v ?to)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-road_0011-tw_00000
    :parameters (?v - vehicle ?r - road_type_road_0011 ?from - loc_type_loc_0008 ?to - loc_type_loc_0055 ?w - window_type_tw_00000)
    :precondition (and
      (at ?v ?from)
      (connects ?r ?from ?to)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
      (not (location-blocked ?from))
      (not (location-blocked ?to))
    )
    :effect (and
      (not (at ?v ?from))
      (at ?v ?to)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-road_0011-tw_00030
    :parameters (?v - vehicle ?r - road_type_road_0011 ?from - loc_type_loc_0008 ?to - loc_type_loc_0055 ?w - window_type_tw_00030)
    :precondition (and
      (at ?v ?from)
      (connects ?r ?from ?to)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
      (not (location-blocked ?from))
      (not (location-blocked ?to))
    )
    :effect (and
      (not (at ?v ?from))
      (at ?v ?to)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-road_0011-tw_00060
    :parameters (?v - vehicle ?r - road_type_road_0011 ?from - loc_type_loc_0008 ?to - loc_type_loc_0055 ?w - window_type_tw_00060)
    :precondition (and
      (at ?v ?from)
      (connects ?r ?from ?to)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
      (not (location-blocked ?from))
      (not (location-blocked ?to))
    )
    :effect (and
      (not (at ?v ?from))
      (at ?v ?to)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-road_0011-tw_00090
    :parameters (?v - vehicle ?r - road_type_road_0011 ?from - loc_type_loc_0008 ?to - loc_type_loc_0055 ?w - window_type_tw_00090)
    :precondition (and
      (at ?v ?from)
      (connects ?r ?from ?to)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
      (not (location-blocked ?from))
      (not (location-blocked ?to))
    )
    :effect (and
      (not (at ?v ?from))
      (at ?v ?to)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-road_0011-tw_00120
    :parameters (?v - vehicle ?r - road_type_road_0011 ?from - loc_type_loc_0008 ?to - loc_type_loc_0055 ?w - window_type_tw_00120)
    :precondition (and
      (at ?v ?from)
      (connects ?r ?from ?to)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
      (not (location-blocked ?from))
      (not (location-blocked ?to))
    )
    :effect (and
      (not (at ?v ?from))
      (at ?v ?to)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-road_0011-tw_00150
    :parameters (?v - vehicle ?r - road_type_road_0011 ?from - loc_type_loc_0008 ?to - loc_type_loc_0055 ?w - window_type_tw_00150)
    :precondition (and
      (at ?v ?from)
      (connects ?r ?from ?to)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
      (not (location-blocked ?from))
      (not (location-blocked ?to))
    )
    :effect (and
      (not (at ?v ?from))
      (at ?v ?to)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-road_0011-tw_00180
    :parameters (?v - vehicle ?r - road_type_road_0011 ?from - loc_type_loc_0008 ?to - loc_type_loc_0055 ?w - window_type_tw_00180)
    :precondition (and
      (at ?v ?from)
      (connects ?r ?from ?to)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
      (not (location-blocked ?from))
      (not (location-blocked ?to))
    )
    :effect (and
      (not (at ?v ?from))
      (at ?v ?to)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-road_0011-tw_00210
    :parameters (?v - vehicle ?r - road_type_road_0011 ?from - loc_type_loc_0008 ?to - loc_type_loc_0055 ?w - window_type_tw_00210)
    :precondition (and
      (at ?v ?from)
      (connects ?r ?from ?to)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
      (not (location-blocked ?from))
      (not (location-blocked ?to))
    )
    :effect (and
      (not (at ?v ?from))
      (at ?v ?to)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-road_0011-tw_00240
    :parameters (?v - vehicle ?r - road_type_road_0011 ?from - loc_type_loc_0008 ?to - loc_type_loc_0055 ?w - window_type_tw_00240)
    :precondition (and
      (at ?v ?from)
      (connects ?r ?from ?to)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
      (not (location-blocked ?from))
      (not (location-blocked ?to))
    )
    :effect (and
      (not (at ?v ?from))
      (at ?v ?to)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-road_0011-tw_00270
    :parameters (?v - vehicle ?r - road_type_road_0011 ?from - loc_type_loc_0008 ?to - loc_type_loc_0055 ?w - window_type_tw_00270)
    :precondition (and
      (at ?v ?from)
      (connects ?r ?from ?to)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
      (not (location-blocked ?from))
      (not (location-blocked ?to))
    )
    :effect (and
      (not (at ?v ?from))
      (at ?v ?to)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-road_0011-tw_00300
    :parameters (?v - vehicle ?r - road_type_road_0011 ?from - loc_type_loc_0008 ?to - loc_type_loc_0055 ?w - window_type_tw_00300)
    :precondition (and
      (at ?v ?from)
      (connects ?r ?from ?to)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
      (not (location-blocked ?from))
      (not (location-blocked ?to))
    )
    :effect (and
      (not (at ?v ?from))
      (at ?v ?to)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-road_0011-tw_00330
    :parameters (?v - vehicle ?r - road_type_road_0011 ?from - loc_type_loc_0008 ?to - loc_type_loc_0055 ?w - window_type_tw_00330)
    :precondition (and
      (at ?v ?from)
      (connects ?r ?from ?to)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
      (not (location-blocked ?from))
      (not (location-blocked ?to))
    )
    :effect (and
      (not (at ?v ?from))
      (at ?v ?to)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-road_0011-tw_00360
    :parameters (?v - vehicle ?r - road_type_road_0011 ?from - loc_type_loc_0008 ?to - loc_type_loc_0055 ?w - window_type_tw_00360)
    :precondition (and
      (at ?v ?from)
      (connects ?r ?from ?to)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
      (not (location-blocked ?from))
      (not (location-blocked ?to))
    )
    :effect (and
      (not (at ?v ?from))
      (at ?v ?to)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-road_0011-tw_00390
    :parameters (?v - vehicle ?r - road_type_road_0011 ?from - loc_type_loc_0008 ?to - loc_type_loc_0055 ?w - window_type_tw_00390)
    :precondition (and
      (at ?v ?from)
      (connects ?r ?from ?to)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
      (not (location-blocked ?from))
      (not (location-blocked ?to))
    )
    :effect (and
      (not (at ?v ?from))
      (at ?v ?to)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-road_0011-tw_00420
    :parameters (?v - vehicle ?r - road_type_road_0011 ?from - loc_type_loc_0008 ?to - loc_type_loc_0055 ?w - window_type_tw_00420)
    :precondition (and
      (at ?v ?from)
      (connects ?r ?from ?to)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
      (not (location-blocked ?from))
      (not (location-blocked ?to))
    )
    :effect (and
      (not (at ?v ?from))
      (at ?v ?to)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-road_0011-tw_00450
    :parameters (?v - vehicle ?r - road_type_road_0011 ?from - loc_type_loc_0008 ?to - loc_type_loc_0055 ?w - window_type_tw_00450)
    :precondition (and
      (at ?v ?from)
      (connects ?r ?from ?to)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
      (not (location-blocked ?from))
      (not (location-blocked ?to))
    )
    :effect (and
      (not (at ?v ?from))
      (at ?v ?to)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-road_0012-tw_00000
    :parameters (?v - vehicle ?r - road_type_road_0012 ?from - loc_type_loc_0009 ?to - loc_type_loc_0081 ?w - window_type_tw_00000)
    :precondition (and
      (at ?v ?from)
      (connects ?r ?from ?to)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
      (not (location-blocked ?from))
      (not (location-blocked ?to))
    )
    :effect (and
      (not (at ?v ?from))
      (at ?v ?to)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-road_0012-tw_00030
    :parameters (?v - vehicle ?r - road_type_road_0012 ?from - loc_type_loc_0009 ?to - loc_type_loc_0081 ?w - window_type_tw_00030)
    :precondition (and
      (at ?v ?from)
      (connects ?r ?from ?to)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
      (not (location-blocked ?from))
      (not (location-blocked ?to))
    )
    :effect (and
      (not (at ?v ?from))
      (at ?v ?to)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-road_0012-tw_00060
    :parameters (?v - vehicle ?r - road_type_road_0012 ?from - loc_type_loc_0009 ?to - loc_type_loc_0081 ?w - window_type_tw_00060)
    :precondition (and
      (at ?v ?from)
      (connects ?r ?from ?to)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
      (not (location-blocked ?from))
      (not (location-blocked ?to))
    )
    :effect (and
      (not (at ?v ?from))
      (at ?v ?to)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-road_0012-tw_00090
    :parameters (?v - vehicle ?r - road_type_road_0012 ?from - loc_type_loc_0009 ?to - loc_type_loc_0081 ?w - window_type_tw_00090)
    :precondition (and
      (at ?v ?from)
      (connects ?r ?from ?to)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
      (not (location-blocked ?from))
      (not (location-blocked ?to))
    )
    :effect (and
      (not (at ?v ?from))
      (at ?v ?to)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-road_0012-tw_00120
    :parameters (?v - vehicle ?r - road_type_road_0012 ?from - loc_type_loc_0009 ?to - loc_type_loc_0081 ?w - window_type_tw_00120)
    :precondition (and
      (at ?v ?from)
      (connects ?r ?from ?to)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
      (not (location-blocked ?from))
      (not (location-blocked ?to))
    )
    :effect (and
      (not (at ?v ?from))
      (at ?v ?to)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-road_0012-tw_00150
    :parameters (?v - vehicle ?r - road_type_road_0012 ?from - loc_type_loc_0009 ?to - loc_type_loc_0081 ?w - window_type_tw_00150)
    :precondition (and
      (at ?v ?from)
      (connects ?r ?from ?to)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
      (not (location-blocked ?from))
      (not (location-blocked ?to))
    )
    :effect (and
      (not (at ?v ?from))
      (at ?v ?to)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-road_0012-tw_00180
    :parameters (?v - vehicle ?r - road_type_road_0012 ?from - loc_type_loc_0009 ?to - loc_type_loc_0081 ?w - window_type_tw_00180)
    :precondition (and
      (at ?v ?from)
      (connects ?r ?from ?to)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
      (not (location-blocked ?from))
      (not (location-blocked ?to))
    )
    :effect (and
      (not (at ?v ?from))
      (at ?v ?to)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-road_0012-tw_00210
    :parameters (?v - vehicle ?r - road_type_road_0012 ?from - loc_type_loc_0009 ?to - loc_type_loc_0081 ?w - window_type_tw_00210)
    :precondition (and
      (at ?v ?from)
      (connects ?r ?from ?to)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
      (not (location-blocked ?from))
      (not (location-blocked ?to))
    )
    :effect (and
      (not (at ?v ?from))
      (at ?v ?to)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-road_0012-tw_00240
    :parameters (?v - vehicle ?r - road_type_road_0012 ?from - loc_type_loc_0009 ?to - loc_type_loc_0081 ?w - window_type_tw_00240)
    :precondition (and
      (at ?v ?from)
      (connects ?r ?from ?to)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
      (not (location-blocked ?from))
      (not (location-blocked ?to))
    )
    :effect (and
      (not (at ?v ?from))
      (at ?v ?to)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-road_0012-tw_00270
    :parameters (?v - vehicle ?r - road_type_road_0012 ?from - loc_type_loc_0009 ?to - loc_type_loc_0081 ?w - window_type_tw_00270)
    :precondition (and
      (at ?v ?from)
      (connects ?r ?from ?to)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
      (not (location-blocked ?from))
      (not (location-blocked ?to))
    )
    :effect (and
      (not (at ?v ?from))
      (at ?v ?to)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-road_0012-tw_00300
    :parameters (?v - vehicle ?r - road_type_road_0012 ?from - loc_type_loc_0009 ?to - loc_type_loc_0081 ?w - window_type_tw_00300)
    :precondition (and
      (at ?v ?from)
      (connects ?r ?from ?to)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
      (not (location-blocked ?from))
      (not (location-blocked ?to))
    )
    :effect (and
      (not (at ?v ?from))
      (at ?v ?to)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-road_0012-tw_00330
    :parameters (?v - vehicle ?r - road_type_road_0012 ?from - loc_type_loc_0009 ?to - loc_type_loc_0081 ?w - window_type_tw_00330)
    :precondition (and
      (at ?v ?from)
      (connects ?r ?from ?to)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
      (not (location-blocked ?from))
      (not (location-blocked ?to))
    )
    :effect (and
      (not (at ?v ?from))
      (at ?v ?to)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-road_0012-tw_00360
    :parameters (?v - vehicle ?r - road_type_road_0012 ?from - loc_type_loc_0009 ?to - loc_type_loc_0081 ?w - window_type_tw_00360)
    :precondition (and
      (at ?v ?from)
      (connects ?r ?from ?to)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
      (not (location-blocked ?from))
      (not (location-blocked ?to))
    )
    :effect (and
      (not (at ?v ?from))
      (at ?v ?to)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-road_0012-tw_00390
    :parameters (?v - vehicle ?r - road_type_road_0012 ?from - loc_type_loc_0009 ?to - loc_type_loc_0081 ?w - window_type_tw_00390)
    :precondition (and
      (at ?v ?from)
      (connects ?r ?from ?to)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
      (not (location-blocked ?from))
      (not (location-blocked ?to))
    )
    :effect (and
      (not (at ?v ?from))
      (at ?v ?to)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-road_0012-tw_00420
    :parameters (?v - vehicle ?r - road_type_road_0012 ?from - loc_type_loc_0009 ?to - loc_type_loc_0081 ?w - window_type_tw_00420)
    :precondition (and
      (at ?v ?from)
      (connects ?r ?from ?to)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
      (not (location-blocked ?from))
      (not (location-blocked ?to))
    )
    :effect (and
      (not (at ?v ?from))
      (at ?v ?to)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-road_0012-tw_00450
    :parameters (?v - vehicle ?r - road_type_road_0012 ?from - loc_type_loc_0009 ?to - loc_type_loc_0081 ?w - window_type_tw_00450)
    :precondition (and
      (at ?v ?from)
      (connects ?r ?from ?to)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
      (not (location-blocked ?from))
      (not (location-blocked ?to))
    )
    :effect (and
      (not (at ?v ?from))
      (at ?v ?to)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-road_0013-tw_00000
    :parameters (?v - vehicle ?r - road_type_road_0013 ?from - loc_type_loc_0009 ?to - loc_type_loc_0080 ?w - window_type_tw_00000)
    :precondition (and
      (at ?v ?from)
      (connects ?r ?from ?to)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
      (not (location-blocked ?from))
      (not (location-blocked ?to))
    )
    :effect (and
      (not (at ?v ?from))
      (at ?v ?to)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-road_0013-tw_00030
    :parameters (?v - vehicle ?r - road_type_road_0013 ?from - loc_type_loc_0009 ?to - loc_type_loc_0080 ?w - window_type_tw_00030)
    :precondition (and
      (at ?v ?from)
      (connects ?r ?from ?to)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
      (not (location-blocked ?from))
      (not (location-blocked ?to))
    )
    :effect (and
      (not (at ?v ?from))
      (at ?v ?to)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-road_0013-tw_00060
    :parameters (?v - vehicle ?r - road_type_road_0013 ?from - loc_type_loc_0009 ?to - loc_type_loc_0080 ?w - window_type_tw_00060)
    :precondition (and
      (at ?v ?from)
      (connects ?r ?from ?to)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
      (not (location-blocked ?from))
      (not (location-blocked ?to))
    )
    :effect (and
      (not (at ?v ?from))
      (at ?v ?to)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-road_0013-tw_00090
    :parameters (?v - vehicle ?r - road_type_road_0013 ?from - loc_type_loc_0009 ?to - loc_type_loc_0080 ?w - window_type_tw_00090)
    :precondition (and
      (at ?v ?from)
      (connects ?r ?from ?to)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
      (not (location-blocked ?from))
      (not (location-blocked ?to))
    )
    :effect (and
      (not (at ?v ?from))
      (at ?v ?to)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-road_0013-tw_00120
    :parameters (?v - vehicle ?r - road_type_road_0013 ?from - loc_type_loc_0009 ?to - loc_type_loc_0080 ?w - window_type_tw_00120)
    :precondition (and
      (at ?v ?from)
      (connects ?r ?from ?to)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
      (not (location-blocked ?from))
      (not (location-blocked ?to))
    )
    :effect (and
      (not (at ?v ?from))
      (at ?v ?to)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-road_0013-tw_00150
    :parameters (?v - vehicle ?r - road_type_road_0013 ?from - loc_type_loc_0009 ?to - loc_type_loc_0080 ?w - window_type_tw_00150)
    :precondition (and
      (at ?v ?from)
      (connects ?r ?from ?to)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
      (not (location-blocked ?from))
      (not (location-blocked ?to))
    )
    :effect (and
      (not (at ?v ?from))
      (at ?v ?to)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-road_0013-tw_00180
    :parameters (?v - vehicle ?r - road_type_road_0013 ?from - loc_type_loc_0009 ?to - loc_type_loc_0080 ?w - window_type_tw_00180)
    :precondition (and
      (at ?v ?from)
      (connects ?r ?from ?to)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
      (not (location-blocked ?from))
      (not (location-blocked ?to))
    )
    :effect (and
      (not (at ?v ?from))
      (at ?v ?to)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-road_0013-tw_00210
    :parameters (?v - vehicle ?r - road_type_road_0013 ?from - loc_type_loc_0009 ?to - loc_type_loc_0080 ?w - window_type_tw_00210)
    :precondition (and
      (at ?v ?from)
      (connects ?r ?from ?to)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
      (not (location-blocked ?from))
      (not (location-blocked ?to))
    )
    :effect (and
      (not (at ?v ?from))
      (at ?v ?to)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-road_0013-tw_00240
    :parameters (?v - vehicle ?r - road_type_road_0013 ?from - loc_type_loc_0009 ?to - loc_type_loc_0080 ?w - window_type_tw_00240)
    :precondition (and
      (at ?v ?from)
      (connects ?r ?from ?to)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
      (not (location-blocked ?from))
      (not (location-blocked ?to))
    )
    :effect (and
      (not (at ?v ?from))
      (at ?v ?to)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-road_0013-tw_00270
    :parameters (?v - vehicle ?r - road_type_road_0013 ?from - loc_type_loc_0009 ?to - loc_type_loc_0080 ?w - window_type_tw_00270)
    :precondition (and
      (at ?v ?from)
      (connects ?r ?from ?to)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
      (not (location-blocked ?from))
      (not (location-blocked ?to))
    )
    :effect (and
      (not (at ?v ?from))
      (at ?v ?to)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-road_0013-tw_00300
    :parameters (?v - vehicle ?r - road_type_road_0013 ?from - loc_type_loc_0009 ?to - loc_type_loc_0080 ?w - window_type_tw_00300)
    :precondition (and
      (at ?v ?from)
      (connects ?r ?from ?to)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
      (not (location-blocked ?from))
      (not (location-blocked ?to))
    )
    :effect (and
      (not (at ?v ?from))
      (at ?v ?to)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-road_0013-tw_00330
    :parameters (?v - vehicle ?r - road_type_road_0013 ?from - loc_type_loc_0009 ?to - loc_type_loc_0080 ?w - window_type_tw_00330)
    :precondition (and
      (at ?v ?from)
      (connects ?r ?from ?to)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
      (not (location-blocked ?from))
      (not (location-blocked ?to))
    )
    :effect (and
      (not (at ?v ?from))
      (at ?v ?to)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-road_0013-tw_00360
    :parameters (?v - vehicle ?r - road_type_road_0013 ?from - loc_type_loc_0009 ?to - loc_type_loc_0080 ?w - window_type_tw_00360)
    :precondition (and
      (at ?v ?from)
      (connects ?r ?from ?to)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
      (not (location-blocked ?from))
      (not (location-blocked ?to))
    )
    :effect (and
      (not (at ?v ?from))
      (at ?v ?to)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-road_0013-tw_00390
    :parameters (?v - vehicle ?r - road_type_road_0013 ?from - loc_type_loc_0009 ?to - loc_type_loc_0080 ?w - window_type_tw_00390)
    :precondition (and
      (at ?v ?from)
      (connects ?r ?from ?to)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
      (not (location-blocked ?from))
      (not (location-blocked ?to))
    )
    :effect (and
      (not (at ?v ?from))
      (at ?v ?to)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-road_0013-tw_00420
    :parameters (?v - vehicle ?r - road_type_road_0013 ?from - loc_type_loc_0009 ?to - loc_type_loc_0080 ?w - window_type_tw_00420)
    :precondition (and
      (at ?v ?from)
      (connects ?r ?from ?to)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
      (not (location-blocked ?from))
      (not (location-blocked ?to))
    )
    :effect (and
      (not (at ?v ?from))
      (at ?v ?to)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-road_0013-tw_00450
    :parameters (?v - vehicle ?r - road_type_road_0013 ?from - loc_type_loc_0009 ?to - loc_type_loc_0080 ?w - window_type_tw_00450)
    :precondition (and
      (at ?v ?from)
      (connects ?r ?from ?to)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
      (not (location-blocked ?from))
      (not (location-blocked ?to))
    )
    :effect (and
      (not (at ?v ?from))
      (at ?v ?to)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-road_0014-tw_00000
    :parameters (?v - vehicle ?r - road_type_road_0014 ?from - loc_type_loc_0009 ?to - loc_type_loc_0053 ?w - window_type_tw_00000)
    :precondition (and
      (at ?v ?from)
      (connects ?r ?from ?to)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
      (not (location-blocked ?from))
      (not (location-blocked ?to))
    )
    :effect (and
      (not (at ?v ?from))
      (at ?v ?to)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-road_0014-tw_00030
    :parameters (?v - vehicle ?r - road_type_road_0014 ?from - loc_type_loc_0009 ?to - loc_type_loc_0053 ?w - window_type_tw_00030)
    :precondition (and
      (at ?v ?from)
      (connects ?r ?from ?to)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
      (not (location-blocked ?from))
      (not (location-blocked ?to))
    )
    :effect (and
      (not (at ?v ?from))
      (at ?v ?to)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-road_0014-tw_00060
    :parameters (?v - vehicle ?r - road_type_road_0014 ?from - loc_type_loc_0009 ?to - loc_type_loc_0053 ?w - window_type_tw_00060)
    :precondition (and
      (at ?v ?from)
      (connects ?r ?from ?to)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
      (not (location-blocked ?from))
      (not (location-blocked ?to))
    )
    :effect (and
      (not (at ?v ?from))
      (at ?v ?to)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-road_0014-tw_00090
    :parameters (?v - vehicle ?r - road_type_road_0014 ?from - loc_type_loc_0009 ?to - loc_type_loc_0053 ?w - window_type_tw_00090)
    :precondition (and
      (at ?v ?from)
      (connects ?r ?from ?to)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
      (not (location-blocked ?from))
      (not (location-blocked ?to))
    )
    :effect (and
      (not (at ?v ?from))
      (at ?v ?to)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-road_0014-tw_00120
    :parameters (?v - vehicle ?r - road_type_road_0014 ?from - loc_type_loc_0009 ?to - loc_type_loc_0053 ?w - window_type_tw_00120)
    :precondition (and
      (at ?v ?from)
      (connects ?r ?from ?to)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
      (not (location-blocked ?from))
      (not (location-blocked ?to))
    )
    :effect (and
      (not (at ?v ?from))
      (at ?v ?to)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-road_0014-tw_00150
    :parameters (?v - vehicle ?r - road_type_road_0014 ?from - loc_type_loc_0009 ?to - loc_type_loc_0053 ?w - window_type_tw_00150)
    :precondition (and
      (at ?v ?from)
      (connects ?r ?from ?to)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
      (not (location-blocked ?from))
      (not (location-blocked ?to))
    )
    :effect (and
      (not (at ?v ?from))
      (at ?v ?to)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-road_0014-tw_00180
    :parameters (?v - vehicle ?r - road_type_road_0014 ?from - loc_type_loc_0009 ?to - loc_type_loc_0053 ?w - window_type_tw_00180)
    :precondition (and
      (at ?v ?from)
      (connects ?r ?from ?to)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
      (not (location-blocked ?from))
      (not (location-blocked ?to))
    )
    :effect (and
      (not (at ?v ?from))
      (at ?v ?to)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-road_0014-tw_00210
    :parameters (?v - vehicle ?r - road_type_road_0014 ?from - loc_type_loc_0009 ?to - loc_type_loc_0053 ?w - window_type_tw_00210)
    :precondition (and
      (at ?v ?from)
      (connects ?r ?from ?to)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
      (not (location-blocked ?from))
      (not (location-blocked ?to))
    )
    :effect (and
      (not (at ?v ?from))
      (at ?v ?to)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-road_0014-tw_00240
    :parameters (?v - vehicle ?r - road_type_road_0014 ?from - loc_type_loc_0009 ?to - loc_type_loc_0053 ?w - window_type_tw_00240)
    :precondition (and
      (at ?v ?from)
      (connects ?r ?from ?to)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
      (not (location-blocked ?from))
      (not (location-blocked ?to))
    )
    :effect (and
      (not (at ?v ?from))
      (at ?v ?to)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-road_0014-tw_00270
    :parameters (?v - vehicle ?r - road_type_road_0014 ?from - loc_type_loc_0009 ?to - loc_type_loc_0053 ?w - window_type_tw_00270)
    :precondition (and
      (at ?v ?from)
      (connects ?r ?from ?to)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
      (not (location-blocked ?from))
      (not (location-blocked ?to))
    )
    :effect (and
      (not (at ?v ?from))
      (at ?v ?to)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-road_0014-tw_00300
    :parameters (?v - vehicle ?r - road_type_road_0014 ?from - loc_type_loc_0009 ?to - loc_type_loc_0053 ?w - window_type_tw_00300)
    :precondition (and
      (at ?v ?from)
      (connects ?r ?from ?to)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
      (not (location-blocked ?from))
      (not (location-blocked ?to))
    )
    :effect (and
      (not (at ?v ?from))
      (at ?v ?to)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-road_0014-tw_00330
    :parameters (?v - vehicle ?r - road_type_road_0014 ?from - loc_type_loc_0009 ?to - loc_type_loc_0053 ?w - window_type_tw_00330)
    :precondition (and
      (at ?v ?from)
      (connects ?r ?from ?to)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
      (not (location-blocked ?from))
      (not (location-blocked ?to))
    )
    :effect (and
      (not (at ?v ?from))
      (at ?v ?to)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-road_0014-tw_00360
    :parameters (?v - vehicle ?r - road_type_road_0014 ?from - loc_type_loc_0009 ?to - loc_type_loc_0053 ?w - window_type_tw_00360)
    :precondition (and
      (at ?v ?from)
      (connects ?r ?from ?to)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
      (not (location-blocked ?from))
      (not (location-blocked ?to))
    )
    :effect (and
      (not (at ?v ?from))
      (at ?v ?to)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-road_0014-tw_00390
    :parameters (?v - vehicle ?r - road_type_road_0014 ?from - loc_type_loc_0009 ?to - loc_type_loc_0053 ?w - window_type_tw_00390)
    :precondition (and
      (at ?v ?from)
      (connects ?r ?from ?to)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
      (not (location-blocked ?from))
      (not (location-blocked ?to))
    )
    :effect (and
      (not (at ?v ?from))
      (at ?v ?to)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-road_0014-tw_00420
    :parameters (?v - vehicle ?r - road_type_road_0014 ?from - loc_type_loc_0009 ?to - loc_type_loc_0053 ?w - window_type_tw_00420)
    :precondition (and
      (at ?v ?from)
      (connects ?r ?from ?to)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
      (not (location-blocked ?from))
      (not (location-blocked ?to))
    )
    :effect (and
      (not (at ?v ?from))
      (at ?v ?to)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-road_0014-tw_00450
    :parameters (?v - vehicle ?r - road_type_road_0014 ?from - loc_type_loc_0009 ?to - loc_type_loc_0053 ?w - window_type_tw_00450)
    :precondition (and
      (at ?v ?from)
      (connects ?r ?from ?to)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
      (not (location-blocked ?from))
      (not (location-blocked ?to))
    )
    :effect (and
      (not (at ?v ?from))
      (at ?v ?to)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-road_0015-tw_00000
    :parameters (?v - vehicle ?r - road_type_road_0015 ?from - loc_type_loc_0010 ?to - loc_type_loc_0059 ?w - window_type_tw_00000)
    :precondition (and
      (at ?v ?from)
      (connects ?r ?from ?to)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
      (not (location-blocked ?from))
      (not (location-blocked ?to))
    )
    :effect (and
      (not (at ?v ?from))
      (at ?v ?to)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-road_0015-tw_00030
    :parameters (?v - vehicle ?r - road_type_road_0015 ?from - loc_type_loc_0010 ?to - loc_type_loc_0059 ?w - window_type_tw_00030)
    :precondition (and
      (at ?v ?from)
      (connects ?r ?from ?to)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
      (not (location-blocked ?from))
      (not (location-blocked ?to))
    )
    :effect (and
      (not (at ?v ?from))
      (at ?v ?to)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-road_0015-tw_00060
    :parameters (?v - vehicle ?r - road_type_road_0015 ?from - loc_type_loc_0010 ?to - loc_type_loc_0059 ?w - window_type_tw_00060)
    :precondition (and
      (at ?v ?from)
      (connects ?r ?from ?to)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
      (not (location-blocked ?from))
      (not (location-blocked ?to))
    )
    :effect (and
      (not (at ?v ?from))
      (at ?v ?to)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-road_0015-tw_00090
    :parameters (?v - vehicle ?r - road_type_road_0015 ?from - loc_type_loc_0010 ?to - loc_type_loc_0059 ?w - window_type_tw_00090)
    :precondition (and
      (at ?v ?from)
      (connects ?r ?from ?to)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
      (not (location-blocked ?from))
      (not (location-blocked ?to))
    )
    :effect (and
      (not (at ?v ?from))
      (at ?v ?to)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-road_0015-tw_00120
    :parameters (?v - vehicle ?r - road_type_road_0015 ?from - loc_type_loc_0010 ?to - loc_type_loc_0059 ?w - window_type_tw_00120)
    :precondition (and
      (at ?v ?from)
      (connects ?r ?from ?to)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
      (not (location-blocked ?from))
      (not (location-blocked ?to))
    )
    :effect (and
      (not (at ?v ?from))
      (at ?v ?to)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-road_0015-tw_00150
    :parameters (?v - vehicle ?r - road_type_road_0015 ?from - loc_type_loc_0010 ?to - loc_type_loc_0059 ?w - window_type_tw_00150)
    :precondition (and
      (at ?v ?from)
      (connects ?r ?from ?to)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
      (not (location-blocked ?from))
      (not (location-blocked ?to))
    )
    :effect (and
      (not (at ?v ?from))
      (at ?v ?to)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-road_0015-tw_00180
    :parameters (?v - vehicle ?r - road_type_road_0015 ?from - loc_type_loc_0010 ?to - loc_type_loc_0059 ?w - window_type_tw_00180)
    :precondition (and
      (at ?v ?from)
      (connects ?r ?from ?to)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
      (not (location-blocked ?from))
      (not (location-blocked ?to))
    )
    :effect (and
      (not (at ?v ?from))
      (at ?v ?to)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-road_0015-tw_00210
    :parameters (?v - vehicle ?r - road_type_road_0015 ?from - loc_type_loc_0010 ?to - loc_type_loc_0059 ?w - window_type_tw_00210)
    :precondition (and
      (at ?v ?from)
      (connects ?r ?from ?to)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
      (not (location-blocked ?from))
      (not (location-blocked ?to))
    )
    :effect (and
      (not (at ?v ?from))
      (at ?v ?to)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-road_0015-tw_00240
    :parameters (?v - vehicle ?r - road_type_road_0015 ?from - loc_type_loc_0010 ?to - loc_type_loc_0059 ?w - window_type_tw_00240)
    :precondition (and
      (at ?v ?from)
      (connects ?r ?from ?to)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
      (not (location-blocked ?from))
      (not (location-blocked ?to))
    )
    :effect (and
      (not (at ?v ?from))
      (at ?v ?to)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-road_0015-tw_00270
    :parameters (?v - vehicle ?r - road_type_road_0015 ?from - loc_type_loc_0010 ?to - loc_type_loc_0059 ?w - window_type_tw_00270)
    :precondition (and
      (at ?v ?from)
      (connects ?r ?from ?to)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
      (not (location-blocked ?from))
      (not (location-blocked ?to))
    )
    :effect (and
      (not (at ?v ?from))
      (at ?v ?to)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-road_0015-tw_00300
    :parameters (?v - vehicle ?r - road_type_road_0015 ?from - loc_type_loc_0010 ?to - loc_type_loc_0059 ?w - window_type_tw_00300)
    :precondition (and
      (at ?v ?from)
      (connects ?r ?from ?to)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
      (not (location-blocked ?from))
      (not (location-blocked ?to))
    )
    :effect (and
      (not (at ?v ?from))
      (at ?v ?to)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-road_0015-tw_00330
    :parameters (?v - vehicle ?r - road_type_road_0015 ?from - loc_type_loc_0010 ?to - loc_type_loc_0059 ?w - window_type_tw_00330)
    :precondition (and
      (at ?v ?from)
      (connects ?r ?from ?to)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
      (not (location-blocked ?from))
      (not (location-blocked ?to))
    )
    :effect (and
      (not (at ?v ?from))
      (at ?v ?to)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-road_0015-tw_00360
    :parameters (?v - vehicle ?r - road_type_road_0015 ?from - loc_type_loc_0010 ?to - loc_type_loc_0059 ?w - window_type_tw_00360)
    :precondition (and
      (at ?v ?from)
      (connects ?r ?from ?to)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
      (not (location-blocked ?from))
      (not (location-blocked ?to))
    )
    :effect (and
      (not (at ?v ?from))
      (at ?v ?to)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-road_0015-tw_00390
    :parameters (?v - vehicle ?r - road_type_road_0015 ?from - loc_type_loc_0010 ?to - loc_type_loc_0059 ?w - window_type_tw_00390)
    :precondition (and
      (at ?v ?from)
      (connects ?r ?from ?to)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
      (not (location-blocked ?from))
      (not (location-blocked ?to))
    )
    :effect (and
      (not (at ?v ?from))
      (at ?v ?to)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-road_0015-tw_00420
    :parameters (?v - vehicle ?r - road_type_road_0015 ?from - loc_type_loc_0010 ?to - loc_type_loc_0059 ?w - window_type_tw_00420)
    :precondition (and
      (at ?v ?from)
      (connects ?r ?from ?to)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
      (not (location-blocked ?from))
      (not (location-blocked ?to))
    )
    :effect (and
      (not (at ?v ?from))
      (at ?v ?to)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-road_0015-tw_00450
    :parameters (?v - vehicle ?r - road_type_road_0015 ?from - loc_type_loc_0010 ?to - loc_type_loc_0059 ?w - window_type_tw_00450)
    :precondition (and
      (at ?v ?from)
      (connects ?r ?from ?to)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
      (not (location-blocked ?from))
      (not (location-blocked ?to))
    )
    :effect (and
      (not (at ?v ?from))
      (at ?v ?to)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-road_0017-tw_00000
    :parameters (?v - vehicle ?r - road_type_road_0017 ?from - loc_type_loc_0012 ?to - loc_type_loc_0013 ?w - window_type_tw_00000)
    :precondition (and
      (at ?v ?from)
      (connects ?r ?from ?to)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
      (not (location-blocked ?from))
      (not (location-blocked ?to))
    )
    :effect (and
      (not (at ?v ?from))
      (at ?v ?to)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-road_0017-tw_00030
    :parameters (?v - vehicle ?r - road_type_road_0017 ?from - loc_type_loc_0012 ?to - loc_type_loc_0013 ?w - window_type_tw_00030)
    :precondition (and
      (at ?v ?from)
      (connects ?r ?from ?to)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
      (not (location-blocked ?from))
      (not (location-blocked ?to))
    )
    :effect (and
      (not (at ?v ?from))
      (at ?v ?to)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-road_0017-tw_00060
    :parameters (?v - vehicle ?r - road_type_road_0017 ?from - loc_type_loc_0012 ?to - loc_type_loc_0013 ?w - window_type_tw_00060)
    :precondition (and
      (at ?v ?from)
      (connects ?r ?from ?to)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
      (not (location-blocked ?from))
      (not (location-blocked ?to))
    )
    :effect (and
      (not (at ?v ?from))
      (at ?v ?to)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-road_0017-tw_00090
    :parameters (?v - vehicle ?r - road_type_road_0017 ?from - loc_type_loc_0012 ?to - loc_type_loc_0013 ?w - window_type_tw_00090)
    :precondition (and
      (at ?v ?from)
      (connects ?r ?from ?to)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
      (not (location-blocked ?from))
      (not (location-blocked ?to))
    )
    :effect (and
      (not (at ?v ?from))
      (at ?v ?to)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-road_0017-tw_00120
    :parameters (?v - vehicle ?r - road_type_road_0017 ?from - loc_type_loc_0012 ?to - loc_type_loc_0013 ?w - window_type_tw_00120)
    :precondition (and
      (at ?v ?from)
      (connects ?r ?from ?to)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
      (not (location-blocked ?from))
      (not (location-blocked ?to))
    )
    :effect (and
      (not (at ?v ?from))
      (at ?v ?to)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-road_0017-tw_00150
    :parameters (?v - vehicle ?r - road_type_road_0017 ?from - loc_type_loc_0012 ?to - loc_type_loc_0013 ?w - window_type_tw_00150)
    :precondition (and
      (at ?v ?from)
      (connects ?r ?from ?to)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
      (not (location-blocked ?from))
      (not (location-blocked ?to))
    )
    :effect (and
      (not (at ?v ?from))
      (at ?v ?to)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-road_0017-tw_00180
    :parameters (?v - vehicle ?r - road_type_road_0017 ?from - loc_type_loc_0012 ?to - loc_type_loc_0013 ?w - window_type_tw_00180)
    :precondition (and
      (at ?v ?from)
      (connects ?r ?from ?to)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
      (not (location-blocked ?from))
      (not (location-blocked ?to))
    )
    :effect (and
      (not (at ?v ?from))
      (at ?v ?to)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-road_0017-tw_00210
    :parameters (?v - vehicle ?r - road_type_road_0017 ?from - loc_type_loc_0012 ?to - loc_type_loc_0013 ?w - window_type_tw_00210)
    :precondition (and
      (at ?v ?from)
      (connects ?r ?from ?to)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
      (not (location-blocked ?from))
      (not (location-blocked ?to))
    )
    :effect (and
      (not (at ?v ?from))
      (at ?v ?to)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-road_0017-tw_00240
    :parameters (?v - vehicle ?r - road_type_road_0017 ?from - loc_type_loc_0012 ?to - loc_type_loc_0013 ?w - window_type_tw_00240)
    :precondition (and
      (at ?v ?from)
      (connects ?r ?from ?to)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
      (not (location-blocked ?from))
      (not (location-blocked ?to))
    )
    :effect (and
      (not (at ?v ?from))
      (at ?v ?to)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-road_0017-tw_00270
    :parameters (?v - vehicle ?r - road_type_road_0017 ?from - loc_type_loc_0012 ?to - loc_type_loc_0013 ?w - window_type_tw_00270)
    :precondition (and
      (at ?v ?from)
      (connects ?r ?from ?to)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
      (not (location-blocked ?from))
      (not (location-blocked ?to))
    )
    :effect (and
      (not (at ?v ?from))
      (at ?v ?to)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-road_0017-tw_00300
    :parameters (?v - vehicle ?r - road_type_road_0017 ?from - loc_type_loc_0012 ?to - loc_type_loc_0013 ?w - window_type_tw_00300)
    :precondition (and
      (at ?v ?from)
      (connects ?r ?from ?to)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
      (not (location-blocked ?from))
      (not (location-blocked ?to))
    )
    :effect (and
      (not (at ?v ?from))
      (at ?v ?to)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-road_0017-tw_00330
    :parameters (?v - vehicle ?r - road_type_road_0017 ?from - loc_type_loc_0012 ?to - loc_type_loc_0013 ?w - window_type_tw_00330)
    :precondition (and
      (at ?v ?from)
      (connects ?r ?from ?to)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
      (not (location-blocked ?from))
      (not (location-blocked ?to))
    )
    :effect (and
      (not (at ?v ?from))
      (at ?v ?to)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-road_0017-tw_00360
    :parameters (?v - vehicle ?r - road_type_road_0017 ?from - loc_type_loc_0012 ?to - loc_type_loc_0013 ?w - window_type_tw_00360)
    :precondition (and
      (at ?v ?from)
      (connects ?r ?from ?to)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
      (not (location-blocked ?from))
      (not (location-blocked ?to))
    )
    :effect (and
      (not (at ?v ?from))
      (at ?v ?to)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-road_0017-tw_00390
    :parameters (?v - vehicle ?r - road_type_road_0017 ?from - loc_type_loc_0012 ?to - loc_type_loc_0013 ?w - window_type_tw_00390)
    :precondition (and
      (at ?v ?from)
      (connects ?r ?from ?to)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
      (not (location-blocked ?from))
      (not (location-blocked ?to))
    )
    :effect (and
      (not (at ?v ?from))
      (at ?v ?to)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-road_0017-tw_00420
    :parameters (?v - vehicle ?r - road_type_road_0017 ?from - loc_type_loc_0012 ?to - loc_type_loc_0013 ?w - window_type_tw_00420)
    :precondition (and
      (at ?v ?from)
      (connects ?r ?from ?to)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
      (not (location-blocked ?from))
      (not (location-blocked ?to))
    )
    :effect (and
      (not (at ?v ?from))
      (at ?v ?to)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-road_0017-tw_00450
    :parameters (?v - vehicle ?r - road_type_road_0017 ?from - loc_type_loc_0012 ?to - loc_type_loc_0013 ?w - window_type_tw_00450)
    :precondition (and
      (at ?v ?from)
      (connects ?r ?from ?to)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
      (not (location-blocked ?from))
      (not (location-blocked ?to))
    )
    :effect (and
      (not (at ?v ?from))
      (at ?v ?to)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-road_0018-tw_00000
    :parameters (?v - vehicle ?r - road_type_road_0018 ?from - loc_type_loc_0012 ?to - loc_type_loc_0018 ?w - window_type_tw_00000)
    :precondition (and
      (at ?v ?from)
      (connects ?r ?from ?to)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
      (not (location-blocked ?from))
      (not (location-blocked ?to))
    )
    :effect (and
      (not (at ?v ?from))
      (at ?v ?to)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-road_0018-tw_00030
    :parameters (?v - vehicle ?r - road_type_road_0018 ?from - loc_type_loc_0012 ?to - loc_type_loc_0018 ?w - window_type_tw_00030)
    :precondition (and
      (at ?v ?from)
      (connects ?r ?from ?to)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
      (not (location-blocked ?from))
      (not (location-blocked ?to))
    )
    :effect (and
      (not (at ?v ?from))
      (at ?v ?to)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-road_0018-tw_00060
    :parameters (?v - vehicle ?r - road_type_road_0018 ?from - loc_type_loc_0012 ?to - loc_type_loc_0018 ?w - window_type_tw_00060)
    :precondition (and
      (at ?v ?from)
      (connects ?r ?from ?to)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
      (not (location-blocked ?from))
      (not (location-blocked ?to))
    )
    :effect (and
      (not (at ?v ?from))
      (at ?v ?to)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-road_0018-tw_00090
    :parameters (?v - vehicle ?r - road_type_road_0018 ?from - loc_type_loc_0012 ?to - loc_type_loc_0018 ?w - window_type_tw_00090)
    :precondition (and
      (at ?v ?from)
      (connects ?r ?from ?to)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
      (not (location-blocked ?from))
      (not (location-blocked ?to))
    )
    :effect (and
      (not (at ?v ?from))
      (at ?v ?to)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-road_0018-tw_00120
    :parameters (?v - vehicle ?r - road_type_road_0018 ?from - loc_type_loc_0012 ?to - loc_type_loc_0018 ?w - window_type_tw_00120)
    :precondition (and
      (at ?v ?from)
      (connects ?r ?from ?to)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
      (not (location-blocked ?from))
      (not (location-blocked ?to))
    )
    :effect (and
      (not (at ?v ?from))
      (at ?v ?to)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-road_0018-tw_00150
    :parameters (?v - vehicle ?r - road_type_road_0018 ?from - loc_type_loc_0012 ?to - loc_type_loc_0018 ?w - window_type_tw_00150)
    :precondition (and
      (at ?v ?from)
      (connects ?r ?from ?to)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
      (not (location-blocked ?from))
      (not (location-blocked ?to))
    )
    :effect (and
      (not (at ?v ?from))
      (at ?v ?to)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-road_0018-tw_00180
    :parameters (?v - vehicle ?r - road_type_road_0018 ?from - loc_type_loc_0012 ?to - loc_type_loc_0018 ?w - window_type_tw_00180)
    :precondition (and
      (at ?v ?from)
      (connects ?r ?from ?to)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
      (not (location-blocked ?from))
      (not (location-blocked ?to))
    )
    :effect (and
      (not (at ?v ?from))
      (at ?v ?to)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-road_0018-tw_00210
    :parameters (?v - vehicle ?r - road_type_road_0018 ?from - loc_type_loc_0012 ?to - loc_type_loc_0018 ?w - window_type_tw_00210)
    :precondition (and
      (at ?v ?from)
      (connects ?r ?from ?to)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
      (not (location-blocked ?from))
      (not (location-blocked ?to))
    )
    :effect (and
      (not (at ?v ?from))
      (at ?v ?to)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-road_0018-tw_00240
    :parameters (?v - vehicle ?r - road_type_road_0018 ?from - loc_type_loc_0012 ?to - loc_type_loc_0018 ?w - window_type_tw_00240)
    :precondition (and
      (at ?v ?from)
      (connects ?r ?from ?to)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
      (not (location-blocked ?from))
      (not (location-blocked ?to))
    )
    :effect (and
      (not (at ?v ?from))
      (at ?v ?to)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-road_0018-tw_00270
    :parameters (?v - vehicle ?r - road_type_road_0018 ?from - loc_type_loc_0012 ?to - loc_type_loc_0018 ?w - window_type_tw_00270)
    :precondition (and
      (at ?v ?from)
      (connects ?r ?from ?to)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
      (not (location-blocked ?from))
      (not (location-blocked ?to))
    )
    :effect (and
      (not (at ?v ?from))
      (at ?v ?to)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-road_0018-tw_00300
    :parameters (?v - vehicle ?r - road_type_road_0018 ?from - loc_type_loc_0012 ?to - loc_type_loc_0018 ?w - window_type_tw_00300)
    :precondition (and
      (at ?v ?from)
      (connects ?r ?from ?to)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
      (not (location-blocked ?from))
      (not (location-blocked ?to))
    )
    :effect (and
      (not (at ?v ?from))
      (at ?v ?to)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-road_0018-tw_00330
    :parameters (?v - vehicle ?r - road_type_road_0018 ?from - loc_type_loc_0012 ?to - loc_type_loc_0018 ?w - window_type_tw_00330)
    :precondition (and
      (at ?v ?from)
      (connects ?r ?from ?to)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
      (not (location-blocked ?from))
      (not (location-blocked ?to))
    )
    :effect (and
      (not (at ?v ?from))
      (at ?v ?to)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-road_0018-tw_00360
    :parameters (?v - vehicle ?r - road_type_road_0018 ?from - loc_type_loc_0012 ?to - loc_type_loc_0018 ?w - window_type_tw_00360)
    :precondition (and
      (at ?v ?from)
      (connects ?r ?from ?to)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
      (not (location-blocked ?from))
      (not (location-blocked ?to))
    )
    :effect (and
      (not (at ?v ?from))
      (at ?v ?to)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-road_0018-tw_00390
    :parameters (?v - vehicle ?r - road_type_road_0018 ?from - loc_type_loc_0012 ?to - loc_type_loc_0018 ?w - window_type_tw_00390)
    :precondition (and
      (at ?v ?from)
      (connects ?r ?from ?to)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
      (not (location-blocked ?from))
      (not (location-blocked ?to))
    )
    :effect (and
      (not (at ?v ?from))
      (at ?v ?to)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-road_0018-tw_00420
    :parameters (?v - vehicle ?r - road_type_road_0018 ?from - loc_type_loc_0012 ?to - loc_type_loc_0018 ?w - window_type_tw_00420)
    :precondition (and
      (at ?v ?from)
      (connects ?r ?from ?to)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
      (not (location-blocked ?from))
      (not (location-blocked ?to))
    )
    :effect (and
      (not (at ?v ?from))
      (at ?v ?to)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-road_0018-tw_00450
    :parameters (?v - vehicle ?r - road_type_road_0018 ?from - loc_type_loc_0012 ?to - loc_type_loc_0018 ?w - window_type_tw_00450)
    :precondition (and
      (at ?v ?from)
      (connects ?r ?from ?to)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
      (not (location-blocked ?from))
      (not (location-blocked ?to))
    )
    :effect (and
      (not (at ?v ?from))
      (at ?v ?to)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-road_0019-tw_00000
    :parameters (?v - vehicle ?r - road_type_road_0019 ?from - loc_type_loc_0013 ?to - loc_type_loc_0096 ?w - window_type_tw_00000)
    :precondition (and
      (at ?v ?from)
      (connects ?r ?from ?to)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
      (not (location-blocked ?from))
      (not (location-blocked ?to))
    )
    :effect (and
      (not (at ?v ?from))
      (at ?v ?to)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-road_0019-tw_00030
    :parameters (?v - vehicle ?r - road_type_road_0019 ?from - loc_type_loc_0013 ?to - loc_type_loc_0096 ?w - window_type_tw_00030)
    :precondition (and
      (at ?v ?from)
      (connects ?r ?from ?to)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
      (not (location-blocked ?from))
      (not (location-blocked ?to))
    )
    :effect (and
      (not (at ?v ?from))
      (at ?v ?to)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-road_0019-tw_00060
    :parameters (?v - vehicle ?r - road_type_road_0019 ?from - loc_type_loc_0013 ?to - loc_type_loc_0096 ?w - window_type_tw_00060)
    :precondition (and
      (at ?v ?from)
      (connects ?r ?from ?to)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
      (not (location-blocked ?from))
      (not (location-blocked ?to))
    )
    :effect (and
      (not (at ?v ?from))
      (at ?v ?to)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-road_0019-tw_00090
    :parameters (?v - vehicle ?r - road_type_road_0019 ?from - loc_type_loc_0013 ?to - loc_type_loc_0096 ?w - window_type_tw_00090)
    :precondition (and
      (at ?v ?from)
      (connects ?r ?from ?to)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
      (not (location-blocked ?from))
      (not (location-blocked ?to))
    )
    :effect (and
      (not (at ?v ?from))
      (at ?v ?to)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-road_0019-tw_00120
    :parameters (?v - vehicle ?r - road_type_road_0019 ?from - loc_type_loc_0013 ?to - loc_type_loc_0096 ?w - window_type_tw_00120)
    :precondition (and
      (at ?v ?from)
      (connects ?r ?from ?to)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
      (not (location-blocked ?from))
      (not (location-blocked ?to))
    )
    :effect (and
      (not (at ?v ?from))
      (at ?v ?to)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-road_0019-tw_00150
    :parameters (?v - vehicle ?r - road_type_road_0019 ?from - loc_type_loc_0013 ?to - loc_type_loc_0096 ?w - window_type_tw_00150)
    :precondition (and
      (at ?v ?from)
      (connects ?r ?from ?to)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
      (not (location-blocked ?from))
      (not (location-blocked ?to))
    )
    :effect (and
      (not (at ?v ?from))
      (at ?v ?to)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-road_0019-tw_00180
    :parameters (?v - vehicle ?r - road_type_road_0019 ?from - loc_type_loc_0013 ?to - loc_type_loc_0096 ?w - window_type_tw_00180)
    :precondition (and
      (at ?v ?from)
      (connects ?r ?from ?to)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
      (not (location-blocked ?from))
      (not (location-blocked ?to))
    )
    :effect (and
      (not (at ?v ?from))
      (at ?v ?to)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-road_0019-tw_00210
    :parameters (?v - vehicle ?r - road_type_road_0019 ?from - loc_type_loc_0013 ?to - loc_type_loc_0096 ?w - window_type_tw_00210)
    :precondition (and
      (at ?v ?from)
      (connects ?r ?from ?to)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
      (not (location-blocked ?from))
      (not (location-blocked ?to))
    )
    :effect (and
      (not (at ?v ?from))
      (at ?v ?to)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-road_0019-tw_00240
    :parameters (?v - vehicle ?r - road_type_road_0019 ?from - loc_type_loc_0013 ?to - loc_type_loc_0096 ?w - window_type_tw_00240)
    :precondition (and
      (at ?v ?from)
      (connects ?r ?from ?to)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
      (not (location-blocked ?from))
      (not (location-blocked ?to))
    )
    :effect (and
      (not (at ?v ?from))
      (at ?v ?to)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-road_0019-tw_00270
    :parameters (?v - vehicle ?r - road_type_road_0019 ?from - loc_type_loc_0013 ?to - loc_type_loc_0096 ?w - window_type_tw_00270)
    :precondition (and
      (at ?v ?from)
      (connects ?r ?from ?to)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
      (not (location-blocked ?from))
      (not (location-blocked ?to))
    )
    :effect (and
      (not (at ?v ?from))
      (at ?v ?to)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-road_0019-tw_00300
    :parameters (?v - vehicle ?r - road_type_road_0019 ?from - loc_type_loc_0013 ?to - loc_type_loc_0096 ?w - window_type_tw_00300)
    :precondition (and
      (at ?v ?from)
      (connects ?r ?from ?to)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
      (not (location-blocked ?from))
      (not (location-blocked ?to))
    )
    :effect (and
      (not (at ?v ?from))
      (at ?v ?to)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-road_0019-tw_00330
    :parameters (?v - vehicle ?r - road_type_road_0019 ?from - loc_type_loc_0013 ?to - loc_type_loc_0096 ?w - window_type_tw_00330)
    :precondition (and
      (at ?v ?from)
      (connects ?r ?from ?to)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
      (not (location-blocked ?from))
      (not (location-blocked ?to))
    )
    :effect (and
      (not (at ?v ?from))
      (at ?v ?to)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-road_0019-tw_00360
    :parameters (?v - vehicle ?r - road_type_road_0019 ?from - loc_type_loc_0013 ?to - loc_type_loc_0096 ?w - window_type_tw_00360)
    :precondition (and
      (at ?v ?from)
      (connects ?r ?from ?to)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
      (not (location-blocked ?from))
      (not (location-blocked ?to))
    )
    :effect (and
      (not (at ?v ?from))
      (at ?v ?to)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-road_0019-tw_00390
    :parameters (?v - vehicle ?r - road_type_road_0019 ?from - loc_type_loc_0013 ?to - loc_type_loc_0096 ?w - window_type_tw_00390)
    :precondition (and
      (at ?v ?from)
      (connects ?r ?from ?to)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
      (not (location-blocked ?from))
      (not (location-blocked ?to))
    )
    :effect (and
      (not (at ?v ?from))
      (at ?v ?to)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-road_0019-tw_00420
    :parameters (?v - vehicle ?r - road_type_road_0019 ?from - loc_type_loc_0013 ?to - loc_type_loc_0096 ?w - window_type_tw_00420)
    :precondition (and
      (at ?v ?from)
      (connects ?r ?from ?to)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
      (not (location-blocked ?from))
      (not (location-blocked ?to))
    )
    :effect (and
      (not (at ?v ?from))
      (at ?v ?to)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-road_0019-tw_00450
    :parameters (?v - vehicle ?r - road_type_road_0019 ?from - loc_type_loc_0013 ?to - loc_type_loc_0096 ?w - window_type_tw_00450)
    :precondition (and
      (at ?v ?from)
      (connects ?r ?from ?to)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
      (not (location-blocked ?from))
      (not (location-blocked ?to))
    )
    :effect (and
      (not (at ?v ?from))
      (at ?v ?to)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-road_0020-tw_00000
    :parameters (?v - vehicle ?r - road_type_road_0020 ?from - loc_type_loc_0013 ?to - loc_type_loc_0047 ?w - window_type_tw_00000)
    :precondition (and
      (at ?v ?from)
      (connects ?r ?from ?to)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
      (not (location-blocked ?from))
      (not (location-blocked ?to))
    )
    :effect (and
      (not (at ?v ?from))
      (at ?v ?to)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-road_0020-tw_00030
    :parameters (?v - vehicle ?r - road_type_road_0020 ?from - loc_type_loc_0013 ?to - loc_type_loc_0047 ?w - window_type_tw_00030)
    :precondition (and
      (at ?v ?from)
      (connects ?r ?from ?to)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
      (not (location-blocked ?from))
      (not (location-blocked ?to))
    )
    :effect (and
      (not (at ?v ?from))
      (at ?v ?to)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-road_0020-tw_00060
    :parameters (?v - vehicle ?r - road_type_road_0020 ?from - loc_type_loc_0013 ?to - loc_type_loc_0047 ?w - window_type_tw_00060)
    :precondition (and
      (at ?v ?from)
      (connects ?r ?from ?to)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
      (not (location-blocked ?from))
      (not (location-blocked ?to))
    )
    :effect (and
      (not (at ?v ?from))
      (at ?v ?to)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-road_0020-tw_00090
    :parameters (?v - vehicle ?r - road_type_road_0020 ?from - loc_type_loc_0013 ?to - loc_type_loc_0047 ?w - window_type_tw_00090)
    :precondition (and
      (at ?v ?from)
      (connects ?r ?from ?to)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
      (not (location-blocked ?from))
      (not (location-blocked ?to))
    )
    :effect (and
      (not (at ?v ?from))
      (at ?v ?to)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-road_0020-tw_00120
    :parameters (?v - vehicle ?r - road_type_road_0020 ?from - loc_type_loc_0013 ?to - loc_type_loc_0047 ?w - window_type_tw_00120)
    :precondition (and
      (at ?v ?from)
      (connects ?r ?from ?to)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
      (not (location-blocked ?from))
      (not (location-blocked ?to))
    )
    :effect (and
      (not (at ?v ?from))
      (at ?v ?to)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-road_0020-tw_00150
    :parameters (?v - vehicle ?r - road_type_road_0020 ?from - loc_type_loc_0013 ?to - loc_type_loc_0047 ?w - window_type_tw_00150)
    :precondition (and
      (at ?v ?from)
      (connects ?r ?from ?to)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
      (not (location-blocked ?from))
      (not (location-blocked ?to))
    )
    :effect (and
      (not (at ?v ?from))
      (at ?v ?to)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-road_0020-tw_00180
    :parameters (?v - vehicle ?r - road_type_road_0020 ?from - loc_type_loc_0013 ?to - loc_type_loc_0047 ?w - window_type_tw_00180)
    :precondition (and
      (at ?v ?from)
      (connects ?r ?from ?to)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
      (not (location-blocked ?from))
      (not (location-blocked ?to))
    )
    :effect (and
      (not (at ?v ?from))
      (at ?v ?to)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-road_0020-tw_00210
    :parameters (?v - vehicle ?r - road_type_road_0020 ?from - loc_type_loc_0013 ?to - loc_type_loc_0047 ?w - window_type_tw_00210)
    :precondition (and
      (at ?v ?from)
      (connects ?r ?from ?to)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
      (not (location-blocked ?from))
      (not (location-blocked ?to))
    )
    :effect (and
      (not (at ?v ?from))
      (at ?v ?to)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-road_0020-tw_00240
    :parameters (?v - vehicle ?r - road_type_road_0020 ?from - loc_type_loc_0013 ?to - loc_type_loc_0047 ?w - window_type_tw_00240)
    :precondition (and
      (at ?v ?from)
      (connects ?r ?from ?to)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
      (not (location-blocked ?from))
      (not (location-blocked ?to))
    )
    :effect (and
      (not (at ?v ?from))
      (at ?v ?to)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-road_0020-tw_00270
    :parameters (?v - vehicle ?r - road_type_road_0020 ?from - loc_type_loc_0013 ?to - loc_type_loc_0047 ?w - window_type_tw_00270)
    :precondition (and
      (at ?v ?from)
      (connects ?r ?from ?to)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
      (not (location-blocked ?from))
      (not (location-blocked ?to))
    )
    :effect (and
      (not (at ?v ?from))
      (at ?v ?to)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-road_0020-tw_00300
    :parameters (?v - vehicle ?r - road_type_road_0020 ?from - loc_type_loc_0013 ?to - loc_type_loc_0047 ?w - window_type_tw_00300)
    :precondition (and
      (at ?v ?from)
      (connects ?r ?from ?to)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
      (not (location-blocked ?from))
      (not (location-blocked ?to))
    )
    :effect (and
      (not (at ?v ?from))
      (at ?v ?to)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-road_0020-tw_00330
    :parameters (?v - vehicle ?r - road_type_road_0020 ?from - loc_type_loc_0013 ?to - loc_type_loc_0047 ?w - window_type_tw_00330)
    :precondition (and
      (at ?v ?from)
      (connects ?r ?from ?to)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
      (not (location-blocked ?from))
      (not (location-blocked ?to))
    )
    :effect (and
      (not (at ?v ?from))
      (at ?v ?to)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-road_0020-tw_00360
    :parameters (?v - vehicle ?r - road_type_road_0020 ?from - loc_type_loc_0013 ?to - loc_type_loc_0047 ?w - window_type_tw_00360)
    :precondition (and
      (at ?v ?from)
      (connects ?r ?from ?to)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
      (not (location-blocked ?from))
      (not (location-blocked ?to))
    )
    :effect (and
      (not (at ?v ?from))
      (at ?v ?to)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-road_0020-tw_00390
    :parameters (?v - vehicle ?r - road_type_road_0020 ?from - loc_type_loc_0013 ?to - loc_type_loc_0047 ?w - window_type_tw_00390)
    :precondition (and
      (at ?v ?from)
      (connects ?r ?from ?to)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
      (not (location-blocked ?from))
      (not (location-blocked ?to))
    )
    :effect (and
      (not (at ?v ?from))
      (at ?v ?to)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-road_0020-tw_00420
    :parameters (?v - vehicle ?r - road_type_road_0020 ?from - loc_type_loc_0013 ?to - loc_type_loc_0047 ?w - window_type_tw_00420)
    :precondition (and
      (at ?v ?from)
      (connects ?r ?from ?to)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
      (not (location-blocked ?from))
      (not (location-blocked ?to))
    )
    :effect (and
      (not (at ?v ?from))
      (at ?v ?to)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-road_0020-tw_00450
    :parameters (?v - vehicle ?r - road_type_road_0020 ?from - loc_type_loc_0013 ?to - loc_type_loc_0047 ?w - window_type_tw_00450)
    :precondition (and
      (at ?v ?from)
      (connects ?r ?from ?to)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
      (not (location-blocked ?from))
      (not (location-blocked ?to))
    )
    :effect (and
      (not (at ?v ?from))
      (at ?v ?to)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-road_0021-tw_00000
    :parameters (?v - vehicle ?r - road_type_road_0021 ?from - loc_type_loc_0015 ?to - loc_type_loc_0034 ?w - window_type_tw_00000)
    :precondition (and
      (at ?v ?from)
      (connects ?r ?from ?to)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
      (not (location-blocked ?from))
      (not (location-blocked ?to))
    )
    :effect (and
      (not (at ?v ?from))
      (at ?v ?to)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-road_0021-tw_00030
    :parameters (?v - vehicle ?r - road_type_road_0021 ?from - loc_type_loc_0015 ?to - loc_type_loc_0034 ?w - window_type_tw_00030)
    :precondition (and
      (at ?v ?from)
      (connects ?r ?from ?to)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
      (not (location-blocked ?from))
      (not (location-blocked ?to))
    )
    :effect (and
      (not (at ?v ?from))
      (at ?v ?to)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-road_0021-tw_00060
    :parameters (?v - vehicle ?r - road_type_road_0021 ?from - loc_type_loc_0015 ?to - loc_type_loc_0034 ?w - window_type_tw_00060)
    :precondition (and
      (at ?v ?from)
      (connects ?r ?from ?to)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
      (not (location-blocked ?from))
      (not (location-blocked ?to))
    )
    :effect (and
      (not (at ?v ?from))
      (at ?v ?to)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-road_0021-tw_00090
    :parameters (?v - vehicle ?r - road_type_road_0021 ?from - loc_type_loc_0015 ?to - loc_type_loc_0034 ?w - window_type_tw_00090)
    :precondition (and
      (at ?v ?from)
      (connects ?r ?from ?to)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
      (not (location-blocked ?from))
      (not (location-blocked ?to))
    )
    :effect (and
      (not (at ?v ?from))
      (at ?v ?to)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-road_0021-tw_00120
    :parameters (?v - vehicle ?r - road_type_road_0021 ?from - loc_type_loc_0015 ?to - loc_type_loc_0034 ?w - window_type_tw_00120)
    :precondition (and
      (at ?v ?from)
      (connects ?r ?from ?to)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
      (not (location-blocked ?from))
      (not (location-blocked ?to))
    )
    :effect (and
      (not (at ?v ?from))
      (at ?v ?to)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-road_0021-tw_00150
    :parameters (?v - vehicle ?r - road_type_road_0021 ?from - loc_type_loc_0015 ?to - loc_type_loc_0034 ?w - window_type_tw_00150)
    :precondition (and
      (at ?v ?from)
      (connects ?r ?from ?to)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
      (not (location-blocked ?from))
      (not (location-blocked ?to))
    )
    :effect (and
      (not (at ?v ?from))
      (at ?v ?to)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-road_0021-tw_00180
    :parameters (?v - vehicle ?r - road_type_road_0021 ?from - loc_type_loc_0015 ?to - loc_type_loc_0034 ?w - window_type_tw_00180)
    :precondition (and
      (at ?v ?from)
      (connects ?r ?from ?to)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
      (not (location-blocked ?from))
      (not (location-blocked ?to))
    )
    :effect (and
      (not (at ?v ?from))
      (at ?v ?to)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-road_0021-tw_00210
    :parameters (?v - vehicle ?r - road_type_road_0021 ?from - loc_type_loc_0015 ?to - loc_type_loc_0034 ?w - window_type_tw_00210)
    :precondition (and
      (at ?v ?from)
      (connects ?r ?from ?to)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
      (not (location-blocked ?from))
      (not (location-blocked ?to))
    )
    :effect (and
      (not (at ?v ?from))
      (at ?v ?to)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-road_0021-tw_00240
    :parameters (?v - vehicle ?r - road_type_road_0021 ?from - loc_type_loc_0015 ?to - loc_type_loc_0034 ?w - window_type_tw_00240)
    :precondition (and
      (at ?v ?from)
      (connects ?r ?from ?to)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
      (not (location-blocked ?from))
      (not (location-blocked ?to))
    )
    :effect (and
      (not (at ?v ?from))
      (at ?v ?to)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-road_0021-tw_00270
    :parameters (?v - vehicle ?r - road_type_road_0021 ?from - loc_type_loc_0015 ?to - loc_type_loc_0034 ?w - window_type_tw_00270)
    :precondition (and
      (at ?v ?from)
      (connects ?r ?from ?to)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
      (not (location-blocked ?from))
      (not (location-blocked ?to))
    )
    :effect (and
      (not (at ?v ?from))
      (at ?v ?to)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-road_0021-tw_00300
    :parameters (?v - vehicle ?r - road_type_road_0021 ?from - loc_type_loc_0015 ?to - loc_type_loc_0034 ?w - window_type_tw_00300)
    :precondition (and
      (at ?v ?from)
      (connects ?r ?from ?to)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
      (not (location-blocked ?from))
      (not (location-blocked ?to))
    )
    :effect (and
      (not (at ?v ?from))
      (at ?v ?to)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-road_0021-tw_00330
    :parameters (?v - vehicle ?r - road_type_road_0021 ?from - loc_type_loc_0015 ?to - loc_type_loc_0034 ?w - window_type_tw_00330)
    :precondition (and
      (at ?v ?from)
      (connects ?r ?from ?to)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
      (not (location-blocked ?from))
      (not (location-blocked ?to))
    )
    :effect (and
      (not (at ?v ?from))
      (at ?v ?to)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-road_0021-tw_00360
    :parameters (?v - vehicle ?r - road_type_road_0021 ?from - loc_type_loc_0015 ?to - loc_type_loc_0034 ?w - window_type_tw_00360)
    :precondition (and
      (at ?v ?from)
      (connects ?r ?from ?to)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
      (not (location-blocked ?from))
      (not (location-blocked ?to))
    )
    :effect (and
      (not (at ?v ?from))
      (at ?v ?to)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-road_0021-tw_00390
    :parameters (?v - vehicle ?r - road_type_road_0021 ?from - loc_type_loc_0015 ?to - loc_type_loc_0034 ?w - window_type_tw_00390)
    :precondition (and
      (at ?v ?from)
      (connects ?r ?from ?to)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
      (not (location-blocked ?from))
      (not (location-blocked ?to))
    )
    :effect (and
      (not (at ?v ?from))
      (at ?v ?to)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-road_0021-tw_00420
    :parameters (?v - vehicle ?r - road_type_road_0021 ?from - loc_type_loc_0015 ?to - loc_type_loc_0034 ?w - window_type_tw_00420)
    :precondition (and
      (at ?v ?from)
      (connects ?r ?from ?to)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
      (not (location-blocked ?from))
      (not (location-blocked ?to))
    )
    :effect (and
      (not (at ?v ?from))
      (at ?v ?to)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-road_0021-tw_00450
    :parameters (?v - vehicle ?r - road_type_road_0021 ?from - loc_type_loc_0015 ?to - loc_type_loc_0034 ?w - window_type_tw_00450)
    :precondition (and
      (at ?v ?from)
      (connects ?r ?from ?to)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
      (not (location-blocked ?from))
      (not (location-blocked ?to))
    )
    :effect (and
      (not (at ?v ?from))
      (at ?v ?to)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-road_0022-tw_00000
    :parameters (?v - vehicle ?r - road_type_road_0022 ?from - loc_type_loc_0016 ?to - loc_type_loc_0048 ?w - window_type_tw_00000)
    :precondition (and
      (at ?v ?from)
      (connects ?r ?from ?to)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
      (not (location-blocked ?from))
      (not (location-blocked ?to))
    )
    :effect (and
      (not (at ?v ?from))
      (at ?v ?to)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-road_0022-tw_00030
    :parameters (?v - vehicle ?r - road_type_road_0022 ?from - loc_type_loc_0016 ?to - loc_type_loc_0048 ?w - window_type_tw_00030)
    :precondition (and
      (at ?v ?from)
      (connects ?r ?from ?to)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
      (not (location-blocked ?from))
      (not (location-blocked ?to))
    )
    :effect (and
      (not (at ?v ?from))
      (at ?v ?to)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-road_0022-tw_00060
    :parameters (?v - vehicle ?r - road_type_road_0022 ?from - loc_type_loc_0016 ?to - loc_type_loc_0048 ?w - window_type_tw_00060)
    :precondition (and
      (at ?v ?from)
      (connects ?r ?from ?to)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
      (not (location-blocked ?from))
      (not (location-blocked ?to))
    )
    :effect (and
      (not (at ?v ?from))
      (at ?v ?to)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-road_0022-tw_00090
    :parameters (?v - vehicle ?r - road_type_road_0022 ?from - loc_type_loc_0016 ?to - loc_type_loc_0048 ?w - window_type_tw_00090)
    :precondition (and
      (at ?v ?from)
      (connects ?r ?from ?to)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
      (not (location-blocked ?from))
      (not (location-blocked ?to))
    )
    :effect (and
      (not (at ?v ?from))
      (at ?v ?to)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-road_0022-tw_00120
    :parameters (?v - vehicle ?r - road_type_road_0022 ?from - loc_type_loc_0016 ?to - loc_type_loc_0048 ?w - window_type_tw_00120)
    :precondition (and
      (at ?v ?from)
      (connects ?r ?from ?to)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
      (not (location-blocked ?from))
      (not (location-blocked ?to))
    )
    :effect (and
      (not (at ?v ?from))
      (at ?v ?to)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-road_0022-tw_00150
    :parameters (?v - vehicle ?r - road_type_road_0022 ?from - loc_type_loc_0016 ?to - loc_type_loc_0048 ?w - window_type_tw_00150)
    :precondition (and
      (at ?v ?from)
      (connects ?r ?from ?to)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
      (not (location-blocked ?from))
      (not (location-blocked ?to))
    )
    :effect (and
      (not (at ?v ?from))
      (at ?v ?to)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-road_0022-tw_00180
    :parameters (?v - vehicle ?r - road_type_road_0022 ?from - loc_type_loc_0016 ?to - loc_type_loc_0048 ?w - window_type_tw_00180)
    :precondition (and
      (at ?v ?from)
      (connects ?r ?from ?to)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
      (not (location-blocked ?from))
      (not (location-blocked ?to))
    )
    :effect (and
      (not (at ?v ?from))
      (at ?v ?to)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-road_0022-tw_00210
    :parameters (?v - vehicle ?r - road_type_road_0022 ?from - loc_type_loc_0016 ?to - loc_type_loc_0048 ?w - window_type_tw_00210)
    :precondition (and
      (at ?v ?from)
      (connects ?r ?from ?to)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
      (not (location-blocked ?from))
      (not (location-blocked ?to))
    )
    :effect (and
      (not (at ?v ?from))
      (at ?v ?to)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-road_0022-tw_00240
    :parameters (?v - vehicle ?r - road_type_road_0022 ?from - loc_type_loc_0016 ?to - loc_type_loc_0048 ?w - window_type_tw_00240)
    :precondition (and
      (at ?v ?from)
      (connects ?r ?from ?to)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
      (not (location-blocked ?from))
      (not (location-blocked ?to))
    )
    :effect (and
      (not (at ?v ?from))
      (at ?v ?to)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-road_0022-tw_00270
    :parameters (?v - vehicle ?r - road_type_road_0022 ?from - loc_type_loc_0016 ?to - loc_type_loc_0048 ?w - window_type_tw_00270)
    :precondition (and
      (at ?v ?from)
      (connects ?r ?from ?to)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
      (not (location-blocked ?from))
      (not (location-blocked ?to))
    )
    :effect (and
      (not (at ?v ?from))
      (at ?v ?to)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-road_0022-tw_00300
    :parameters (?v - vehicle ?r - road_type_road_0022 ?from - loc_type_loc_0016 ?to - loc_type_loc_0048 ?w - window_type_tw_00300)
    :precondition (and
      (at ?v ?from)
      (connects ?r ?from ?to)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
      (not (location-blocked ?from))
      (not (location-blocked ?to))
    )
    :effect (and
      (not (at ?v ?from))
      (at ?v ?to)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-road_0022-tw_00330
    :parameters (?v - vehicle ?r - road_type_road_0022 ?from - loc_type_loc_0016 ?to - loc_type_loc_0048 ?w - window_type_tw_00330)
    :precondition (and
      (at ?v ?from)
      (connects ?r ?from ?to)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
      (not (location-blocked ?from))
      (not (location-blocked ?to))
    )
    :effect (and
      (not (at ?v ?from))
      (at ?v ?to)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-road_0022-tw_00360
    :parameters (?v - vehicle ?r - road_type_road_0022 ?from - loc_type_loc_0016 ?to - loc_type_loc_0048 ?w - window_type_tw_00360)
    :precondition (and
      (at ?v ?from)
      (connects ?r ?from ?to)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
      (not (location-blocked ?from))
      (not (location-blocked ?to))
    )
    :effect (and
      (not (at ?v ?from))
      (at ?v ?to)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-road_0022-tw_00390
    :parameters (?v - vehicle ?r - road_type_road_0022 ?from - loc_type_loc_0016 ?to - loc_type_loc_0048 ?w - window_type_tw_00390)
    :precondition (and
      (at ?v ?from)
      (connects ?r ?from ?to)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
      (not (location-blocked ?from))
      (not (location-blocked ?to))
    )
    :effect (and
      (not (at ?v ?from))
      (at ?v ?to)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-road_0022-tw_00420
    :parameters (?v - vehicle ?r - road_type_road_0022 ?from - loc_type_loc_0016 ?to - loc_type_loc_0048 ?w - window_type_tw_00420)
    :precondition (and
      (at ?v ?from)
      (connects ?r ?from ?to)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
      (not (location-blocked ?from))
      (not (location-blocked ?to))
    )
    :effect (and
      (not (at ?v ?from))
      (at ?v ?to)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-road_0022-tw_00450
    :parameters (?v - vehicle ?r - road_type_road_0022 ?from - loc_type_loc_0016 ?to - loc_type_loc_0048 ?w - window_type_tw_00450)
    :precondition (and
      (at ?v ?from)
      (connects ?r ?from ?to)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
      (not (location-blocked ?from))
      (not (location-blocked ?to))
    )
    :effect (and
      (not (at ?v ?from))
      (at ?v ?to)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-road_0023-tw_00000
    :parameters (?v - vehicle ?r - road_type_road_0023 ?from - loc_type_loc_0016 ?to - loc_type_loc_0092 ?w - window_type_tw_00000)
    :precondition (and
      (at ?v ?from)
      (connects ?r ?from ?to)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
      (not (location-blocked ?from))
      (not (location-blocked ?to))
    )
    :effect (and
      (not (at ?v ?from))
      (at ?v ?to)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-road_0023-tw_00030
    :parameters (?v - vehicle ?r - road_type_road_0023 ?from - loc_type_loc_0016 ?to - loc_type_loc_0092 ?w - window_type_tw_00030)
    :precondition (and
      (at ?v ?from)
      (connects ?r ?from ?to)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
      (not (location-blocked ?from))
      (not (location-blocked ?to))
    )
    :effect (and
      (not (at ?v ?from))
      (at ?v ?to)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-road_0023-tw_00060
    :parameters (?v - vehicle ?r - road_type_road_0023 ?from - loc_type_loc_0016 ?to - loc_type_loc_0092 ?w - window_type_tw_00060)
    :precondition (and
      (at ?v ?from)
      (connects ?r ?from ?to)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
      (not (location-blocked ?from))
      (not (location-blocked ?to))
    )
    :effect (and
      (not (at ?v ?from))
      (at ?v ?to)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-road_0023-tw_00090
    :parameters (?v - vehicle ?r - road_type_road_0023 ?from - loc_type_loc_0016 ?to - loc_type_loc_0092 ?w - window_type_tw_00090)
    :precondition (and
      (at ?v ?from)
      (connects ?r ?from ?to)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
      (not (location-blocked ?from))
      (not (location-blocked ?to))
    )
    :effect (and
      (not (at ?v ?from))
      (at ?v ?to)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-road_0023-tw_00120
    :parameters (?v - vehicle ?r - road_type_road_0023 ?from - loc_type_loc_0016 ?to - loc_type_loc_0092 ?w - window_type_tw_00120)
    :precondition (and
      (at ?v ?from)
      (connects ?r ?from ?to)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
      (not (location-blocked ?from))
      (not (location-blocked ?to))
    )
    :effect (and
      (not (at ?v ?from))
      (at ?v ?to)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-road_0023-tw_00150
    :parameters (?v - vehicle ?r - road_type_road_0023 ?from - loc_type_loc_0016 ?to - loc_type_loc_0092 ?w - window_type_tw_00150)
    :precondition (and
      (at ?v ?from)
      (connects ?r ?from ?to)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
      (not (location-blocked ?from))
      (not (location-blocked ?to))
    )
    :effect (and
      (not (at ?v ?from))
      (at ?v ?to)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-road_0023-tw_00180
    :parameters (?v - vehicle ?r - road_type_road_0023 ?from - loc_type_loc_0016 ?to - loc_type_loc_0092 ?w - window_type_tw_00180)
    :precondition (and
      (at ?v ?from)
      (connects ?r ?from ?to)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
      (not (location-blocked ?from))
      (not (location-blocked ?to))
    )
    :effect (and
      (not (at ?v ?from))
      (at ?v ?to)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-road_0023-tw_00210
    :parameters (?v - vehicle ?r - road_type_road_0023 ?from - loc_type_loc_0016 ?to - loc_type_loc_0092 ?w - window_type_tw_00210)
    :precondition (and
      (at ?v ?from)
      (connects ?r ?from ?to)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
      (not (location-blocked ?from))
      (not (location-blocked ?to))
    )
    :effect (and
      (not (at ?v ?from))
      (at ?v ?to)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-road_0023-tw_00240
    :parameters (?v - vehicle ?r - road_type_road_0023 ?from - loc_type_loc_0016 ?to - loc_type_loc_0092 ?w - window_type_tw_00240)
    :precondition (and
      (at ?v ?from)
      (connects ?r ?from ?to)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
      (not (location-blocked ?from))
      (not (location-blocked ?to))
    )
    :effect (and
      (not (at ?v ?from))
      (at ?v ?to)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-road_0023-tw_00270
    :parameters (?v - vehicle ?r - road_type_road_0023 ?from - loc_type_loc_0016 ?to - loc_type_loc_0092 ?w - window_type_tw_00270)
    :precondition (and
      (at ?v ?from)
      (connects ?r ?from ?to)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
      (not (location-blocked ?from))
      (not (location-blocked ?to))
    )
    :effect (and
      (not (at ?v ?from))
      (at ?v ?to)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-road_0023-tw_00300
    :parameters (?v - vehicle ?r - road_type_road_0023 ?from - loc_type_loc_0016 ?to - loc_type_loc_0092 ?w - window_type_tw_00300)
    :precondition (and
      (at ?v ?from)
      (connects ?r ?from ?to)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
      (not (location-blocked ?from))
      (not (location-blocked ?to))
    )
    :effect (and
      (not (at ?v ?from))
      (at ?v ?to)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-road_0023-tw_00330
    :parameters (?v - vehicle ?r - road_type_road_0023 ?from - loc_type_loc_0016 ?to - loc_type_loc_0092 ?w - window_type_tw_00330)
    :precondition (and
      (at ?v ?from)
      (connects ?r ?from ?to)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
      (not (location-blocked ?from))
      (not (location-blocked ?to))
    )
    :effect (and
      (not (at ?v ?from))
      (at ?v ?to)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-road_0023-tw_00360
    :parameters (?v - vehicle ?r - road_type_road_0023 ?from - loc_type_loc_0016 ?to - loc_type_loc_0092 ?w - window_type_tw_00360)
    :precondition (and
      (at ?v ?from)
      (connects ?r ?from ?to)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
      (not (location-blocked ?from))
      (not (location-blocked ?to))
    )
    :effect (and
      (not (at ?v ?from))
      (at ?v ?to)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-road_0023-tw_00390
    :parameters (?v - vehicle ?r - road_type_road_0023 ?from - loc_type_loc_0016 ?to - loc_type_loc_0092 ?w - window_type_tw_00390)
    :precondition (and
      (at ?v ?from)
      (connects ?r ?from ?to)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
      (not (location-blocked ?from))
      (not (location-blocked ?to))
    )
    :effect (and
      (not (at ?v ?from))
      (at ?v ?to)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-road_0023-tw_00420
    :parameters (?v - vehicle ?r - road_type_road_0023 ?from - loc_type_loc_0016 ?to - loc_type_loc_0092 ?w - window_type_tw_00420)
    :precondition (and
      (at ?v ?from)
      (connects ?r ?from ?to)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
      (not (location-blocked ?from))
      (not (location-blocked ?to))
    )
    :effect (and
      (not (at ?v ?from))
      (at ?v ?to)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-road_0023-tw_00450
    :parameters (?v - vehicle ?r - road_type_road_0023 ?from - loc_type_loc_0016 ?to - loc_type_loc_0092 ?w - window_type_tw_00450)
    :precondition (and
      (at ?v ?from)
      (connects ?r ?from ?to)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
      (not (location-blocked ?from))
      (not (location-blocked ?to))
    )
    :effect (and
      (not (at ?v ?from))
      (at ?v ?to)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-road_0024-tw_00000
    :parameters (?v - vehicle ?r - road_type_road_0024 ?from - loc_type_loc_0017 ?to - loc_type_loc_0060 ?w - window_type_tw_00000)
    :precondition (and
      (at ?v ?from)
      (connects ?r ?from ?to)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
      (not (location-blocked ?from))
      (not (location-blocked ?to))
    )
    :effect (and
      (not (at ?v ?from))
      (at ?v ?to)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-road_0024-tw_00030
    :parameters (?v - vehicle ?r - road_type_road_0024 ?from - loc_type_loc_0017 ?to - loc_type_loc_0060 ?w - window_type_tw_00030)
    :precondition (and
      (at ?v ?from)
      (connects ?r ?from ?to)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
      (not (location-blocked ?from))
      (not (location-blocked ?to))
    )
    :effect (and
      (not (at ?v ?from))
      (at ?v ?to)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-road_0024-tw_00060
    :parameters (?v - vehicle ?r - road_type_road_0024 ?from - loc_type_loc_0017 ?to - loc_type_loc_0060 ?w - window_type_tw_00060)
    :precondition (and
      (at ?v ?from)
      (connects ?r ?from ?to)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
      (not (location-blocked ?from))
      (not (location-blocked ?to))
    )
    :effect (and
      (not (at ?v ?from))
      (at ?v ?to)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-road_0024-tw_00090
    :parameters (?v - vehicle ?r - road_type_road_0024 ?from - loc_type_loc_0017 ?to - loc_type_loc_0060 ?w - window_type_tw_00090)
    :precondition (and
      (at ?v ?from)
      (connects ?r ?from ?to)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
      (not (location-blocked ?from))
      (not (location-blocked ?to))
    )
    :effect (and
      (not (at ?v ?from))
      (at ?v ?to)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-road_0024-tw_00120
    :parameters (?v - vehicle ?r - road_type_road_0024 ?from - loc_type_loc_0017 ?to - loc_type_loc_0060 ?w - window_type_tw_00120)
    :precondition (and
      (at ?v ?from)
      (connects ?r ?from ?to)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
      (not (location-blocked ?from))
      (not (location-blocked ?to))
    )
    :effect (and
      (not (at ?v ?from))
      (at ?v ?to)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-road_0024-tw_00150
    :parameters (?v - vehicle ?r - road_type_road_0024 ?from - loc_type_loc_0017 ?to - loc_type_loc_0060 ?w - window_type_tw_00150)
    :precondition (and
      (at ?v ?from)
      (connects ?r ?from ?to)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
      (not (location-blocked ?from))
      (not (location-blocked ?to))
    )
    :effect (and
      (not (at ?v ?from))
      (at ?v ?to)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-road_0024-tw_00180
    :parameters (?v - vehicle ?r - road_type_road_0024 ?from - loc_type_loc_0017 ?to - loc_type_loc_0060 ?w - window_type_tw_00180)
    :precondition (and
      (at ?v ?from)
      (connects ?r ?from ?to)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
      (not (location-blocked ?from))
      (not (location-blocked ?to))
    )
    :effect (and
      (not (at ?v ?from))
      (at ?v ?to)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-road_0024-tw_00210
    :parameters (?v - vehicle ?r - road_type_road_0024 ?from - loc_type_loc_0017 ?to - loc_type_loc_0060 ?w - window_type_tw_00210)
    :precondition (and
      (at ?v ?from)
      (connects ?r ?from ?to)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
      (not (location-blocked ?from))
      (not (location-blocked ?to))
    )
    :effect (and
      (not (at ?v ?from))
      (at ?v ?to)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-road_0024-tw_00240
    :parameters (?v - vehicle ?r - road_type_road_0024 ?from - loc_type_loc_0017 ?to - loc_type_loc_0060 ?w - window_type_tw_00240)
    :precondition (and
      (at ?v ?from)
      (connects ?r ?from ?to)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
      (not (location-blocked ?from))
      (not (location-blocked ?to))
    )
    :effect (and
      (not (at ?v ?from))
      (at ?v ?to)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-road_0024-tw_00270
    :parameters (?v - vehicle ?r - road_type_road_0024 ?from - loc_type_loc_0017 ?to - loc_type_loc_0060 ?w - window_type_tw_00270)
    :precondition (and
      (at ?v ?from)
      (connects ?r ?from ?to)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
      (not (location-blocked ?from))
      (not (location-blocked ?to))
    )
    :effect (and
      (not (at ?v ?from))
      (at ?v ?to)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-road_0024-tw_00300
    :parameters (?v - vehicle ?r - road_type_road_0024 ?from - loc_type_loc_0017 ?to - loc_type_loc_0060 ?w - window_type_tw_00300)
    :precondition (and
      (at ?v ?from)
      (connects ?r ?from ?to)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
      (not (location-blocked ?from))
      (not (location-blocked ?to))
    )
    :effect (and
      (not (at ?v ?from))
      (at ?v ?to)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-road_0024-tw_00330
    :parameters (?v - vehicle ?r - road_type_road_0024 ?from - loc_type_loc_0017 ?to - loc_type_loc_0060 ?w - window_type_tw_00330)
    :precondition (and
      (at ?v ?from)
      (connects ?r ?from ?to)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
      (not (location-blocked ?from))
      (not (location-blocked ?to))
    )
    :effect (and
      (not (at ?v ?from))
      (at ?v ?to)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-road_0024-tw_00360
    :parameters (?v - vehicle ?r - road_type_road_0024 ?from - loc_type_loc_0017 ?to - loc_type_loc_0060 ?w - window_type_tw_00360)
    :precondition (and
      (at ?v ?from)
      (connects ?r ?from ?to)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
      (not (location-blocked ?from))
      (not (location-blocked ?to))
    )
    :effect (and
      (not (at ?v ?from))
      (at ?v ?to)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-road_0024-tw_00390
    :parameters (?v - vehicle ?r - road_type_road_0024 ?from - loc_type_loc_0017 ?to - loc_type_loc_0060 ?w - window_type_tw_00390)
    :precondition (and
      (at ?v ?from)
      (connects ?r ?from ?to)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
      (not (location-blocked ?from))
      (not (location-blocked ?to))
    )
    :effect (and
      (not (at ?v ?from))
      (at ?v ?to)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-road_0024-tw_00420
    :parameters (?v - vehicle ?r - road_type_road_0024 ?from - loc_type_loc_0017 ?to - loc_type_loc_0060 ?w - window_type_tw_00420)
    :precondition (and
      (at ?v ?from)
      (connects ?r ?from ?to)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
      (not (location-blocked ?from))
      (not (location-blocked ?to))
    )
    :effect (and
      (not (at ?v ?from))
      (at ?v ?to)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-road_0024-tw_00450
    :parameters (?v - vehicle ?r - road_type_road_0024 ?from - loc_type_loc_0017 ?to - loc_type_loc_0060 ?w - window_type_tw_00450)
    :precondition (and
      (at ?v ?from)
      (connects ?r ?from ?to)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
      (not (location-blocked ?from))
      (not (location-blocked ?to))
    )
    :effect (and
      (not (at ?v ?from))
      (at ?v ?to)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-road_0025-tw_00000
    :parameters (?v - vehicle ?r - road_type_road_0025 ?from - loc_type_loc_0018 ?to - loc_type_loc_0097 ?w - window_type_tw_00000)
    :precondition (and
      (at ?v ?from)
      (connects ?r ?from ?to)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
      (not (location-blocked ?from))
      (not (location-blocked ?to))
    )
    :effect (and
      (not (at ?v ?from))
      (at ?v ?to)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-road_0025-tw_00030
    :parameters (?v - vehicle ?r - road_type_road_0025 ?from - loc_type_loc_0018 ?to - loc_type_loc_0097 ?w - window_type_tw_00030)
    :precondition (and
      (at ?v ?from)
      (connects ?r ?from ?to)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
      (not (location-blocked ?from))
      (not (location-blocked ?to))
    )
    :effect (and
      (not (at ?v ?from))
      (at ?v ?to)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-road_0025-tw_00060
    :parameters (?v - vehicle ?r - road_type_road_0025 ?from - loc_type_loc_0018 ?to - loc_type_loc_0097 ?w - window_type_tw_00060)
    :precondition (and
      (at ?v ?from)
      (connects ?r ?from ?to)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
      (not (location-blocked ?from))
      (not (location-blocked ?to))
    )
    :effect (and
      (not (at ?v ?from))
      (at ?v ?to)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-road_0025-tw_00090
    :parameters (?v - vehicle ?r - road_type_road_0025 ?from - loc_type_loc_0018 ?to - loc_type_loc_0097 ?w - window_type_tw_00090)
    :precondition (and
      (at ?v ?from)
      (connects ?r ?from ?to)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
      (not (location-blocked ?from))
      (not (location-blocked ?to))
    )
    :effect (and
      (not (at ?v ?from))
      (at ?v ?to)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-road_0025-tw_00120
    :parameters (?v - vehicle ?r - road_type_road_0025 ?from - loc_type_loc_0018 ?to - loc_type_loc_0097 ?w - window_type_tw_00120)
    :precondition (and
      (at ?v ?from)
      (connects ?r ?from ?to)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
      (not (location-blocked ?from))
      (not (location-blocked ?to))
    )
    :effect (and
      (not (at ?v ?from))
      (at ?v ?to)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-road_0025-tw_00150
    :parameters (?v - vehicle ?r - road_type_road_0025 ?from - loc_type_loc_0018 ?to - loc_type_loc_0097 ?w - window_type_tw_00150)
    :precondition (and
      (at ?v ?from)
      (connects ?r ?from ?to)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
      (not (location-blocked ?from))
      (not (location-blocked ?to))
    )
    :effect (and
      (not (at ?v ?from))
      (at ?v ?to)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-road_0025-tw_00180
    :parameters (?v - vehicle ?r - road_type_road_0025 ?from - loc_type_loc_0018 ?to - loc_type_loc_0097 ?w - window_type_tw_00180)
    :precondition (and
      (at ?v ?from)
      (connects ?r ?from ?to)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
      (not (location-blocked ?from))
      (not (location-blocked ?to))
    )
    :effect (and
      (not (at ?v ?from))
      (at ?v ?to)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-road_0025-tw_00210
    :parameters (?v - vehicle ?r - road_type_road_0025 ?from - loc_type_loc_0018 ?to - loc_type_loc_0097 ?w - window_type_tw_00210)
    :precondition (and
      (at ?v ?from)
      (connects ?r ?from ?to)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
      (not (location-blocked ?from))
      (not (location-blocked ?to))
    )
    :effect (and
      (not (at ?v ?from))
      (at ?v ?to)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-road_0025-tw_00240
    :parameters (?v - vehicle ?r - road_type_road_0025 ?from - loc_type_loc_0018 ?to - loc_type_loc_0097 ?w - window_type_tw_00240)
    :precondition (and
      (at ?v ?from)
      (connects ?r ?from ?to)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
      (not (location-blocked ?from))
      (not (location-blocked ?to))
    )
    :effect (and
      (not (at ?v ?from))
      (at ?v ?to)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-road_0025-tw_00270
    :parameters (?v - vehicle ?r - road_type_road_0025 ?from - loc_type_loc_0018 ?to - loc_type_loc_0097 ?w - window_type_tw_00270)
    :precondition (and
      (at ?v ?from)
      (connects ?r ?from ?to)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
      (not (location-blocked ?from))
      (not (location-blocked ?to))
    )
    :effect (and
      (not (at ?v ?from))
      (at ?v ?to)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-road_0025-tw_00300
    :parameters (?v - vehicle ?r - road_type_road_0025 ?from - loc_type_loc_0018 ?to - loc_type_loc_0097 ?w - window_type_tw_00300)
    :precondition (and
      (at ?v ?from)
      (connects ?r ?from ?to)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
      (not (location-blocked ?from))
      (not (location-blocked ?to))
    )
    :effect (and
      (not (at ?v ?from))
      (at ?v ?to)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-road_0025-tw_00330
    :parameters (?v - vehicle ?r - road_type_road_0025 ?from - loc_type_loc_0018 ?to - loc_type_loc_0097 ?w - window_type_tw_00330)
    :precondition (and
      (at ?v ?from)
      (connects ?r ?from ?to)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
      (not (location-blocked ?from))
      (not (location-blocked ?to))
    )
    :effect (and
      (not (at ?v ?from))
      (at ?v ?to)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-road_0025-tw_00360
    :parameters (?v - vehicle ?r - road_type_road_0025 ?from - loc_type_loc_0018 ?to - loc_type_loc_0097 ?w - window_type_tw_00360)
    :precondition (and
      (at ?v ?from)
      (connects ?r ?from ?to)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
      (not (location-blocked ?from))
      (not (location-blocked ?to))
    )
    :effect (and
      (not (at ?v ?from))
      (at ?v ?to)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-road_0025-tw_00390
    :parameters (?v - vehicle ?r - road_type_road_0025 ?from - loc_type_loc_0018 ?to - loc_type_loc_0097 ?w - window_type_tw_00390)
    :precondition (and
      (at ?v ?from)
      (connects ?r ?from ?to)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
      (not (location-blocked ?from))
      (not (location-blocked ?to))
    )
    :effect (and
      (not (at ?v ?from))
      (at ?v ?to)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-road_0025-tw_00420
    :parameters (?v - vehicle ?r - road_type_road_0025 ?from - loc_type_loc_0018 ?to - loc_type_loc_0097 ?w - window_type_tw_00420)
    :precondition (and
      (at ?v ?from)
      (connects ?r ?from ?to)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
      (not (location-blocked ?from))
      (not (location-blocked ?to))
    )
    :effect (and
      (not (at ?v ?from))
      (at ?v ?to)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-road_0025-tw_00450
    :parameters (?v - vehicle ?r - road_type_road_0025 ?from - loc_type_loc_0018 ?to - loc_type_loc_0097 ?w - window_type_tw_00450)
    :precondition (and
      (at ?v ?from)
      (connects ?r ?from ?to)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
      (not (location-blocked ?from))
      (not (location-blocked ?to))
    )
    :effect (and
      (not (at ?v ?from))
      (at ?v ?to)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-road_0026-tw_00000
    :parameters (?v - vehicle ?r - road_type_road_0026 ?from - loc_type_loc_0019 ?to - loc_type_loc_0023 ?w - window_type_tw_00000)
    :precondition (and
      (at ?v ?from)
      (connects ?r ?from ?to)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
      (not (location-blocked ?from))
      (not (location-blocked ?to))
    )
    :effect (and
      (not (at ?v ?from))
      (at ?v ?to)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-road_0026-tw_00030
    :parameters (?v - vehicle ?r - road_type_road_0026 ?from - loc_type_loc_0019 ?to - loc_type_loc_0023 ?w - window_type_tw_00030)
    :precondition (and
      (at ?v ?from)
      (connects ?r ?from ?to)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
      (not (location-blocked ?from))
      (not (location-blocked ?to))
    )
    :effect (and
      (not (at ?v ?from))
      (at ?v ?to)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-road_0026-tw_00060
    :parameters (?v - vehicle ?r - road_type_road_0026 ?from - loc_type_loc_0019 ?to - loc_type_loc_0023 ?w - window_type_tw_00060)
    :precondition (and
      (at ?v ?from)
      (connects ?r ?from ?to)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
      (not (location-blocked ?from))
      (not (location-blocked ?to))
    )
    :effect (and
      (not (at ?v ?from))
      (at ?v ?to)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-road_0026-tw_00090
    :parameters (?v - vehicle ?r - road_type_road_0026 ?from - loc_type_loc_0019 ?to - loc_type_loc_0023 ?w - window_type_tw_00090)
    :precondition (and
      (at ?v ?from)
      (connects ?r ?from ?to)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
      (not (location-blocked ?from))
      (not (location-blocked ?to))
    )
    :effect (and
      (not (at ?v ?from))
      (at ?v ?to)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-road_0026-tw_00120
    :parameters (?v - vehicle ?r - road_type_road_0026 ?from - loc_type_loc_0019 ?to - loc_type_loc_0023 ?w - window_type_tw_00120)
    :precondition (and
      (at ?v ?from)
      (connects ?r ?from ?to)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
      (not (location-blocked ?from))
      (not (location-blocked ?to))
    )
    :effect (and
      (not (at ?v ?from))
      (at ?v ?to)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-road_0026-tw_00150
    :parameters (?v - vehicle ?r - road_type_road_0026 ?from - loc_type_loc_0019 ?to - loc_type_loc_0023 ?w - window_type_tw_00150)
    :precondition (and
      (at ?v ?from)
      (connects ?r ?from ?to)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
      (not (location-blocked ?from))
      (not (location-blocked ?to))
    )
    :effect (and
      (not (at ?v ?from))
      (at ?v ?to)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-road_0026-tw_00180
    :parameters (?v - vehicle ?r - road_type_road_0026 ?from - loc_type_loc_0019 ?to - loc_type_loc_0023 ?w - window_type_tw_00180)
    :precondition (and
      (at ?v ?from)
      (connects ?r ?from ?to)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
      (not (location-blocked ?from))
      (not (location-blocked ?to))
    )
    :effect (and
      (not (at ?v ?from))
      (at ?v ?to)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-road_0026-tw_00210
    :parameters (?v - vehicle ?r - road_type_road_0026 ?from - loc_type_loc_0019 ?to - loc_type_loc_0023 ?w - window_type_tw_00210)
    :precondition (and
      (at ?v ?from)
      (connects ?r ?from ?to)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
      (not (location-blocked ?from))
      (not (location-blocked ?to))
    )
    :effect (and
      (not (at ?v ?from))
      (at ?v ?to)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-road_0026-tw_00240
    :parameters (?v - vehicle ?r - road_type_road_0026 ?from - loc_type_loc_0019 ?to - loc_type_loc_0023 ?w - window_type_tw_00240)
    :precondition (and
      (at ?v ?from)
      (connects ?r ?from ?to)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
      (not (location-blocked ?from))
      (not (location-blocked ?to))
    )
    :effect (and
      (not (at ?v ?from))
      (at ?v ?to)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-road_0026-tw_00270
    :parameters (?v - vehicle ?r - road_type_road_0026 ?from - loc_type_loc_0019 ?to - loc_type_loc_0023 ?w - window_type_tw_00270)
    :precondition (and
      (at ?v ?from)
      (connects ?r ?from ?to)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
      (not (location-blocked ?from))
      (not (location-blocked ?to))
    )
    :effect (and
      (not (at ?v ?from))
      (at ?v ?to)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-road_0026-tw_00300
    :parameters (?v - vehicle ?r - road_type_road_0026 ?from - loc_type_loc_0019 ?to - loc_type_loc_0023 ?w - window_type_tw_00300)
    :precondition (and
      (at ?v ?from)
      (connects ?r ?from ?to)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
      (not (location-blocked ?from))
      (not (location-blocked ?to))
    )
    :effect (and
      (not (at ?v ?from))
      (at ?v ?to)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-road_0026-tw_00330
    :parameters (?v - vehicle ?r - road_type_road_0026 ?from - loc_type_loc_0019 ?to - loc_type_loc_0023 ?w - window_type_tw_00330)
    :precondition (and
      (at ?v ?from)
      (connects ?r ?from ?to)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
      (not (location-blocked ?from))
      (not (location-blocked ?to))
    )
    :effect (and
      (not (at ?v ?from))
      (at ?v ?to)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-road_0026-tw_00360
    :parameters (?v - vehicle ?r - road_type_road_0026 ?from - loc_type_loc_0019 ?to - loc_type_loc_0023 ?w - window_type_tw_00360)
    :precondition (and
      (at ?v ?from)
      (connects ?r ?from ?to)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
      (not (location-blocked ?from))
      (not (location-blocked ?to))
    )
    :effect (and
      (not (at ?v ?from))
      (at ?v ?to)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-road_0026-tw_00390
    :parameters (?v - vehicle ?r - road_type_road_0026 ?from - loc_type_loc_0019 ?to - loc_type_loc_0023 ?w - window_type_tw_00390)
    :precondition (and
      (at ?v ?from)
      (connects ?r ?from ?to)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
      (not (location-blocked ?from))
      (not (location-blocked ?to))
    )
    :effect (and
      (not (at ?v ?from))
      (at ?v ?to)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-road_0026-tw_00420
    :parameters (?v - vehicle ?r - road_type_road_0026 ?from - loc_type_loc_0019 ?to - loc_type_loc_0023 ?w - window_type_tw_00420)
    :precondition (and
      (at ?v ?from)
      (connects ?r ?from ?to)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
      (not (location-blocked ?from))
      (not (location-blocked ?to))
    )
    :effect (and
      (not (at ?v ?from))
      (at ?v ?to)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-road_0026-tw_00450
    :parameters (?v - vehicle ?r - road_type_road_0026 ?from - loc_type_loc_0019 ?to - loc_type_loc_0023 ?w - window_type_tw_00450)
    :precondition (and
      (at ?v ?from)
      (connects ?r ?from ?to)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
      (not (location-blocked ?from))
      (not (location-blocked ?to))
    )
    :effect (and
      (not (at ?v ?from))
      (at ?v ?to)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-road_0027-tw_00000
    :parameters (?v - vehicle ?r - road_type_road_0027 ?from - loc_type_loc_0019 ?to - loc_type_loc_0095 ?w - window_type_tw_00000)
    :precondition (and
      (at ?v ?from)
      (connects ?r ?from ?to)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
      (not (location-blocked ?from))
      (not (location-blocked ?to))
    )
    :effect (and
      (not (at ?v ?from))
      (at ?v ?to)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-road_0027-tw_00030
    :parameters (?v - vehicle ?r - road_type_road_0027 ?from - loc_type_loc_0019 ?to - loc_type_loc_0095 ?w - window_type_tw_00030)
    :precondition (and
      (at ?v ?from)
      (connects ?r ?from ?to)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
      (not (location-blocked ?from))
      (not (location-blocked ?to))
    )
    :effect (and
      (not (at ?v ?from))
      (at ?v ?to)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-road_0027-tw_00060
    :parameters (?v - vehicle ?r - road_type_road_0027 ?from - loc_type_loc_0019 ?to - loc_type_loc_0095 ?w - window_type_tw_00060)
    :precondition (and
      (at ?v ?from)
      (connects ?r ?from ?to)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
      (not (location-blocked ?from))
      (not (location-blocked ?to))
    )
    :effect (and
      (not (at ?v ?from))
      (at ?v ?to)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-road_0027-tw_00090
    :parameters (?v - vehicle ?r - road_type_road_0027 ?from - loc_type_loc_0019 ?to - loc_type_loc_0095 ?w - window_type_tw_00090)
    :precondition (and
      (at ?v ?from)
      (connects ?r ?from ?to)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
      (not (location-blocked ?from))
      (not (location-blocked ?to))
    )
    :effect (and
      (not (at ?v ?from))
      (at ?v ?to)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-road_0027-tw_00120
    :parameters (?v - vehicle ?r - road_type_road_0027 ?from - loc_type_loc_0019 ?to - loc_type_loc_0095 ?w - window_type_tw_00120)
    :precondition (and
      (at ?v ?from)
      (connects ?r ?from ?to)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
      (not (location-blocked ?from))
      (not (location-blocked ?to))
    )
    :effect (and
      (not (at ?v ?from))
      (at ?v ?to)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-road_0027-tw_00150
    :parameters (?v - vehicle ?r - road_type_road_0027 ?from - loc_type_loc_0019 ?to - loc_type_loc_0095 ?w - window_type_tw_00150)
    :precondition (and
      (at ?v ?from)
      (connects ?r ?from ?to)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
      (not (location-blocked ?from))
      (not (location-blocked ?to))
    )
    :effect (and
      (not (at ?v ?from))
      (at ?v ?to)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-road_0027-tw_00180
    :parameters (?v - vehicle ?r - road_type_road_0027 ?from - loc_type_loc_0019 ?to - loc_type_loc_0095 ?w - window_type_tw_00180)
    :precondition (and
      (at ?v ?from)
      (connects ?r ?from ?to)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
      (not (location-blocked ?from))
      (not (location-blocked ?to))
    )
    :effect (and
      (not (at ?v ?from))
      (at ?v ?to)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-road_0027-tw_00210
    :parameters (?v - vehicle ?r - road_type_road_0027 ?from - loc_type_loc_0019 ?to - loc_type_loc_0095 ?w - window_type_tw_00210)
    :precondition (and
      (at ?v ?from)
      (connects ?r ?from ?to)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
      (not (location-blocked ?from))
      (not (location-blocked ?to))
    )
    :effect (and
      (not (at ?v ?from))
      (at ?v ?to)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-road_0027-tw_00240
    :parameters (?v - vehicle ?r - road_type_road_0027 ?from - loc_type_loc_0019 ?to - loc_type_loc_0095 ?w - window_type_tw_00240)
    :precondition (and
      (at ?v ?from)
      (connects ?r ?from ?to)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
      (not (location-blocked ?from))
      (not (location-blocked ?to))
    )
    :effect (and
      (not (at ?v ?from))
      (at ?v ?to)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-road_0027-tw_00270
    :parameters (?v - vehicle ?r - road_type_road_0027 ?from - loc_type_loc_0019 ?to - loc_type_loc_0095 ?w - window_type_tw_00270)
    :precondition (and
      (at ?v ?from)
      (connects ?r ?from ?to)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
      (not (location-blocked ?from))
      (not (location-blocked ?to))
    )
    :effect (and
      (not (at ?v ?from))
      (at ?v ?to)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-road_0027-tw_00300
    :parameters (?v - vehicle ?r - road_type_road_0027 ?from - loc_type_loc_0019 ?to - loc_type_loc_0095 ?w - window_type_tw_00300)
    :precondition (and
      (at ?v ?from)
      (connects ?r ?from ?to)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
      (not (location-blocked ?from))
      (not (location-blocked ?to))
    )
    :effect (and
      (not (at ?v ?from))
      (at ?v ?to)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-road_0027-tw_00330
    :parameters (?v - vehicle ?r - road_type_road_0027 ?from - loc_type_loc_0019 ?to - loc_type_loc_0095 ?w - window_type_tw_00330)
    :precondition (and
      (at ?v ?from)
      (connects ?r ?from ?to)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
      (not (location-blocked ?from))
      (not (location-blocked ?to))
    )
    :effect (and
      (not (at ?v ?from))
      (at ?v ?to)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-road_0027-tw_00360
    :parameters (?v - vehicle ?r - road_type_road_0027 ?from - loc_type_loc_0019 ?to - loc_type_loc_0095 ?w - window_type_tw_00360)
    :precondition (and
      (at ?v ?from)
      (connects ?r ?from ?to)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
      (not (location-blocked ?from))
      (not (location-blocked ?to))
    )
    :effect (and
      (not (at ?v ?from))
      (at ?v ?to)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-road_0027-tw_00390
    :parameters (?v - vehicle ?r - road_type_road_0027 ?from - loc_type_loc_0019 ?to - loc_type_loc_0095 ?w - window_type_tw_00390)
    :precondition (and
      (at ?v ?from)
      (connects ?r ?from ?to)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
      (not (location-blocked ?from))
      (not (location-blocked ?to))
    )
    :effect (and
      (not (at ?v ?from))
      (at ?v ?to)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-road_0027-tw_00420
    :parameters (?v - vehicle ?r - road_type_road_0027 ?from - loc_type_loc_0019 ?to - loc_type_loc_0095 ?w - window_type_tw_00420)
    :precondition (and
      (at ?v ?from)
      (connects ?r ?from ?to)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
      (not (location-blocked ?from))
      (not (location-blocked ?to))
    )
    :effect (and
      (not (at ?v ?from))
      (at ?v ?to)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-road_0027-tw_00450
    :parameters (?v - vehicle ?r - road_type_road_0027 ?from - loc_type_loc_0019 ?to - loc_type_loc_0095 ?w - window_type_tw_00450)
    :precondition (and
      (at ?v ?from)
      (connects ?r ?from ?to)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
      (not (location-blocked ?from))
      (not (location-blocked ?to))
    )
    :effect (and
      (not (at ?v ?from))
      (at ?v ?to)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-road_0028-tw_00000
    :parameters (?v - vehicle ?r - road_type_road_0028 ?from - loc_type_loc_0020 ?to - loc_type_loc_0030 ?w - window_type_tw_00000)
    :precondition (and
      (at ?v ?from)
      (connects ?r ?from ?to)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
      (not (location-blocked ?from))
      (not (location-blocked ?to))
    )
    :effect (and
      (not (at ?v ?from))
      (at ?v ?to)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-road_0028-tw_00030
    :parameters (?v - vehicle ?r - road_type_road_0028 ?from - loc_type_loc_0020 ?to - loc_type_loc_0030 ?w - window_type_tw_00030)
    :precondition (and
      (at ?v ?from)
      (connects ?r ?from ?to)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
      (not (location-blocked ?from))
      (not (location-blocked ?to))
    )
    :effect (and
      (not (at ?v ?from))
      (at ?v ?to)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-road_0028-tw_00060
    :parameters (?v - vehicle ?r - road_type_road_0028 ?from - loc_type_loc_0020 ?to - loc_type_loc_0030 ?w - window_type_tw_00060)
    :precondition (and
      (at ?v ?from)
      (connects ?r ?from ?to)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
      (not (location-blocked ?from))
      (not (location-blocked ?to))
    )
    :effect (and
      (not (at ?v ?from))
      (at ?v ?to)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-road_0028-tw_00090
    :parameters (?v - vehicle ?r - road_type_road_0028 ?from - loc_type_loc_0020 ?to - loc_type_loc_0030 ?w - window_type_tw_00090)
    :precondition (and
      (at ?v ?from)
      (connects ?r ?from ?to)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
      (not (location-blocked ?from))
      (not (location-blocked ?to))
    )
    :effect (and
      (not (at ?v ?from))
      (at ?v ?to)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-road_0028-tw_00120
    :parameters (?v - vehicle ?r - road_type_road_0028 ?from - loc_type_loc_0020 ?to - loc_type_loc_0030 ?w - window_type_tw_00120)
    :precondition (and
      (at ?v ?from)
      (connects ?r ?from ?to)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
      (not (location-blocked ?from))
      (not (location-blocked ?to))
    )
    :effect (and
      (not (at ?v ?from))
      (at ?v ?to)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-road_0028-tw_00150
    :parameters (?v - vehicle ?r - road_type_road_0028 ?from - loc_type_loc_0020 ?to - loc_type_loc_0030 ?w - window_type_tw_00150)
    :precondition (and
      (at ?v ?from)
      (connects ?r ?from ?to)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
      (not (location-blocked ?from))
      (not (location-blocked ?to))
    )
    :effect (and
      (not (at ?v ?from))
      (at ?v ?to)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-road_0028-tw_00180
    :parameters (?v - vehicle ?r - road_type_road_0028 ?from - loc_type_loc_0020 ?to - loc_type_loc_0030 ?w - window_type_tw_00180)
    :precondition (and
      (at ?v ?from)
      (connects ?r ?from ?to)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
      (not (location-blocked ?from))
      (not (location-blocked ?to))
    )
    :effect (and
      (not (at ?v ?from))
      (at ?v ?to)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-road_0028-tw_00210
    :parameters (?v - vehicle ?r - road_type_road_0028 ?from - loc_type_loc_0020 ?to - loc_type_loc_0030 ?w - window_type_tw_00210)
    :precondition (and
      (at ?v ?from)
      (connects ?r ?from ?to)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
      (not (location-blocked ?from))
      (not (location-blocked ?to))
    )
    :effect (and
      (not (at ?v ?from))
      (at ?v ?to)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-road_0028-tw_00240
    :parameters (?v - vehicle ?r - road_type_road_0028 ?from - loc_type_loc_0020 ?to - loc_type_loc_0030 ?w - window_type_tw_00240)
    :precondition (and
      (at ?v ?from)
      (connects ?r ?from ?to)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
      (not (location-blocked ?from))
      (not (location-blocked ?to))
    )
    :effect (and
      (not (at ?v ?from))
      (at ?v ?to)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-road_0028-tw_00270
    :parameters (?v - vehicle ?r - road_type_road_0028 ?from - loc_type_loc_0020 ?to - loc_type_loc_0030 ?w - window_type_tw_00270)
    :precondition (and
      (at ?v ?from)
      (connects ?r ?from ?to)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
      (not (location-blocked ?from))
      (not (location-blocked ?to))
    )
    :effect (and
      (not (at ?v ?from))
      (at ?v ?to)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-road_0028-tw_00300
    :parameters (?v - vehicle ?r - road_type_road_0028 ?from - loc_type_loc_0020 ?to - loc_type_loc_0030 ?w - window_type_tw_00300)
    :precondition (and
      (at ?v ?from)
      (connects ?r ?from ?to)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
      (not (location-blocked ?from))
      (not (location-blocked ?to))
    )
    :effect (and
      (not (at ?v ?from))
      (at ?v ?to)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-road_0028-tw_00330
    :parameters (?v - vehicle ?r - road_type_road_0028 ?from - loc_type_loc_0020 ?to - loc_type_loc_0030 ?w - window_type_tw_00330)
    :precondition (and
      (at ?v ?from)
      (connects ?r ?from ?to)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
      (not (location-blocked ?from))
      (not (location-blocked ?to))
    )
    :effect (and
      (not (at ?v ?from))
      (at ?v ?to)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-road_0028-tw_00360
    :parameters (?v - vehicle ?r - road_type_road_0028 ?from - loc_type_loc_0020 ?to - loc_type_loc_0030 ?w - window_type_tw_00360)
    :precondition (and
      (at ?v ?from)
      (connects ?r ?from ?to)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
      (not (location-blocked ?from))
      (not (location-blocked ?to))
    )
    :effect (and
      (not (at ?v ?from))
      (at ?v ?to)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-road_0028-tw_00390
    :parameters (?v - vehicle ?r - road_type_road_0028 ?from - loc_type_loc_0020 ?to - loc_type_loc_0030 ?w - window_type_tw_00390)
    :precondition (and
      (at ?v ?from)
      (connects ?r ?from ?to)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
      (not (location-blocked ?from))
      (not (location-blocked ?to))
    )
    :effect (and
      (not (at ?v ?from))
      (at ?v ?to)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-road_0028-tw_00420
    :parameters (?v - vehicle ?r - road_type_road_0028 ?from - loc_type_loc_0020 ?to - loc_type_loc_0030 ?w - window_type_tw_00420)
    :precondition (and
      (at ?v ?from)
      (connects ?r ?from ?to)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
      (not (location-blocked ?from))
      (not (location-blocked ?to))
    )
    :effect (and
      (not (at ?v ?from))
      (at ?v ?to)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-road_0028-tw_00450
    :parameters (?v - vehicle ?r - road_type_road_0028 ?from - loc_type_loc_0020 ?to - loc_type_loc_0030 ?w - window_type_tw_00450)
    :precondition (and
      (at ?v ?from)
      (connects ?r ?from ?to)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
      (not (location-blocked ?from))
      (not (location-blocked ?to))
    )
    :effect (and
      (not (at ?v ?from))
      (at ?v ?to)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-road_0029-tw_00000
    :parameters (?v - vehicle ?r - road_type_road_0029 ?from - loc_type_loc_0020 ?to - loc_type_loc_0041 ?w - window_type_tw_00000)
    :precondition (and
      (at ?v ?from)
      (connects ?r ?from ?to)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
      (not (location-blocked ?from))
      (not (location-blocked ?to))
    )
    :effect (and
      (not (at ?v ?from))
      (at ?v ?to)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-road_0029-tw_00030
    :parameters (?v - vehicle ?r - road_type_road_0029 ?from - loc_type_loc_0020 ?to - loc_type_loc_0041 ?w - window_type_tw_00030)
    :precondition (and
      (at ?v ?from)
      (connects ?r ?from ?to)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
      (not (location-blocked ?from))
      (not (location-blocked ?to))
    )
    :effect (and
      (not (at ?v ?from))
      (at ?v ?to)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-road_0029-tw_00060
    :parameters (?v - vehicle ?r - road_type_road_0029 ?from - loc_type_loc_0020 ?to - loc_type_loc_0041 ?w - window_type_tw_00060)
    :precondition (and
      (at ?v ?from)
      (connects ?r ?from ?to)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
      (not (location-blocked ?from))
      (not (location-blocked ?to))
    )
    :effect (and
      (not (at ?v ?from))
      (at ?v ?to)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-road_0029-tw_00090
    :parameters (?v - vehicle ?r - road_type_road_0029 ?from - loc_type_loc_0020 ?to - loc_type_loc_0041 ?w - window_type_tw_00090)
    :precondition (and
      (at ?v ?from)
      (connects ?r ?from ?to)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
      (not (location-blocked ?from))
      (not (location-blocked ?to))
    )
    :effect (and
      (not (at ?v ?from))
      (at ?v ?to)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-road_0029-tw_00120
    :parameters (?v - vehicle ?r - road_type_road_0029 ?from - loc_type_loc_0020 ?to - loc_type_loc_0041 ?w - window_type_tw_00120)
    :precondition (and
      (at ?v ?from)
      (connects ?r ?from ?to)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
      (not (location-blocked ?from))
      (not (location-blocked ?to))
    )
    :effect (and
      (not (at ?v ?from))
      (at ?v ?to)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-road_0029-tw_00150
    :parameters (?v - vehicle ?r - road_type_road_0029 ?from - loc_type_loc_0020 ?to - loc_type_loc_0041 ?w - window_type_tw_00150)
    :precondition (and
      (at ?v ?from)
      (connects ?r ?from ?to)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
      (not (location-blocked ?from))
      (not (location-blocked ?to))
    )
    :effect (and
      (not (at ?v ?from))
      (at ?v ?to)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-road_0029-tw_00180
    :parameters (?v - vehicle ?r - road_type_road_0029 ?from - loc_type_loc_0020 ?to - loc_type_loc_0041 ?w - window_type_tw_00180)
    :precondition (and
      (at ?v ?from)
      (connects ?r ?from ?to)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
      (not (location-blocked ?from))
      (not (location-blocked ?to))
    )
    :effect (and
      (not (at ?v ?from))
      (at ?v ?to)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-road_0029-tw_00210
    :parameters (?v - vehicle ?r - road_type_road_0029 ?from - loc_type_loc_0020 ?to - loc_type_loc_0041 ?w - window_type_tw_00210)
    :precondition (and
      (at ?v ?from)
      (connects ?r ?from ?to)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
      (not (location-blocked ?from))
      (not (location-blocked ?to))
    )
    :effect (and
      (not (at ?v ?from))
      (at ?v ?to)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-road_0029-tw_00240
    :parameters (?v - vehicle ?r - road_type_road_0029 ?from - loc_type_loc_0020 ?to - loc_type_loc_0041 ?w - window_type_tw_00240)
    :precondition (and
      (at ?v ?from)
      (connects ?r ?from ?to)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
      (not (location-blocked ?from))
      (not (location-blocked ?to))
    )
    :effect (and
      (not (at ?v ?from))
      (at ?v ?to)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-road_0029-tw_00270
    :parameters (?v - vehicle ?r - road_type_road_0029 ?from - loc_type_loc_0020 ?to - loc_type_loc_0041 ?w - window_type_tw_00270)
    :precondition (and
      (at ?v ?from)
      (connects ?r ?from ?to)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
      (not (location-blocked ?from))
      (not (location-blocked ?to))
    )
    :effect (and
      (not (at ?v ?from))
      (at ?v ?to)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-road_0029-tw_00300
    :parameters (?v - vehicle ?r - road_type_road_0029 ?from - loc_type_loc_0020 ?to - loc_type_loc_0041 ?w - window_type_tw_00300)
    :precondition (and
      (at ?v ?from)
      (connects ?r ?from ?to)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
      (not (location-blocked ?from))
      (not (location-blocked ?to))
    )
    :effect (and
      (not (at ?v ?from))
      (at ?v ?to)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-road_0029-tw_00330
    :parameters (?v - vehicle ?r - road_type_road_0029 ?from - loc_type_loc_0020 ?to - loc_type_loc_0041 ?w - window_type_tw_00330)
    :precondition (and
      (at ?v ?from)
      (connects ?r ?from ?to)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
      (not (location-blocked ?from))
      (not (location-blocked ?to))
    )
    :effect (and
      (not (at ?v ?from))
      (at ?v ?to)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-road_0029-tw_00360
    :parameters (?v - vehicle ?r - road_type_road_0029 ?from - loc_type_loc_0020 ?to - loc_type_loc_0041 ?w - window_type_tw_00360)
    :precondition (and
      (at ?v ?from)
      (connects ?r ?from ?to)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
      (not (location-blocked ?from))
      (not (location-blocked ?to))
    )
    :effect (and
      (not (at ?v ?from))
      (at ?v ?to)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-road_0029-tw_00390
    :parameters (?v - vehicle ?r - road_type_road_0029 ?from - loc_type_loc_0020 ?to - loc_type_loc_0041 ?w - window_type_tw_00390)
    :precondition (and
      (at ?v ?from)
      (connects ?r ?from ?to)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
      (not (location-blocked ?from))
      (not (location-blocked ?to))
    )
    :effect (and
      (not (at ?v ?from))
      (at ?v ?to)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-road_0029-tw_00420
    :parameters (?v - vehicle ?r - road_type_road_0029 ?from - loc_type_loc_0020 ?to - loc_type_loc_0041 ?w - window_type_tw_00420)
    :precondition (and
      (at ?v ?from)
      (connects ?r ?from ?to)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
      (not (location-blocked ?from))
      (not (location-blocked ?to))
    )
    :effect (and
      (not (at ?v ?from))
      (at ?v ?to)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-road_0029-tw_00450
    :parameters (?v - vehicle ?r - road_type_road_0029 ?from - loc_type_loc_0020 ?to - loc_type_loc_0041 ?w - window_type_tw_00450)
    :precondition (and
      (at ?v ?from)
      (connects ?r ?from ?to)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
      (not (location-blocked ?from))
      (not (location-blocked ?to))
    )
    :effect (and
      (not (at ?v ?from))
      (at ?v ?to)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-road_0030-tw_00000
    :parameters (?v - vehicle ?r - road_type_road_0030 ?from - loc_type_loc_0021 ?to - loc_type_loc_0080 ?w - window_type_tw_00000)
    :precondition (and
      (at ?v ?from)
      (connects ?r ?from ?to)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
      (not (location-blocked ?from))
      (not (location-blocked ?to))
    )
    :effect (and
      (not (at ?v ?from))
      (at ?v ?to)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-road_0030-tw_00030
    :parameters (?v - vehicle ?r - road_type_road_0030 ?from - loc_type_loc_0021 ?to - loc_type_loc_0080 ?w - window_type_tw_00030)
    :precondition (and
      (at ?v ?from)
      (connects ?r ?from ?to)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
      (not (location-blocked ?from))
      (not (location-blocked ?to))
    )
    :effect (and
      (not (at ?v ?from))
      (at ?v ?to)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-road_0030-tw_00060
    :parameters (?v - vehicle ?r - road_type_road_0030 ?from - loc_type_loc_0021 ?to - loc_type_loc_0080 ?w - window_type_tw_00060)
    :precondition (and
      (at ?v ?from)
      (connects ?r ?from ?to)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
      (not (location-blocked ?from))
      (not (location-blocked ?to))
    )
    :effect (and
      (not (at ?v ?from))
      (at ?v ?to)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-road_0030-tw_00090
    :parameters (?v - vehicle ?r - road_type_road_0030 ?from - loc_type_loc_0021 ?to - loc_type_loc_0080 ?w - window_type_tw_00090)
    :precondition (and
      (at ?v ?from)
      (connects ?r ?from ?to)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
      (not (location-blocked ?from))
      (not (location-blocked ?to))
    )
    :effect (and
      (not (at ?v ?from))
      (at ?v ?to)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-road_0030-tw_00120
    :parameters (?v - vehicle ?r - road_type_road_0030 ?from - loc_type_loc_0021 ?to - loc_type_loc_0080 ?w - window_type_tw_00120)
    :precondition (and
      (at ?v ?from)
      (connects ?r ?from ?to)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
      (not (location-blocked ?from))
      (not (location-blocked ?to))
    )
    :effect (and
      (not (at ?v ?from))
      (at ?v ?to)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-road_0030-tw_00150
    :parameters (?v - vehicle ?r - road_type_road_0030 ?from - loc_type_loc_0021 ?to - loc_type_loc_0080 ?w - window_type_tw_00150)
    :precondition (and
      (at ?v ?from)
      (connects ?r ?from ?to)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
      (not (location-blocked ?from))
      (not (location-blocked ?to))
    )
    :effect (and
      (not (at ?v ?from))
      (at ?v ?to)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-road_0030-tw_00180
    :parameters (?v - vehicle ?r - road_type_road_0030 ?from - loc_type_loc_0021 ?to - loc_type_loc_0080 ?w - window_type_tw_00180)
    :precondition (and
      (at ?v ?from)
      (connects ?r ?from ?to)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
      (not (location-blocked ?from))
      (not (location-blocked ?to))
    )
    :effect (and
      (not (at ?v ?from))
      (at ?v ?to)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-road_0030-tw_00210
    :parameters (?v - vehicle ?r - road_type_road_0030 ?from - loc_type_loc_0021 ?to - loc_type_loc_0080 ?w - window_type_tw_00210)
    :precondition (and
      (at ?v ?from)
      (connects ?r ?from ?to)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
      (not (location-blocked ?from))
      (not (location-blocked ?to))
    )
    :effect (and
      (not (at ?v ?from))
      (at ?v ?to)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-road_0030-tw_00240
    :parameters (?v - vehicle ?r - road_type_road_0030 ?from - loc_type_loc_0021 ?to - loc_type_loc_0080 ?w - window_type_tw_00240)
    :precondition (and
      (at ?v ?from)
      (connects ?r ?from ?to)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
      (not (location-blocked ?from))
      (not (location-blocked ?to))
    )
    :effect (and
      (not (at ?v ?from))
      (at ?v ?to)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-road_0030-tw_00270
    :parameters (?v - vehicle ?r - road_type_road_0030 ?from - loc_type_loc_0021 ?to - loc_type_loc_0080 ?w - window_type_tw_00270)
    :precondition (and
      (at ?v ?from)
      (connects ?r ?from ?to)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
      (not (location-blocked ?from))
      (not (location-blocked ?to))
    )
    :effect (and
      (not (at ?v ?from))
      (at ?v ?to)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-road_0030-tw_00300
    :parameters (?v - vehicle ?r - road_type_road_0030 ?from - loc_type_loc_0021 ?to - loc_type_loc_0080 ?w - window_type_tw_00300)
    :precondition (and
      (at ?v ?from)
      (connects ?r ?from ?to)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
      (not (location-blocked ?from))
      (not (location-blocked ?to))
    )
    :effect (and
      (not (at ?v ?from))
      (at ?v ?to)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-road_0030-tw_00330
    :parameters (?v - vehicle ?r - road_type_road_0030 ?from - loc_type_loc_0021 ?to - loc_type_loc_0080 ?w - window_type_tw_00330)
    :precondition (and
      (at ?v ?from)
      (connects ?r ?from ?to)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
      (not (location-blocked ?from))
      (not (location-blocked ?to))
    )
    :effect (and
      (not (at ?v ?from))
      (at ?v ?to)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-road_0030-tw_00360
    :parameters (?v - vehicle ?r - road_type_road_0030 ?from - loc_type_loc_0021 ?to - loc_type_loc_0080 ?w - window_type_tw_00360)
    :precondition (and
      (at ?v ?from)
      (connects ?r ?from ?to)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
      (not (location-blocked ?from))
      (not (location-blocked ?to))
    )
    :effect (and
      (not (at ?v ?from))
      (at ?v ?to)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-road_0030-tw_00390
    :parameters (?v - vehicle ?r - road_type_road_0030 ?from - loc_type_loc_0021 ?to - loc_type_loc_0080 ?w - window_type_tw_00390)
    :precondition (and
      (at ?v ?from)
      (connects ?r ?from ?to)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
      (not (location-blocked ?from))
      (not (location-blocked ?to))
    )
    :effect (and
      (not (at ?v ?from))
      (at ?v ?to)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-road_0030-tw_00420
    :parameters (?v - vehicle ?r - road_type_road_0030 ?from - loc_type_loc_0021 ?to - loc_type_loc_0080 ?w - window_type_tw_00420)
    :precondition (and
      (at ?v ?from)
      (connects ?r ?from ?to)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
      (not (location-blocked ?from))
      (not (location-blocked ?to))
    )
    :effect (and
      (not (at ?v ?from))
      (at ?v ?to)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-road_0030-tw_00450
    :parameters (?v - vehicle ?r - road_type_road_0030 ?from - loc_type_loc_0021 ?to - loc_type_loc_0080 ?w - window_type_tw_00450)
    :precondition (and
      (at ?v ?from)
      (connects ?r ?from ?to)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
      (not (location-blocked ?from))
      (not (location-blocked ?to))
    )
    :effect (and
      (not (at ?v ?from))
      (at ?v ?to)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-road_0031-tw_00000
    :parameters (?v - vehicle ?r - road_type_road_0031 ?from - loc_type_loc_0021 ?to - loc_type_loc_0057 ?w - window_type_tw_00000)
    :precondition (and
      (at ?v ?from)
      (connects ?r ?from ?to)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
      (not (location-blocked ?from))
      (not (location-blocked ?to))
    )
    :effect (and
      (not (at ?v ?from))
      (at ?v ?to)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-road_0031-tw_00030
    :parameters (?v - vehicle ?r - road_type_road_0031 ?from - loc_type_loc_0021 ?to - loc_type_loc_0057 ?w - window_type_tw_00030)
    :precondition (and
      (at ?v ?from)
      (connects ?r ?from ?to)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
      (not (location-blocked ?from))
      (not (location-blocked ?to))
    )
    :effect (and
      (not (at ?v ?from))
      (at ?v ?to)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-road_0031-tw_00060
    :parameters (?v - vehicle ?r - road_type_road_0031 ?from - loc_type_loc_0021 ?to - loc_type_loc_0057 ?w - window_type_tw_00060)
    :precondition (and
      (at ?v ?from)
      (connects ?r ?from ?to)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
      (not (location-blocked ?from))
      (not (location-blocked ?to))
    )
    :effect (and
      (not (at ?v ?from))
      (at ?v ?to)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-road_0031-tw_00090
    :parameters (?v - vehicle ?r - road_type_road_0031 ?from - loc_type_loc_0021 ?to - loc_type_loc_0057 ?w - window_type_tw_00090)
    :precondition (and
      (at ?v ?from)
      (connects ?r ?from ?to)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
      (not (location-blocked ?from))
      (not (location-blocked ?to))
    )
    :effect (and
      (not (at ?v ?from))
      (at ?v ?to)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-road_0031-tw_00120
    :parameters (?v - vehicle ?r - road_type_road_0031 ?from - loc_type_loc_0021 ?to - loc_type_loc_0057 ?w - window_type_tw_00120)
    :precondition (and
      (at ?v ?from)
      (connects ?r ?from ?to)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
      (not (location-blocked ?from))
      (not (location-blocked ?to))
    )
    :effect (and
      (not (at ?v ?from))
      (at ?v ?to)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-road_0031-tw_00150
    :parameters (?v - vehicle ?r - road_type_road_0031 ?from - loc_type_loc_0021 ?to - loc_type_loc_0057 ?w - window_type_tw_00150)
    :precondition (and
      (at ?v ?from)
      (connects ?r ?from ?to)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
      (not (location-blocked ?from))
      (not (location-blocked ?to))
    )
    :effect (and
      (not (at ?v ?from))
      (at ?v ?to)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-road_0031-tw_00180
    :parameters (?v - vehicle ?r - road_type_road_0031 ?from - loc_type_loc_0021 ?to - loc_type_loc_0057 ?w - window_type_tw_00180)
    :precondition (and
      (at ?v ?from)
      (connects ?r ?from ?to)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
      (not (location-blocked ?from))
      (not (location-blocked ?to))
    )
    :effect (and
      (not (at ?v ?from))
      (at ?v ?to)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-road_0031-tw_00210
    :parameters (?v - vehicle ?r - road_type_road_0031 ?from - loc_type_loc_0021 ?to - loc_type_loc_0057 ?w - window_type_tw_00210)
    :precondition (and
      (at ?v ?from)
      (connects ?r ?from ?to)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
      (not (location-blocked ?from))
      (not (location-blocked ?to))
    )
    :effect (and
      (not (at ?v ?from))
      (at ?v ?to)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-road_0031-tw_00240
    :parameters (?v - vehicle ?r - road_type_road_0031 ?from - loc_type_loc_0021 ?to - loc_type_loc_0057 ?w - window_type_tw_00240)
    :precondition (and
      (at ?v ?from)
      (connects ?r ?from ?to)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
      (not (location-blocked ?from))
      (not (location-blocked ?to))
    )
    :effect (and
      (not (at ?v ?from))
      (at ?v ?to)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-road_0031-tw_00270
    :parameters (?v - vehicle ?r - road_type_road_0031 ?from - loc_type_loc_0021 ?to - loc_type_loc_0057 ?w - window_type_tw_00270)
    :precondition (and
      (at ?v ?from)
      (connects ?r ?from ?to)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
      (not (location-blocked ?from))
      (not (location-blocked ?to))
    )
    :effect (and
      (not (at ?v ?from))
      (at ?v ?to)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-road_0031-tw_00300
    :parameters (?v - vehicle ?r - road_type_road_0031 ?from - loc_type_loc_0021 ?to - loc_type_loc_0057 ?w - window_type_tw_00300)
    :precondition (and
      (at ?v ?from)
      (connects ?r ?from ?to)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
      (not (location-blocked ?from))
      (not (location-blocked ?to))
    )
    :effect (and
      (not (at ?v ?from))
      (at ?v ?to)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-road_0031-tw_00330
    :parameters (?v - vehicle ?r - road_type_road_0031 ?from - loc_type_loc_0021 ?to - loc_type_loc_0057 ?w - window_type_tw_00330)
    :precondition (and
      (at ?v ?from)
      (connects ?r ?from ?to)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
      (not (location-blocked ?from))
      (not (location-blocked ?to))
    )
    :effect (and
      (not (at ?v ?from))
      (at ?v ?to)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-road_0031-tw_00360
    :parameters (?v - vehicle ?r - road_type_road_0031 ?from - loc_type_loc_0021 ?to - loc_type_loc_0057 ?w - window_type_tw_00360)
    :precondition (and
      (at ?v ?from)
      (connects ?r ?from ?to)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
      (not (location-blocked ?from))
      (not (location-blocked ?to))
    )
    :effect (and
      (not (at ?v ?from))
      (at ?v ?to)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-road_0031-tw_00390
    :parameters (?v - vehicle ?r - road_type_road_0031 ?from - loc_type_loc_0021 ?to - loc_type_loc_0057 ?w - window_type_tw_00390)
    :precondition (and
      (at ?v ?from)
      (connects ?r ?from ?to)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
      (not (location-blocked ?from))
      (not (location-blocked ?to))
    )
    :effect (and
      (not (at ?v ?from))
      (at ?v ?to)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-road_0031-tw_00420
    :parameters (?v - vehicle ?r - road_type_road_0031 ?from - loc_type_loc_0021 ?to - loc_type_loc_0057 ?w - window_type_tw_00420)
    :precondition (and
      (at ?v ?from)
      (connects ?r ?from ?to)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
      (not (location-blocked ?from))
      (not (location-blocked ?to))
    )
    :effect (and
      (not (at ?v ?from))
      (at ?v ?to)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-road_0031-tw_00450
    :parameters (?v - vehicle ?r - road_type_road_0031 ?from - loc_type_loc_0021 ?to - loc_type_loc_0057 ?w - window_type_tw_00450)
    :precondition (and
      (at ?v ?from)
      (connects ?r ?from ?to)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
      (not (location-blocked ?from))
      (not (location-blocked ?to))
    )
    :effect (and
      (not (at ?v ?from))
      (at ?v ?to)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-road_0032-tw_00000
    :parameters (?v - vehicle ?r - road_type_road_0032 ?from - loc_type_loc_0022 ?to - loc_type_loc_0065 ?w - window_type_tw_00000)
    :precondition (and
      (at ?v ?from)
      (connects ?r ?from ?to)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
      (not (location-blocked ?from))
      (not (location-blocked ?to))
    )
    :effect (and
      (not (at ?v ?from))
      (at ?v ?to)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-road_0032-tw_00030
    :parameters (?v - vehicle ?r - road_type_road_0032 ?from - loc_type_loc_0022 ?to - loc_type_loc_0065 ?w - window_type_tw_00030)
    :precondition (and
      (at ?v ?from)
      (connects ?r ?from ?to)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
      (not (location-blocked ?from))
      (not (location-blocked ?to))
    )
    :effect (and
      (not (at ?v ?from))
      (at ?v ?to)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-road_0032-tw_00060
    :parameters (?v - vehicle ?r - road_type_road_0032 ?from - loc_type_loc_0022 ?to - loc_type_loc_0065 ?w - window_type_tw_00060)
    :precondition (and
      (at ?v ?from)
      (connects ?r ?from ?to)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
      (not (location-blocked ?from))
      (not (location-blocked ?to))
    )
    :effect (and
      (not (at ?v ?from))
      (at ?v ?to)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-road_0032-tw_00090
    :parameters (?v - vehicle ?r - road_type_road_0032 ?from - loc_type_loc_0022 ?to - loc_type_loc_0065 ?w - window_type_tw_00090)
    :precondition (and
      (at ?v ?from)
      (connects ?r ?from ?to)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
      (not (location-blocked ?from))
      (not (location-blocked ?to))
    )
    :effect (and
      (not (at ?v ?from))
      (at ?v ?to)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-road_0032-tw_00120
    :parameters (?v - vehicle ?r - road_type_road_0032 ?from - loc_type_loc_0022 ?to - loc_type_loc_0065 ?w - window_type_tw_00120)
    :precondition (and
      (at ?v ?from)
      (connects ?r ?from ?to)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
      (not (location-blocked ?from))
      (not (location-blocked ?to))
    )
    :effect (and
      (not (at ?v ?from))
      (at ?v ?to)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-road_0032-tw_00150
    :parameters (?v - vehicle ?r - road_type_road_0032 ?from - loc_type_loc_0022 ?to - loc_type_loc_0065 ?w - window_type_tw_00150)
    :precondition (and
      (at ?v ?from)
      (connects ?r ?from ?to)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
      (not (location-blocked ?from))
      (not (location-blocked ?to))
    )
    :effect (and
      (not (at ?v ?from))
      (at ?v ?to)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-road_0032-tw_00180
    :parameters (?v - vehicle ?r - road_type_road_0032 ?from - loc_type_loc_0022 ?to - loc_type_loc_0065 ?w - window_type_tw_00180)
    :precondition (and
      (at ?v ?from)
      (connects ?r ?from ?to)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
      (not (location-blocked ?from))
      (not (location-blocked ?to))
    )
    :effect (and
      (not (at ?v ?from))
      (at ?v ?to)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-road_0032-tw_00210
    :parameters (?v - vehicle ?r - road_type_road_0032 ?from - loc_type_loc_0022 ?to - loc_type_loc_0065 ?w - window_type_tw_00210)
    :precondition (and
      (at ?v ?from)
      (connects ?r ?from ?to)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
      (not (location-blocked ?from))
      (not (location-blocked ?to))
    )
    :effect (and
      (not (at ?v ?from))
      (at ?v ?to)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-road_0032-tw_00240
    :parameters (?v - vehicle ?r - road_type_road_0032 ?from - loc_type_loc_0022 ?to - loc_type_loc_0065 ?w - window_type_tw_00240)
    :precondition (and
      (at ?v ?from)
      (connects ?r ?from ?to)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
      (not (location-blocked ?from))
      (not (location-blocked ?to))
    )
    :effect (and
      (not (at ?v ?from))
      (at ?v ?to)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-road_0032-tw_00270
    :parameters (?v - vehicle ?r - road_type_road_0032 ?from - loc_type_loc_0022 ?to - loc_type_loc_0065 ?w - window_type_tw_00270)
    :precondition (and
      (at ?v ?from)
      (connects ?r ?from ?to)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
      (not (location-blocked ?from))
      (not (location-blocked ?to))
    )
    :effect (and
      (not (at ?v ?from))
      (at ?v ?to)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-road_0032-tw_00300
    :parameters (?v - vehicle ?r - road_type_road_0032 ?from - loc_type_loc_0022 ?to - loc_type_loc_0065 ?w - window_type_tw_00300)
    :precondition (and
      (at ?v ?from)
      (connects ?r ?from ?to)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
      (not (location-blocked ?from))
      (not (location-blocked ?to))
    )
    :effect (and
      (not (at ?v ?from))
      (at ?v ?to)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-road_0032-tw_00330
    :parameters (?v - vehicle ?r - road_type_road_0032 ?from - loc_type_loc_0022 ?to - loc_type_loc_0065 ?w - window_type_tw_00330)
    :precondition (and
      (at ?v ?from)
      (connects ?r ?from ?to)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
      (not (location-blocked ?from))
      (not (location-blocked ?to))
    )
    :effect (and
      (not (at ?v ?from))
      (at ?v ?to)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-road_0032-tw_00360
    :parameters (?v - vehicle ?r - road_type_road_0032 ?from - loc_type_loc_0022 ?to - loc_type_loc_0065 ?w - window_type_tw_00360)
    :precondition (and
      (at ?v ?from)
      (connects ?r ?from ?to)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
      (not (location-blocked ?from))
      (not (location-blocked ?to))
    )
    :effect (and
      (not (at ?v ?from))
      (at ?v ?to)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-road_0032-tw_00390
    :parameters (?v - vehicle ?r - road_type_road_0032 ?from - loc_type_loc_0022 ?to - loc_type_loc_0065 ?w - window_type_tw_00390)
    :precondition (and
      (at ?v ?from)
      (connects ?r ?from ?to)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
      (not (location-blocked ?from))
      (not (location-blocked ?to))
    )
    :effect (and
      (not (at ?v ?from))
      (at ?v ?to)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-road_0032-tw_00420
    :parameters (?v - vehicle ?r - road_type_road_0032 ?from - loc_type_loc_0022 ?to - loc_type_loc_0065 ?w - window_type_tw_00420)
    :precondition (and
      (at ?v ?from)
      (connects ?r ?from ?to)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
      (not (location-blocked ?from))
      (not (location-blocked ?to))
    )
    :effect (and
      (not (at ?v ?from))
      (at ?v ?to)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-road_0032-tw_00450
    :parameters (?v - vehicle ?r - road_type_road_0032 ?from - loc_type_loc_0022 ?to - loc_type_loc_0065 ?w - window_type_tw_00450)
    :precondition (and
      (at ?v ?from)
      (connects ?r ?from ?to)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
      (not (location-blocked ?from))
      (not (location-blocked ?to))
    )
    :effect (and
      (not (at ?v ?from))
      (at ?v ?to)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-road_0033-tw_00000
    :parameters (?v - vehicle ?r - road_type_road_0033 ?from - loc_type_loc_0022 ?to - loc_type_loc_0021 ?w - window_type_tw_00000)
    :precondition (and
      (at ?v ?from)
      (connects ?r ?from ?to)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
      (not (location-blocked ?from))
      (not (location-blocked ?to))
    )
    :effect (and
      (not (at ?v ?from))
      (at ?v ?to)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-road_0033-tw_00030
    :parameters (?v - vehicle ?r - road_type_road_0033 ?from - loc_type_loc_0022 ?to - loc_type_loc_0021 ?w - window_type_tw_00030)
    :precondition (and
      (at ?v ?from)
      (connects ?r ?from ?to)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
      (not (location-blocked ?from))
      (not (location-blocked ?to))
    )
    :effect (and
      (not (at ?v ?from))
      (at ?v ?to)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-road_0033-tw_00060
    :parameters (?v - vehicle ?r - road_type_road_0033 ?from - loc_type_loc_0022 ?to - loc_type_loc_0021 ?w - window_type_tw_00060)
    :precondition (and
      (at ?v ?from)
      (connects ?r ?from ?to)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
      (not (location-blocked ?from))
      (not (location-blocked ?to))
    )
    :effect (and
      (not (at ?v ?from))
      (at ?v ?to)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-road_0033-tw_00090
    :parameters (?v - vehicle ?r - road_type_road_0033 ?from - loc_type_loc_0022 ?to - loc_type_loc_0021 ?w - window_type_tw_00090)
    :precondition (and
      (at ?v ?from)
      (connects ?r ?from ?to)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
      (not (location-blocked ?from))
      (not (location-blocked ?to))
    )
    :effect (and
      (not (at ?v ?from))
      (at ?v ?to)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-road_0033-tw_00120
    :parameters (?v - vehicle ?r - road_type_road_0033 ?from - loc_type_loc_0022 ?to - loc_type_loc_0021 ?w - window_type_tw_00120)
    :precondition (and
      (at ?v ?from)
      (connects ?r ?from ?to)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
      (not (location-blocked ?from))
      (not (location-blocked ?to))
    )
    :effect (and
      (not (at ?v ?from))
      (at ?v ?to)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-road_0033-tw_00150
    :parameters (?v - vehicle ?r - road_type_road_0033 ?from - loc_type_loc_0022 ?to - loc_type_loc_0021 ?w - window_type_tw_00150)
    :precondition (and
      (at ?v ?from)
      (connects ?r ?from ?to)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
      (not (location-blocked ?from))
      (not (location-blocked ?to))
    )
    :effect (and
      (not (at ?v ?from))
      (at ?v ?to)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-road_0033-tw_00180
    :parameters (?v - vehicle ?r - road_type_road_0033 ?from - loc_type_loc_0022 ?to - loc_type_loc_0021 ?w - window_type_tw_00180)
    :precondition (and
      (at ?v ?from)
      (connects ?r ?from ?to)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
      (not (location-blocked ?from))
      (not (location-blocked ?to))
    )
    :effect (and
      (not (at ?v ?from))
      (at ?v ?to)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-road_0033-tw_00210
    :parameters (?v - vehicle ?r - road_type_road_0033 ?from - loc_type_loc_0022 ?to - loc_type_loc_0021 ?w - window_type_tw_00210)
    :precondition (and
      (at ?v ?from)
      (connects ?r ?from ?to)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
      (not (location-blocked ?from))
      (not (location-blocked ?to))
    )
    :effect (and
      (not (at ?v ?from))
      (at ?v ?to)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-road_0033-tw_00240
    :parameters (?v - vehicle ?r - road_type_road_0033 ?from - loc_type_loc_0022 ?to - loc_type_loc_0021 ?w - window_type_tw_00240)
    :precondition (and
      (at ?v ?from)
      (connects ?r ?from ?to)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
      (not (location-blocked ?from))
      (not (location-blocked ?to))
    )
    :effect (and
      (not (at ?v ?from))
      (at ?v ?to)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-road_0033-tw_00270
    :parameters (?v - vehicle ?r - road_type_road_0033 ?from - loc_type_loc_0022 ?to - loc_type_loc_0021 ?w - window_type_tw_00270)
    :precondition (and
      (at ?v ?from)
      (connects ?r ?from ?to)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
      (not (location-blocked ?from))
      (not (location-blocked ?to))
    )
    :effect (and
      (not (at ?v ?from))
      (at ?v ?to)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-road_0033-tw_00300
    :parameters (?v - vehicle ?r - road_type_road_0033 ?from - loc_type_loc_0022 ?to - loc_type_loc_0021 ?w - window_type_tw_00300)
    :precondition (and
      (at ?v ?from)
      (connects ?r ?from ?to)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
      (not (location-blocked ?from))
      (not (location-blocked ?to))
    )
    :effect (and
      (not (at ?v ?from))
      (at ?v ?to)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-road_0033-tw_00330
    :parameters (?v - vehicle ?r - road_type_road_0033 ?from - loc_type_loc_0022 ?to - loc_type_loc_0021 ?w - window_type_tw_00330)
    :precondition (and
      (at ?v ?from)
      (connects ?r ?from ?to)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
      (not (location-blocked ?from))
      (not (location-blocked ?to))
    )
    :effect (and
      (not (at ?v ?from))
      (at ?v ?to)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-road_0033-tw_00360
    :parameters (?v - vehicle ?r - road_type_road_0033 ?from - loc_type_loc_0022 ?to - loc_type_loc_0021 ?w - window_type_tw_00360)
    :precondition (and
      (at ?v ?from)
      (connects ?r ?from ?to)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
      (not (location-blocked ?from))
      (not (location-blocked ?to))
    )
    :effect (and
      (not (at ?v ?from))
      (at ?v ?to)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-road_0033-tw_00390
    :parameters (?v - vehicle ?r - road_type_road_0033 ?from - loc_type_loc_0022 ?to - loc_type_loc_0021 ?w - window_type_tw_00390)
    :precondition (and
      (at ?v ?from)
      (connects ?r ?from ?to)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
      (not (location-blocked ?from))
      (not (location-blocked ?to))
    )
    :effect (and
      (not (at ?v ?from))
      (at ?v ?to)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-road_0033-tw_00420
    :parameters (?v - vehicle ?r - road_type_road_0033 ?from - loc_type_loc_0022 ?to - loc_type_loc_0021 ?w - window_type_tw_00420)
    :precondition (and
      (at ?v ?from)
      (connects ?r ?from ?to)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
      (not (location-blocked ?from))
      (not (location-blocked ?to))
    )
    :effect (and
      (not (at ?v ?from))
      (at ?v ?to)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-road_0033-tw_00450
    :parameters (?v - vehicle ?r - road_type_road_0033 ?from - loc_type_loc_0022 ?to - loc_type_loc_0021 ?w - window_type_tw_00450)
    :precondition (and
      (at ?v ?from)
      (connects ?r ?from ?to)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
      (not (location-blocked ?from))
      (not (location-blocked ?to))
    )
    :effect (and
      (not (at ?v ?from))
      (at ?v ?to)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-road_0034-tw_00000
    :parameters (?v - vehicle ?r - road_type_road_0034 ?from - loc_type_loc_0023 ?to - loc_type_loc_0019 ?w - window_type_tw_00000)
    :precondition (and
      (at ?v ?from)
      (connects ?r ?from ?to)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
      (not (location-blocked ?from))
      (not (location-blocked ?to))
    )
    :effect (and
      (not (at ?v ?from))
      (at ?v ?to)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-road_0034-tw_00030
    :parameters (?v - vehicle ?r - road_type_road_0034 ?from - loc_type_loc_0023 ?to - loc_type_loc_0019 ?w - window_type_tw_00030)
    :precondition (and
      (at ?v ?from)
      (connects ?r ?from ?to)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
      (not (location-blocked ?from))
      (not (location-blocked ?to))
    )
    :effect (and
      (not (at ?v ?from))
      (at ?v ?to)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-road_0034-tw_00060
    :parameters (?v - vehicle ?r - road_type_road_0034 ?from - loc_type_loc_0023 ?to - loc_type_loc_0019 ?w - window_type_tw_00060)
    :precondition (and
      (at ?v ?from)
      (connects ?r ?from ?to)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
      (not (location-blocked ?from))
      (not (location-blocked ?to))
    )
    :effect (and
      (not (at ?v ?from))
      (at ?v ?to)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-road_0034-tw_00090
    :parameters (?v - vehicle ?r - road_type_road_0034 ?from - loc_type_loc_0023 ?to - loc_type_loc_0019 ?w - window_type_tw_00090)
    :precondition (and
      (at ?v ?from)
      (connects ?r ?from ?to)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
      (not (location-blocked ?from))
      (not (location-blocked ?to))
    )
    :effect (and
      (not (at ?v ?from))
      (at ?v ?to)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-road_0034-tw_00120
    :parameters (?v - vehicle ?r - road_type_road_0034 ?from - loc_type_loc_0023 ?to - loc_type_loc_0019 ?w - window_type_tw_00120)
    :precondition (and
      (at ?v ?from)
      (connects ?r ?from ?to)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
      (not (location-blocked ?from))
      (not (location-blocked ?to))
    )
    :effect (and
      (not (at ?v ?from))
      (at ?v ?to)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-road_0034-tw_00150
    :parameters (?v - vehicle ?r - road_type_road_0034 ?from - loc_type_loc_0023 ?to - loc_type_loc_0019 ?w - window_type_tw_00150)
    :precondition (and
      (at ?v ?from)
      (connects ?r ?from ?to)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
      (not (location-blocked ?from))
      (not (location-blocked ?to))
    )
    :effect (and
      (not (at ?v ?from))
      (at ?v ?to)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-road_0034-tw_00180
    :parameters (?v - vehicle ?r - road_type_road_0034 ?from - loc_type_loc_0023 ?to - loc_type_loc_0019 ?w - window_type_tw_00180)
    :precondition (and
      (at ?v ?from)
      (connects ?r ?from ?to)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
      (not (location-blocked ?from))
      (not (location-blocked ?to))
    )
    :effect (and
      (not (at ?v ?from))
      (at ?v ?to)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-road_0034-tw_00210
    :parameters (?v - vehicle ?r - road_type_road_0034 ?from - loc_type_loc_0023 ?to - loc_type_loc_0019 ?w - window_type_tw_00210)
    :precondition (and
      (at ?v ?from)
      (connects ?r ?from ?to)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
      (not (location-blocked ?from))
      (not (location-blocked ?to))
    )
    :effect (and
      (not (at ?v ?from))
      (at ?v ?to)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-road_0034-tw_00240
    :parameters (?v - vehicle ?r - road_type_road_0034 ?from - loc_type_loc_0023 ?to - loc_type_loc_0019 ?w - window_type_tw_00240)
    :precondition (and
      (at ?v ?from)
      (connects ?r ?from ?to)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
      (not (location-blocked ?from))
      (not (location-blocked ?to))
    )
    :effect (and
      (not (at ?v ?from))
      (at ?v ?to)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-road_0034-tw_00270
    :parameters (?v - vehicle ?r - road_type_road_0034 ?from - loc_type_loc_0023 ?to - loc_type_loc_0019 ?w - window_type_tw_00270)
    :precondition (and
      (at ?v ?from)
      (connects ?r ?from ?to)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
      (not (location-blocked ?from))
      (not (location-blocked ?to))
    )
    :effect (and
      (not (at ?v ?from))
      (at ?v ?to)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-road_0034-tw_00300
    :parameters (?v - vehicle ?r - road_type_road_0034 ?from - loc_type_loc_0023 ?to - loc_type_loc_0019 ?w - window_type_tw_00300)
    :precondition (and
      (at ?v ?from)
      (connects ?r ?from ?to)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
      (not (location-blocked ?from))
      (not (location-blocked ?to))
    )
    :effect (and
      (not (at ?v ?from))
      (at ?v ?to)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-road_0034-tw_00330
    :parameters (?v - vehicle ?r - road_type_road_0034 ?from - loc_type_loc_0023 ?to - loc_type_loc_0019 ?w - window_type_tw_00330)
    :precondition (and
      (at ?v ?from)
      (connects ?r ?from ?to)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
      (not (location-blocked ?from))
      (not (location-blocked ?to))
    )
    :effect (and
      (not (at ?v ?from))
      (at ?v ?to)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-road_0034-tw_00360
    :parameters (?v - vehicle ?r - road_type_road_0034 ?from - loc_type_loc_0023 ?to - loc_type_loc_0019 ?w - window_type_tw_00360)
    :precondition (and
      (at ?v ?from)
      (connects ?r ?from ?to)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
      (not (location-blocked ?from))
      (not (location-blocked ?to))
    )
    :effect (and
      (not (at ?v ?from))
      (at ?v ?to)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-road_0034-tw_00390
    :parameters (?v - vehicle ?r - road_type_road_0034 ?from - loc_type_loc_0023 ?to - loc_type_loc_0019 ?w - window_type_tw_00390)
    :precondition (and
      (at ?v ?from)
      (connects ?r ?from ?to)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
      (not (location-blocked ?from))
      (not (location-blocked ?to))
    )
    :effect (and
      (not (at ?v ?from))
      (at ?v ?to)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-road_0034-tw_00420
    :parameters (?v - vehicle ?r - road_type_road_0034 ?from - loc_type_loc_0023 ?to - loc_type_loc_0019 ?w - window_type_tw_00420)
    :precondition (and
      (at ?v ?from)
      (connects ?r ?from ?to)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
      (not (location-blocked ?from))
      (not (location-blocked ?to))
    )
    :effect (and
      (not (at ?v ?from))
      (at ?v ?to)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-road_0034-tw_00450
    :parameters (?v - vehicle ?r - road_type_road_0034 ?from - loc_type_loc_0023 ?to - loc_type_loc_0019 ?w - window_type_tw_00450)
    :precondition (and
      (at ?v ?from)
      (connects ?r ?from ?to)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
      (not (location-blocked ?from))
      (not (location-blocked ?to))
    )
    :effect (and
      (not (at ?v ?from))
      (at ?v ?to)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-road_0035-tw_00000
    :parameters (?v - vehicle ?r - road_type_road_0035 ?from - loc_type_loc_0024 ?to - loc_type_loc_0033 ?w - window_type_tw_00000)
    :precondition (and
      (at ?v ?from)
      (connects ?r ?from ?to)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
      (not (location-blocked ?from))
      (not (location-blocked ?to))
    )
    :effect (and
      (not (at ?v ?from))
      (at ?v ?to)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-road_0035-tw_00030
    :parameters (?v - vehicle ?r - road_type_road_0035 ?from - loc_type_loc_0024 ?to - loc_type_loc_0033 ?w - window_type_tw_00030)
    :precondition (and
      (at ?v ?from)
      (connects ?r ?from ?to)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
      (not (location-blocked ?from))
      (not (location-blocked ?to))
    )
    :effect (and
      (not (at ?v ?from))
      (at ?v ?to)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-road_0035-tw_00060
    :parameters (?v - vehicle ?r - road_type_road_0035 ?from - loc_type_loc_0024 ?to - loc_type_loc_0033 ?w - window_type_tw_00060)
    :precondition (and
      (at ?v ?from)
      (connects ?r ?from ?to)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
      (not (location-blocked ?from))
      (not (location-blocked ?to))
    )
    :effect (and
      (not (at ?v ?from))
      (at ?v ?to)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-road_0035-tw_00090
    :parameters (?v - vehicle ?r - road_type_road_0035 ?from - loc_type_loc_0024 ?to - loc_type_loc_0033 ?w - window_type_tw_00090)
    :precondition (and
      (at ?v ?from)
      (connects ?r ?from ?to)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
      (not (location-blocked ?from))
      (not (location-blocked ?to))
    )
    :effect (and
      (not (at ?v ?from))
      (at ?v ?to)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-road_0035-tw_00120
    :parameters (?v - vehicle ?r - road_type_road_0035 ?from - loc_type_loc_0024 ?to - loc_type_loc_0033 ?w - window_type_tw_00120)
    :precondition (and
      (at ?v ?from)
      (connects ?r ?from ?to)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
      (not (location-blocked ?from))
      (not (location-blocked ?to))
    )
    :effect (and
      (not (at ?v ?from))
      (at ?v ?to)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-road_0035-tw_00150
    :parameters (?v - vehicle ?r - road_type_road_0035 ?from - loc_type_loc_0024 ?to - loc_type_loc_0033 ?w - window_type_tw_00150)
    :precondition (and
      (at ?v ?from)
      (connects ?r ?from ?to)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
      (not (location-blocked ?from))
      (not (location-blocked ?to))
    )
    :effect (and
      (not (at ?v ?from))
      (at ?v ?to)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-road_0035-tw_00180
    :parameters (?v - vehicle ?r - road_type_road_0035 ?from - loc_type_loc_0024 ?to - loc_type_loc_0033 ?w - window_type_tw_00180)
    :precondition (and
      (at ?v ?from)
      (connects ?r ?from ?to)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
      (not (location-blocked ?from))
      (not (location-blocked ?to))
    )
    :effect (and
      (not (at ?v ?from))
      (at ?v ?to)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-road_0035-tw_00210
    :parameters (?v - vehicle ?r - road_type_road_0035 ?from - loc_type_loc_0024 ?to - loc_type_loc_0033 ?w - window_type_tw_00210)
    :precondition (and
      (at ?v ?from)
      (connects ?r ?from ?to)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
      (not (location-blocked ?from))
      (not (location-blocked ?to))
    )
    :effect (and
      (not (at ?v ?from))
      (at ?v ?to)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-road_0035-tw_00240
    :parameters (?v - vehicle ?r - road_type_road_0035 ?from - loc_type_loc_0024 ?to - loc_type_loc_0033 ?w - window_type_tw_00240)
    :precondition (and
      (at ?v ?from)
      (connects ?r ?from ?to)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
      (not (location-blocked ?from))
      (not (location-blocked ?to))
    )
    :effect (and
      (not (at ?v ?from))
      (at ?v ?to)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-road_0035-tw_00270
    :parameters (?v - vehicle ?r - road_type_road_0035 ?from - loc_type_loc_0024 ?to - loc_type_loc_0033 ?w - window_type_tw_00270)
    :precondition (and
      (at ?v ?from)
      (connects ?r ?from ?to)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
      (not (location-blocked ?from))
      (not (location-blocked ?to))
    )
    :effect (and
      (not (at ?v ?from))
      (at ?v ?to)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-road_0035-tw_00300
    :parameters (?v - vehicle ?r - road_type_road_0035 ?from - loc_type_loc_0024 ?to - loc_type_loc_0033 ?w - window_type_tw_00300)
    :precondition (and
      (at ?v ?from)
      (connects ?r ?from ?to)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
      (not (location-blocked ?from))
      (not (location-blocked ?to))
    )
    :effect (and
      (not (at ?v ?from))
      (at ?v ?to)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-road_0035-tw_00330
    :parameters (?v - vehicle ?r - road_type_road_0035 ?from - loc_type_loc_0024 ?to - loc_type_loc_0033 ?w - window_type_tw_00330)
    :precondition (and
      (at ?v ?from)
      (connects ?r ?from ?to)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
      (not (location-blocked ?from))
      (not (location-blocked ?to))
    )
    :effect (and
      (not (at ?v ?from))
      (at ?v ?to)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-road_0035-tw_00360
    :parameters (?v - vehicle ?r - road_type_road_0035 ?from - loc_type_loc_0024 ?to - loc_type_loc_0033 ?w - window_type_tw_00360)
    :precondition (and
      (at ?v ?from)
      (connects ?r ?from ?to)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
      (not (location-blocked ?from))
      (not (location-blocked ?to))
    )
    :effect (and
      (not (at ?v ?from))
      (at ?v ?to)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-road_0035-tw_00390
    :parameters (?v - vehicle ?r - road_type_road_0035 ?from - loc_type_loc_0024 ?to - loc_type_loc_0033 ?w - window_type_tw_00390)
    :precondition (and
      (at ?v ?from)
      (connects ?r ?from ?to)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
      (not (location-blocked ?from))
      (not (location-blocked ?to))
    )
    :effect (and
      (not (at ?v ?from))
      (at ?v ?to)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-road_0035-tw_00420
    :parameters (?v - vehicle ?r - road_type_road_0035 ?from - loc_type_loc_0024 ?to - loc_type_loc_0033 ?w - window_type_tw_00420)
    :precondition (and
      (at ?v ?from)
      (connects ?r ?from ?to)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
      (not (location-blocked ?from))
      (not (location-blocked ?to))
    )
    :effect (and
      (not (at ?v ?from))
      (at ?v ?to)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-road_0035-tw_00450
    :parameters (?v - vehicle ?r - road_type_road_0035 ?from - loc_type_loc_0024 ?to - loc_type_loc_0033 ?w - window_type_tw_00450)
    :precondition (and
      (at ?v ?from)
      (connects ?r ?from ?to)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
      (not (location-blocked ?from))
      (not (location-blocked ?to))
    )
    :effect (and
      (not (at ?v ?from))
      (at ?v ?to)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-road_0036-tw_00000
    :parameters (?v - vehicle ?r - road_type_road_0036 ?from - loc_type_loc_0024 ?to - loc_type_loc_0015 ?w - window_type_tw_00000)
    :precondition (and
      (at ?v ?from)
      (connects ?r ?from ?to)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
      (not (location-blocked ?from))
      (not (location-blocked ?to))
    )
    :effect (and
      (not (at ?v ?from))
      (at ?v ?to)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-road_0036-tw_00030
    :parameters (?v - vehicle ?r - road_type_road_0036 ?from - loc_type_loc_0024 ?to - loc_type_loc_0015 ?w - window_type_tw_00030)
    :precondition (and
      (at ?v ?from)
      (connects ?r ?from ?to)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
      (not (location-blocked ?from))
      (not (location-blocked ?to))
    )
    :effect (and
      (not (at ?v ?from))
      (at ?v ?to)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-road_0036-tw_00060
    :parameters (?v - vehicle ?r - road_type_road_0036 ?from - loc_type_loc_0024 ?to - loc_type_loc_0015 ?w - window_type_tw_00060)
    :precondition (and
      (at ?v ?from)
      (connects ?r ?from ?to)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
      (not (location-blocked ?from))
      (not (location-blocked ?to))
    )
    :effect (and
      (not (at ?v ?from))
      (at ?v ?to)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-road_0036-tw_00090
    :parameters (?v - vehicle ?r - road_type_road_0036 ?from - loc_type_loc_0024 ?to - loc_type_loc_0015 ?w - window_type_tw_00090)
    :precondition (and
      (at ?v ?from)
      (connects ?r ?from ?to)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
      (not (location-blocked ?from))
      (not (location-blocked ?to))
    )
    :effect (and
      (not (at ?v ?from))
      (at ?v ?to)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-road_0036-tw_00120
    :parameters (?v - vehicle ?r - road_type_road_0036 ?from - loc_type_loc_0024 ?to - loc_type_loc_0015 ?w - window_type_tw_00120)
    :precondition (and
      (at ?v ?from)
      (connects ?r ?from ?to)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
      (not (location-blocked ?from))
      (not (location-blocked ?to))
    )
    :effect (and
      (not (at ?v ?from))
      (at ?v ?to)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-road_0036-tw_00150
    :parameters (?v - vehicle ?r - road_type_road_0036 ?from - loc_type_loc_0024 ?to - loc_type_loc_0015 ?w - window_type_tw_00150)
    :precondition (and
      (at ?v ?from)
      (connects ?r ?from ?to)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
      (not (location-blocked ?from))
      (not (location-blocked ?to))
    )
    :effect (and
      (not (at ?v ?from))
      (at ?v ?to)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-road_0036-tw_00180
    :parameters (?v - vehicle ?r - road_type_road_0036 ?from - loc_type_loc_0024 ?to - loc_type_loc_0015 ?w - window_type_tw_00180)
    :precondition (and
      (at ?v ?from)
      (connects ?r ?from ?to)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
      (not (location-blocked ?from))
      (not (location-blocked ?to))
    )
    :effect (and
      (not (at ?v ?from))
      (at ?v ?to)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-road_0036-tw_00210
    :parameters (?v - vehicle ?r - road_type_road_0036 ?from - loc_type_loc_0024 ?to - loc_type_loc_0015 ?w - window_type_tw_00210)
    :precondition (and
      (at ?v ?from)
      (connects ?r ?from ?to)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
      (not (location-blocked ?from))
      (not (location-blocked ?to))
    )
    :effect (and
      (not (at ?v ?from))
      (at ?v ?to)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-road_0036-tw_00240
    :parameters (?v - vehicle ?r - road_type_road_0036 ?from - loc_type_loc_0024 ?to - loc_type_loc_0015 ?w - window_type_tw_00240)
    :precondition (and
      (at ?v ?from)
      (connects ?r ?from ?to)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
      (not (location-blocked ?from))
      (not (location-blocked ?to))
    )
    :effect (and
      (not (at ?v ?from))
      (at ?v ?to)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-road_0036-tw_00270
    :parameters (?v - vehicle ?r - road_type_road_0036 ?from - loc_type_loc_0024 ?to - loc_type_loc_0015 ?w - window_type_tw_00270)
    :precondition (and
      (at ?v ?from)
      (connects ?r ?from ?to)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
      (not (location-blocked ?from))
      (not (location-blocked ?to))
    )
    :effect (and
      (not (at ?v ?from))
      (at ?v ?to)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-road_0036-tw_00300
    :parameters (?v - vehicle ?r - road_type_road_0036 ?from - loc_type_loc_0024 ?to - loc_type_loc_0015 ?w - window_type_tw_00300)
    :precondition (and
      (at ?v ?from)
      (connects ?r ?from ?to)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
      (not (location-blocked ?from))
      (not (location-blocked ?to))
    )
    :effect (and
      (not (at ?v ?from))
      (at ?v ?to)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-road_0036-tw_00330
    :parameters (?v - vehicle ?r - road_type_road_0036 ?from - loc_type_loc_0024 ?to - loc_type_loc_0015 ?w - window_type_tw_00330)
    :precondition (and
      (at ?v ?from)
      (connects ?r ?from ?to)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
      (not (location-blocked ?from))
      (not (location-blocked ?to))
    )
    :effect (and
      (not (at ?v ?from))
      (at ?v ?to)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-road_0036-tw_00360
    :parameters (?v - vehicle ?r - road_type_road_0036 ?from - loc_type_loc_0024 ?to - loc_type_loc_0015 ?w - window_type_tw_00360)
    :precondition (and
      (at ?v ?from)
      (connects ?r ?from ?to)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
      (not (location-blocked ?from))
      (not (location-blocked ?to))
    )
    :effect (and
      (not (at ?v ?from))
      (at ?v ?to)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-road_0036-tw_00390
    :parameters (?v - vehicle ?r - road_type_road_0036 ?from - loc_type_loc_0024 ?to - loc_type_loc_0015 ?w - window_type_tw_00390)
    :precondition (and
      (at ?v ?from)
      (connects ?r ?from ?to)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
      (not (location-blocked ?from))
      (not (location-blocked ?to))
    )
    :effect (and
      (not (at ?v ?from))
      (at ?v ?to)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-road_0036-tw_00420
    :parameters (?v - vehicle ?r - road_type_road_0036 ?from - loc_type_loc_0024 ?to - loc_type_loc_0015 ?w - window_type_tw_00420)
    :precondition (and
      (at ?v ?from)
      (connects ?r ?from ?to)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
      (not (location-blocked ?from))
      (not (location-blocked ?to))
    )
    :effect (and
      (not (at ?v ?from))
      (at ?v ?to)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-road_0036-tw_00450
    :parameters (?v - vehicle ?r - road_type_road_0036 ?from - loc_type_loc_0024 ?to - loc_type_loc_0015 ?w - window_type_tw_00450)
    :precondition (and
      (at ?v ?from)
      (connects ?r ?from ?to)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
      (not (location-blocked ?from))
      (not (location-blocked ?to))
    )
    :effect (and
      (not (at ?v ?from))
      (at ?v ?to)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-road_0037-tw_00000
    :parameters (?v - vehicle ?r - road_type_road_0037 ?from - loc_type_loc_0025 ?to - loc_type_loc_0002 ?w - window_type_tw_00000)
    :precondition (and
      (at ?v ?from)
      (connects ?r ?from ?to)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
      (not (location-blocked ?from))
      (not (location-blocked ?to))
    )
    :effect (and
      (not (at ?v ?from))
      (at ?v ?to)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-road_0037-tw_00030
    :parameters (?v - vehicle ?r - road_type_road_0037 ?from - loc_type_loc_0025 ?to - loc_type_loc_0002 ?w - window_type_tw_00030)
    :precondition (and
      (at ?v ?from)
      (connects ?r ?from ?to)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
      (not (location-blocked ?from))
      (not (location-blocked ?to))
    )
    :effect (and
      (not (at ?v ?from))
      (at ?v ?to)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-road_0037-tw_00060
    :parameters (?v - vehicle ?r - road_type_road_0037 ?from - loc_type_loc_0025 ?to - loc_type_loc_0002 ?w - window_type_tw_00060)
    :precondition (and
      (at ?v ?from)
      (connects ?r ?from ?to)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
      (not (location-blocked ?from))
      (not (location-blocked ?to))
    )
    :effect (and
      (not (at ?v ?from))
      (at ?v ?to)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-road_0037-tw_00090
    :parameters (?v - vehicle ?r - road_type_road_0037 ?from - loc_type_loc_0025 ?to - loc_type_loc_0002 ?w - window_type_tw_00090)
    :precondition (and
      (at ?v ?from)
      (connects ?r ?from ?to)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
      (not (location-blocked ?from))
      (not (location-blocked ?to))
    )
    :effect (and
      (not (at ?v ?from))
      (at ?v ?to)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-road_0037-tw_00120
    :parameters (?v - vehicle ?r - road_type_road_0037 ?from - loc_type_loc_0025 ?to - loc_type_loc_0002 ?w - window_type_tw_00120)
    :precondition (and
      (at ?v ?from)
      (connects ?r ?from ?to)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
      (not (location-blocked ?from))
      (not (location-blocked ?to))
    )
    :effect (and
      (not (at ?v ?from))
      (at ?v ?to)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-road_0037-tw_00150
    :parameters (?v - vehicle ?r - road_type_road_0037 ?from - loc_type_loc_0025 ?to - loc_type_loc_0002 ?w - window_type_tw_00150)
    :precondition (and
      (at ?v ?from)
      (connects ?r ?from ?to)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
      (not (location-blocked ?from))
      (not (location-blocked ?to))
    )
    :effect (and
      (not (at ?v ?from))
      (at ?v ?to)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-road_0037-tw_00180
    :parameters (?v - vehicle ?r - road_type_road_0037 ?from - loc_type_loc_0025 ?to - loc_type_loc_0002 ?w - window_type_tw_00180)
    :precondition (and
      (at ?v ?from)
      (connects ?r ?from ?to)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
      (not (location-blocked ?from))
      (not (location-blocked ?to))
    )
    :effect (and
      (not (at ?v ?from))
      (at ?v ?to)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-road_0037-tw_00210
    :parameters (?v - vehicle ?r - road_type_road_0037 ?from - loc_type_loc_0025 ?to - loc_type_loc_0002 ?w - window_type_tw_00210)
    :precondition (and
      (at ?v ?from)
      (connects ?r ?from ?to)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
      (not (location-blocked ?from))
      (not (location-blocked ?to))
    )
    :effect (and
      (not (at ?v ?from))
      (at ?v ?to)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-road_0037-tw_00240
    :parameters (?v - vehicle ?r - road_type_road_0037 ?from - loc_type_loc_0025 ?to - loc_type_loc_0002 ?w - window_type_tw_00240)
    :precondition (and
      (at ?v ?from)
      (connects ?r ?from ?to)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
      (not (location-blocked ?from))
      (not (location-blocked ?to))
    )
    :effect (and
      (not (at ?v ?from))
      (at ?v ?to)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-road_0037-tw_00270
    :parameters (?v - vehicle ?r - road_type_road_0037 ?from - loc_type_loc_0025 ?to - loc_type_loc_0002 ?w - window_type_tw_00270)
    :precondition (and
      (at ?v ?from)
      (connects ?r ?from ?to)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
      (not (location-blocked ?from))
      (not (location-blocked ?to))
    )
    :effect (and
      (not (at ?v ?from))
      (at ?v ?to)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-road_0037-tw_00300
    :parameters (?v - vehicle ?r - road_type_road_0037 ?from - loc_type_loc_0025 ?to - loc_type_loc_0002 ?w - window_type_tw_00300)
    :precondition (and
      (at ?v ?from)
      (connects ?r ?from ?to)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
      (not (location-blocked ?from))
      (not (location-blocked ?to))
    )
    :effect (and
      (not (at ?v ?from))
      (at ?v ?to)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-road_0037-tw_00330
    :parameters (?v - vehicle ?r - road_type_road_0037 ?from - loc_type_loc_0025 ?to - loc_type_loc_0002 ?w - window_type_tw_00330)
    :precondition (and
      (at ?v ?from)
      (connects ?r ?from ?to)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
      (not (location-blocked ?from))
      (not (location-blocked ?to))
    )
    :effect (and
      (not (at ?v ?from))
      (at ?v ?to)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-road_0037-tw_00360
    :parameters (?v - vehicle ?r - road_type_road_0037 ?from - loc_type_loc_0025 ?to - loc_type_loc_0002 ?w - window_type_tw_00360)
    :precondition (and
      (at ?v ?from)
      (connects ?r ?from ?to)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
      (not (location-blocked ?from))
      (not (location-blocked ?to))
    )
    :effect (and
      (not (at ?v ?from))
      (at ?v ?to)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-road_0037-tw_00390
    :parameters (?v - vehicle ?r - road_type_road_0037 ?from - loc_type_loc_0025 ?to - loc_type_loc_0002 ?w - window_type_tw_00390)
    :precondition (and
      (at ?v ?from)
      (connects ?r ?from ?to)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
      (not (location-blocked ?from))
      (not (location-blocked ?to))
    )
    :effect (and
      (not (at ?v ?from))
      (at ?v ?to)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-road_0037-tw_00420
    :parameters (?v - vehicle ?r - road_type_road_0037 ?from - loc_type_loc_0025 ?to - loc_type_loc_0002 ?w - window_type_tw_00420)
    :precondition (and
      (at ?v ?from)
      (connects ?r ?from ?to)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
      (not (location-blocked ?from))
      (not (location-blocked ?to))
    )
    :effect (and
      (not (at ?v ?from))
      (at ?v ?to)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-road_0037-tw_00450
    :parameters (?v - vehicle ?r - road_type_road_0037 ?from - loc_type_loc_0025 ?to - loc_type_loc_0002 ?w - window_type_tw_00450)
    :precondition (and
      (at ?v ?from)
      (connects ?r ?from ?to)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
      (not (location-blocked ?from))
      (not (location-blocked ?to))
    )
    :effect (and
      (not (at ?v ?from))
      (at ?v ?to)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-road_0038-tw_00000
    :parameters (?v - vehicle ?r - road_type_road_0038 ?from - loc_type_loc_0025 ?to - loc_type_loc_0060 ?w - window_type_tw_00000)
    :precondition (and
      (at ?v ?from)
      (connects ?r ?from ?to)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
      (not (location-blocked ?from))
      (not (location-blocked ?to))
    )
    :effect (and
      (not (at ?v ?from))
      (at ?v ?to)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-road_0038-tw_00030
    :parameters (?v - vehicle ?r - road_type_road_0038 ?from - loc_type_loc_0025 ?to - loc_type_loc_0060 ?w - window_type_tw_00030)
    :precondition (and
      (at ?v ?from)
      (connects ?r ?from ?to)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
      (not (location-blocked ?from))
      (not (location-blocked ?to))
    )
    :effect (and
      (not (at ?v ?from))
      (at ?v ?to)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-road_0038-tw_00060
    :parameters (?v - vehicle ?r - road_type_road_0038 ?from - loc_type_loc_0025 ?to - loc_type_loc_0060 ?w - window_type_tw_00060)
    :precondition (and
      (at ?v ?from)
      (connects ?r ?from ?to)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
      (not (location-blocked ?from))
      (not (location-blocked ?to))
    )
    :effect (and
      (not (at ?v ?from))
      (at ?v ?to)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-road_0038-tw_00090
    :parameters (?v - vehicle ?r - road_type_road_0038 ?from - loc_type_loc_0025 ?to - loc_type_loc_0060 ?w - window_type_tw_00090)
    :precondition (and
      (at ?v ?from)
      (connects ?r ?from ?to)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
      (not (location-blocked ?from))
      (not (location-blocked ?to))
    )
    :effect (and
      (not (at ?v ?from))
      (at ?v ?to)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-road_0038-tw_00120
    :parameters (?v - vehicle ?r - road_type_road_0038 ?from - loc_type_loc_0025 ?to - loc_type_loc_0060 ?w - window_type_tw_00120)
    :precondition (and
      (at ?v ?from)
      (connects ?r ?from ?to)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
      (not (location-blocked ?from))
      (not (location-blocked ?to))
    )
    :effect (and
      (not (at ?v ?from))
      (at ?v ?to)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-road_0038-tw_00150
    :parameters (?v - vehicle ?r - road_type_road_0038 ?from - loc_type_loc_0025 ?to - loc_type_loc_0060 ?w - window_type_tw_00150)
    :precondition (and
      (at ?v ?from)
      (connects ?r ?from ?to)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
      (not (location-blocked ?from))
      (not (location-blocked ?to))
    )
    :effect (and
      (not (at ?v ?from))
      (at ?v ?to)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-road_0038-tw_00180
    :parameters (?v - vehicle ?r - road_type_road_0038 ?from - loc_type_loc_0025 ?to - loc_type_loc_0060 ?w - window_type_tw_00180)
    :precondition (and
      (at ?v ?from)
      (connects ?r ?from ?to)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
      (not (location-blocked ?from))
      (not (location-blocked ?to))
    )
    :effect (and
      (not (at ?v ?from))
      (at ?v ?to)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-road_0038-tw_00210
    :parameters (?v - vehicle ?r - road_type_road_0038 ?from - loc_type_loc_0025 ?to - loc_type_loc_0060 ?w - window_type_tw_00210)
    :precondition (and
      (at ?v ?from)
      (connects ?r ?from ?to)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
      (not (location-blocked ?from))
      (not (location-blocked ?to))
    )
    :effect (and
      (not (at ?v ?from))
      (at ?v ?to)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-road_0038-tw_00240
    :parameters (?v - vehicle ?r - road_type_road_0038 ?from - loc_type_loc_0025 ?to - loc_type_loc_0060 ?w - window_type_tw_00240)
    :precondition (and
      (at ?v ?from)
      (connects ?r ?from ?to)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
      (not (location-blocked ?from))
      (not (location-blocked ?to))
    )
    :effect (and
      (not (at ?v ?from))
      (at ?v ?to)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-road_0038-tw_00270
    :parameters (?v - vehicle ?r - road_type_road_0038 ?from - loc_type_loc_0025 ?to - loc_type_loc_0060 ?w - window_type_tw_00270)
    :precondition (and
      (at ?v ?from)
      (connects ?r ?from ?to)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
      (not (location-blocked ?from))
      (not (location-blocked ?to))
    )
    :effect (and
      (not (at ?v ?from))
      (at ?v ?to)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-road_0038-tw_00300
    :parameters (?v - vehicle ?r - road_type_road_0038 ?from - loc_type_loc_0025 ?to - loc_type_loc_0060 ?w - window_type_tw_00300)
    :precondition (and
      (at ?v ?from)
      (connects ?r ?from ?to)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
      (not (location-blocked ?from))
      (not (location-blocked ?to))
    )
    :effect (and
      (not (at ?v ?from))
      (at ?v ?to)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-road_0038-tw_00330
    :parameters (?v - vehicle ?r - road_type_road_0038 ?from - loc_type_loc_0025 ?to - loc_type_loc_0060 ?w - window_type_tw_00330)
    :precondition (and
      (at ?v ?from)
      (connects ?r ?from ?to)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
      (not (location-blocked ?from))
      (not (location-blocked ?to))
    )
    :effect (and
      (not (at ?v ?from))
      (at ?v ?to)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-road_0038-tw_00360
    :parameters (?v - vehicle ?r - road_type_road_0038 ?from - loc_type_loc_0025 ?to - loc_type_loc_0060 ?w - window_type_tw_00360)
    :precondition (and
      (at ?v ?from)
      (connects ?r ?from ?to)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
      (not (location-blocked ?from))
      (not (location-blocked ?to))
    )
    :effect (and
      (not (at ?v ?from))
      (at ?v ?to)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-road_0038-tw_00390
    :parameters (?v - vehicle ?r - road_type_road_0038 ?from - loc_type_loc_0025 ?to - loc_type_loc_0060 ?w - window_type_tw_00390)
    :precondition (and
      (at ?v ?from)
      (connects ?r ?from ?to)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
      (not (location-blocked ?from))
      (not (location-blocked ?to))
    )
    :effect (and
      (not (at ?v ?from))
      (at ?v ?to)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-road_0038-tw_00420
    :parameters (?v - vehicle ?r - road_type_road_0038 ?from - loc_type_loc_0025 ?to - loc_type_loc_0060 ?w - window_type_tw_00420)
    :precondition (and
      (at ?v ?from)
      (connects ?r ?from ?to)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
      (not (location-blocked ?from))
      (not (location-blocked ?to))
    )
    :effect (and
      (not (at ?v ?from))
      (at ?v ?to)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-road_0038-tw_00450
    :parameters (?v - vehicle ?r - road_type_road_0038 ?from - loc_type_loc_0025 ?to - loc_type_loc_0060 ?w - window_type_tw_00450)
    :precondition (and
      (at ?v ?from)
      (connects ?r ?from ?to)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
      (not (location-blocked ?from))
      (not (location-blocked ?to))
    )
    :effect (and
      (not (at ?v ?from))
      (at ?v ?to)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-road_0039-tw_00000
    :parameters (?v - vehicle ?r - road_type_road_0039 ?from - loc_type_loc_0025 ?to - loc_type_loc_0024 ?w - window_type_tw_00000)
    :precondition (and
      (at ?v ?from)
      (connects ?r ?from ?to)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
      (not (location-blocked ?from))
      (not (location-blocked ?to))
    )
    :effect (and
      (not (at ?v ?from))
      (at ?v ?to)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-road_0039-tw_00030
    :parameters (?v - vehicle ?r - road_type_road_0039 ?from - loc_type_loc_0025 ?to - loc_type_loc_0024 ?w - window_type_tw_00030)
    :precondition (and
      (at ?v ?from)
      (connects ?r ?from ?to)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
      (not (location-blocked ?from))
      (not (location-blocked ?to))
    )
    :effect (and
      (not (at ?v ?from))
      (at ?v ?to)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-road_0039-tw_00060
    :parameters (?v - vehicle ?r - road_type_road_0039 ?from - loc_type_loc_0025 ?to - loc_type_loc_0024 ?w - window_type_tw_00060)
    :precondition (and
      (at ?v ?from)
      (connects ?r ?from ?to)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
      (not (location-blocked ?from))
      (not (location-blocked ?to))
    )
    :effect (and
      (not (at ?v ?from))
      (at ?v ?to)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-road_0039-tw_00090
    :parameters (?v - vehicle ?r - road_type_road_0039 ?from - loc_type_loc_0025 ?to - loc_type_loc_0024 ?w - window_type_tw_00090)
    :precondition (and
      (at ?v ?from)
      (connects ?r ?from ?to)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
      (not (location-blocked ?from))
      (not (location-blocked ?to))
    )
    :effect (and
      (not (at ?v ?from))
      (at ?v ?to)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-road_0039-tw_00120
    :parameters (?v - vehicle ?r - road_type_road_0039 ?from - loc_type_loc_0025 ?to - loc_type_loc_0024 ?w - window_type_tw_00120)
    :precondition (and
      (at ?v ?from)
      (connects ?r ?from ?to)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
      (not (location-blocked ?from))
      (not (location-blocked ?to))
    )
    :effect (and
      (not (at ?v ?from))
      (at ?v ?to)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-road_0039-tw_00150
    :parameters (?v - vehicle ?r - road_type_road_0039 ?from - loc_type_loc_0025 ?to - loc_type_loc_0024 ?w - window_type_tw_00150)
    :precondition (and
      (at ?v ?from)
      (connects ?r ?from ?to)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
      (not (location-blocked ?from))
      (not (location-blocked ?to))
    )
    :effect (and
      (not (at ?v ?from))
      (at ?v ?to)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-road_0039-tw_00180
    :parameters (?v - vehicle ?r - road_type_road_0039 ?from - loc_type_loc_0025 ?to - loc_type_loc_0024 ?w - window_type_tw_00180)
    :precondition (and
      (at ?v ?from)
      (connects ?r ?from ?to)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
      (not (location-blocked ?from))
      (not (location-blocked ?to))
    )
    :effect (and
      (not (at ?v ?from))
      (at ?v ?to)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-road_0039-tw_00210
    :parameters (?v - vehicle ?r - road_type_road_0039 ?from - loc_type_loc_0025 ?to - loc_type_loc_0024 ?w - window_type_tw_00210)
    :precondition (and
      (at ?v ?from)
      (connects ?r ?from ?to)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
      (not (location-blocked ?from))
      (not (location-blocked ?to))
    )
    :effect (and
      (not (at ?v ?from))
      (at ?v ?to)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-road_0039-tw_00240
    :parameters (?v - vehicle ?r - road_type_road_0039 ?from - loc_type_loc_0025 ?to - loc_type_loc_0024 ?w - window_type_tw_00240)
    :precondition (and
      (at ?v ?from)
      (connects ?r ?from ?to)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
      (not (location-blocked ?from))
      (not (location-blocked ?to))
    )
    :effect (and
      (not (at ?v ?from))
      (at ?v ?to)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-road_0039-tw_00270
    :parameters (?v - vehicle ?r - road_type_road_0039 ?from - loc_type_loc_0025 ?to - loc_type_loc_0024 ?w - window_type_tw_00270)
    :precondition (and
      (at ?v ?from)
      (connects ?r ?from ?to)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
      (not (location-blocked ?from))
      (not (location-blocked ?to))
    )
    :effect (and
      (not (at ?v ?from))
      (at ?v ?to)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-road_0039-tw_00300
    :parameters (?v - vehicle ?r - road_type_road_0039 ?from - loc_type_loc_0025 ?to - loc_type_loc_0024 ?w - window_type_tw_00300)
    :precondition (and
      (at ?v ?from)
      (connects ?r ?from ?to)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
      (not (location-blocked ?from))
      (not (location-blocked ?to))
    )
    :effect (and
      (not (at ?v ?from))
      (at ?v ?to)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-road_0039-tw_00330
    :parameters (?v - vehicle ?r - road_type_road_0039 ?from - loc_type_loc_0025 ?to - loc_type_loc_0024 ?w - window_type_tw_00330)
    :precondition (and
      (at ?v ?from)
      (connects ?r ?from ?to)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
      (not (location-blocked ?from))
      (not (location-blocked ?to))
    )
    :effect (and
      (not (at ?v ?from))
      (at ?v ?to)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-road_0039-tw_00360
    :parameters (?v - vehicle ?r - road_type_road_0039 ?from - loc_type_loc_0025 ?to - loc_type_loc_0024 ?w - window_type_tw_00360)
    :precondition (and
      (at ?v ?from)
      (connects ?r ?from ?to)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
      (not (location-blocked ?from))
      (not (location-blocked ?to))
    )
    :effect (and
      (not (at ?v ?from))
      (at ?v ?to)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-road_0039-tw_00390
    :parameters (?v - vehicle ?r - road_type_road_0039 ?from - loc_type_loc_0025 ?to - loc_type_loc_0024 ?w - window_type_tw_00390)
    :precondition (and
      (at ?v ?from)
      (connects ?r ?from ?to)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
      (not (location-blocked ?from))
      (not (location-blocked ?to))
    )
    :effect (and
      (not (at ?v ?from))
      (at ?v ?to)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-road_0039-tw_00420
    :parameters (?v - vehicle ?r - road_type_road_0039 ?from - loc_type_loc_0025 ?to - loc_type_loc_0024 ?w - window_type_tw_00420)
    :precondition (and
      (at ?v ?from)
      (connects ?r ?from ?to)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
      (not (location-blocked ?from))
      (not (location-blocked ?to))
    )
    :effect (and
      (not (at ?v ?from))
      (at ?v ?to)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-road_0039-tw_00450
    :parameters (?v - vehicle ?r - road_type_road_0039 ?from - loc_type_loc_0025 ?to - loc_type_loc_0024 ?w - window_type_tw_00450)
    :precondition (and
      (at ?v ?from)
      (connects ?r ?from ?to)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
      (not (location-blocked ?from))
      (not (location-blocked ?to))
    )
    :effect (and
      (not (at ?v ?from))
      (at ?v ?to)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-road_0040-tw_00000
    :parameters (?v - vehicle ?r - road_type_road_0040 ?from - loc_type_loc_0026 ?to - loc_type_loc_0094 ?w - window_type_tw_00000)
    :precondition (and
      (at ?v ?from)
      (connects ?r ?from ?to)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
      (not (location-blocked ?from))
      (not (location-blocked ?to))
    )
    :effect (and
      (not (at ?v ?from))
      (at ?v ?to)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-road_0040-tw_00030
    :parameters (?v - vehicle ?r - road_type_road_0040 ?from - loc_type_loc_0026 ?to - loc_type_loc_0094 ?w - window_type_tw_00030)
    :precondition (and
      (at ?v ?from)
      (connects ?r ?from ?to)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
      (not (location-blocked ?from))
      (not (location-blocked ?to))
    )
    :effect (and
      (not (at ?v ?from))
      (at ?v ?to)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-road_0040-tw_00060
    :parameters (?v - vehicle ?r - road_type_road_0040 ?from - loc_type_loc_0026 ?to - loc_type_loc_0094 ?w - window_type_tw_00060)
    :precondition (and
      (at ?v ?from)
      (connects ?r ?from ?to)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
      (not (location-blocked ?from))
      (not (location-blocked ?to))
    )
    :effect (and
      (not (at ?v ?from))
      (at ?v ?to)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-road_0040-tw_00090
    :parameters (?v - vehicle ?r - road_type_road_0040 ?from - loc_type_loc_0026 ?to - loc_type_loc_0094 ?w - window_type_tw_00090)
    :precondition (and
      (at ?v ?from)
      (connects ?r ?from ?to)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
      (not (location-blocked ?from))
      (not (location-blocked ?to))
    )
    :effect (and
      (not (at ?v ?from))
      (at ?v ?to)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-road_0040-tw_00120
    :parameters (?v - vehicle ?r - road_type_road_0040 ?from - loc_type_loc_0026 ?to - loc_type_loc_0094 ?w - window_type_tw_00120)
    :precondition (and
      (at ?v ?from)
      (connects ?r ?from ?to)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
      (not (location-blocked ?from))
      (not (location-blocked ?to))
    )
    :effect (and
      (not (at ?v ?from))
      (at ?v ?to)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-road_0040-tw_00150
    :parameters (?v - vehicle ?r - road_type_road_0040 ?from - loc_type_loc_0026 ?to - loc_type_loc_0094 ?w - window_type_tw_00150)
    :precondition (and
      (at ?v ?from)
      (connects ?r ?from ?to)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
      (not (location-blocked ?from))
      (not (location-blocked ?to))
    )
    :effect (and
      (not (at ?v ?from))
      (at ?v ?to)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-road_0040-tw_00180
    :parameters (?v - vehicle ?r - road_type_road_0040 ?from - loc_type_loc_0026 ?to - loc_type_loc_0094 ?w - window_type_tw_00180)
    :precondition (and
      (at ?v ?from)
      (connects ?r ?from ?to)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
      (not (location-blocked ?from))
      (not (location-blocked ?to))
    )
    :effect (and
      (not (at ?v ?from))
      (at ?v ?to)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-road_0040-tw_00210
    :parameters (?v - vehicle ?r - road_type_road_0040 ?from - loc_type_loc_0026 ?to - loc_type_loc_0094 ?w - window_type_tw_00210)
    :precondition (and
      (at ?v ?from)
      (connects ?r ?from ?to)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
      (not (location-blocked ?from))
      (not (location-blocked ?to))
    )
    :effect (and
      (not (at ?v ?from))
      (at ?v ?to)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-road_0040-tw_00240
    :parameters (?v - vehicle ?r - road_type_road_0040 ?from - loc_type_loc_0026 ?to - loc_type_loc_0094 ?w - window_type_tw_00240)
    :precondition (and
      (at ?v ?from)
      (connects ?r ?from ?to)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
      (not (location-blocked ?from))
      (not (location-blocked ?to))
    )
    :effect (and
      (not (at ?v ?from))
      (at ?v ?to)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-road_0040-tw_00270
    :parameters (?v - vehicle ?r - road_type_road_0040 ?from - loc_type_loc_0026 ?to - loc_type_loc_0094 ?w - window_type_tw_00270)
    :precondition (and
      (at ?v ?from)
      (connects ?r ?from ?to)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
      (not (location-blocked ?from))
      (not (location-blocked ?to))
    )
    :effect (and
      (not (at ?v ?from))
      (at ?v ?to)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-road_0040-tw_00300
    :parameters (?v - vehicle ?r - road_type_road_0040 ?from - loc_type_loc_0026 ?to - loc_type_loc_0094 ?w - window_type_tw_00300)
    :precondition (and
      (at ?v ?from)
      (connects ?r ?from ?to)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
      (not (location-blocked ?from))
      (not (location-blocked ?to))
    )
    :effect (and
      (not (at ?v ?from))
      (at ?v ?to)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-road_0040-tw_00330
    :parameters (?v - vehicle ?r - road_type_road_0040 ?from - loc_type_loc_0026 ?to - loc_type_loc_0094 ?w - window_type_tw_00330)
    :precondition (and
      (at ?v ?from)
      (connects ?r ?from ?to)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
      (not (location-blocked ?from))
      (not (location-blocked ?to))
    )
    :effect (and
      (not (at ?v ?from))
      (at ?v ?to)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-road_0040-tw_00360
    :parameters (?v - vehicle ?r - road_type_road_0040 ?from - loc_type_loc_0026 ?to - loc_type_loc_0094 ?w - window_type_tw_00360)
    :precondition (and
      (at ?v ?from)
      (connects ?r ?from ?to)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
      (not (location-blocked ?from))
      (not (location-blocked ?to))
    )
    :effect (and
      (not (at ?v ?from))
      (at ?v ?to)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-road_0040-tw_00390
    :parameters (?v - vehicle ?r - road_type_road_0040 ?from - loc_type_loc_0026 ?to - loc_type_loc_0094 ?w - window_type_tw_00390)
    :precondition (and
      (at ?v ?from)
      (connects ?r ?from ?to)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
      (not (location-blocked ?from))
      (not (location-blocked ?to))
    )
    :effect (and
      (not (at ?v ?from))
      (at ?v ?to)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-road_0040-tw_00420
    :parameters (?v - vehicle ?r - road_type_road_0040 ?from - loc_type_loc_0026 ?to - loc_type_loc_0094 ?w - window_type_tw_00420)
    :precondition (and
      (at ?v ?from)
      (connects ?r ?from ?to)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
      (not (location-blocked ?from))
      (not (location-blocked ?to))
    )
    :effect (and
      (not (at ?v ?from))
      (at ?v ?to)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-road_0040-tw_00450
    :parameters (?v - vehicle ?r - road_type_road_0040 ?from - loc_type_loc_0026 ?to - loc_type_loc_0094 ?w - window_type_tw_00450)
    :precondition (and
      (at ?v ?from)
      (connects ?r ?from ?to)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
      (not (location-blocked ?from))
      (not (location-blocked ?to))
    )
    :effect (and
      (not (at ?v ?from))
      (at ?v ?to)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-road_0041-tw_00000
    :parameters (?v - vehicle ?r - road_type_road_0041 ?from - loc_type_loc_0026 ?to - loc_type_loc_0011 ?w - window_type_tw_00000)
    :precondition (and
      (at ?v ?from)
      (connects ?r ?from ?to)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
      (not (location-blocked ?from))
      (not (location-blocked ?to))
    )
    :effect (and
      (not (at ?v ?from))
      (at ?v ?to)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-road_0041-tw_00030
    :parameters (?v - vehicle ?r - road_type_road_0041 ?from - loc_type_loc_0026 ?to - loc_type_loc_0011 ?w - window_type_tw_00030)
    :precondition (and
      (at ?v ?from)
      (connects ?r ?from ?to)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
      (not (location-blocked ?from))
      (not (location-blocked ?to))
    )
    :effect (and
      (not (at ?v ?from))
      (at ?v ?to)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-road_0041-tw_00060
    :parameters (?v - vehicle ?r - road_type_road_0041 ?from - loc_type_loc_0026 ?to - loc_type_loc_0011 ?w - window_type_tw_00060)
    :precondition (and
      (at ?v ?from)
      (connects ?r ?from ?to)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
      (not (location-blocked ?from))
      (not (location-blocked ?to))
    )
    :effect (and
      (not (at ?v ?from))
      (at ?v ?to)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-road_0041-tw_00090
    :parameters (?v - vehicle ?r - road_type_road_0041 ?from - loc_type_loc_0026 ?to - loc_type_loc_0011 ?w - window_type_tw_00090)
    :precondition (and
      (at ?v ?from)
      (connects ?r ?from ?to)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
      (not (location-blocked ?from))
      (not (location-blocked ?to))
    )
    :effect (and
      (not (at ?v ?from))
      (at ?v ?to)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-road_0041-tw_00120
    :parameters (?v - vehicle ?r - road_type_road_0041 ?from - loc_type_loc_0026 ?to - loc_type_loc_0011 ?w - window_type_tw_00120)
    :precondition (and
      (at ?v ?from)
      (connects ?r ?from ?to)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
      (not (location-blocked ?from))
      (not (location-blocked ?to))
    )
    :effect (and
      (not (at ?v ?from))
      (at ?v ?to)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-road_0041-tw_00150
    :parameters (?v - vehicle ?r - road_type_road_0041 ?from - loc_type_loc_0026 ?to - loc_type_loc_0011 ?w - window_type_tw_00150)
    :precondition (and
      (at ?v ?from)
      (connects ?r ?from ?to)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
      (not (location-blocked ?from))
      (not (location-blocked ?to))
    )
    :effect (and
      (not (at ?v ?from))
      (at ?v ?to)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-road_0041-tw_00180
    :parameters (?v - vehicle ?r - road_type_road_0041 ?from - loc_type_loc_0026 ?to - loc_type_loc_0011 ?w - window_type_tw_00180)
    :precondition (and
      (at ?v ?from)
      (connects ?r ?from ?to)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
      (not (location-blocked ?from))
      (not (location-blocked ?to))
    )
    :effect (and
      (not (at ?v ?from))
      (at ?v ?to)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-road_0041-tw_00210
    :parameters (?v - vehicle ?r - road_type_road_0041 ?from - loc_type_loc_0026 ?to - loc_type_loc_0011 ?w - window_type_tw_00210)
    :precondition (and
      (at ?v ?from)
      (connects ?r ?from ?to)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
      (not (location-blocked ?from))
      (not (location-blocked ?to))
    )
    :effect (and
      (not (at ?v ?from))
      (at ?v ?to)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-road_0041-tw_00240
    :parameters (?v - vehicle ?r - road_type_road_0041 ?from - loc_type_loc_0026 ?to - loc_type_loc_0011 ?w - window_type_tw_00240)
    :precondition (and
      (at ?v ?from)
      (connects ?r ?from ?to)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
      (not (location-blocked ?from))
      (not (location-blocked ?to))
    )
    :effect (and
      (not (at ?v ?from))
      (at ?v ?to)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-road_0041-tw_00270
    :parameters (?v - vehicle ?r - road_type_road_0041 ?from - loc_type_loc_0026 ?to - loc_type_loc_0011 ?w - window_type_tw_00270)
    :precondition (and
      (at ?v ?from)
      (connects ?r ?from ?to)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
      (not (location-blocked ?from))
      (not (location-blocked ?to))
    )
    :effect (and
      (not (at ?v ?from))
      (at ?v ?to)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-road_0041-tw_00300
    :parameters (?v - vehicle ?r - road_type_road_0041 ?from - loc_type_loc_0026 ?to - loc_type_loc_0011 ?w - window_type_tw_00300)
    :precondition (and
      (at ?v ?from)
      (connects ?r ?from ?to)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
      (not (location-blocked ?from))
      (not (location-blocked ?to))
    )
    :effect (and
      (not (at ?v ?from))
      (at ?v ?to)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-road_0041-tw_00330
    :parameters (?v - vehicle ?r - road_type_road_0041 ?from - loc_type_loc_0026 ?to - loc_type_loc_0011 ?w - window_type_tw_00330)
    :precondition (and
      (at ?v ?from)
      (connects ?r ?from ?to)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
      (not (location-blocked ?from))
      (not (location-blocked ?to))
    )
    :effect (and
      (not (at ?v ?from))
      (at ?v ?to)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-road_0041-tw_00360
    :parameters (?v - vehicle ?r - road_type_road_0041 ?from - loc_type_loc_0026 ?to - loc_type_loc_0011 ?w - window_type_tw_00360)
    :precondition (and
      (at ?v ?from)
      (connects ?r ?from ?to)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
      (not (location-blocked ?from))
      (not (location-blocked ?to))
    )
    :effect (and
      (not (at ?v ?from))
      (at ?v ?to)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-road_0041-tw_00390
    :parameters (?v - vehicle ?r - road_type_road_0041 ?from - loc_type_loc_0026 ?to - loc_type_loc_0011 ?w - window_type_tw_00390)
    :precondition (and
      (at ?v ?from)
      (connects ?r ?from ?to)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
      (not (location-blocked ?from))
      (not (location-blocked ?to))
    )
    :effect (and
      (not (at ?v ?from))
      (at ?v ?to)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-road_0041-tw_00420
    :parameters (?v - vehicle ?r - road_type_road_0041 ?from - loc_type_loc_0026 ?to - loc_type_loc_0011 ?w - window_type_tw_00420)
    :precondition (and
      (at ?v ?from)
      (connects ?r ?from ?to)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
      (not (location-blocked ?from))
      (not (location-blocked ?to))
    )
    :effect (and
      (not (at ?v ?from))
      (at ?v ?to)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-road_0041-tw_00450
    :parameters (?v - vehicle ?r - road_type_road_0041 ?from - loc_type_loc_0026 ?to - loc_type_loc_0011 ?w - window_type_tw_00450)
    :precondition (and
      (at ?v ?from)
      (connects ?r ?from ?to)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
      (not (location-blocked ?from))
      (not (location-blocked ?to))
    )
    :effect (and
      (not (at ?v ?from))
      (at ?v ?to)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-road_0042-tw_00000
    :parameters (?v - vehicle ?r - road_type_road_0042 ?from - loc_type_loc_0027 ?to - loc_type_loc_0050 ?w - window_type_tw_00000)
    :precondition (and
      (at ?v ?from)
      (connects ?r ?from ?to)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
      (not (location-blocked ?from))
      (not (location-blocked ?to))
    )
    :effect (and
      (not (at ?v ?from))
      (at ?v ?to)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-road_0042-tw_00030
    :parameters (?v - vehicle ?r - road_type_road_0042 ?from - loc_type_loc_0027 ?to - loc_type_loc_0050 ?w - window_type_tw_00030)
    :precondition (and
      (at ?v ?from)
      (connects ?r ?from ?to)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
      (not (location-blocked ?from))
      (not (location-blocked ?to))
    )
    :effect (and
      (not (at ?v ?from))
      (at ?v ?to)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-road_0042-tw_00060
    :parameters (?v - vehicle ?r - road_type_road_0042 ?from - loc_type_loc_0027 ?to - loc_type_loc_0050 ?w - window_type_tw_00060)
    :precondition (and
      (at ?v ?from)
      (connects ?r ?from ?to)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
      (not (location-blocked ?from))
      (not (location-blocked ?to))
    )
    :effect (and
      (not (at ?v ?from))
      (at ?v ?to)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-road_0042-tw_00090
    :parameters (?v - vehicle ?r - road_type_road_0042 ?from - loc_type_loc_0027 ?to - loc_type_loc_0050 ?w - window_type_tw_00090)
    :precondition (and
      (at ?v ?from)
      (connects ?r ?from ?to)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
      (not (location-blocked ?from))
      (not (location-blocked ?to))
    )
    :effect (and
      (not (at ?v ?from))
      (at ?v ?to)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-road_0042-tw_00120
    :parameters (?v - vehicle ?r - road_type_road_0042 ?from - loc_type_loc_0027 ?to - loc_type_loc_0050 ?w - window_type_tw_00120)
    :precondition (and
      (at ?v ?from)
      (connects ?r ?from ?to)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
      (not (location-blocked ?from))
      (not (location-blocked ?to))
    )
    :effect (and
      (not (at ?v ?from))
      (at ?v ?to)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-road_0042-tw_00150
    :parameters (?v - vehicle ?r - road_type_road_0042 ?from - loc_type_loc_0027 ?to - loc_type_loc_0050 ?w - window_type_tw_00150)
    :precondition (and
      (at ?v ?from)
      (connects ?r ?from ?to)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
      (not (location-blocked ?from))
      (not (location-blocked ?to))
    )
    :effect (and
      (not (at ?v ?from))
      (at ?v ?to)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-road_0042-tw_00180
    :parameters (?v - vehicle ?r - road_type_road_0042 ?from - loc_type_loc_0027 ?to - loc_type_loc_0050 ?w - window_type_tw_00180)
    :precondition (and
      (at ?v ?from)
      (connects ?r ?from ?to)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
      (not (location-blocked ?from))
      (not (location-blocked ?to))
    )
    :effect (and
      (not (at ?v ?from))
      (at ?v ?to)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-road_0042-tw_00210
    :parameters (?v - vehicle ?r - road_type_road_0042 ?from - loc_type_loc_0027 ?to - loc_type_loc_0050 ?w - window_type_tw_00210)
    :precondition (and
      (at ?v ?from)
      (connects ?r ?from ?to)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
      (not (location-blocked ?from))
      (not (location-blocked ?to))
    )
    :effect (and
      (not (at ?v ?from))
      (at ?v ?to)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-road_0042-tw_00240
    :parameters (?v - vehicle ?r - road_type_road_0042 ?from - loc_type_loc_0027 ?to - loc_type_loc_0050 ?w - window_type_tw_00240)
    :precondition (and
      (at ?v ?from)
      (connects ?r ?from ?to)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
      (not (location-blocked ?from))
      (not (location-blocked ?to))
    )
    :effect (and
      (not (at ?v ?from))
      (at ?v ?to)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-road_0042-tw_00270
    :parameters (?v - vehicle ?r - road_type_road_0042 ?from - loc_type_loc_0027 ?to - loc_type_loc_0050 ?w - window_type_tw_00270)
    :precondition (and
      (at ?v ?from)
      (connects ?r ?from ?to)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
      (not (location-blocked ?from))
      (not (location-blocked ?to))
    )
    :effect (and
      (not (at ?v ?from))
      (at ?v ?to)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-road_0042-tw_00300
    :parameters (?v - vehicle ?r - road_type_road_0042 ?from - loc_type_loc_0027 ?to - loc_type_loc_0050 ?w - window_type_tw_00300)
    :precondition (and
      (at ?v ?from)
      (connects ?r ?from ?to)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
      (not (location-blocked ?from))
      (not (location-blocked ?to))
    )
    :effect (and
      (not (at ?v ?from))
      (at ?v ?to)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-road_0042-tw_00330
    :parameters (?v - vehicle ?r - road_type_road_0042 ?from - loc_type_loc_0027 ?to - loc_type_loc_0050 ?w - window_type_tw_00330)
    :precondition (and
      (at ?v ?from)
      (connects ?r ?from ?to)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
      (not (location-blocked ?from))
      (not (location-blocked ?to))
    )
    :effect (and
      (not (at ?v ?from))
      (at ?v ?to)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-road_0042-tw_00360
    :parameters (?v - vehicle ?r - road_type_road_0042 ?from - loc_type_loc_0027 ?to - loc_type_loc_0050 ?w - window_type_tw_00360)
    :precondition (and
      (at ?v ?from)
      (connects ?r ?from ?to)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
      (not (location-blocked ?from))
      (not (location-blocked ?to))
    )
    :effect (and
      (not (at ?v ?from))
      (at ?v ?to)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-road_0042-tw_00390
    :parameters (?v - vehicle ?r - road_type_road_0042 ?from - loc_type_loc_0027 ?to - loc_type_loc_0050 ?w - window_type_tw_00390)
    :precondition (and
      (at ?v ?from)
      (connects ?r ?from ?to)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
      (not (location-blocked ?from))
      (not (location-blocked ?to))
    )
    :effect (and
      (not (at ?v ?from))
      (at ?v ?to)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-road_0042-tw_00420
    :parameters (?v - vehicle ?r - road_type_road_0042 ?from - loc_type_loc_0027 ?to - loc_type_loc_0050 ?w - window_type_tw_00420)
    :precondition (and
      (at ?v ?from)
      (connects ?r ?from ?to)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
      (not (location-blocked ?from))
      (not (location-blocked ?to))
    )
    :effect (and
      (not (at ?v ?from))
      (at ?v ?to)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-road_0042-tw_00450
    :parameters (?v - vehicle ?r - road_type_road_0042 ?from - loc_type_loc_0027 ?to - loc_type_loc_0050 ?w - window_type_tw_00450)
    :precondition (and
      (at ?v ?from)
      (connects ?r ?from ?to)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
      (not (location-blocked ?from))
      (not (location-blocked ?to))
    )
    :effect (and
      (not (at ?v ?from))
      (at ?v ?to)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-road_0043-tw_00000
    :parameters (?v - vehicle ?r - road_type_road_0043 ?from - loc_type_loc_0027 ?to - loc_type_loc_0088 ?w - window_type_tw_00000)
    :precondition (and
      (at ?v ?from)
      (connects ?r ?from ?to)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
      (not (location-blocked ?from))
      (not (location-blocked ?to))
    )
    :effect (and
      (not (at ?v ?from))
      (at ?v ?to)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-road_0043-tw_00030
    :parameters (?v - vehicle ?r - road_type_road_0043 ?from - loc_type_loc_0027 ?to - loc_type_loc_0088 ?w - window_type_tw_00030)
    :precondition (and
      (at ?v ?from)
      (connects ?r ?from ?to)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
      (not (location-blocked ?from))
      (not (location-blocked ?to))
    )
    :effect (and
      (not (at ?v ?from))
      (at ?v ?to)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-road_0043-tw_00060
    :parameters (?v - vehicle ?r - road_type_road_0043 ?from - loc_type_loc_0027 ?to - loc_type_loc_0088 ?w - window_type_tw_00060)
    :precondition (and
      (at ?v ?from)
      (connects ?r ?from ?to)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
      (not (location-blocked ?from))
      (not (location-blocked ?to))
    )
    :effect (and
      (not (at ?v ?from))
      (at ?v ?to)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-road_0043-tw_00090
    :parameters (?v - vehicle ?r - road_type_road_0043 ?from - loc_type_loc_0027 ?to - loc_type_loc_0088 ?w - window_type_tw_00090)
    :precondition (and
      (at ?v ?from)
      (connects ?r ?from ?to)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
      (not (location-blocked ?from))
      (not (location-blocked ?to))
    )
    :effect (and
      (not (at ?v ?from))
      (at ?v ?to)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-road_0043-tw_00120
    :parameters (?v - vehicle ?r - road_type_road_0043 ?from - loc_type_loc_0027 ?to - loc_type_loc_0088 ?w - window_type_tw_00120)
    :precondition (and
      (at ?v ?from)
      (connects ?r ?from ?to)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
      (not (location-blocked ?from))
      (not (location-blocked ?to))
    )
    :effect (and
      (not (at ?v ?from))
      (at ?v ?to)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-road_0043-tw_00150
    :parameters (?v - vehicle ?r - road_type_road_0043 ?from - loc_type_loc_0027 ?to - loc_type_loc_0088 ?w - window_type_tw_00150)
    :precondition (and
      (at ?v ?from)
      (connects ?r ?from ?to)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
      (not (location-blocked ?from))
      (not (location-blocked ?to))
    )
    :effect (and
      (not (at ?v ?from))
      (at ?v ?to)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-road_0043-tw_00180
    :parameters (?v - vehicle ?r - road_type_road_0043 ?from - loc_type_loc_0027 ?to - loc_type_loc_0088 ?w - window_type_tw_00180)
    :precondition (and
      (at ?v ?from)
      (connects ?r ?from ?to)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
      (not (location-blocked ?from))
      (not (location-blocked ?to))
    )
    :effect (and
      (not (at ?v ?from))
      (at ?v ?to)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-road_0043-tw_00210
    :parameters (?v - vehicle ?r - road_type_road_0043 ?from - loc_type_loc_0027 ?to - loc_type_loc_0088 ?w - window_type_tw_00210)
    :precondition (and
      (at ?v ?from)
      (connects ?r ?from ?to)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
      (not (location-blocked ?from))
      (not (location-blocked ?to))
    )
    :effect (and
      (not (at ?v ?from))
      (at ?v ?to)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-road_0043-tw_00240
    :parameters (?v - vehicle ?r - road_type_road_0043 ?from - loc_type_loc_0027 ?to - loc_type_loc_0088 ?w - window_type_tw_00240)
    :precondition (and
      (at ?v ?from)
      (connects ?r ?from ?to)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
      (not (location-blocked ?from))
      (not (location-blocked ?to))
    )
    :effect (and
      (not (at ?v ?from))
      (at ?v ?to)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-road_0043-tw_00270
    :parameters (?v - vehicle ?r - road_type_road_0043 ?from - loc_type_loc_0027 ?to - loc_type_loc_0088 ?w - window_type_tw_00270)
    :precondition (and
      (at ?v ?from)
      (connects ?r ?from ?to)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
      (not (location-blocked ?from))
      (not (location-blocked ?to))
    )
    :effect (and
      (not (at ?v ?from))
      (at ?v ?to)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-road_0043-tw_00300
    :parameters (?v - vehicle ?r - road_type_road_0043 ?from - loc_type_loc_0027 ?to - loc_type_loc_0088 ?w - window_type_tw_00300)
    :precondition (and
      (at ?v ?from)
      (connects ?r ?from ?to)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
      (not (location-blocked ?from))
      (not (location-blocked ?to))
    )
    :effect (and
      (not (at ?v ?from))
      (at ?v ?to)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-road_0043-tw_00330
    :parameters (?v - vehicle ?r - road_type_road_0043 ?from - loc_type_loc_0027 ?to - loc_type_loc_0088 ?w - window_type_tw_00330)
    :precondition (and
      (at ?v ?from)
      (connects ?r ?from ?to)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
      (not (location-blocked ?from))
      (not (location-blocked ?to))
    )
    :effect (and
      (not (at ?v ?from))
      (at ?v ?to)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-road_0043-tw_00360
    :parameters (?v - vehicle ?r - road_type_road_0043 ?from - loc_type_loc_0027 ?to - loc_type_loc_0088 ?w - window_type_tw_00360)
    :precondition (and
      (at ?v ?from)
      (connects ?r ?from ?to)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
      (not (location-blocked ?from))
      (not (location-blocked ?to))
    )
    :effect (and
      (not (at ?v ?from))
      (at ?v ?to)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-road_0043-tw_00390
    :parameters (?v - vehicle ?r - road_type_road_0043 ?from - loc_type_loc_0027 ?to - loc_type_loc_0088 ?w - window_type_tw_00390)
    :precondition (and
      (at ?v ?from)
      (connects ?r ?from ?to)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
      (not (location-blocked ?from))
      (not (location-blocked ?to))
    )
    :effect (and
      (not (at ?v ?from))
      (at ?v ?to)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-road_0043-tw_00420
    :parameters (?v - vehicle ?r - road_type_road_0043 ?from - loc_type_loc_0027 ?to - loc_type_loc_0088 ?w - window_type_tw_00420)
    :precondition (and
      (at ?v ?from)
      (connects ?r ?from ?to)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
      (not (location-blocked ?from))
      (not (location-blocked ?to))
    )
    :effect (and
      (not (at ?v ?from))
      (at ?v ?to)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-road_0043-tw_00450
    :parameters (?v - vehicle ?r - road_type_road_0043 ?from - loc_type_loc_0027 ?to - loc_type_loc_0088 ?w - window_type_tw_00450)
    :precondition (and
      (at ?v ?from)
      (connects ?r ?from ?to)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
      (not (location-blocked ?from))
      (not (location-blocked ?to))
    )
    :effect (and
      (not (at ?v ?from))
      (at ?v ?to)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-road_0044-tw_00000
    :parameters (?v - vehicle ?r - road_type_road_0044 ?from - loc_type_loc_0028 ?to - loc_type_loc_0022 ?w - window_type_tw_00000)
    :precondition (and
      (at ?v ?from)
      (connects ?r ?from ?to)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
      (not (location-blocked ?from))
      (not (location-blocked ?to))
    )
    :effect (and
      (not (at ?v ?from))
      (at ?v ?to)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-road_0044-tw_00030
    :parameters (?v - vehicle ?r - road_type_road_0044 ?from - loc_type_loc_0028 ?to - loc_type_loc_0022 ?w - window_type_tw_00030)
    :precondition (and
      (at ?v ?from)
      (connects ?r ?from ?to)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
      (not (location-blocked ?from))
      (not (location-blocked ?to))
    )
    :effect (and
      (not (at ?v ?from))
      (at ?v ?to)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-road_0044-tw_00060
    :parameters (?v - vehicle ?r - road_type_road_0044 ?from - loc_type_loc_0028 ?to - loc_type_loc_0022 ?w - window_type_tw_00060)
    :precondition (and
      (at ?v ?from)
      (connects ?r ?from ?to)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
      (not (location-blocked ?from))
      (not (location-blocked ?to))
    )
    :effect (and
      (not (at ?v ?from))
      (at ?v ?to)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-road_0044-tw_00090
    :parameters (?v - vehicle ?r - road_type_road_0044 ?from - loc_type_loc_0028 ?to - loc_type_loc_0022 ?w - window_type_tw_00090)
    :precondition (and
      (at ?v ?from)
      (connects ?r ?from ?to)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
      (not (location-blocked ?from))
      (not (location-blocked ?to))
    )
    :effect (and
      (not (at ?v ?from))
      (at ?v ?to)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-road_0044-tw_00120
    :parameters (?v - vehicle ?r - road_type_road_0044 ?from - loc_type_loc_0028 ?to - loc_type_loc_0022 ?w - window_type_tw_00120)
    :precondition (and
      (at ?v ?from)
      (connects ?r ?from ?to)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
      (not (location-blocked ?from))
      (not (location-blocked ?to))
    )
    :effect (and
      (not (at ?v ?from))
      (at ?v ?to)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-road_0044-tw_00150
    :parameters (?v - vehicle ?r - road_type_road_0044 ?from - loc_type_loc_0028 ?to - loc_type_loc_0022 ?w - window_type_tw_00150)
    :precondition (and
      (at ?v ?from)
      (connects ?r ?from ?to)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
      (not (location-blocked ?from))
      (not (location-blocked ?to))
    )
    :effect (and
      (not (at ?v ?from))
      (at ?v ?to)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-road_0044-tw_00180
    :parameters (?v - vehicle ?r - road_type_road_0044 ?from - loc_type_loc_0028 ?to - loc_type_loc_0022 ?w - window_type_tw_00180)
    :precondition (and
      (at ?v ?from)
      (connects ?r ?from ?to)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
      (not (location-blocked ?from))
      (not (location-blocked ?to))
    )
    :effect (and
      (not (at ?v ?from))
      (at ?v ?to)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-road_0044-tw_00210
    :parameters (?v - vehicle ?r - road_type_road_0044 ?from - loc_type_loc_0028 ?to - loc_type_loc_0022 ?w - window_type_tw_00210)
    :precondition (and
      (at ?v ?from)
      (connects ?r ?from ?to)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
      (not (location-blocked ?from))
      (not (location-blocked ?to))
    )
    :effect (and
      (not (at ?v ?from))
      (at ?v ?to)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-road_0044-tw_00240
    :parameters (?v - vehicle ?r - road_type_road_0044 ?from - loc_type_loc_0028 ?to - loc_type_loc_0022 ?w - window_type_tw_00240)
    :precondition (and
      (at ?v ?from)
      (connects ?r ?from ?to)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
      (not (location-blocked ?from))
      (not (location-blocked ?to))
    )
    :effect (and
      (not (at ?v ?from))
      (at ?v ?to)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-road_0044-tw_00270
    :parameters (?v - vehicle ?r - road_type_road_0044 ?from - loc_type_loc_0028 ?to - loc_type_loc_0022 ?w - window_type_tw_00270)
    :precondition (and
      (at ?v ?from)
      (connects ?r ?from ?to)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
      (not (location-blocked ?from))
      (not (location-blocked ?to))
    )
    :effect (and
      (not (at ?v ?from))
      (at ?v ?to)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-road_0044-tw_00300
    :parameters (?v - vehicle ?r - road_type_road_0044 ?from - loc_type_loc_0028 ?to - loc_type_loc_0022 ?w - window_type_tw_00300)
    :precondition (and
      (at ?v ?from)
      (connects ?r ?from ?to)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
      (not (location-blocked ?from))
      (not (location-blocked ?to))
    )
    :effect (and
      (not (at ?v ?from))
      (at ?v ?to)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-road_0044-tw_00330
    :parameters (?v - vehicle ?r - road_type_road_0044 ?from - loc_type_loc_0028 ?to - loc_type_loc_0022 ?w - window_type_tw_00330)
    :precondition (and
      (at ?v ?from)
      (connects ?r ?from ?to)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
      (not (location-blocked ?from))
      (not (location-blocked ?to))
    )
    :effect (and
      (not (at ?v ?from))
      (at ?v ?to)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-road_0044-tw_00360
    :parameters (?v - vehicle ?r - road_type_road_0044 ?from - loc_type_loc_0028 ?to - loc_type_loc_0022 ?w - window_type_tw_00360)
    :precondition (and
      (at ?v ?from)
      (connects ?r ?from ?to)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
      (not (location-blocked ?from))
      (not (location-blocked ?to))
    )
    :effect (and
      (not (at ?v ?from))
      (at ?v ?to)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-road_0044-tw_00390
    :parameters (?v - vehicle ?r - road_type_road_0044 ?from - loc_type_loc_0028 ?to - loc_type_loc_0022 ?w - window_type_tw_00390)
    :precondition (and
      (at ?v ?from)
      (connects ?r ?from ?to)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
      (not (location-blocked ?from))
      (not (location-blocked ?to))
    )
    :effect (and
      (not (at ?v ?from))
      (at ?v ?to)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-road_0044-tw_00420
    :parameters (?v - vehicle ?r - road_type_road_0044 ?from - loc_type_loc_0028 ?to - loc_type_loc_0022 ?w - window_type_tw_00420)
    :precondition (and
      (at ?v ?from)
      (connects ?r ?from ?to)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
      (not (location-blocked ?from))
      (not (location-blocked ?to))
    )
    :effect (and
      (not (at ?v ?from))
      (at ?v ?to)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-road_0044-tw_00450
    :parameters (?v - vehicle ?r - road_type_road_0044 ?from - loc_type_loc_0028 ?to - loc_type_loc_0022 ?w - window_type_tw_00450)
    :precondition (and
      (at ?v ?from)
      (connects ?r ?from ?to)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
      (not (location-blocked ?from))
      (not (location-blocked ?to))
    )
    :effect (and
      (not (at ?v ?from))
      (at ?v ?to)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-road_0045-tw_00000
    :parameters (?v - vehicle ?r - road_type_road_0045 ?from - loc_type_loc_0030 ?to - loc_type_loc_0078 ?w - window_type_tw_00000)
    :precondition (and
      (at ?v ?from)
      (connects ?r ?from ?to)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
      (not (location-blocked ?from))
      (not (location-blocked ?to))
    )
    :effect (and
      (not (at ?v ?from))
      (at ?v ?to)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-road_0045-tw_00030
    :parameters (?v - vehicle ?r - road_type_road_0045 ?from - loc_type_loc_0030 ?to - loc_type_loc_0078 ?w - window_type_tw_00030)
    :precondition (and
      (at ?v ?from)
      (connects ?r ?from ?to)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
      (not (location-blocked ?from))
      (not (location-blocked ?to))
    )
    :effect (and
      (not (at ?v ?from))
      (at ?v ?to)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-road_0045-tw_00060
    :parameters (?v - vehicle ?r - road_type_road_0045 ?from - loc_type_loc_0030 ?to - loc_type_loc_0078 ?w - window_type_tw_00060)
    :precondition (and
      (at ?v ?from)
      (connects ?r ?from ?to)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
      (not (location-blocked ?from))
      (not (location-blocked ?to))
    )
    :effect (and
      (not (at ?v ?from))
      (at ?v ?to)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-road_0045-tw_00090
    :parameters (?v - vehicle ?r - road_type_road_0045 ?from - loc_type_loc_0030 ?to - loc_type_loc_0078 ?w - window_type_tw_00090)
    :precondition (and
      (at ?v ?from)
      (connects ?r ?from ?to)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
      (not (location-blocked ?from))
      (not (location-blocked ?to))
    )
    :effect (and
      (not (at ?v ?from))
      (at ?v ?to)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-road_0045-tw_00120
    :parameters (?v - vehicle ?r - road_type_road_0045 ?from - loc_type_loc_0030 ?to - loc_type_loc_0078 ?w - window_type_tw_00120)
    :precondition (and
      (at ?v ?from)
      (connects ?r ?from ?to)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
      (not (location-blocked ?from))
      (not (location-blocked ?to))
    )
    :effect (and
      (not (at ?v ?from))
      (at ?v ?to)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-road_0045-tw_00150
    :parameters (?v - vehicle ?r - road_type_road_0045 ?from - loc_type_loc_0030 ?to - loc_type_loc_0078 ?w - window_type_tw_00150)
    :precondition (and
      (at ?v ?from)
      (connects ?r ?from ?to)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
      (not (location-blocked ?from))
      (not (location-blocked ?to))
    )
    :effect (and
      (not (at ?v ?from))
      (at ?v ?to)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-road_0045-tw_00180
    :parameters (?v - vehicle ?r - road_type_road_0045 ?from - loc_type_loc_0030 ?to - loc_type_loc_0078 ?w - window_type_tw_00180)
    :precondition (and
      (at ?v ?from)
      (connects ?r ?from ?to)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
      (not (location-blocked ?from))
      (not (location-blocked ?to))
    )
    :effect (and
      (not (at ?v ?from))
      (at ?v ?to)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-road_0045-tw_00210
    :parameters (?v - vehicle ?r - road_type_road_0045 ?from - loc_type_loc_0030 ?to - loc_type_loc_0078 ?w - window_type_tw_00210)
    :precondition (and
      (at ?v ?from)
      (connects ?r ?from ?to)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
      (not (location-blocked ?from))
      (not (location-blocked ?to))
    )
    :effect (and
      (not (at ?v ?from))
      (at ?v ?to)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-road_0045-tw_00240
    :parameters (?v - vehicle ?r - road_type_road_0045 ?from - loc_type_loc_0030 ?to - loc_type_loc_0078 ?w - window_type_tw_00240)
    :precondition (and
      (at ?v ?from)
      (connects ?r ?from ?to)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
      (not (location-blocked ?from))
      (not (location-blocked ?to))
    )
    :effect (and
      (not (at ?v ?from))
      (at ?v ?to)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-road_0045-tw_00270
    :parameters (?v - vehicle ?r - road_type_road_0045 ?from - loc_type_loc_0030 ?to - loc_type_loc_0078 ?w - window_type_tw_00270)
    :precondition (and
      (at ?v ?from)
      (connects ?r ?from ?to)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
      (not (location-blocked ?from))
      (not (location-blocked ?to))
    )
    :effect (and
      (not (at ?v ?from))
      (at ?v ?to)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-road_0045-tw_00300
    :parameters (?v - vehicle ?r - road_type_road_0045 ?from - loc_type_loc_0030 ?to - loc_type_loc_0078 ?w - window_type_tw_00300)
    :precondition (and
      (at ?v ?from)
      (connects ?r ?from ?to)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
      (not (location-blocked ?from))
      (not (location-blocked ?to))
    )
    :effect (and
      (not (at ?v ?from))
      (at ?v ?to)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-road_0045-tw_00330
    :parameters (?v - vehicle ?r - road_type_road_0045 ?from - loc_type_loc_0030 ?to - loc_type_loc_0078 ?w - window_type_tw_00330)
    :precondition (and
      (at ?v ?from)
      (connects ?r ?from ?to)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
      (not (location-blocked ?from))
      (not (location-blocked ?to))
    )
    :effect (and
      (not (at ?v ?from))
      (at ?v ?to)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-road_0045-tw_00360
    :parameters (?v - vehicle ?r - road_type_road_0045 ?from - loc_type_loc_0030 ?to - loc_type_loc_0078 ?w - window_type_tw_00360)
    :precondition (and
      (at ?v ?from)
      (connects ?r ?from ?to)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
      (not (location-blocked ?from))
      (not (location-blocked ?to))
    )
    :effect (and
      (not (at ?v ?from))
      (at ?v ?to)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-road_0045-tw_00390
    :parameters (?v - vehicle ?r - road_type_road_0045 ?from - loc_type_loc_0030 ?to - loc_type_loc_0078 ?w - window_type_tw_00390)
    :precondition (and
      (at ?v ?from)
      (connects ?r ?from ?to)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
      (not (location-blocked ?from))
      (not (location-blocked ?to))
    )
    :effect (and
      (not (at ?v ?from))
      (at ?v ?to)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-road_0045-tw_00420
    :parameters (?v - vehicle ?r - road_type_road_0045 ?from - loc_type_loc_0030 ?to - loc_type_loc_0078 ?w - window_type_tw_00420)
    :precondition (and
      (at ?v ?from)
      (connects ?r ?from ?to)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
      (not (location-blocked ?from))
      (not (location-blocked ?to))
    )
    :effect (and
      (not (at ?v ?from))
      (at ?v ?to)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-road_0045-tw_00450
    :parameters (?v - vehicle ?r - road_type_road_0045 ?from - loc_type_loc_0030 ?to - loc_type_loc_0078 ?w - window_type_tw_00450)
    :precondition (and
      (at ?v ?from)
      (connects ?r ?from ?to)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
      (not (location-blocked ?from))
      (not (location-blocked ?to))
    )
    :effect (and
      (not (at ?v ?from))
      (at ?v ?to)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-road_0046-tw_00000
    :parameters (?v - vehicle ?r - road_type_road_0046 ?from - loc_type_loc_0030 ?to - loc_type_loc_0062 ?w - window_type_tw_00000)
    :precondition (and
      (at ?v ?from)
      (connects ?r ?from ?to)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
      (not (location-blocked ?from))
      (not (location-blocked ?to))
    )
    :effect (and
      (not (at ?v ?from))
      (at ?v ?to)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-road_0046-tw_00030
    :parameters (?v - vehicle ?r - road_type_road_0046 ?from - loc_type_loc_0030 ?to - loc_type_loc_0062 ?w - window_type_tw_00030)
    :precondition (and
      (at ?v ?from)
      (connects ?r ?from ?to)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
      (not (location-blocked ?from))
      (not (location-blocked ?to))
    )
    :effect (and
      (not (at ?v ?from))
      (at ?v ?to)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-road_0046-tw_00060
    :parameters (?v - vehicle ?r - road_type_road_0046 ?from - loc_type_loc_0030 ?to - loc_type_loc_0062 ?w - window_type_tw_00060)
    :precondition (and
      (at ?v ?from)
      (connects ?r ?from ?to)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
      (not (location-blocked ?from))
      (not (location-blocked ?to))
    )
    :effect (and
      (not (at ?v ?from))
      (at ?v ?to)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-road_0046-tw_00090
    :parameters (?v - vehicle ?r - road_type_road_0046 ?from - loc_type_loc_0030 ?to - loc_type_loc_0062 ?w - window_type_tw_00090)
    :precondition (and
      (at ?v ?from)
      (connects ?r ?from ?to)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
      (not (location-blocked ?from))
      (not (location-blocked ?to))
    )
    :effect (and
      (not (at ?v ?from))
      (at ?v ?to)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-road_0046-tw_00120
    :parameters (?v - vehicle ?r - road_type_road_0046 ?from - loc_type_loc_0030 ?to - loc_type_loc_0062 ?w - window_type_tw_00120)
    :precondition (and
      (at ?v ?from)
      (connects ?r ?from ?to)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
      (not (location-blocked ?from))
      (not (location-blocked ?to))
    )
    :effect (and
      (not (at ?v ?from))
      (at ?v ?to)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-road_0046-tw_00150
    :parameters (?v - vehicle ?r - road_type_road_0046 ?from - loc_type_loc_0030 ?to - loc_type_loc_0062 ?w - window_type_tw_00150)
    :precondition (and
      (at ?v ?from)
      (connects ?r ?from ?to)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
      (not (location-blocked ?from))
      (not (location-blocked ?to))
    )
    :effect (and
      (not (at ?v ?from))
      (at ?v ?to)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-road_0046-tw_00180
    :parameters (?v - vehicle ?r - road_type_road_0046 ?from - loc_type_loc_0030 ?to - loc_type_loc_0062 ?w - window_type_tw_00180)
    :precondition (and
      (at ?v ?from)
      (connects ?r ?from ?to)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
      (not (location-blocked ?from))
      (not (location-blocked ?to))
    )
    :effect (and
      (not (at ?v ?from))
      (at ?v ?to)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-road_0046-tw_00210
    :parameters (?v - vehicle ?r - road_type_road_0046 ?from - loc_type_loc_0030 ?to - loc_type_loc_0062 ?w - window_type_tw_00210)
    :precondition (and
      (at ?v ?from)
      (connects ?r ?from ?to)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
      (not (location-blocked ?from))
      (not (location-blocked ?to))
    )
    :effect (and
      (not (at ?v ?from))
      (at ?v ?to)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-road_0046-tw_00240
    :parameters (?v - vehicle ?r - road_type_road_0046 ?from - loc_type_loc_0030 ?to - loc_type_loc_0062 ?w - window_type_tw_00240)
    :precondition (and
      (at ?v ?from)
      (connects ?r ?from ?to)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
      (not (location-blocked ?from))
      (not (location-blocked ?to))
    )
    :effect (and
      (not (at ?v ?from))
      (at ?v ?to)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-road_0046-tw_00270
    :parameters (?v - vehicle ?r - road_type_road_0046 ?from - loc_type_loc_0030 ?to - loc_type_loc_0062 ?w - window_type_tw_00270)
    :precondition (and
      (at ?v ?from)
      (connects ?r ?from ?to)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
      (not (location-blocked ?from))
      (not (location-blocked ?to))
    )
    :effect (and
      (not (at ?v ?from))
      (at ?v ?to)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-road_0046-tw_00300
    :parameters (?v - vehicle ?r - road_type_road_0046 ?from - loc_type_loc_0030 ?to - loc_type_loc_0062 ?w - window_type_tw_00300)
    :precondition (and
      (at ?v ?from)
      (connects ?r ?from ?to)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
      (not (location-blocked ?from))
      (not (location-blocked ?to))
    )
    :effect (and
      (not (at ?v ?from))
      (at ?v ?to)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-road_0046-tw_00330
    :parameters (?v - vehicle ?r - road_type_road_0046 ?from - loc_type_loc_0030 ?to - loc_type_loc_0062 ?w - window_type_tw_00330)
    :precondition (and
      (at ?v ?from)
      (connects ?r ?from ?to)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
      (not (location-blocked ?from))
      (not (location-blocked ?to))
    )
    :effect (and
      (not (at ?v ?from))
      (at ?v ?to)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-road_0046-tw_00360
    :parameters (?v - vehicle ?r - road_type_road_0046 ?from - loc_type_loc_0030 ?to - loc_type_loc_0062 ?w - window_type_tw_00360)
    :precondition (and
      (at ?v ?from)
      (connects ?r ?from ?to)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
      (not (location-blocked ?from))
      (not (location-blocked ?to))
    )
    :effect (and
      (not (at ?v ?from))
      (at ?v ?to)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-road_0046-tw_00390
    :parameters (?v - vehicle ?r - road_type_road_0046 ?from - loc_type_loc_0030 ?to - loc_type_loc_0062 ?w - window_type_tw_00390)
    :precondition (and
      (at ?v ?from)
      (connects ?r ?from ?to)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
      (not (location-blocked ?from))
      (not (location-blocked ?to))
    )
    :effect (and
      (not (at ?v ?from))
      (at ?v ?to)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-road_0046-tw_00420
    :parameters (?v - vehicle ?r - road_type_road_0046 ?from - loc_type_loc_0030 ?to - loc_type_loc_0062 ?w - window_type_tw_00420)
    :precondition (and
      (at ?v ?from)
      (connects ?r ?from ?to)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
      (not (location-blocked ?from))
      (not (location-blocked ?to))
    )
    :effect (and
      (not (at ?v ?from))
      (at ?v ?to)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-road_0046-tw_00450
    :parameters (?v - vehicle ?r - road_type_road_0046 ?from - loc_type_loc_0030 ?to - loc_type_loc_0062 ?w - window_type_tw_00450)
    :precondition (and
      (at ?v ?from)
      (connects ?r ?from ?to)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
      (not (location-blocked ?from))
      (not (location-blocked ?to))
    )
    :effect (and
      (not (at ?v ?from))
      (at ?v ?to)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-road_0047-tw_00000
    :parameters (?v - vehicle ?r - road_type_road_0047 ?from - loc_type_loc_0031 ?to - loc_type_loc_0079 ?w - window_type_tw_00000)
    :precondition (and
      (at ?v ?from)
      (connects ?r ?from ?to)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
      (not (location-blocked ?from))
      (not (location-blocked ?to))
    )
    :effect (and
      (not (at ?v ?from))
      (at ?v ?to)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-road_0047-tw_00030
    :parameters (?v - vehicle ?r - road_type_road_0047 ?from - loc_type_loc_0031 ?to - loc_type_loc_0079 ?w - window_type_tw_00030)
    :precondition (and
      (at ?v ?from)
      (connects ?r ?from ?to)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
      (not (location-blocked ?from))
      (not (location-blocked ?to))
    )
    :effect (and
      (not (at ?v ?from))
      (at ?v ?to)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-road_0047-tw_00060
    :parameters (?v - vehicle ?r - road_type_road_0047 ?from - loc_type_loc_0031 ?to - loc_type_loc_0079 ?w - window_type_tw_00060)
    :precondition (and
      (at ?v ?from)
      (connects ?r ?from ?to)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
      (not (location-blocked ?from))
      (not (location-blocked ?to))
    )
    :effect (and
      (not (at ?v ?from))
      (at ?v ?to)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-road_0047-tw_00090
    :parameters (?v - vehicle ?r - road_type_road_0047 ?from - loc_type_loc_0031 ?to - loc_type_loc_0079 ?w - window_type_tw_00090)
    :precondition (and
      (at ?v ?from)
      (connects ?r ?from ?to)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
      (not (location-blocked ?from))
      (not (location-blocked ?to))
    )
    :effect (and
      (not (at ?v ?from))
      (at ?v ?to)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-road_0047-tw_00120
    :parameters (?v - vehicle ?r - road_type_road_0047 ?from - loc_type_loc_0031 ?to - loc_type_loc_0079 ?w - window_type_tw_00120)
    :precondition (and
      (at ?v ?from)
      (connects ?r ?from ?to)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
      (not (location-blocked ?from))
      (not (location-blocked ?to))
    )
    :effect (and
      (not (at ?v ?from))
      (at ?v ?to)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-road_0047-tw_00150
    :parameters (?v - vehicle ?r - road_type_road_0047 ?from - loc_type_loc_0031 ?to - loc_type_loc_0079 ?w - window_type_tw_00150)
    :precondition (and
      (at ?v ?from)
      (connects ?r ?from ?to)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
      (not (location-blocked ?from))
      (not (location-blocked ?to))
    )
    :effect (and
      (not (at ?v ?from))
      (at ?v ?to)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-road_0047-tw_00180
    :parameters (?v - vehicle ?r - road_type_road_0047 ?from - loc_type_loc_0031 ?to - loc_type_loc_0079 ?w - window_type_tw_00180)
    :precondition (and
      (at ?v ?from)
      (connects ?r ?from ?to)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
      (not (location-blocked ?from))
      (not (location-blocked ?to))
    )
    :effect (and
      (not (at ?v ?from))
      (at ?v ?to)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-road_0047-tw_00210
    :parameters (?v - vehicle ?r - road_type_road_0047 ?from - loc_type_loc_0031 ?to - loc_type_loc_0079 ?w - window_type_tw_00210)
    :precondition (and
      (at ?v ?from)
      (connects ?r ?from ?to)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
      (not (location-blocked ?from))
      (not (location-blocked ?to))
    )
    :effect (and
      (not (at ?v ?from))
      (at ?v ?to)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-road_0047-tw_00240
    :parameters (?v - vehicle ?r - road_type_road_0047 ?from - loc_type_loc_0031 ?to - loc_type_loc_0079 ?w - window_type_tw_00240)
    :precondition (and
      (at ?v ?from)
      (connects ?r ?from ?to)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
      (not (location-blocked ?from))
      (not (location-blocked ?to))
    )
    :effect (and
      (not (at ?v ?from))
      (at ?v ?to)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-road_0047-tw_00270
    :parameters (?v - vehicle ?r - road_type_road_0047 ?from - loc_type_loc_0031 ?to - loc_type_loc_0079 ?w - window_type_tw_00270)
    :precondition (and
      (at ?v ?from)
      (connects ?r ?from ?to)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
      (not (location-blocked ?from))
      (not (location-blocked ?to))
    )
    :effect (and
      (not (at ?v ?from))
      (at ?v ?to)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-road_0047-tw_00300
    :parameters (?v - vehicle ?r - road_type_road_0047 ?from - loc_type_loc_0031 ?to - loc_type_loc_0079 ?w - window_type_tw_00300)
    :precondition (and
      (at ?v ?from)
      (connects ?r ?from ?to)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
      (not (location-blocked ?from))
      (not (location-blocked ?to))
    )
    :effect (and
      (not (at ?v ?from))
      (at ?v ?to)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-road_0047-tw_00330
    :parameters (?v - vehicle ?r - road_type_road_0047 ?from - loc_type_loc_0031 ?to - loc_type_loc_0079 ?w - window_type_tw_00330)
    :precondition (and
      (at ?v ?from)
      (connects ?r ?from ?to)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
      (not (location-blocked ?from))
      (not (location-blocked ?to))
    )
    :effect (and
      (not (at ?v ?from))
      (at ?v ?to)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-road_0047-tw_00360
    :parameters (?v - vehicle ?r - road_type_road_0047 ?from - loc_type_loc_0031 ?to - loc_type_loc_0079 ?w - window_type_tw_00360)
    :precondition (and
      (at ?v ?from)
      (connects ?r ?from ?to)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
      (not (location-blocked ?from))
      (not (location-blocked ?to))
    )
    :effect (and
      (not (at ?v ?from))
      (at ?v ?to)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-road_0047-tw_00390
    :parameters (?v - vehicle ?r - road_type_road_0047 ?from - loc_type_loc_0031 ?to - loc_type_loc_0079 ?w - window_type_tw_00390)
    :precondition (and
      (at ?v ?from)
      (connects ?r ?from ?to)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
      (not (location-blocked ?from))
      (not (location-blocked ?to))
    )
    :effect (and
      (not (at ?v ?from))
      (at ?v ?to)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-road_0047-tw_00420
    :parameters (?v - vehicle ?r - road_type_road_0047 ?from - loc_type_loc_0031 ?to - loc_type_loc_0079 ?w - window_type_tw_00420)
    :precondition (and
      (at ?v ?from)
      (connects ?r ?from ?to)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
      (not (location-blocked ?from))
      (not (location-blocked ?to))
    )
    :effect (and
      (not (at ?v ?from))
      (at ?v ?to)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-road_0047-tw_00450
    :parameters (?v - vehicle ?r - road_type_road_0047 ?from - loc_type_loc_0031 ?to - loc_type_loc_0079 ?w - window_type_tw_00450)
    :precondition (and
      (at ?v ?from)
      (connects ?r ?from ?to)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
      (not (location-blocked ?from))
      (not (location-blocked ?to))
    )
    :effect (and
      (not (at ?v ?from))
      (at ?v ?to)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-road_0049-tw_00000
    :parameters (?v - vehicle ?r - road_type_road_0049 ?from - loc_type_loc_0031 ?to - loc_type_loc_0029 ?w - window_type_tw_00000)
    :precondition (and
      (at ?v ?from)
      (connects ?r ?from ?to)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
      (not (location-blocked ?from))
      (not (location-blocked ?to))
    )
    :effect (and
      (not (at ?v ?from))
      (at ?v ?to)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-road_0049-tw_00030
    :parameters (?v - vehicle ?r - road_type_road_0049 ?from - loc_type_loc_0031 ?to - loc_type_loc_0029 ?w - window_type_tw_00030)
    :precondition (and
      (at ?v ?from)
      (connects ?r ?from ?to)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
      (not (location-blocked ?from))
      (not (location-blocked ?to))
    )
    :effect (and
      (not (at ?v ?from))
      (at ?v ?to)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-road_0049-tw_00060
    :parameters (?v - vehicle ?r - road_type_road_0049 ?from - loc_type_loc_0031 ?to - loc_type_loc_0029 ?w - window_type_tw_00060)
    :precondition (and
      (at ?v ?from)
      (connects ?r ?from ?to)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
      (not (location-blocked ?from))
      (not (location-blocked ?to))
    )
    :effect (and
      (not (at ?v ?from))
      (at ?v ?to)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-road_0049-tw_00090
    :parameters (?v - vehicle ?r - road_type_road_0049 ?from - loc_type_loc_0031 ?to - loc_type_loc_0029 ?w - window_type_tw_00090)
    :precondition (and
      (at ?v ?from)
      (connects ?r ?from ?to)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
      (not (location-blocked ?from))
      (not (location-blocked ?to))
    )
    :effect (and
      (not (at ?v ?from))
      (at ?v ?to)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-road_0049-tw_00120
    :parameters (?v - vehicle ?r - road_type_road_0049 ?from - loc_type_loc_0031 ?to - loc_type_loc_0029 ?w - window_type_tw_00120)
    :precondition (and
      (at ?v ?from)
      (connects ?r ?from ?to)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
      (not (location-blocked ?from))
      (not (location-blocked ?to))
    )
    :effect (and
      (not (at ?v ?from))
      (at ?v ?to)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-road_0049-tw_00150
    :parameters (?v - vehicle ?r - road_type_road_0049 ?from - loc_type_loc_0031 ?to - loc_type_loc_0029 ?w - window_type_tw_00150)
    :precondition (and
      (at ?v ?from)
      (connects ?r ?from ?to)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
      (not (location-blocked ?from))
      (not (location-blocked ?to))
    )
    :effect (and
      (not (at ?v ?from))
      (at ?v ?to)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-road_0049-tw_00180
    :parameters (?v - vehicle ?r - road_type_road_0049 ?from - loc_type_loc_0031 ?to - loc_type_loc_0029 ?w - window_type_tw_00180)
    :precondition (and
      (at ?v ?from)
      (connects ?r ?from ?to)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
      (not (location-blocked ?from))
      (not (location-blocked ?to))
    )
    :effect (and
      (not (at ?v ?from))
      (at ?v ?to)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-road_0049-tw_00210
    :parameters (?v - vehicle ?r - road_type_road_0049 ?from - loc_type_loc_0031 ?to - loc_type_loc_0029 ?w - window_type_tw_00210)
    :precondition (and
      (at ?v ?from)
      (connects ?r ?from ?to)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
      (not (location-blocked ?from))
      (not (location-blocked ?to))
    )
    :effect (and
      (not (at ?v ?from))
      (at ?v ?to)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-road_0049-tw_00240
    :parameters (?v - vehicle ?r - road_type_road_0049 ?from - loc_type_loc_0031 ?to - loc_type_loc_0029 ?w - window_type_tw_00240)
    :precondition (and
      (at ?v ?from)
      (connects ?r ?from ?to)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
      (not (location-blocked ?from))
      (not (location-blocked ?to))
    )
    :effect (and
      (not (at ?v ?from))
      (at ?v ?to)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-road_0049-tw_00270
    :parameters (?v - vehicle ?r - road_type_road_0049 ?from - loc_type_loc_0031 ?to - loc_type_loc_0029 ?w - window_type_tw_00270)
    :precondition (and
      (at ?v ?from)
      (connects ?r ?from ?to)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
      (not (location-blocked ?from))
      (not (location-blocked ?to))
    )
    :effect (and
      (not (at ?v ?from))
      (at ?v ?to)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-road_0049-tw_00300
    :parameters (?v - vehicle ?r - road_type_road_0049 ?from - loc_type_loc_0031 ?to - loc_type_loc_0029 ?w - window_type_tw_00300)
    :precondition (and
      (at ?v ?from)
      (connects ?r ?from ?to)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
      (not (location-blocked ?from))
      (not (location-blocked ?to))
    )
    :effect (and
      (not (at ?v ?from))
      (at ?v ?to)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-road_0049-tw_00330
    :parameters (?v - vehicle ?r - road_type_road_0049 ?from - loc_type_loc_0031 ?to - loc_type_loc_0029 ?w - window_type_tw_00330)
    :precondition (and
      (at ?v ?from)
      (connects ?r ?from ?to)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
      (not (location-blocked ?from))
      (not (location-blocked ?to))
    )
    :effect (and
      (not (at ?v ?from))
      (at ?v ?to)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-road_0049-tw_00360
    :parameters (?v - vehicle ?r - road_type_road_0049 ?from - loc_type_loc_0031 ?to - loc_type_loc_0029 ?w - window_type_tw_00360)
    :precondition (and
      (at ?v ?from)
      (connects ?r ?from ?to)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
      (not (location-blocked ?from))
      (not (location-blocked ?to))
    )
    :effect (and
      (not (at ?v ?from))
      (at ?v ?to)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-road_0049-tw_00390
    :parameters (?v - vehicle ?r - road_type_road_0049 ?from - loc_type_loc_0031 ?to - loc_type_loc_0029 ?w - window_type_tw_00390)
    :precondition (and
      (at ?v ?from)
      (connects ?r ?from ?to)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
      (not (location-blocked ?from))
      (not (location-blocked ?to))
    )
    :effect (and
      (not (at ?v ?from))
      (at ?v ?to)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-road_0049-tw_00420
    :parameters (?v - vehicle ?r - road_type_road_0049 ?from - loc_type_loc_0031 ?to - loc_type_loc_0029 ?w - window_type_tw_00420)
    :precondition (and
      (at ?v ?from)
      (connects ?r ?from ?to)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
      (not (location-blocked ?from))
      (not (location-blocked ?to))
    )
    :effect (and
      (not (at ?v ?from))
      (at ?v ?to)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-road_0049-tw_00450
    :parameters (?v - vehicle ?r - road_type_road_0049 ?from - loc_type_loc_0031 ?to - loc_type_loc_0029 ?w - window_type_tw_00450)
    :precondition (and
      (at ?v ?from)
      (connects ?r ?from ?to)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
      (not (location-blocked ?from))
      (not (location-blocked ?to))
    )
    :effect (and
      (not (at ?v ?from))
      (at ?v ?to)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-road_0050-tw_00000
    :parameters (?v - vehicle ?r - road_type_road_0050 ?from - loc_type_loc_0032 ?to - loc_type_loc_0092 ?w - window_type_tw_00000)
    :precondition (and
      (at ?v ?from)
      (connects ?r ?from ?to)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
      (not (location-blocked ?from))
      (not (location-blocked ?to))
    )
    :effect (and
      (not (at ?v ?from))
      (at ?v ?to)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-road_0050-tw_00030
    :parameters (?v - vehicle ?r - road_type_road_0050 ?from - loc_type_loc_0032 ?to - loc_type_loc_0092 ?w - window_type_tw_00030)
    :precondition (and
      (at ?v ?from)
      (connects ?r ?from ?to)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
      (not (location-blocked ?from))
      (not (location-blocked ?to))
    )
    :effect (and
      (not (at ?v ?from))
      (at ?v ?to)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-road_0050-tw_00060
    :parameters (?v - vehicle ?r - road_type_road_0050 ?from - loc_type_loc_0032 ?to - loc_type_loc_0092 ?w - window_type_tw_00060)
    :precondition (and
      (at ?v ?from)
      (connects ?r ?from ?to)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
      (not (location-blocked ?from))
      (not (location-blocked ?to))
    )
    :effect (and
      (not (at ?v ?from))
      (at ?v ?to)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-road_0050-tw_00090
    :parameters (?v - vehicle ?r - road_type_road_0050 ?from - loc_type_loc_0032 ?to - loc_type_loc_0092 ?w - window_type_tw_00090)
    :precondition (and
      (at ?v ?from)
      (connects ?r ?from ?to)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
      (not (location-blocked ?from))
      (not (location-blocked ?to))
    )
    :effect (and
      (not (at ?v ?from))
      (at ?v ?to)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-road_0050-tw_00120
    :parameters (?v - vehicle ?r - road_type_road_0050 ?from - loc_type_loc_0032 ?to - loc_type_loc_0092 ?w - window_type_tw_00120)
    :precondition (and
      (at ?v ?from)
      (connects ?r ?from ?to)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
      (not (location-blocked ?from))
      (not (location-blocked ?to))
    )
    :effect (and
      (not (at ?v ?from))
      (at ?v ?to)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-road_0050-tw_00150
    :parameters (?v - vehicle ?r - road_type_road_0050 ?from - loc_type_loc_0032 ?to - loc_type_loc_0092 ?w - window_type_tw_00150)
    :precondition (and
      (at ?v ?from)
      (connects ?r ?from ?to)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
      (not (location-blocked ?from))
      (not (location-blocked ?to))
    )
    :effect (and
      (not (at ?v ?from))
      (at ?v ?to)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-road_0050-tw_00180
    :parameters (?v - vehicle ?r - road_type_road_0050 ?from - loc_type_loc_0032 ?to - loc_type_loc_0092 ?w - window_type_tw_00180)
    :precondition (and
      (at ?v ?from)
      (connects ?r ?from ?to)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
      (not (location-blocked ?from))
      (not (location-blocked ?to))
    )
    :effect (and
      (not (at ?v ?from))
      (at ?v ?to)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-road_0050-tw_00210
    :parameters (?v - vehicle ?r - road_type_road_0050 ?from - loc_type_loc_0032 ?to - loc_type_loc_0092 ?w - window_type_tw_00210)
    :precondition (and
      (at ?v ?from)
      (connects ?r ?from ?to)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
      (not (location-blocked ?from))
      (not (location-blocked ?to))
    )
    :effect (and
      (not (at ?v ?from))
      (at ?v ?to)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-road_0050-tw_00240
    :parameters (?v - vehicle ?r - road_type_road_0050 ?from - loc_type_loc_0032 ?to - loc_type_loc_0092 ?w - window_type_tw_00240)
    :precondition (and
      (at ?v ?from)
      (connects ?r ?from ?to)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
      (not (location-blocked ?from))
      (not (location-blocked ?to))
    )
    :effect (and
      (not (at ?v ?from))
      (at ?v ?to)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-road_0050-tw_00270
    :parameters (?v - vehicle ?r - road_type_road_0050 ?from - loc_type_loc_0032 ?to - loc_type_loc_0092 ?w - window_type_tw_00270)
    :precondition (and
      (at ?v ?from)
      (connects ?r ?from ?to)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
      (not (location-blocked ?from))
      (not (location-blocked ?to))
    )
    :effect (and
      (not (at ?v ?from))
      (at ?v ?to)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-road_0050-tw_00300
    :parameters (?v - vehicle ?r - road_type_road_0050 ?from - loc_type_loc_0032 ?to - loc_type_loc_0092 ?w - window_type_tw_00300)
    :precondition (and
      (at ?v ?from)
      (connects ?r ?from ?to)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
      (not (location-blocked ?from))
      (not (location-blocked ?to))
    )
    :effect (and
      (not (at ?v ?from))
      (at ?v ?to)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-road_0050-tw_00330
    :parameters (?v - vehicle ?r - road_type_road_0050 ?from - loc_type_loc_0032 ?to - loc_type_loc_0092 ?w - window_type_tw_00330)
    :precondition (and
      (at ?v ?from)
      (connects ?r ?from ?to)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
      (not (location-blocked ?from))
      (not (location-blocked ?to))
    )
    :effect (and
      (not (at ?v ?from))
      (at ?v ?to)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-road_0050-tw_00360
    :parameters (?v - vehicle ?r - road_type_road_0050 ?from - loc_type_loc_0032 ?to - loc_type_loc_0092 ?w - window_type_tw_00360)
    :precondition (and
      (at ?v ?from)
      (connects ?r ?from ?to)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
      (not (location-blocked ?from))
      (not (location-blocked ?to))
    )
    :effect (and
      (not (at ?v ?from))
      (at ?v ?to)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-road_0050-tw_00390
    :parameters (?v - vehicle ?r - road_type_road_0050 ?from - loc_type_loc_0032 ?to - loc_type_loc_0092 ?w - window_type_tw_00390)
    :precondition (and
      (at ?v ?from)
      (connects ?r ?from ?to)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
      (not (location-blocked ?from))
      (not (location-blocked ?to))
    )
    :effect (and
      (not (at ?v ?from))
      (at ?v ?to)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-road_0050-tw_00420
    :parameters (?v - vehicle ?r - road_type_road_0050 ?from - loc_type_loc_0032 ?to - loc_type_loc_0092 ?w - window_type_tw_00420)
    :precondition (and
      (at ?v ?from)
      (connects ?r ?from ?to)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
      (not (location-blocked ?from))
      (not (location-blocked ?to))
    )
    :effect (and
      (not (at ?v ?from))
      (at ?v ?to)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-road_0050-tw_00450
    :parameters (?v - vehicle ?r - road_type_road_0050 ?from - loc_type_loc_0032 ?to - loc_type_loc_0092 ?w - window_type_tw_00450)
    :precondition (and
      (at ?v ?from)
      (connects ?r ?from ?to)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
      (not (location-blocked ?from))
      (not (location-blocked ?to))
    )
    :effect (and
      (not (at ?v ?from))
      (at ?v ?to)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-road_0051-tw_00000
    :parameters (?v - vehicle ?r - road_type_road_0051 ?from - loc_type_loc_0033 ?to - loc_type_loc_0024 ?w - window_type_tw_00000)
    :precondition (and
      (at ?v ?from)
      (connects ?r ?from ?to)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
      (not (location-blocked ?from))
      (not (location-blocked ?to))
    )
    :effect (and
      (not (at ?v ?from))
      (at ?v ?to)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-road_0051-tw_00030
    :parameters (?v - vehicle ?r - road_type_road_0051 ?from - loc_type_loc_0033 ?to - loc_type_loc_0024 ?w - window_type_tw_00030)
    :precondition (and
      (at ?v ?from)
      (connects ?r ?from ?to)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
      (not (location-blocked ?from))
      (not (location-blocked ?to))
    )
    :effect (and
      (not (at ?v ?from))
      (at ?v ?to)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-road_0051-tw_00060
    :parameters (?v - vehicle ?r - road_type_road_0051 ?from - loc_type_loc_0033 ?to - loc_type_loc_0024 ?w - window_type_tw_00060)
    :precondition (and
      (at ?v ?from)
      (connects ?r ?from ?to)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
      (not (location-blocked ?from))
      (not (location-blocked ?to))
    )
    :effect (and
      (not (at ?v ?from))
      (at ?v ?to)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-road_0051-tw_00090
    :parameters (?v - vehicle ?r - road_type_road_0051 ?from - loc_type_loc_0033 ?to - loc_type_loc_0024 ?w - window_type_tw_00090)
    :precondition (and
      (at ?v ?from)
      (connects ?r ?from ?to)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
      (not (location-blocked ?from))
      (not (location-blocked ?to))
    )
    :effect (and
      (not (at ?v ?from))
      (at ?v ?to)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-road_0051-tw_00120
    :parameters (?v - vehicle ?r - road_type_road_0051 ?from - loc_type_loc_0033 ?to - loc_type_loc_0024 ?w - window_type_tw_00120)
    :precondition (and
      (at ?v ?from)
      (connects ?r ?from ?to)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
      (not (location-blocked ?from))
      (not (location-blocked ?to))
    )
    :effect (and
      (not (at ?v ?from))
      (at ?v ?to)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-road_0051-tw_00150
    :parameters (?v - vehicle ?r - road_type_road_0051 ?from - loc_type_loc_0033 ?to - loc_type_loc_0024 ?w - window_type_tw_00150)
    :precondition (and
      (at ?v ?from)
      (connects ?r ?from ?to)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
      (not (location-blocked ?from))
      (not (location-blocked ?to))
    )
    :effect (and
      (not (at ?v ?from))
      (at ?v ?to)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-road_0051-tw_00180
    :parameters (?v - vehicle ?r - road_type_road_0051 ?from - loc_type_loc_0033 ?to - loc_type_loc_0024 ?w - window_type_tw_00180)
    :precondition (and
      (at ?v ?from)
      (connects ?r ?from ?to)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
      (not (location-blocked ?from))
      (not (location-blocked ?to))
    )
    :effect (and
      (not (at ?v ?from))
      (at ?v ?to)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-road_0051-tw_00210
    :parameters (?v - vehicle ?r - road_type_road_0051 ?from - loc_type_loc_0033 ?to - loc_type_loc_0024 ?w - window_type_tw_00210)
    :precondition (and
      (at ?v ?from)
      (connects ?r ?from ?to)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
      (not (location-blocked ?from))
      (not (location-blocked ?to))
    )
    :effect (and
      (not (at ?v ?from))
      (at ?v ?to)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-road_0051-tw_00240
    :parameters (?v - vehicle ?r - road_type_road_0051 ?from - loc_type_loc_0033 ?to - loc_type_loc_0024 ?w - window_type_tw_00240)
    :precondition (and
      (at ?v ?from)
      (connects ?r ?from ?to)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
      (not (location-blocked ?from))
      (not (location-blocked ?to))
    )
    :effect (and
      (not (at ?v ?from))
      (at ?v ?to)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-road_0051-tw_00270
    :parameters (?v - vehicle ?r - road_type_road_0051 ?from - loc_type_loc_0033 ?to - loc_type_loc_0024 ?w - window_type_tw_00270)
    :precondition (and
      (at ?v ?from)
      (connects ?r ?from ?to)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
      (not (location-blocked ?from))
      (not (location-blocked ?to))
    )
    :effect (and
      (not (at ?v ?from))
      (at ?v ?to)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-road_0051-tw_00300
    :parameters (?v - vehicle ?r - road_type_road_0051 ?from - loc_type_loc_0033 ?to - loc_type_loc_0024 ?w - window_type_tw_00300)
    :precondition (and
      (at ?v ?from)
      (connects ?r ?from ?to)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
      (not (location-blocked ?from))
      (not (location-blocked ?to))
    )
    :effect (and
      (not (at ?v ?from))
      (at ?v ?to)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-road_0051-tw_00330
    :parameters (?v - vehicle ?r - road_type_road_0051 ?from - loc_type_loc_0033 ?to - loc_type_loc_0024 ?w - window_type_tw_00330)
    :precondition (and
      (at ?v ?from)
      (connects ?r ?from ?to)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
      (not (location-blocked ?from))
      (not (location-blocked ?to))
    )
    :effect (and
      (not (at ?v ?from))
      (at ?v ?to)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-road_0051-tw_00360
    :parameters (?v - vehicle ?r - road_type_road_0051 ?from - loc_type_loc_0033 ?to - loc_type_loc_0024 ?w - window_type_tw_00360)
    :precondition (and
      (at ?v ?from)
      (connects ?r ?from ?to)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
      (not (location-blocked ?from))
      (not (location-blocked ?to))
    )
    :effect (and
      (not (at ?v ?from))
      (at ?v ?to)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-road_0051-tw_00390
    :parameters (?v - vehicle ?r - road_type_road_0051 ?from - loc_type_loc_0033 ?to - loc_type_loc_0024 ?w - window_type_tw_00390)
    :precondition (and
      (at ?v ?from)
      (connects ?r ?from ?to)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
      (not (location-blocked ?from))
      (not (location-blocked ?to))
    )
    :effect (and
      (not (at ?v ?from))
      (at ?v ?to)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-road_0051-tw_00420
    :parameters (?v - vehicle ?r - road_type_road_0051 ?from - loc_type_loc_0033 ?to - loc_type_loc_0024 ?w - window_type_tw_00420)
    :precondition (and
      (at ?v ?from)
      (connects ?r ?from ?to)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
      (not (location-blocked ?from))
      (not (location-blocked ?to))
    )
    :effect (and
      (not (at ?v ?from))
      (at ?v ?to)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-road_0051-tw_00450
    :parameters (?v - vehicle ?r - road_type_road_0051 ?from - loc_type_loc_0033 ?to - loc_type_loc_0024 ?w - window_type_tw_00450)
    :precondition (and
      (at ?v ?from)
      (connects ?r ?from ?to)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
      (not (location-blocked ?from))
      (not (location-blocked ?to))
    )
    :effect (and
      (not (at ?v ?from))
      (at ?v ?to)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-road_0052-tw_00000
    :parameters (?v - vehicle ?r - road_type_road_0052 ?from - loc_type_loc_0034 ?to - loc_type_loc_0055 ?w - window_type_tw_00000)
    :precondition (and
      (at ?v ?from)
      (connects ?r ?from ?to)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
      (not (location-blocked ?from))
      (not (location-blocked ?to))
    )
    :effect (and
      (not (at ?v ?from))
      (at ?v ?to)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-road_0052-tw_00030
    :parameters (?v - vehicle ?r - road_type_road_0052 ?from - loc_type_loc_0034 ?to - loc_type_loc_0055 ?w - window_type_tw_00030)
    :precondition (and
      (at ?v ?from)
      (connects ?r ?from ?to)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
      (not (location-blocked ?from))
      (not (location-blocked ?to))
    )
    :effect (and
      (not (at ?v ?from))
      (at ?v ?to)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-road_0052-tw_00060
    :parameters (?v - vehicle ?r - road_type_road_0052 ?from - loc_type_loc_0034 ?to - loc_type_loc_0055 ?w - window_type_tw_00060)
    :precondition (and
      (at ?v ?from)
      (connects ?r ?from ?to)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
      (not (location-blocked ?from))
      (not (location-blocked ?to))
    )
    :effect (and
      (not (at ?v ?from))
      (at ?v ?to)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-road_0052-tw_00090
    :parameters (?v - vehicle ?r - road_type_road_0052 ?from - loc_type_loc_0034 ?to - loc_type_loc_0055 ?w - window_type_tw_00090)
    :precondition (and
      (at ?v ?from)
      (connects ?r ?from ?to)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
      (not (location-blocked ?from))
      (not (location-blocked ?to))
    )
    :effect (and
      (not (at ?v ?from))
      (at ?v ?to)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-road_0052-tw_00120
    :parameters (?v - vehicle ?r - road_type_road_0052 ?from - loc_type_loc_0034 ?to - loc_type_loc_0055 ?w - window_type_tw_00120)
    :precondition (and
      (at ?v ?from)
      (connects ?r ?from ?to)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
      (not (location-blocked ?from))
      (not (location-blocked ?to))
    )
    :effect (and
      (not (at ?v ?from))
      (at ?v ?to)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-road_0052-tw_00150
    :parameters (?v - vehicle ?r - road_type_road_0052 ?from - loc_type_loc_0034 ?to - loc_type_loc_0055 ?w - window_type_tw_00150)
    :precondition (and
      (at ?v ?from)
      (connects ?r ?from ?to)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
      (not (location-blocked ?from))
      (not (location-blocked ?to))
    )
    :effect (and
      (not (at ?v ?from))
      (at ?v ?to)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-road_0052-tw_00180
    :parameters (?v - vehicle ?r - road_type_road_0052 ?from - loc_type_loc_0034 ?to - loc_type_loc_0055 ?w - window_type_tw_00180)
    :precondition (and
      (at ?v ?from)
      (connects ?r ?from ?to)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
      (not (location-blocked ?from))
      (not (location-blocked ?to))
    )
    :effect (and
      (not (at ?v ?from))
      (at ?v ?to)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-road_0052-tw_00210
    :parameters (?v - vehicle ?r - road_type_road_0052 ?from - loc_type_loc_0034 ?to - loc_type_loc_0055 ?w - window_type_tw_00210)
    :precondition (and
      (at ?v ?from)
      (connects ?r ?from ?to)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
      (not (location-blocked ?from))
      (not (location-blocked ?to))
    )
    :effect (and
      (not (at ?v ?from))
      (at ?v ?to)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-road_0052-tw_00240
    :parameters (?v - vehicle ?r - road_type_road_0052 ?from - loc_type_loc_0034 ?to - loc_type_loc_0055 ?w - window_type_tw_00240)
    :precondition (and
      (at ?v ?from)
      (connects ?r ?from ?to)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
      (not (location-blocked ?from))
      (not (location-blocked ?to))
    )
    :effect (and
      (not (at ?v ?from))
      (at ?v ?to)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-road_0052-tw_00270
    :parameters (?v - vehicle ?r - road_type_road_0052 ?from - loc_type_loc_0034 ?to - loc_type_loc_0055 ?w - window_type_tw_00270)
    :precondition (and
      (at ?v ?from)
      (connects ?r ?from ?to)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
      (not (location-blocked ?from))
      (not (location-blocked ?to))
    )
    :effect (and
      (not (at ?v ?from))
      (at ?v ?to)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-road_0052-tw_00300
    :parameters (?v - vehicle ?r - road_type_road_0052 ?from - loc_type_loc_0034 ?to - loc_type_loc_0055 ?w - window_type_tw_00300)
    :precondition (and
      (at ?v ?from)
      (connects ?r ?from ?to)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
      (not (location-blocked ?from))
      (not (location-blocked ?to))
    )
    :effect (and
      (not (at ?v ?from))
      (at ?v ?to)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-road_0052-tw_00330
    :parameters (?v - vehicle ?r - road_type_road_0052 ?from - loc_type_loc_0034 ?to - loc_type_loc_0055 ?w - window_type_tw_00330)
    :precondition (and
      (at ?v ?from)
      (connects ?r ?from ?to)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
      (not (location-blocked ?from))
      (not (location-blocked ?to))
    )
    :effect (and
      (not (at ?v ?from))
      (at ?v ?to)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-road_0052-tw_00360
    :parameters (?v - vehicle ?r - road_type_road_0052 ?from - loc_type_loc_0034 ?to - loc_type_loc_0055 ?w - window_type_tw_00360)
    :precondition (and
      (at ?v ?from)
      (connects ?r ?from ?to)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
      (not (location-blocked ?from))
      (not (location-blocked ?to))
    )
    :effect (and
      (not (at ?v ?from))
      (at ?v ?to)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-road_0052-tw_00390
    :parameters (?v - vehicle ?r - road_type_road_0052 ?from - loc_type_loc_0034 ?to - loc_type_loc_0055 ?w - window_type_tw_00390)
    :precondition (and
      (at ?v ?from)
      (connects ?r ?from ?to)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
      (not (location-blocked ?from))
      (not (location-blocked ?to))
    )
    :effect (and
      (not (at ?v ?from))
      (at ?v ?to)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-road_0052-tw_00420
    :parameters (?v - vehicle ?r - road_type_road_0052 ?from - loc_type_loc_0034 ?to - loc_type_loc_0055 ?w - window_type_tw_00420)
    :precondition (and
      (at ?v ?from)
      (connects ?r ?from ?to)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
      (not (location-blocked ?from))
      (not (location-blocked ?to))
    )
    :effect (and
      (not (at ?v ?from))
      (at ?v ?to)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-road_0052-tw_00450
    :parameters (?v - vehicle ?r - road_type_road_0052 ?from - loc_type_loc_0034 ?to - loc_type_loc_0055 ?w - window_type_tw_00450)
    :precondition (and
      (at ?v ?from)
      (connects ?r ?from ?to)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
      (not (location-blocked ?from))
      (not (location-blocked ?to))
    )
    :effect (and
      (not (at ?v ?from))
      (at ?v ?to)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-road_0054-tw_00000
    :parameters (?v - vehicle ?r - road_type_road_0054 ?from - loc_type_loc_0037 ?to - loc_type_loc_0052 ?w - window_type_tw_00000)
    :precondition (and
      (at ?v ?from)
      (connects ?r ?from ?to)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
      (not (location-blocked ?from))
      (not (location-blocked ?to))
    )
    :effect (and
      (not (at ?v ?from))
      (at ?v ?to)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-road_0054-tw_00030
    :parameters (?v - vehicle ?r - road_type_road_0054 ?from - loc_type_loc_0037 ?to - loc_type_loc_0052 ?w - window_type_tw_00030)
    :precondition (and
      (at ?v ?from)
      (connects ?r ?from ?to)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
      (not (location-blocked ?from))
      (not (location-blocked ?to))
    )
    :effect (and
      (not (at ?v ?from))
      (at ?v ?to)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-road_0054-tw_00060
    :parameters (?v - vehicle ?r - road_type_road_0054 ?from - loc_type_loc_0037 ?to - loc_type_loc_0052 ?w - window_type_tw_00060)
    :precondition (and
      (at ?v ?from)
      (connects ?r ?from ?to)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
      (not (location-blocked ?from))
      (not (location-blocked ?to))
    )
    :effect (and
      (not (at ?v ?from))
      (at ?v ?to)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-road_0054-tw_00090
    :parameters (?v - vehicle ?r - road_type_road_0054 ?from - loc_type_loc_0037 ?to - loc_type_loc_0052 ?w - window_type_tw_00090)
    :precondition (and
      (at ?v ?from)
      (connects ?r ?from ?to)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
      (not (location-blocked ?from))
      (not (location-blocked ?to))
    )
    :effect (and
      (not (at ?v ?from))
      (at ?v ?to)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-road_0054-tw_00120
    :parameters (?v - vehicle ?r - road_type_road_0054 ?from - loc_type_loc_0037 ?to - loc_type_loc_0052 ?w - window_type_tw_00120)
    :precondition (and
      (at ?v ?from)
      (connects ?r ?from ?to)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
      (not (location-blocked ?from))
      (not (location-blocked ?to))
    )
    :effect (and
      (not (at ?v ?from))
      (at ?v ?to)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-road_0054-tw_00150
    :parameters (?v - vehicle ?r - road_type_road_0054 ?from - loc_type_loc_0037 ?to - loc_type_loc_0052 ?w - window_type_tw_00150)
    :precondition (and
      (at ?v ?from)
      (connects ?r ?from ?to)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
      (not (location-blocked ?from))
      (not (location-blocked ?to))
    )
    :effect (and
      (not (at ?v ?from))
      (at ?v ?to)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-road_0054-tw_00180
    :parameters (?v - vehicle ?r - road_type_road_0054 ?from - loc_type_loc_0037 ?to - loc_type_loc_0052 ?w - window_type_tw_00180)
    :precondition (and
      (at ?v ?from)
      (connects ?r ?from ?to)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
      (not (location-blocked ?from))
      (not (location-blocked ?to))
    )
    :effect (and
      (not (at ?v ?from))
      (at ?v ?to)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-road_0054-tw_00210
    :parameters (?v - vehicle ?r - road_type_road_0054 ?from - loc_type_loc_0037 ?to - loc_type_loc_0052 ?w - window_type_tw_00210)
    :precondition (and
      (at ?v ?from)
      (connects ?r ?from ?to)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
      (not (location-blocked ?from))
      (not (location-blocked ?to))
    )
    :effect (and
      (not (at ?v ?from))
      (at ?v ?to)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-road_0054-tw_00240
    :parameters (?v - vehicle ?r - road_type_road_0054 ?from - loc_type_loc_0037 ?to - loc_type_loc_0052 ?w - window_type_tw_00240)
    :precondition (and
      (at ?v ?from)
      (connects ?r ?from ?to)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
      (not (location-blocked ?from))
      (not (location-blocked ?to))
    )
    :effect (and
      (not (at ?v ?from))
      (at ?v ?to)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-road_0054-tw_00270
    :parameters (?v - vehicle ?r - road_type_road_0054 ?from - loc_type_loc_0037 ?to - loc_type_loc_0052 ?w - window_type_tw_00270)
    :precondition (and
      (at ?v ?from)
      (connects ?r ?from ?to)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
      (not (location-blocked ?from))
      (not (location-blocked ?to))
    )
    :effect (and
      (not (at ?v ?from))
      (at ?v ?to)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-road_0054-tw_00300
    :parameters (?v - vehicle ?r - road_type_road_0054 ?from - loc_type_loc_0037 ?to - loc_type_loc_0052 ?w - window_type_tw_00300)
    :precondition (and
      (at ?v ?from)
      (connects ?r ?from ?to)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
      (not (location-blocked ?from))
      (not (location-blocked ?to))
    )
    :effect (and
      (not (at ?v ?from))
      (at ?v ?to)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-road_0054-tw_00330
    :parameters (?v - vehicle ?r - road_type_road_0054 ?from - loc_type_loc_0037 ?to - loc_type_loc_0052 ?w - window_type_tw_00330)
    :precondition (and
      (at ?v ?from)
      (connects ?r ?from ?to)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
      (not (location-blocked ?from))
      (not (location-blocked ?to))
    )
    :effect (and
      (not (at ?v ?from))
      (at ?v ?to)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-road_0054-tw_00360
    :parameters (?v - vehicle ?r - road_type_road_0054 ?from - loc_type_loc_0037 ?to - loc_type_loc_0052 ?w - window_type_tw_00360)
    :precondition (and
      (at ?v ?from)
      (connects ?r ?from ?to)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
      (not (location-blocked ?from))
      (not (location-blocked ?to))
    )
    :effect (and
      (not (at ?v ?from))
      (at ?v ?to)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-road_0054-tw_00390
    :parameters (?v - vehicle ?r - road_type_road_0054 ?from - loc_type_loc_0037 ?to - loc_type_loc_0052 ?w - window_type_tw_00390)
    :precondition (and
      (at ?v ?from)
      (connects ?r ?from ?to)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
      (not (location-blocked ?from))
      (not (location-blocked ?to))
    )
    :effect (and
      (not (at ?v ?from))
      (at ?v ?to)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-road_0054-tw_00420
    :parameters (?v - vehicle ?r - road_type_road_0054 ?from - loc_type_loc_0037 ?to - loc_type_loc_0052 ?w - window_type_tw_00420)
    :precondition (and
      (at ?v ?from)
      (connects ?r ?from ?to)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
      (not (location-blocked ?from))
      (not (location-blocked ?to))
    )
    :effect (and
      (not (at ?v ?from))
      (at ?v ?to)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-road_0054-tw_00450
    :parameters (?v - vehicle ?r - road_type_road_0054 ?from - loc_type_loc_0037 ?to - loc_type_loc_0052 ?w - window_type_tw_00450)
    :precondition (and
      (at ?v ?from)
      (connects ?r ?from ?to)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
      (not (location-blocked ?from))
      (not (location-blocked ?to))
    )
    :effect (and
      (not (at ?v ?from))
      (at ?v ?to)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-road_0056-tw_00000
    :parameters (?v - vehicle ?r - road_type_road_0056 ?from - loc_type_loc_0038 ?to - loc_type_loc_0066 ?w - window_type_tw_00000)
    :precondition (and
      (at ?v ?from)
      (connects ?r ?from ?to)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
      (not (location-blocked ?from))
      (not (location-blocked ?to))
    )
    :effect (and
      (not (at ?v ?from))
      (at ?v ?to)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-road_0056-tw_00030
    :parameters (?v - vehicle ?r - road_type_road_0056 ?from - loc_type_loc_0038 ?to - loc_type_loc_0066 ?w - window_type_tw_00030)
    :precondition (and
      (at ?v ?from)
      (connects ?r ?from ?to)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
      (not (location-blocked ?from))
      (not (location-blocked ?to))
    )
    :effect (and
      (not (at ?v ?from))
      (at ?v ?to)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-road_0056-tw_00060
    :parameters (?v - vehicle ?r - road_type_road_0056 ?from - loc_type_loc_0038 ?to - loc_type_loc_0066 ?w - window_type_tw_00060)
    :precondition (and
      (at ?v ?from)
      (connects ?r ?from ?to)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
      (not (location-blocked ?from))
      (not (location-blocked ?to))
    )
    :effect (and
      (not (at ?v ?from))
      (at ?v ?to)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-road_0056-tw_00090
    :parameters (?v - vehicle ?r - road_type_road_0056 ?from - loc_type_loc_0038 ?to - loc_type_loc_0066 ?w - window_type_tw_00090)
    :precondition (and
      (at ?v ?from)
      (connects ?r ?from ?to)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
      (not (location-blocked ?from))
      (not (location-blocked ?to))
    )
    :effect (and
      (not (at ?v ?from))
      (at ?v ?to)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-road_0056-tw_00120
    :parameters (?v - vehicle ?r - road_type_road_0056 ?from - loc_type_loc_0038 ?to - loc_type_loc_0066 ?w - window_type_tw_00120)
    :precondition (and
      (at ?v ?from)
      (connects ?r ?from ?to)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
      (not (location-blocked ?from))
      (not (location-blocked ?to))
    )
    :effect (and
      (not (at ?v ?from))
      (at ?v ?to)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-road_0056-tw_00150
    :parameters (?v - vehicle ?r - road_type_road_0056 ?from - loc_type_loc_0038 ?to - loc_type_loc_0066 ?w - window_type_tw_00150)
    :precondition (and
      (at ?v ?from)
      (connects ?r ?from ?to)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
      (not (location-blocked ?from))
      (not (location-blocked ?to))
    )
    :effect (and
      (not (at ?v ?from))
      (at ?v ?to)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-road_0056-tw_00180
    :parameters (?v - vehicle ?r - road_type_road_0056 ?from - loc_type_loc_0038 ?to - loc_type_loc_0066 ?w - window_type_tw_00180)
    :precondition (and
      (at ?v ?from)
      (connects ?r ?from ?to)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
      (not (location-blocked ?from))
      (not (location-blocked ?to))
    )
    :effect (and
      (not (at ?v ?from))
      (at ?v ?to)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-road_0056-tw_00210
    :parameters (?v - vehicle ?r - road_type_road_0056 ?from - loc_type_loc_0038 ?to - loc_type_loc_0066 ?w - window_type_tw_00210)
    :precondition (and
      (at ?v ?from)
      (connects ?r ?from ?to)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
      (not (location-blocked ?from))
      (not (location-blocked ?to))
    )
    :effect (and
      (not (at ?v ?from))
      (at ?v ?to)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-road_0056-tw_00240
    :parameters (?v - vehicle ?r - road_type_road_0056 ?from - loc_type_loc_0038 ?to - loc_type_loc_0066 ?w - window_type_tw_00240)
    :precondition (and
      (at ?v ?from)
      (connects ?r ?from ?to)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
      (not (location-blocked ?from))
      (not (location-blocked ?to))
    )
    :effect (and
      (not (at ?v ?from))
      (at ?v ?to)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-road_0056-tw_00270
    :parameters (?v - vehicle ?r - road_type_road_0056 ?from - loc_type_loc_0038 ?to - loc_type_loc_0066 ?w - window_type_tw_00270)
    :precondition (and
      (at ?v ?from)
      (connects ?r ?from ?to)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
      (not (location-blocked ?from))
      (not (location-blocked ?to))
    )
    :effect (and
      (not (at ?v ?from))
      (at ?v ?to)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-road_0056-tw_00300
    :parameters (?v - vehicle ?r - road_type_road_0056 ?from - loc_type_loc_0038 ?to - loc_type_loc_0066 ?w - window_type_tw_00300)
    :precondition (and
      (at ?v ?from)
      (connects ?r ?from ?to)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
      (not (location-blocked ?from))
      (not (location-blocked ?to))
    )
    :effect (and
      (not (at ?v ?from))
      (at ?v ?to)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-road_0056-tw_00330
    :parameters (?v - vehicle ?r - road_type_road_0056 ?from - loc_type_loc_0038 ?to - loc_type_loc_0066 ?w - window_type_tw_00330)
    :precondition (and
      (at ?v ?from)
      (connects ?r ?from ?to)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
      (not (location-blocked ?from))
      (not (location-blocked ?to))
    )
    :effect (and
      (not (at ?v ?from))
      (at ?v ?to)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-road_0056-tw_00360
    :parameters (?v - vehicle ?r - road_type_road_0056 ?from - loc_type_loc_0038 ?to - loc_type_loc_0066 ?w - window_type_tw_00360)
    :precondition (and
      (at ?v ?from)
      (connects ?r ?from ?to)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
      (not (location-blocked ?from))
      (not (location-blocked ?to))
    )
    :effect (and
      (not (at ?v ?from))
      (at ?v ?to)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-road_0056-tw_00390
    :parameters (?v - vehicle ?r - road_type_road_0056 ?from - loc_type_loc_0038 ?to - loc_type_loc_0066 ?w - window_type_tw_00390)
    :precondition (and
      (at ?v ?from)
      (connects ?r ?from ?to)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
      (not (location-blocked ?from))
      (not (location-blocked ?to))
    )
    :effect (and
      (not (at ?v ?from))
      (at ?v ?to)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-road_0056-tw_00420
    :parameters (?v - vehicle ?r - road_type_road_0056 ?from - loc_type_loc_0038 ?to - loc_type_loc_0066 ?w - window_type_tw_00420)
    :precondition (and
      (at ?v ?from)
      (connects ?r ?from ?to)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
      (not (location-blocked ?from))
      (not (location-blocked ?to))
    )
    :effect (and
      (not (at ?v ?from))
      (at ?v ?to)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-road_0056-tw_00450
    :parameters (?v - vehicle ?r - road_type_road_0056 ?from - loc_type_loc_0038 ?to - loc_type_loc_0066 ?w - window_type_tw_00450)
    :precondition (and
      (at ?v ?from)
      (connects ?r ?from ?to)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
      (not (location-blocked ?from))
      (not (location-blocked ?to))
    )
    :effect (and
      (not (at ?v ?from))
      (at ?v ?to)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-road_0057-tw_00000
    :parameters (?v - vehicle ?r - road_type_road_0057 ?from - loc_type_loc_0038 ?to - loc_type_loc_0077 ?w - window_type_tw_00000)
    :precondition (and
      (at ?v ?from)
      (connects ?r ?from ?to)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
      (not (location-blocked ?from))
      (not (location-blocked ?to))
    )
    :effect (and
      (not (at ?v ?from))
      (at ?v ?to)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-road_0057-tw_00030
    :parameters (?v - vehicle ?r - road_type_road_0057 ?from - loc_type_loc_0038 ?to - loc_type_loc_0077 ?w - window_type_tw_00030)
    :precondition (and
      (at ?v ?from)
      (connects ?r ?from ?to)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
      (not (location-blocked ?from))
      (not (location-blocked ?to))
    )
    :effect (and
      (not (at ?v ?from))
      (at ?v ?to)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-road_0057-tw_00060
    :parameters (?v - vehicle ?r - road_type_road_0057 ?from - loc_type_loc_0038 ?to - loc_type_loc_0077 ?w - window_type_tw_00060)
    :precondition (and
      (at ?v ?from)
      (connects ?r ?from ?to)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
      (not (location-blocked ?from))
      (not (location-blocked ?to))
    )
    :effect (and
      (not (at ?v ?from))
      (at ?v ?to)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-road_0057-tw_00090
    :parameters (?v - vehicle ?r - road_type_road_0057 ?from - loc_type_loc_0038 ?to - loc_type_loc_0077 ?w - window_type_tw_00090)
    :precondition (and
      (at ?v ?from)
      (connects ?r ?from ?to)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
      (not (location-blocked ?from))
      (not (location-blocked ?to))
    )
    :effect (and
      (not (at ?v ?from))
      (at ?v ?to)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-road_0057-tw_00120
    :parameters (?v - vehicle ?r - road_type_road_0057 ?from - loc_type_loc_0038 ?to - loc_type_loc_0077 ?w - window_type_tw_00120)
    :precondition (and
      (at ?v ?from)
      (connects ?r ?from ?to)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
      (not (location-blocked ?from))
      (not (location-blocked ?to))
    )
    :effect (and
      (not (at ?v ?from))
      (at ?v ?to)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-road_0057-tw_00150
    :parameters (?v - vehicle ?r - road_type_road_0057 ?from - loc_type_loc_0038 ?to - loc_type_loc_0077 ?w - window_type_tw_00150)
    :precondition (and
      (at ?v ?from)
      (connects ?r ?from ?to)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
      (not (location-blocked ?from))
      (not (location-blocked ?to))
    )
    :effect (and
      (not (at ?v ?from))
      (at ?v ?to)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-road_0057-tw_00180
    :parameters (?v - vehicle ?r - road_type_road_0057 ?from - loc_type_loc_0038 ?to - loc_type_loc_0077 ?w - window_type_tw_00180)
    :precondition (and
      (at ?v ?from)
      (connects ?r ?from ?to)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
      (not (location-blocked ?from))
      (not (location-blocked ?to))
    )
    :effect (and
      (not (at ?v ?from))
      (at ?v ?to)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-road_0057-tw_00210
    :parameters (?v - vehicle ?r - road_type_road_0057 ?from - loc_type_loc_0038 ?to - loc_type_loc_0077 ?w - window_type_tw_00210)
    :precondition (and
      (at ?v ?from)
      (connects ?r ?from ?to)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
      (not (location-blocked ?from))
      (not (location-blocked ?to))
    )
    :effect (and
      (not (at ?v ?from))
      (at ?v ?to)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-road_0057-tw_00240
    :parameters (?v - vehicle ?r - road_type_road_0057 ?from - loc_type_loc_0038 ?to - loc_type_loc_0077 ?w - window_type_tw_00240)
    :precondition (and
      (at ?v ?from)
      (connects ?r ?from ?to)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
      (not (location-blocked ?from))
      (not (location-blocked ?to))
    )
    :effect (and
      (not (at ?v ?from))
      (at ?v ?to)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-road_0057-tw_00270
    :parameters (?v - vehicle ?r - road_type_road_0057 ?from - loc_type_loc_0038 ?to - loc_type_loc_0077 ?w - window_type_tw_00270)
    :precondition (and
      (at ?v ?from)
      (connects ?r ?from ?to)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
      (not (location-blocked ?from))
      (not (location-blocked ?to))
    )
    :effect (and
      (not (at ?v ?from))
      (at ?v ?to)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-road_0057-tw_00300
    :parameters (?v - vehicle ?r - road_type_road_0057 ?from - loc_type_loc_0038 ?to - loc_type_loc_0077 ?w - window_type_tw_00300)
    :precondition (and
      (at ?v ?from)
      (connects ?r ?from ?to)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
      (not (location-blocked ?from))
      (not (location-blocked ?to))
    )
    :effect (and
      (not (at ?v ?from))
      (at ?v ?to)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-road_0057-tw_00330
    :parameters (?v - vehicle ?r - road_type_road_0057 ?from - loc_type_loc_0038 ?to - loc_type_loc_0077 ?w - window_type_tw_00330)
    :precondition (and
      (at ?v ?from)
      (connects ?r ?from ?to)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
      (not (location-blocked ?from))
      (not (location-blocked ?to))
    )
    :effect (and
      (not (at ?v ?from))
      (at ?v ?to)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-road_0057-tw_00360
    :parameters (?v - vehicle ?r - road_type_road_0057 ?from - loc_type_loc_0038 ?to - loc_type_loc_0077 ?w - window_type_tw_00360)
    :precondition (and
      (at ?v ?from)
      (connects ?r ?from ?to)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
      (not (location-blocked ?from))
      (not (location-blocked ?to))
    )
    :effect (and
      (not (at ?v ?from))
      (at ?v ?to)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-road_0057-tw_00390
    :parameters (?v - vehicle ?r - road_type_road_0057 ?from - loc_type_loc_0038 ?to - loc_type_loc_0077 ?w - window_type_tw_00390)
    :precondition (and
      (at ?v ?from)
      (connects ?r ?from ?to)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
      (not (location-blocked ?from))
      (not (location-blocked ?to))
    )
    :effect (and
      (not (at ?v ?from))
      (at ?v ?to)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-road_0057-tw_00420
    :parameters (?v - vehicle ?r - road_type_road_0057 ?from - loc_type_loc_0038 ?to - loc_type_loc_0077 ?w - window_type_tw_00420)
    :precondition (and
      (at ?v ?from)
      (connects ?r ?from ?to)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
      (not (location-blocked ?from))
      (not (location-blocked ?to))
    )
    :effect (and
      (not (at ?v ?from))
      (at ?v ?to)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-road_0057-tw_00450
    :parameters (?v - vehicle ?r - road_type_road_0057 ?from - loc_type_loc_0038 ?to - loc_type_loc_0077 ?w - window_type_tw_00450)
    :precondition (and
      (at ?v ?from)
      (connects ?r ?from ?to)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
      (not (location-blocked ?from))
      (not (location-blocked ?to))
    )
    :effect (and
      (not (at ?v ?from))
      (at ?v ?to)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-road_0058-tw_00000
    :parameters (?v - vehicle ?r - road_type_road_0058 ?from - loc_type_loc_0038 ?to - loc_type_loc_0057 ?w - window_type_tw_00000)
    :precondition (and
      (at ?v ?from)
      (connects ?r ?from ?to)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
      (not (location-blocked ?from))
      (not (location-blocked ?to))
    )
    :effect (and
      (not (at ?v ?from))
      (at ?v ?to)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-road_0058-tw_00030
    :parameters (?v - vehicle ?r - road_type_road_0058 ?from - loc_type_loc_0038 ?to - loc_type_loc_0057 ?w - window_type_tw_00030)
    :precondition (and
      (at ?v ?from)
      (connects ?r ?from ?to)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
      (not (location-blocked ?from))
      (not (location-blocked ?to))
    )
    :effect (and
      (not (at ?v ?from))
      (at ?v ?to)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-road_0058-tw_00060
    :parameters (?v - vehicle ?r - road_type_road_0058 ?from - loc_type_loc_0038 ?to - loc_type_loc_0057 ?w - window_type_tw_00060)
    :precondition (and
      (at ?v ?from)
      (connects ?r ?from ?to)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
      (not (location-blocked ?from))
      (not (location-blocked ?to))
    )
    :effect (and
      (not (at ?v ?from))
      (at ?v ?to)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-road_0058-tw_00090
    :parameters (?v - vehicle ?r - road_type_road_0058 ?from - loc_type_loc_0038 ?to - loc_type_loc_0057 ?w - window_type_tw_00090)
    :precondition (and
      (at ?v ?from)
      (connects ?r ?from ?to)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
      (not (location-blocked ?from))
      (not (location-blocked ?to))
    )
    :effect (and
      (not (at ?v ?from))
      (at ?v ?to)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-road_0058-tw_00120
    :parameters (?v - vehicle ?r - road_type_road_0058 ?from - loc_type_loc_0038 ?to - loc_type_loc_0057 ?w - window_type_tw_00120)
    :precondition (and
      (at ?v ?from)
      (connects ?r ?from ?to)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
      (not (location-blocked ?from))
      (not (location-blocked ?to))
    )
    :effect (and
      (not (at ?v ?from))
      (at ?v ?to)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-road_0058-tw_00150
    :parameters (?v - vehicle ?r - road_type_road_0058 ?from - loc_type_loc_0038 ?to - loc_type_loc_0057 ?w - window_type_tw_00150)
    :precondition (and
      (at ?v ?from)
      (connects ?r ?from ?to)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
      (not (location-blocked ?from))
      (not (location-blocked ?to))
    )
    :effect (and
      (not (at ?v ?from))
      (at ?v ?to)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-road_0058-tw_00180
    :parameters (?v - vehicle ?r - road_type_road_0058 ?from - loc_type_loc_0038 ?to - loc_type_loc_0057 ?w - window_type_tw_00180)
    :precondition (and
      (at ?v ?from)
      (connects ?r ?from ?to)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
      (not (location-blocked ?from))
      (not (location-blocked ?to))
    )
    :effect (and
      (not (at ?v ?from))
      (at ?v ?to)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-road_0058-tw_00210
    :parameters (?v - vehicle ?r - road_type_road_0058 ?from - loc_type_loc_0038 ?to - loc_type_loc_0057 ?w - window_type_tw_00210)
    :precondition (and
      (at ?v ?from)
      (connects ?r ?from ?to)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
      (not (location-blocked ?from))
      (not (location-blocked ?to))
    )
    :effect (and
      (not (at ?v ?from))
      (at ?v ?to)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-road_0058-tw_00240
    :parameters (?v - vehicle ?r - road_type_road_0058 ?from - loc_type_loc_0038 ?to - loc_type_loc_0057 ?w - window_type_tw_00240)
    :precondition (and
      (at ?v ?from)
      (connects ?r ?from ?to)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
      (not (location-blocked ?from))
      (not (location-blocked ?to))
    )
    :effect (and
      (not (at ?v ?from))
      (at ?v ?to)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-road_0058-tw_00270
    :parameters (?v - vehicle ?r - road_type_road_0058 ?from - loc_type_loc_0038 ?to - loc_type_loc_0057 ?w - window_type_tw_00270)
    :precondition (and
      (at ?v ?from)
      (connects ?r ?from ?to)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
      (not (location-blocked ?from))
      (not (location-blocked ?to))
    )
    :effect (and
      (not (at ?v ?from))
      (at ?v ?to)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-road_0058-tw_00300
    :parameters (?v - vehicle ?r - road_type_road_0058 ?from - loc_type_loc_0038 ?to - loc_type_loc_0057 ?w - window_type_tw_00300)
    :precondition (and
      (at ?v ?from)
      (connects ?r ?from ?to)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
      (not (location-blocked ?from))
      (not (location-blocked ?to))
    )
    :effect (and
      (not (at ?v ?from))
      (at ?v ?to)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-road_0058-tw_00330
    :parameters (?v - vehicle ?r - road_type_road_0058 ?from - loc_type_loc_0038 ?to - loc_type_loc_0057 ?w - window_type_tw_00330)
    :precondition (and
      (at ?v ?from)
      (connects ?r ?from ?to)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
      (not (location-blocked ?from))
      (not (location-blocked ?to))
    )
    :effect (and
      (not (at ?v ?from))
      (at ?v ?to)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-road_0058-tw_00360
    :parameters (?v - vehicle ?r - road_type_road_0058 ?from - loc_type_loc_0038 ?to - loc_type_loc_0057 ?w - window_type_tw_00360)
    :precondition (and
      (at ?v ?from)
      (connects ?r ?from ?to)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
      (not (location-blocked ?from))
      (not (location-blocked ?to))
    )
    :effect (and
      (not (at ?v ?from))
      (at ?v ?to)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-road_0058-tw_00390
    :parameters (?v - vehicle ?r - road_type_road_0058 ?from - loc_type_loc_0038 ?to - loc_type_loc_0057 ?w - window_type_tw_00390)
    :precondition (and
      (at ?v ?from)
      (connects ?r ?from ?to)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
      (not (location-blocked ?from))
      (not (location-blocked ?to))
    )
    :effect (and
      (not (at ?v ?from))
      (at ?v ?to)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-road_0058-tw_00420
    :parameters (?v - vehicle ?r - road_type_road_0058 ?from - loc_type_loc_0038 ?to - loc_type_loc_0057 ?w - window_type_tw_00420)
    :precondition (and
      (at ?v ?from)
      (connects ?r ?from ?to)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
      (not (location-blocked ?from))
      (not (location-blocked ?to))
    )
    :effect (and
      (not (at ?v ?from))
      (at ?v ?to)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-road_0058-tw_00450
    :parameters (?v - vehicle ?r - road_type_road_0058 ?from - loc_type_loc_0038 ?to - loc_type_loc_0057 ?w - window_type_tw_00450)
    :precondition (and
      (at ?v ?from)
      (connects ?r ?from ?to)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
      (not (location-blocked ?from))
      (not (location-blocked ?to))
    )
    :effect (and
      (not (at ?v ?from))
      (at ?v ?to)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-road_0059-tw_00000
    :parameters (?v - vehicle ?r - road_type_road_0059 ?from - loc_type_loc_0039 ?to - loc_type_loc_0073 ?w - window_type_tw_00000)
    :precondition (and
      (at ?v ?from)
      (connects ?r ?from ?to)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
      (not (location-blocked ?from))
      (not (location-blocked ?to))
    )
    :effect (and
      (not (at ?v ?from))
      (at ?v ?to)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-road_0059-tw_00030
    :parameters (?v - vehicle ?r - road_type_road_0059 ?from - loc_type_loc_0039 ?to - loc_type_loc_0073 ?w - window_type_tw_00030)
    :precondition (and
      (at ?v ?from)
      (connects ?r ?from ?to)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
      (not (location-blocked ?from))
      (not (location-blocked ?to))
    )
    :effect (and
      (not (at ?v ?from))
      (at ?v ?to)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-road_0059-tw_00060
    :parameters (?v - vehicle ?r - road_type_road_0059 ?from - loc_type_loc_0039 ?to - loc_type_loc_0073 ?w - window_type_tw_00060)
    :precondition (and
      (at ?v ?from)
      (connects ?r ?from ?to)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
      (not (location-blocked ?from))
      (not (location-blocked ?to))
    )
    :effect (and
      (not (at ?v ?from))
      (at ?v ?to)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-road_0059-tw_00090
    :parameters (?v - vehicle ?r - road_type_road_0059 ?from - loc_type_loc_0039 ?to - loc_type_loc_0073 ?w - window_type_tw_00090)
    :precondition (and
      (at ?v ?from)
      (connects ?r ?from ?to)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
      (not (location-blocked ?from))
      (not (location-blocked ?to))
    )
    :effect (and
      (not (at ?v ?from))
      (at ?v ?to)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-road_0059-tw_00120
    :parameters (?v - vehicle ?r - road_type_road_0059 ?from - loc_type_loc_0039 ?to - loc_type_loc_0073 ?w - window_type_tw_00120)
    :precondition (and
      (at ?v ?from)
      (connects ?r ?from ?to)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
      (not (location-blocked ?from))
      (not (location-blocked ?to))
    )
    :effect (and
      (not (at ?v ?from))
      (at ?v ?to)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-road_0059-tw_00150
    :parameters (?v - vehicle ?r - road_type_road_0059 ?from - loc_type_loc_0039 ?to - loc_type_loc_0073 ?w - window_type_tw_00150)
    :precondition (and
      (at ?v ?from)
      (connects ?r ?from ?to)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
      (not (location-blocked ?from))
      (not (location-blocked ?to))
    )
    :effect (and
      (not (at ?v ?from))
      (at ?v ?to)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-road_0059-tw_00180
    :parameters (?v - vehicle ?r - road_type_road_0059 ?from - loc_type_loc_0039 ?to - loc_type_loc_0073 ?w - window_type_tw_00180)
    :precondition (and
      (at ?v ?from)
      (connects ?r ?from ?to)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
      (not (location-blocked ?from))
      (not (location-blocked ?to))
    )
    :effect (and
      (not (at ?v ?from))
      (at ?v ?to)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-road_0059-tw_00210
    :parameters (?v - vehicle ?r - road_type_road_0059 ?from - loc_type_loc_0039 ?to - loc_type_loc_0073 ?w - window_type_tw_00210)
    :precondition (and
      (at ?v ?from)
      (connects ?r ?from ?to)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
      (not (location-blocked ?from))
      (not (location-blocked ?to))
    )
    :effect (and
      (not (at ?v ?from))
      (at ?v ?to)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-road_0059-tw_00240
    :parameters (?v - vehicle ?r - road_type_road_0059 ?from - loc_type_loc_0039 ?to - loc_type_loc_0073 ?w - window_type_tw_00240)
    :precondition (and
      (at ?v ?from)
      (connects ?r ?from ?to)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
      (not (location-blocked ?from))
      (not (location-blocked ?to))
    )
    :effect (and
      (not (at ?v ?from))
      (at ?v ?to)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-road_0059-tw_00270
    :parameters (?v - vehicle ?r - road_type_road_0059 ?from - loc_type_loc_0039 ?to - loc_type_loc_0073 ?w - window_type_tw_00270)
    :precondition (and
      (at ?v ?from)
      (connects ?r ?from ?to)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
      (not (location-blocked ?from))
      (not (location-blocked ?to))
    )
    :effect (and
      (not (at ?v ?from))
      (at ?v ?to)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-road_0059-tw_00300
    :parameters (?v - vehicle ?r - road_type_road_0059 ?from - loc_type_loc_0039 ?to - loc_type_loc_0073 ?w - window_type_tw_00300)
    :precondition (and
      (at ?v ?from)
      (connects ?r ?from ?to)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
      (not (location-blocked ?from))
      (not (location-blocked ?to))
    )
    :effect (and
      (not (at ?v ?from))
      (at ?v ?to)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-road_0059-tw_00330
    :parameters (?v - vehicle ?r - road_type_road_0059 ?from - loc_type_loc_0039 ?to - loc_type_loc_0073 ?w - window_type_tw_00330)
    :precondition (and
      (at ?v ?from)
      (connects ?r ?from ?to)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
      (not (location-blocked ?from))
      (not (location-blocked ?to))
    )
    :effect (and
      (not (at ?v ?from))
      (at ?v ?to)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-road_0059-tw_00360
    :parameters (?v - vehicle ?r - road_type_road_0059 ?from - loc_type_loc_0039 ?to - loc_type_loc_0073 ?w - window_type_tw_00360)
    :precondition (and
      (at ?v ?from)
      (connects ?r ?from ?to)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
      (not (location-blocked ?from))
      (not (location-blocked ?to))
    )
    :effect (and
      (not (at ?v ?from))
      (at ?v ?to)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-road_0059-tw_00390
    :parameters (?v - vehicle ?r - road_type_road_0059 ?from - loc_type_loc_0039 ?to - loc_type_loc_0073 ?w - window_type_tw_00390)
    :precondition (and
      (at ?v ?from)
      (connects ?r ?from ?to)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
      (not (location-blocked ?from))
      (not (location-blocked ?to))
    )
    :effect (and
      (not (at ?v ?from))
      (at ?v ?to)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-road_0059-tw_00420
    :parameters (?v - vehicle ?r - road_type_road_0059 ?from - loc_type_loc_0039 ?to - loc_type_loc_0073 ?w - window_type_tw_00420)
    :precondition (and
      (at ?v ?from)
      (connects ?r ?from ?to)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
      (not (location-blocked ?from))
      (not (location-blocked ?to))
    )
    :effect (and
      (not (at ?v ?from))
      (at ?v ?to)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-road_0059-tw_00450
    :parameters (?v - vehicle ?r - road_type_road_0059 ?from - loc_type_loc_0039 ?to - loc_type_loc_0073 ?w - window_type_tw_00450)
    :precondition (and
      (at ?v ?from)
      (connects ?r ?from ?to)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
      (not (location-blocked ?from))
      (not (location-blocked ?to))
    )
    :effect (and
      (not (at ?v ?from))
      (at ?v ?to)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-road_0060-tw_00000
    :parameters (?v - vehicle ?r - road_type_road_0060 ?from - loc_type_loc_0041 ?to - loc_type_loc_0063 ?w - window_type_tw_00000)
    :precondition (and
      (at ?v ?from)
      (connects ?r ?from ?to)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
      (not (location-blocked ?from))
      (not (location-blocked ?to))
    )
    :effect (and
      (not (at ?v ?from))
      (at ?v ?to)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-road_0060-tw_00030
    :parameters (?v - vehicle ?r - road_type_road_0060 ?from - loc_type_loc_0041 ?to - loc_type_loc_0063 ?w - window_type_tw_00030)
    :precondition (and
      (at ?v ?from)
      (connects ?r ?from ?to)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
      (not (location-blocked ?from))
      (not (location-blocked ?to))
    )
    :effect (and
      (not (at ?v ?from))
      (at ?v ?to)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-road_0060-tw_00060
    :parameters (?v - vehicle ?r - road_type_road_0060 ?from - loc_type_loc_0041 ?to - loc_type_loc_0063 ?w - window_type_tw_00060)
    :precondition (and
      (at ?v ?from)
      (connects ?r ?from ?to)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
      (not (location-blocked ?from))
      (not (location-blocked ?to))
    )
    :effect (and
      (not (at ?v ?from))
      (at ?v ?to)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-road_0060-tw_00090
    :parameters (?v - vehicle ?r - road_type_road_0060 ?from - loc_type_loc_0041 ?to - loc_type_loc_0063 ?w - window_type_tw_00090)
    :precondition (and
      (at ?v ?from)
      (connects ?r ?from ?to)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
      (not (location-blocked ?from))
      (not (location-blocked ?to))
    )
    :effect (and
      (not (at ?v ?from))
      (at ?v ?to)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-road_0060-tw_00120
    :parameters (?v - vehicle ?r - road_type_road_0060 ?from - loc_type_loc_0041 ?to - loc_type_loc_0063 ?w - window_type_tw_00120)
    :precondition (and
      (at ?v ?from)
      (connects ?r ?from ?to)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
      (not (location-blocked ?from))
      (not (location-blocked ?to))
    )
    :effect (and
      (not (at ?v ?from))
      (at ?v ?to)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-road_0060-tw_00150
    :parameters (?v - vehicle ?r - road_type_road_0060 ?from - loc_type_loc_0041 ?to - loc_type_loc_0063 ?w - window_type_tw_00150)
    :precondition (and
      (at ?v ?from)
      (connects ?r ?from ?to)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
      (not (location-blocked ?from))
      (not (location-blocked ?to))
    )
    :effect (and
      (not (at ?v ?from))
      (at ?v ?to)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-road_0060-tw_00180
    :parameters (?v - vehicle ?r - road_type_road_0060 ?from - loc_type_loc_0041 ?to - loc_type_loc_0063 ?w - window_type_tw_00180)
    :precondition (and
      (at ?v ?from)
      (connects ?r ?from ?to)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
      (not (location-blocked ?from))
      (not (location-blocked ?to))
    )
    :effect (and
      (not (at ?v ?from))
      (at ?v ?to)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-road_0060-tw_00210
    :parameters (?v - vehicle ?r - road_type_road_0060 ?from - loc_type_loc_0041 ?to - loc_type_loc_0063 ?w - window_type_tw_00210)
    :precondition (and
      (at ?v ?from)
      (connects ?r ?from ?to)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
      (not (location-blocked ?from))
      (not (location-blocked ?to))
    )
    :effect (and
      (not (at ?v ?from))
      (at ?v ?to)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-road_0060-tw_00240
    :parameters (?v - vehicle ?r - road_type_road_0060 ?from - loc_type_loc_0041 ?to - loc_type_loc_0063 ?w - window_type_tw_00240)
    :precondition (and
      (at ?v ?from)
      (connects ?r ?from ?to)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
      (not (location-blocked ?from))
      (not (location-blocked ?to))
    )
    :effect (and
      (not (at ?v ?from))
      (at ?v ?to)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-road_0060-tw_00270
    :parameters (?v - vehicle ?r - road_type_road_0060 ?from - loc_type_loc_0041 ?to - loc_type_loc_0063 ?w - window_type_tw_00270)
    :precondition (and
      (at ?v ?from)
      (connects ?r ?from ?to)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
      (not (location-blocked ?from))
      (not (location-blocked ?to))
    )
    :effect (and
      (not (at ?v ?from))
      (at ?v ?to)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-road_0060-tw_00300
    :parameters (?v - vehicle ?r - road_type_road_0060 ?from - loc_type_loc_0041 ?to - loc_type_loc_0063 ?w - window_type_tw_00300)
    :precondition (and
      (at ?v ?from)
      (connects ?r ?from ?to)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
      (not (location-blocked ?from))
      (not (location-blocked ?to))
    )
    :effect (and
      (not (at ?v ?from))
      (at ?v ?to)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-road_0060-tw_00330
    :parameters (?v - vehicle ?r - road_type_road_0060 ?from - loc_type_loc_0041 ?to - loc_type_loc_0063 ?w - window_type_tw_00330)
    :precondition (and
      (at ?v ?from)
      (connects ?r ?from ?to)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
      (not (location-blocked ?from))
      (not (location-blocked ?to))
    )
    :effect (and
      (not (at ?v ?from))
      (at ?v ?to)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-road_0060-tw_00360
    :parameters (?v - vehicle ?r - road_type_road_0060 ?from - loc_type_loc_0041 ?to - loc_type_loc_0063 ?w - window_type_tw_00360)
    :precondition (and
      (at ?v ?from)
      (connects ?r ?from ?to)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
      (not (location-blocked ?from))
      (not (location-blocked ?to))
    )
    :effect (and
      (not (at ?v ?from))
      (at ?v ?to)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-road_0060-tw_00390
    :parameters (?v - vehicle ?r - road_type_road_0060 ?from - loc_type_loc_0041 ?to - loc_type_loc_0063 ?w - window_type_tw_00390)
    :precondition (and
      (at ?v ?from)
      (connects ?r ?from ?to)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
      (not (location-blocked ?from))
      (not (location-blocked ?to))
    )
    :effect (and
      (not (at ?v ?from))
      (at ?v ?to)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-road_0060-tw_00420
    :parameters (?v - vehicle ?r - road_type_road_0060 ?from - loc_type_loc_0041 ?to - loc_type_loc_0063 ?w - window_type_tw_00420)
    :precondition (and
      (at ?v ?from)
      (connects ?r ?from ?to)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
      (not (location-blocked ?from))
      (not (location-blocked ?to))
    )
    :effect (and
      (not (at ?v ?from))
      (at ?v ?to)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-road_0060-tw_00450
    :parameters (?v - vehicle ?r - road_type_road_0060 ?from - loc_type_loc_0041 ?to - loc_type_loc_0063 ?w - window_type_tw_00450)
    :precondition (and
      (at ?v ?from)
      (connects ?r ?from ?to)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
      (not (location-blocked ?from))
      (not (location-blocked ?to))
    )
    :effect (and
      (not (at ?v ?from))
      (at ?v ?to)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-road_0061-tw_00000
    :parameters (?v - vehicle ?r - road_type_road_0061 ?from - loc_type_loc_0041 ?to - loc_type_loc_0045 ?w - window_type_tw_00000)
    :precondition (and
      (at ?v ?from)
      (connects ?r ?from ?to)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
      (not (location-blocked ?from))
      (not (location-blocked ?to))
    )
    :effect (and
      (not (at ?v ?from))
      (at ?v ?to)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-road_0061-tw_00030
    :parameters (?v - vehicle ?r - road_type_road_0061 ?from - loc_type_loc_0041 ?to - loc_type_loc_0045 ?w - window_type_tw_00030)
    :precondition (and
      (at ?v ?from)
      (connects ?r ?from ?to)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
      (not (location-blocked ?from))
      (not (location-blocked ?to))
    )
    :effect (and
      (not (at ?v ?from))
      (at ?v ?to)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-road_0061-tw_00060
    :parameters (?v - vehicle ?r - road_type_road_0061 ?from - loc_type_loc_0041 ?to - loc_type_loc_0045 ?w - window_type_tw_00060)
    :precondition (and
      (at ?v ?from)
      (connects ?r ?from ?to)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
      (not (location-blocked ?from))
      (not (location-blocked ?to))
    )
    :effect (and
      (not (at ?v ?from))
      (at ?v ?to)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-road_0061-tw_00090
    :parameters (?v - vehicle ?r - road_type_road_0061 ?from - loc_type_loc_0041 ?to - loc_type_loc_0045 ?w - window_type_tw_00090)
    :precondition (and
      (at ?v ?from)
      (connects ?r ?from ?to)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
      (not (location-blocked ?from))
      (not (location-blocked ?to))
    )
    :effect (and
      (not (at ?v ?from))
      (at ?v ?to)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-road_0061-tw_00120
    :parameters (?v - vehicle ?r - road_type_road_0061 ?from - loc_type_loc_0041 ?to - loc_type_loc_0045 ?w - window_type_tw_00120)
    :precondition (and
      (at ?v ?from)
      (connects ?r ?from ?to)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
      (not (location-blocked ?from))
      (not (location-blocked ?to))
    )
    :effect (and
      (not (at ?v ?from))
      (at ?v ?to)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-road_0061-tw_00150
    :parameters (?v - vehicle ?r - road_type_road_0061 ?from - loc_type_loc_0041 ?to - loc_type_loc_0045 ?w - window_type_tw_00150)
    :precondition (and
      (at ?v ?from)
      (connects ?r ?from ?to)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
      (not (location-blocked ?from))
      (not (location-blocked ?to))
    )
    :effect (and
      (not (at ?v ?from))
      (at ?v ?to)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-road_0061-tw_00180
    :parameters (?v - vehicle ?r - road_type_road_0061 ?from - loc_type_loc_0041 ?to - loc_type_loc_0045 ?w - window_type_tw_00180)
    :precondition (and
      (at ?v ?from)
      (connects ?r ?from ?to)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
      (not (location-blocked ?from))
      (not (location-blocked ?to))
    )
    :effect (and
      (not (at ?v ?from))
      (at ?v ?to)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-road_0061-tw_00210
    :parameters (?v - vehicle ?r - road_type_road_0061 ?from - loc_type_loc_0041 ?to - loc_type_loc_0045 ?w - window_type_tw_00210)
    :precondition (and
      (at ?v ?from)
      (connects ?r ?from ?to)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
      (not (location-blocked ?from))
      (not (location-blocked ?to))
    )
    :effect (and
      (not (at ?v ?from))
      (at ?v ?to)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-road_0061-tw_00240
    :parameters (?v - vehicle ?r - road_type_road_0061 ?from - loc_type_loc_0041 ?to - loc_type_loc_0045 ?w - window_type_tw_00240)
    :precondition (and
      (at ?v ?from)
      (connects ?r ?from ?to)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
      (not (location-blocked ?from))
      (not (location-blocked ?to))
    )
    :effect (and
      (not (at ?v ?from))
      (at ?v ?to)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-road_0061-tw_00270
    :parameters (?v - vehicle ?r - road_type_road_0061 ?from - loc_type_loc_0041 ?to - loc_type_loc_0045 ?w - window_type_tw_00270)
    :precondition (and
      (at ?v ?from)
      (connects ?r ?from ?to)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
      (not (location-blocked ?from))
      (not (location-blocked ?to))
    )
    :effect (and
      (not (at ?v ?from))
      (at ?v ?to)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-road_0061-tw_00300
    :parameters (?v - vehicle ?r - road_type_road_0061 ?from - loc_type_loc_0041 ?to - loc_type_loc_0045 ?w - window_type_tw_00300)
    :precondition (and
      (at ?v ?from)
      (connects ?r ?from ?to)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
      (not (location-blocked ?from))
      (not (location-blocked ?to))
    )
    :effect (and
      (not (at ?v ?from))
      (at ?v ?to)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-road_0061-tw_00330
    :parameters (?v - vehicle ?r - road_type_road_0061 ?from - loc_type_loc_0041 ?to - loc_type_loc_0045 ?w - window_type_tw_00330)
    :precondition (and
      (at ?v ?from)
      (connects ?r ?from ?to)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
      (not (location-blocked ?from))
      (not (location-blocked ?to))
    )
    :effect (and
      (not (at ?v ?from))
      (at ?v ?to)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-road_0061-tw_00360
    :parameters (?v - vehicle ?r - road_type_road_0061 ?from - loc_type_loc_0041 ?to - loc_type_loc_0045 ?w - window_type_tw_00360)
    :precondition (and
      (at ?v ?from)
      (connects ?r ?from ?to)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
      (not (location-blocked ?from))
      (not (location-blocked ?to))
    )
    :effect (and
      (not (at ?v ?from))
      (at ?v ?to)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-road_0061-tw_00390
    :parameters (?v - vehicle ?r - road_type_road_0061 ?from - loc_type_loc_0041 ?to - loc_type_loc_0045 ?w - window_type_tw_00390)
    :precondition (and
      (at ?v ?from)
      (connects ?r ?from ?to)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
      (not (location-blocked ?from))
      (not (location-blocked ?to))
    )
    :effect (and
      (not (at ?v ?from))
      (at ?v ?to)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-road_0061-tw_00420
    :parameters (?v - vehicle ?r - road_type_road_0061 ?from - loc_type_loc_0041 ?to - loc_type_loc_0045 ?w - window_type_tw_00420)
    :precondition (and
      (at ?v ?from)
      (connects ?r ?from ?to)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
      (not (location-blocked ?from))
      (not (location-blocked ?to))
    )
    :effect (and
      (not (at ?v ?from))
      (at ?v ?to)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-road_0061-tw_00450
    :parameters (?v - vehicle ?r - road_type_road_0061 ?from - loc_type_loc_0041 ?to - loc_type_loc_0045 ?w - window_type_tw_00450)
    :precondition (and
      (at ?v ?from)
      (connects ?r ?from ?to)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
      (not (location-blocked ?from))
      (not (location-blocked ?to))
    )
    :effect (and
      (not (at ?v ?from))
      (at ?v ?to)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-road_0063-tw_00000
    :parameters (?v - vehicle ?r - road_type_road_0063 ?from - loc_type_loc_0043 ?to - loc_type_loc_0026 ?w - window_type_tw_00000)
    :precondition (and
      (at ?v ?from)
      (connects ?r ?from ?to)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
      (not (location-blocked ?from))
      (not (location-blocked ?to))
    )
    :effect (and
      (not (at ?v ?from))
      (at ?v ?to)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-road_0063-tw_00030
    :parameters (?v - vehicle ?r - road_type_road_0063 ?from - loc_type_loc_0043 ?to - loc_type_loc_0026 ?w - window_type_tw_00030)
    :precondition (and
      (at ?v ?from)
      (connects ?r ?from ?to)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
      (not (location-blocked ?from))
      (not (location-blocked ?to))
    )
    :effect (and
      (not (at ?v ?from))
      (at ?v ?to)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-road_0063-tw_00060
    :parameters (?v - vehicle ?r - road_type_road_0063 ?from - loc_type_loc_0043 ?to - loc_type_loc_0026 ?w - window_type_tw_00060)
    :precondition (and
      (at ?v ?from)
      (connects ?r ?from ?to)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
      (not (location-blocked ?from))
      (not (location-blocked ?to))
    )
    :effect (and
      (not (at ?v ?from))
      (at ?v ?to)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-road_0063-tw_00090
    :parameters (?v - vehicle ?r - road_type_road_0063 ?from - loc_type_loc_0043 ?to - loc_type_loc_0026 ?w - window_type_tw_00090)
    :precondition (and
      (at ?v ?from)
      (connects ?r ?from ?to)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
      (not (location-blocked ?from))
      (not (location-blocked ?to))
    )
    :effect (and
      (not (at ?v ?from))
      (at ?v ?to)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-road_0063-tw_00120
    :parameters (?v - vehicle ?r - road_type_road_0063 ?from - loc_type_loc_0043 ?to - loc_type_loc_0026 ?w - window_type_tw_00120)
    :precondition (and
      (at ?v ?from)
      (connects ?r ?from ?to)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
      (not (location-blocked ?from))
      (not (location-blocked ?to))
    )
    :effect (and
      (not (at ?v ?from))
      (at ?v ?to)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-road_0063-tw_00150
    :parameters (?v - vehicle ?r - road_type_road_0063 ?from - loc_type_loc_0043 ?to - loc_type_loc_0026 ?w - window_type_tw_00150)
    :precondition (and
      (at ?v ?from)
      (connects ?r ?from ?to)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
      (not (location-blocked ?from))
      (not (location-blocked ?to))
    )
    :effect (and
      (not (at ?v ?from))
      (at ?v ?to)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-road_0063-tw_00180
    :parameters (?v - vehicle ?r - road_type_road_0063 ?from - loc_type_loc_0043 ?to - loc_type_loc_0026 ?w - window_type_tw_00180)
    :precondition (and
      (at ?v ?from)
      (connects ?r ?from ?to)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
      (not (location-blocked ?from))
      (not (location-blocked ?to))
    )
    :effect (and
      (not (at ?v ?from))
      (at ?v ?to)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-road_0063-tw_00210
    :parameters (?v - vehicle ?r - road_type_road_0063 ?from - loc_type_loc_0043 ?to - loc_type_loc_0026 ?w - window_type_tw_00210)
    :precondition (and
      (at ?v ?from)
      (connects ?r ?from ?to)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
      (not (location-blocked ?from))
      (not (location-blocked ?to))
    )
    :effect (and
      (not (at ?v ?from))
      (at ?v ?to)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-road_0063-tw_00240
    :parameters (?v - vehicle ?r - road_type_road_0063 ?from - loc_type_loc_0043 ?to - loc_type_loc_0026 ?w - window_type_tw_00240)
    :precondition (and
      (at ?v ?from)
      (connects ?r ?from ?to)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
      (not (location-blocked ?from))
      (not (location-blocked ?to))
    )
    :effect (and
      (not (at ?v ?from))
      (at ?v ?to)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-road_0063-tw_00270
    :parameters (?v - vehicle ?r - road_type_road_0063 ?from - loc_type_loc_0043 ?to - loc_type_loc_0026 ?w - window_type_tw_00270)
    :precondition (and
      (at ?v ?from)
      (connects ?r ?from ?to)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
      (not (location-blocked ?from))
      (not (location-blocked ?to))
    )
    :effect (and
      (not (at ?v ?from))
      (at ?v ?to)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-road_0063-tw_00300
    :parameters (?v - vehicle ?r - road_type_road_0063 ?from - loc_type_loc_0043 ?to - loc_type_loc_0026 ?w - window_type_tw_00300)
    :precondition (and
      (at ?v ?from)
      (connects ?r ?from ?to)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
      (not (location-blocked ?from))
      (not (location-blocked ?to))
    )
    :effect (and
      (not (at ?v ?from))
      (at ?v ?to)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-road_0063-tw_00330
    :parameters (?v - vehicle ?r - road_type_road_0063 ?from - loc_type_loc_0043 ?to - loc_type_loc_0026 ?w - window_type_tw_00330)
    :precondition (and
      (at ?v ?from)
      (connects ?r ?from ?to)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
      (not (location-blocked ?from))
      (not (location-blocked ?to))
    )
    :effect (and
      (not (at ?v ?from))
      (at ?v ?to)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-road_0063-tw_00360
    :parameters (?v - vehicle ?r - road_type_road_0063 ?from - loc_type_loc_0043 ?to - loc_type_loc_0026 ?w - window_type_tw_00360)
    :precondition (and
      (at ?v ?from)
      (connects ?r ?from ?to)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
      (not (location-blocked ?from))
      (not (location-blocked ?to))
    )
    :effect (and
      (not (at ?v ?from))
      (at ?v ?to)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-road_0063-tw_00390
    :parameters (?v - vehicle ?r - road_type_road_0063 ?from - loc_type_loc_0043 ?to - loc_type_loc_0026 ?w - window_type_tw_00390)
    :precondition (and
      (at ?v ?from)
      (connects ?r ?from ?to)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
      (not (location-blocked ?from))
      (not (location-blocked ?to))
    )
    :effect (and
      (not (at ?v ?from))
      (at ?v ?to)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-road_0063-tw_00420
    :parameters (?v - vehicle ?r - road_type_road_0063 ?from - loc_type_loc_0043 ?to - loc_type_loc_0026 ?w - window_type_tw_00420)
    :precondition (and
      (at ?v ?from)
      (connects ?r ?from ?to)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
      (not (location-blocked ?from))
      (not (location-blocked ?to))
    )
    :effect (and
      (not (at ?v ?from))
      (at ?v ?to)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-road_0063-tw_00450
    :parameters (?v - vehicle ?r - road_type_road_0063 ?from - loc_type_loc_0043 ?to - loc_type_loc_0026 ?w - window_type_tw_00450)
    :precondition (and
      (at ?v ?from)
      (connects ?r ?from ?to)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
      (not (location-blocked ?from))
      (not (location-blocked ?to))
    )
    :effect (and
      (not (at ?v ?from))
      (at ?v ?to)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-road_0064-tw_00000
    :parameters (?v - vehicle ?r - road_type_road_0064 ?from - loc_type_loc_0044 ?to - loc_type_loc_0068 ?w - window_type_tw_00000)
    :precondition (and
      (at ?v ?from)
      (connects ?r ?from ?to)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
      (not (location-blocked ?from))
      (not (location-blocked ?to))
    )
    :effect (and
      (not (at ?v ?from))
      (at ?v ?to)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-road_0064-tw_00030
    :parameters (?v - vehicle ?r - road_type_road_0064 ?from - loc_type_loc_0044 ?to - loc_type_loc_0068 ?w - window_type_tw_00030)
    :precondition (and
      (at ?v ?from)
      (connects ?r ?from ?to)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
      (not (location-blocked ?from))
      (not (location-blocked ?to))
    )
    :effect (and
      (not (at ?v ?from))
      (at ?v ?to)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-road_0064-tw_00060
    :parameters (?v - vehicle ?r - road_type_road_0064 ?from - loc_type_loc_0044 ?to - loc_type_loc_0068 ?w - window_type_tw_00060)
    :precondition (and
      (at ?v ?from)
      (connects ?r ?from ?to)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
      (not (location-blocked ?from))
      (not (location-blocked ?to))
    )
    :effect (and
      (not (at ?v ?from))
      (at ?v ?to)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-road_0064-tw_00090
    :parameters (?v - vehicle ?r - road_type_road_0064 ?from - loc_type_loc_0044 ?to - loc_type_loc_0068 ?w - window_type_tw_00090)
    :precondition (and
      (at ?v ?from)
      (connects ?r ?from ?to)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
      (not (location-blocked ?from))
      (not (location-blocked ?to))
    )
    :effect (and
      (not (at ?v ?from))
      (at ?v ?to)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-road_0064-tw_00120
    :parameters (?v - vehicle ?r - road_type_road_0064 ?from - loc_type_loc_0044 ?to - loc_type_loc_0068 ?w - window_type_tw_00120)
    :precondition (and
      (at ?v ?from)
      (connects ?r ?from ?to)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
      (not (location-blocked ?from))
      (not (location-blocked ?to))
    )
    :effect (and
      (not (at ?v ?from))
      (at ?v ?to)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-road_0064-tw_00150
    :parameters (?v - vehicle ?r - road_type_road_0064 ?from - loc_type_loc_0044 ?to - loc_type_loc_0068 ?w - window_type_tw_00150)
    :precondition (and
      (at ?v ?from)
      (connects ?r ?from ?to)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
      (not (location-blocked ?from))
      (not (location-blocked ?to))
    )
    :effect (and
      (not (at ?v ?from))
      (at ?v ?to)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-road_0064-tw_00180
    :parameters (?v - vehicle ?r - road_type_road_0064 ?from - loc_type_loc_0044 ?to - loc_type_loc_0068 ?w - window_type_tw_00180)
    :precondition (and
      (at ?v ?from)
      (connects ?r ?from ?to)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
      (not (location-blocked ?from))
      (not (location-blocked ?to))
    )
    :effect (and
      (not (at ?v ?from))
      (at ?v ?to)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-road_0064-tw_00210
    :parameters (?v - vehicle ?r - road_type_road_0064 ?from - loc_type_loc_0044 ?to - loc_type_loc_0068 ?w - window_type_tw_00210)
    :precondition (and
      (at ?v ?from)
      (connects ?r ?from ?to)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
      (not (location-blocked ?from))
      (not (location-blocked ?to))
    )
    :effect (and
      (not (at ?v ?from))
      (at ?v ?to)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-road_0064-tw_00240
    :parameters (?v - vehicle ?r - road_type_road_0064 ?from - loc_type_loc_0044 ?to - loc_type_loc_0068 ?w - window_type_tw_00240)
    :precondition (and
      (at ?v ?from)
      (connects ?r ?from ?to)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
      (not (location-blocked ?from))
      (not (location-blocked ?to))
    )
    :effect (and
      (not (at ?v ?from))
      (at ?v ?to)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-road_0064-tw_00270
    :parameters (?v - vehicle ?r - road_type_road_0064 ?from - loc_type_loc_0044 ?to - loc_type_loc_0068 ?w - window_type_tw_00270)
    :precondition (and
      (at ?v ?from)
      (connects ?r ?from ?to)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
      (not (location-blocked ?from))
      (not (location-blocked ?to))
    )
    :effect (and
      (not (at ?v ?from))
      (at ?v ?to)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-road_0064-tw_00300
    :parameters (?v - vehicle ?r - road_type_road_0064 ?from - loc_type_loc_0044 ?to - loc_type_loc_0068 ?w - window_type_tw_00300)
    :precondition (and
      (at ?v ?from)
      (connects ?r ?from ?to)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
      (not (location-blocked ?from))
      (not (location-blocked ?to))
    )
    :effect (and
      (not (at ?v ?from))
      (at ?v ?to)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-road_0064-tw_00330
    :parameters (?v - vehicle ?r - road_type_road_0064 ?from - loc_type_loc_0044 ?to - loc_type_loc_0068 ?w - window_type_tw_00330)
    :precondition (and
      (at ?v ?from)
      (connects ?r ?from ?to)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
      (not (location-blocked ?from))
      (not (location-blocked ?to))
    )
    :effect (and
      (not (at ?v ?from))
      (at ?v ?to)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-road_0064-tw_00360
    :parameters (?v - vehicle ?r - road_type_road_0064 ?from - loc_type_loc_0044 ?to - loc_type_loc_0068 ?w - window_type_tw_00360)
    :precondition (and
      (at ?v ?from)
      (connects ?r ?from ?to)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
      (not (location-blocked ?from))
      (not (location-blocked ?to))
    )
    :effect (and
      (not (at ?v ?from))
      (at ?v ?to)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-road_0064-tw_00390
    :parameters (?v - vehicle ?r - road_type_road_0064 ?from - loc_type_loc_0044 ?to - loc_type_loc_0068 ?w - window_type_tw_00390)
    :precondition (and
      (at ?v ?from)
      (connects ?r ?from ?to)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
      (not (location-blocked ?from))
      (not (location-blocked ?to))
    )
    :effect (and
      (not (at ?v ?from))
      (at ?v ?to)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-road_0064-tw_00420
    :parameters (?v - vehicle ?r - road_type_road_0064 ?from - loc_type_loc_0044 ?to - loc_type_loc_0068 ?w - window_type_tw_00420)
    :precondition (and
      (at ?v ?from)
      (connects ?r ?from ?to)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
      (not (location-blocked ?from))
      (not (location-blocked ?to))
    )
    :effect (and
      (not (at ?v ?from))
      (at ?v ?to)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-road_0064-tw_00450
    :parameters (?v - vehicle ?r - road_type_road_0064 ?from - loc_type_loc_0044 ?to - loc_type_loc_0068 ?w - window_type_tw_00450)
    :precondition (and
      (at ?v ?from)
      (connects ?r ?from ?to)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
      (not (location-blocked ?from))
      (not (location-blocked ?to))
    )
    :effect (and
      (not (at ?v ?from))
      (at ?v ?to)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-road_0065-tw_00000
    :parameters (?v - vehicle ?r - road_type_road_0065 ?from - loc_type_loc_0044 ?to - loc_type_loc_0086 ?w - window_type_tw_00000)
    :precondition (and
      (at ?v ?from)
      (connects ?r ?from ?to)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
      (not (location-blocked ?from))
      (not (location-blocked ?to))
    )
    :effect (and
      (not (at ?v ?from))
      (at ?v ?to)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-road_0065-tw_00030
    :parameters (?v - vehicle ?r - road_type_road_0065 ?from - loc_type_loc_0044 ?to - loc_type_loc_0086 ?w - window_type_tw_00030)
    :precondition (and
      (at ?v ?from)
      (connects ?r ?from ?to)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
      (not (location-blocked ?from))
      (not (location-blocked ?to))
    )
    :effect (and
      (not (at ?v ?from))
      (at ?v ?to)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-road_0065-tw_00060
    :parameters (?v - vehicle ?r - road_type_road_0065 ?from - loc_type_loc_0044 ?to - loc_type_loc_0086 ?w - window_type_tw_00060)
    :precondition (and
      (at ?v ?from)
      (connects ?r ?from ?to)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
      (not (location-blocked ?from))
      (not (location-blocked ?to))
    )
    :effect (and
      (not (at ?v ?from))
      (at ?v ?to)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-road_0065-tw_00090
    :parameters (?v - vehicle ?r - road_type_road_0065 ?from - loc_type_loc_0044 ?to - loc_type_loc_0086 ?w - window_type_tw_00090)
    :precondition (and
      (at ?v ?from)
      (connects ?r ?from ?to)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
      (not (location-blocked ?from))
      (not (location-blocked ?to))
    )
    :effect (and
      (not (at ?v ?from))
      (at ?v ?to)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-road_0065-tw_00120
    :parameters (?v - vehicle ?r - road_type_road_0065 ?from - loc_type_loc_0044 ?to - loc_type_loc_0086 ?w - window_type_tw_00120)
    :precondition (and
      (at ?v ?from)
      (connects ?r ?from ?to)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
      (not (location-blocked ?from))
      (not (location-blocked ?to))
    )
    :effect (and
      (not (at ?v ?from))
      (at ?v ?to)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-road_0065-tw_00150
    :parameters (?v - vehicle ?r - road_type_road_0065 ?from - loc_type_loc_0044 ?to - loc_type_loc_0086 ?w - window_type_tw_00150)
    :precondition (and
      (at ?v ?from)
      (connects ?r ?from ?to)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
      (not (location-blocked ?from))
      (not (location-blocked ?to))
    )
    :effect (and
      (not (at ?v ?from))
      (at ?v ?to)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-road_0065-tw_00180
    :parameters (?v - vehicle ?r - road_type_road_0065 ?from - loc_type_loc_0044 ?to - loc_type_loc_0086 ?w - window_type_tw_00180)
    :precondition (and
      (at ?v ?from)
      (connects ?r ?from ?to)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
      (not (location-blocked ?from))
      (not (location-blocked ?to))
    )
    :effect (and
      (not (at ?v ?from))
      (at ?v ?to)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-road_0065-tw_00210
    :parameters (?v - vehicle ?r - road_type_road_0065 ?from - loc_type_loc_0044 ?to - loc_type_loc_0086 ?w - window_type_tw_00210)
    :precondition (and
      (at ?v ?from)
      (connects ?r ?from ?to)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
      (not (location-blocked ?from))
      (not (location-blocked ?to))
    )
    :effect (and
      (not (at ?v ?from))
      (at ?v ?to)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-road_0065-tw_00240
    :parameters (?v - vehicle ?r - road_type_road_0065 ?from - loc_type_loc_0044 ?to - loc_type_loc_0086 ?w - window_type_tw_00240)
    :precondition (and
      (at ?v ?from)
      (connects ?r ?from ?to)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
      (not (location-blocked ?from))
      (not (location-blocked ?to))
    )
    :effect (and
      (not (at ?v ?from))
      (at ?v ?to)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-road_0065-tw_00270
    :parameters (?v - vehicle ?r - road_type_road_0065 ?from - loc_type_loc_0044 ?to - loc_type_loc_0086 ?w - window_type_tw_00270)
    :precondition (and
      (at ?v ?from)
      (connects ?r ?from ?to)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
      (not (location-blocked ?from))
      (not (location-blocked ?to))
    )
    :effect (and
      (not (at ?v ?from))
      (at ?v ?to)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-road_0065-tw_00300
    :parameters (?v - vehicle ?r - road_type_road_0065 ?from - loc_type_loc_0044 ?to - loc_type_loc_0086 ?w - window_type_tw_00300)
    :precondition (and
      (at ?v ?from)
      (connects ?r ?from ?to)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
      (not (location-blocked ?from))
      (not (location-blocked ?to))
    )
    :effect (and
      (not (at ?v ?from))
      (at ?v ?to)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-road_0065-tw_00330
    :parameters (?v - vehicle ?r - road_type_road_0065 ?from - loc_type_loc_0044 ?to - loc_type_loc_0086 ?w - window_type_tw_00330)
    :precondition (and
      (at ?v ?from)
      (connects ?r ?from ?to)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
      (not (location-blocked ?from))
      (not (location-blocked ?to))
    )
    :effect (and
      (not (at ?v ?from))
      (at ?v ?to)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-road_0065-tw_00360
    :parameters (?v - vehicle ?r - road_type_road_0065 ?from - loc_type_loc_0044 ?to - loc_type_loc_0086 ?w - window_type_tw_00360)
    :precondition (and
      (at ?v ?from)
      (connects ?r ?from ?to)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
      (not (location-blocked ?from))
      (not (location-blocked ?to))
    )
    :effect (and
      (not (at ?v ?from))
      (at ?v ?to)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-road_0065-tw_00390
    :parameters (?v - vehicle ?r - road_type_road_0065 ?from - loc_type_loc_0044 ?to - loc_type_loc_0086 ?w - window_type_tw_00390)
    :precondition (and
      (at ?v ?from)
      (connects ?r ?from ?to)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
      (not (location-blocked ?from))
      (not (location-blocked ?to))
    )
    :effect (and
      (not (at ?v ?from))
      (at ?v ?to)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-road_0065-tw_00420
    :parameters (?v - vehicle ?r - road_type_road_0065 ?from - loc_type_loc_0044 ?to - loc_type_loc_0086 ?w - window_type_tw_00420)
    :precondition (and
      (at ?v ?from)
      (connects ?r ?from ?to)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
      (not (location-blocked ?from))
      (not (location-blocked ?to))
    )
    :effect (and
      (not (at ?v ?from))
      (at ?v ?to)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-road_0065-tw_00450
    :parameters (?v - vehicle ?r - road_type_road_0065 ?from - loc_type_loc_0044 ?to - loc_type_loc_0086 ?w - window_type_tw_00450)
    :precondition (and
      (at ?v ?from)
      (connects ?r ?from ?to)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
      (not (location-blocked ?from))
      (not (location-blocked ?to))
    )
    :effect (and
      (not (at ?v ?from))
      (at ?v ?to)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-road_0066-tw_00000
    :parameters (?v - vehicle ?r - road_type_road_0066 ?from - loc_type_loc_0045 ?to - loc_type_loc_0041 ?w - window_type_tw_00000)
    :precondition (and
      (at ?v ?from)
      (connects ?r ?from ?to)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
      (not (location-blocked ?from))
      (not (location-blocked ?to))
    )
    :effect (and
      (not (at ?v ?from))
      (at ?v ?to)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-road_0066-tw_00030
    :parameters (?v - vehicle ?r - road_type_road_0066 ?from - loc_type_loc_0045 ?to - loc_type_loc_0041 ?w - window_type_tw_00030)
    :precondition (and
      (at ?v ?from)
      (connects ?r ?from ?to)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
      (not (location-blocked ?from))
      (not (location-blocked ?to))
    )
    :effect (and
      (not (at ?v ?from))
      (at ?v ?to)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-road_0066-tw_00060
    :parameters (?v - vehicle ?r - road_type_road_0066 ?from - loc_type_loc_0045 ?to - loc_type_loc_0041 ?w - window_type_tw_00060)
    :precondition (and
      (at ?v ?from)
      (connects ?r ?from ?to)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
      (not (location-blocked ?from))
      (not (location-blocked ?to))
    )
    :effect (and
      (not (at ?v ?from))
      (at ?v ?to)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-road_0066-tw_00090
    :parameters (?v - vehicle ?r - road_type_road_0066 ?from - loc_type_loc_0045 ?to - loc_type_loc_0041 ?w - window_type_tw_00090)
    :precondition (and
      (at ?v ?from)
      (connects ?r ?from ?to)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
      (not (location-blocked ?from))
      (not (location-blocked ?to))
    )
    :effect (and
      (not (at ?v ?from))
      (at ?v ?to)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-road_0066-tw_00120
    :parameters (?v - vehicle ?r - road_type_road_0066 ?from - loc_type_loc_0045 ?to - loc_type_loc_0041 ?w - window_type_tw_00120)
    :precondition (and
      (at ?v ?from)
      (connects ?r ?from ?to)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
      (not (location-blocked ?from))
      (not (location-blocked ?to))
    )
    :effect (and
      (not (at ?v ?from))
      (at ?v ?to)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-road_0066-tw_00150
    :parameters (?v - vehicle ?r - road_type_road_0066 ?from - loc_type_loc_0045 ?to - loc_type_loc_0041 ?w - window_type_tw_00150)
    :precondition (and
      (at ?v ?from)
      (connects ?r ?from ?to)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
      (not (location-blocked ?from))
      (not (location-blocked ?to))
    )
    :effect (and
      (not (at ?v ?from))
      (at ?v ?to)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-road_0066-tw_00180
    :parameters (?v - vehicle ?r - road_type_road_0066 ?from - loc_type_loc_0045 ?to - loc_type_loc_0041 ?w - window_type_tw_00180)
    :precondition (and
      (at ?v ?from)
      (connects ?r ?from ?to)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
      (not (location-blocked ?from))
      (not (location-blocked ?to))
    )
    :effect (and
      (not (at ?v ?from))
      (at ?v ?to)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-road_0066-tw_00210
    :parameters (?v - vehicle ?r - road_type_road_0066 ?from - loc_type_loc_0045 ?to - loc_type_loc_0041 ?w - window_type_tw_00210)
    :precondition (and
      (at ?v ?from)
      (connects ?r ?from ?to)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
      (not (location-blocked ?from))
      (not (location-blocked ?to))
    )
    :effect (and
      (not (at ?v ?from))
      (at ?v ?to)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-road_0066-tw_00240
    :parameters (?v - vehicle ?r - road_type_road_0066 ?from - loc_type_loc_0045 ?to - loc_type_loc_0041 ?w - window_type_tw_00240)
    :precondition (and
      (at ?v ?from)
      (connects ?r ?from ?to)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
      (not (location-blocked ?from))
      (not (location-blocked ?to))
    )
    :effect (and
      (not (at ?v ?from))
      (at ?v ?to)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-road_0066-tw_00270
    :parameters (?v - vehicle ?r - road_type_road_0066 ?from - loc_type_loc_0045 ?to - loc_type_loc_0041 ?w - window_type_tw_00270)
    :precondition (and
      (at ?v ?from)
      (connects ?r ?from ?to)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
      (not (location-blocked ?from))
      (not (location-blocked ?to))
    )
    :effect (and
      (not (at ?v ?from))
      (at ?v ?to)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-road_0066-tw_00300
    :parameters (?v - vehicle ?r - road_type_road_0066 ?from - loc_type_loc_0045 ?to - loc_type_loc_0041 ?w - window_type_tw_00300)
    :precondition (and
      (at ?v ?from)
      (connects ?r ?from ?to)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
      (not (location-blocked ?from))
      (not (location-blocked ?to))
    )
    :effect (and
      (not (at ?v ?from))
      (at ?v ?to)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-road_0066-tw_00330
    :parameters (?v - vehicle ?r - road_type_road_0066 ?from - loc_type_loc_0045 ?to - loc_type_loc_0041 ?w - window_type_tw_00330)
    :precondition (and
      (at ?v ?from)
      (connects ?r ?from ?to)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
      (not (location-blocked ?from))
      (not (location-blocked ?to))
    )
    :effect (and
      (not (at ?v ?from))
      (at ?v ?to)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-road_0066-tw_00360
    :parameters (?v - vehicle ?r - road_type_road_0066 ?from - loc_type_loc_0045 ?to - loc_type_loc_0041 ?w - window_type_tw_00360)
    :precondition (and
      (at ?v ?from)
      (connects ?r ?from ?to)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
      (not (location-blocked ?from))
      (not (location-blocked ?to))
    )
    :effect (and
      (not (at ?v ?from))
      (at ?v ?to)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-road_0066-tw_00390
    :parameters (?v - vehicle ?r - road_type_road_0066 ?from - loc_type_loc_0045 ?to - loc_type_loc_0041 ?w - window_type_tw_00390)
    :precondition (and
      (at ?v ?from)
      (connects ?r ?from ?to)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
      (not (location-blocked ?from))
      (not (location-blocked ?to))
    )
    :effect (and
      (not (at ?v ?from))
      (at ?v ?to)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-road_0066-tw_00420
    :parameters (?v - vehicle ?r - road_type_road_0066 ?from - loc_type_loc_0045 ?to - loc_type_loc_0041 ?w - window_type_tw_00420)
    :precondition (and
      (at ?v ?from)
      (connects ?r ?from ?to)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
      (not (location-blocked ?from))
      (not (location-blocked ?to))
    )
    :effect (and
      (not (at ?v ?from))
      (at ?v ?to)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-road_0066-tw_00450
    :parameters (?v - vehicle ?r - road_type_road_0066 ?from - loc_type_loc_0045 ?to - loc_type_loc_0041 ?w - window_type_tw_00450)
    :precondition (and
      (at ?v ?from)
      (connects ?r ?from ?to)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
      (not (location-blocked ?from))
      (not (location-blocked ?to))
    )
    :effect (and
      (not (at ?v ?from))
      (at ?v ?to)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-road_0067-tw_00000
    :parameters (?v - vehicle ?r - road_type_road_0067 ?from - loc_type_loc_0046 ?to - loc_type_loc_0031 ?w - window_type_tw_00000)
    :precondition (and
      (at ?v ?from)
      (connects ?r ?from ?to)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
      (not (location-blocked ?from))
      (not (location-blocked ?to))
    )
    :effect (and
      (not (at ?v ?from))
      (at ?v ?to)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-road_0067-tw_00030
    :parameters (?v - vehicle ?r - road_type_road_0067 ?from - loc_type_loc_0046 ?to - loc_type_loc_0031 ?w - window_type_tw_00030)
    :precondition (and
      (at ?v ?from)
      (connects ?r ?from ?to)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
      (not (location-blocked ?from))
      (not (location-blocked ?to))
    )
    :effect (and
      (not (at ?v ?from))
      (at ?v ?to)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-road_0067-tw_00060
    :parameters (?v - vehicle ?r - road_type_road_0067 ?from - loc_type_loc_0046 ?to - loc_type_loc_0031 ?w - window_type_tw_00060)
    :precondition (and
      (at ?v ?from)
      (connects ?r ?from ?to)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
      (not (location-blocked ?from))
      (not (location-blocked ?to))
    )
    :effect (and
      (not (at ?v ?from))
      (at ?v ?to)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-road_0067-tw_00090
    :parameters (?v - vehicle ?r - road_type_road_0067 ?from - loc_type_loc_0046 ?to - loc_type_loc_0031 ?w - window_type_tw_00090)
    :precondition (and
      (at ?v ?from)
      (connects ?r ?from ?to)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
      (not (location-blocked ?from))
      (not (location-blocked ?to))
    )
    :effect (and
      (not (at ?v ?from))
      (at ?v ?to)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-road_0067-tw_00120
    :parameters (?v - vehicle ?r - road_type_road_0067 ?from - loc_type_loc_0046 ?to - loc_type_loc_0031 ?w - window_type_tw_00120)
    :precondition (and
      (at ?v ?from)
      (connects ?r ?from ?to)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
      (not (location-blocked ?from))
      (not (location-blocked ?to))
    )
    :effect (and
      (not (at ?v ?from))
      (at ?v ?to)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-road_0067-tw_00150
    :parameters (?v - vehicle ?r - road_type_road_0067 ?from - loc_type_loc_0046 ?to - loc_type_loc_0031 ?w - window_type_tw_00150)
    :precondition (and
      (at ?v ?from)
      (connects ?r ?from ?to)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
      (not (location-blocked ?from))
      (not (location-blocked ?to))
    )
    :effect (and
      (not (at ?v ?from))
      (at ?v ?to)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-road_0067-tw_00180
    :parameters (?v - vehicle ?r - road_type_road_0067 ?from - loc_type_loc_0046 ?to - loc_type_loc_0031 ?w - window_type_tw_00180)
    :precondition (and
      (at ?v ?from)
      (connects ?r ?from ?to)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
      (not (location-blocked ?from))
      (not (location-blocked ?to))
    )
    :effect (and
      (not (at ?v ?from))
      (at ?v ?to)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-road_0067-tw_00210
    :parameters (?v - vehicle ?r - road_type_road_0067 ?from - loc_type_loc_0046 ?to - loc_type_loc_0031 ?w - window_type_tw_00210)
    :precondition (and
      (at ?v ?from)
      (connects ?r ?from ?to)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
      (not (location-blocked ?from))
      (not (location-blocked ?to))
    )
    :effect (and
      (not (at ?v ?from))
      (at ?v ?to)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-road_0067-tw_00240
    :parameters (?v - vehicle ?r - road_type_road_0067 ?from - loc_type_loc_0046 ?to - loc_type_loc_0031 ?w - window_type_tw_00240)
    :precondition (and
      (at ?v ?from)
      (connects ?r ?from ?to)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
      (not (location-blocked ?from))
      (not (location-blocked ?to))
    )
    :effect (and
      (not (at ?v ?from))
      (at ?v ?to)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-road_0067-tw_00270
    :parameters (?v - vehicle ?r - road_type_road_0067 ?from - loc_type_loc_0046 ?to - loc_type_loc_0031 ?w - window_type_tw_00270)
    :precondition (and
      (at ?v ?from)
      (connects ?r ?from ?to)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
      (not (location-blocked ?from))
      (not (location-blocked ?to))
    )
    :effect (and
      (not (at ?v ?from))
      (at ?v ?to)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-road_0067-tw_00300
    :parameters (?v - vehicle ?r - road_type_road_0067 ?from - loc_type_loc_0046 ?to - loc_type_loc_0031 ?w - window_type_tw_00300)
    :precondition (and
      (at ?v ?from)
      (connects ?r ?from ?to)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
      (not (location-blocked ?from))
      (not (location-blocked ?to))
    )
    :effect (and
      (not (at ?v ?from))
      (at ?v ?to)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-road_0067-tw_00330
    :parameters (?v - vehicle ?r - road_type_road_0067 ?from - loc_type_loc_0046 ?to - loc_type_loc_0031 ?w - window_type_tw_00330)
    :precondition (and
      (at ?v ?from)
      (connects ?r ?from ?to)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
      (not (location-blocked ?from))
      (not (location-blocked ?to))
    )
    :effect (and
      (not (at ?v ?from))
      (at ?v ?to)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-road_0067-tw_00360
    :parameters (?v - vehicle ?r - road_type_road_0067 ?from - loc_type_loc_0046 ?to - loc_type_loc_0031 ?w - window_type_tw_00360)
    :precondition (and
      (at ?v ?from)
      (connects ?r ?from ?to)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
      (not (location-blocked ?from))
      (not (location-blocked ?to))
    )
    :effect (and
      (not (at ?v ?from))
      (at ?v ?to)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-road_0067-tw_00390
    :parameters (?v - vehicle ?r - road_type_road_0067 ?from - loc_type_loc_0046 ?to - loc_type_loc_0031 ?w - window_type_tw_00390)
    :precondition (and
      (at ?v ?from)
      (connects ?r ?from ?to)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
      (not (location-blocked ?from))
      (not (location-blocked ?to))
    )
    :effect (and
      (not (at ?v ?from))
      (at ?v ?to)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-road_0067-tw_00420
    :parameters (?v - vehicle ?r - road_type_road_0067 ?from - loc_type_loc_0046 ?to - loc_type_loc_0031 ?w - window_type_tw_00420)
    :precondition (and
      (at ?v ?from)
      (connects ?r ?from ?to)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
      (not (location-blocked ?from))
      (not (location-blocked ?to))
    )
    :effect (and
      (not (at ?v ?from))
      (at ?v ?to)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-road_0067-tw_00450
    :parameters (?v - vehicle ?r - road_type_road_0067 ?from - loc_type_loc_0046 ?to - loc_type_loc_0031 ?w - window_type_tw_00450)
    :precondition (and
      (at ?v ?from)
      (connects ?r ?from ?to)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
      (not (location-blocked ?from))
      (not (location-blocked ?to))
    )
    :effect (and
      (not (at ?v ?from))
      (at ?v ?to)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-road_0069-tw_00000
    :parameters (?v - vehicle ?r - road_type_road_0069 ?from - loc_type_loc_0047 ?to - loc_type_loc_0013 ?w - window_type_tw_00000)
    :precondition (and
      (at ?v ?from)
      (connects ?r ?from ?to)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
      (not (location-blocked ?from))
      (not (location-blocked ?to))
    )
    :effect (and
      (not (at ?v ?from))
      (at ?v ?to)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-road_0069-tw_00030
    :parameters (?v - vehicle ?r - road_type_road_0069 ?from - loc_type_loc_0047 ?to - loc_type_loc_0013 ?w - window_type_tw_00030)
    :precondition (and
      (at ?v ?from)
      (connects ?r ?from ?to)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
      (not (location-blocked ?from))
      (not (location-blocked ?to))
    )
    :effect (and
      (not (at ?v ?from))
      (at ?v ?to)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-road_0069-tw_00060
    :parameters (?v - vehicle ?r - road_type_road_0069 ?from - loc_type_loc_0047 ?to - loc_type_loc_0013 ?w - window_type_tw_00060)
    :precondition (and
      (at ?v ?from)
      (connects ?r ?from ?to)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
      (not (location-blocked ?from))
      (not (location-blocked ?to))
    )
    :effect (and
      (not (at ?v ?from))
      (at ?v ?to)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-road_0069-tw_00090
    :parameters (?v - vehicle ?r - road_type_road_0069 ?from - loc_type_loc_0047 ?to - loc_type_loc_0013 ?w - window_type_tw_00090)
    :precondition (and
      (at ?v ?from)
      (connects ?r ?from ?to)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
      (not (location-blocked ?from))
      (not (location-blocked ?to))
    )
    :effect (and
      (not (at ?v ?from))
      (at ?v ?to)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-road_0069-tw_00120
    :parameters (?v - vehicle ?r - road_type_road_0069 ?from - loc_type_loc_0047 ?to - loc_type_loc_0013 ?w - window_type_tw_00120)
    :precondition (and
      (at ?v ?from)
      (connects ?r ?from ?to)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
      (not (location-blocked ?from))
      (not (location-blocked ?to))
    )
    :effect (and
      (not (at ?v ?from))
      (at ?v ?to)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-road_0069-tw_00150
    :parameters (?v - vehicle ?r - road_type_road_0069 ?from - loc_type_loc_0047 ?to - loc_type_loc_0013 ?w - window_type_tw_00150)
    :precondition (and
      (at ?v ?from)
      (connects ?r ?from ?to)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
      (not (location-blocked ?from))
      (not (location-blocked ?to))
    )
    :effect (and
      (not (at ?v ?from))
      (at ?v ?to)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-road_0069-tw_00180
    :parameters (?v - vehicle ?r - road_type_road_0069 ?from - loc_type_loc_0047 ?to - loc_type_loc_0013 ?w - window_type_tw_00180)
    :precondition (and
      (at ?v ?from)
      (connects ?r ?from ?to)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
      (not (location-blocked ?from))
      (not (location-blocked ?to))
    )
    :effect (and
      (not (at ?v ?from))
      (at ?v ?to)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-road_0069-tw_00210
    :parameters (?v - vehicle ?r - road_type_road_0069 ?from - loc_type_loc_0047 ?to - loc_type_loc_0013 ?w - window_type_tw_00210)
    :precondition (and
      (at ?v ?from)
      (connects ?r ?from ?to)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
      (not (location-blocked ?from))
      (not (location-blocked ?to))
    )
    :effect (and
      (not (at ?v ?from))
      (at ?v ?to)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-road_0069-tw_00240
    :parameters (?v - vehicle ?r - road_type_road_0069 ?from - loc_type_loc_0047 ?to - loc_type_loc_0013 ?w - window_type_tw_00240)
    :precondition (and
      (at ?v ?from)
      (connects ?r ?from ?to)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
      (not (location-blocked ?from))
      (not (location-blocked ?to))
    )
    :effect (and
      (not (at ?v ?from))
      (at ?v ?to)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-road_0069-tw_00270
    :parameters (?v - vehicle ?r - road_type_road_0069 ?from - loc_type_loc_0047 ?to - loc_type_loc_0013 ?w - window_type_tw_00270)
    :precondition (and
      (at ?v ?from)
      (connects ?r ?from ?to)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
      (not (location-blocked ?from))
      (not (location-blocked ?to))
    )
    :effect (and
      (not (at ?v ?from))
      (at ?v ?to)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-road_0069-tw_00300
    :parameters (?v - vehicle ?r - road_type_road_0069 ?from - loc_type_loc_0047 ?to - loc_type_loc_0013 ?w - window_type_tw_00300)
    :precondition (and
      (at ?v ?from)
      (connects ?r ?from ?to)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
      (not (location-blocked ?from))
      (not (location-blocked ?to))
    )
    :effect (and
      (not (at ?v ?from))
      (at ?v ?to)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-road_0069-tw_00330
    :parameters (?v - vehicle ?r - road_type_road_0069 ?from - loc_type_loc_0047 ?to - loc_type_loc_0013 ?w - window_type_tw_00330)
    :precondition (and
      (at ?v ?from)
      (connects ?r ?from ?to)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
      (not (location-blocked ?from))
      (not (location-blocked ?to))
    )
    :effect (and
      (not (at ?v ?from))
      (at ?v ?to)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-road_0069-tw_00360
    :parameters (?v - vehicle ?r - road_type_road_0069 ?from - loc_type_loc_0047 ?to - loc_type_loc_0013 ?w - window_type_tw_00360)
    :precondition (and
      (at ?v ?from)
      (connects ?r ?from ?to)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
      (not (location-blocked ?from))
      (not (location-blocked ?to))
    )
    :effect (and
      (not (at ?v ?from))
      (at ?v ?to)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-road_0069-tw_00390
    :parameters (?v - vehicle ?r - road_type_road_0069 ?from - loc_type_loc_0047 ?to - loc_type_loc_0013 ?w - window_type_tw_00390)
    :precondition (and
      (at ?v ?from)
      (connects ?r ?from ?to)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
      (not (location-blocked ?from))
      (not (location-blocked ?to))
    )
    :effect (and
      (not (at ?v ?from))
      (at ?v ?to)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-road_0069-tw_00420
    :parameters (?v - vehicle ?r - road_type_road_0069 ?from - loc_type_loc_0047 ?to - loc_type_loc_0013 ?w - window_type_tw_00420)
    :precondition (and
      (at ?v ?from)
      (connects ?r ?from ?to)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
      (not (location-blocked ?from))
      (not (location-blocked ?to))
    )
    :effect (and
      (not (at ?v ?from))
      (at ?v ?to)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-road_0069-tw_00450
    :parameters (?v - vehicle ?r - road_type_road_0069 ?from - loc_type_loc_0047 ?to - loc_type_loc_0013 ?w - window_type_tw_00450)
    :precondition (and
      (at ?v ?from)
      (connects ?r ?from ?to)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
      (not (location-blocked ?from))
      (not (location-blocked ?to))
    )
    :effect (and
      (not (at ?v ?from))
      (at ?v ?to)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-road_0070-tw_00000
    :parameters (?v - vehicle ?r - road_type_road_0070 ?from - loc_type_loc_0047 ?to - loc_type_loc_0097 ?w - window_type_tw_00000)
    :precondition (and
      (at ?v ?from)
      (connects ?r ?from ?to)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
      (not (location-blocked ?from))
      (not (location-blocked ?to))
    )
    :effect (and
      (not (at ?v ?from))
      (at ?v ?to)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-road_0070-tw_00030
    :parameters (?v - vehicle ?r - road_type_road_0070 ?from - loc_type_loc_0047 ?to - loc_type_loc_0097 ?w - window_type_tw_00030)
    :precondition (and
      (at ?v ?from)
      (connects ?r ?from ?to)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
      (not (location-blocked ?from))
      (not (location-blocked ?to))
    )
    :effect (and
      (not (at ?v ?from))
      (at ?v ?to)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-road_0070-tw_00060
    :parameters (?v - vehicle ?r - road_type_road_0070 ?from - loc_type_loc_0047 ?to - loc_type_loc_0097 ?w - window_type_tw_00060)
    :precondition (and
      (at ?v ?from)
      (connects ?r ?from ?to)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
      (not (location-blocked ?from))
      (not (location-blocked ?to))
    )
    :effect (and
      (not (at ?v ?from))
      (at ?v ?to)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-road_0070-tw_00090
    :parameters (?v - vehicle ?r - road_type_road_0070 ?from - loc_type_loc_0047 ?to - loc_type_loc_0097 ?w - window_type_tw_00090)
    :precondition (and
      (at ?v ?from)
      (connects ?r ?from ?to)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
      (not (location-blocked ?from))
      (not (location-blocked ?to))
    )
    :effect (and
      (not (at ?v ?from))
      (at ?v ?to)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-road_0070-tw_00120
    :parameters (?v - vehicle ?r - road_type_road_0070 ?from - loc_type_loc_0047 ?to - loc_type_loc_0097 ?w - window_type_tw_00120)
    :precondition (and
      (at ?v ?from)
      (connects ?r ?from ?to)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
      (not (location-blocked ?from))
      (not (location-blocked ?to))
    )
    :effect (and
      (not (at ?v ?from))
      (at ?v ?to)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-road_0070-tw_00150
    :parameters (?v - vehicle ?r - road_type_road_0070 ?from - loc_type_loc_0047 ?to - loc_type_loc_0097 ?w - window_type_tw_00150)
    :precondition (and
      (at ?v ?from)
      (connects ?r ?from ?to)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
      (not (location-blocked ?from))
      (not (location-blocked ?to))
    )
    :effect (and
      (not (at ?v ?from))
      (at ?v ?to)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-road_0070-tw_00180
    :parameters (?v - vehicle ?r - road_type_road_0070 ?from - loc_type_loc_0047 ?to - loc_type_loc_0097 ?w - window_type_tw_00180)
    :precondition (and
      (at ?v ?from)
      (connects ?r ?from ?to)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
      (not (location-blocked ?from))
      (not (location-blocked ?to))
    )
    :effect (and
      (not (at ?v ?from))
      (at ?v ?to)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-road_0070-tw_00210
    :parameters (?v - vehicle ?r - road_type_road_0070 ?from - loc_type_loc_0047 ?to - loc_type_loc_0097 ?w - window_type_tw_00210)
    :precondition (and
      (at ?v ?from)
      (connects ?r ?from ?to)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
      (not (location-blocked ?from))
      (not (location-blocked ?to))
    )
    :effect (and
      (not (at ?v ?from))
      (at ?v ?to)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-road_0070-tw_00240
    :parameters (?v - vehicle ?r - road_type_road_0070 ?from - loc_type_loc_0047 ?to - loc_type_loc_0097 ?w - window_type_tw_00240)
    :precondition (and
      (at ?v ?from)
      (connects ?r ?from ?to)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
      (not (location-blocked ?from))
      (not (location-blocked ?to))
    )
    :effect (and
      (not (at ?v ?from))
      (at ?v ?to)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-road_0070-tw_00270
    :parameters (?v - vehicle ?r - road_type_road_0070 ?from - loc_type_loc_0047 ?to - loc_type_loc_0097 ?w - window_type_tw_00270)
    :precondition (and
      (at ?v ?from)
      (connects ?r ?from ?to)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
      (not (location-blocked ?from))
      (not (location-blocked ?to))
    )
    :effect (and
      (not (at ?v ?from))
      (at ?v ?to)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-road_0070-tw_00300
    :parameters (?v - vehicle ?r - road_type_road_0070 ?from - loc_type_loc_0047 ?to - loc_type_loc_0097 ?w - window_type_tw_00300)
    :precondition (and
      (at ?v ?from)
      (connects ?r ?from ?to)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
      (not (location-blocked ?from))
      (not (location-blocked ?to))
    )
    :effect (and
      (not (at ?v ?from))
      (at ?v ?to)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-road_0070-tw_00330
    :parameters (?v - vehicle ?r - road_type_road_0070 ?from - loc_type_loc_0047 ?to - loc_type_loc_0097 ?w - window_type_tw_00330)
    :precondition (and
      (at ?v ?from)
      (connects ?r ?from ?to)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
      (not (location-blocked ?from))
      (not (location-blocked ?to))
    )
    :effect (and
      (not (at ?v ?from))
      (at ?v ?to)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-road_0070-tw_00360
    :parameters (?v - vehicle ?r - road_type_road_0070 ?from - loc_type_loc_0047 ?to - loc_type_loc_0097 ?w - window_type_tw_00360)
    :precondition (and
      (at ?v ?from)
      (connects ?r ?from ?to)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
      (not (location-blocked ?from))
      (not (location-blocked ?to))
    )
    :effect (and
      (not (at ?v ?from))
      (at ?v ?to)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-road_0070-tw_00390
    :parameters (?v - vehicle ?r - road_type_road_0070 ?from - loc_type_loc_0047 ?to - loc_type_loc_0097 ?w - window_type_tw_00390)
    :precondition (and
      (at ?v ?from)
      (connects ?r ?from ?to)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
      (not (location-blocked ?from))
      (not (location-blocked ?to))
    )
    :effect (and
      (not (at ?v ?from))
      (at ?v ?to)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-road_0070-tw_00420
    :parameters (?v - vehicle ?r - road_type_road_0070 ?from - loc_type_loc_0047 ?to - loc_type_loc_0097 ?w - window_type_tw_00420)
    :precondition (and
      (at ?v ?from)
      (connects ?r ?from ?to)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
      (not (location-blocked ?from))
      (not (location-blocked ?to))
    )
    :effect (and
      (not (at ?v ?from))
      (at ?v ?to)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-road_0070-tw_00450
    :parameters (?v - vehicle ?r - road_type_road_0070 ?from - loc_type_loc_0047 ?to - loc_type_loc_0097 ?w - window_type_tw_00450)
    :precondition (and
      (at ?v ?from)
      (connects ?r ?from ?to)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
      (not (location-blocked ?from))
      (not (location-blocked ?to))
    )
    :effect (and
      (not (at ?v ?from))
      (at ?v ?to)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-road_0071-tw_00000
    :parameters (?v - vehicle ?r - road_type_road_0071 ?from - loc_type_loc_0048 ?to - loc_type_loc_0081 ?w - window_type_tw_00000)
    :precondition (and
      (at ?v ?from)
      (connects ?r ?from ?to)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
      (not (location-blocked ?from))
      (not (location-blocked ?to))
    )
    :effect (and
      (not (at ?v ?from))
      (at ?v ?to)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-road_0071-tw_00030
    :parameters (?v - vehicle ?r - road_type_road_0071 ?from - loc_type_loc_0048 ?to - loc_type_loc_0081 ?w - window_type_tw_00030)
    :precondition (and
      (at ?v ?from)
      (connects ?r ?from ?to)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
      (not (location-blocked ?from))
      (not (location-blocked ?to))
    )
    :effect (and
      (not (at ?v ?from))
      (at ?v ?to)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-road_0071-tw_00060
    :parameters (?v - vehicle ?r - road_type_road_0071 ?from - loc_type_loc_0048 ?to - loc_type_loc_0081 ?w - window_type_tw_00060)
    :precondition (and
      (at ?v ?from)
      (connects ?r ?from ?to)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
      (not (location-blocked ?from))
      (not (location-blocked ?to))
    )
    :effect (and
      (not (at ?v ?from))
      (at ?v ?to)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-road_0071-tw_00090
    :parameters (?v - vehicle ?r - road_type_road_0071 ?from - loc_type_loc_0048 ?to - loc_type_loc_0081 ?w - window_type_tw_00090)
    :precondition (and
      (at ?v ?from)
      (connects ?r ?from ?to)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
      (not (location-blocked ?from))
      (not (location-blocked ?to))
    )
    :effect (and
      (not (at ?v ?from))
      (at ?v ?to)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-road_0071-tw_00120
    :parameters (?v - vehicle ?r - road_type_road_0071 ?from - loc_type_loc_0048 ?to - loc_type_loc_0081 ?w - window_type_tw_00120)
    :precondition (and
      (at ?v ?from)
      (connects ?r ?from ?to)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
      (not (location-blocked ?from))
      (not (location-blocked ?to))
    )
    :effect (and
      (not (at ?v ?from))
      (at ?v ?to)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-road_0071-tw_00150
    :parameters (?v - vehicle ?r - road_type_road_0071 ?from - loc_type_loc_0048 ?to - loc_type_loc_0081 ?w - window_type_tw_00150)
    :precondition (and
      (at ?v ?from)
      (connects ?r ?from ?to)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
      (not (location-blocked ?from))
      (not (location-blocked ?to))
    )
    :effect (and
      (not (at ?v ?from))
      (at ?v ?to)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-road_0071-tw_00180
    :parameters (?v - vehicle ?r - road_type_road_0071 ?from - loc_type_loc_0048 ?to - loc_type_loc_0081 ?w - window_type_tw_00180)
    :precondition (and
      (at ?v ?from)
      (connects ?r ?from ?to)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
      (not (location-blocked ?from))
      (not (location-blocked ?to))
    )
    :effect (and
      (not (at ?v ?from))
      (at ?v ?to)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-road_0071-tw_00210
    :parameters (?v - vehicle ?r - road_type_road_0071 ?from - loc_type_loc_0048 ?to - loc_type_loc_0081 ?w - window_type_tw_00210)
    :precondition (and
      (at ?v ?from)
      (connects ?r ?from ?to)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
      (not (location-blocked ?from))
      (not (location-blocked ?to))
    )
    :effect (and
      (not (at ?v ?from))
      (at ?v ?to)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-road_0071-tw_00240
    :parameters (?v - vehicle ?r - road_type_road_0071 ?from - loc_type_loc_0048 ?to - loc_type_loc_0081 ?w - window_type_tw_00240)
    :precondition (and
      (at ?v ?from)
      (connects ?r ?from ?to)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
      (not (location-blocked ?from))
      (not (location-blocked ?to))
    )
    :effect (and
      (not (at ?v ?from))
      (at ?v ?to)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-road_0071-tw_00270
    :parameters (?v - vehicle ?r - road_type_road_0071 ?from - loc_type_loc_0048 ?to - loc_type_loc_0081 ?w - window_type_tw_00270)
    :precondition (and
      (at ?v ?from)
      (connects ?r ?from ?to)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
      (not (location-blocked ?from))
      (not (location-blocked ?to))
    )
    :effect (and
      (not (at ?v ?from))
      (at ?v ?to)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-road_0071-tw_00300
    :parameters (?v - vehicle ?r - road_type_road_0071 ?from - loc_type_loc_0048 ?to - loc_type_loc_0081 ?w - window_type_tw_00300)
    :precondition (and
      (at ?v ?from)
      (connects ?r ?from ?to)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
      (not (location-blocked ?from))
      (not (location-blocked ?to))
    )
    :effect (and
      (not (at ?v ?from))
      (at ?v ?to)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-road_0071-tw_00330
    :parameters (?v - vehicle ?r - road_type_road_0071 ?from - loc_type_loc_0048 ?to - loc_type_loc_0081 ?w - window_type_tw_00330)
    :precondition (and
      (at ?v ?from)
      (connects ?r ?from ?to)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
      (not (location-blocked ?from))
      (not (location-blocked ?to))
    )
    :effect (and
      (not (at ?v ?from))
      (at ?v ?to)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-road_0071-tw_00360
    :parameters (?v - vehicle ?r - road_type_road_0071 ?from - loc_type_loc_0048 ?to - loc_type_loc_0081 ?w - window_type_tw_00360)
    :precondition (and
      (at ?v ?from)
      (connects ?r ?from ?to)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
      (not (location-blocked ?from))
      (not (location-blocked ?to))
    )
    :effect (and
      (not (at ?v ?from))
      (at ?v ?to)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-road_0071-tw_00390
    :parameters (?v - vehicle ?r - road_type_road_0071 ?from - loc_type_loc_0048 ?to - loc_type_loc_0081 ?w - window_type_tw_00390)
    :precondition (and
      (at ?v ?from)
      (connects ?r ?from ?to)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
      (not (location-blocked ?from))
      (not (location-blocked ?to))
    )
    :effect (and
      (not (at ?v ?from))
      (at ?v ?to)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-road_0071-tw_00420
    :parameters (?v - vehicle ?r - road_type_road_0071 ?from - loc_type_loc_0048 ?to - loc_type_loc_0081 ?w - window_type_tw_00420)
    :precondition (and
      (at ?v ?from)
      (connects ?r ?from ?to)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
      (not (location-blocked ?from))
      (not (location-blocked ?to))
    )
    :effect (and
      (not (at ?v ?from))
      (at ?v ?to)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-road_0071-tw_00450
    :parameters (?v - vehicle ?r - road_type_road_0071 ?from - loc_type_loc_0048 ?to - loc_type_loc_0081 ?w - window_type_tw_00450)
    :precondition (and
      (at ?v ?from)
      (connects ?r ?from ?to)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
      (not (location-blocked ?from))
      (not (location-blocked ?to))
    )
    :effect (and
      (not (at ?v ?from))
      (at ?v ?to)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-static-road_0072
    :parameters (?v - vehicle ?r - road_type_road_0072 ?from - loc_type_loc_0048 ?to - loc_type_loc_0078)
    :precondition (and
      (at ?v ?from)
      (connects ?r ?from ?to)
      (road-open ?r)
      (static-road ?r)
      (not (road-blocked ?r))
      (not (location-blocked ?from))
      (not (location-blocked ?to))
    )
    :effect (and
      (not (at ?v ?from))
      (at ?v ?to)
      (increase (travel-time ?v) (travel-duration ?r))
      (increase (sim-time) (travel-duration ?r))
    )
  )

  (:action traverse-road-dynamic-road_0073-tw_00000
    :parameters (?v - vehicle ?r - road_type_road_0073 ?from - loc_type_loc_0049 ?to - loc_type_loc_0093 ?w - window_type_tw_00000)
    :precondition (and
      (at ?v ?from)
      (connects ?r ?from ?to)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
      (not (location-blocked ?from))
      (not (location-blocked ?to))
    )
    :effect (and
      (not (at ?v ?from))
      (at ?v ?to)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-road_0073-tw_00030
    :parameters (?v - vehicle ?r - road_type_road_0073 ?from - loc_type_loc_0049 ?to - loc_type_loc_0093 ?w - window_type_tw_00030)
    :precondition (and
      (at ?v ?from)
      (connects ?r ?from ?to)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
      (not (location-blocked ?from))
      (not (location-blocked ?to))
    )
    :effect (and
      (not (at ?v ?from))
      (at ?v ?to)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-road_0073-tw_00060
    :parameters (?v - vehicle ?r - road_type_road_0073 ?from - loc_type_loc_0049 ?to - loc_type_loc_0093 ?w - window_type_tw_00060)
    :precondition (and
      (at ?v ?from)
      (connects ?r ?from ?to)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
      (not (location-blocked ?from))
      (not (location-blocked ?to))
    )
    :effect (and
      (not (at ?v ?from))
      (at ?v ?to)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-road_0073-tw_00090
    :parameters (?v - vehicle ?r - road_type_road_0073 ?from - loc_type_loc_0049 ?to - loc_type_loc_0093 ?w - window_type_tw_00090)
    :precondition (and
      (at ?v ?from)
      (connects ?r ?from ?to)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
      (not (location-blocked ?from))
      (not (location-blocked ?to))
    )
    :effect (and
      (not (at ?v ?from))
      (at ?v ?to)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-road_0073-tw_00120
    :parameters (?v - vehicle ?r - road_type_road_0073 ?from - loc_type_loc_0049 ?to - loc_type_loc_0093 ?w - window_type_tw_00120)
    :precondition (and
      (at ?v ?from)
      (connects ?r ?from ?to)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
      (not (location-blocked ?from))
      (not (location-blocked ?to))
    )
    :effect (and
      (not (at ?v ?from))
      (at ?v ?to)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-road_0073-tw_00150
    :parameters (?v - vehicle ?r - road_type_road_0073 ?from - loc_type_loc_0049 ?to - loc_type_loc_0093 ?w - window_type_tw_00150)
    :precondition (and
      (at ?v ?from)
      (connects ?r ?from ?to)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
      (not (location-blocked ?from))
      (not (location-blocked ?to))
    )
    :effect (and
      (not (at ?v ?from))
      (at ?v ?to)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-road_0073-tw_00180
    :parameters (?v - vehicle ?r - road_type_road_0073 ?from - loc_type_loc_0049 ?to - loc_type_loc_0093 ?w - window_type_tw_00180)
    :precondition (and
      (at ?v ?from)
      (connects ?r ?from ?to)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
      (not (location-blocked ?from))
      (not (location-blocked ?to))
    )
    :effect (and
      (not (at ?v ?from))
      (at ?v ?to)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-road_0073-tw_00210
    :parameters (?v - vehicle ?r - road_type_road_0073 ?from - loc_type_loc_0049 ?to - loc_type_loc_0093 ?w - window_type_tw_00210)
    :precondition (and
      (at ?v ?from)
      (connects ?r ?from ?to)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
      (not (location-blocked ?from))
      (not (location-blocked ?to))
    )
    :effect (and
      (not (at ?v ?from))
      (at ?v ?to)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-road_0073-tw_00240
    :parameters (?v - vehicle ?r - road_type_road_0073 ?from - loc_type_loc_0049 ?to - loc_type_loc_0093 ?w - window_type_tw_00240)
    :precondition (and
      (at ?v ?from)
      (connects ?r ?from ?to)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
      (not (location-blocked ?from))
      (not (location-blocked ?to))
    )
    :effect (and
      (not (at ?v ?from))
      (at ?v ?to)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-road_0073-tw_00270
    :parameters (?v - vehicle ?r - road_type_road_0073 ?from - loc_type_loc_0049 ?to - loc_type_loc_0093 ?w - window_type_tw_00270)
    :precondition (and
      (at ?v ?from)
      (connects ?r ?from ?to)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
      (not (location-blocked ?from))
      (not (location-blocked ?to))
    )
    :effect (and
      (not (at ?v ?from))
      (at ?v ?to)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-road_0073-tw_00300
    :parameters (?v - vehicle ?r - road_type_road_0073 ?from - loc_type_loc_0049 ?to - loc_type_loc_0093 ?w - window_type_tw_00300)
    :precondition (and
      (at ?v ?from)
      (connects ?r ?from ?to)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
      (not (location-blocked ?from))
      (not (location-blocked ?to))
    )
    :effect (and
      (not (at ?v ?from))
      (at ?v ?to)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-road_0073-tw_00330
    :parameters (?v - vehicle ?r - road_type_road_0073 ?from - loc_type_loc_0049 ?to - loc_type_loc_0093 ?w - window_type_tw_00330)
    :precondition (and
      (at ?v ?from)
      (connects ?r ?from ?to)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
      (not (location-blocked ?from))
      (not (location-blocked ?to))
    )
    :effect (and
      (not (at ?v ?from))
      (at ?v ?to)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-road_0073-tw_00360
    :parameters (?v - vehicle ?r - road_type_road_0073 ?from - loc_type_loc_0049 ?to - loc_type_loc_0093 ?w - window_type_tw_00360)
    :precondition (and
      (at ?v ?from)
      (connects ?r ?from ?to)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
      (not (location-blocked ?from))
      (not (location-blocked ?to))
    )
    :effect (and
      (not (at ?v ?from))
      (at ?v ?to)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-road_0073-tw_00390
    :parameters (?v - vehicle ?r - road_type_road_0073 ?from - loc_type_loc_0049 ?to - loc_type_loc_0093 ?w - window_type_tw_00390)
    :precondition (and
      (at ?v ?from)
      (connects ?r ?from ?to)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
      (not (location-blocked ?from))
      (not (location-blocked ?to))
    )
    :effect (and
      (not (at ?v ?from))
      (at ?v ?to)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-road_0073-tw_00420
    :parameters (?v - vehicle ?r - road_type_road_0073 ?from - loc_type_loc_0049 ?to - loc_type_loc_0093 ?w - window_type_tw_00420)
    :precondition (and
      (at ?v ?from)
      (connects ?r ?from ?to)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
      (not (location-blocked ?from))
      (not (location-blocked ?to))
    )
    :effect (and
      (not (at ?v ?from))
      (at ?v ?to)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-road_0073-tw_00450
    :parameters (?v - vehicle ?r - road_type_road_0073 ?from - loc_type_loc_0049 ?to - loc_type_loc_0093 ?w - window_type_tw_00450)
    :precondition (and
      (at ?v ?from)
      (connects ?r ?from ?to)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
      (not (location-blocked ?from))
      (not (location-blocked ?to))
    )
    :effect (and
      (not (at ?v ?from))
      (at ?v ?to)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-road_0075-tw_00000
    :parameters (?v - vehicle ?r - road_type_road_0075 ?from - loc_type_loc_0050 ?to - loc_type_loc_0005 ?w - window_type_tw_00000)
    :precondition (and
      (at ?v ?from)
      (connects ?r ?from ?to)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
      (not (location-blocked ?from))
      (not (location-blocked ?to))
    )
    :effect (and
      (not (at ?v ?from))
      (at ?v ?to)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-road_0075-tw_00030
    :parameters (?v - vehicle ?r - road_type_road_0075 ?from - loc_type_loc_0050 ?to - loc_type_loc_0005 ?w - window_type_tw_00030)
    :precondition (and
      (at ?v ?from)
      (connects ?r ?from ?to)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
      (not (location-blocked ?from))
      (not (location-blocked ?to))
    )
    :effect (and
      (not (at ?v ?from))
      (at ?v ?to)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-road_0075-tw_00060
    :parameters (?v - vehicle ?r - road_type_road_0075 ?from - loc_type_loc_0050 ?to - loc_type_loc_0005 ?w - window_type_tw_00060)
    :precondition (and
      (at ?v ?from)
      (connects ?r ?from ?to)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
      (not (location-blocked ?from))
      (not (location-blocked ?to))
    )
    :effect (and
      (not (at ?v ?from))
      (at ?v ?to)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-road_0075-tw_00090
    :parameters (?v - vehicle ?r - road_type_road_0075 ?from - loc_type_loc_0050 ?to - loc_type_loc_0005 ?w - window_type_tw_00090)
    :precondition (and
      (at ?v ?from)
      (connects ?r ?from ?to)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
      (not (location-blocked ?from))
      (not (location-blocked ?to))
    )
    :effect (and
      (not (at ?v ?from))
      (at ?v ?to)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-road_0075-tw_00120
    :parameters (?v - vehicle ?r - road_type_road_0075 ?from - loc_type_loc_0050 ?to - loc_type_loc_0005 ?w - window_type_tw_00120)
    :precondition (and
      (at ?v ?from)
      (connects ?r ?from ?to)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
      (not (location-blocked ?from))
      (not (location-blocked ?to))
    )
    :effect (and
      (not (at ?v ?from))
      (at ?v ?to)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-road_0075-tw_00150
    :parameters (?v - vehicle ?r - road_type_road_0075 ?from - loc_type_loc_0050 ?to - loc_type_loc_0005 ?w - window_type_tw_00150)
    :precondition (and
      (at ?v ?from)
      (connects ?r ?from ?to)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
      (not (location-blocked ?from))
      (not (location-blocked ?to))
    )
    :effect (and
      (not (at ?v ?from))
      (at ?v ?to)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-road_0075-tw_00180
    :parameters (?v - vehicle ?r - road_type_road_0075 ?from - loc_type_loc_0050 ?to - loc_type_loc_0005 ?w - window_type_tw_00180)
    :precondition (and
      (at ?v ?from)
      (connects ?r ?from ?to)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
      (not (location-blocked ?from))
      (not (location-blocked ?to))
    )
    :effect (and
      (not (at ?v ?from))
      (at ?v ?to)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-road_0075-tw_00210
    :parameters (?v - vehicle ?r - road_type_road_0075 ?from - loc_type_loc_0050 ?to - loc_type_loc_0005 ?w - window_type_tw_00210)
    :precondition (and
      (at ?v ?from)
      (connects ?r ?from ?to)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
      (not (location-blocked ?from))
      (not (location-blocked ?to))
    )
    :effect (and
      (not (at ?v ?from))
      (at ?v ?to)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-road_0075-tw_00240
    :parameters (?v - vehicle ?r - road_type_road_0075 ?from - loc_type_loc_0050 ?to - loc_type_loc_0005 ?w - window_type_tw_00240)
    :precondition (and
      (at ?v ?from)
      (connects ?r ?from ?to)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
      (not (location-blocked ?from))
      (not (location-blocked ?to))
    )
    :effect (and
      (not (at ?v ?from))
      (at ?v ?to)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-road_0075-tw_00270
    :parameters (?v - vehicle ?r - road_type_road_0075 ?from - loc_type_loc_0050 ?to - loc_type_loc_0005 ?w - window_type_tw_00270)
    :precondition (and
      (at ?v ?from)
      (connects ?r ?from ?to)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
      (not (location-blocked ?from))
      (not (location-blocked ?to))
    )
    :effect (and
      (not (at ?v ?from))
      (at ?v ?to)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-road_0075-tw_00300
    :parameters (?v - vehicle ?r - road_type_road_0075 ?from - loc_type_loc_0050 ?to - loc_type_loc_0005 ?w - window_type_tw_00300)
    :precondition (and
      (at ?v ?from)
      (connects ?r ?from ?to)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
      (not (location-blocked ?from))
      (not (location-blocked ?to))
    )
    :effect (and
      (not (at ?v ?from))
      (at ?v ?to)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-road_0075-tw_00330
    :parameters (?v - vehicle ?r - road_type_road_0075 ?from - loc_type_loc_0050 ?to - loc_type_loc_0005 ?w - window_type_tw_00330)
    :precondition (and
      (at ?v ?from)
      (connects ?r ?from ?to)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
      (not (location-blocked ?from))
      (not (location-blocked ?to))
    )
    :effect (and
      (not (at ?v ?from))
      (at ?v ?to)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-road_0075-tw_00360
    :parameters (?v - vehicle ?r - road_type_road_0075 ?from - loc_type_loc_0050 ?to - loc_type_loc_0005 ?w - window_type_tw_00360)
    :precondition (and
      (at ?v ?from)
      (connects ?r ?from ?to)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
      (not (location-blocked ?from))
      (not (location-blocked ?to))
    )
    :effect (and
      (not (at ?v ?from))
      (at ?v ?to)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-road_0075-tw_00390
    :parameters (?v - vehicle ?r - road_type_road_0075 ?from - loc_type_loc_0050 ?to - loc_type_loc_0005 ?w - window_type_tw_00390)
    :precondition (and
      (at ?v ?from)
      (connects ?r ?from ?to)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
      (not (location-blocked ?from))
      (not (location-blocked ?to))
    )
    :effect (and
      (not (at ?v ?from))
      (at ?v ?to)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-road_0075-tw_00420
    :parameters (?v - vehicle ?r - road_type_road_0075 ?from - loc_type_loc_0050 ?to - loc_type_loc_0005 ?w - window_type_tw_00420)
    :precondition (and
      (at ?v ?from)
      (connects ?r ?from ?to)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
      (not (location-blocked ?from))
      (not (location-blocked ?to))
    )
    :effect (and
      (not (at ?v ?from))
      (at ?v ?to)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-road_0075-tw_00450
    :parameters (?v - vehicle ?r - road_type_road_0075 ?from - loc_type_loc_0050 ?to - loc_type_loc_0005 ?w - window_type_tw_00450)
    :precondition (and
      (at ?v ?from)
      (connects ?r ?from ?to)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
      (not (location-blocked ?from))
      (not (location-blocked ?to))
    )
    :effect (and
      (not (at ?v ?from))
      (at ?v ?to)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-road_0076-tw_00000
    :parameters (?v - vehicle ?r - road_type_road_0076 ?from - loc_type_loc_0051 ?to - loc_type_loc_0028 ?w - window_type_tw_00000)
    :precondition (and
      (at ?v ?from)
      (connects ?r ?from ?to)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
      (not (location-blocked ?from))
      (not (location-blocked ?to))
    )
    :effect (and
      (not (at ?v ?from))
      (at ?v ?to)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-road_0076-tw_00030
    :parameters (?v - vehicle ?r - road_type_road_0076 ?from - loc_type_loc_0051 ?to - loc_type_loc_0028 ?w - window_type_tw_00030)
    :precondition (and
      (at ?v ?from)
      (connects ?r ?from ?to)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
      (not (location-blocked ?from))
      (not (location-blocked ?to))
    )
    :effect (and
      (not (at ?v ?from))
      (at ?v ?to)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-road_0076-tw_00060
    :parameters (?v - vehicle ?r - road_type_road_0076 ?from - loc_type_loc_0051 ?to - loc_type_loc_0028 ?w - window_type_tw_00060)
    :precondition (and
      (at ?v ?from)
      (connects ?r ?from ?to)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
      (not (location-blocked ?from))
      (not (location-blocked ?to))
    )
    :effect (and
      (not (at ?v ?from))
      (at ?v ?to)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-road_0076-tw_00090
    :parameters (?v - vehicle ?r - road_type_road_0076 ?from - loc_type_loc_0051 ?to - loc_type_loc_0028 ?w - window_type_tw_00090)
    :precondition (and
      (at ?v ?from)
      (connects ?r ?from ?to)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
      (not (location-blocked ?from))
      (not (location-blocked ?to))
    )
    :effect (and
      (not (at ?v ?from))
      (at ?v ?to)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-road_0076-tw_00120
    :parameters (?v - vehicle ?r - road_type_road_0076 ?from - loc_type_loc_0051 ?to - loc_type_loc_0028 ?w - window_type_tw_00120)
    :precondition (and
      (at ?v ?from)
      (connects ?r ?from ?to)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
      (not (location-blocked ?from))
      (not (location-blocked ?to))
    )
    :effect (and
      (not (at ?v ?from))
      (at ?v ?to)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-road_0076-tw_00150
    :parameters (?v - vehicle ?r - road_type_road_0076 ?from - loc_type_loc_0051 ?to - loc_type_loc_0028 ?w - window_type_tw_00150)
    :precondition (and
      (at ?v ?from)
      (connects ?r ?from ?to)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
      (not (location-blocked ?from))
      (not (location-blocked ?to))
    )
    :effect (and
      (not (at ?v ?from))
      (at ?v ?to)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-road_0076-tw_00180
    :parameters (?v - vehicle ?r - road_type_road_0076 ?from - loc_type_loc_0051 ?to - loc_type_loc_0028 ?w - window_type_tw_00180)
    :precondition (and
      (at ?v ?from)
      (connects ?r ?from ?to)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
      (not (location-blocked ?from))
      (not (location-blocked ?to))
    )
    :effect (and
      (not (at ?v ?from))
      (at ?v ?to)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-road_0076-tw_00210
    :parameters (?v - vehicle ?r - road_type_road_0076 ?from - loc_type_loc_0051 ?to - loc_type_loc_0028 ?w - window_type_tw_00210)
    :precondition (and
      (at ?v ?from)
      (connects ?r ?from ?to)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
      (not (location-blocked ?from))
      (not (location-blocked ?to))
    )
    :effect (and
      (not (at ?v ?from))
      (at ?v ?to)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-road_0076-tw_00240
    :parameters (?v - vehicle ?r - road_type_road_0076 ?from - loc_type_loc_0051 ?to - loc_type_loc_0028 ?w - window_type_tw_00240)
    :precondition (and
      (at ?v ?from)
      (connects ?r ?from ?to)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
      (not (location-blocked ?from))
      (not (location-blocked ?to))
    )
    :effect (and
      (not (at ?v ?from))
      (at ?v ?to)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-road_0076-tw_00270
    :parameters (?v - vehicle ?r - road_type_road_0076 ?from - loc_type_loc_0051 ?to - loc_type_loc_0028 ?w - window_type_tw_00270)
    :precondition (and
      (at ?v ?from)
      (connects ?r ?from ?to)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
      (not (location-blocked ?from))
      (not (location-blocked ?to))
    )
    :effect (and
      (not (at ?v ?from))
      (at ?v ?to)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-road_0076-tw_00300
    :parameters (?v - vehicle ?r - road_type_road_0076 ?from - loc_type_loc_0051 ?to - loc_type_loc_0028 ?w - window_type_tw_00300)
    :precondition (and
      (at ?v ?from)
      (connects ?r ?from ?to)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
      (not (location-blocked ?from))
      (not (location-blocked ?to))
    )
    :effect (and
      (not (at ?v ?from))
      (at ?v ?to)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-road_0076-tw_00330
    :parameters (?v - vehicle ?r - road_type_road_0076 ?from - loc_type_loc_0051 ?to - loc_type_loc_0028 ?w - window_type_tw_00330)
    :precondition (and
      (at ?v ?from)
      (connects ?r ?from ?to)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
      (not (location-blocked ?from))
      (not (location-blocked ?to))
    )
    :effect (and
      (not (at ?v ?from))
      (at ?v ?to)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-road_0076-tw_00360
    :parameters (?v - vehicle ?r - road_type_road_0076 ?from - loc_type_loc_0051 ?to - loc_type_loc_0028 ?w - window_type_tw_00360)
    :precondition (and
      (at ?v ?from)
      (connects ?r ?from ?to)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
      (not (location-blocked ?from))
      (not (location-blocked ?to))
    )
    :effect (and
      (not (at ?v ?from))
      (at ?v ?to)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-road_0076-tw_00390
    :parameters (?v - vehicle ?r - road_type_road_0076 ?from - loc_type_loc_0051 ?to - loc_type_loc_0028 ?w - window_type_tw_00390)
    :precondition (and
      (at ?v ?from)
      (connects ?r ?from ?to)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
      (not (location-blocked ?from))
      (not (location-blocked ?to))
    )
    :effect (and
      (not (at ?v ?from))
      (at ?v ?to)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-road_0076-tw_00420
    :parameters (?v - vehicle ?r - road_type_road_0076 ?from - loc_type_loc_0051 ?to - loc_type_loc_0028 ?w - window_type_tw_00420)
    :precondition (and
      (at ?v ?from)
      (connects ?r ?from ?to)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
      (not (location-blocked ?from))
      (not (location-blocked ?to))
    )
    :effect (and
      (not (at ?v ?from))
      (at ?v ?to)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-road_0076-tw_00450
    :parameters (?v - vehicle ?r - road_type_road_0076 ?from - loc_type_loc_0051 ?to - loc_type_loc_0028 ?w - window_type_tw_00450)
    :precondition (and
      (at ?v ?from)
      (connects ?r ?from ?to)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
      (not (location-blocked ?from))
      (not (location-blocked ?to))
    )
    :effect (and
      (not (at ?v ?from))
      (at ?v ?to)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-road_0077-tw_00000
    :parameters (?v - vehicle ?r - road_type_road_0077 ?from - loc_type_loc_0052 ?to - loc_type_loc_0040 ?w - window_type_tw_00000)
    :precondition (and
      (at ?v ?from)
      (connects ?r ?from ?to)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
      (not (location-blocked ?from))
      (not (location-blocked ?to))
    )
    :effect (and
      (not (at ?v ?from))
      (at ?v ?to)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-road_0077-tw_00030
    :parameters (?v - vehicle ?r - road_type_road_0077 ?from - loc_type_loc_0052 ?to - loc_type_loc_0040 ?w - window_type_tw_00030)
    :precondition (and
      (at ?v ?from)
      (connects ?r ?from ?to)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
      (not (location-blocked ?from))
      (not (location-blocked ?to))
    )
    :effect (and
      (not (at ?v ?from))
      (at ?v ?to)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-road_0077-tw_00060
    :parameters (?v - vehicle ?r - road_type_road_0077 ?from - loc_type_loc_0052 ?to - loc_type_loc_0040 ?w - window_type_tw_00060)
    :precondition (and
      (at ?v ?from)
      (connects ?r ?from ?to)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
      (not (location-blocked ?from))
      (not (location-blocked ?to))
    )
    :effect (and
      (not (at ?v ?from))
      (at ?v ?to)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-road_0077-tw_00090
    :parameters (?v - vehicle ?r - road_type_road_0077 ?from - loc_type_loc_0052 ?to - loc_type_loc_0040 ?w - window_type_tw_00090)
    :precondition (and
      (at ?v ?from)
      (connects ?r ?from ?to)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
      (not (location-blocked ?from))
      (not (location-blocked ?to))
    )
    :effect (and
      (not (at ?v ?from))
      (at ?v ?to)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-road_0077-tw_00120
    :parameters (?v - vehicle ?r - road_type_road_0077 ?from - loc_type_loc_0052 ?to - loc_type_loc_0040 ?w - window_type_tw_00120)
    :precondition (and
      (at ?v ?from)
      (connects ?r ?from ?to)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
      (not (location-blocked ?from))
      (not (location-blocked ?to))
    )
    :effect (and
      (not (at ?v ?from))
      (at ?v ?to)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-road_0077-tw_00150
    :parameters (?v - vehicle ?r - road_type_road_0077 ?from - loc_type_loc_0052 ?to - loc_type_loc_0040 ?w - window_type_tw_00150)
    :precondition (and
      (at ?v ?from)
      (connects ?r ?from ?to)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
      (not (location-blocked ?from))
      (not (location-blocked ?to))
    )
    :effect (and
      (not (at ?v ?from))
      (at ?v ?to)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-road_0077-tw_00180
    :parameters (?v - vehicle ?r - road_type_road_0077 ?from - loc_type_loc_0052 ?to - loc_type_loc_0040 ?w - window_type_tw_00180)
    :precondition (and
      (at ?v ?from)
      (connects ?r ?from ?to)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
      (not (location-blocked ?from))
      (not (location-blocked ?to))
    )
    :effect (and
      (not (at ?v ?from))
      (at ?v ?to)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-road_0077-tw_00210
    :parameters (?v - vehicle ?r - road_type_road_0077 ?from - loc_type_loc_0052 ?to - loc_type_loc_0040 ?w - window_type_tw_00210)
    :precondition (and
      (at ?v ?from)
      (connects ?r ?from ?to)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
      (not (location-blocked ?from))
      (not (location-blocked ?to))
    )
    :effect (and
      (not (at ?v ?from))
      (at ?v ?to)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-road_0077-tw_00240
    :parameters (?v - vehicle ?r - road_type_road_0077 ?from - loc_type_loc_0052 ?to - loc_type_loc_0040 ?w - window_type_tw_00240)
    :precondition (and
      (at ?v ?from)
      (connects ?r ?from ?to)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
      (not (location-blocked ?from))
      (not (location-blocked ?to))
    )
    :effect (and
      (not (at ?v ?from))
      (at ?v ?to)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-road_0077-tw_00270
    :parameters (?v - vehicle ?r - road_type_road_0077 ?from - loc_type_loc_0052 ?to - loc_type_loc_0040 ?w - window_type_tw_00270)
    :precondition (and
      (at ?v ?from)
      (connects ?r ?from ?to)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
      (not (location-blocked ?from))
      (not (location-blocked ?to))
    )
    :effect (and
      (not (at ?v ?from))
      (at ?v ?to)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-road_0077-tw_00300
    :parameters (?v - vehicle ?r - road_type_road_0077 ?from - loc_type_loc_0052 ?to - loc_type_loc_0040 ?w - window_type_tw_00300)
    :precondition (and
      (at ?v ?from)
      (connects ?r ?from ?to)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
      (not (location-blocked ?from))
      (not (location-blocked ?to))
    )
    :effect (and
      (not (at ?v ?from))
      (at ?v ?to)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-road_0077-tw_00330
    :parameters (?v - vehicle ?r - road_type_road_0077 ?from - loc_type_loc_0052 ?to - loc_type_loc_0040 ?w - window_type_tw_00330)
    :precondition (and
      (at ?v ?from)
      (connects ?r ?from ?to)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
      (not (location-blocked ?from))
      (not (location-blocked ?to))
    )
    :effect (and
      (not (at ?v ?from))
      (at ?v ?to)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-road_0077-tw_00360
    :parameters (?v - vehicle ?r - road_type_road_0077 ?from - loc_type_loc_0052 ?to - loc_type_loc_0040 ?w - window_type_tw_00360)
    :precondition (and
      (at ?v ?from)
      (connects ?r ?from ?to)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
      (not (location-blocked ?from))
      (not (location-blocked ?to))
    )
    :effect (and
      (not (at ?v ?from))
      (at ?v ?to)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-road_0077-tw_00390
    :parameters (?v - vehicle ?r - road_type_road_0077 ?from - loc_type_loc_0052 ?to - loc_type_loc_0040 ?w - window_type_tw_00390)
    :precondition (and
      (at ?v ?from)
      (connects ?r ?from ?to)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
      (not (location-blocked ?from))
      (not (location-blocked ?to))
    )
    :effect (and
      (not (at ?v ?from))
      (at ?v ?to)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-road_0077-tw_00420
    :parameters (?v - vehicle ?r - road_type_road_0077 ?from - loc_type_loc_0052 ?to - loc_type_loc_0040 ?w - window_type_tw_00420)
    :precondition (and
      (at ?v ?from)
      (connects ?r ?from ?to)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
      (not (location-blocked ?from))
      (not (location-blocked ?to))
    )
    :effect (and
      (not (at ?v ?from))
      (at ?v ?to)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-road_0077-tw_00450
    :parameters (?v - vehicle ?r - road_type_road_0077 ?from - loc_type_loc_0052 ?to - loc_type_loc_0040 ?w - window_type_tw_00450)
    :precondition (and
      (at ?v ?from)
      (connects ?r ?from ?to)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
      (not (location-blocked ?from))
      (not (location-blocked ?to))
    )
    :effect (and
      (not (at ?v ?from))
      (at ?v ?to)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-road_0078-tw_00000
    :parameters (?v - vehicle ?r - road_type_road_0078 ?from - loc_type_loc_0053 ?to - loc_type_loc_0049 ?w - window_type_tw_00000)
    :precondition (and
      (at ?v ?from)
      (connects ?r ?from ?to)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
      (not (location-blocked ?from))
      (not (location-blocked ?to))
    )
    :effect (and
      (not (at ?v ?from))
      (at ?v ?to)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-road_0078-tw_00030
    :parameters (?v - vehicle ?r - road_type_road_0078 ?from - loc_type_loc_0053 ?to - loc_type_loc_0049 ?w - window_type_tw_00030)
    :precondition (and
      (at ?v ?from)
      (connects ?r ?from ?to)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
      (not (location-blocked ?from))
      (not (location-blocked ?to))
    )
    :effect (and
      (not (at ?v ?from))
      (at ?v ?to)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-road_0078-tw_00060
    :parameters (?v - vehicle ?r - road_type_road_0078 ?from - loc_type_loc_0053 ?to - loc_type_loc_0049 ?w - window_type_tw_00060)
    :precondition (and
      (at ?v ?from)
      (connects ?r ?from ?to)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
      (not (location-blocked ?from))
      (not (location-blocked ?to))
    )
    :effect (and
      (not (at ?v ?from))
      (at ?v ?to)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-road_0078-tw_00090
    :parameters (?v - vehicle ?r - road_type_road_0078 ?from - loc_type_loc_0053 ?to - loc_type_loc_0049 ?w - window_type_tw_00090)
    :precondition (and
      (at ?v ?from)
      (connects ?r ?from ?to)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
      (not (location-blocked ?from))
      (not (location-blocked ?to))
    )
    :effect (and
      (not (at ?v ?from))
      (at ?v ?to)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-road_0078-tw_00120
    :parameters (?v - vehicle ?r - road_type_road_0078 ?from - loc_type_loc_0053 ?to - loc_type_loc_0049 ?w - window_type_tw_00120)
    :precondition (and
      (at ?v ?from)
      (connects ?r ?from ?to)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
      (not (location-blocked ?from))
      (not (location-blocked ?to))
    )
    :effect (and
      (not (at ?v ?from))
      (at ?v ?to)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-road_0078-tw_00150
    :parameters (?v - vehicle ?r - road_type_road_0078 ?from - loc_type_loc_0053 ?to - loc_type_loc_0049 ?w - window_type_tw_00150)
    :precondition (and
      (at ?v ?from)
      (connects ?r ?from ?to)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
      (not (location-blocked ?from))
      (not (location-blocked ?to))
    )
    :effect (and
      (not (at ?v ?from))
      (at ?v ?to)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-road_0078-tw_00180
    :parameters (?v - vehicle ?r - road_type_road_0078 ?from - loc_type_loc_0053 ?to - loc_type_loc_0049 ?w - window_type_tw_00180)
    :precondition (and
      (at ?v ?from)
      (connects ?r ?from ?to)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
      (not (location-blocked ?from))
      (not (location-blocked ?to))
    )
    :effect (and
      (not (at ?v ?from))
      (at ?v ?to)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-road_0078-tw_00210
    :parameters (?v - vehicle ?r - road_type_road_0078 ?from - loc_type_loc_0053 ?to - loc_type_loc_0049 ?w - window_type_tw_00210)
    :precondition (and
      (at ?v ?from)
      (connects ?r ?from ?to)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
      (not (location-blocked ?from))
      (not (location-blocked ?to))
    )
    :effect (and
      (not (at ?v ?from))
      (at ?v ?to)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-road_0078-tw_00240
    :parameters (?v - vehicle ?r - road_type_road_0078 ?from - loc_type_loc_0053 ?to - loc_type_loc_0049 ?w - window_type_tw_00240)
    :precondition (and
      (at ?v ?from)
      (connects ?r ?from ?to)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
      (not (location-blocked ?from))
      (not (location-blocked ?to))
    )
    :effect (and
      (not (at ?v ?from))
      (at ?v ?to)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-road_0078-tw_00270
    :parameters (?v - vehicle ?r - road_type_road_0078 ?from - loc_type_loc_0053 ?to - loc_type_loc_0049 ?w - window_type_tw_00270)
    :precondition (and
      (at ?v ?from)
      (connects ?r ?from ?to)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
      (not (location-blocked ?from))
      (not (location-blocked ?to))
    )
    :effect (and
      (not (at ?v ?from))
      (at ?v ?to)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-road_0078-tw_00300
    :parameters (?v - vehicle ?r - road_type_road_0078 ?from - loc_type_loc_0053 ?to - loc_type_loc_0049 ?w - window_type_tw_00300)
    :precondition (and
      (at ?v ?from)
      (connects ?r ?from ?to)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
      (not (location-blocked ?from))
      (not (location-blocked ?to))
    )
    :effect (and
      (not (at ?v ?from))
      (at ?v ?to)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-road_0078-tw_00330
    :parameters (?v - vehicle ?r - road_type_road_0078 ?from - loc_type_loc_0053 ?to - loc_type_loc_0049 ?w - window_type_tw_00330)
    :precondition (and
      (at ?v ?from)
      (connects ?r ?from ?to)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
      (not (location-blocked ?from))
      (not (location-blocked ?to))
    )
    :effect (and
      (not (at ?v ?from))
      (at ?v ?to)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-road_0078-tw_00360
    :parameters (?v - vehicle ?r - road_type_road_0078 ?from - loc_type_loc_0053 ?to - loc_type_loc_0049 ?w - window_type_tw_00360)
    :precondition (and
      (at ?v ?from)
      (connects ?r ?from ?to)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
      (not (location-blocked ?from))
      (not (location-blocked ?to))
    )
    :effect (and
      (not (at ?v ?from))
      (at ?v ?to)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-road_0078-tw_00390
    :parameters (?v - vehicle ?r - road_type_road_0078 ?from - loc_type_loc_0053 ?to - loc_type_loc_0049 ?w - window_type_tw_00390)
    :precondition (and
      (at ?v ?from)
      (connects ?r ?from ?to)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
      (not (location-blocked ?from))
      (not (location-blocked ?to))
    )
    :effect (and
      (not (at ?v ?from))
      (at ?v ?to)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-road_0078-tw_00420
    :parameters (?v - vehicle ?r - road_type_road_0078 ?from - loc_type_loc_0053 ?to - loc_type_loc_0049 ?w - window_type_tw_00420)
    :precondition (and
      (at ?v ?from)
      (connects ?r ?from ?to)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
      (not (location-blocked ?from))
      (not (location-blocked ?to))
    )
    :effect (and
      (not (at ?v ?from))
      (at ?v ?to)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-road_0078-tw_00450
    :parameters (?v - vehicle ?r - road_type_road_0078 ?from - loc_type_loc_0053 ?to - loc_type_loc_0049 ?w - window_type_tw_00450)
    :precondition (and
      (at ?v ?from)
      (connects ?r ?from ?to)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
      (not (location-blocked ?from))
      (not (location-blocked ?to))
    )
    :effect (and
      (not (at ?v ?from))
      (at ?v ?to)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-road_0079-tw_00000
    :parameters (?v - vehicle ?r - road_type_road_0079 ?from - loc_type_loc_0053 ?to - loc_type_loc_0008 ?w - window_type_tw_00000)
    :precondition (and
      (at ?v ?from)
      (connects ?r ?from ?to)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
      (not (location-blocked ?from))
      (not (location-blocked ?to))
    )
    :effect (and
      (not (at ?v ?from))
      (at ?v ?to)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-road_0079-tw_00030
    :parameters (?v - vehicle ?r - road_type_road_0079 ?from - loc_type_loc_0053 ?to - loc_type_loc_0008 ?w - window_type_tw_00030)
    :precondition (and
      (at ?v ?from)
      (connects ?r ?from ?to)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
      (not (location-blocked ?from))
      (not (location-blocked ?to))
    )
    :effect (and
      (not (at ?v ?from))
      (at ?v ?to)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-road_0079-tw_00060
    :parameters (?v - vehicle ?r - road_type_road_0079 ?from - loc_type_loc_0053 ?to - loc_type_loc_0008 ?w - window_type_tw_00060)
    :precondition (and
      (at ?v ?from)
      (connects ?r ?from ?to)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
      (not (location-blocked ?from))
      (not (location-blocked ?to))
    )
    :effect (and
      (not (at ?v ?from))
      (at ?v ?to)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-road_0079-tw_00090
    :parameters (?v - vehicle ?r - road_type_road_0079 ?from - loc_type_loc_0053 ?to - loc_type_loc_0008 ?w - window_type_tw_00090)
    :precondition (and
      (at ?v ?from)
      (connects ?r ?from ?to)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
      (not (location-blocked ?from))
      (not (location-blocked ?to))
    )
    :effect (and
      (not (at ?v ?from))
      (at ?v ?to)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-road_0079-tw_00120
    :parameters (?v - vehicle ?r - road_type_road_0079 ?from - loc_type_loc_0053 ?to - loc_type_loc_0008 ?w - window_type_tw_00120)
    :precondition (and
      (at ?v ?from)
      (connects ?r ?from ?to)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
      (not (location-blocked ?from))
      (not (location-blocked ?to))
    )
    :effect (and
      (not (at ?v ?from))
      (at ?v ?to)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-road_0079-tw_00150
    :parameters (?v - vehicle ?r - road_type_road_0079 ?from - loc_type_loc_0053 ?to - loc_type_loc_0008 ?w - window_type_tw_00150)
    :precondition (and
      (at ?v ?from)
      (connects ?r ?from ?to)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
      (not (location-blocked ?from))
      (not (location-blocked ?to))
    )
    :effect (and
      (not (at ?v ?from))
      (at ?v ?to)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-road_0079-tw_00180
    :parameters (?v - vehicle ?r - road_type_road_0079 ?from - loc_type_loc_0053 ?to - loc_type_loc_0008 ?w - window_type_tw_00180)
    :precondition (and
      (at ?v ?from)
      (connects ?r ?from ?to)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
      (not (location-blocked ?from))
      (not (location-blocked ?to))
    )
    :effect (and
      (not (at ?v ?from))
      (at ?v ?to)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-road_0079-tw_00210
    :parameters (?v - vehicle ?r - road_type_road_0079 ?from - loc_type_loc_0053 ?to - loc_type_loc_0008 ?w - window_type_tw_00210)
    :precondition (and
      (at ?v ?from)
      (connects ?r ?from ?to)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
      (not (location-blocked ?from))
      (not (location-blocked ?to))
    )
    :effect (and
      (not (at ?v ?from))
      (at ?v ?to)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-road_0079-tw_00240
    :parameters (?v - vehicle ?r - road_type_road_0079 ?from - loc_type_loc_0053 ?to - loc_type_loc_0008 ?w - window_type_tw_00240)
    :precondition (and
      (at ?v ?from)
      (connects ?r ?from ?to)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
      (not (location-blocked ?from))
      (not (location-blocked ?to))
    )
    :effect (and
      (not (at ?v ?from))
      (at ?v ?to)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-road_0079-tw_00270
    :parameters (?v - vehicle ?r - road_type_road_0079 ?from - loc_type_loc_0053 ?to - loc_type_loc_0008 ?w - window_type_tw_00270)
    :precondition (and
      (at ?v ?from)
      (connects ?r ?from ?to)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
      (not (location-blocked ?from))
      (not (location-blocked ?to))
    )
    :effect (and
      (not (at ?v ?from))
      (at ?v ?to)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-road_0079-tw_00300
    :parameters (?v - vehicle ?r - road_type_road_0079 ?from - loc_type_loc_0053 ?to - loc_type_loc_0008 ?w - window_type_tw_00300)
    :precondition (and
      (at ?v ?from)
      (connects ?r ?from ?to)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
      (not (location-blocked ?from))
      (not (location-blocked ?to))
    )
    :effect (and
      (not (at ?v ?from))
      (at ?v ?to)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-road_0079-tw_00330
    :parameters (?v - vehicle ?r - road_type_road_0079 ?from - loc_type_loc_0053 ?to - loc_type_loc_0008 ?w - window_type_tw_00330)
    :precondition (and
      (at ?v ?from)
      (connects ?r ?from ?to)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
      (not (location-blocked ?from))
      (not (location-blocked ?to))
    )
    :effect (and
      (not (at ?v ?from))
      (at ?v ?to)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-road_0079-tw_00360
    :parameters (?v - vehicle ?r - road_type_road_0079 ?from - loc_type_loc_0053 ?to - loc_type_loc_0008 ?w - window_type_tw_00360)
    :precondition (and
      (at ?v ?from)
      (connects ?r ?from ?to)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
      (not (location-blocked ?from))
      (not (location-blocked ?to))
    )
    :effect (and
      (not (at ?v ?from))
      (at ?v ?to)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-road_0079-tw_00390
    :parameters (?v - vehicle ?r - road_type_road_0079 ?from - loc_type_loc_0053 ?to - loc_type_loc_0008 ?w - window_type_tw_00390)
    :precondition (and
      (at ?v ?from)
      (connects ?r ?from ?to)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
      (not (location-blocked ?from))
      (not (location-blocked ?to))
    )
    :effect (and
      (not (at ?v ?from))
      (at ?v ?to)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-road_0079-tw_00420
    :parameters (?v - vehicle ?r - road_type_road_0079 ?from - loc_type_loc_0053 ?to - loc_type_loc_0008 ?w - window_type_tw_00420)
    :precondition (and
      (at ?v ?from)
      (connects ?r ?from ?to)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
      (not (location-blocked ?from))
      (not (location-blocked ?to))
    )
    :effect (and
      (not (at ?v ?from))
      (at ?v ?to)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-road_0079-tw_00450
    :parameters (?v - vehicle ?r - road_type_road_0079 ?from - loc_type_loc_0053 ?to - loc_type_loc_0008 ?w - window_type_tw_00450)
    :precondition (and
      (at ?v ?from)
      (connects ?r ?from ?to)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
      (not (location-blocked ?from))
      (not (location-blocked ?to))
    )
    :effect (and
      (not (at ?v ?from))
      (at ?v ?to)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-road_0080-tw_00000
    :parameters (?v - vehicle ?r - road_type_road_0080 ?from - loc_type_loc_0054 ?to - loc_type_loc_0058 ?w - window_type_tw_00000)
    :precondition (and
      (at ?v ?from)
      (connects ?r ?from ?to)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
      (not (location-blocked ?from))
      (not (location-blocked ?to))
    )
    :effect (and
      (not (at ?v ?from))
      (at ?v ?to)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-road_0080-tw_00030
    :parameters (?v - vehicle ?r - road_type_road_0080 ?from - loc_type_loc_0054 ?to - loc_type_loc_0058 ?w - window_type_tw_00030)
    :precondition (and
      (at ?v ?from)
      (connects ?r ?from ?to)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
      (not (location-blocked ?from))
      (not (location-blocked ?to))
    )
    :effect (and
      (not (at ?v ?from))
      (at ?v ?to)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-road_0080-tw_00060
    :parameters (?v - vehicle ?r - road_type_road_0080 ?from - loc_type_loc_0054 ?to - loc_type_loc_0058 ?w - window_type_tw_00060)
    :precondition (and
      (at ?v ?from)
      (connects ?r ?from ?to)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
      (not (location-blocked ?from))
      (not (location-blocked ?to))
    )
    :effect (and
      (not (at ?v ?from))
      (at ?v ?to)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-road_0080-tw_00090
    :parameters (?v - vehicle ?r - road_type_road_0080 ?from - loc_type_loc_0054 ?to - loc_type_loc_0058 ?w - window_type_tw_00090)
    :precondition (and
      (at ?v ?from)
      (connects ?r ?from ?to)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
      (not (location-blocked ?from))
      (not (location-blocked ?to))
    )
    :effect (and
      (not (at ?v ?from))
      (at ?v ?to)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-road_0080-tw_00120
    :parameters (?v - vehicle ?r - road_type_road_0080 ?from - loc_type_loc_0054 ?to - loc_type_loc_0058 ?w - window_type_tw_00120)
    :precondition (and
      (at ?v ?from)
      (connects ?r ?from ?to)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
      (not (location-blocked ?from))
      (not (location-blocked ?to))
    )
    :effect (and
      (not (at ?v ?from))
      (at ?v ?to)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-road_0080-tw_00150
    :parameters (?v - vehicle ?r - road_type_road_0080 ?from - loc_type_loc_0054 ?to - loc_type_loc_0058 ?w - window_type_tw_00150)
    :precondition (and
      (at ?v ?from)
      (connects ?r ?from ?to)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
      (not (location-blocked ?from))
      (not (location-blocked ?to))
    )
    :effect (and
      (not (at ?v ?from))
      (at ?v ?to)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-road_0080-tw_00180
    :parameters (?v - vehicle ?r - road_type_road_0080 ?from - loc_type_loc_0054 ?to - loc_type_loc_0058 ?w - window_type_tw_00180)
    :precondition (and
      (at ?v ?from)
      (connects ?r ?from ?to)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
      (not (location-blocked ?from))
      (not (location-blocked ?to))
    )
    :effect (and
      (not (at ?v ?from))
      (at ?v ?to)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-road_0080-tw_00210
    :parameters (?v - vehicle ?r - road_type_road_0080 ?from - loc_type_loc_0054 ?to - loc_type_loc_0058 ?w - window_type_tw_00210)
    :precondition (and
      (at ?v ?from)
      (connects ?r ?from ?to)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
      (not (location-blocked ?from))
      (not (location-blocked ?to))
    )
    :effect (and
      (not (at ?v ?from))
      (at ?v ?to)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-road_0080-tw_00240
    :parameters (?v - vehicle ?r - road_type_road_0080 ?from - loc_type_loc_0054 ?to - loc_type_loc_0058 ?w - window_type_tw_00240)
    :precondition (and
      (at ?v ?from)
      (connects ?r ?from ?to)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
      (not (location-blocked ?from))
      (not (location-blocked ?to))
    )
    :effect (and
      (not (at ?v ?from))
      (at ?v ?to)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-road_0080-tw_00270
    :parameters (?v - vehicle ?r - road_type_road_0080 ?from - loc_type_loc_0054 ?to - loc_type_loc_0058 ?w - window_type_tw_00270)
    :precondition (and
      (at ?v ?from)
      (connects ?r ?from ?to)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
      (not (location-blocked ?from))
      (not (location-blocked ?to))
    )
    :effect (and
      (not (at ?v ?from))
      (at ?v ?to)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-road_0080-tw_00300
    :parameters (?v - vehicle ?r - road_type_road_0080 ?from - loc_type_loc_0054 ?to - loc_type_loc_0058 ?w - window_type_tw_00300)
    :precondition (and
      (at ?v ?from)
      (connects ?r ?from ?to)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
      (not (location-blocked ?from))
      (not (location-blocked ?to))
    )
    :effect (and
      (not (at ?v ?from))
      (at ?v ?to)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-road_0080-tw_00330
    :parameters (?v - vehicle ?r - road_type_road_0080 ?from - loc_type_loc_0054 ?to - loc_type_loc_0058 ?w - window_type_tw_00330)
    :precondition (and
      (at ?v ?from)
      (connects ?r ?from ?to)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
      (not (location-blocked ?from))
      (not (location-blocked ?to))
    )
    :effect (and
      (not (at ?v ?from))
      (at ?v ?to)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-road_0080-tw_00360
    :parameters (?v - vehicle ?r - road_type_road_0080 ?from - loc_type_loc_0054 ?to - loc_type_loc_0058 ?w - window_type_tw_00360)
    :precondition (and
      (at ?v ?from)
      (connects ?r ?from ?to)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
      (not (location-blocked ?from))
      (not (location-blocked ?to))
    )
    :effect (and
      (not (at ?v ?from))
      (at ?v ?to)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-road_0080-tw_00390
    :parameters (?v - vehicle ?r - road_type_road_0080 ?from - loc_type_loc_0054 ?to - loc_type_loc_0058 ?w - window_type_tw_00390)
    :precondition (and
      (at ?v ?from)
      (connects ?r ?from ?to)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
      (not (location-blocked ?from))
      (not (location-blocked ?to))
    )
    :effect (and
      (not (at ?v ?from))
      (at ?v ?to)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-road_0080-tw_00420
    :parameters (?v - vehicle ?r - road_type_road_0080 ?from - loc_type_loc_0054 ?to - loc_type_loc_0058 ?w - window_type_tw_00420)
    :precondition (and
      (at ?v ?from)
      (connects ?r ?from ?to)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
      (not (location-blocked ?from))
      (not (location-blocked ?to))
    )
    :effect (and
      (not (at ?v ?from))
      (at ?v ?to)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-road_0080-tw_00450
    :parameters (?v - vehicle ?r - road_type_road_0080 ?from - loc_type_loc_0054 ?to - loc_type_loc_0058 ?w - window_type_tw_00450)
    :precondition (and
      (at ?v ?from)
      (connects ?r ?from ?to)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
      (not (location-blocked ?from))
      (not (location-blocked ?to))
    )
    :effect (and
      (not (at ?v ?from))
      (at ?v ?to)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-road_0083-tw_00000
    :parameters (?v - vehicle ?r - road_type_road_0083 ?from - loc_type_loc_0054 ?to - loc_type_loc_0089 ?w - window_type_tw_00000)
    :precondition (and
      (at ?v ?from)
      (connects ?r ?from ?to)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
      (not (location-blocked ?from))
      (not (location-blocked ?to))
    )
    :effect (and
      (not (at ?v ?from))
      (at ?v ?to)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-road_0083-tw_00030
    :parameters (?v - vehicle ?r - road_type_road_0083 ?from - loc_type_loc_0054 ?to - loc_type_loc_0089 ?w - window_type_tw_00030)
    :precondition (and
      (at ?v ?from)
      (connects ?r ?from ?to)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
      (not (location-blocked ?from))
      (not (location-blocked ?to))
    )
    :effect (and
      (not (at ?v ?from))
      (at ?v ?to)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-road_0083-tw_00060
    :parameters (?v - vehicle ?r - road_type_road_0083 ?from - loc_type_loc_0054 ?to - loc_type_loc_0089 ?w - window_type_tw_00060)
    :precondition (and
      (at ?v ?from)
      (connects ?r ?from ?to)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
      (not (location-blocked ?from))
      (not (location-blocked ?to))
    )
    :effect (and
      (not (at ?v ?from))
      (at ?v ?to)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-road_0083-tw_00090
    :parameters (?v - vehicle ?r - road_type_road_0083 ?from - loc_type_loc_0054 ?to - loc_type_loc_0089 ?w - window_type_tw_00090)
    :precondition (and
      (at ?v ?from)
      (connects ?r ?from ?to)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
      (not (location-blocked ?from))
      (not (location-blocked ?to))
    )
    :effect (and
      (not (at ?v ?from))
      (at ?v ?to)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-road_0083-tw_00120
    :parameters (?v - vehicle ?r - road_type_road_0083 ?from - loc_type_loc_0054 ?to - loc_type_loc_0089 ?w - window_type_tw_00120)
    :precondition (and
      (at ?v ?from)
      (connects ?r ?from ?to)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
      (not (location-blocked ?from))
      (not (location-blocked ?to))
    )
    :effect (and
      (not (at ?v ?from))
      (at ?v ?to)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-road_0083-tw_00150
    :parameters (?v - vehicle ?r - road_type_road_0083 ?from - loc_type_loc_0054 ?to - loc_type_loc_0089 ?w - window_type_tw_00150)
    :precondition (and
      (at ?v ?from)
      (connects ?r ?from ?to)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
      (not (location-blocked ?from))
      (not (location-blocked ?to))
    )
    :effect (and
      (not (at ?v ?from))
      (at ?v ?to)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-road_0083-tw_00180
    :parameters (?v - vehicle ?r - road_type_road_0083 ?from - loc_type_loc_0054 ?to - loc_type_loc_0089 ?w - window_type_tw_00180)
    :precondition (and
      (at ?v ?from)
      (connects ?r ?from ?to)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
      (not (location-blocked ?from))
      (not (location-blocked ?to))
    )
    :effect (and
      (not (at ?v ?from))
      (at ?v ?to)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-road_0083-tw_00210
    :parameters (?v - vehicle ?r - road_type_road_0083 ?from - loc_type_loc_0054 ?to - loc_type_loc_0089 ?w - window_type_tw_00210)
    :precondition (and
      (at ?v ?from)
      (connects ?r ?from ?to)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
      (not (location-blocked ?from))
      (not (location-blocked ?to))
    )
    :effect (and
      (not (at ?v ?from))
      (at ?v ?to)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-road_0083-tw_00240
    :parameters (?v - vehicle ?r - road_type_road_0083 ?from - loc_type_loc_0054 ?to - loc_type_loc_0089 ?w - window_type_tw_00240)
    :precondition (and
      (at ?v ?from)
      (connects ?r ?from ?to)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
      (not (location-blocked ?from))
      (not (location-blocked ?to))
    )
    :effect (and
      (not (at ?v ?from))
      (at ?v ?to)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-road_0083-tw_00270
    :parameters (?v - vehicle ?r - road_type_road_0083 ?from - loc_type_loc_0054 ?to - loc_type_loc_0089 ?w - window_type_tw_00270)
    :precondition (and
      (at ?v ?from)
      (connects ?r ?from ?to)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
      (not (location-blocked ?from))
      (not (location-blocked ?to))
    )
    :effect (and
      (not (at ?v ?from))
      (at ?v ?to)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-road_0083-tw_00300
    :parameters (?v - vehicle ?r - road_type_road_0083 ?from - loc_type_loc_0054 ?to - loc_type_loc_0089 ?w - window_type_tw_00300)
    :precondition (and
      (at ?v ?from)
      (connects ?r ?from ?to)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
      (not (location-blocked ?from))
      (not (location-blocked ?to))
    )
    :effect (and
      (not (at ?v ?from))
      (at ?v ?to)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-road_0083-tw_00330
    :parameters (?v - vehicle ?r - road_type_road_0083 ?from - loc_type_loc_0054 ?to - loc_type_loc_0089 ?w - window_type_tw_00330)
    :precondition (and
      (at ?v ?from)
      (connects ?r ?from ?to)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
      (not (location-blocked ?from))
      (not (location-blocked ?to))
    )
    :effect (and
      (not (at ?v ?from))
      (at ?v ?to)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-road_0083-tw_00360
    :parameters (?v - vehicle ?r - road_type_road_0083 ?from - loc_type_loc_0054 ?to - loc_type_loc_0089 ?w - window_type_tw_00360)
    :precondition (and
      (at ?v ?from)
      (connects ?r ?from ?to)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
      (not (location-blocked ?from))
      (not (location-blocked ?to))
    )
    :effect (and
      (not (at ?v ?from))
      (at ?v ?to)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-road_0083-tw_00390
    :parameters (?v - vehicle ?r - road_type_road_0083 ?from - loc_type_loc_0054 ?to - loc_type_loc_0089 ?w - window_type_tw_00390)
    :precondition (and
      (at ?v ?from)
      (connects ?r ?from ?to)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
      (not (location-blocked ?from))
      (not (location-blocked ?to))
    )
    :effect (and
      (not (at ?v ?from))
      (at ?v ?to)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-road_0083-tw_00420
    :parameters (?v - vehicle ?r - road_type_road_0083 ?from - loc_type_loc_0054 ?to - loc_type_loc_0089 ?w - window_type_tw_00420)
    :precondition (and
      (at ?v ?from)
      (connects ?r ?from ?to)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
      (not (location-blocked ?from))
      (not (location-blocked ?to))
    )
    :effect (and
      (not (at ?v ?from))
      (at ?v ?to)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-road_0083-tw_00450
    :parameters (?v - vehicle ?r - road_type_road_0083 ?from - loc_type_loc_0054 ?to - loc_type_loc_0089 ?w - window_type_tw_00450)
    :precondition (and
      (at ?v ?from)
      (connects ?r ?from ?to)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
      (not (location-blocked ?from))
      (not (location-blocked ?to))
    )
    :effect (and
      (not (at ?v ?from))
      (at ?v ?to)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-road_0084-tw_00000
    :parameters (?v - vehicle ?r - road_type_road_0084 ?from - loc_type_loc_0055 ?to - loc_type_loc_0012 ?w - window_type_tw_00000)
    :precondition (and
      (at ?v ?from)
      (connects ?r ?from ?to)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
      (not (location-blocked ?from))
      (not (location-blocked ?to))
    )
    :effect (and
      (not (at ?v ?from))
      (at ?v ?to)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-road_0084-tw_00030
    :parameters (?v - vehicle ?r - road_type_road_0084 ?from - loc_type_loc_0055 ?to - loc_type_loc_0012 ?w - window_type_tw_00030)
    :precondition (and
      (at ?v ?from)
      (connects ?r ?from ?to)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
      (not (location-blocked ?from))
      (not (location-blocked ?to))
    )
    :effect (and
      (not (at ?v ?from))
      (at ?v ?to)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-road_0084-tw_00060
    :parameters (?v - vehicle ?r - road_type_road_0084 ?from - loc_type_loc_0055 ?to - loc_type_loc_0012 ?w - window_type_tw_00060)
    :precondition (and
      (at ?v ?from)
      (connects ?r ?from ?to)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
      (not (location-blocked ?from))
      (not (location-blocked ?to))
    )
    :effect (and
      (not (at ?v ?from))
      (at ?v ?to)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-road_0084-tw_00090
    :parameters (?v - vehicle ?r - road_type_road_0084 ?from - loc_type_loc_0055 ?to - loc_type_loc_0012 ?w - window_type_tw_00090)
    :precondition (and
      (at ?v ?from)
      (connects ?r ?from ?to)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
      (not (location-blocked ?from))
      (not (location-blocked ?to))
    )
    :effect (and
      (not (at ?v ?from))
      (at ?v ?to)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-road_0084-tw_00120
    :parameters (?v - vehicle ?r - road_type_road_0084 ?from - loc_type_loc_0055 ?to - loc_type_loc_0012 ?w - window_type_tw_00120)
    :precondition (and
      (at ?v ?from)
      (connects ?r ?from ?to)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
      (not (location-blocked ?from))
      (not (location-blocked ?to))
    )
    :effect (and
      (not (at ?v ?from))
      (at ?v ?to)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-road_0084-tw_00150
    :parameters (?v - vehicle ?r - road_type_road_0084 ?from - loc_type_loc_0055 ?to - loc_type_loc_0012 ?w - window_type_tw_00150)
    :precondition (and
      (at ?v ?from)
      (connects ?r ?from ?to)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
      (not (location-blocked ?from))
      (not (location-blocked ?to))
    )
    :effect (and
      (not (at ?v ?from))
      (at ?v ?to)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-road_0084-tw_00180
    :parameters (?v - vehicle ?r - road_type_road_0084 ?from - loc_type_loc_0055 ?to - loc_type_loc_0012 ?w - window_type_tw_00180)
    :precondition (and
      (at ?v ?from)
      (connects ?r ?from ?to)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
      (not (location-blocked ?from))
      (not (location-blocked ?to))
    )
    :effect (and
      (not (at ?v ?from))
      (at ?v ?to)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-road_0084-tw_00210
    :parameters (?v - vehicle ?r - road_type_road_0084 ?from - loc_type_loc_0055 ?to - loc_type_loc_0012 ?w - window_type_tw_00210)
    :precondition (and
      (at ?v ?from)
      (connects ?r ?from ?to)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
      (not (location-blocked ?from))
      (not (location-blocked ?to))
    )
    :effect (and
      (not (at ?v ?from))
      (at ?v ?to)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-road_0084-tw_00240
    :parameters (?v - vehicle ?r - road_type_road_0084 ?from - loc_type_loc_0055 ?to - loc_type_loc_0012 ?w - window_type_tw_00240)
    :precondition (and
      (at ?v ?from)
      (connects ?r ?from ?to)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
      (not (location-blocked ?from))
      (not (location-blocked ?to))
    )
    :effect (and
      (not (at ?v ?from))
      (at ?v ?to)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-road_0084-tw_00270
    :parameters (?v - vehicle ?r - road_type_road_0084 ?from - loc_type_loc_0055 ?to - loc_type_loc_0012 ?w - window_type_tw_00270)
    :precondition (and
      (at ?v ?from)
      (connects ?r ?from ?to)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
      (not (location-blocked ?from))
      (not (location-blocked ?to))
    )
    :effect (and
      (not (at ?v ?from))
      (at ?v ?to)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-road_0084-tw_00300
    :parameters (?v - vehicle ?r - road_type_road_0084 ?from - loc_type_loc_0055 ?to - loc_type_loc_0012 ?w - window_type_tw_00300)
    :precondition (and
      (at ?v ?from)
      (connects ?r ?from ?to)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
      (not (location-blocked ?from))
      (not (location-blocked ?to))
    )
    :effect (and
      (not (at ?v ?from))
      (at ?v ?to)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-road_0084-tw_00330
    :parameters (?v - vehicle ?r - road_type_road_0084 ?from - loc_type_loc_0055 ?to - loc_type_loc_0012 ?w - window_type_tw_00330)
    :precondition (and
      (at ?v ?from)
      (connects ?r ?from ?to)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
      (not (location-blocked ?from))
      (not (location-blocked ?to))
    )
    :effect (and
      (not (at ?v ?from))
      (at ?v ?to)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-road_0084-tw_00360
    :parameters (?v - vehicle ?r - road_type_road_0084 ?from - loc_type_loc_0055 ?to - loc_type_loc_0012 ?w - window_type_tw_00360)
    :precondition (and
      (at ?v ?from)
      (connects ?r ?from ?to)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
      (not (location-blocked ?from))
      (not (location-blocked ?to))
    )
    :effect (and
      (not (at ?v ?from))
      (at ?v ?to)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-road_0084-tw_00390
    :parameters (?v - vehicle ?r - road_type_road_0084 ?from - loc_type_loc_0055 ?to - loc_type_loc_0012 ?w - window_type_tw_00390)
    :precondition (and
      (at ?v ?from)
      (connects ?r ?from ?to)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
      (not (location-blocked ?from))
      (not (location-blocked ?to))
    )
    :effect (and
      (not (at ?v ?from))
      (at ?v ?to)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-road_0084-tw_00420
    :parameters (?v - vehicle ?r - road_type_road_0084 ?from - loc_type_loc_0055 ?to - loc_type_loc_0012 ?w - window_type_tw_00420)
    :precondition (and
      (at ?v ?from)
      (connects ?r ?from ?to)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
      (not (location-blocked ?from))
      (not (location-blocked ?to))
    )
    :effect (and
      (not (at ?v ?from))
      (at ?v ?to)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-road_0084-tw_00450
    :parameters (?v - vehicle ?r - road_type_road_0084 ?from - loc_type_loc_0055 ?to - loc_type_loc_0012 ?w - window_type_tw_00450)
    :precondition (and
      (at ?v ?from)
      (connects ?r ?from ?to)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
      (not (location-blocked ?from))
      (not (location-blocked ?to))
    )
    :effect (and
      (not (at ?v ?from))
      (at ?v ?to)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-road_0085-tw_00000
    :parameters (?v - vehicle ?r - road_type_road_0085 ?from - loc_type_loc_0055 ?to - loc_type_loc_0056 ?w - window_type_tw_00000)
    :precondition (and
      (at ?v ?from)
      (connects ?r ?from ?to)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
      (not (location-blocked ?from))
      (not (location-blocked ?to))
    )
    :effect (and
      (not (at ?v ?from))
      (at ?v ?to)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-road_0085-tw_00030
    :parameters (?v - vehicle ?r - road_type_road_0085 ?from - loc_type_loc_0055 ?to - loc_type_loc_0056 ?w - window_type_tw_00030)
    :precondition (and
      (at ?v ?from)
      (connects ?r ?from ?to)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
      (not (location-blocked ?from))
      (not (location-blocked ?to))
    )
    :effect (and
      (not (at ?v ?from))
      (at ?v ?to)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-road_0085-tw_00060
    :parameters (?v - vehicle ?r - road_type_road_0085 ?from - loc_type_loc_0055 ?to - loc_type_loc_0056 ?w - window_type_tw_00060)
    :precondition (and
      (at ?v ?from)
      (connects ?r ?from ?to)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
      (not (location-blocked ?from))
      (not (location-blocked ?to))
    )
    :effect (and
      (not (at ?v ?from))
      (at ?v ?to)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-road_0085-tw_00090
    :parameters (?v - vehicle ?r - road_type_road_0085 ?from - loc_type_loc_0055 ?to - loc_type_loc_0056 ?w - window_type_tw_00090)
    :precondition (and
      (at ?v ?from)
      (connects ?r ?from ?to)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
      (not (location-blocked ?from))
      (not (location-blocked ?to))
    )
    :effect (and
      (not (at ?v ?from))
      (at ?v ?to)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-road_0085-tw_00120
    :parameters (?v - vehicle ?r - road_type_road_0085 ?from - loc_type_loc_0055 ?to - loc_type_loc_0056 ?w - window_type_tw_00120)
    :precondition (and
      (at ?v ?from)
      (connects ?r ?from ?to)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
      (not (location-blocked ?from))
      (not (location-blocked ?to))
    )
    :effect (and
      (not (at ?v ?from))
      (at ?v ?to)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-road_0085-tw_00150
    :parameters (?v - vehicle ?r - road_type_road_0085 ?from - loc_type_loc_0055 ?to - loc_type_loc_0056 ?w - window_type_tw_00150)
    :precondition (and
      (at ?v ?from)
      (connects ?r ?from ?to)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
      (not (location-blocked ?from))
      (not (location-blocked ?to))
    )
    :effect (and
      (not (at ?v ?from))
      (at ?v ?to)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-road_0085-tw_00180
    :parameters (?v - vehicle ?r - road_type_road_0085 ?from - loc_type_loc_0055 ?to - loc_type_loc_0056 ?w - window_type_tw_00180)
    :precondition (and
      (at ?v ?from)
      (connects ?r ?from ?to)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
      (not (location-blocked ?from))
      (not (location-blocked ?to))
    )
    :effect (and
      (not (at ?v ?from))
      (at ?v ?to)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-road_0085-tw_00210
    :parameters (?v - vehicle ?r - road_type_road_0085 ?from - loc_type_loc_0055 ?to - loc_type_loc_0056 ?w - window_type_tw_00210)
    :precondition (and
      (at ?v ?from)
      (connects ?r ?from ?to)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
      (not (location-blocked ?from))
      (not (location-blocked ?to))
    )
    :effect (and
      (not (at ?v ?from))
      (at ?v ?to)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-road_0085-tw_00240
    :parameters (?v - vehicle ?r - road_type_road_0085 ?from - loc_type_loc_0055 ?to - loc_type_loc_0056 ?w - window_type_tw_00240)
    :precondition (and
      (at ?v ?from)
      (connects ?r ?from ?to)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
      (not (location-blocked ?from))
      (not (location-blocked ?to))
    )
    :effect (and
      (not (at ?v ?from))
      (at ?v ?to)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-road_0085-tw_00270
    :parameters (?v - vehicle ?r - road_type_road_0085 ?from - loc_type_loc_0055 ?to - loc_type_loc_0056 ?w - window_type_tw_00270)
    :precondition (and
      (at ?v ?from)
      (connects ?r ?from ?to)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
      (not (location-blocked ?from))
      (not (location-blocked ?to))
    )
    :effect (and
      (not (at ?v ?from))
      (at ?v ?to)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-road_0085-tw_00300
    :parameters (?v - vehicle ?r - road_type_road_0085 ?from - loc_type_loc_0055 ?to - loc_type_loc_0056 ?w - window_type_tw_00300)
    :precondition (and
      (at ?v ?from)
      (connects ?r ?from ?to)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
      (not (location-blocked ?from))
      (not (location-blocked ?to))
    )
    :effect (and
      (not (at ?v ?from))
      (at ?v ?to)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-road_0085-tw_00330
    :parameters (?v - vehicle ?r - road_type_road_0085 ?from - loc_type_loc_0055 ?to - loc_type_loc_0056 ?w - window_type_tw_00330)
    :precondition (and
      (at ?v ?from)
      (connects ?r ?from ?to)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
      (not (location-blocked ?from))
      (not (location-blocked ?to))
    )
    :effect (and
      (not (at ?v ?from))
      (at ?v ?to)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-road_0085-tw_00360
    :parameters (?v - vehicle ?r - road_type_road_0085 ?from - loc_type_loc_0055 ?to - loc_type_loc_0056 ?w - window_type_tw_00360)
    :precondition (and
      (at ?v ?from)
      (connects ?r ?from ?to)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
      (not (location-blocked ?from))
      (not (location-blocked ?to))
    )
    :effect (and
      (not (at ?v ?from))
      (at ?v ?to)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-road_0085-tw_00390
    :parameters (?v - vehicle ?r - road_type_road_0085 ?from - loc_type_loc_0055 ?to - loc_type_loc_0056 ?w - window_type_tw_00390)
    :precondition (and
      (at ?v ?from)
      (connects ?r ?from ?to)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
      (not (location-blocked ?from))
      (not (location-blocked ?to))
    )
    :effect (and
      (not (at ?v ?from))
      (at ?v ?to)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-road_0085-tw_00420
    :parameters (?v - vehicle ?r - road_type_road_0085 ?from - loc_type_loc_0055 ?to - loc_type_loc_0056 ?w - window_type_tw_00420)
    :precondition (and
      (at ?v ?from)
      (connects ?r ?from ?to)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
      (not (location-blocked ?from))
      (not (location-blocked ?to))
    )
    :effect (and
      (not (at ?v ?from))
      (at ?v ?to)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-road_0085-tw_00450
    :parameters (?v - vehicle ?r - road_type_road_0085 ?from - loc_type_loc_0055 ?to - loc_type_loc_0056 ?w - window_type_tw_00450)
    :precondition (and
      (at ?v ?from)
      (connects ?r ?from ?to)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
      (not (location-blocked ?from))
      (not (location-blocked ?to))
    )
    :effect (and
      (not (at ?v ?from))
      (at ?v ?to)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-road_0086-tw_00000
    :parameters (?v - vehicle ?r - road_type_road_0086 ?from - loc_type_loc_0056 ?to - loc_type_loc_0055 ?w - window_type_tw_00000)
    :precondition (and
      (at ?v ?from)
      (connects ?r ?from ?to)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
      (not (location-blocked ?from))
      (not (location-blocked ?to))
    )
    :effect (and
      (not (at ?v ?from))
      (at ?v ?to)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-road_0086-tw_00030
    :parameters (?v - vehicle ?r - road_type_road_0086 ?from - loc_type_loc_0056 ?to - loc_type_loc_0055 ?w - window_type_tw_00030)
    :precondition (and
      (at ?v ?from)
      (connects ?r ?from ?to)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
      (not (location-blocked ?from))
      (not (location-blocked ?to))
    )
    :effect (and
      (not (at ?v ?from))
      (at ?v ?to)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-road_0086-tw_00060
    :parameters (?v - vehicle ?r - road_type_road_0086 ?from - loc_type_loc_0056 ?to - loc_type_loc_0055 ?w - window_type_tw_00060)
    :precondition (and
      (at ?v ?from)
      (connects ?r ?from ?to)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
      (not (location-blocked ?from))
      (not (location-blocked ?to))
    )
    :effect (and
      (not (at ?v ?from))
      (at ?v ?to)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-road_0086-tw_00090
    :parameters (?v - vehicle ?r - road_type_road_0086 ?from - loc_type_loc_0056 ?to - loc_type_loc_0055 ?w - window_type_tw_00090)
    :precondition (and
      (at ?v ?from)
      (connects ?r ?from ?to)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
      (not (location-blocked ?from))
      (not (location-blocked ?to))
    )
    :effect (and
      (not (at ?v ?from))
      (at ?v ?to)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-road_0086-tw_00120
    :parameters (?v - vehicle ?r - road_type_road_0086 ?from - loc_type_loc_0056 ?to - loc_type_loc_0055 ?w - window_type_tw_00120)
    :precondition (and
      (at ?v ?from)
      (connects ?r ?from ?to)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
      (not (location-blocked ?from))
      (not (location-blocked ?to))
    )
    :effect (and
      (not (at ?v ?from))
      (at ?v ?to)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-road_0086-tw_00150
    :parameters (?v - vehicle ?r - road_type_road_0086 ?from - loc_type_loc_0056 ?to - loc_type_loc_0055 ?w - window_type_tw_00150)
    :precondition (and
      (at ?v ?from)
      (connects ?r ?from ?to)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
      (not (location-blocked ?from))
      (not (location-blocked ?to))
    )
    :effect (and
      (not (at ?v ?from))
      (at ?v ?to)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-road_0086-tw_00180
    :parameters (?v - vehicle ?r - road_type_road_0086 ?from - loc_type_loc_0056 ?to - loc_type_loc_0055 ?w - window_type_tw_00180)
    :precondition (and
      (at ?v ?from)
      (connects ?r ?from ?to)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
      (not (location-blocked ?from))
      (not (location-blocked ?to))
    )
    :effect (and
      (not (at ?v ?from))
      (at ?v ?to)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-road_0086-tw_00210
    :parameters (?v - vehicle ?r - road_type_road_0086 ?from - loc_type_loc_0056 ?to - loc_type_loc_0055 ?w - window_type_tw_00210)
    :precondition (and
      (at ?v ?from)
      (connects ?r ?from ?to)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
      (not (location-blocked ?from))
      (not (location-blocked ?to))
    )
    :effect (and
      (not (at ?v ?from))
      (at ?v ?to)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-road_0086-tw_00240
    :parameters (?v - vehicle ?r - road_type_road_0086 ?from - loc_type_loc_0056 ?to - loc_type_loc_0055 ?w - window_type_tw_00240)
    :precondition (and
      (at ?v ?from)
      (connects ?r ?from ?to)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
      (not (location-blocked ?from))
      (not (location-blocked ?to))
    )
    :effect (and
      (not (at ?v ?from))
      (at ?v ?to)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-road_0086-tw_00270
    :parameters (?v - vehicle ?r - road_type_road_0086 ?from - loc_type_loc_0056 ?to - loc_type_loc_0055 ?w - window_type_tw_00270)
    :precondition (and
      (at ?v ?from)
      (connects ?r ?from ?to)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
      (not (location-blocked ?from))
      (not (location-blocked ?to))
    )
    :effect (and
      (not (at ?v ?from))
      (at ?v ?to)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-road_0086-tw_00300
    :parameters (?v - vehicle ?r - road_type_road_0086 ?from - loc_type_loc_0056 ?to - loc_type_loc_0055 ?w - window_type_tw_00300)
    :precondition (and
      (at ?v ?from)
      (connects ?r ?from ?to)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
      (not (location-blocked ?from))
      (not (location-blocked ?to))
    )
    :effect (and
      (not (at ?v ?from))
      (at ?v ?to)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-road_0086-tw_00330
    :parameters (?v - vehicle ?r - road_type_road_0086 ?from - loc_type_loc_0056 ?to - loc_type_loc_0055 ?w - window_type_tw_00330)
    :precondition (and
      (at ?v ?from)
      (connects ?r ?from ?to)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
      (not (location-blocked ?from))
      (not (location-blocked ?to))
    )
    :effect (and
      (not (at ?v ?from))
      (at ?v ?to)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-road_0086-tw_00360
    :parameters (?v - vehicle ?r - road_type_road_0086 ?from - loc_type_loc_0056 ?to - loc_type_loc_0055 ?w - window_type_tw_00360)
    :precondition (and
      (at ?v ?from)
      (connects ?r ?from ?to)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
      (not (location-blocked ?from))
      (not (location-blocked ?to))
    )
    :effect (and
      (not (at ?v ?from))
      (at ?v ?to)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-road_0086-tw_00390
    :parameters (?v - vehicle ?r - road_type_road_0086 ?from - loc_type_loc_0056 ?to - loc_type_loc_0055 ?w - window_type_tw_00390)
    :precondition (and
      (at ?v ?from)
      (connects ?r ?from ?to)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
      (not (location-blocked ?from))
      (not (location-blocked ?to))
    )
    :effect (and
      (not (at ?v ?from))
      (at ?v ?to)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-road_0086-tw_00420
    :parameters (?v - vehicle ?r - road_type_road_0086 ?from - loc_type_loc_0056 ?to - loc_type_loc_0055 ?w - window_type_tw_00420)
    :precondition (and
      (at ?v ?from)
      (connects ?r ?from ?to)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
      (not (location-blocked ?from))
      (not (location-blocked ?to))
    )
    :effect (and
      (not (at ?v ?from))
      (at ?v ?to)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-road_0086-tw_00450
    :parameters (?v - vehicle ?r - road_type_road_0086 ?from - loc_type_loc_0056 ?to - loc_type_loc_0055 ?w - window_type_tw_00450)
    :precondition (and
      (at ?v ?from)
      (connects ?r ?from ?to)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
      (not (location-blocked ?from))
      (not (location-blocked ?to))
    )
    :effect (and
      (not (at ?v ?from))
      (at ?v ?to)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-road_0088-tw_00000
    :parameters (?v - vehicle ?r - road_type_road_0088 ?from - loc_type_loc_0057 ?to - loc_type_loc_0038 ?w - window_type_tw_00000)
    :precondition (and
      (at ?v ?from)
      (connects ?r ?from ?to)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
      (not (location-blocked ?from))
      (not (location-blocked ?to))
    )
    :effect (and
      (not (at ?v ?from))
      (at ?v ?to)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-road_0088-tw_00030
    :parameters (?v - vehicle ?r - road_type_road_0088 ?from - loc_type_loc_0057 ?to - loc_type_loc_0038 ?w - window_type_tw_00030)
    :precondition (and
      (at ?v ?from)
      (connects ?r ?from ?to)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
      (not (location-blocked ?from))
      (not (location-blocked ?to))
    )
    :effect (and
      (not (at ?v ?from))
      (at ?v ?to)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-road_0088-tw_00060
    :parameters (?v - vehicle ?r - road_type_road_0088 ?from - loc_type_loc_0057 ?to - loc_type_loc_0038 ?w - window_type_tw_00060)
    :precondition (and
      (at ?v ?from)
      (connects ?r ?from ?to)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
      (not (location-blocked ?from))
      (not (location-blocked ?to))
    )
    :effect (and
      (not (at ?v ?from))
      (at ?v ?to)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-road_0088-tw_00090
    :parameters (?v - vehicle ?r - road_type_road_0088 ?from - loc_type_loc_0057 ?to - loc_type_loc_0038 ?w - window_type_tw_00090)
    :precondition (and
      (at ?v ?from)
      (connects ?r ?from ?to)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
      (not (location-blocked ?from))
      (not (location-blocked ?to))
    )
    :effect (and
      (not (at ?v ?from))
      (at ?v ?to)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-road_0088-tw_00120
    :parameters (?v - vehicle ?r - road_type_road_0088 ?from - loc_type_loc_0057 ?to - loc_type_loc_0038 ?w - window_type_tw_00120)
    :precondition (and
      (at ?v ?from)
      (connects ?r ?from ?to)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
      (not (location-blocked ?from))
      (not (location-blocked ?to))
    )
    :effect (and
      (not (at ?v ?from))
      (at ?v ?to)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-road_0088-tw_00150
    :parameters (?v - vehicle ?r - road_type_road_0088 ?from - loc_type_loc_0057 ?to - loc_type_loc_0038 ?w - window_type_tw_00150)
    :precondition (and
      (at ?v ?from)
      (connects ?r ?from ?to)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
      (not (location-blocked ?from))
      (not (location-blocked ?to))
    )
    :effect (and
      (not (at ?v ?from))
      (at ?v ?to)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-road_0088-tw_00180
    :parameters (?v - vehicle ?r - road_type_road_0088 ?from - loc_type_loc_0057 ?to - loc_type_loc_0038 ?w - window_type_tw_00180)
    :precondition (and
      (at ?v ?from)
      (connects ?r ?from ?to)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
      (not (location-blocked ?from))
      (not (location-blocked ?to))
    )
    :effect (and
      (not (at ?v ?from))
      (at ?v ?to)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-road_0088-tw_00210
    :parameters (?v - vehicle ?r - road_type_road_0088 ?from - loc_type_loc_0057 ?to - loc_type_loc_0038 ?w - window_type_tw_00210)
    :precondition (and
      (at ?v ?from)
      (connects ?r ?from ?to)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
      (not (location-blocked ?from))
      (not (location-blocked ?to))
    )
    :effect (and
      (not (at ?v ?from))
      (at ?v ?to)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-road_0088-tw_00240
    :parameters (?v - vehicle ?r - road_type_road_0088 ?from - loc_type_loc_0057 ?to - loc_type_loc_0038 ?w - window_type_tw_00240)
    :precondition (and
      (at ?v ?from)
      (connects ?r ?from ?to)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
      (not (location-blocked ?from))
      (not (location-blocked ?to))
    )
    :effect (and
      (not (at ?v ?from))
      (at ?v ?to)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-road_0088-tw_00270
    :parameters (?v - vehicle ?r - road_type_road_0088 ?from - loc_type_loc_0057 ?to - loc_type_loc_0038 ?w - window_type_tw_00270)
    :precondition (and
      (at ?v ?from)
      (connects ?r ?from ?to)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
      (not (location-blocked ?from))
      (not (location-blocked ?to))
    )
    :effect (and
      (not (at ?v ?from))
      (at ?v ?to)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-road_0088-tw_00300
    :parameters (?v - vehicle ?r - road_type_road_0088 ?from - loc_type_loc_0057 ?to - loc_type_loc_0038 ?w - window_type_tw_00300)
    :precondition (and
      (at ?v ?from)
      (connects ?r ?from ?to)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
      (not (location-blocked ?from))
      (not (location-blocked ?to))
    )
    :effect (and
      (not (at ?v ?from))
      (at ?v ?to)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-road_0088-tw_00330
    :parameters (?v - vehicle ?r - road_type_road_0088 ?from - loc_type_loc_0057 ?to - loc_type_loc_0038 ?w - window_type_tw_00330)
    :precondition (and
      (at ?v ?from)
      (connects ?r ?from ?to)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
      (not (location-blocked ?from))
      (not (location-blocked ?to))
    )
    :effect (and
      (not (at ?v ?from))
      (at ?v ?to)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-road_0088-tw_00360
    :parameters (?v - vehicle ?r - road_type_road_0088 ?from - loc_type_loc_0057 ?to - loc_type_loc_0038 ?w - window_type_tw_00360)
    :precondition (and
      (at ?v ?from)
      (connects ?r ?from ?to)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
      (not (location-blocked ?from))
      (not (location-blocked ?to))
    )
    :effect (and
      (not (at ?v ?from))
      (at ?v ?to)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-road_0088-tw_00390
    :parameters (?v - vehicle ?r - road_type_road_0088 ?from - loc_type_loc_0057 ?to - loc_type_loc_0038 ?w - window_type_tw_00390)
    :precondition (and
      (at ?v ?from)
      (connects ?r ?from ?to)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
      (not (location-blocked ?from))
      (not (location-blocked ?to))
    )
    :effect (and
      (not (at ?v ?from))
      (at ?v ?to)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-road_0088-tw_00420
    :parameters (?v - vehicle ?r - road_type_road_0088 ?from - loc_type_loc_0057 ?to - loc_type_loc_0038 ?w - window_type_tw_00420)
    :precondition (and
      (at ?v ?from)
      (connects ?r ?from ?to)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
      (not (location-blocked ?from))
      (not (location-blocked ?to))
    )
    :effect (and
      (not (at ?v ?from))
      (at ?v ?to)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-road_0088-tw_00450
    :parameters (?v - vehicle ?r - road_type_road_0088 ?from - loc_type_loc_0057 ?to - loc_type_loc_0038 ?w - window_type_tw_00450)
    :precondition (and
      (at ?v ?from)
      (connects ?r ?from ?to)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
      (not (location-blocked ?from))
      (not (location-blocked ?to))
    )
    :effect (and
      (not (at ?v ?from))
      (at ?v ?to)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-road_0089-tw_00000
    :parameters (?v - vehicle ?r - road_type_road_0089 ?from - loc_type_loc_0057 ?to - loc_type_loc_0028 ?w - window_type_tw_00000)
    :precondition (and
      (at ?v ?from)
      (connects ?r ?from ?to)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
      (not (location-blocked ?from))
      (not (location-blocked ?to))
    )
    :effect (and
      (not (at ?v ?from))
      (at ?v ?to)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-road_0089-tw_00030
    :parameters (?v - vehicle ?r - road_type_road_0089 ?from - loc_type_loc_0057 ?to - loc_type_loc_0028 ?w - window_type_tw_00030)
    :precondition (and
      (at ?v ?from)
      (connects ?r ?from ?to)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
      (not (location-blocked ?from))
      (not (location-blocked ?to))
    )
    :effect (and
      (not (at ?v ?from))
      (at ?v ?to)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-road_0089-tw_00060
    :parameters (?v - vehicle ?r - road_type_road_0089 ?from - loc_type_loc_0057 ?to - loc_type_loc_0028 ?w - window_type_tw_00060)
    :precondition (and
      (at ?v ?from)
      (connects ?r ?from ?to)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
      (not (location-blocked ?from))
      (not (location-blocked ?to))
    )
    :effect (and
      (not (at ?v ?from))
      (at ?v ?to)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-road_0089-tw_00090
    :parameters (?v - vehicle ?r - road_type_road_0089 ?from - loc_type_loc_0057 ?to - loc_type_loc_0028 ?w - window_type_tw_00090)
    :precondition (and
      (at ?v ?from)
      (connects ?r ?from ?to)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
      (not (location-blocked ?from))
      (not (location-blocked ?to))
    )
    :effect (and
      (not (at ?v ?from))
      (at ?v ?to)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-road_0089-tw_00120
    :parameters (?v - vehicle ?r - road_type_road_0089 ?from - loc_type_loc_0057 ?to - loc_type_loc_0028 ?w - window_type_tw_00120)
    :precondition (and
      (at ?v ?from)
      (connects ?r ?from ?to)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
      (not (location-blocked ?from))
      (not (location-blocked ?to))
    )
    :effect (and
      (not (at ?v ?from))
      (at ?v ?to)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-road_0089-tw_00150
    :parameters (?v - vehicle ?r - road_type_road_0089 ?from - loc_type_loc_0057 ?to - loc_type_loc_0028 ?w - window_type_tw_00150)
    :precondition (and
      (at ?v ?from)
      (connects ?r ?from ?to)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
      (not (location-blocked ?from))
      (not (location-blocked ?to))
    )
    :effect (and
      (not (at ?v ?from))
      (at ?v ?to)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-road_0089-tw_00180
    :parameters (?v - vehicle ?r - road_type_road_0089 ?from - loc_type_loc_0057 ?to - loc_type_loc_0028 ?w - window_type_tw_00180)
    :precondition (and
      (at ?v ?from)
      (connects ?r ?from ?to)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
      (not (location-blocked ?from))
      (not (location-blocked ?to))
    )
    :effect (and
      (not (at ?v ?from))
      (at ?v ?to)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-road_0089-tw_00210
    :parameters (?v - vehicle ?r - road_type_road_0089 ?from - loc_type_loc_0057 ?to - loc_type_loc_0028 ?w - window_type_tw_00210)
    :precondition (and
      (at ?v ?from)
      (connects ?r ?from ?to)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
      (not (location-blocked ?from))
      (not (location-blocked ?to))
    )
    :effect (and
      (not (at ?v ?from))
      (at ?v ?to)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-road_0089-tw_00240
    :parameters (?v - vehicle ?r - road_type_road_0089 ?from - loc_type_loc_0057 ?to - loc_type_loc_0028 ?w - window_type_tw_00240)
    :precondition (and
      (at ?v ?from)
      (connects ?r ?from ?to)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
      (not (location-blocked ?from))
      (not (location-blocked ?to))
    )
    :effect (and
      (not (at ?v ?from))
      (at ?v ?to)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-road_0089-tw_00270
    :parameters (?v - vehicle ?r - road_type_road_0089 ?from - loc_type_loc_0057 ?to - loc_type_loc_0028 ?w - window_type_tw_00270)
    :precondition (and
      (at ?v ?from)
      (connects ?r ?from ?to)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
      (not (location-blocked ?from))
      (not (location-blocked ?to))
    )
    :effect (and
      (not (at ?v ?from))
      (at ?v ?to)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-road_0089-tw_00300
    :parameters (?v - vehicle ?r - road_type_road_0089 ?from - loc_type_loc_0057 ?to - loc_type_loc_0028 ?w - window_type_tw_00300)
    :precondition (and
      (at ?v ?from)
      (connects ?r ?from ?to)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
      (not (location-blocked ?from))
      (not (location-blocked ?to))
    )
    :effect (and
      (not (at ?v ?from))
      (at ?v ?to)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-road_0089-tw_00330
    :parameters (?v - vehicle ?r - road_type_road_0089 ?from - loc_type_loc_0057 ?to - loc_type_loc_0028 ?w - window_type_tw_00330)
    :precondition (and
      (at ?v ?from)
      (connects ?r ?from ?to)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
      (not (location-blocked ?from))
      (not (location-blocked ?to))
    )
    :effect (and
      (not (at ?v ?from))
      (at ?v ?to)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-road_0089-tw_00360
    :parameters (?v - vehicle ?r - road_type_road_0089 ?from - loc_type_loc_0057 ?to - loc_type_loc_0028 ?w - window_type_tw_00360)
    :precondition (and
      (at ?v ?from)
      (connects ?r ?from ?to)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
      (not (location-blocked ?from))
      (not (location-blocked ?to))
    )
    :effect (and
      (not (at ?v ?from))
      (at ?v ?to)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-road_0089-tw_00390
    :parameters (?v - vehicle ?r - road_type_road_0089 ?from - loc_type_loc_0057 ?to - loc_type_loc_0028 ?w - window_type_tw_00390)
    :precondition (and
      (at ?v ?from)
      (connects ?r ?from ?to)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
      (not (location-blocked ?from))
      (not (location-blocked ?to))
    )
    :effect (and
      (not (at ?v ?from))
      (at ?v ?to)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-road_0089-tw_00420
    :parameters (?v - vehicle ?r - road_type_road_0089 ?from - loc_type_loc_0057 ?to - loc_type_loc_0028 ?w - window_type_tw_00420)
    :precondition (and
      (at ?v ?from)
      (connects ?r ?from ?to)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
      (not (location-blocked ?from))
      (not (location-blocked ?to))
    )
    :effect (and
      (not (at ?v ?from))
      (at ?v ?to)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-road_0089-tw_00450
    :parameters (?v - vehicle ?r - road_type_road_0089 ?from - loc_type_loc_0057 ?to - loc_type_loc_0028 ?w - window_type_tw_00450)
    :precondition (and
      (at ?v ?from)
      (connects ?r ?from ?to)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
      (not (location-blocked ?from))
      (not (location-blocked ?to))
    )
    :effect (and
      (not (at ?v ?from))
      (at ?v ?to)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-road_0090-tw_00000
    :parameters (?v - vehicle ?r - road_type_road_0090 ?from - loc_type_loc_0058 ?to - loc_type_loc_0070 ?w - window_type_tw_00000)
    :precondition (and
      (at ?v ?from)
      (connects ?r ?from ?to)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
      (not (location-blocked ?from))
      (not (location-blocked ?to))
    )
    :effect (and
      (not (at ?v ?from))
      (at ?v ?to)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-road_0090-tw_00030
    :parameters (?v - vehicle ?r - road_type_road_0090 ?from - loc_type_loc_0058 ?to - loc_type_loc_0070 ?w - window_type_tw_00030)
    :precondition (and
      (at ?v ?from)
      (connects ?r ?from ?to)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
      (not (location-blocked ?from))
      (not (location-blocked ?to))
    )
    :effect (and
      (not (at ?v ?from))
      (at ?v ?to)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-road_0090-tw_00060
    :parameters (?v - vehicle ?r - road_type_road_0090 ?from - loc_type_loc_0058 ?to - loc_type_loc_0070 ?w - window_type_tw_00060)
    :precondition (and
      (at ?v ?from)
      (connects ?r ?from ?to)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
      (not (location-blocked ?from))
      (not (location-blocked ?to))
    )
    :effect (and
      (not (at ?v ?from))
      (at ?v ?to)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-road_0090-tw_00090
    :parameters (?v - vehicle ?r - road_type_road_0090 ?from - loc_type_loc_0058 ?to - loc_type_loc_0070 ?w - window_type_tw_00090)
    :precondition (and
      (at ?v ?from)
      (connects ?r ?from ?to)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
      (not (location-blocked ?from))
      (not (location-blocked ?to))
    )
    :effect (and
      (not (at ?v ?from))
      (at ?v ?to)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-road_0090-tw_00120
    :parameters (?v - vehicle ?r - road_type_road_0090 ?from - loc_type_loc_0058 ?to - loc_type_loc_0070 ?w - window_type_tw_00120)
    :precondition (and
      (at ?v ?from)
      (connects ?r ?from ?to)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
      (not (location-blocked ?from))
      (not (location-blocked ?to))
    )
    :effect (and
      (not (at ?v ?from))
      (at ?v ?to)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-road_0090-tw_00150
    :parameters (?v - vehicle ?r - road_type_road_0090 ?from - loc_type_loc_0058 ?to - loc_type_loc_0070 ?w - window_type_tw_00150)
    :precondition (and
      (at ?v ?from)
      (connects ?r ?from ?to)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
      (not (location-blocked ?from))
      (not (location-blocked ?to))
    )
    :effect (and
      (not (at ?v ?from))
      (at ?v ?to)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-road_0090-tw_00180
    :parameters (?v - vehicle ?r - road_type_road_0090 ?from - loc_type_loc_0058 ?to - loc_type_loc_0070 ?w - window_type_tw_00180)
    :precondition (and
      (at ?v ?from)
      (connects ?r ?from ?to)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
      (not (location-blocked ?from))
      (not (location-blocked ?to))
    )
    :effect (and
      (not (at ?v ?from))
      (at ?v ?to)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-road_0090-tw_00210
    :parameters (?v - vehicle ?r - road_type_road_0090 ?from - loc_type_loc_0058 ?to - loc_type_loc_0070 ?w - window_type_tw_00210)
    :precondition (and
      (at ?v ?from)
      (connects ?r ?from ?to)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
      (not (location-blocked ?from))
      (not (location-blocked ?to))
    )
    :effect (and
      (not (at ?v ?from))
      (at ?v ?to)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-road_0090-tw_00240
    :parameters (?v - vehicle ?r - road_type_road_0090 ?from - loc_type_loc_0058 ?to - loc_type_loc_0070 ?w - window_type_tw_00240)
    :precondition (and
      (at ?v ?from)
      (connects ?r ?from ?to)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
      (not (location-blocked ?from))
      (not (location-blocked ?to))
    )
    :effect (and
      (not (at ?v ?from))
      (at ?v ?to)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-road_0090-tw_00270
    :parameters (?v - vehicle ?r - road_type_road_0090 ?from - loc_type_loc_0058 ?to - loc_type_loc_0070 ?w - window_type_tw_00270)
    :precondition (and
      (at ?v ?from)
      (connects ?r ?from ?to)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
      (not (location-blocked ?from))
      (not (location-blocked ?to))
    )
    :effect (and
      (not (at ?v ?from))
      (at ?v ?to)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-road_0090-tw_00300
    :parameters (?v - vehicle ?r - road_type_road_0090 ?from - loc_type_loc_0058 ?to - loc_type_loc_0070 ?w - window_type_tw_00300)
    :precondition (and
      (at ?v ?from)
      (connects ?r ?from ?to)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
      (not (location-blocked ?from))
      (not (location-blocked ?to))
    )
    :effect (and
      (not (at ?v ?from))
      (at ?v ?to)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-road_0090-tw_00330
    :parameters (?v - vehicle ?r - road_type_road_0090 ?from - loc_type_loc_0058 ?to - loc_type_loc_0070 ?w - window_type_tw_00330)
    :precondition (and
      (at ?v ?from)
      (connects ?r ?from ?to)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
      (not (location-blocked ?from))
      (not (location-blocked ?to))
    )
    :effect (and
      (not (at ?v ?from))
      (at ?v ?to)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-road_0090-tw_00360
    :parameters (?v - vehicle ?r - road_type_road_0090 ?from - loc_type_loc_0058 ?to - loc_type_loc_0070 ?w - window_type_tw_00360)
    :precondition (and
      (at ?v ?from)
      (connects ?r ?from ?to)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
      (not (location-blocked ?from))
      (not (location-blocked ?to))
    )
    :effect (and
      (not (at ?v ?from))
      (at ?v ?to)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-road_0090-tw_00390
    :parameters (?v - vehicle ?r - road_type_road_0090 ?from - loc_type_loc_0058 ?to - loc_type_loc_0070 ?w - window_type_tw_00390)
    :precondition (and
      (at ?v ?from)
      (connects ?r ?from ?to)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
      (not (location-blocked ?from))
      (not (location-blocked ?to))
    )
    :effect (and
      (not (at ?v ?from))
      (at ?v ?to)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-road_0090-tw_00420
    :parameters (?v - vehicle ?r - road_type_road_0090 ?from - loc_type_loc_0058 ?to - loc_type_loc_0070 ?w - window_type_tw_00420)
    :precondition (and
      (at ?v ?from)
      (connects ?r ?from ?to)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
      (not (location-blocked ?from))
      (not (location-blocked ?to))
    )
    :effect (and
      (not (at ?v ?from))
      (at ?v ?to)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-road_0090-tw_00450
    :parameters (?v - vehicle ?r - road_type_road_0090 ?from - loc_type_loc_0058 ?to - loc_type_loc_0070 ?w - window_type_tw_00450)
    :precondition (and
      (at ?v ?from)
      (connects ?r ?from ?to)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
      (not (location-blocked ?from))
      (not (location-blocked ?to))
    )
    :effect (and
      (not (at ?v ?from))
      (at ?v ?to)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-road_0092-tw_00000
    :parameters (?v - vehicle ?r - road_type_road_0092 ?from - loc_type_loc_0058 ?to - loc_type_loc_0067 ?w - window_type_tw_00000)
    :precondition (and
      (at ?v ?from)
      (connects ?r ?from ?to)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
      (not (location-blocked ?from))
      (not (location-blocked ?to))
    )
    :effect (and
      (not (at ?v ?from))
      (at ?v ?to)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-road_0092-tw_00030
    :parameters (?v - vehicle ?r - road_type_road_0092 ?from - loc_type_loc_0058 ?to - loc_type_loc_0067 ?w - window_type_tw_00030)
    :precondition (and
      (at ?v ?from)
      (connects ?r ?from ?to)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
      (not (location-blocked ?from))
      (not (location-blocked ?to))
    )
    :effect (and
      (not (at ?v ?from))
      (at ?v ?to)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-road_0092-tw_00060
    :parameters (?v - vehicle ?r - road_type_road_0092 ?from - loc_type_loc_0058 ?to - loc_type_loc_0067 ?w - window_type_tw_00060)
    :precondition (and
      (at ?v ?from)
      (connects ?r ?from ?to)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
      (not (location-blocked ?from))
      (not (location-blocked ?to))
    )
    :effect (and
      (not (at ?v ?from))
      (at ?v ?to)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-road_0092-tw_00090
    :parameters (?v - vehicle ?r - road_type_road_0092 ?from - loc_type_loc_0058 ?to - loc_type_loc_0067 ?w - window_type_tw_00090)
    :precondition (and
      (at ?v ?from)
      (connects ?r ?from ?to)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
      (not (location-blocked ?from))
      (not (location-blocked ?to))
    )
    :effect (and
      (not (at ?v ?from))
      (at ?v ?to)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-road_0092-tw_00120
    :parameters (?v - vehicle ?r - road_type_road_0092 ?from - loc_type_loc_0058 ?to - loc_type_loc_0067 ?w - window_type_tw_00120)
    :precondition (and
      (at ?v ?from)
      (connects ?r ?from ?to)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
      (not (location-blocked ?from))
      (not (location-blocked ?to))
    )
    :effect (and
      (not (at ?v ?from))
      (at ?v ?to)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-road_0092-tw_00150
    :parameters (?v - vehicle ?r - road_type_road_0092 ?from - loc_type_loc_0058 ?to - loc_type_loc_0067 ?w - window_type_tw_00150)
    :precondition (and
      (at ?v ?from)
      (connects ?r ?from ?to)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
      (not (location-blocked ?from))
      (not (location-blocked ?to))
    )
    :effect (and
      (not (at ?v ?from))
      (at ?v ?to)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-road_0092-tw_00180
    :parameters (?v - vehicle ?r - road_type_road_0092 ?from - loc_type_loc_0058 ?to - loc_type_loc_0067 ?w - window_type_tw_00180)
    :precondition (and
      (at ?v ?from)
      (connects ?r ?from ?to)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
      (not (location-blocked ?from))
      (not (location-blocked ?to))
    )
    :effect (and
      (not (at ?v ?from))
      (at ?v ?to)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-road_0092-tw_00210
    :parameters (?v - vehicle ?r - road_type_road_0092 ?from - loc_type_loc_0058 ?to - loc_type_loc_0067 ?w - window_type_tw_00210)
    :precondition (and
      (at ?v ?from)
      (connects ?r ?from ?to)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
      (not (location-blocked ?from))
      (not (location-blocked ?to))
    )
    :effect (and
      (not (at ?v ?from))
      (at ?v ?to)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-road_0092-tw_00240
    :parameters (?v - vehicle ?r - road_type_road_0092 ?from - loc_type_loc_0058 ?to - loc_type_loc_0067 ?w - window_type_tw_00240)
    :precondition (and
      (at ?v ?from)
      (connects ?r ?from ?to)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
      (not (location-blocked ?from))
      (not (location-blocked ?to))
    )
    :effect (and
      (not (at ?v ?from))
      (at ?v ?to)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-road_0092-tw_00270
    :parameters (?v - vehicle ?r - road_type_road_0092 ?from - loc_type_loc_0058 ?to - loc_type_loc_0067 ?w - window_type_tw_00270)
    :precondition (and
      (at ?v ?from)
      (connects ?r ?from ?to)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
      (not (location-blocked ?from))
      (not (location-blocked ?to))
    )
    :effect (and
      (not (at ?v ?from))
      (at ?v ?to)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-road_0092-tw_00300
    :parameters (?v - vehicle ?r - road_type_road_0092 ?from - loc_type_loc_0058 ?to - loc_type_loc_0067 ?w - window_type_tw_00300)
    :precondition (and
      (at ?v ?from)
      (connects ?r ?from ?to)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
      (not (location-blocked ?from))
      (not (location-blocked ?to))
    )
    :effect (and
      (not (at ?v ?from))
      (at ?v ?to)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-road_0092-tw_00330
    :parameters (?v - vehicle ?r - road_type_road_0092 ?from - loc_type_loc_0058 ?to - loc_type_loc_0067 ?w - window_type_tw_00330)
    :precondition (and
      (at ?v ?from)
      (connects ?r ?from ?to)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
      (not (location-blocked ?from))
      (not (location-blocked ?to))
    )
    :effect (and
      (not (at ?v ?from))
      (at ?v ?to)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-road_0092-tw_00360
    :parameters (?v - vehicle ?r - road_type_road_0092 ?from - loc_type_loc_0058 ?to - loc_type_loc_0067 ?w - window_type_tw_00360)
    :precondition (and
      (at ?v ?from)
      (connects ?r ?from ?to)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
      (not (location-blocked ?from))
      (not (location-blocked ?to))
    )
    :effect (and
      (not (at ?v ?from))
      (at ?v ?to)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-road_0092-tw_00390
    :parameters (?v - vehicle ?r - road_type_road_0092 ?from - loc_type_loc_0058 ?to - loc_type_loc_0067 ?w - window_type_tw_00390)
    :precondition (and
      (at ?v ?from)
      (connects ?r ?from ?to)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
      (not (location-blocked ?from))
      (not (location-blocked ?to))
    )
    :effect (and
      (not (at ?v ?from))
      (at ?v ?to)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-road_0092-tw_00420
    :parameters (?v - vehicle ?r - road_type_road_0092 ?from - loc_type_loc_0058 ?to - loc_type_loc_0067 ?w - window_type_tw_00420)
    :precondition (and
      (at ?v ?from)
      (connects ?r ?from ?to)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
      (not (location-blocked ?from))
      (not (location-blocked ?to))
    )
    :effect (and
      (not (at ?v ?from))
      (at ?v ?to)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-road_0092-tw_00450
    :parameters (?v - vehicle ?r - road_type_road_0092 ?from - loc_type_loc_0058 ?to - loc_type_loc_0067 ?w - window_type_tw_00450)
    :precondition (and
      (at ?v ?from)
      (connects ?r ?from ?to)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
      (not (location-blocked ?from))
      (not (location-blocked ?to))
    )
    :effect (and
      (not (at ?v ?from))
      (at ?v ?to)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-road_0093-tw_00000
    :parameters (?v - vehicle ?r - road_type_road_0093 ?from - loc_type_loc_0058 ?to - loc_type_loc_0054 ?w - window_type_tw_00000)
    :precondition (and
      (at ?v ?from)
      (connects ?r ?from ?to)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
      (not (location-blocked ?from))
      (not (location-blocked ?to))
    )
    :effect (and
      (not (at ?v ?from))
      (at ?v ?to)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-road_0093-tw_00030
    :parameters (?v - vehicle ?r - road_type_road_0093 ?from - loc_type_loc_0058 ?to - loc_type_loc_0054 ?w - window_type_tw_00030)
    :precondition (and
      (at ?v ?from)
      (connects ?r ?from ?to)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
      (not (location-blocked ?from))
      (not (location-blocked ?to))
    )
    :effect (and
      (not (at ?v ?from))
      (at ?v ?to)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-road_0093-tw_00060
    :parameters (?v - vehicle ?r - road_type_road_0093 ?from - loc_type_loc_0058 ?to - loc_type_loc_0054 ?w - window_type_tw_00060)
    :precondition (and
      (at ?v ?from)
      (connects ?r ?from ?to)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
      (not (location-blocked ?from))
      (not (location-blocked ?to))
    )
    :effect (and
      (not (at ?v ?from))
      (at ?v ?to)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-road_0093-tw_00090
    :parameters (?v - vehicle ?r - road_type_road_0093 ?from - loc_type_loc_0058 ?to - loc_type_loc_0054 ?w - window_type_tw_00090)
    :precondition (and
      (at ?v ?from)
      (connects ?r ?from ?to)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
      (not (location-blocked ?from))
      (not (location-blocked ?to))
    )
    :effect (and
      (not (at ?v ?from))
      (at ?v ?to)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-road_0093-tw_00120
    :parameters (?v - vehicle ?r - road_type_road_0093 ?from - loc_type_loc_0058 ?to - loc_type_loc_0054 ?w - window_type_tw_00120)
    :precondition (and
      (at ?v ?from)
      (connects ?r ?from ?to)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
      (not (location-blocked ?from))
      (not (location-blocked ?to))
    )
    :effect (and
      (not (at ?v ?from))
      (at ?v ?to)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-road_0093-tw_00150
    :parameters (?v - vehicle ?r - road_type_road_0093 ?from - loc_type_loc_0058 ?to - loc_type_loc_0054 ?w - window_type_tw_00150)
    :precondition (and
      (at ?v ?from)
      (connects ?r ?from ?to)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
      (not (location-blocked ?from))
      (not (location-blocked ?to))
    )
    :effect (and
      (not (at ?v ?from))
      (at ?v ?to)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-road_0093-tw_00180
    :parameters (?v - vehicle ?r - road_type_road_0093 ?from - loc_type_loc_0058 ?to - loc_type_loc_0054 ?w - window_type_tw_00180)
    :precondition (and
      (at ?v ?from)
      (connects ?r ?from ?to)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
      (not (location-blocked ?from))
      (not (location-blocked ?to))
    )
    :effect (and
      (not (at ?v ?from))
      (at ?v ?to)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-road_0093-tw_00210
    :parameters (?v - vehicle ?r - road_type_road_0093 ?from - loc_type_loc_0058 ?to - loc_type_loc_0054 ?w - window_type_tw_00210)
    :precondition (and
      (at ?v ?from)
      (connects ?r ?from ?to)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
      (not (location-blocked ?from))
      (not (location-blocked ?to))
    )
    :effect (and
      (not (at ?v ?from))
      (at ?v ?to)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-road_0093-tw_00240
    :parameters (?v - vehicle ?r - road_type_road_0093 ?from - loc_type_loc_0058 ?to - loc_type_loc_0054 ?w - window_type_tw_00240)
    :precondition (and
      (at ?v ?from)
      (connects ?r ?from ?to)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
      (not (location-blocked ?from))
      (not (location-blocked ?to))
    )
    :effect (and
      (not (at ?v ?from))
      (at ?v ?to)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-road_0093-tw_00270
    :parameters (?v - vehicle ?r - road_type_road_0093 ?from - loc_type_loc_0058 ?to - loc_type_loc_0054 ?w - window_type_tw_00270)
    :precondition (and
      (at ?v ?from)
      (connects ?r ?from ?to)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
      (not (location-blocked ?from))
      (not (location-blocked ?to))
    )
    :effect (and
      (not (at ?v ?from))
      (at ?v ?to)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-road_0093-tw_00300
    :parameters (?v - vehicle ?r - road_type_road_0093 ?from - loc_type_loc_0058 ?to - loc_type_loc_0054 ?w - window_type_tw_00300)
    :precondition (and
      (at ?v ?from)
      (connects ?r ?from ?to)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
      (not (location-blocked ?from))
      (not (location-blocked ?to))
    )
    :effect (and
      (not (at ?v ?from))
      (at ?v ?to)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-road_0093-tw_00330
    :parameters (?v - vehicle ?r - road_type_road_0093 ?from - loc_type_loc_0058 ?to - loc_type_loc_0054 ?w - window_type_tw_00330)
    :precondition (and
      (at ?v ?from)
      (connects ?r ?from ?to)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
      (not (location-blocked ?from))
      (not (location-blocked ?to))
    )
    :effect (and
      (not (at ?v ?from))
      (at ?v ?to)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-road_0093-tw_00360
    :parameters (?v - vehicle ?r - road_type_road_0093 ?from - loc_type_loc_0058 ?to - loc_type_loc_0054 ?w - window_type_tw_00360)
    :precondition (and
      (at ?v ?from)
      (connects ?r ?from ?to)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
      (not (location-blocked ?from))
      (not (location-blocked ?to))
    )
    :effect (and
      (not (at ?v ?from))
      (at ?v ?to)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-road_0093-tw_00390
    :parameters (?v - vehicle ?r - road_type_road_0093 ?from - loc_type_loc_0058 ?to - loc_type_loc_0054 ?w - window_type_tw_00390)
    :precondition (and
      (at ?v ?from)
      (connects ?r ?from ?to)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
      (not (location-blocked ?from))
      (not (location-blocked ?to))
    )
    :effect (and
      (not (at ?v ?from))
      (at ?v ?to)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-road_0093-tw_00420
    :parameters (?v - vehicle ?r - road_type_road_0093 ?from - loc_type_loc_0058 ?to - loc_type_loc_0054 ?w - window_type_tw_00420)
    :precondition (and
      (at ?v ?from)
      (connects ?r ?from ?to)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
      (not (location-blocked ?from))
      (not (location-blocked ?to))
    )
    :effect (and
      (not (at ?v ?from))
      (at ?v ?to)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-road_0093-tw_00450
    :parameters (?v - vehicle ?r - road_type_road_0093 ?from - loc_type_loc_0058 ?to - loc_type_loc_0054 ?w - window_type_tw_00450)
    :precondition (and
      (at ?v ?from)
      (connects ?r ?from ?to)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
      (not (location-blocked ?from))
      (not (location-blocked ?to))
    )
    :effect (and
      (not (at ?v ?from))
      (at ?v ?to)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-road_0094-tw_00000
    :parameters (?v - vehicle ?r - road_type_road_0094 ?from - loc_type_loc_0059 ?to - loc_type_loc_0010 ?w - window_type_tw_00000)
    :precondition (and
      (at ?v ?from)
      (connects ?r ?from ?to)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
      (not (location-blocked ?from))
      (not (location-blocked ?to))
    )
    :effect (and
      (not (at ?v ?from))
      (at ?v ?to)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-road_0094-tw_00030
    :parameters (?v - vehicle ?r - road_type_road_0094 ?from - loc_type_loc_0059 ?to - loc_type_loc_0010 ?w - window_type_tw_00030)
    :precondition (and
      (at ?v ?from)
      (connects ?r ?from ?to)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
      (not (location-blocked ?from))
      (not (location-blocked ?to))
    )
    :effect (and
      (not (at ?v ?from))
      (at ?v ?to)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-road_0094-tw_00060
    :parameters (?v - vehicle ?r - road_type_road_0094 ?from - loc_type_loc_0059 ?to - loc_type_loc_0010 ?w - window_type_tw_00060)
    :precondition (and
      (at ?v ?from)
      (connects ?r ?from ?to)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
      (not (location-blocked ?from))
      (not (location-blocked ?to))
    )
    :effect (and
      (not (at ?v ?from))
      (at ?v ?to)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-road_0094-tw_00090
    :parameters (?v - vehicle ?r - road_type_road_0094 ?from - loc_type_loc_0059 ?to - loc_type_loc_0010 ?w - window_type_tw_00090)
    :precondition (and
      (at ?v ?from)
      (connects ?r ?from ?to)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
      (not (location-blocked ?from))
      (not (location-blocked ?to))
    )
    :effect (and
      (not (at ?v ?from))
      (at ?v ?to)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-road_0094-tw_00120
    :parameters (?v - vehicle ?r - road_type_road_0094 ?from - loc_type_loc_0059 ?to - loc_type_loc_0010 ?w - window_type_tw_00120)
    :precondition (and
      (at ?v ?from)
      (connects ?r ?from ?to)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
      (not (location-blocked ?from))
      (not (location-blocked ?to))
    )
    :effect (and
      (not (at ?v ?from))
      (at ?v ?to)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-road_0094-tw_00150
    :parameters (?v - vehicle ?r - road_type_road_0094 ?from - loc_type_loc_0059 ?to - loc_type_loc_0010 ?w - window_type_tw_00150)
    :precondition (and
      (at ?v ?from)
      (connects ?r ?from ?to)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
      (not (location-blocked ?from))
      (not (location-blocked ?to))
    )
    :effect (and
      (not (at ?v ?from))
      (at ?v ?to)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-road_0094-tw_00180
    :parameters (?v - vehicle ?r - road_type_road_0094 ?from - loc_type_loc_0059 ?to - loc_type_loc_0010 ?w - window_type_tw_00180)
    :precondition (and
      (at ?v ?from)
      (connects ?r ?from ?to)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
      (not (location-blocked ?from))
      (not (location-blocked ?to))
    )
    :effect (and
      (not (at ?v ?from))
      (at ?v ?to)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-road_0094-tw_00210
    :parameters (?v - vehicle ?r - road_type_road_0094 ?from - loc_type_loc_0059 ?to - loc_type_loc_0010 ?w - window_type_tw_00210)
    :precondition (and
      (at ?v ?from)
      (connects ?r ?from ?to)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
      (not (location-blocked ?from))
      (not (location-blocked ?to))
    )
    :effect (and
      (not (at ?v ?from))
      (at ?v ?to)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-road_0094-tw_00240
    :parameters (?v - vehicle ?r - road_type_road_0094 ?from - loc_type_loc_0059 ?to - loc_type_loc_0010 ?w - window_type_tw_00240)
    :precondition (and
      (at ?v ?from)
      (connects ?r ?from ?to)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
      (not (location-blocked ?from))
      (not (location-blocked ?to))
    )
    :effect (and
      (not (at ?v ?from))
      (at ?v ?to)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-road_0094-tw_00270
    :parameters (?v - vehicle ?r - road_type_road_0094 ?from - loc_type_loc_0059 ?to - loc_type_loc_0010 ?w - window_type_tw_00270)
    :precondition (and
      (at ?v ?from)
      (connects ?r ?from ?to)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
      (not (location-blocked ?from))
      (not (location-blocked ?to))
    )
    :effect (and
      (not (at ?v ?from))
      (at ?v ?to)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-road_0094-tw_00300
    :parameters (?v - vehicle ?r - road_type_road_0094 ?from - loc_type_loc_0059 ?to - loc_type_loc_0010 ?w - window_type_tw_00300)
    :precondition (and
      (at ?v ?from)
      (connects ?r ?from ?to)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
      (not (location-blocked ?from))
      (not (location-blocked ?to))
    )
    :effect (and
      (not (at ?v ?from))
      (at ?v ?to)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-road_0094-tw_00330
    :parameters (?v - vehicle ?r - road_type_road_0094 ?from - loc_type_loc_0059 ?to - loc_type_loc_0010 ?w - window_type_tw_00330)
    :precondition (and
      (at ?v ?from)
      (connects ?r ?from ?to)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
      (not (location-blocked ?from))
      (not (location-blocked ?to))
    )
    :effect (and
      (not (at ?v ?from))
      (at ?v ?to)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-road_0094-tw_00360
    :parameters (?v - vehicle ?r - road_type_road_0094 ?from - loc_type_loc_0059 ?to - loc_type_loc_0010 ?w - window_type_tw_00360)
    :precondition (and
      (at ?v ?from)
      (connects ?r ?from ?to)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
      (not (location-blocked ?from))
      (not (location-blocked ?to))
    )
    :effect (and
      (not (at ?v ?from))
      (at ?v ?to)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-road_0094-tw_00390
    :parameters (?v - vehicle ?r - road_type_road_0094 ?from - loc_type_loc_0059 ?to - loc_type_loc_0010 ?w - window_type_tw_00390)
    :precondition (and
      (at ?v ?from)
      (connects ?r ?from ?to)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
      (not (location-blocked ?from))
      (not (location-blocked ?to))
    )
    :effect (and
      (not (at ?v ?from))
      (at ?v ?to)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-road_0094-tw_00420
    :parameters (?v - vehicle ?r - road_type_road_0094 ?from - loc_type_loc_0059 ?to - loc_type_loc_0010 ?w - window_type_tw_00420)
    :precondition (and
      (at ?v ?from)
      (connects ?r ?from ?to)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
      (not (location-blocked ?from))
      (not (location-blocked ?to))
    )
    :effect (and
      (not (at ?v ?from))
      (at ?v ?to)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-road_0094-tw_00450
    :parameters (?v - vehicle ?r - road_type_road_0094 ?from - loc_type_loc_0059 ?to - loc_type_loc_0010 ?w - window_type_tw_00450)
    :precondition (and
      (at ?v ?from)
      (connects ?r ?from ?to)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
      (not (location-blocked ?from))
      (not (location-blocked ?to))
    )
    :effect (and
      (not (at ?v ?from))
      (at ?v ?to)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-road_0095-tw_00000
    :parameters (?v - vehicle ?r - road_type_road_0095 ?from - loc_type_loc_0060 ?to - loc_type_loc_0056 ?w - window_type_tw_00000)
    :precondition (and
      (at ?v ?from)
      (connects ?r ?from ?to)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
      (not (location-blocked ?from))
      (not (location-blocked ?to))
    )
    :effect (and
      (not (at ?v ?from))
      (at ?v ?to)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-road_0095-tw_00030
    :parameters (?v - vehicle ?r - road_type_road_0095 ?from - loc_type_loc_0060 ?to - loc_type_loc_0056 ?w - window_type_tw_00030)
    :precondition (and
      (at ?v ?from)
      (connects ?r ?from ?to)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
      (not (location-blocked ?from))
      (not (location-blocked ?to))
    )
    :effect (and
      (not (at ?v ?from))
      (at ?v ?to)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-road_0095-tw_00060
    :parameters (?v - vehicle ?r - road_type_road_0095 ?from - loc_type_loc_0060 ?to - loc_type_loc_0056 ?w - window_type_tw_00060)
    :precondition (and
      (at ?v ?from)
      (connects ?r ?from ?to)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
      (not (location-blocked ?from))
      (not (location-blocked ?to))
    )
    :effect (and
      (not (at ?v ?from))
      (at ?v ?to)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-road_0095-tw_00090
    :parameters (?v - vehicle ?r - road_type_road_0095 ?from - loc_type_loc_0060 ?to - loc_type_loc_0056 ?w - window_type_tw_00090)
    :precondition (and
      (at ?v ?from)
      (connects ?r ?from ?to)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
      (not (location-blocked ?from))
      (not (location-blocked ?to))
    )
    :effect (and
      (not (at ?v ?from))
      (at ?v ?to)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-road_0095-tw_00120
    :parameters (?v - vehicle ?r - road_type_road_0095 ?from - loc_type_loc_0060 ?to - loc_type_loc_0056 ?w - window_type_tw_00120)
    :precondition (and
      (at ?v ?from)
      (connects ?r ?from ?to)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
      (not (location-blocked ?from))
      (not (location-blocked ?to))
    )
    :effect (and
      (not (at ?v ?from))
      (at ?v ?to)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-road_0095-tw_00150
    :parameters (?v - vehicle ?r - road_type_road_0095 ?from - loc_type_loc_0060 ?to - loc_type_loc_0056 ?w - window_type_tw_00150)
    :precondition (and
      (at ?v ?from)
      (connects ?r ?from ?to)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
      (not (location-blocked ?from))
      (not (location-blocked ?to))
    )
    :effect (and
      (not (at ?v ?from))
      (at ?v ?to)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-road_0095-tw_00180
    :parameters (?v - vehicle ?r - road_type_road_0095 ?from - loc_type_loc_0060 ?to - loc_type_loc_0056 ?w - window_type_tw_00180)
    :precondition (and
      (at ?v ?from)
      (connects ?r ?from ?to)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
      (not (location-blocked ?from))
      (not (location-blocked ?to))
    )
    :effect (and
      (not (at ?v ?from))
      (at ?v ?to)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-road_0095-tw_00210
    :parameters (?v - vehicle ?r - road_type_road_0095 ?from - loc_type_loc_0060 ?to - loc_type_loc_0056 ?w - window_type_tw_00210)
    :precondition (and
      (at ?v ?from)
      (connects ?r ?from ?to)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
      (not (location-blocked ?from))
      (not (location-blocked ?to))
    )
    :effect (and
      (not (at ?v ?from))
      (at ?v ?to)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-road_0095-tw_00240
    :parameters (?v - vehicle ?r - road_type_road_0095 ?from - loc_type_loc_0060 ?to - loc_type_loc_0056 ?w - window_type_tw_00240)
    :precondition (and
      (at ?v ?from)
      (connects ?r ?from ?to)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
      (not (location-blocked ?from))
      (not (location-blocked ?to))
    )
    :effect (and
      (not (at ?v ?from))
      (at ?v ?to)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-road_0095-tw_00270
    :parameters (?v - vehicle ?r - road_type_road_0095 ?from - loc_type_loc_0060 ?to - loc_type_loc_0056 ?w - window_type_tw_00270)
    :precondition (and
      (at ?v ?from)
      (connects ?r ?from ?to)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
      (not (location-blocked ?from))
      (not (location-blocked ?to))
    )
    :effect (and
      (not (at ?v ?from))
      (at ?v ?to)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-road_0095-tw_00300
    :parameters (?v - vehicle ?r - road_type_road_0095 ?from - loc_type_loc_0060 ?to - loc_type_loc_0056 ?w - window_type_tw_00300)
    :precondition (and
      (at ?v ?from)
      (connects ?r ?from ?to)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
      (not (location-blocked ?from))
      (not (location-blocked ?to))
    )
    :effect (and
      (not (at ?v ?from))
      (at ?v ?to)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-road_0095-tw_00330
    :parameters (?v - vehicle ?r - road_type_road_0095 ?from - loc_type_loc_0060 ?to - loc_type_loc_0056 ?w - window_type_tw_00330)
    :precondition (and
      (at ?v ?from)
      (connects ?r ?from ?to)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
      (not (location-blocked ?from))
      (not (location-blocked ?to))
    )
    :effect (and
      (not (at ?v ?from))
      (at ?v ?to)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-road_0095-tw_00360
    :parameters (?v - vehicle ?r - road_type_road_0095 ?from - loc_type_loc_0060 ?to - loc_type_loc_0056 ?w - window_type_tw_00360)
    :precondition (and
      (at ?v ?from)
      (connects ?r ?from ?to)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
      (not (location-blocked ?from))
      (not (location-blocked ?to))
    )
    :effect (and
      (not (at ?v ?from))
      (at ?v ?to)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-road_0095-tw_00390
    :parameters (?v - vehicle ?r - road_type_road_0095 ?from - loc_type_loc_0060 ?to - loc_type_loc_0056 ?w - window_type_tw_00390)
    :precondition (and
      (at ?v ?from)
      (connects ?r ?from ?to)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
      (not (location-blocked ?from))
      (not (location-blocked ?to))
    )
    :effect (and
      (not (at ?v ?from))
      (at ?v ?to)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-road_0095-tw_00420
    :parameters (?v - vehicle ?r - road_type_road_0095 ?from - loc_type_loc_0060 ?to - loc_type_loc_0056 ?w - window_type_tw_00420)
    :precondition (and
      (at ?v ?from)
      (connects ?r ?from ?to)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
      (not (location-blocked ?from))
      (not (location-blocked ?to))
    )
    :effect (and
      (not (at ?v ?from))
      (at ?v ?to)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-road_0095-tw_00450
    :parameters (?v - vehicle ?r - road_type_road_0095 ?from - loc_type_loc_0060 ?to - loc_type_loc_0056 ?w - window_type_tw_00450)
    :precondition (and
      (at ?v ?from)
      (connects ?r ?from ?to)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
      (not (location-blocked ?from))
      (not (location-blocked ?to))
    )
    :effect (and
      (not (at ?v ?from))
      (at ?v ?to)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-road_0096-tw_00000
    :parameters (?v - vehicle ?r - road_type_road_0096 ?from - loc_type_loc_0060 ?to - loc_type_loc_0025 ?w - window_type_tw_00000)
    :precondition (and
      (at ?v ?from)
      (connects ?r ?from ?to)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
      (not (location-blocked ?from))
      (not (location-blocked ?to))
    )
    :effect (and
      (not (at ?v ?from))
      (at ?v ?to)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-road_0096-tw_00030
    :parameters (?v - vehicle ?r - road_type_road_0096 ?from - loc_type_loc_0060 ?to - loc_type_loc_0025 ?w - window_type_tw_00030)
    :precondition (and
      (at ?v ?from)
      (connects ?r ?from ?to)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
      (not (location-blocked ?from))
      (not (location-blocked ?to))
    )
    :effect (and
      (not (at ?v ?from))
      (at ?v ?to)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-road_0096-tw_00060
    :parameters (?v - vehicle ?r - road_type_road_0096 ?from - loc_type_loc_0060 ?to - loc_type_loc_0025 ?w - window_type_tw_00060)
    :precondition (and
      (at ?v ?from)
      (connects ?r ?from ?to)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
      (not (location-blocked ?from))
      (not (location-blocked ?to))
    )
    :effect (and
      (not (at ?v ?from))
      (at ?v ?to)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-road_0096-tw_00090
    :parameters (?v - vehicle ?r - road_type_road_0096 ?from - loc_type_loc_0060 ?to - loc_type_loc_0025 ?w - window_type_tw_00090)
    :precondition (and
      (at ?v ?from)
      (connects ?r ?from ?to)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
      (not (location-blocked ?from))
      (not (location-blocked ?to))
    )
    :effect (and
      (not (at ?v ?from))
      (at ?v ?to)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-road_0096-tw_00120
    :parameters (?v - vehicle ?r - road_type_road_0096 ?from - loc_type_loc_0060 ?to - loc_type_loc_0025 ?w - window_type_tw_00120)
    :precondition (and
      (at ?v ?from)
      (connects ?r ?from ?to)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
      (not (location-blocked ?from))
      (not (location-blocked ?to))
    )
    :effect (and
      (not (at ?v ?from))
      (at ?v ?to)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-road_0096-tw_00150
    :parameters (?v - vehicle ?r - road_type_road_0096 ?from - loc_type_loc_0060 ?to - loc_type_loc_0025 ?w - window_type_tw_00150)
    :precondition (and
      (at ?v ?from)
      (connects ?r ?from ?to)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
      (not (location-blocked ?from))
      (not (location-blocked ?to))
    )
    :effect (and
      (not (at ?v ?from))
      (at ?v ?to)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-road_0096-tw_00180
    :parameters (?v - vehicle ?r - road_type_road_0096 ?from - loc_type_loc_0060 ?to - loc_type_loc_0025 ?w - window_type_tw_00180)
    :precondition (and
      (at ?v ?from)
      (connects ?r ?from ?to)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
      (not (location-blocked ?from))
      (not (location-blocked ?to))
    )
    :effect (and
      (not (at ?v ?from))
      (at ?v ?to)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-road_0096-tw_00210
    :parameters (?v - vehicle ?r - road_type_road_0096 ?from - loc_type_loc_0060 ?to - loc_type_loc_0025 ?w - window_type_tw_00210)
    :precondition (and
      (at ?v ?from)
      (connects ?r ?from ?to)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
      (not (location-blocked ?from))
      (not (location-blocked ?to))
    )
    :effect (and
      (not (at ?v ?from))
      (at ?v ?to)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-road_0096-tw_00240
    :parameters (?v - vehicle ?r - road_type_road_0096 ?from - loc_type_loc_0060 ?to - loc_type_loc_0025 ?w - window_type_tw_00240)
    :precondition (and
      (at ?v ?from)
      (connects ?r ?from ?to)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
      (not (location-blocked ?from))
      (not (location-blocked ?to))
    )
    :effect (and
      (not (at ?v ?from))
      (at ?v ?to)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-road_0096-tw_00270
    :parameters (?v - vehicle ?r - road_type_road_0096 ?from - loc_type_loc_0060 ?to - loc_type_loc_0025 ?w - window_type_tw_00270)
    :precondition (and
      (at ?v ?from)
      (connects ?r ?from ?to)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
      (not (location-blocked ?from))
      (not (location-blocked ?to))
    )
    :effect (and
      (not (at ?v ?from))
      (at ?v ?to)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-road_0096-tw_00300
    :parameters (?v - vehicle ?r - road_type_road_0096 ?from - loc_type_loc_0060 ?to - loc_type_loc_0025 ?w - window_type_tw_00300)
    :precondition (and
      (at ?v ?from)
      (connects ?r ?from ?to)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
      (not (location-blocked ?from))
      (not (location-blocked ?to))
    )
    :effect (and
      (not (at ?v ?from))
      (at ?v ?to)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-road_0096-tw_00330
    :parameters (?v - vehicle ?r - road_type_road_0096 ?from - loc_type_loc_0060 ?to - loc_type_loc_0025 ?w - window_type_tw_00330)
    :precondition (and
      (at ?v ?from)
      (connects ?r ?from ?to)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
      (not (location-blocked ?from))
      (not (location-blocked ?to))
    )
    :effect (and
      (not (at ?v ?from))
      (at ?v ?to)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-road_0096-tw_00360
    :parameters (?v - vehicle ?r - road_type_road_0096 ?from - loc_type_loc_0060 ?to - loc_type_loc_0025 ?w - window_type_tw_00360)
    :precondition (and
      (at ?v ?from)
      (connects ?r ?from ?to)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
      (not (location-blocked ?from))
      (not (location-blocked ?to))
    )
    :effect (and
      (not (at ?v ?from))
      (at ?v ?to)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-road_0096-tw_00390
    :parameters (?v - vehicle ?r - road_type_road_0096 ?from - loc_type_loc_0060 ?to - loc_type_loc_0025 ?w - window_type_tw_00390)
    :precondition (and
      (at ?v ?from)
      (connects ?r ?from ?to)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
      (not (location-blocked ?from))
      (not (location-blocked ?to))
    )
    :effect (and
      (not (at ?v ?from))
      (at ?v ?to)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-road_0096-tw_00420
    :parameters (?v - vehicle ?r - road_type_road_0096 ?from - loc_type_loc_0060 ?to - loc_type_loc_0025 ?w - window_type_tw_00420)
    :precondition (and
      (at ?v ?from)
      (connects ?r ?from ?to)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
      (not (location-blocked ?from))
      (not (location-blocked ?to))
    )
    :effect (and
      (not (at ?v ?from))
      (at ?v ?to)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-road_0096-tw_00450
    :parameters (?v - vehicle ?r - road_type_road_0096 ?from - loc_type_loc_0060 ?to - loc_type_loc_0025 ?w - window_type_tw_00450)
    :precondition (and
      (at ?v ?from)
      (connects ?r ?from ?to)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
      (not (location-blocked ?from))
      (not (location-blocked ?to))
    )
    :effect (and
      (not (at ?v ?from))
      (at ?v ?to)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-road_0098-tw_00000
    :parameters (?v - vehicle ?r - road_type_road_0098 ?from - loc_type_loc_0061 ?to - loc_type_loc_0054 ?w - window_type_tw_00000)
    :precondition (and
      (at ?v ?from)
      (connects ?r ?from ?to)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
      (not (location-blocked ?from))
      (not (location-blocked ?to))
    )
    :effect (and
      (not (at ?v ?from))
      (at ?v ?to)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-road_0098-tw_00030
    :parameters (?v - vehicle ?r - road_type_road_0098 ?from - loc_type_loc_0061 ?to - loc_type_loc_0054 ?w - window_type_tw_00030)
    :precondition (and
      (at ?v ?from)
      (connects ?r ?from ?to)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
      (not (location-blocked ?from))
      (not (location-blocked ?to))
    )
    :effect (and
      (not (at ?v ?from))
      (at ?v ?to)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-road_0098-tw_00060
    :parameters (?v - vehicle ?r - road_type_road_0098 ?from - loc_type_loc_0061 ?to - loc_type_loc_0054 ?w - window_type_tw_00060)
    :precondition (and
      (at ?v ?from)
      (connects ?r ?from ?to)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
      (not (location-blocked ?from))
      (not (location-blocked ?to))
    )
    :effect (and
      (not (at ?v ?from))
      (at ?v ?to)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-road_0098-tw_00090
    :parameters (?v - vehicle ?r - road_type_road_0098 ?from - loc_type_loc_0061 ?to - loc_type_loc_0054 ?w - window_type_tw_00090)
    :precondition (and
      (at ?v ?from)
      (connects ?r ?from ?to)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
      (not (location-blocked ?from))
      (not (location-blocked ?to))
    )
    :effect (and
      (not (at ?v ?from))
      (at ?v ?to)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-road_0098-tw_00120
    :parameters (?v - vehicle ?r - road_type_road_0098 ?from - loc_type_loc_0061 ?to - loc_type_loc_0054 ?w - window_type_tw_00120)
    :precondition (and
      (at ?v ?from)
      (connects ?r ?from ?to)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
      (not (location-blocked ?from))
      (not (location-blocked ?to))
    )
    :effect (and
      (not (at ?v ?from))
      (at ?v ?to)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-road_0098-tw_00150
    :parameters (?v - vehicle ?r - road_type_road_0098 ?from - loc_type_loc_0061 ?to - loc_type_loc_0054 ?w - window_type_tw_00150)
    :precondition (and
      (at ?v ?from)
      (connects ?r ?from ?to)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
      (not (location-blocked ?from))
      (not (location-blocked ?to))
    )
    :effect (and
      (not (at ?v ?from))
      (at ?v ?to)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-road_0098-tw_00180
    :parameters (?v - vehicle ?r - road_type_road_0098 ?from - loc_type_loc_0061 ?to - loc_type_loc_0054 ?w - window_type_tw_00180)
    :precondition (and
      (at ?v ?from)
      (connects ?r ?from ?to)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
      (not (location-blocked ?from))
      (not (location-blocked ?to))
    )
    :effect (and
      (not (at ?v ?from))
      (at ?v ?to)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-road_0098-tw_00210
    :parameters (?v - vehicle ?r - road_type_road_0098 ?from - loc_type_loc_0061 ?to - loc_type_loc_0054 ?w - window_type_tw_00210)
    :precondition (and
      (at ?v ?from)
      (connects ?r ?from ?to)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
      (not (location-blocked ?from))
      (not (location-blocked ?to))
    )
    :effect (and
      (not (at ?v ?from))
      (at ?v ?to)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-road_0098-tw_00240
    :parameters (?v - vehicle ?r - road_type_road_0098 ?from - loc_type_loc_0061 ?to - loc_type_loc_0054 ?w - window_type_tw_00240)
    :precondition (and
      (at ?v ?from)
      (connects ?r ?from ?to)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
      (not (location-blocked ?from))
      (not (location-blocked ?to))
    )
    :effect (and
      (not (at ?v ?from))
      (at ?v ?to)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-road_0098-tw_00270
    :parameters (?v - vehicle ?r - road_type_road_0098 ?from - loc_type_loc_0061 ?to - loc_type_loc_0054 ?w - window_type_tw_00270)
    :precondition (and
      (at ?v ?from)
      (connects ?r ?from ?to)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
      (not (location-blocked ?from))
      (not (location-blocked ?to))
    )
    :effect (and
      (not (at ?v ?from))
      (at ?v ?to)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-road_0098-tw_00300
    :parameters (?v - vehicle ?r - road_type_road_0098 ?from - loc_type_loc_0061 ?to - loc_type_loc_0054 ?w - window_type_tw_00300)
    :precondition (and
      (at ?v ?from)
      (connects ?r ?from ?to)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
      (not (location-blocked ?from))
      (not (location-blocked ?to))
    )
    :effect (and
      (not (at ?v ?from))
      (at ?v ?to)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-road_0098-tw_00330
    :parameters (?v - vehicle ?r - road_type_road_0098 ?from - loc_type_loc_0061 ?to - loc_type_loc_0054 ?w - window_type_tw_00330)
    :precondition (and
      (at ?v ?from)
      (connects ?r ?from ?to)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
      (not (location-blocked ?from))
      (not (location-blocked ?to))
    )
    :effect (and
      (not (at ?v ?from))
      (at ?v ?to)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-road_0098-tw_00360
    :parameters (?v - vehicle ?r - road_type_road_0098 ?from - loc_type_loc_0061 ?to - loc_type_loc_0054 ?w - window_type_tw_00360)
    :precondition (and
      (at ?v ?from)
      (connects ?r ?from ?to)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
      (not (location-blocked ?from))
      (not (location-blocked ?to))
    )
    :effect (and
      (not (at ?v ?from))
      (at ?v ?to)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-road_0098-tw_00390
    :parameters (?v - vehicle ?r - road_type_road_0098 ?from - loc_type_loc_0061 ?to - loc_type_loc_0054 ?w - window_type_tw_00390)
    :precondition (and
      (at ?v ?from)
      (connects ?r ?from ?to)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
      (not (location-blocked ?from))
      (not (location-blocked ?to))
    )
    :effect (and
      (not (at ?v ?from))
      (at ?v ?to)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-road_0098-tw_00420
    :parameters (?v - vehicle ?r - road_type_road_0098 ?from - loc_type_loc_0061 ?to - loc_type_loc_0054 ?w - window_type_tw_00420)
    :precondition (and
      (at ?v ?from)
      (connects ?r ?from ?to)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
      (not (location-blocked ?from))
      (not (location-blocked ?to))
    )
    :effect (and
      (not (at ?v ?from))
      (at ?v ?to)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-road_0098-tw_00450
    :parameters (?v - vehicle ?r - road_type_road_0098 ?from - loc_type_loc_0061 ?to - loc_type_loc_0054 ?w - window_type_tw_00450)
    :precondition (and
      (at ?v ?from)
      (connects ?r ?from ?to)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
      (not (location-blocked ?from))
      (not (location-blocked ?to))
    )
    :effect (and
      (not (at ?v ?from))
      (at ?v ?to)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-road_0100-tw_00000
    :parameters (?v - vehicle ?r - road_type_road_0100 ?from - loc_type_loc_0062 ?to - loc_type_loc_0020 ?w - window_type_tw_00000)
    :precondition (and
      (at ?v ?from)
      (connects ?r ?from ?to)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
      (not (location-blocked ?from))
      (not (location-blocked ?to))
    )
    :effect (and
      (not (at ?v ?from))
      (at ?v ?to)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-road_0100-tw_00030
    :parameters (?v - vehicle ?r - road_type_road_0100 ?from - loc_type_loc_0062 ?to - loc_type_loc_0020 ?w - window_type_tw_00030)
    :precondition (and
      (at ?v ?from)
      (connects ?r ?from ?to)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
      (not (location-blocked ?from))
      (not (location-blocked ?to))
    )
    :effect (and
      (not (at ?v ?from))
      (at ?v ?to)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-road_0100-tw_00060
    :parameters (?v - vehicle ?r - road_type_road_0100 ?from - loc_type_loc_0062 ?to - loc_type_loc_0020 ?w - window_type_tw_00060)
    :precondition (and
      (at ?v ?from)
      (connects ?r ?from ?to)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
      (not (location-blocked ?from))
      (not (location-blocked ?to))
    )
    :effect (and
      (not (at ?v ?from))
      (at ?v ?to)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-road_0100-tw_00090
    :parameters (?v - vehicle ?r - road_type_road_0100 ?from - loc_type_loc_0062 ?to - loc_type_loc_0020 ?w - window_type_tw_00090)
    :precondition (and
      (at ?v ?from)
      (connects ?r ?from ?to)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
      (not (location-blocked ?from))
      (not (location-blocked ?to))
    )
    :effect (and
      (not (at ?v ?from))
      (at ?v ?to)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-road_0100-tw_00120
    :parameters (?v - vehicle ?r - road_type_road_0100 ?from - loc_type_loc_0062 ?to - loc_type_loc_0020 ?w - window_type_tw_00120)
    :precondition (and
      (at ?v ?from)
      (connects ?r ?from ?to)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
      (not (location-blocked ?from))
      (not (location-blocked ?to))
    )
    :effect (and
      (not (at ?v ?from))
      (at ?v ?to)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-road_0100-tw_00150
    :parameters (?v - vehicle ?r - road_type_road_0100 ?from - loc_type_loc_0062 ?to - loc_type_loc_0020 ?w - window_type_tw_00150)
    :precondition (and
      (at ?v ?from)
      (connects ?r ?from ?to)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
      (not (location-blocked ?from))
      (not (location-blocked ?to))
    )
    :effect (and
      (not (at ?v ?from))
      (at ?v ?to)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-road_0100-tw_00180
    :parameters (?v - vehicle ?r - road_type_road_0100 ?from - loc_type_loc_0062 ?to - loc_type_loc_0020 ?w - window_type_tw_00180)
    :precondition (and
      (at ?v ?from)
      (connects ?r ?from ?to)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
      (not (location-blocked ?from))
      (not (location-blocked ?to))
    )
    :effect (and
      (not (at ?v ?from))
      (at ?v ?to)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-road_0100-tw_00210
    :parameters (?v - vehicle ?r - road_type_road_0100 ?from - loc_type_loc_0062 ?to - loc_type_loc_0020 ?w - window_type_tw_00210)
    :precondition (and
      (at ?v ?from)
      (connects ?r ?from ?to)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
      (not (location-blocked ?from))
      (not (location-blocked ?to))
    )
    :effect (and
      (not (at ?v ?from))
      (at ?v ?to)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-road_0100-tw_00240
    :parameters (?v - vehicle ?r - road_type_road_0100 ?from - loc_type_loc_0062 ?to - loc_type_loc_0020 ?w - window_type_tw_00240)
    :precondition (and
      (at ?v ?from)
      (connects ?r ?from ?to)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
      (not (location-blocked ?from))
      (not (location-blocked ?to))
    )
    :effect (and
      (not (at ?v ?from))
      (at ?v ?to)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-road_0100-tw_00270
    :parameters (?v - vehicle ?r - road_type_road_0100 ?from - loc_type_loc_0062 ?to - loc_type_loc_0020 ?w - window_type_tw_00270)
    :precondition (and
      (at ?v ?from)
      (connects ?r ?from ?to)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
      (not (location-blocked ?from))
      (not (location-blocked ?to))
    )
    :effect (and
      (not (at ?v ?from))
      (at ?v ?to)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-road_0100-tw_00300
    :parameters (?v - vehicle ?r - road_type_road_0100 ?from - loc_type_loc_0062 ?to - loc_type_loc_0020 ?w - window_type_tw_00300)
    :precondition (and
      (at ?v ?from)
      (connects ?r ?from ?to)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
      (not (location-blocked ?from))
      (not (location-blocked ?to))
    )
    :effect (and
      (not (at ?v ?from))
      (at ?v ?to)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-road_0100-tw_00330
    :parameters (?v - vehicle ?r - road_type_road_0100 ?from - loc_type_loc_0062 ?to - loc_type_loc_0020 ?w - window_type_tw_00330)
    :precondition (and
      (at ?v ?from)
      (connects ?r ?from ?to)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
      (not (location-blocked ?from))
      (not (location-blocked ?to))
    )
    :effect (and
      (not (at ?v ?from))
      (at ?v ?to)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-road_0100-tw_00360
    :parameters (?v - vehicle ?r - road_type_road_0100 ?from - loc_type_loc_0062 ?to - loc_type_loc_0020 ?w - window_type_tw_00360)
    :precondition (and
      (at ?v ?from)
      (connects ?r ?from ?to)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
      (not (location-blocked ?from))
      (not (location-blocked ?to))
    )
    :effect (and
      (not (at ?v ?from))
      (at ?v ?to)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-road_0100-tw_00390
    :parameters (?v - vehicle ?r - road_type_road_0100 ?from - loc_type_loc_0062 ?to - loc_type_loc_0020 ?w - window_type_tw_00390)
    :precondition (and
      (at ?v ?from)
      (connects ?r ?from ?to)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
      (not (location-blocked ?from))
      (not (location-blocked ?to))
    )
    :effect (and
      (not (at ?v ?from))
      (at ?v ?to)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-road_0100-tw_00420
    :parameters (?v - vehicle ?r - road_type_road_0100 ?from - loc_type_loc_0062 ?to - loc_type_loc_0020 ?w - window_type_tw_00420)
    :precondition (and
      (at ?v ?from)
      (connects ?r ?from ?to)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
      (not (location-blocked ?from))
      (not (location-blocked ?to))
    )
    :effect (and
      (not (at ?v ?from))
      (at ?v ?to)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-road_0100-tw_00450
    :parameters (?v - vehicle ?r - road_type_road_0100 ?from - loc_type_loc_0062 ?to - loc_type_loc_0020 ?w - window_type_tw_00450)
    :precondition (and
      (at ?v ?from)
      (connects ?r ?from ?to)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
      (not (location-blocked ?from))
      (not (location-blocked ?to))
    )
    :effect (and
      (not (at ?v ?from))
      (at ?v ?to)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-road_0101-tw_00000
    :parameters (?v - vehicle ?r - road_type_road_0101 ?from - loc_type_loc_0062 ?to - loc_type_loc_0030 ?w - window_type_tw_00000)
    :precondition (and
      (at ?v ?from)
      (connects ?r ?from ?to)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
      (not (location-blocked ?from))
      (not (location-blocked ?to))
    )
    :effect (and
      (not (at ?v ?from))
      (at ?v ?to)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-road_0101-tw_00030
    :parameters (?v - vehicle ?r - road_type_road_0101 ?from - loc_type_loc_0062 ?to - loc_type_loc_0030 ?w - window_type_tw_00030)
    :precondition (and
      (at ?v ?from)
      (connects ?r ?from ?to)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
      (not (location-blocked ?from))
      (not (location-blocked ?to))
    )
    :effect (and
      (not (at ?v ?from))
      (at ?v ?to)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-road_0101-tw_00060
    :parameters (?v - vehicle ?r - road_type_road_0101 ?from - loc_type_loc_0062 ?to - loc_type_loc_0030 ?w - window_type_tw_00060)
    :precondition (and
      (at ?v ?from)
      (connects ?r ?from ?to)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
      (not (location-blocked ?from))
      (not (location-blocked ?to))
    )
    :effect (and
      (not (at ?v ?from))
      (at ?v ?to)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-road_0101-tw_00090
    :parameters (?v - vehicle ?r - road_type_road_0101 ?from - loc_type_loc_0062 ?to - loc_type_loc_0030 ?w - window_type_tw_00090)
    :precondition (and
      (at ?v ?from)
      (connects ?r ?from ?to)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
      (not (location-blocked ?from))
      (not (location-blocked ?to))
    )
    :effect (and
      (not (at ?v ?from))
      (at ?v ?to)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-road_0101-tw_00120
    :parameters (?v - vehicle ?r - road_type_road_0101 ?from - loc_type_loc_0062 ?to - loc_type_loc_0030 ?w - window_type_tw_00120)
    :precondition (and
      (at ?v ?from)
      (connects ?r ?from ?to)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
      (not (location-blocked ?from))
      (not (location-blocked ?to))
    )
    :effect (and
      (not (at ?v ?from))
      (at ?v ?to)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-road_0101-tw_00150
    :parameters (?v - vehicle ?r - road_type_road_0101 ?from - loc_type_loc_0062 ?to - loc_type_loc_0030 ?w - window_type_tw_00150)
    :precondition (and
      (at ?v ?from)
      (connects ?r ?from ?to)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
      (not (location-blocked ?from))
      (not (location-blocked ?to))
    )
    :effect (and
      (not (at ?v ?from))
      (at ?v ?to)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-road_0101-tw_00180
    :parameters (?v - vehicle ?r - road_type_road_0101 ?from - loc_type_loc_0062 ?to - loc_type_loc_0030 ?w - window_type_tw_00180)
    :precondition (and
      (at ?v ?from)
      (connects ?r ?from ?to)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
      (not (location-blocked ?from))
      (not (location-blocked ?to))
    )
    :effect (and
      (not (at ?v ?from))
      (at ?v ?to)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-road_0101-tw_00210
    :parameters (?v - vehicle ?r - road_type_road_0101 ?from - loc_type_loc_0062 ?to - loc_type_loc_0030 ?w - window_type_tw_00210)
    :precondition (and
      (at ?v ?from)
      (connects ?r ?from ?to)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
      (not (location-blocked ?from))
      (not (location-blocked ?to))
    )
    :effect (and
      (not (at ?v ?from))
      (at ?v ?to)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-road_0101-tw_00240
    :parameters (?v - vehicle ?r - road_type_road_0101 ?from - loc_type_loc_0062 ?to - loc_type_loc_0030 ?w - window_type_tw_00240)
    :precondition (and
      (at ?v ?from)
      (connects ?r ?from ?to)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
      (not (location-blocked ?from))
      (not (location-blocked ?to))
    )
    :effect (and
      (not (at ?v ?from))
      (at ?v ?to)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-road_0101-tw_00270
    :parameters (?v - vehicle ?r - road_type_road_0101 ?from - loc_type_loc_0062 ?to - loc_type_loc_0030 ?w - window_type_tw_00270)
    :precondition (and
      (at ?v ?from)
      (connects ?r ?from ?to)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
      (not (location-blocked ?from))
      (not (location-blocked ?to))
    )
    :effect (and
      (not (at ?v ?from))
      (at ?v ?to)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-road_0101-tw_00300
    :parameters (?v - vehicle ?r - road_type_road_0101 ?from - loc_type_loc_0062 ?to - loc_type_loc_0030 ?w - window_type_tw_00300)
    :precondition (and
      (at ?v ?from)
      (connects ?r ?from ?to)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
      (not (location-blocked ?from))
      (not (location-blocked ?to))
    )
    :effect (and
      (not (at ?v ?from))
      (at ?v ?to)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-road_0101-tw_00330
    :parameters (?v - vehicle ?r - road_type_road_0101 ?from - loc_type_loc_0062 ?to - loc_type_loc_0030 ?w - window_type_tw_00330)
    :precondition (and
      (at ?v ?from)
      (connects ?r ?from ?to)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
      (not (location-blocked ?from))
      (not (location-blocked ?to))
    )
    :effect (and
      (not (at ?v ?from))
      (at ?v ?to)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-road_0101-tw_00360
    :parameters (?v - vehicle ?r - road_type_road_0101 ?from - loc_type_loc_0062 ?to - loc_type_loc_0030 ?w - window_type_tw_00360)
    :precondition (and
      (at ?v ?from)
      (connects ?r ?from ?to)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
      (not (location-blocked ?from))
      (not (location-blocked ?to))
    )
    :effect (and
      (not (at ?v ?from))
      (at ?v ?to)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-road_0101-tw_00390
    :parameters (?v - vehicle ?r - road_type_road_0101 ?from - loc_type_loc_0062 ?to - loc_type_loc_0030 ?w - window_type_tw_00390)
    :precondition (and
      (at ?v ?from)
      (connects ?r ?from ?to)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
      (not (location-blocked ?from))
      (not (location-blocked ?to))
    )
    :effect (and
      (not (at ?v ?from))
      (at ?v ?to)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-road_0101-tw_00420
    :parameters (?v - vehicle ?r - road_type_road_0101 ?from - loc_type_loc_0062 ?to - loc_type_loc_0030 ?w - window_type_tw_00420)
    :precondition (and
      (at ?v ?from)
      (connects ?r ?from ?to)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
      (not (location-blocked ?from))
      (not (location-blocked ?to))
    )
    :effect (and
      (not (at ?v ?from))
      (at ?v ?to)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-road_0101-tw_00450
    :parameters (?v - vehicle ?r - road_type_road_0101 ?from - loc_type_loc_0062 ?to - loc_type_loc_0030 ?w - window_type_tw_00450)
    :precondition (and
      (at ?v ?from)
      (connects ?r ?from ?to)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
      (not (location-blocked ?from))
      (not (location-blocked ?to))
    )
    :effect (and
      (not (at ?v ?from))
      (at ?v ?to)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-road_0102-tw_00000
    :parameters (?v - vehicle ?r - road_type_road_0102 ?from - loc_type_loc_0064 ?to - loc_type_loc_0002 ?w - window_type_tw_00000)
    :precondition (and
      (at ?v ?from)
      (connects ?r ?from ?to)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
      (not (location-blocked ?from))
      (not (location-blocked ?to))
    )
    :effect (and
      (not (at ?v ?from))
      (at ?v ?to)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-road_0102-tw_00030
    :parameters (?v - vehicle ?r - road_type_road_0102 ?from - loc_type_loc_0064 ?to - loc_type_loc_0002 ?w - window_type_tw_00030)
    :precondition (and
      (at ?v ?from)
      (connects ?r ?from ?to)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
      (not (location-blocked ?from))
      (not (location-blocked ?to))
    )
    :effect (and
      (not (at ?v ?from))
      (at ?v ?to)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-road_0102-tw_00060
    :parameters (?v - vehicle ?r - road_type_road_0102 ?from - loc_type_loc_0064 ?to - loc_type_loc_0002 ?w - window_type_tw_00060)
    :precondition (and
      (at ?v ?from)
      (connects ?r ?from ?to)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
      (not (location-blocked ?from))
      (not (location-blocked ?to))
    )
    :effect (and
      (not (at ?v ?from))
      (at ?v ?to)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-road_0102-tw_00090
    :parameters (?v - vehicle ?r - road_type_road_0102 ?from - loc_type_loc_0064 ?to - loc_type_loc_0002 ?w - window_type_tw_00090)
    :precondition (and
      (at ?v ?from)
      (connects ?r ?from ?to)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
      (not (location-blocked ?from))
      (not (location-blocked ?to))
    )
    :effect (and
      (not (at ?v ?from))
      (at ?v ?to)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-road_0102-tw_00120
    :parameters (?v - vehicle ?r - road_type_road_0102 ?from - loc_type_loc_0064 ?to - loc_type_loc_0002 ?w - window_type_tw_00120)
    :precondition (and
      (at ?v ?from)
      (connects ?r ?from ?to)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
      (not (location-blocked ?from))
      (not (location-blocked ?to))
    )
    :effect (and
      (not (at ?v ?from))
      (at ?v ?to)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-road_0102-tw_00150
    :parameters (?v - vehicle ?r - road_type_road_0102 ?from - loc_type_loc_0064 ?to - loc_type_loc_0002 ?w - window_type_tw_00150)
    :precondition (and
      (at ?v ?from)
      (connects ?r ?from ?to)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
      (not (location-blocked ?from))
      (not (location-blocked ?to))
    )
    :effect (and
      (not (at ?v ?from))
      (at ?v ?to)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-road_0102-tw_00180
    :parameters (?v - vehicle ?r - road_type_road_0102 ?from - loc_type_loc_0064 ?to - loc_type_loc_0002 ?w - window_type_tw_00180)
    :precondition (and
      (at ?v ?from)
      (connects ?r ?from ?to)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
      (not (location-blocked ?from))
      (not (location-blocked ?to))
    )
    :effect (and
      (not (at ?v ?from))
      (at ?v ?to)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-road_0102-tw_00210
    :parameters (?v - vehicle ?r - road_type_road_0102 ?from - loc_type_loc_0064 ?to - loc_type_loc_0002 ?w - window_type_tw_00210)
    :precondition (and
      (at ?v ?from)
      (connects ?r ?from ?to)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
      (not (location-blocked ?from))
      (not (location-blocked ?to))
    )
    :effect (and
      (not (at ?v ?from))
      (at ?v ?to)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-road_0102-tw_00240
    :parameters (?v - vehicle ?r - road_type_road_0102 ?from - loc_type_loc_0064 ?to - loc_type_loc_0002 ?w - window_type_tw_00240)
    :precondition (and
      (at ?v ?from)
      (connects ?r ?from ?to)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
      (not (location-blocked ?from))
      (not (location-blocked ?to))
    )
    :effect (and
      (not (at ?v ?from))
      (at ?v ?to)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-road_0102-tw_00270
    :parameters (?v - vehicle ?r - road_type_road_0102 ?from - loc_type_loc_0064 ?to - loc_type_loc_0002 ?w - window_type_tw_00270)
    :precondition (and
      (at ?v ?from)
      (connects ?r ?from ?to)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
      (not (location-blocked ?from))
      (not (location-blocked ?to))
    )
    :effect (and
      (not (at ?v ?from))
      (at ?v ?to)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-road_0102-tw_00300
    :parameters (?v - vehicle ?r - road_type_road_0102 ?from - loc_type_loc_0064 ?to - loc_type_loc_0002 ?w - window_type_tw_00300)
    :precondition (and
      (at ?v ?from)
      (connects ?r ?from ?to)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
      (not (location-blocked ?from))
      (not (location-blocked ?to))
    )
    :effect (and
      (not (at ?v ?from))
      (at ?v ?to)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-road_0102-tw_00330
    :parameters (?v - vehicle ?r - road_type_road_0102 ?from - loc_type_loc_0064 ?to - loc_type_loc_0002 ?w - window_type_tw_00330)
    :precondition (and
      (at ?v ?from)
      (connects ?r ?from ?to)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
      (not (location-blocked ?from))
      (not (location-blocked ?to))
    )
    :effect (and
      (not (at ?v ?from))
      (at ?v ?to)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-road_0102-tw_00360
    :parameters (?v - vehicle ?r - road_type_road_0102 ?from - loc_type_loc_0064 ?to - loc_type_loc_0002 ?w - window_type_tw_00360)
    :precondition (and
      (at ?v ?from)
      (connects ?r ?from ?to)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
      (not (location-blocked ?from))
      (not (location-blocked ?to))
    )
    :effect (and
      (not (at ?v ?from))
      (at ?v ?to)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-road_0102-tw_00390
    :parameters (?v - vehicle ?r - road_type_road_0102 ?from - loc_type_loc_0064 ?to - loc_type_loc_0002 ?w - window_type_tw_00390)
    :precondition (and
      (at ?v ?from)
      (connects ?r ?from ?to)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
      (not (location-blocked ?from))
      (not (location-blocked ?to))
    )
    :effect (and
      (not (at ?v ?from))
      (at ?v ?to)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-road_0102-tw_00420
    :parameters (?v - vehicle ?r - road_type_road_0102 ?from - loc_type_loc_0064 ?to - loc_type_loc_0002 ?w - window_type_tw_00420)
    :precondition (and
      (at ?v ?from)
      (connects ?r ?from ?to)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
      (not (location-blocked ?from))
      (not (location-blocked ?to))
    )
    :effect (and
      (not (at ?v ?from))
      (at ?v ?to)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-road_0102-tw_00450
    :parameters (?v - vehicle ?r - road_type_road_0102 ?from - loc_type_loc_0064 ?to - loc_type_loc_0002 ?w - window_type_tw_00450)
    :precondition (and
      (at ?v ?from)
      (connects ?r ?from ?to)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
      (not (location-blocked ?from))
      (not (location-blocked ?to))
    )
    :effect (and
      (not (at ?v ?from))
      (at ?v ?to)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-road_0103-tw_00000
    :parameters (?v - vehicle ?r - road_type_road_0103 ?from - loc_type_loc_0064 ?to - loc_type_loc_0094 ?w - window_type_tw_00000)
    :precondition (and
      (at ?v ?from)
      (connects ?r ?from ?to)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
      (not (location-blocked ?from))
      (not (location-blocked ?to))
    )
    :effect (and
      (not (at ?v ?from))
      (at ?v ?to)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-road_0103-tw_00030
    :parameters (?v - vehicle ?r - road_type_road_0103 ?from - loc_type_loc_0064 ?to - loc_type_loc_0094 ?w - window_type_tw_00030)
    :precondition (and
      (at ?v ?from)
      (connects ?r ?from ?to)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
      (not (location-blocked ?from))
      (not (location-blocked ?to))
    )
    :effect (and
      (not (at ?v ?from))
      (at ?v ?to)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-road_0103-tw_00060
    :parameters (?v - vehicle ?r - road_type_road_0103 ?from - loc_type_loc_0064 ?to - loc_type_loc_0094 ?w - window_type_tw_00060)
    :precondition (and
      (at ?v ?from)
      (connects ?r ?from ?to)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
      (not (location-blocked ?from))
      (not (location-blocked ?to))
    )
    :effect (and
      (not (at ?v ?from))
      (at ?v ?to)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-road_0103-tw_00090
    :parameters (?v - vehicle ?r - road_type_road_0103 ?from - loc_type_loc_0064 ?to - loc_type_loc_0094 ?w - window_type_tw_00090)
    :precondition (and
      (at ?v ?from)
      (connects ?r ?from ?to)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
      (not (location-blocked ?from))
      (not (location-blocked ?to))
    )
    :effect (and
      (not (at ?v ?from))
      (at ?v ?to)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-road_0103-tw_00120
    :parameters (?v - vehicle ?r - road_type_road_0103 ?from - loc_type_loc_0064 ?to - loc_type_loc_0094 ?w - window_type_tw_00120)
    :precondition (and
      (at ?v ?from)
      (connects ?r ?from ?to)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
      (not (location-blocked ?from))
      (not (location-blocked ?to))
    )
    :effect (and
      (not (at ?v ?from))
      (at ?v ?to)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-road_0103-tw_00150
    :parameters (?v - vehicle ?r - road_type_road_0103 ?from - loc_type_loc_0064 ?to - loc_type_loc_0094 ?w - window_type_tw_00150)
    :precondition (and
      (at ?v ?from)
      (connects ?r ?from ?to)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
      (not (location-blocked ?from))
      (not (location-blocked ?to))
    )
    :effect (and
      (not (at ?v ?from))
      (at ?v ?to)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-road_0103-tw_00180
    :parameters (?v - vehicle ?r - road_type_road_0103 ?from - loc_type_loc_0064 ?to - loc_type_loc_0094 ?w - window_type_tw_00180)
    :precondition (and
      (at ?v ?from)
      (connects ?r ?from ?to)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
      (not (location-blocked ?from))
      (not (location-blocked ?to))
    )
    :effect (and
      (not (at ?v ?from))
      (at ?v ?to)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-road_0103-tw_00210
    :parameters (?v - vehicle ?r - road_type_road_0103 ?from - loc_type_loc_0064 ?to - loc_type_loc_0094 ?w - window_type_tw_00210)
    :precondition (and
      (at ?v ?from)
      (connects ?r ?from ?to)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
      (not (location-blocked ?from))
      (not (location-blocked ?to))
    )
    :effect (and
      (not (at ?v ?from))
      (at ?v ?to)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-road_0103-tw_00240
    :parameters (?v - vehicle ?r - road_type_road_0103 ?from - loc_type_loc_0064 ?to - loc_type_loc_0094 ?w - window_type_tw_00240)
    :precondition (and
      (at ?v ?from)
      (connects ?r ?from ?to)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
      (not (location-blocked ?from))
      (not (location-blocked ?to))
    )
    :effect (and
      (not (at ?v ?from))
      (at ?v ?to)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-road_0103-tw_00270
    :parameters (?v - vehicle ?r - road_type_road_0103 ?from - loc_type_loc_0064 ?to - loc_type_loc_0094 ?w - window_type_tw_00270)
    :precondition (and
      (at ?v ?from)
      (connects ?r ?from ?to)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
      (not (location-blocked ?from))
      (not (location-blocked ?to))
    )
    :effect (and
      (not (at ?v ?from))
      (at ?v ?to)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-road_0103-tw_00300
    :parameters (?v - vehicle ?r - road_type_road_0103 ?from - loc_type_loc_0064 ?to - loc_type_loc_0094 ?w - window_type_tw_00300)
    :precondition (and
      (at ?v ?from)
      (connects ?r ?from ?to)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
      (not (location-blocked ?from))
      (not (location-blocked ?to))
    )
    :effect (and
      (not (at ?v ?from))
      (at ?v ?to)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-road_0103-tw_00330
    :parameters (?v - vehicle ?r - road_type_road_0103 ?from - loc_type_loc_0064 ?to - loc_type_loc_0094 ?w - window_type_tw_00330)
    :precondition (and
      (at ?v ?from)
      (connects ?r ?from ?to)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
      (not (location-blocked ?from))
      (not (location-blocked ?to))
    )
    :effect (and
      (not (at ?v ?from))
      (at ?v ?to)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-road_0103-tw_00360
    :parameters (?v - vehicle ?r - road_type_road_0103 ?from - loc_type_loc_0064 ?to - loc_type_loc_0094 ?w - window_type_tw_00360)
    :precondition (and
      (at ?v ?from)
      (connects ?r ?from ?to)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
      (not (location-blocked ?from))
      (not (location-blocked ?to))
    )
    :effect (and
      (not (at ?v ?from))
      (at ?v ?to)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-road_0103-tw_00390
    :parameters (?v - vehicle ?r - road_type_road_0103 ?from - loc_type_loc_0064 ?to - loc_type_loc_0094 ?w - window_type_tw_00390)
    :precondition (and
      (at ?v ?from)
      (connects ?r ?from ?to)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
      (not (location-blocked ?from))
      (not (location-blocked ?to))
    )
    :effect (and
      (not (at ?v ?from))
      (at ?v ?to)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-road_0103-tw_00420
    :parameters (?v - vehicle ?r - road_type_road_0103 ?from - loc_type_loc_0064 ?to - loc_type_loc_0094 ?w - window_type_tw_00420)
    :precondition (and
      (at ?v ?from)
      (connects ?r ?from ?to)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
      (not (location-blocked ?from))
      (not (location-blocked ?to))
    )
    :effect (and
      (not (at ?v ?from))
      (at ?v ?to)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-road_0103-tw_00450
    :parameters (?v - vehicle ?r - road_type_road_0103 ?from - loc_type_loc_0064 ?to - loc_type_loc_0094 ?w - window_type_tw_00450)
    :precondition (and
      (at ?v ?from)
      (connects ?r ?from ?to)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
      (not (location-blocked ?from))
      (not (location-blocked ?to))
    )
    :effect (and
      (not (at ?v ?from))
      (at ?v ?to)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-road_0105-tw_00000
    :parameters (?v - vehicle ?r - road_type_road_0105 ?from - loc_type_loc_0065 ?to - loc_type_loc_0016 ?w - window_type_tw_00000)
    :precondition (and
      (at ?v ?from)
      (connects ?r ?from ?to)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
      (not (location-blocked ?from))
      (not (location-blocked ?to))
    )
    :effect (and
      (not (at ?v ?from))
      (at ?v ?to)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-road_0105-tw_00030
    :parameters (?v - vehicle ?r - road_type_road_0105 ?from - loc_type_loc_0065 ?to - loc_type_loc_0016 ?w - window_type_tw_00030)
    :precondition (and
      (at ?v ?from)
      (connects ?r ?from ?to)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
      (not (location-blocked ?from))
      (not (location-blocked ?to))
    )
    :effect (and
      (not (at ?v ?from))
      (at ?v ?to)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-road_0105-tw_00060
    :parameters (?v - vehicle ?r - road_type_road_0105 ?from - loc_type_loc_0065 ?to - loc_type_loc_0016 ?w - window_type_tw_00060)
    :precondition (and
      (at ?v ?from)
      (connects ?r ?from ?to)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
      (not (location-blocked ?from))
      (not (location-blocked ?to))
    )
    :effect (and
      (not (at ?v ?from))
      (at ?v ?to)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-road_0105-tw_00090
    :parameters (?v - vehicle ?r - road_type_road_0105 ?from - loc_type_loc_0065 ?to - loc_type_loc_0016 ?w - window_type_tw_00090)
    :precondition (and
      (at ?v ?from)
      (connects ?r ?from ?to)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
      (not (location-blocked ?from))
      (not (location-blocked ?to))
    )
    :effect (and
      (not (at ?v ?from))
      (at ?v ?to)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-road_0105-tw_00120
    :parameters (?v - vehicle ?r - road_type_road_0105 ?from - loc_type_loc_0065 ?to - loc_type_loc_0016 ?w - window_type_tw_00120)
    :precondition (and
      (at ?v ?from)
      (connects ?r ?from ?to)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
      (not (location-blocked ?from))
      (not (location-blocked ?to))
    )
    :effect (and
      (not (at ?v ?from))
      (at ?v ?to)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-road_0105-tw_00150
    :parameters (?v - vehicle ?r - road_type_road_0105 ?from - loc_type_loc_0065 ?to - loc_type_loc_0016 ?w - window_type_tw_00150)
    :precondition (and
      (at ?v ?from)
      (connects ?r ?from ?to)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
      (not (location-blocked ?from))
      (not (location-blocked ?to))
    )
    :effect (and
      (not (at ?v ?from))
      (at ?v ?to)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-road_0105-tw_00180
    :parameters (?v - vehicle ?r - road_type_road_0105 ?from - loc_type_loc_0065 ?to - loc_type_loc_0016 ?w - window_type_tw_00180)
    :precondition (and
      (at ?v ?from)
      (connects ?r ?from ?to)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
      (not (location-blocked ?from))
      (not (location-blocked ?to))
    )
    :effect (and
      (not (at ?v ?from))
      (at ?v ?to)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-road_0105-tw_00210
    :parameters (?v - vehicle ?r - road_type_road_0105 ?from - loc_type_loc_0065 ?to - loc_type_loc_0016 ?w - window_type_tw_00210)
    :precondition (and
      (at ?v ?from)
      (connects ?r ?from ?to)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
      (not (location-blocked ?from))
      (not (location-blocked ?to))
    )
    :effect (and
      (not (at ?v ?from))
      (at ?v ?to)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-road_0105-tw_00240
    :parameters (?v - vehicle ?r - road_type_road_0105 ?from - loc_type_loc_0065 ?to - loc_type_loc_0016 ?w - window_type_tw_00240)
    :precondition (and
      (at ?v ?from)
      (connects ?r ?from ?to)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
      (not (location-blocked ?from))
      (not (location-blocked ?to))
    )
    :effect (and
      (not (at ?v ?from))
      (at ?v ?to)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-road_0105-tw_00270
    :parameters (?v - vehicle ?r - road_type_road_0105 ?from - loc_type_loc_0065 ?to - loc_type_loc_0016 ?w - window_type_tw_00270)
    :precondition (and
      (at ?v ?from)
      (connects ?r ?from ?to)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
      (not (location-blocked ?from))
      (not (location-blocked ?to))
    )
    :effect (and
      (not (at ?v ?from))
      (at ?v ?to)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-road_0105-tw_00300
    :parameters (?v - vehicle ?r - road_type_road_0105 ?from - loc_type_loc_0065 ?to - loc_type_loc_0016 ?w - window_type_tw_00300)
    :precondition (and
      (at ?v ?from)
      (connects ?r ?from ?to)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
      (not (location-blocked ?from))
      (not (location-blocked ?to))
    )
    :effect (and
      (not (at ?v ?from))
      (at ?v ?to)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-road_0105-tw_00330
    :parameters (?v - vehicle ?r - road_type_road_0105 ?from - loc_type_loc_0065 ?to - loc_type_loc_0016 ?w - window_type_tw_00330)
    :precondition (and
      (at ?v ?from)
      (connects ?r ?from ?to)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
      (not (location-blocked ?from))
      (not (location-blocked ?to))
    )
    :effect (and
      (not (at ?v ?from))
      (at ?v ?to)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-road_0105-tw_00360
    :parameters (?v - vehicle ?r - road_type_road_0105 ?from - loc_type_loc_0065 ?to - loc_type_loc_0016 ?w - window_type_tw_00360)
    :precondition (and
      (at ?v ?from)
      (connects ?r ?from ?to)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
      (not (location-blocked ?from))
      (not (location-blocked ?to))
    )
    :effect (and
      (not (at ?v ?from))
      (at ?v ?to)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-road_0105-tw_00390
    :parameters (?v - vehicle ?r - road_type_road_0105 ?from - loc_type_loc_0065 ?to - loc_type_loc_0016 ?w - window_type_tw_00390)
    :precondition (and
      (at ?v ?from)
      (connects ?r ?from ?to)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
      (not (location-blocked ?from))
      (not (location-blocked ?to))
    )
    :effect (and
      (not (at ?v ?from))
      (at ?v ?to)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-road_0105-tw_00420
    :parameters (?v - vehicle ?r - road_type_road_0105 ?from - loc_type_loc_0065 ?to - loc_type_loc_0016 ?w - window_type_tw_00420)
    :precondition (and
      (at ?v ?from)
      (connects ?r ?from ?to)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
      (not (location-blocked ?from))
      (not (location-blocked ?to))
    )
    :effect (and
      (not (at ?v ?from))
      (at ?v ?to)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-road_0105-tw_00450
    :parameters (?v - vehicle ?r - road_type_road_0105 ?from - loc_type_loc_0065 ?to - loc_type_loc_0016 ?w - window_type_tw_00450)
    :precondition (and
      (at ?v ?from)
      (connects ?r ?from ?to)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
      (not (location-blocked ?from))
      (not (location-blocked ?to))
    )
    :effect (and
      (not (at ?v ?from))
      (at ?v ?to)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-road_0106-tw_00000
    :parameters (?v - vehicle ?r - road_type_road_0106 ?from - loc_type_loc_0065 ?to - loc_type_loc_0051 ?w - window_type_tw_00000)
    :precondition (and
      (at ?v ?from)
      (connects ?r ?from ?to)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
      (not (location-blocked ?from))
      (not (location-blocked ?to))
    )
    :effect (and
      (not (at ?v ?from))
      (at ?v ?to)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-road_0106-tw_00030
    :parameters (?v - vehicle ?r - road_type_road_0106 ?from - loc_type_loc_0065 ?to - loc_type_loc_0051 ?w - window_type_tw_00030)
    :precondition (and
      (at ?v ?from)
      (connects ?r ?from ?to)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
      (not (location-blocked ?from))
      (not (location-blocked ?to))
    )
    :effect (and
      (not (at ?v ?from))
      (at ?v ?to)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-road_0106-tw_00060
    :parameters (?v - vehicle ?r - road_type_road_0106 ?from - loc_type_loc_0065 ?to - loc_type_loc_0051 ?w - window_type_tw_00060)
    :precondition (and
      (at ?v ?from)
      (connects ?r ?from ?to)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
      (not (location-blocked ?from))
      (not (location-blocked ?to))
    )
    :effect (and
      (not (at ?v ?from))
      (at ?v ?to)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-road_0106-tw_00090
    :parameters (?v - vehicle ?r - road_type_road_0106 ?from - loc_type_loc_0065 ?to - loc_type_loc_0051 ?w - window_type_tw_00090)
    :precondition (and
      (at ?v ?from)
      (connects ?r ?from ?to)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
      (not (location-blocked ?from))
      (not (location-blocked ?to))
    )
    :effect (and
      (not (at ?v ?from))
      (at ?v ?to)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-road_0106-tw_00120
    :parameters (?v - vehicle ?r - road_type_road_0106 ?from - loc_type_loc_0065 ?to - loc_type_loc_0051 ?w - window_type_tw_00120)
    :precondition (and
      (at ?v ?from)
      (connects ?r ?from ?to)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
      (not (location-blocked ?from))
      (not (location-blocked ?to))
    )
    :effect (and
      (not (at ?v ?from))
      (at ?v ?to)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-road_0106-tw_00150
    :parameters (?v - vehicle ?r - road_type_road_0106 ?from - loc_type_loc_0065 ?to - loc_type_loc_0051 ?w - window_type_tw_00150)
    :precondition (and
      (at ?v ?from)
      (connects ?r ?from ?to)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
      (not (location-blocked ?from))
      (not (location-blocked ?to))
    )
    :effect (and
      (not (at ?v ?from))
      (at ?v ?to)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-road_0106-tw_00180
    :parameters (?v - vehicle ?r - road_type_road_0106 ?from - loc_type_loc_0065 ?to - loc_type_loc_0051 ?w - window_type_tw_00180)
    :precondition (and
      (at ?v ?from)
      (connects ?r ?from ?to)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
      (not (location-blocked ?from))
      (not (location-blocked ?to))
    )
    :effect (and
      (not (at ?v ?from))
      (at ?v ?to)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-road_0106-tw_00210
    :parameters (?v - vehicle ?r - road_type_road_0106 ?from - loc_type_loc_0065 ?to - loc_type_loc_0051 ?w - window_type_tw_00210)
    :precondition (and
      (at ?v ?from)
      (connects ?r ?from ?to)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
      (not (location-blocked ?from))
      (not (location-blocked ?to))
    )
    :effect (and
      (not (at ?v ?from))
      (at ?v ?to)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-road_0106-tw_00240
    :parameters (?v - vehicle ?r - road_type_road_0106 ?from - loc_type_loc_0065 ?to - loc_type_loc_0051 ?w - window_type_tw_00240)
    :precondition (and
      (at ?v ?from)
      (connects ?r ?from ?to)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
      (not (location-blocked ?from))
      (not (location-blocked ?to))
    )
    :effect (and
      (not (at ?v ?from))
      (at ?v ?to)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-road_0106-tw_00270
    :parameters (?v - vehicle ?r - road_type_road_0106 ?from - loc_type_loc_0065 ?to - loc_type_loc_0051 ?w - window_type_tw_00270)
    :precondition (and
      (at ?v ?from)
      (connects ?r ?from ?to)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
      (not (location-blocked ?from))
      (not (location-blocked ?to))
    )
    :effect (and
      (not (at ?v ?from))
      (at ?v ?to)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-road_0106-tw_00300
    :parameters (?v - vehicle ?r - road_type_road_0106 ?from - loc_type_loc_0065 ?to - loc_type_loc_0051 ?w - window_type_tw_00300)
    :precondition (and
      (at ?v ?from)
      (connects ?r ?from ?to)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
      (not (location-blocked ?from))
      (not (location-blocked ?to))
    )
    :effect (and
      (not (at ?v ?from))
      (at ?v ?to)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-road_0106-tw_00330
    :parameters (?v - vehicle ?r - road_type_road_0106 ?from - loc_type_loc_0065 ?to - loc_type_loc_0051 ?w - window_type_tw_00330)
    :precondition (and
      (at ?v ?from)
      (connects ?r ?from ?to)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
      (not (location-blocked ?from))
      (not (location-blocked ?to))
    )
    :effect (and
      (not (at ?v ?from))
      (at ?v ?to)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-road_0106-tw_00360
    :parameters (?v - vehicle ?r - road_type_road_0106 ?from - loc_type_loc_0065 ?to - loc_type_loc_0051 ?w - window_type_tw_00360)
    :precondition (and
      (at ?v ?from)
      (connects ?r ?from ?to)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
      (not (location-blocked ?from))
      (not (location-blocked ?to))
    )
    :effect (and
      (not (at ?v ?from))
      (at ?v ?to)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-road_0106-tw_00390
    :parameters (?v - vehicle ?r - road_type_road_0106 ?from - loc_type_loc_0065 ?to - loc_type_loc_0051 ?w - window_type_tw_00390)
    :precondition (and
      (at ?v ?from)
      (connects ?r ?from ?to)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
      (not (location-blocked ?from))
      (not (location-blocked ?to))
    )
    :effect (and
      (not (at ?v ?from))
      (at ?v ?to)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-road_0106-tw_00420
    :parameters (?v - vehicle ?r - road_type_road_0106 ?from - loc_type_loc_0065 ?to - loc_type_loc_0051 ?w - window_type_tw_00420)
    :precondition (and
      (at ?v ?from)
      (connects ?r ?from ?to)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
      (not (location-blocked ?from))
      (not (location-blocked ?to))
    )
    :effect (and
      (not (at ?v ?from))
      (at ?v ?to)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-road_0106-tw_00450
    :parameters (?v - vehicle ?r - road_type_road_0106 ?from - loc_type_loc_0065 ?to - loc_type_loc_0051 ?w - window_type_tw_00450)
    :precondition (and
      (at ?v ?from)
      (connects ?r ?from ?to)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
      (not (location-blocked ?from))
      (not (location-blocked ?to))
    )
    :effect (and
      (not (at ?v ?from))
      (at ?v ?to)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-road_0108-tw_00000
    :parameters (?v - vehicle ?r - road_type_road_0108 ?from - loc_type_loc_0067 ?to - loc_type_loc_0083 ?w - window_type_tw_00000)
    :precondition (and
      (at ?v ?from)
      (connects ?r ?from ?to)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
      (not (location-blocked ?from))
      (not (location-blocked ?to))
    )
    :effect (and
      (not (at ?v ?from))
      (at ?v ?to)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-road_0108-tw_00030
    :parameters (?v - vehicle ?r - road_type_road_0108 ?from - loc_type_loc_0067 ?to - loc_type_loc_0083 ?w - window_type_tw_00030)
    :precondition (and
      (at ?v ?from)
      (connects ?r ?from ?to)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
      (not (location-blocked ?from))
      (not (location-blocked ?to))
    )
    :effect (and
      (not (at ?v ?from))
      (at ?v ?to)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-road_0108-tw_00060
    :parameters (?v - vehicle ?r - road_type_road_0108 ?from - loc_type_loc_0067 ?to - loc_type_loc_0083 ?w - window_type_tw_00060)
    :precondition (and
      (at ?v ?from)
      (connects ?r ?from ?to)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
      (not (location-blocked ?from))
      (not (location-blocked ?to))
    )
    :effect (and
      (not (at ?v ?from))
      (at ?v ?to)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-road_0108-tw_00090
    :parameters (?v - vehicle ?r - road_type_road_0108 ?from - loc_type_loc_0067 ?to - loc_type_loc_0083 ?w - window_type_tw_00090)
    :precondition (and
      (at ?v ?from)
      (connects ?r ?from ?to)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
      (not (location-blocked ?from))
      (not (location-blocked ?to))
    )
    :effect (and
      (not (at ?v ?from))
      (at ?v ?to)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-road_0108-tw_00120
    :parameters (?v - vehicle ?r - road_type_road_0108 ?from - loc_type_loc_0067 ?to - loc_type_loc_0083 ?w - window_type_tw_00120)
    :precondition (and
      (at ?v ?from)
      (connects ?r ?from ?to)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
      (not (location-blocked ?from))
      (not (location-blocked ?to))
    )
    :effect (and
      (not (at ?v ?from))
      (at ?v ?to)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-road_0108-tw_00150
    :parameters (?v - vehicle ?r - road_type_road_0108 ?from - loc_type_loc_0067 ?to - loc_type_loc_0083 ?w - window_type_tw_00150)
    :precondition (and
      (at ?v ?from)
      (connects ?r ?from ?to)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
      (not (location-blocked ?from))
      (not (location-blocked ?to))
    )
    :effect (and
      (not (at ?v ?from))
      (at ?v ?to)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-road_0108-tw_00180
    :parameters (?v - vehicle ?r - road_type_road_0108 ?from - loc_type_loc_0067 ?to - loc_type_loc_0083 ?w - window_type_tw_00180)
    :precondition (and
      (at ?v ?from)
      (connects ?r ?from ?to)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
      (not (location-blocked ?from))
      (not (location-blocked ?to))
    )
    :effect (and
      (not (at ?v ?from))
      (at ?v ?to)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-road_0108-tw_00210
    :parameters (?v - vehicle ?r - road_type_road_0108 ?from - loc_type_loc_0067 ?to - loc_type_loc_0083 ?w - window_type_tw_00210)
    :precondition (and
      (at ?v ?from)
      (connects ?r ?from ?to)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
      (not (location-blocked ?from))
      (not (location-blocked ?to))
    )
    :effect (and
      (not (at ?v ?from))
      (at ?v ?to)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-road_0108-tw_00240
    :parameters (?v - vehicle ?r - road_type_road_0108 ?from - loc_type_loc_0067 ?to - loc_type_loc_0083 ?w - window_type_tw_00240)
    :precondition (and
      (at ?v ?from)
      (connects ?r ?from ?to)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
      (not (location-blocked ?from))
      (not (location-blocked ?to))
    )
    :effect (and
      (not (at ?v ?from))
      (at ?v ?to)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-road_0108-tw_00270
    :parameters (?v - vehicle ?r - road_type_road_0108 ?from - loc_type_loc_0067 ?to - loc_type_loc_0083 ?w - window_type_tw_00270)
    :precondition (and
      (at ?v ?from)
      (connects ?r ?from ?to)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
      (not (location-blocked ?from))
      (not (location-blocked ?to))
    )
    :effect (and
      (not (at ?v ?from))
      (at ?v ?to)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-road_0108-tw_00300
    :parameters (?v - vehicle ?r - road_type_road_0108 ?from - loc_type_loc_0067 ?to - loc_type_loc_0083 ?w - window_type_tw_00300)
    :precondition (and
      (at ?v ?from)
      (connects ?r ?from ?to)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
      (not (location-blocked ?from))
      (not (location-blocked ?to))
    )
    :effect (and
      (not (at ?v ?from))
      (at ?v ?to)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-road_0108-tw_00330
    :parameters (?v - vehicle ?r - road_type_road_0108 ?from - loc_type_loc_0067 ?to - loc_type_loc_0083 ?w - window_type_tw_00330)
    :precondition (and
      (at ?v ?from)
      (connects ?r ?from ?to)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
      (not (location-blocked ?from))
      (not (location-blocked ?to))
    )
    :effect (and
      (not (at ?v ?from))
      (at ?v ?to)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-road_0108-tw_00360
    :parameters (?v - vehicle ?r - road_type_road_0108 ?from - loc_type_loc_0067 ?to - loc_type_loc_0083 ?w - window_type_tw_00360)
    :precondition (and
      (at ?v ?from)
      (connects ?r ?from ?to)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
      (not (location-blocked ?from))
      (not (location-blocked ?to))
    )
    :effect (and
      (not (at ?v ?from))
      (at ?v ?to)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-road_0108-tw_00390
    :parameters (?v - vehicle ?r - road_type_road_0108 ?from - loc_type_loc_0067 ?to - loc_type_loc_0083 ?w - window_type_tw_00390)
    :precondition (and
      (at ?v ?from)
      (connects ?r ?from ?to)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
      (not (location-blocked ?from))
      (not (location-blocked ?to))
    )
    :effect (and
      (not (at ?v ?from))
      (at ?v ?to)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-road_0108-tw_00420
    :parameters (?v - vehicle ?r - road_type_road_0108 ?from - loc_type_loc_0067 ?to - loc_type_loc_0083 ?w - window_type_tw_00420)
    :precondition (and
      (at ?v ?from)
      (connects ?r ?from ?to)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
      (not (location-blocked ?from))
      (not (location-blocked ?to))
    )
    :effect (and
      (not (at ?v ?from))
      (at ?v ?to)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-road_0108-tw_00450
    :parameters (?v - vehicle ?r - road_type_road_0108 ?from - loc_type_loc_0067 ?to - loc_type_loc_0083 ?w - window_type_tw_00450)
    :precondition (and
      (at ?v ?from)
      (connects ?r ?from ?to)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
      (not (location-blocked ?from))
      (not (location-blocked ?to))
    )
    :effect (and
      (not (at ?v ?from))
      (at ?v ?to)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-road_0109-tw_00000
    :parameters (?v - vehicle ?r - road_type_road_0109 ?from - loc_type_loc_0067 ?to - loc_type_loc_0068 ?w - window_type_tw_00000)
    :precondition (and
      (at ?v ?from)
      (connects ?r ?from ?to)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
      (not (location-blocked ?from))
      (not (location-blocked ?to))
    )
    :effect (and
      (not (at ?v ?from))
      (at ?v ?to)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-road_0109-tw_00030
    :parameters (?v - vehicle ?r - road_type_road_0109 ?from - loc_type_loc_0067 ?to - loc_type_loc_0068 ?w - window_type_tw_00030)
    :precondition (and
      (at ?v ?from)
      (connects ?r ?from ?to)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
      (not (location-blocked ?from))
      (not (location-blocked ?to))
    )
    :effect (and
      (not (at ?v ?from))
      (at ?v ?to)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-road_0109-tw_00060
    :parameters (?v - vehicle ?r - road_type_road_0109 ?from - loc_type_loc_0067 ?to - loc_type_loc_0068 ?w - window_type_tw_00060)
    :precondition (and
      (at ?v ?from)
      (connects ?r ?from ?to)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
      (not (location-blocked ?from))
      (not (location-blocked ?to))
    )
    :effect (and
      (not (at ?v ?from))
      (at ?v ?to)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-road_0109-tw_00090
    :parameters (?v - vehicle ?r - road_type_road_0109 ?from - loc_type_loc_0067 ?to - loc_type_loc_0068 ?w - window_type_tw_00090)
    :precondition (and
      (at ?v ?from)
      (connects ?r ?from ?to)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
      (not (location-blocked ?from))
      (not (location-blocked ?to))
    )
    :effect (and
      (not (at ?v ?from))
      (at ?v ?to)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-road_0109-tw_00120
    :parameters (?v - vehicle ?r - road_type_road_0109 ?from - loc_type_loc_0067 ?to - loc_type_loc_0068 ?w - window_type_tw_00120)
    :precondition (and
      (at ?v ?from)
      (connects ?r ?from ?to)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
      (not (location-blocked ?from))
      (not (location-blocked ?to))
    )
    :effect (and
      (not (at ?v ?from))
      (at ?v ?to)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-road_0109-tw_00150
    :parameters (?v - vehicle ?r - road_type_road_0109 ?from - loc_type_loc_0067 ?to - loc_type_loc_0068 ?w - window_type_tw_00150)
    :precondition (and
      (at ?v ?from)
      (connects ?r ?from ?to)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
      (not (location-blocked ?from))
      (not (location-blocked ?to))
    )
    :effect (and
      (not (at ?v ?from))
      (at ?v ?to)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-road_0109-tw_00180
    :parameters (?v - vehicle ?r - road_type_road_0109 ?from - loc_type_loc_0067 ?to - loc_type_loc_0068 ?w - window_type_tw_00180)
    :precondition (and
      (at ?v ?from)
      (connects ?r ?from ?to)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
      (not (location-blocked ?from))
      (not (location-blocked ?to))
    )
    :effect (and
      (not (at ?v ?from))
      (at ?v ?to)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-road_0109-tw_00210
    :parameters (?v - vehicle ?r - road_type_road_0109 ?from - loc_type_loc_0067 ?to - loc_type_loc_0068 ?w - window_type_tw_00210)
    :precondition (and
      (at ?v ?from)
      (connects ?r ?from ?to)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
      (not (location-blocked ?from))
      (not (location-blocked ?to))
    )
    :effect (and
      (not (at ?v ?from))
      (at ?v ?to)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-road_0109-tw_00240
    :parameters (?v - vehicle ?r - road_type_road_0109 ?from - loc_type_loc_0067 ?to - loc_type_loc_0068 ?w - window_type_tw_00240)
    :precondition (and
      (at ?v ?from)
      (connects ?r ?from ?to)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
      (not (location-blocked ?from))
      (not (location-blocked ?to))
    )
    :effect (and
      (not (at ?v ?from))
      (at ?v ?to)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-road_0109-tw_00270
    :parameters (?v - vehicle ?r - road_type_road_0109 ?from - loc_type_loc_0067 ?to - loc_type_loc_0068 ?w - window_type_tw_00270)
    :precondition (and
      (at ?v ?from)
      (connects ?r ?from ?to)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
      (not (location-blocked ?from))
      (not (location-blocked ?to))
    )
    :effect (and
      (not (at ?v ?from))
      (at ?v ?to)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-road_0109-tw_00300
    :parameters (?v - vehicle ?r - road_type_road_0109 ?from - loc_type_loc_0067 ?to - loc_type_loc_0068 ?w - window_type_tw_00300)
    :precondition (and
      (at ?v ?from)
      (connects ?r ?from ?to)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
      (not (location-blocked ?from))
      (not (location-blocked ?to))
    )
    :effect (and
      (not (at ?v ?from))
      (at ?v ?to)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-road_0109-tw_00330
    :parameters (?v - vehicle ?r - road_type_road_0109 ?from - loc_type_loc_0067 ?to - loc_type_loc_0068 ?w - window_type_tw_00330)
    :precondition (and
      (at ?v ?from)
      (connects ?r ?from ?to)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
      (not (location-blocked ?from))
      (not (location-blocked ?to))
    )
    :effect (and
      (not (at ?v ?from))
      (at ?v ?to)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-road_0109-tw_00360
    :parameters (?v - vehicle ?r - road_type_road_0109 ?from - loc_type_loc_0067 ?to - loc_type_loc_0068 ?w - window_type_tw_00360)
    :precondition (and
      (at ?v ?from)
      (connects ?r ?from ?to)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
      (not (location-blocked ?from))
      (not (location-blocked ?to))
    )
    :effect (and
      (not (at ?v ?from))
      (at ?v ?to)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-road_0109-tw_00390
    :parameters (?v - vehicle ?r - road_type_road_0109 ?from - loc_type_loc_0067 ?to - loc_type_loc_0068 ?w - window_type_tw_00390)
    :precondition (and
      (at ?v ?from)
      (connects ?r ?from ?to)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
      (not (location-blocked ?from))
      (not (location-blocked ?to))
    )
    :effect (and
      (not (at ?v ?from))
      (at ?v ?to)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-road_0109-tw_00420
    :parameters (?v - vehicle ?r - road_type_road_0109 ?from - loc_type_loc_0067 ?to - loc_type_loc_0068 ?w - window_type_tw_00420)
    :precondition (and
      (at ?v ?from)
      (connects ?r ?from ?to)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
      (not (location-blocked ?from))
      (not (location-blocked ?to))
    )
    :effect (and
      (not (at ?v ?from))
      (at ?v ?to)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-road_0109-tw_00450
    :parameters (?v - vehicle ?r - road_type_road_0109 ?from - loc_type_loc_0067 ?to - loc_type_loc_0068 ?w - window_type_tw_00450)
    :precondition (and
      (at ?v ?from)
      (connects ?r ?from ?to)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
      (not (location-blocked ?from))
      (not (location-blocked ?to))
    )
    :effect (and
      (not (at ?v ?from))
      (at ?v ?to)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-road_0110-tw_00000
    :parameters (?v - vehicle ?r - road_type_road_0110 ?from - loc_type_loc_0067 ?to - loc_type_loc_0058 ?w - window_type_tw_00000)
    :precondition (and
      (at ?v ?from)
      (connects ?r ?from ?to)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
      (not (location-blocked ?from))
      (not (location-blocked ?to))
    )
    :effect (and
      (not (at ?v ?from))
      (at ?v ?to)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-road_0110-tw_00030
    :parameters (?v - vehicle ?r - road_type_road_0110 ?from - loc_type_loc_0067 ?to - loc_type_loc_0058 ?w - window_type_tw_00030)
    :precondition (and
      (at ?v ?from)
      (connects ?r ?from ?to)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
      (not (location-blocked ?from))
      (not (location-blocked ?to))
    )
    :effect (and
      (not (at ?v ?from))
      (at ?v ?to)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-road_0110-tw_00060
    :parameters (?v - vehicle ?r - road_type_road_0110 ?from - loc_type_loc_0067 ?to - loc_type_loc_0058 ?w - window_type_tw_00060)
    :precondition (and
      (at ?v ?from)
      (connects ?r ?from ?to)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
      (not (location-blocked ?from))
      (not (location-blocked ?to))
    )
    :effect (and
      (not (at ?v ?from))
      (at ?v ?to)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-road_0110-tw_00090
    :parameters (?v - vehicle ?r - road_type_road_0110 ?from - loc_type_loc_0067 ?to - loc_type_loc_0058 ?w - window_type_tw_00090)
    :precondition (and
      (at ?v ?from)
      (connects ?r ?from ?to)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
      (not (location-blocked ?from))
      (not (location-blocked ?to))
    )
    :effect (and
      (not (at ?v ?from))
      (at ?v ?to)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-road_0110-tw_00120
    :parameters (?v - vehicle ?r - road_type_road_0110 ?from - loc_type_loc_0067 ?to - loc_type_loc_0058 ?w - window_type_tw_00120)
    :precondition (and
      (at ?v ?from)
      (connects ?r ?from ?to)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
      (not (location-blocked ?from))
      (not (location-blocked ?to))
    )
    :effect (and
      (not (at ?v ?from))
      (at ?v ?to)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-road_0110-tw_00150
    :parameters (?v - vehicle ?r - road_type_road_0110 ?from - loc_type_loc_0067 ?to - loc_type_loc_0058 ?w - window_type_tw_00150)
    :precondition (and
      (at ?v ?from)
      (connects ?r ?from ?to)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
      (not (location-blocked ?from))
      (not (location-blocked ?to))
    )
    :effect (and
      (not (at ?v ?from))
      (at ?v ?to)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-road_0110-tw_00180
    :parameters (?v - vehicle ?r - road_type_road_0110 ?from - loc_type_loc_0067 ?to - loc_type_loc_0058 ?w - window_type_tw_00180)
    :precondition (and
      (at ?v ?from)
      (connects ?r ?from ?to)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
      (not (location-blocked ?from))
      (not (location-blocked ?to))
    )
    :effect (and
      (not (at ?v ?from))
      (at ?v ?to)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-road_0110-tw_00210
    :parameters (?v - vehicle ?r - road_type_road_0110 ?from - loc_type_loc_0067 ?to - loc_type_loc_0058 ?w - window_type_tw_00210)
    :precondition (and
      (at ?v ?from)
      (connects ?r ?from ?to)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
      (not (location-blocked ?from))
      (not (location-blocked ?to))
    )
    :effect (and
      (not (at ?v ?from))
      (at ?v ?to)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-road_0110-tw_00240
    :parameters (?v - vehicle ?r - road_type_road_0110 ?from - loc_type_loc_0067 ?to - loc_type_loc_0058 ?w - window_type_tw_00240)
    :precondition (and
      (at ?v ?from)
      (connects ?r ?from ?to)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
      (not (location-blocked ?from))
      (not (location-blocked ?to))
    )
    :effect (and
      (not (at ?v ?from))
      (at ?v ?to)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-road_0110-tw_00270
    :parameters (?v - vehicle ?r - road_type_road_0110 ?from - loc_type_loc_0067 ?to - loc_type_loc_0058 ?w - window_type_tw_00270)
    :precondition (and
      (at ?v ?from)
      (connects ?r ?from ?to)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
      (not (location-blocked ?from))
      (not (location-blocked ?to))
    )
    :effect (and
      (not (at ?v ?from))
      (at ?v ?to)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-road_0110-tw_00300
    :parameters (?v - vehicle ?r - road_type_road_0110 ?from - loc_type_loc_0067 ?to - loc_type_loc_0058 ?w - window_type_tw_00300)
    :precondition (and
      (at ?v ?from)
      (connects ?r ?from ?to)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
      (not (location-blocked ?from))
      (not (location-blocked ?to))
    )
    :effect (and
      (not (at ?v ?from))
      (at ?v ?to)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-road_0110-tw_00330
    :parameters (?v - vehicle ?r - road_type_road_0110 ?from - loc_type_loc_0067 ?to - loc_type_loc_0058 ?w - window_type_tw_00330)
    :precondition (and
      (at ?v ?from)
      (connects ?r ?from ?to)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
      (not (location-blocked ?from))
      (not (location-blocked ?to))
    )
    :effect (and
      (not (at ?v ?from))
      (at ?v ?to)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-road_0110-tw_00360
    :parameters (?v - vehicle ?r - road_type_road_0110 ?from - loc_type_loc_0067 ?to - loc_type_loc_0058 ?w - window_type_tw_00360)
    :precondition (and
      (at ?v ?from)
      (connects ?r ?from ?to)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
      (not (location-blocked ?from))
      (not (location-blocked ?to))
    )
    :effect (and
      (not (at ?v ?from))
      (at ?v ?to)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-road_0110-tw_00390
    :parameters (?v - vehicle ?r - road_type_road_0110 ?from - loc_type_loc_0067 ?to - loc_type_loc_0058 ?w - window_type_tw_00390)
    :precondition (and
      (at ?v ?from)
      (connects ?r ?from ?to)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
      (not (location-blocked ?from))
      (not (location-blocked ?to))
    )
    :effect (and
      (not (at ?v ?from))
      (at ?v ?to)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-road_0110-tw_00420
    :parameters (?v - vehicle ?r - road_type_road_0110 ?from - loc_type_loc_0067 ?to - loc_type_loc_0058 ?w - window_type_tw_00420)
    :precondition (and
      (at ?v ?from)
      (connects ?r ?from ?to)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
      (not (location-blocked ?from))
      (not (location-blocked ?to))
    )
    :effect (and
      (not (at ?v ?from))
      (at ?v ?to)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-road_0110-tw_00450
    :parameters (?v - vehicle ?r - road_type_road_0110 ?from - loc_type_loc_0067 ?to - loc_type_loc_0058 ?w - window_type_tw_00450)
    :precondition (and
      (at ?v ?from)
      (connects ?r ?from ?to)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
      (not (location-blocked ?from))
      (not (location-blocked ?to))
    )
    :effect (and
      (not (at ?v ?from))
      (at ?v ?to)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-road_0111-tw_00000
    :parameters (?v - vehicle ?r - road_type_road_0111 ?from - loc_type_loc_0068 ?to - loc_type_loc_0046 ?w - window_type_tw_00000)
    :precondition (and
      (at ?v ?from)
      (connects ?r ?from ?to)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
      (not (location-blocked ?from))
      (not (location-blocked ?to))
    )
    :effect (and
      (not (at ?v ?from))
      (at ?v ?to)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-road_0111-tw_00030
    :parameters (?v - vehicle ?r - road_type_road_0111 ?from - loc_type_loc_0068 ?to - loc_type_loc_0046 ?w - window_type_tw_00030)
    :precondition (and
      (at ?v ?from)
      (connects ?r ?from ?to)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
      (not (location-blocked ?from))
      (not (location-blocked ?to))
    )
    :effect (and
      (not (at ?v ?from))
      (at ?v ?to)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-road_0111-tw_00060
    :parameters (?v - vehicle ?r - road_type_road_0111 ?from - loc_type_loc_0068 ?to - loc_type_loc_0046 ?w - window_type_tw_00060)
    :precondition (and
      (at ?v ?from)
      (connects ?r ?from ?to)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
      (not (location-blocked ?from))
      (not (location-blocked ?to))
    )
    :effect (and
      (not (at ?v ?from))
      (at ?v ?to)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-road_0111-tw_00090
    :parameters (?v - vehicle ?r - road_type_road_0111 ?from - loc_type_loc_0068 ?to - loc_type_loc_0046 ?w - window_type_tw_00090)
    :precondition (and
      (at ?v ?from)
      (connects ?r ?from ?to)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
      (not (location-blocked ?from))
      (not (location-blocked ?to))
    )
    :effect (and
      (not (at ?v ?from))
      (at ?v ?to)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-road_0111-tw_00120
    :parameters (?v - vehicle ?r - road_type_road_0111 ?from - loc_type_loc_0068 ?to - loc_type_loc_0046 ?w - window_type_tw_00120)
    :precondition (and
      (at ?v ?from)
      (connects ?r ?from ?to)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
      (not (location-blocked ?from))
      (not (location-blocked ?to))
    )
    :effect (and
      (not (at ?v ?from))
      (at ?v ?to)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-road_0111-tw_00150
    :parameters (?v - vehicle ?r - road_type_road_0111 ?from - loc_type_loc_0068 ?to - loc_type_loc_0046 ?w - window_type_tw_00150)
    :precondition (and
      (at ?v ?from)
      (connects ?r ?from ?to)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
      (not (location-blocked ?from))
      (not (location-blocked ?to))
    )
    :effect (and
      (not (at ?v ?from))
      (at ?v ?to)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-road_0111-tw_00180
    :parameters (?v - vehicle ?r - road_type_road_0111 ?from - loc_type_loc_0068 ?to - loc_type_loc_0046 ?w - window_type_tw_00180)
    :precondition (and
      (at ?v ?from)
      (connects ?r ?from ?to)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
      (not (location-blocked ?from))
      (not (location-blocked ?to))
    )
    :effect (and
      (not (at ?v ?from))
      (at ?v ?to)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-road_0111-tw_00210
    :parameters (?v - vehicle ?r - road_type_road_0111 ?from - loc_type_loc_0068 ?to - loc_type_loc_0046 ?w - window_type_tw_00210)
    :precondition (and
      (at ?v ?from)
      (connects ?r ?from ?to)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
      (not (location-blocked ?from))
      (not (location-blocked ?to))
    )
    :effect (and
      (not (at ?v ?from))
      (at ?v ?to)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-road_0111-tw_00240
    :parameters (?v - vehicle ?r - road_type_road_0111 ?from - loc_type_loc_0068 ?to - loc_type_loc_0046 ?w - window_type_tw_00240)
    :precondition (and
      (at ?v ?from)
      (connects ?r ?from ?to)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
      (not (location-blocked ?from))
      (not (location-blocked ?to))
    )
    :effect (and
      (not (at ?v ?from))
      (at ?v ?to)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-road_0111-tw_00270
    :parameters (?v - vehicle ?r - road_type_road_0111 ?from - loc_type_loc_0068 ?to - loc_type_loc_0046 ?w - window_type_tw_00270)
    :precondition (and
      (at ?v ?from)
      (connects ?r ?from ?to)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
      (not (location-blocked ?from))
      (not (location-blocked ?to))
    )
    :effect (and
      (not (at ?v ?from))
      (at ?v ?to)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-road_0111-tw_00300
    :parameters (?v - vehicle ?r - road_type_road_0111 ?from - loc_type_loc_0068 ?to - loc_type_loc_0046 ?w - window_type_tw_00300)
    :precondition (and
      (at ?v ?from)
      (connects ?r ?from ?to)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
      (not (location-blocked ?from))
      (not (location-blocked ?to))
    )
    :effect (and
      (not (at ?v ?from))
      (at ?v ?to)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-road_0111-tw_00330
    :parameters (?v - vehicle ?r - road_type_road_0111 ?from - loc_type_loc_0068 ?to - loc_type_loc_0046 ?w - window_type_tw_00330)
    :precondition (and
      (at ?v ?from)
      (connects ?r ?from ?to)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
      (not (location-blocked ?from))
      (not (location-blocked ?to))
    )
    :effect (and
      (not (at ?v ?from))
      (at ?v ?to)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-road_0111-tw_00360
    :parameters (?v - vehicle ?r - road_type_road_0111 ?from - loc_type_loc_0068 ?to - loc_type_loc_0046 ?w - window_type_tw_00360)
    :precondition (and
      (at ?v ?from)
      (connects ?r ?from ?to)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
      (not (location-blocked ?from))
      (not (location-blocked ?to))
    )
    :effect (and
      (not (at ?v ?from))
      (at ?v ?to)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-road_0111-tw_00390
    :parameters (?v - vehicle ?r - road_type_road_0111 ?from - loc_type_loc_0068 ?to - loc_type_loc_0046 ?w - window_type_tw_00390)
    :precondition (and
      (at ?v ?from)
      (connects ?r ?from ?to)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
      (not (location-blocked ?from))
      (not (location-blocked ?to))
    )
    :effect (and
      (not (at ?v ?from))
      (at ?v ?to)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-road_0111-tw_00420
    :parameters (?v - vehicle ?r - road_type_road_0111 ?from - loc_type_loc_0068 ?to - loc_type_loc_0046 ?w - window_type_tw_00420)
    :precondition (and
      (at ?v ?from)
      (connects ?r ?from ?to)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
      (not (location-blocked ?from))
      (not (location-blocked ?to))
    )
    :effect (and
      (not (at ?v ?from))
      (at ?v ?to)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-road_0111-tw_00450
    :parameters (?v - vehicle ?r - road_type_road_0111 ?from - loc_type_loc_0068 ?to - loc_type_loc_0046 ?w - window_type_tw_00450)
    :precondition (and
      (at ?v ?from)
      (connects ?r ?from ?to)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
      (not (location-blocked ?from))
      (not (location-blocked ?to))
    )
    :effect (and
      (not (at ?v ?from))
      (at ?v ?to)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-road_0112-tw_00000
    :parameters (?v - vehicle ?r - road_type_road_0112 ?from - loc_type_loc_0069 ?to - loc_type_loc_0067 ?w - window_type_tw_00000)
    :precondition (and
      (at ?v ?from)
      (connects ?r ?from ?to)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
      (not (location-blocked ?from))
      (not (location-blocked ?to))
    )
    :effect (and
      (not (at ?v ?from))
      (at ?v ?to)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-road_0112-tw_00030
    :parameters (?v - vehicle ?r - road_type_road_0112 ?from - loc_type_loc_0069 ?to - loc_type_loc_0067 ?w - window_type_tw_00030)
    :precondition (and
      (at ?v ?from)
      (connects ?r ?from ?to)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
      (not (location-blocked ?from))
      (not (location-blocked ?to))
    )
    :effect (and
      (not (at ?v ?from))
      (at ?v ?to)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-road_0112-tw_00060
    :parameters (?v - vehicle ?r - road_type_road_0112 ?from - loc_type_loc_0069 ?to - loc_type_loc_0067 ?w - window_type_tw_00060)
    :precondition (and
      (at ?v ?from)
      (connects ?r ?from ?to)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
      (not (location-blocked ?from))
      (not (location-blocked ?to))
    )
    :effect (and
      (not (at ?v ?from))
      (at ?v ?to)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-road_0112-tw_00090
    :parameters (?v - vehicle ?r - road_type_road_0112 ?from - loc_type_loc_0069 ?to - loc_type_loc_0067 ?w - window_type_tw_00090)
    :precondition (and
      (at ?v ?from)
      (connects ?r ?from ?to)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
      (not (location-blocked ?from))
      (not (location-blocked ?to))
    )
    :effect (and
      (not (at ?v ?from))
      (at ?v ?to)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-road_0112-tw_00120
    :parameters (?v - vehicle ?r - road_type_road_0112 ?from - loc_type_loc_0069 ?to - loc_type_loc_0067 ?w - window_type_tw_00120)
    :precondition (and
      (at ?v ?from)
      (connects ?r ?from ?to)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
      (not (location-blocked ?from))
      (not (location-blocked ?to))
    )
    :effect (and
      (not (at ?v ?from))
      (at ?v ?to)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-road_0112-tw_00150
    :parameters (?v - vehicle ?r - road_type_road_0112 ?from - loc_type_loc_0069 ?to - loc_type_loc_0067 ?w - window_type_tw_00150)
    :precondition (and
      (at ?v ?from)
      (connects ?r ?from ?to)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
      (not (location-blocked ?from))
      (not (location-blocked ?to))
    )
    :effect (and
      (not (at ?v ?from))
      (at ?v ?to)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-road_0112-tw_00180
    :parameters (?v - vehicle ?r - road_type_road_0112 ?from - loc_type_loc_0069 ?to - loc_type_loc_0067 ?w - window_type_tw_00180)
    :precondition (and
      (at ?v ?from)
      (connects ?r ?from ?to)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
      (not (location-blocked ?from))
      (not (location-blocked ?to))
    )
    :effect (and
      (not (at ?v ?from))
      (at ?v ?to)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-road_0112-tw_00210
    :parameters (?v - vehicle ?r - road_type_road_0112 ?from - loc_type_loc_0069 ?to - loc_type_loc_0067 ?w - window_type_tw_00210)
    :precondition (and
      (at ?v ?from)
      (connects ?r ?from ?to)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
      (not (location-blocked ?from))
      (not (location-blocked ?to))
    )
    :effect (and
      (not (at ?v ?from))
      (at ?v ?to)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-road_0112-tw_00240
    :parameters (?v - vehicle ?r - road_type_road_0112 ?from - loc_type_loc_0069 ?to - loc_type_loc_0067 ?w - window_type_tw_00240)
    :precondition (and
      (at ?v ?from)
      (connects ?r ?from ?to)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
      (not (location-blocked ?from))
      (not (location-blocked ?to))
    )
    :effect (and
      (not (at ?v ?from))
      (at ?v ?to)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-road_0112-tw_00270
    :parameters (?v - vehicle ?r - road_type_road_0112 ?from - loc_type_loc_0069 ?to - loc_type_loc_0067 ?w - window_type_tw_00270)
    :precondition (and
      (at ?v ?from)
      (connects ?r ?from ?to)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
      (not (location-blocked ?from))
      (not (location-blocked ?to))
    )
    :effect (and
      (not (at ?v ?from))
      (at ?v ?to)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-road_0112-tw_00300
    :parameters (?v - vehicle ?r - road_type_road_0112 ?from - loc_type_loc_0069 ?to - loc_type_loc_0067 ?w - window_type_tw_00300)
    :precondition (and
      (at ?v ?from)
      (connects ?r ?from ?to)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
      (not (location-blocked ?from))
      (not (location-blocked ?to))
    )
    :effect (and
      (not (at ?v ?from))
      (at ?v ?to)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-road_0112-tw_00330
    :parameters (?v - vehicle ?r - road_type_road_0112 ?from - loc_type_loc_0069 ?to - loc_type_loc_0067 ?w - window_type_tw_00330)
    :precondition (and
      (at ?v ?from)
      (connects ?r ?from ?to)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
      (not (location-blocked ?from))
      (not (location-blocked ?to))
    )
    :effect (and
      (not (at ?v ?from))
      (at ?v ?to)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-road_0112-tw_00360
    :parameters (?v - vehicle ?r - road_type_road_0112 ?from - loc_type_loc_0069 ?to - loc_type_loc_0067 ?w - window_type_tw_00360)
    :precondition (and
      (at ?v ?from)
      (connects ?r ?from ?to)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
      (not (location-blocked ?from))
      (not (location-blocked ?to))
    )
    :effect (and
      (not (at ?v ?from))
      (at ?v ?to)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-road_0112-tw_00390
    :parameters (?v - vehicle ?r - road_type_road_0112 ?from - loc_type_loc_0069 ?to - loc_type_loc_0067 ?w - window_type_tw_00390)
    :precondition (and
      (at ?v ?from)
      (connects ?r ?from ?to)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
      (not (location-blocked ?from))
      (not (location-blocked ?to))
    )
    :effect (and
      (not (at ?v ?from))
      (at ?v ?to)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-road_0112-tw_00420
    :parameters (?v - vehicle ?r - road_type_road_0112 ?from - loc_type_loc_0069 ?to - loc_type_loc_0067 ?w - window_type_tw_00420)
    :precondition (and
      (at ?v ?from)
      (connects ?r ?from ?to)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
      (not (location-blocked ?from))
      (not (location-blocked ?to))
    )
    :effect (and
      (not (at ?v ?from))
      (at ?v ?to)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-road_0112-tw_00450
    :parameters (?v - vehicle ?r - road_type_road_0112 ?from - loc_type_loc_0069 ?to - loc_type_loc_0067 ?w - window_type_tw_00450)
    :precondition (and
      (at ?v ?from)
      (connects ?r ?from ?to)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
      (not (location-blocked ?from))
      (not (location-blocked ?to))
    )
    :effect (and
      (not (at ?v ?from))
      (at ?v ?to)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-road_0113-tw_00000
    :parameters (?v - vehicle ?r - road_type_road_0113 ?from - loc_type_loc_0070 ?to - loc_type_loc_0036 ?w - window_type_tw_00000)
    :precondition (and
      (at ?v ?from)
      (connects ?r ?from ?to)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
      (not (location-blocked ?from))
      (not (location-blocked ?to))
    )
    :effect (and
      (not (at ?v ?from))
      (at ?v ?to)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-road_0113-tw_00030
    :parameters (?v - vehicle ?r - road_type_road_0113 ?from - loc_type_loc_0070 ?to - loc_type_loc_0036 ?w - window_type_tw_00030)
    :precondition (and
      (at ?v ?from)
      (connects ?r ?from ?to)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
      (not (location-blocked ?from))
      (not (location-blocked ?to))
    )
    :effect (and
      (not (at ?v ?from))
      (at ?v ?to)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-road_0113-tw_00060
    :parameters (?v - vehicle ?r - road_type_road_0113 ?from - loc_type_loc_0070 ?to - loc_type_loc_0036 ?w - window_type_tw_00060)
    :precondition (and
      (at ?v ?from)
      (connects ?r ?from ?to)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
      (not (location-blocked ?from))
      (not (location-blocked ?to))
    )
    :effect (and
      (not (at ?v ?from))
      (at ?v ?to)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-road_0113-tw_00090
    :parameters (?v - vehicle ?r - road_type_road_0113 ?from - loc_type_loc_0070 ?to - loc_type_loc_0036 ?w - window_type_tw_00090)
    :precondition (and
      (at ?v ?from)
      (connects ?r ?from ?to)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
      (not (location-blocked ?from))
      (not (location-blocked ?to))
    )
    :effect (and
      (not (at ?v ?from))
      (at ?v ?to)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-road_0113-tw_00120
    :parameters (?v - vehicle ?r - road_type_road_0113 ?from - loc_type_loc_0070 ?to - loc_type_loc_0036 ?w - window_type_tw_00120)
    :precondition (and
      (at ?v ?from)
      (connects ?r ?from ?to)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
      (not (location-blocked ?from))
      (not (location-blocked ?to))
    )
    :effect (and
      (not (at ?v ?from))
      (at ?v ?to)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-road_0113-tw_00150
    :parameters (?v - vehicle ?r - road_type_road_0113 ?from - loc_type_loc_0070 ?to - loc_type_loc_0036 ?w - window_type_tw_00150)
    :precondition (and
      (at ?v ?from)
      (connects ?r ?from ?to)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
      (not (location-blocked ?from))
      (not (location-blocked ?to))
    )
    :effect (and
      (not (at ?v ?from))
      (at ?v ?to)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-road_0113-tw_00180
    :parameters (?v - vehicle ?r - road_type_road_0113 ?from - loc_type_loc_0070 ?to - loc_type_loc_0036 ?w - window_type_tw_00180)
    :precondition (and
      (at ?v ?from)
      (connects ?r ?from ?to)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
      (not (location-blocked ?from))
      (not (location-blocked ?to))
    )
    :effect (and
      (not (at ?v ?from))
      (at ?v ?to)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-road_0113-tw_00210
    :parameters (?v - vehicle ?r - road_type_road_0113 ?from - loc_type_loc_0070 ?to - loc_type_loc_0036 ?w - window_type_tw_00210)
    :precondition (and
      (at ?v ?from)
      (connects ?r ?from ?to)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
      (not (location-blocked ?from))
      (not (location-blocked ?to))
    )
    :effect (and
      (not (at ?v ?from))
      (at ?v ?to)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-road_0113-tw_00240
    :parameters (?v - vehicle ?r - road_type_road_0113 ?from - loc_type_loc_0070 ?to - loc_type_loc_0036 ?w - window_type_tw_00240)
    :precondition (and
      (at ?v ?from)
      (connects ?r ?from ?to)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
      (not (location-blocked ?from))
      (not (location-blocked ?to))
    )
    :effect (and
      (not (at ?v ?from))
      (at ?v ?to)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-road_0113-tw_00270
    :parameters (?v - vehicle ?r - road_type_road_0113 ?from - loc_type_loc_0070 ?to - loc_type_loc_0036 ?w - window_type_tw_00270)
    :precondition (and
      (at ?v ?from)
      (connects ?r ?from ?to)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
      (not (location-blocked ?from))
      (not (location-blocked ?to))
    )
    :effect (and
      (not (at ?v ?from))
      (at ?v ?to)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-road_0113-tw_00300
    :parameters (?v - vehicle ?r - road_type_road_0113 ?from - loc_type_loc_0070 ?to - loc_type_loc_0036 ?w - window_type_tw_00300)
    :precondition (and
      (at ?v ?from)
      (connects ?r ?from ?to)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
      (not (location-blocked ?from))
      (not (location-blocked ?to))
    )
    :effect (and
      (not (at ?v ?from))
      (at ?v ?to)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-road_0113-tw_00330
    :parameters (?v - vehicle ?r - road_type_road_0113 ?from - loc_type_loc_0070 ?to - loc_type_loc_0036 ?w - window_type_tw_00330)
    :precondition (and
      (at ?v ?from)
      (connects ?r ?from ?to)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
      (not (location-blocked ?from))
      (not (location-blocked ?to))
    )
    :effect (and
      (not (at ?v ?from))
      (at ?v ?to)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-road_0113-tw_00360
    :parameters (?v - vehicle ?r - road_type_road_0113 ?from - loc_type_loc_0070 ?to - loc_type_loc_0036 ?w - window_type_tw_00360)
    :precondition (and
      (at ?v ?from)
      (connects ?r ?from ?to)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
      (not (location-blocked ?from))
      (not (location-blocked ?to))
    )
    :effect (and
      (not (at ?v ?from))
      (at ?v ?to)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-road_0113-tw_00390
    :parameters (?v - vehicle ?r - road_type_road_0113 ?from - loc_type_loc_0070 ?to - loc_type_loc_0036 ?w - window_type_tw_00390)
    :precondition (and
      (at ?v ?from)
      (connects ?r ?from ?to)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
      (not (location-blocked ?from))
      (not (location-blocked ?to))
    )
    :effect (and
      (not (at ?v ?from))
      (at ?v ?to)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-road_0113-tw_00420
    :parameters (?v - vehicle ?r - road_type_road_0113 ?from - loc_type_loc_0070 ?to - loc_type_loc_0036 ?w - window_type_tw_00420)
    :precondition (and
      (at ?v ?from)
      (connects ?r ?from ?to)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
      (not (location-blocked ?from))
      (not (location-blocked ?to))
    )
    :effect (and
      (not (at ?v ?from))
      (at ?v ?to)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-road_0113-tw_00450
    :parameters (?v - vehicle ?r - road_type_road_0113 ?from - loc_type_loc_0070 ?to - loc_type_loc_0036 ?w - window_type_tw_00450)
    :precondition (and
      (at ?v ?from)
      (connects ?r ?from ?to)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
      (not (location-blocked ?from))
      (not (location-blocked ?to))
    )
    :effect (and
      (not (at ?v ?from))
      (at ?v ?to)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-road_0114-tw_00000
    :parameters (?v - vehicle ?r - road_type_road_0114 ?from - loc_type_loc_0070 ?to - loc_type_loc_0046 ?w - window_type_tw_00000)
    :precondition (and
      (at ?v ?from)
      (connects ?r ?from ?to)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
      (not (location-blocked ?from))
      (not (location-blocked ?to))
    )
    :effect (and
      (not (at ?v ?from))
      (at ?v ?to)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-road_0114-tw_00030
    :parameters (?v - vehicle ?r - road_type_road_0114 ?from - loc_type_loc_0070 ?to - loc_type_loc_0046 ?w - window_type_tw_00030)
    :precondition (and
      (at ?v ?from)
      (connects ?r ?from ?to)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
      (not (location-blocked ?from))
      (not (location-blocked ?to))
    )
    :effect (and
      (not (at ?v ?from))
      (at ?v ?to)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-road_0114-tw_00060
    :parameters (?v - vehicle ?r - road_type_road_0114 ?from - loc_type_loc_0070 ?to - loc_type_loc_0046 ?w - window_type_tw_00060)
    :precondition (and
      (at ?v ?from)
      (connects ?r ?from ?to)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
      (not (location-blocked ?from))
      (not (location-blocked ?to))
    )
    :effect (and
      (not (at ?v ?from))
      (at ?v ?to)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-road_0114-tw_00090
    :parameters (?v - vehicle ?r - road_type_road_0114 ?from - loc_type_loc_0070 ?to - loc_type_loc_0046 ?w - window_type_tw_00090)
    :precondition (and
      (at ?v ?from)
      (connects ?r ?from ?to)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
      (not (location-blocked ?from))
      (not (location-blocked ?to))
    )
    :effect (and
      (not (at ?v ?from))
      (at ?v ?to)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-road_0114-tw_00120
    :parameters (?v - vehicle ?r - road_type_road_0114 ?from - loc_type_loc_0070 ?to - loc_type_loc_0046 ?w - window_type_tw_00120)
    :precondition (and
      (at ?v ?from)
      (connects ?r ?from ?to)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
      (not (location-blocked ?from))
      (not (location-blocked ?to))
    )
    :effect (and
      (not (at ?v ?from))
      (at ?v ?to)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-road_0114-tw_00150
    :parameters (?v - vehicle ?r - road_type_road_0114 ?from - loc_type_loc_0070 ?to - loc_type_loc_0046 ?w - window_type_tw_00150)
    :precondition (and
      (at ?v ?from)
      (connects ?r ?from ?to)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
      (not (location-blocked ?from))
      (not (location-blocked ?to))
    )
    :effect (and
      (not (at ?v ?from))
      (at ?v ?to)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-road_0114-tw_00180
    :parameters (?v - vehicle ?r - road_type_road_0114 ?from - loc_type_loc_0070 ?to - loc_type_loc_0046 ?w - window_type_tw_00180)
    :precondition (and
      (at ?v ?from)
      (connects ?r ?from ?to)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
      (not (location-blocked ?from))
      (not (location-blocked ?to))
    )
    :effect (and
      (not (at ?v ?from))
      (at ?v ?to)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-road_0114-tw_00210
    :parameters (?v - vehicle ?r - road_type_road_0114 ?from - loc_type_loc_0070 ?to - loc_type_loc_0046 ?w - window_type_tw_00210)
    :precondition (and
      (at ?v ?from)
      (connects ?r ?from ?to)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
      (not (location-blocked ?from))
      (not (location-blocked ?to))
    )
    :effect (and
      (not (at ?v ?from))
      (at ?v ?to)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-road_0114-tw_00240
    :parameters (?v - vehicle ?r - road_type_road_0114 ?from - loc_type_loc_0070 ?to - loc_type_loc_0046 ?w - window_type_tw_00240)
    :precondition (and
      (at ?v ?from)
      (connects ?r ?from ?to)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
      (not (location-blocked ?from))
      (not (location-blocked ?to))
    )
    :effect (and
      (not (at ?v ?from))
      (at ?v ?to)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-road_0114-tw_00270
    :parameters (?v - vehicle ?r - road_type_road_0114 ?from - loc_type_loc_0070 ?to - loc_type_loc_0046 ?w - window_type_tw_00270)
    :precondition (and
      (at ?v ?from)
      (connects ?r ?from ?to)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
      (not (location-blocked ?from))
      (not (location-blocked ?to))
    )
    :effect (and
      (not (at ?v ?from))
      (at ?v ?to)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-road_0114-tw_00300
    :parameters (?v - vehicle ?r - road_type_road_0114 ?from - loc_type_loc_0070 ?to - loc_type_loc_0046 ?w - window_type_tw_00300)
    :precondition (and
      (at ?v ?from)
      (connects ?r ?from ?to)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
      (not (location-blocked ?from))
      (not (location-blocked ?to))
    )
    :effect (and
      (not (at ?v ?from))
      (at ?v ?to)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-road_0114-tw_00330
    :parameters (?v - vehicle ?r - road_type_road_0114 ?from - loc_type_loc_0070 ?to - loc_type_loc_0046 ?w - window_type_tw_00330)
    :precondition (and
      (at ?v ?from)
      (connects ?r ?from ?to)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
      (not (location-blocked ?from))
      (not (location-blocked ?to))
    )
    :effect (and
      (not (at ?v ?from))
      (at ?v ?to)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-road_0114-tw_00360
    :parameters (?v - vehicle ?r - road_type_road_0114 ?from - loc_type_loc_0070 ?to - loc_type_loc_0046 ?w - window_type_tw_00360)
    :precondition (and
      (at ?v ?from)
      (connects ?r ?from ?to)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
      (not (location-blocked ?from))
      (not (location-blocked ?to))
    )
    :effect (and
      (not (at ?v ?from))
      (at ?v ?to)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-road_0114-tw_00390
    :parameters (?v - vehicle ?r - road_type_road_0114 ?from - loc_type_loc_0070 ?to - loc_type_loc_0046 ?w - window_type_tw_00390)
    :precondition (and
      (at ?v ?from)
      (connects ?r ?from ?to)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
      (not (location-blocked ?from))
      (not (location-blocked ?to))
    )
    :effect (and
      (not (at ?v ?from))
      (at ?v ?to)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-road_0114-tw_00420
    :parameters (?v - vehicle ?r - road_type_road_0114 ?from - loc_type_loc_0070 ?to - loc_type_loc_0046 ?w - window_type_tw_00420)
    :precondition (and
      (at ?v ?from)
      (connects ?r ?from ?to)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
      (not (location-blocked ?from))
      (not (location-blocked ?to))
    )
    :effect (and
      (not (at ?v ?from))
      (at ?v ?to)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-road_0114-tw_00450
    :parameters (?v - vehicle ?r - road_type_road_0114 ?from - loc_type_loc_0070 ?to - loc_type_loc_0046 ?w - window_type_tw_00450)
    :precondition (and
      (at ?v ?from)
      (connects ?r ?from ?to)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
      (not (location-blocked ?from))
      (not (location-blocked ?to))
    )
    :effect (and
      (not (at ?v ?from))
      (at ?v ?to)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-road_0116-tw_00000
    :parameters (?v - vehicle ?r - road_type_road_0116 ?from - loc_type_loc_0071 ?to - loc_type_loc_0054 ?w - window_type_tw_00000)
    :precondition (and
      (at ?v ?from)
      (connects ?r ?from ?to)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
      (not (location-blocked ?from))
      (not (location-blocked ?to))
    )
    :effect (and
      (not (at ?v ?from))
      (at ?v ?to)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-road_0116-tw_00030
    :parameters (?v - vehicle ?r - road_type_road_0116 ?from - loc_type_loc_0071 ?to - loc_type_loc_0054 ?w - window_type_tw_00030)
    :precondition (and
      (at ?v ?from)
      (connects ?r ?from ?to)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
      (not (location-blocked ?from))
      (not (location-blocked ?to))
    )
    :effect (and
      (not (at ?v ?from))
      (at ?v ?to)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-road_0116-tw_00060
    :parameters (?v - vehicle ?r - road_type_road_0116 ?from - loc_type_loc_0071 ?to - loc_type_loc_0054 ?w - window_type_tw_00060)
    :precondition (and
      (at ?v ?from)
      (connects ?r ?from ?to)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
      (not (location-blocked ?from))
      (not (location-blocked ?to))
    )
    :effect (and
      (not (at ?v ?from))
      (at ?v ?to)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-road_0116-tw_00090
    :parameters (?v - vehicle ?r - road_type_road_0116 ?from - loc_type_loc_0071 ?to - loc_type_loc_0054 ?w - window_type_tw_00090)
    :precondition (and
      (at ?v ?from)
      (connects ?r ?from ?to)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
      (not (location-blocked ?from))
      (not (location-blocked ?to))
    )
    :effect (and
      (not (at ?v ?from))
      (at ?v ?to)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-road_0116-tw_00120
    :parameters (?v - vehicle ?r - road_type_road_0116 ?from - loc_type_loc_0071 ?to - loc_type_loc_0054 ?w - window_type_tw_00120)
    :precondition (and
      (at ?v ?from)
      (connects ?r ?from ?to)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
      (not (location-blocked ?from))
      (not (location-blocked ?to))
    )
    :effect (and
      (not (at ?v ?from))
      (at ?v ?to)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-road_0116-tw_00150
    :parameters (?v - vehicle ?r - road_type_road_0116 ?from - loc_type_loc_0071 ?to - loc_type_loc_0054 ?w - window_type_tw_00150)
    :precondition (and
      (at ?v ?from)
      (connects ?r ?from ?to)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
      (not (location-blocked ?from))
      (not (location-blocked ?to))
    )
    :effect (and
      (not (at ?v ?from))
      (at ?v ?to)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-road_0116-tw_00180
    :parameters (?v - vehicle ?r - road_type_road_0116 ?from - loc_type_loc_0071 ?to - loc_type_loc_0054 ?w - window_type_tw_00180)
    :precondition (and
      (at ?v ?from)
      (connects ?r ?from ?to)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
      (not (location-blocked ?from))
      (not (location-blocked ?to))
    )
    :effect (and
      (not (at ?v ?from))
      (at ?v ?to)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-road_0116-tw_00210
    :parameters (?v - vehicle ?r - road_type_road_0116 ?from - loc_type_loc_0071 ?to - loc_type_loc_0054 ?w - window_type_tw_00210)
    :precondition (and
      (at ?v ?from)
      (connects ?r ?from ?to)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
      (not (location-blocked ?from))
      (not (location-blocked ?to))
    )
    :effect (and
      (not (at ?v ?from))
      (at ?v ?to)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-road_0116-tw_00240
    :parameters (?v - vehicle ?r - road_type_road_0116 ?from - loc_type_loc_0071 ?to - loc_type_loc_0054 ?w - window_type_tw_00240)
    :precondition (and
      (at ?v ?from)
      (connects ?r ?from ?to)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
      (not (location-blocked ?from))
      (not (location-blocked ?to))
    )
    :effect (and
      (not (at ?v ?from))
      (at ?v ?to)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-road_0116-tw_00270
    :parameters (?v - vehicle ?r - road_type_road_0116 ?from - loc_type_loc_0071 ?to - loc_type_loc_0054 ?w - window_type_tw_00270)
    :precondition (and
      (at ?v ?from)
      (connects ?r ?from ?to)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
      (not (location-blocked ?from))
      (not (location-blocked ?to))
    )
    :effect (and
      (not (at ?v ?from))
      (at ?v ?to)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-road_0116-tw_00300
    :parameters (?v - vehicle ?r - road_type_road_0116 ?from - loc_type_loc_0071 ?to - loc_type_loc_0054 ?w - window_type_tw_00300)
    :precondition (and
      (at ?v ?from)
      (connects ?r ?from ?to)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
      (not (location-blocked ?from))
      (not (location-blocked ?to))
    )
    :effect (and
      (not (at ?v ?from))
      (at ?v ?to)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-road_0116-tw_00330
    :parameters (?v - vehicle ?r - road_type_road_0116 ?from - loc_type_loc_0071 ?to - loc_type_loc_0054 ?w - window_type_tw_00330)
    :precondition (and
      (at ?v ?from)
      (connects ?r ?from ?to)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
      (not (location-blocked ?from))
      (not (location-blocked ?to))
    )
    :effect (and
      (not (at ?v ?from))
      (at ?v ?to)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-road_0116-tw_00360
    :parameters (?v - vehicle ?r - road_type_road_0116 ?from - loc_type_loc_0071 ?to - loc_type_loc_0054 ?w - window_type_tw_00360)
    :precondition (and
      (at ?v ?from)
      (connects ?r ?from ?to)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
      (not (location-blocked ?from))
      (not (location-blocked ?to))
    )
    :effect (and
      (not (at ?v ?from))
      (at ?v ?to)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-road_0116-tw_00390
    :parameters (?v - vehicle ?r - road_type_road_0116 ?from - loc_type_loc_0071 ?to - loc_type_loc_0054 ?w - window_type_tw_00390)
    :precondition (and
      (at ?v ?from)
      (connects ?r ?from ?to)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
      (not (location-blocked ?from))
      (not (location-blocked ?to))
    )
    :effect (and
      (not (at ?v ?from))
      (at ?v ?to)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-road_0116-tw_00420
    :parameters (?v - vehicle ?r - road_type_road_0116 ?from - loc_type_loc_0071 ?to - loc_type_loc_0054 ?w - window_type_tw_00420)
    :precondition (and
      (at ?v ?from)
      (connects ?r ?from ?to)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
      (not (location-blocked ?from))
      (not (location-blocked ?to))
    )
    :effect (and
      (not (at ?v ?from))
      (at ?v ?to)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-road_0116-tw_00450
    :parameters (?v - vehicle ?r - road_type_road_0116 ?from - loc_type_loc_0071 ?to - loc_type_loc_0054 ?w - window_type_tw_00450)
    :precondition (and
      (at ?v ?from)
      (connects ?r ?from ?to)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
      (not (location-blocked ?from))
      (not (location-blocked ?to))
    )
    :effect (and
      (not (at ?v ?from))
      (at ?v ?to)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-road_0117-tw_00000
    :parameters (?v - vehicle ?r - road_type_road_0117 ?from - loc_type_loc_0072 ?to - loc_type_loc_0089 ?w - window_type_tw_00000)
    :precondition (and
      (at ?v ?from)
      (connects ?r ?from ?to)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
      (not (location-blocked ?from))
      (not (location-blocked ?to))
    )
    :effect (and
      (not (at ?v ?from))
      (at ?v ?to)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-road_0117-tw_00030
    :parameters (?v - vehicle ?r - road_type_road_0117 ?from - loc_type_loc_0072 ?to - loc_type_loc_0089 ?w - window_type_tw_00030)
    :precondition (and
      (at ?v ?from)
      (connects ?r ?from ?to)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
      (not (location-blocked ?from))
      (not (location-blocked ?to))
    )
    :effect (and
      (not (at ?v ?from))
      (at ?v ?to)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-road_0117-tw_00060
    :parameters (?v - vehicle ?r - road_type_road_0117 ?from - loc_type_loc_0072 ?to - loc_type_loc_0089 ?w - window_type_tw_00060)
    :precondition (and
      (at ?v ?from)
      (connects ?r ?from ?to)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
      (not (location-blocked ?from))
      (not (location-blocked ?to))
    )
    :effect (and
      (not (at ?v ?from))
      (at ?v ?to)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-road_0117-tw_00090
    :parameters (?v - vehicle ?r - road_type_road_0117 ?from - loc_type_loc_0072 ?to - loc_type_loc_0089 ?w - window_type_tw_00090)
    :precondition (and
      (at ?v ?from)
      (connects ?r ?from ?to)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
      (not (location-blocked ?from))
      (not (location-blocked ?to))
    )
    :effect (and
      (not (at ?v ?from))
      (at ?v ?to)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-road_0117-tw_00120
    :parameters (?v - vehicle ?r - road_type_road_0117 ?from - loc_type_loc_0072 ?to - loc_type_loc_0089 ?w - window_type_tw_00120)
    :precondition (and
      (at ?v ?from)
      (connects ?r ?from ?to)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
      (not (location-blocked ?from))
      (not (location-blocked ?to))
    )
    :effect (and
      (not (at ?v ?from))
      (at ?v ?to)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-road_0117-tw_00150
    :parameters (?v - vehicle ?r - road_type_road_0117 ?from - loc_type_loc_0072 ?to - loc_type_loc_0089 ?w - window_type_tw_00150)
    :precondition (and
      (at ?v ?from)
      (connects ?r ?from ?to)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
      (not (location-blocked ?from))
      (not (location-blocked ?to))
    )
    :effect (and
      (not (at ?v ?from))
      (at ?v ?to)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-road_0117-tw_00180
    :parameters (?v - vehicle ?r - road_type_road_0117 ?from - loc_type_loc_0072 ?to - loc_type_loc_0089 ?w - window_type_tw_00180)
    :precondition (and
      (at ?v ?from)
      (connects ?r ?from ?to)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
      (not (location-blocked ?from))
      (not (location-blocked ?to))
    )
    :effect (and
      (not (at ?v ?from))
      (at ?v ?to)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-road_0117-tw_00210
    :parameters (?v - vehicle ?r - road_type_road_0117 ?from - loc_type_loc_0072 ?to - loc_type_loc_0089 ?w - window_type_tw_00210)
    :precondition (and
      (at ?v ?from)
      (connects ?r ?from ?to)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
      (not (location-blocked ?from))
      (not (location-blocked ?to))
    )
    :effect (and
      (not (at ?v ?from))
      (at ?v ?to)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-road_0117-tw_00240
    :parameters (?v - vehicle ?r - road_type_road_0117 ?from - loc_type_loc_0072 ?to - loc_type_loc_0089 ?w - window_type_tw_00240)
    :precondition (and
      (at ?v ?from)
      (connects ?r ?from ?to)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
      (not (location-blocked ?from))
      (not (location-blocked ?to))
    )
    :effect (and
      (not (at ?v ?from))
      (at ?v ?to)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-road_0117-tw_00270
    :parameters (?v - vehicle ?r - road_type_road_0117 ?from - loc_type_loc_0072 ?to - loc_type_loc_0089 ?w - window_type_tw_00270)
    :precondition (and
      (at ?v ?from)
      (connects ?r ?from ?to)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
      (not (location-blocked ?from))
      (not (location-blocked ?to))
    )
    :effect (and
      (not (at ?v ?from))
      (at ?v ?to)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-road_0117-tw_00300
    :parameters (?v - vehicle ?r - road_type_road_0117 ?from - loc_type_loc_0072 ?to - loc_type_loc_0089 ?w - window_type_tw_00300)
    :precondition (and
      (at ?v ?from)
      (connects ?r ?from ?to)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
      (not (location-blocked ?from))
      (not (location-blocked ?to))
    )
    :effect (and
      (not (at ?v ?from))
      (at ?v ?to)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-road_0117-tw_00330
    :parameters (?v - vehicle ?r - road_type_road_0117 ?from - loc_type_loc_0072 ?to - loc_type_loc_0089 ?w - window_type_tw_00330)
    :precondition (and
      (at ?v ?from)
      (connects ?r ?from ?to)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
      (not (location-blocked ?from))
      (not (location-blocked ?to))
    )
    :effect (and
      (not (at ?v ?from))
      (at ?v ?to)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-road_0117-tw_00360
    :parameters (?v - vehicle ?r - road_type_road_0117 ?from - loc_type_loc_0072 ?to - loc_type_loc_0089 ?w - window_type_tw_00360)
    :precondition (and
      (at ?v ?from)
      (connects ?r ?from ?to)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
      (not (location-blocked ?from))
      (not (location-blocked ?to))
    )
    :effect (and
      (not (at ?v ?from))
      (at ?v ?to)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-road_0117-tw_00390
    :parameters (?v - vehicle ?r - road_type_road_0117 ?from - loc_type_loc_0072 ?to - loc_type_loc_0089 ?w - window_type_tw_00390)
    :precondition (and
      (at ?v ?from)
      (connects ?r ?from ?to)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
      (not (location-blocked ?from))
      (not (location-blocked ?to))
    )
    :effect (and
      (not (at ?v ?from))
      (at ?v ?to)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-road_0117-tw_00420
    :parameters (?v - vehicle ?r - road_type_road_0117 ?from - loc_type_loc_0072 ?to - loc_type_loc_0089 ?w - window_type_tw_00420)
    :precondition (and
      (at ?v ?from)
      (connects ?r ?from ?to)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
      (not (location-blocked ?from))
      (not (location-blocked ?to))
    )
    :effect (and
      (not (at ?v ?from))
      (at ?v ?to)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-road_0117-tw_00450
    :parameters (?v - vehicle ?r - road_type_road_0117 ?from - loc_type_loc_0072 ?to - loc_type_loc_0089 ?w - window_type_tw_00450)
    :precondition (and
      (at ?v ?from)
      (connects ?r ?from ?to)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
      (not (location-blocked ?from))
      (not (location-blocked ?to))
    )
    :effect (and
      (not (at ?v ?from))
      (at ?v ?to)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-road_0118-tw_00000
    :parameters (?v - vehicle ?r - road_type_road_0118 ?from - loc_type_loc_0072 ?to - loc_type_loc_0054 ?w - window_type_tw_00000)
    :precondition (and
      (at ?v ?from)
      (connects ?r ?from ?to)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
      (not (location-blocked ?from))
      (not (location-blocked ?to))
    )
    :effect (and
      (not (at ?v ?from))
      (at ?v ?to)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-road_0118-tw_00030
    :parameters (?v - vehicle ?r - road_type_road_0118 ?from - loc_type_loc_0072 ?to - loc_type_loc_0054 ?w - window_type_tw_00030)
    :precondition (and
      (at ?v ?from)
      (connects ?r ?from ?to)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
      (not (location-blocked ?from))
      (not (location-blocked ?to))
    )
    :effect (and
      (not (at ?v ?from))
      (at ?v ?to)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-road_0118-tw_00060
    :parameters (?v - vehicle ?r - road_type_road_0118 ?from - loc_type_loc_0072 ?to - loc_type_loc_0054 ?w - window_type_tw_00060)
    :precondition (and
      (at ?v ?from)
      (connects ?r ?from ?to)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
      (not (location-blocked ?from))
      (not (location-blocked ?to))
    )
    :effect (and
      (not (at ?v ?from))
      (at ?v ?to)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-road_0118-tw_00090
    :parameters (?v - vehicle ?r - road_type_road_0118 ?from - loc_type_loc_0072 ?to - loc_type_loc_0054 ?w - window_type_tw_00090)
    :precondition (and
      (at ?v ?from)
      (connects ?r ?from ?to)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
      (not (location-blocked ?from))
      (not (location-blocked ?to))
    )
    :effect (and
      (not (at ?v ?from))
      (at ?v ?to)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-road_0118-tw_00120
    :parameters (?v - vehicle ?r - road_type_road_0118 ?from - loc_type_loc_0072 ?to - loc_type_loc_0054 ?w - window_type_tw_00120)
    :precondition (and
      (at ?v ?from)
      (connects ?r ?from ?to)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
      (not (location-blocked ?from))
      (not (location-blocked ?to))
    )
    :effect (and
      (not (at ?v ?from))
      (at ?v ?to)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-road_0118-tw_00150
    :parameters (?v - vehicle ?r - road_type_road_0118 ?from - loc_type_loc_0072 ?to - loc_type_loc_0054 ?w - window_type_tw_00150)
    :precondition (and
      (at ?v ?from)
      (connects ?r ?from ?to)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
      (not (location-blocked ?from))
      (not (location-blocked ?to))
    )
    :effect (and
      (not (at ?v ?from))
      (at ?v ?to)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-road_0118-tw_00180
    :parameters (?v - vehicle ?r - road_type_road_0118 ?from - loc_type_loc_0072 ?to - loc_type_loc_0054 ?w - window_type_tw_00180)
    :precondition (and
      (at ?v ?from)
      (connects ?r ?from ?to)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
      (not (location-blocked ?from))
      (not (location-blocked ?to))
    )
    :effect (and
      (not (at ?v ?from))
      (at ?v ?to)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-road_0118-tw_00210
    :parameters (?v - vehicle ?r - road_type_road_0118 ?from - loc_type_loc_0072 ?to - loc_type_loc_0054 ?w - window_type_tw_00210)
    :precondition (and
      (at ?v ?from)
      (connects ?r ?from ?to)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
      (not (location-blocked ?from))
      (not (location-blocked ?to))
    )
    :effect (and
      (not (at ?v ?from))
      (at ?v ?to)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-road_0118-tw_00240
    :parameters (?v - vehicle ?r - road_type_road_0118 ?from - loc_type_loc_0072 ?to - loc_type_loc_0054 ?w - window_type_tw_00240)
    :precondition (and
      (at ?v ?from)
      (connects ?r ?from ?to)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
      (not (location-blocked ?from))
      (not (location-blocked ?to))
    )
    :effect (and
      (not (at ?v ?from))
      (at ?v ?to)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-road_0118-tw_00270
    :parameters (?v - vehicle ?r - road_type_road_0118 ?from - loc_type_loc_0072 ?to - loc_type_loc_0054 ?w - window_type_tw_00270)
    :precondition (and
      (at ?v ?from)
      (connects ?r ?from ?to)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
      (not (location-blocked ?from))
      (not (location-blocked ?to))
    )
    :effect (and
      (not (at ?v ?from))
      (at ?v ?to)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-road_0118-tw_00300
    :parameters (?v - vehicle ?r - road_type_road_0118 ?from - loc_type_loc_0072 ?to - loc_type_loc_0054 ?w - window_type_tw_00300)
    :precondition (and
      (at ?v ?from)
      (connects ?r ?from ?to)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
      (not (location-blocked ?from))
      (not (location-blocked ?to))
    )
    :effect (and
      (not (at ?v ?from))
      (at ?v ?to)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-road_0118-tw_00330
    :parameters (?v - vehicle ?r - road_type_road_0118 ?from - loc_type_loc_0072 ?to - loc_type_loc_0054 ?w - window_type_tw_00330)
    :precondition (and
      (at ?v ?from)
      (connects ?r ?from ?to)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
      (not (location-blocked ?from))
      (not (location-blocked ?to))
    )
    :effect (and
      (not (at ?v ?from))
      (at ?v ?to)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-road_0118-tw_00360
    :parameters (?v - vehicle ?r - road_type_road_0118 ?from - loc_type_loc_0072 ?to - loc_type_loc_0054 ?w - window_type_tw_00360)
    :precondition (and
      (at ?v ?from)
      (connects ?r ?from ?to)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
      (not (location-blocked ?from))
      (not (location-blocked ?to))
    )
    :effect (and
      (not (at ?v ?from))
      (at ?v ?to)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-road_0118-tw_00390
    :parameters (?v - vehicle ?r - road_type_road_0118 ?from - loc_type_loc_0072 ?to - loc_type_loc_0054 ?w - window_type_tw_00390)
    :precondition (and
      (at ?v ?from)
      (connects ?r ?from ?to)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
      (not (location-blocked ?from))
      (not (location-blocked ?to))
    )
    :effect (and
      (not (at ?v ?from))
      (at ?v ?to)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-road_0118-tw_00420
    :parameters (?v - vehicle ?r - road_type_road_0118 ?from - loc_type_loc_0072 ?to - loc_type_loc_0054 ?w - window_type_tw_00420)
    :precondition (and
      (at ?v ?from)
      (connects ?r ?from ?to)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
      (not (location-blocked ?from))
      (not (location-blocked ?to))
    )
    :effect (and
      (not (at ?v ?from))
      (at ?v ?to)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-road_0118-tw_00450
    :parameters (?v - vehicle ?r - road_type_road_0118 ?from - loc_type_loc_0072 ?to - loc_type_loc_0054 ?w - window_type_tw_00450)
    :precondition (and
      (at ?v ?from)
      (connects ?r ?from ?to)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
      (not (location-blocked ?from))
      (not (location-blocked ?to))
    )
    :effect (and
      (not (at ?v ?from))
      (at ?v ?to)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-road_0122-tw_00000
    :parameters (?v - vehicle ?r - road_type_road_0122 ?from - loc_type_loc_0076 ?to - loc_type_loc_0035 ?w - window_type_tw_00000)
    :precondition (and
      (at ?v ?from)
      (connects ?r ?from ?to)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
      (not (location-blocked ?from))
      (not (location-blocked ?to))
    )
    :effect (and
      (not (at ?v ?from))
      (at ?v ?to)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-road_0122-tw_00030
    :parameters (?v - vehicle ?r - road_type_road_0122 ?from - loc_type_loc_0076 ?to - loc_type_loc_0035 ?w - window_type_tw_00030)
    :precondition (and
      (at ?v ?from)
      (connects ?r ?from ?to)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
      (not (location-blocked ?from))
      (not (location-blocked ?to))
    )
    :effect (and
      (not (at ?v ?from))
      (at ?v ?to)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-road_0122-tw_00060
    :parameters (?v - vehicle ?r - road_type_road_0122 ?from - loc_type_loc_0076 ?to - loc_type_loc_0035 ?w - window_type_tw_00060)
    :precondition (and
      (at ?v ?from)
      (connects ?r ?from ?to)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
      (not (location-blocked ?from))
      (not (location-blocked ?to))
    )
    :effect (and
      (not (at ?v ?from))
      (at ?v ?to)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-road_0122-tw_00090
    :parameters (?v - vehicle ?r - road_type_road_0122 ?from - loc_type_loc_0076 ?to - loc_type_loc_0035 ?w - window_type_tw_00090)
    :precondition (and
      (at ?v ?from)
      (connects ?r ?from ?to)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
      (not (location-blocked ?from))
      (not (location-blocked ?to))
    )
    :effect (and
      (not (at ?v ?from))
      (at ?v ?to)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-road_0122-tw_00120
    :parameters (?v - vehicle ?r - road_type_road_0122 ?from - loc_type_loc_0076 ?to - loc_type_loc_0035 ?w - window_type_tw_00120)
    :precondition (and
      (at ?v ?from)
      (connects ?r ?from ?to)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
      (not (location-blocked ?from))
      (not (location-blocked ?to))
    )
    :effect (and
      (not (at ?v ?from))
      (at ?v ?to)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-road_0122-tw_00150
    :parameters (?v - vehicle ?r - road_type_road_0122 ?from - loc_type_loc_0076 ?to - loc_type_loc_0035 ?w - window_type_tw_00150)
    :precondition (and
      (at ?v ?from)
      (connects ?r ?from ?to)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
      (not (location-blocked ?from))
      (not (location-blocked ?to))
    )
    :effect (and
      (not (at ?v ?from))
      (at ?v ?to)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-road_0122-tw_00180
    :parameters (?v - vehicle ?r - road_type_road_0122 ?from - loc_type_loc_0076 ?to - loc_type_loc_0035 ?w - window_type_tw_00180)
    :precondition (and
      (at ?v ?from)
      (connects ?r ?from ?to)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
      (not (location-blocked ?from))
      (not (location-blocked ?to))
    )
    :effect (and
      (not (at ?v ?from))
      (at ?v ?to)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-road_0122-tw_00210
    :parameters (?v - vehicle ?r - road_type_road_0122 ?from - loc_type_loc_0076 ?to - loc_type_loc_0035 ?w - window_type_tw_00210)
    :precondition (and
      (at ?v ?from)
      (connects ?r ?from ?to)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
      (not (location-blocked ?from))
      (not (location-blocked ?to))
    )
    :effect (and
      (not (at ?v ?from))
      (at ?v ?to)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-road_0122-tw_00240
    :parameters (?v - vehicle ?r - road_type_road_0122 ?from - loc_type_loc_0076 ?to - loc_type_loc_0035 ?w - window_type_tw_00240)
    :precondition (and
      (at ?v ?from)
      (connects ?r ?from ?to)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
      (not (location-blocked ?from))
      (not (location-blocked ?to))
    )
    :effect (and
      (not (at ?v ?from))
      (at ?v ?to)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-road_0122-tw_00270
    :parameters (?v - vehicle ?r - road_type_road_0122 ?from - loc_type_loc_0076 ?to - loc_type_loc_0035 ?w - window_type_tw_00270)
    :precondition (and
      (at ?v ?from)
      (connects ?r ?from ?to)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
      (not (location-blocked ?from))
      (not (location-blocked ?to))
    )
    :effect (and
      (not (at ?v ?from))
      (at ?v ?to)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-road_0122-tw_00300
    :parameters (?v - vehicle ?r - road_type_road_0122 ?from - loc_type_loc_0076 ?to - loc_type_loc_0035 ?w - window_type_tw_00300)
    :precondition (and
      (at ?v ?from)
      (connects ?r ?from ?to)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
      (not (location-blocked ?from))
      (not (location-blocked ?to))
    )
    :effect (and
      (not (at ?v ?from))
      (at ?v ?to)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-road_0122-tw_00330
    :parameters (?v - vehicle ?r - road_type_road_0122 ?from - loc_type_loc_0076 ?to - loc_type_loc_0035 ?w - window_type_tw_00330)
    :precondition (and
      (at ?v ?from)
      (connects ?r ?from ?to)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
      (not (location-blocked ?from))
      (not (location-blocked ?to))
    )
    :effect (and
      (not (at ?v ?from))
      (at ?v ?to)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-road_0122-tw_00360
    :parameters (?v - vehicle ?r - road_type_road_0122 ?from - loc_type_loc_0076 ?to - loc_type_loc_0035 ?w - window_type_tw_00360)
    :precondition (and
      (at ?v ?from)
      (connects ?r ?from ?to)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
      (not (location-blocked ?from))
      (not (location-blocked ?to))
    )
    :effect (and
      (not (at ?v ?from))
      (at ?v ?to)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-road_0122-tw_00390
    :parameters (?v - vehicle ?r - road_type_road_0122 ?from - loc_type_loc_0076 ?to - loc_type_loc_0035 ?w - window_type_tw_00390)
    :precondition (and
      (at ?v ?from)
      (connects ?r ?from ?to)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
      (not (location-blocked ?from))
      (not (location-blocked ?to))
    )
    :effect (and
      (not (at ?v ?from))
      (at ?v ?to)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-road_0122-tw_00420
    :parameters (?v - vehicle ?r - road_type_road_0122 ?from - loc_type_loc_0076 ?to - loc_type_loc_0035 ?w - window_type_tw_00420)
    :precondition (and
      (at ?v ?from)
      (connects ?r ?from ?to)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
      (not (location-blocked ?from))
      (not (location-blocked ?to))
    )
    :effect (and
      (not (at ?v ?from))
      (at ?v ?to)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-road_0122-tw_00450
    :parameters (?v - vehicle ?r - road_type_road_0122 ?from - loc_type_loc_0076 ?to - loc_type_loc_0035 ?w - window_type_tw_00450)
    :precondition (and
      (at ?v ?from)
      (connects ?r ?from ?to)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
      (not (location-blocked ?from))
      (not (location-blocked ?to))
    )
    :effect (and
      (not (at ?v ?from))
      (at ?v ?to)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-road_0123-tw_00000
    :parameters (?v - vehicle ?r - road_type_road_0123 ?from - loc_type_loc_0076 ?to - loc_type_loc_0096 ?w - window_type_tw_00000)
    :precondition (and
      (at ?v ?from)
      (connects ?r ?from ?to)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
      (not (location-blocked ?from))
      (not (location-blocked ?to))
    )
    :effect (and
      (not (at ?v ?from))
      (at ?v ?to)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-road_0123-tw_00030
    :parameters (?v - vehicle ?r - road_type_road_0123 ?from - loc_type_loc_0076 ?to - loc_type_loc_0096 ?w - window_type_tw_00030)
    :precondition (and
      (at ?v ?from)
      (connects ?r ?from ?to)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
      (not (location-blocked ?from))
      (not (location-blocked ?to))
    )
    :effect (and
      (not (at ?v ?from))
      (at ?v ?to)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-road_0123-tw_00060
    :parameters (?v - vehicle ?r - road_type_road_0123 ?from - loc_type_loc_0076 ?to - loc_type_loc_0096 ?w - window_type_tw_00060)
    :precondition (and
      (at ?v ?from)
      (connects ?r ?from ?to)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
      (not (location-blocked ?from))
      (not (location-blocked ?to))
    )
    :effect (and
      (not (at ?v ?from))
      (at ?v ?to)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-road_0123-tw_00090
    :parameters (?v - vehicle ?r - road_type_road_0123 ?from - loc_type_loc_0076 ?to - loc_type_loc_0096 ?w - window_type_tw_00090)
    :precondition (and
      (at ?v ?from)
      (connects ?r ?from ?to)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
      (not (location-blocked ?from))
      (not (location-blocked ?to))
    )
    :effect (and
      (not (at ?v ?from))
      (at ?v ?to)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-road_0123-tw_00120
    :parameters (?v - vehicle ?r - road_type_road_0123 ?from - loc_type_loc_0076 ?to - loc_type_loc_0096 ?w - window_type_tw_00120)
    :precondition (and
      (at ?v ?from)
      (connects ?r ?from ?to)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
      (not (location-blocked ?from))
      (not (location-blocked ?to))
    )
    :effect (and
      (not (at ?v ?from))
      (at ?v ?to)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-road_0123-tw_00150
    :parameters (?v - vehicle ?r - road_type_road_0123 ?from - loc_type_loc_0076 ?to - loc_type_loc_0096 ?w - window_type_tw_00150)
    :precondition (and
      (at ?v ?from)
      (connects ?r ?from ?to)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
      (not (location-blocked ?from))
      (not (location-blocked ?to))
    )
    :effect (and
      (not (at ?v ?from))
      (at ?v ?to)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-road_0123-tw_00180
    :parameters (?v - vehicle ?r - road_type_road_0123 ?from - loc_type_loc_0076 ?to - loc_type_loc_0096 ?w - window_type_tw_00180)
    :precondition (and
      (at ?v ?from)
      (connects ?r ?from ?to)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
      (not (location-blocked ?from))
      (not (location-blocked ?to))
    )
    :effect (and
      (not (at ?v ?from))
      (at ?v ?to)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-road_0123-tw_00210
    :parameters (?v - vehicle ?r - road_type_road_0123 ?from - loc_type_loc_0076 ?to - loc_type_loc_0096 ?w - window_type_tw_00210)
    :precondition (and
      (at ?v ?from)
      (connects ?r ?from ?to)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
      (not (location-blocked ?from))
      (not (location-blocked ?to))
    )
    :effect (and
      (not (at ?v ?from))
      (at ?v ?to)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-road_0123-tw_00240
    :parameters (?v - vehicle ?r - road_type_road_0123 ?from - loc_type_loc_0076 ?to - loc_type_loc_0096 ?w - window_type_tw_00240)
    :precondition (and
      (at ?v ?from)
      (connects ?r ?from ?to)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
      (not (location-blocked ?from))
      (not (location-blocked ?to))
    )
    :effect (and
      (not (at ?v ?from))
      (at ?v ?to)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-road_0123-tw_00270
    :parameters (?v - vehicle ?r - road_type_road_0123 ?from - loc_type_loc_0076 ?to - loc_type_loc_0096 ?w - window_type_tw_00270)
    :precondition (and
      (at ?v ?from)
      (connects ?r ?from ?to)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
      (not (location-blocked ?from))
      (not (location-blocked ?to))
    )
    :effect (and
      (not (at ?v ?from))
      (at ?v ?to)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-road_0123-tw_00300
    :parameters (?v - vehicle ?r - road_type_road_0123 ?from - loc_type_loc_0076 ?to - loc_type_loc_0096 ?w - window_type_tw_00300)
    :precondition (and
      (at ?v ?from)
      (connects ?r ?from ?to)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
      (not (location-blocked ?from))
      (not (location-blocked ?to))
    )
    :effect (and
      (not (at ?v ?from))
      (at ?v ?to)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-road_0123-tw_00330
    :parameters (?v - vehicle ?r - road_type_road_0123 ?from - loc_type_loc_0076 ?to - loc_type_loc_0096 ?w - window_type_tw_00330)
    :precondition (and
      (at ?v ?from)
      (connects ?r ?from ?to)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
      (not (location-blocked ?from))
      (not (location-blocked ?to))
    )
    :effect (and
      (not (at ?v ?from))
      (at ?v ?to)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-road_0123-tw_00360
    :parameters (?v - vehicle ?r - road_type_road_0123 ?from - loc_type_loc_0076 ?to - loc_type_loc_0096 ?w - window_type_tw_00360)
    :precondition (and
      (at ?v ?from)
      (connects ?r ?from ?to)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
      (not (location-blocked ?from))
      (not (location-blocked ?to))
    )
    :effect (and
      (not (at ?v ?from))
      (at ?v ?to)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-road_0123-tw_00390
    :parameters (?v - vehicle ?r - road_type_road_0123 ?from - loc_type_loc_0076 ?to - loc_type_loc_0096 ?w - window_type_tw_00390)
    :precondition (and
      (at ?v ?from)
      (connects ?r ?from ?to)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
      (not (location-blocked ?from))
      (not (location-blocked ?to))
    )
    :effect (and
      (not (at ?v ?from))
      (at ?v ?to)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-road_0123-tw_00420
    :parameters (?v - vehicle ?r - road_type_road_0123 ?from - loc_type_loc_0076 ?to - loc_type_loc_0096 ?w - window_type_tw_00420)
    :precondition (and
      (at ?v ?from)
      (connects ?r ?from ?to)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
      (not (location-blocked ?from))
      (not (location-blocked ?to))
    )
    :effect (and
      (not (at ?v ?from))
      (at ?v ?to)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-road_0123-tw_00450
    :parameters (?v - vehicle ?r - road_type_road_0123 ?from - loc_type_loc_0076 ?to - loc_type_loc_0096 ?w - window_type_tw_00450)
    :precondition (and
      (at ?v ?from)
      (connects ?r ?from ?to)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
      (not (location-blocked ?from))
      (not (location-blocked ?to))
    )
    :effect (and
      (not (at ?v ?from))
      (at ?v ?to)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-road_0124-tw_00000
    :parameters (?v - vehicle ?r - road_type_road_0124 ?from - loc_type_loc_0077 ?to - loc_type_loc_0080 ?w - window_type_tw_00000)
    :precondition (and
      (at ?v ?from)
      (connects ?r ?from ?to)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
      (not (location-blocked ?from))
      (not (location-blocked ?to))
    )
    :effect (and
      (not (at ?v ?from))
      (at ?v ?to)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-road_0124-tw_00030
    :parameters (?v - vehicle ?r - road_type_road_0124 ?from - loc_type_loc_0077 ?to - loc_type_loc_0080 ?w - window_type_tw_00030)
    :precondition (and
      (at ?v ?from)
      (connects ?r ?from ?to)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
      (not (location-blocked ?from))
      (not (location-blocked ?to))
    )
    :effect (and
      (not (at ?v ?from))
      (at ?v ?to)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-road_0124-tw_00060
    :parameters (?v - vehicle ?r - road_type_road_0124 ?from - loc_type_loc_0077 ?to - loc_type_loc_0080 ?w - window_type_tw_00060)
    :precondition (and
      (at ?v ?from)
      (connects ?r ?from ?to)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
      (not (location-blocked ?from))
      (not (location-blocked ?to))
    )
    :effect (and
      (not (at ?v ?from))
      (at ?v ?to)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-road_0124-tw_00090
    :parameters (?v - vehicle ?r - road_type_road_0124 ?from - loc_type_loc_0077 ?to - loc_type_loc_0080 ?w - window_type_tw_00090)
    :precondition (and
      (at ?v ?from)
      (connects ?r ?from ?to)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
      (not (location-blocked ?from))
      (not (location-blocked ?to))
    )
    :effect (and
      (not (at ?v ?from))
      (at ?v ?to)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-road_0124-tw_00120
    :parameters (?v - vehicle ?r - road_type_road_0124 ?from - loc_type_loc_0077 ?to - loc_type_loc_0080 ?w - window_type_tw_00120)
    :precondition (and
      (at ?v ?from)
      (connects ?r ?from ?to)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
      (not (location-blocked ?from))
      (not (location-blocked ?to))
    )
    :effect (and
      (not (at ?v ?from))
      (at ?v ?to)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-road_0124-tw_00150
    :parameters (?v - vehicle ?r - road_type_road_0124 ?from - loc_type_loc_0077 ?to - loc_type_loc_0080 ?w - window_type_tw_00150)
    :precondition (and
      (at ?v ?from)
      (connects ?r ?from ?to)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
      (not (location-blocked ?from))
      (not (location-blocked ?to))
    )
    :effect (and
      (not (at ?v ?from))
      (at ?v ?to)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-road_0124-tw_00180
    :parameters (?v - vehicle ?r - road_type_road_0124 ?from - loc_type_loc_0077 ?to - loc_type_loc_0080 ?w - window_type_tw_00180)
    :precondition (and
      (at ?v ?from)
      (connects ?r ?from ?to)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
      (not (location-blocked ?from))
      (not (location-blocked ?to))
    )
    :effect (and
      (not (at ?v ?from))
      (at ?v ?to)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-road_0124-tw_00210
    :parameters (?v - vehicle ?r - road_type_road_0124 ?from - loc_type_loc_0077 ?to - loc_type_loc_0080 ?w - window_type_tw_00210)
    :precondition (and
      (at ?v ?from)
      (connects ?r ?from ?to)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
      (not (location-blocked ?from))
      (not (location-blocked ?to))
    )
    :effect (and
      (not (at ?v ?from))
      (at ?v ?to)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-road_0124-tw_00240
    :parameters (?v - vehicle ?r - road_type_road_0124 ?from - loc_type_loc_0077 ?to - loc_type_loc_0080 ?w - window_type_tw_00240)
    :precondition (and
      (at ?v ?from)
      (connects ?r ?from ?to)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
      (not (location-blocked ?from))
      (not (location-blocked ?to))
    )
    :effect (and
      (not (at ?v ?from))
      (at ?v ?to)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-road_0124-tw_00270
    :parameters (?v - vehicle ?r - road_type_road_0124 ?from - loc_type_loc_0077 ?to - loc_type_loc_0080 ?w - window_type_tw_00270)
    :precondition (and
      (at ?v ?from)
      (connects ?r ?from ?to)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
      (not (location-blocked ?from))
      (not (location-blocked ?to))
    )
    :effect (and
      (not (at ?v ?from))
      (at ?v ?to)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-road_0124-tw_00300
    :parameters (?v - vehicle ?r - road_type_road_0124 ?from - loc_type_loc_0077 ?to - loc_type_loc_0080 ?w - window_type_tw_00300)
    :precondition (and
      (at ?v ?from)
      (connects ?r ?from ?to)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
      (not (location-blocked ?from))
      (not (location-blocked ?to))
    )
    :effect (and
      (not (at ?v ?from))
      (at ?v ?to)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-road_0124-tw_00330
    :parameters (?v - vehicle ?r - road_type_road_0124 ?from - loc_type_loc_0077 ?to - loc_type_loc_0080 ?w - window_type_tw_00330)
    :precondition (and
      (at ?v ?from)
      (connects ?r ?from ?to)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
      (not (location-blocked ?from))
      (not (location-blocked ?to))
    )
    :effect (and
      (not (at ?v ?from))
      (at ?v ?to)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-road_0124-tw_00360
    :parameters (?v - vehicle ?r - road_type_road_0124 ?from - loc_type_loc_0077 ?to - loc_type_loc_0080 ?w - window_type_tw_00360)
    :precondition (and
      (at ?v ?from)
      (connects ?r ?from ?to)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
      (not (location-blocked ?from))
      (not (location-blocked ?to))
    )
    :effect (and
      (not (at ?v ?from))
      (at ?v ?to)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-road_0124-tw_00390
    :parameters (?v - vehicle ?r - road_type_road_0124 ?from - loc_type_loc_0077 ?to - loc_type_loc_0080 ?w - window_type_tw_00390)
    :precondition (and
      (at ?v ?from)
      (connects ?r ?from ?to)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
      (not (location-blocked ?from))
      (not (location-blocked ?to))
    )
    :effect (and
      (not (at ?v ?from))
      (at ?v ?to)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-road_0124-tw_00420
    :parameters (?v - vehicle ?r - road_type_road_0124 ?from - loc_type_loc_0077 ?to - loc_type_loc_0080 ?w - window_type_tw_00420)
    :precondition (and
      (at ?v ?from)
      (connects ?r ?from ?to)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
      (not (location-blocked ?from))
      (not (location-blocked ?to))
    )
    :effect (and
      (not (at ?v ?from))
      (at ?v ?to)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-road_0124-tw_00450
    :parameters (?v - vehicle ?r - road_type_road_0124 ?from - loc_type_loc_0077 ?to - loc_type_loc_0080 ?w - window_type_tw_00450)
    :precondition (and
      (at ?v ?from)
      (connects ?r ?from ?to)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
      (not (location-blocked ?from))
      (not (location-blocked ?to))
    )
    :effect (and
      (not (at ?v ?from))
      (at ?v ?to)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-road_0125-tw_00000
    :parameters (?v - vehicle ?r - road_type_road_0125 ?from - loc_type_loc_0078 ?to - loc_type_loc_0051 ?w - window_type_tw_00000)
    :precondition (and
      (at ?v ?from)
      (connects ?r ?from ?to)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
      (not (location-blocked ?from))
      (not (location-blocked ?to))
    )
    :effect (and
      (not (at ?v ?from))
      (at ?v ?to)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-road_0125-tw_00030
    :parameters (?v - vehicle ?r - road_type_road_0125 ?from - loc_type_loc_0078 ?to - loc_type_loc_0051 ?w - window_type_tw_00030)
    :precondition (and
      (at ?v ?from)
      (connects ?r ?from ?to)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
      (not (location-blocked ?from))
      (not (location-blocked ?to))
    )
    :effect (and
      (not (at ?v ?from))
      (at ?v ?to)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-road_0125-tw_00060
    :parameters (?v - vehicle ?r - road_type_road_0125 ?from - loc_type_loc_0078 ?to - loc_type_loc_0051 ?w - window_type_tw_00060)
    :precondition (and
      (at ?v ?from)
      (connects ?r ?from ?to)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
      (not (location-blocked ?from))
      (not (location-blocked ?to))
    )
    :effect (and
      (not (at ?v ?from))
      (at ?v ?to)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-road_0125-tw_00090
    :parameters (?v - vehicle ?r - road_type_road_0125 ?from - loc_type_loc_0078 ?to - loc_type_loc_0051 ?w - window_type_tw_00090)
    :precondition (and
      (at ?v ?from)
      (connects ?r ?from ?to)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
      (not (location-blocked ?from))
      (not (location-blocked ?to))
    )
    :effect (and
      (not (at ?v ?from))
      (at ?v ?to)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-road_0125-tw_00120
    :parameters (?v - vehicle ?r - road_type_road_0125 ?from - loc_type_loc_0078 ?to - loc_type_loc_0051 ?w - window_type_tw_00120)
    :precondition (and
      (at ?v ?from)
      (connects ?r ?from ?to)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
      (not (location-blocked ?from))
      (not (location-blocked ?to))
    )
    :effect (and
      (not (at ?v ?from))
      (at ?v ?to)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-road_0125-tw_00150
    :parameters (?v - vehicle ?r - road_type_road_0125 ?from - loc_type_loc_0078 ?to - loc_type_loc_0051 ?w - window_type_tw_00150)
    :precondition (and
      (at ?v ?from)
      (connects ?r ?from ?to)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
      (not (location-blocked ?from))
      (not (location-blocked ?to))
    )
    :effect (and
      (not (at ?v ?from))
      (at ?v ?to)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-road_0125-tw_00180
    :parameters (?v - vehicle ?r - road_type_road_0125 ?from - loc_type_loc_0078 ?to - loc_type_loc_0051 ?w - window_type_tw_00180)
    :precondition (and
      (at ?v ?from)
      (connects ?r ?from ?to)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
      (not (location-blocked ?from))
      (not (location-blocked ?to))
    )
    :effect (and
      (not (at ?v ?from))
      (at ?v ?to)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-road_0125-tw_00210
    :parameters (?v - vehicle ?r - road_type_road_0125 ?from - loc_type_loc_0078 ?to - loc_type_loc_0051 ?w - window_type_tw_00210)
    :precondition (and
      (at ?v ?from)
      (connects ?r ?from ?to)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
      (not (location-blocked ?from))
      (not (location-blocked ?to))
    )
    :effect (and
      (not (at ?v ?from))
      (at ?v ?to)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-road_0125-tw_00240
    :parameters (?v - vehicle ?r - road_type_road_0125 ?from - loc_type_loc_0078 ?to - loc_type_loc_0051 ?w - window_type_tw_00240)
    :precondition (and
      (at ?v ?from)
      (connects ?r ?from ?to)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
      (not (location-blocked ?from))
      (not (location-blocked ?to))
    )
    :effect (and
      (not (at ?v ?from))
      (at ?v ?to)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-road_0125-tw_00270
    :parameters (?v - vehicle ?r - road_type_road_0125 ?from - loc_type_loc_0078 ?to - loc_type_loc_0051 ?w - window_type_tw_00270)
    :precondition (and
      (at ?v ?from)
      (connects ?r ?from ?to)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
      (not (location-blocked ?from))
      (not (location-blocked ?to))
    )
    :effect (and
      (not (at ?v ?from))
      (at ?v ?to)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-road_0125-tw_00300
    :parameters (?v - vehicle ?r - road_type_road_0125 ?from - loc_type_loc_0078 ?to - loc_type_loc_0051 ?w - window_type_tw_00300)
    :precondition (and
      (at ?v ?from)
      (connects ?r ?from ?to)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
      (not (location-blocked ?from))
      (not (location-blocked ?to))
    )
    :effect (and
      (not (at ?v ?from))
      (at ?v ?to)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-road_0125-tw_00330
    :parameters (?v - vehicle ?r - road_type_road_0125 ?from - loc_type_loc_0078 ?to - loc_type_loc_0051 ?w - window_type_tw_00330)
    :precondition (and
      (at ?v ?from)
      (connects ?r ?from ?to)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
      (not (location-blocked ?from))
      (not (location-blocked ?to))
    )
    :effect (and
      (not (at ?v ?from))
      (at ?v ?to)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-road_0125-tw_00360
    :parameters (?v - vehicle ?r - road_type_road_0125 ?from - loc_type_loc_0078 ?to - loc_type_loc_0051 ?w - window_type_tw_00360)
    :precondition (and
      (at ?v ?from)
      (connects ?r ?from ?to)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
      (not (location-blocked ?from))
      (not (location-blocked ?to))
    )
    :effect (and
      (not (at ?v ?from))
      (at ?v ?to)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-road_0125-tw_00390
    :parameters (?v - vehicle ?r - road_type_road_0125 ?from - loc_type_loc_0078 ?to - loc_type_loc_0051 ?w - window_type_tw_00390)
    :precondition (and
      (at ?v ?from)
      (connects ?r ?from ?to)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
      (not (location-blocked ?from))
      (not (location-blocked ?to))
    )
    :effect (and
      (not (at ?v ?from))
      (at ?v ?to)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-road_0125-tw_00420
    :parameters (?v - vehicle ?r - road_type_road_0125 ?from - loc_type_loc_0078 ?to - loc_type_loc_0051 ?w - window_type_tw_00420)
    :precondition (and
      (at ?v ?from)
      (connects ?r ?from ?to)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
      (not (location-blocked ?from))
      (not (location-blocked ?to))
    )
    :effect (and
      (not (at ?v ?from))
      (at ?v ?to)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-road_0125-tw_00450
    :parameters (?v - vehicle ?r - road_type_road_0125 ?from - loc_type_loc_0078 ?to - loc_type_loc_0051 ?w - window_type_tw_00450)
    :precondition (and
      (at ?v ?from)
      (connects ?r ?from ?to)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
      (not (location-blocked ?from))
      (not (location-blocked ?to))
    )
    :effect (and
      (not (at ?v ?from))
      (at ?v ?to)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-road_0126-tw_00000
    :parameters (?v - vehicle ?r - road_type_road_0126 ?from - loc_type_loc_0078 ?to - loc_type_loc_0030 ?w - window_type_tw_00000)
    :precondition (and
      (at ?v ?from)
      (connects ?r ?from ?to)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
      (not (location-blocked ?from))
      (not (location-blocked ?to))
    )
    :effect (and
      (not (at ?v ?from))
      (at ?v ?to)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-road_0126-tw_00030
    :parameters (?v - vehicle ?r - road_type_road_0126 ?from - loc_type_loc_0078 ?to - loc_type_loc_0030 ?w - window_type_tw_00030)
    :precondition (and
      (at ?v ?from)
      (connects ?r ?from ?to)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
      (not (location-blocked ?from))
      (not (location-blocked ?to))
    )
    :effect (and
      (not (at ?v ?from))
      (at ?v ?to)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-road_0126-tw_00060
    :parameters (?v - vehicle ?r - road_type_road_0126 ?from - loc_type_loc_0078 ?to - loc_type_loc_0030 ?w - window_type_tw_00060)
    :precondition (and
      (at ?v ?from)
      (connects ?r ?from ?to)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
      (not (location-blocked ?from))
      (not (location-blocked ?to))
    )
    :effect (and
      (not (at ?v ?from))
      (at ?v ?to)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-road_0126-tw_00090
    :parameters (?v - vehicle ?r - road_type_road_0126 ?from - loc_type_loc_0078 ?to - loc_type_loc_0030 ?w - window_type_tw_00090)
    :precondition (and
      (at ?v ?from)
      (connects ?r ?from ?to)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
      (not (location-blocked ?from))
      (not (location-blocked ?to))
    )
    :effect (and
      (not (at ?v ?from))
      (at ?v ?to)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-road_0126-tw_00120
    :parameters (?v - vehicle ?r - road_type_road_0126 ?from - loc_type_loc_0078 ?to - loc_type_loc_0030 ?w - window_type_tw_00120)
    :precondition (and
      (at ?v ?from)
      (connects ?r ?from ?to)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
      (not (location-blocked ?from))
      (not (location-blocked ?to))
    )
    :effect (and
      (not (at ?v ?from))
      (at ?v ?to)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-road_0126-tw_00150
    :parameters (?v - vehicle ?r - road_type_road_0126 ?from - loc_type_loc_0078 ?to - loc_type_loc_0030 ?w - window_type_tw_00150)
    :precondition (and
      (at ?v ?from)
      (connects ?r ?from ?to)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
      (not (location-blocked ?from))
      (not (location-blocked ?to))
    )
    :effect (and
      (not (at ?v ?from))
      (at ?v ?to)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-road_0126-tw_00180
    :parameters (?v - vehicle ?r - road_type_road_0126 ?from - loc_type_loc_0078 ?to - loc_type_loc_0030 ?w - window_type_tw_00180)
    :precondition (and
      (at ?v ?from)
      (connects ?r ?from ?to)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
      (not (location-blocked ?from))
      (not (location-blocked ?to))
    )
    :effect (and
      (not (at ?v ?from))
      (at ?v ?to)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-road_0126-tw_00210
    :parameters (?v - vehicle ?r - road_type_road_0126 ?from - loc_type_loc_0078 ?to - loc_type_loc_0030 ?w - window_type_tw_00210)
    :precondition (and
      (at ?v ?from)
      (connects ?r ?from ?to)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
      (not (location-blocked ?from))
      (not (location-blocked ?to))
    )
    :effect (and
      (not (at ?v ?from))
      (at ?v ?to)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-road_0126-tw_00240
    :parameters (?v - vehicle ?r - road_type_road_0126 ?from - loc_type_loc_0078 ?to - loc_type_loc_0030 ?w - window_type_tw_00240)
    :precondition (and
      (at ?v ?from)
      (connects ?r ?from ?to)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
      (not (location-blocked ?from))
      (not (location-blocked ?to))
    )
    :effect (and
      (not (at ?v ?from))
      (at ?v ?to)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-road_0126-tw_00270
    :parameters (?v - vehicle ?r - road_type_road_0126 ?from - loc_type_loc_0078 ?to - loc_type_loc_0030 ?w - window_type_tw_00270)
    :precondition (and
      (at ?v ?from)
      (connects ?r ?from ?to)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
      (not (location-blocked ?from))
      (not (location-blocked ?to))
    )
    :effect (and
      (not (at ?v ?from))
      (at ?v ?to)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-road_0126-tw_00300
    :parameters (?v - vehicle ?r - road_type_road_0126 ?from - loc_type_loc_0078 ?to - loc_type_loc_0030 ?w - window_type_tw_00300)
    :precondition (and
      (at ?v ?from)
      (connects ?r ?from ?to)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
      (not (location-blocked ?from))
      (not (location-blocked ?to))
    )
    :effect (and
      (not (at ?v ?from))
      (at ?v ?to)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-road_0126-tw_00330
    :parameters (?v - vehicle ?r - road_type_road_0126 ?from - loc_type_loc_0078 ?to - loc_type_loc_0030 ?w - window_type_tw_00330)
    :precondition (and
      (at ?v ?from)
      (connects ?r ?from ?to)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
      (not (location-blocked ?from))
      (not (location-blocked ?to))
    )
    :effect (and
      (not (at ?v ?from))
      (at ?v ?to)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-road_0126-tw_00360
    :parameters (?v - vehicle ?r - road_type_road_0126 ?from - loc_type_loc_0078 ?to - loc_type_loc_0030 ?w - window_type_tw_00360)
    :precondition (and
      (at ?v ?from)
      (connects ?r ?from ?to)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
      (not (location-blocked ?from))
      (not (location-blocked ?to))
    )
    :effect (and
      (not (at ?v ?from))
      (at ?v ?to)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-road_0126-tw_00390
    :parameters (?v - vehicle ?r - road_type_road_0126 ?from - loc_type_loc_0078 ?to - loc_type_loc_0030 ?w - window_type_tw_00390)
    :precondition (and
      (at ?v ?from)
      (connects ?r ?from ?to)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
      (not (location-blocked ?from))
      (not (location-blocked ?to))
    )
    :effect (and
      (not (at ?v ?from))
      (at ?v ?to)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-road_0126-tw_00420
    :parameters (?v - vehicle ?r - road_type_road_0126 ?from - loc_type_loc_0078 ?to - loc_type_loc_0030 ?w - window_type_tw_00420)
    :precondition (and
      (at ?v ?from)
      (connects ?r ?from ?to)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
      (not (location-blocked ?from))
      (not (location-blocked ?to))
    )
    :effect (and
      (not (at ?v ?from))
      (at ?v ?to)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-road_0126-tw_00450
    :parameters (?v - vehicle ?r - road_type_road_0126 ?from - loc_type_loc_0078 ?to - loc_type_loc_0030 ?w - window_type_tw_00450)
    :precondition (and
      (at ?v ?from)
      (connects ?r ?from ?to)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
      (not (location-blocked ?from))
      (not (location-blocked ?to))
    )
    :effect (and
      (not (at ?v ?from))
      (at ?v ?to)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-static-road_0127
    :parameters (?v - vehicle ?r - road_type_road_0127 ?from - loc_type_loc_0078 ?to - loc_type_loc_0048)
    :precondition (and
      (at ?v ?from)
      (connects ?r ?from ?to)
      (road-open ?r)
      (static-road ?r)
      (not (road-blocked ?r))
      (not (location-blocked ?from))
      (not (location-blocked ?to))
    )
    :effect (and
      (not (at ?v ?from))
      (at ?v ?to)
      (increase (travel-time ?v) (travel-duration ?r))
      (increase (sim-time) (travel-duration ?r))
    )
  )

  (:action traverse-road-dynamic-road_0128-tw_00000
    :parameters (?v - vehicle ?r - road_type_road_0128 ?from - loc_type_loc_0079 ?to - loc_type_loc_0031 ?w - window_type_tw_00000)
    :precondition (and
      (at ?v ?from)
      (connects ?r ?from ?to)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
      (not (location-blocked ?from))
      (not (location-blocked ?to))
    )
    :effect (and
      (not (at ?v ?from))
      (at ?v ?to)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-road_0128-tw_00030
    :parameters (?v - vehicle ?r - road_type_road_0128 ?from - loc_type_loc_0079 ?to - loc_type_loc_0031 ?w - window_type_tw_00030)
    :precondition (and
      (at ?v ?from)
      (connects ?r ?from ?to)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
      (not (location-blocked ?from))
      (not (location-blocked ?to))
    )
    :effect (and
      (not (at ?v ?from))
      (at ?v ?to)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-road_0128-tw_00060
    :parameters (?v - vehicle ?r - road_type_road_0128 ?from - loc_type_loc_0079 ?to - loc_type_loc_0031 ?w - window_type_tw_00060)
    :precondition (and
      (at ?v ?from)
      (connects ?r ?from ?to)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
      (not (location-blocked ?from))
      (not (location-blocked ?to))
    )
    :effect (and
      (not (at ?v ?from))
      (at ?v ?to)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-road_0128-tw_00090
    :parameters (?v - vehicle ?r - road_type_road_0128 ?from - loc_type_loc_0079 ?to - loc_type_loc_0031 ?w - window_type_tw_00090)
    :precondition (and
      (at ?v ?from)
      (connects ?r ?from ?to)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
      (not (location-blocked ?from))
      (not (location-blocked ?to))
    )
    :effect (and
      (not (at ?v ?from))
      (at ?v ?to)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-road_0128-tw_00120
    :parameters (?v - vehicle ?r - road_type_road_0128 ?from - loc_type_loc_0079 ?to - loc_type_loc_0031 ?w - window_type_tw_00120)
    :precondition (and
      (at ?v ?from)
      (connects ?r ?from ?to)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
      (not (location-blocked ?from))
      (not (location-blocked ?to))
    )
    :effect (and
      (not (at ?v ?from))
      (at ?v ?to)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-road_0128-tw_00150
    :parameters (?v - vehicle ?r - road_type_road_0128 ?from - loc_type_loc_0079 ?to - loc_type_loc_0031 ?w - window_type_tw_00150)
    :precondition (and
      (at ?v ?from)
      (connects ?r ?from ?to)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
      (not (location-blocked ?from))
      (not (location-blocked ?to))
    )
    :effect (and
      (not (at ?v ?from))
      (at ?v ?to)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-road_0128-tw_00180
    :parameters (?v - vehicle ?r - road_type_road_0128 ?from - loc_type_loc_0079 ?to - loc_type_loc_0031 ?w - window_type_tw_00180)
    :precondition (and
      (at ?v ?from)
      (connects ?r ?from ?to)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
      (not (location-blocked ?from))
      (not (location-blocked ?to))
    )
    :effect (and
      (not (at ?v ?from))
      (at ?v ?to)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-road_0128-tw_00210
    :parameters (?v - vehicle ?r - road_type_road_0128 ?from - loc_type_loc_0079 ?to - loc_type_loc_0031 ?w - window_type_tw_00210)
    :precondition (and
      (at ?v ?from)
      (connects ?r ?from ?to)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
      (not (location-blocked ?from))
      (not (location-blocked ?to))
    )
    :effect (and
      (not (at ?v ?from))
      (at ?v ?to)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-road_0128-tw_00240
    :parameters (?v - vehicle ?r - road_type_road_0128 ?from - loc_type_loc_0079 ?to - loc_type_loc_0031 ?w - window_type_tw_00240)
    :precondition (and
      (at ?v ?from)
      (connects ?r ?from ?to)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
      (not (location-blocked ?from))
      (not (location-blocked ?to))
    )
    :effect (and
      (not (at ?v ?from))
      (at ?v ?to)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-road_0128-tw_00270
    :parameters (?v - vehicle ?r - road_type_road_0128 ?from - loc_type_loc_0079 ?to - loc_type_loc_0031 ?w - window_type_tw_00270)
    :precondition (and
      (at ?v ?from)
      (connects ?r ?from ?to)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
      (not (location-blocked ?from))
      (not (location-blocked ?to))
    )
    :effect (and
      (not (at ?v ?from))
      (at ?v ?to)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-road_0128-tw_00300
    :parameters (?v - vehicle ?r - road_type_road_0128 ?from - loc_type_loc_0079 ?to - loc_type_loc_0031 ?w - window_type_tw_00300)
    :precondition (and
      (at ?v ?from)
      (connects ?r ?from ?to)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
      (not (location-blocked ?from))
      (not (location-blocked ?to))
    )
    :effect (and
      (not (at ?v ?from))
      (at ?v ?to)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-road_0128-tw_00330
    :parameters (?v - vehicle ?r - road_type_road_0128 ?from - loc_type_loc_0079 ?to - loc_type_loc_0031 ?w - window_type_tw_00330)
    :precondition (and
      (at ?v ?from)
      (connects ?r ?from ?to)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
      (not (location-blocked ?from))
      (not (location-blocked ?to))
    )
    :effect (and
      (not (at ?v ?from))
      (at ?v ?to)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-road_0128-tw_00360
    :parameters (?v - vehicle ?r - road_type_road_0128 ?from - loc_type_loc_0079 ?to - loc_type_loc_0031 ?w - window_type_tw_00360)
    :precondition (and
      (at ?v ?from)
      (connects ?r ?from ?to)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
      (not (location-blocked ?from))
      (not (location-blocked ?to))
    )
    :effect (and
      (not (at ?v ?from))
      (at ?v ?to)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-road_0128-tw_00390
    :parameters (?v - vehicle ?r - road_type_road_0128 ?from - loc_type_loc_0079 ?to - loc_type_loc_0031 ?w - window_type_tw_00390)
    :precondition (and
      (at ?v ?from)
      (connects ?r ?from ?to)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
      (not (location-blocked ?from))
      (not (location-blocked ?to))
    )
    :effect (and
      (not (at ?v ?from))
      (at ?v ?to)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-road_0128-tw_00420
    :parameters (?v - vehicle ?r - road_type_road_0128 ?from - loc_type_loc_0079 ?to - loc_type_loc_0031 ?w - window_type_tw_00420)
    :precondition (and
      (at ?v ?from)
      (connects ?r ?from ?to)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
      (not (location-blocked ?from))
      (not (location-blocked ?to))
    )
    :effect (and
      (not (at ?v ?from))
      (at ?v ?to)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-road_0128-tw_00450
    :parameters (?v - vehicle ?r - road_type_road_0128 ?from - loc_type_loc_0079 ?to - loc_type_loc_0031 ?w - window_type_tw_00450)
    :precondition (and
      (at ?v ?from)
      (connects ?r ?from ?to)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
      (not (location-blocked ?from))
      (not (location-blocked ?to))
    )
    :effect (and
      (not (at ?v ?from))
      (at ?v ?to)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-road_0129-tw_00000
    :parameters (?v - vehicle ?r - road_type_road_0129 ?from - loc_type_loc_0080 ?to - loc_type_loc_0021 ?w - window_type_tw_00000)
    :precondition (and
      (at ?v ?from)
      (connects ?r ?from ?to)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
      (not (location-blocked ?from))
      (not (location-blocked ?to))
    )
    :effect (and
      (not (at ?v ?from))
      (at ?v ?to)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-road_0129-tw_00030
    :parameters (?v - vehicle ?r - road_type_road_0129 ?from - loc_type_loc_0080 ?to - loc_type_loc_0021 ?w - window_type_tw_00030)
    :precondition (and
      (at ?v ?from)
      (connects ?r ?from ?to)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
      (not (location-blocked ?from))
      (not (location-blocked ?to))
    )
    :effect (and
      (not (at ?v ?from))
      (at ?v ?to)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-road_0129-tw_00060
    :parameters (?v - vehicle ?r - road_type_road_0129 ?from - loc_type_loc_0080 ?to - loc_type_loc_0021 ?w - window_type_tw_00060)
    :precondition (and
      (at ?v ?from)
      (connects ?r ?from ?to)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
      (not (location-blocked ?from))
      (not (location-blocked ?to))
    )
    :effect (and
      (not (at ?v ?from))
      (at ?v ?to)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-road_0129-tw_00090
    :parameters (?v - vehicle ?r - road_type_road_0129 ?from - loc_type_loc_0080 ?to - loc_type_loc_0021 ?w - window_type_tw_00090)
    :precondition (and
      (at ?v ?from)
      (connects ?r ?from ?to)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
      (not (location-blocked ?from))
      (not (location-blocked ?to))
    )
    :effect (and
      (not (at ?v ?from))
      (at ?v ?to)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-road_0129-tw_00120
    :parameters (?v - vehicle ?r - road_type_road_0129 ?from - loc_type_loc_0080 ?to - loc_type_loc_0021 ?w - window_type_tw_00120)
    :precondition (and
      (at ?v ?from)
      (connects ?r ?from ?to)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
      (not (location-blocked ?from))
      (not (location-blocked ?to))
    )
    :effect (and
      (not (at ?v ?from))
      (at ?v ?to)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-road_0129-tw_00150
    :parameters (?v - vehicle ?r - road_type_road_0129 ?from - loc_type_loc_0080 ?to - loc_type_loc_0021 ?w - window_type_tw_00150)
    :precondition (and
      (at ?v ?from)
      (connects ?r ?from ?to)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
      (not (location-blocked ?from))
      (not (location-blocked ?to))
    )
    :effect (and
      (not (at ?v ?from))
      (at ?v ?to)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-road_0129-tw_00180
    :parameters (?v - vehicle ?r - road_type_road_0129 ?from - loc_type_loc_0080 ?to - loc_type_loc_0021 ?w - window_type_tw_00180)
    :precondition (and
      (at ?v ?from)
      (connects ?r ?from ?to)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
      (not (location-blocked ?from))
      (not (location-blocked ?to))
    )
    :effect (and
      (not (at ?v ?from))
      (at ?v ?to)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-road_0129-tw_00210
    :parameters (?v - vehicle ?r - road_type_road_0129 ?from - loc_type_loc_0080 ?to - loc_type_loc_0021 ?w - window_type_tw_00210)
    :precondition (and
      (at ?v ?from)
      (connects ?r ?from ?to)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
      (not (location-blocked ?from))
      (not (location-blocked ?to))
    )
    :effect (and
      (not (at ?v ?from))
      (at ?v ?to)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-road_0129-tw_00240
    :parameters (?v - vehicle ?r - road_type_road_0129 ?from - loc_type_loc_0080 ?to - loc_type_loc_0021 ?w - window_type_tw_00240)
    :precondition (and
      (at ?v ?from)
      (connects ?r ?from ?to)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
      (not (location-blocked ?from))
      (not (location-blocked ?to))
    )
    :effect (and
      (not (at ?v ?from))
      (at ?v ?to)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-road_0129-tw_00270
    :parameters (?v - vehicle ?r - road_type_road_0129 ?from - loc_type_loc_0080 ?to - loc_type_loc_0021 ?w - window_type_tw_00270)
    :precondition (and
      (at ?v ?from)
      (connects ?r ?from ?to)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
      (not (location-blocked ?from))
      (not (location-blocked ?to))
    )
    :effect (and
      (not (at ?v ?from))
      (at ?v ?to)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-road_0129-tw_00300
    :parameters (?v - vehicle ?r - road_type_road_0129 ?from - loc_type_loc_0080 ?to - loc_type_loc_0021 ?w - window_type_tw_00300)
    :precondition (and
      (at ?v ?from)
      (connects ?r ?from ?to)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
      (not (location-blocked ?from))
      (not (location-blocked ?to))
    )
    :effect (and
      (not (at ?v ?from))
      (at ?v ?to)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-road_0129-tw_00330
    :parameters (?v - vehicle ?r - road_type_road_0129 ?from - loc_type_loc_0080 ?to - loc_type_loc_0021 ?w - window_type_tw_00330)
    :precondition (and
      (at ?v ?from)
      (connects ?r ?from ?to)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
      (not (location-blocked ?from))
      (not (location-blocked ?to))
    )
    :effect (and
      (not (at ?v ?from))
      (at ?v ?to)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-road_0129-tw_00360
    :parameters (?v - vehicle ?r - road_type_road_0129 ?from - loc_type_loc_0080 ?to - loc_type_loc_0021 ?w - window_type_tw_00360)
    :precondition (and
      (at ?v ?from)
      (connects ?r ?from ?to)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
      (not (location-blocked ?from))
      (not (location-blocked ?to))
    )
    :effect (and
      (not (at ?v ?from))
      (at ?v ?to)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-road_0129-tw_00390
    :parameters (?v - vehicle ?r - road_type_road_0129 ?from - loc_type_loc_0080 ?to - loc_type_loc_0021 ?w - window_type_tw_00390)
    :precondition (and
      (at ?v ?from)
      (connects ?r ?from ?to)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
      (not (location-blocked ?from))
      (not (location-blocked ?to))
    )
    :effect (and
      (not (at ?v ?from))
      (at ?v ?to)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-road_0129-tw_00420
    :parameters (?v - vehicle ?r - road_type_road_0129 ?from - loc_type_loc_0080 ?to - loc_type_loc_0021 ?w - window_type_tw_00420)
    :precondition (and
      (at ?v ?from)
      (connects ?r ?from ?to)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
      (not (location-blocked ?from))
      (not (location-blocked ?to))
    )
    :effect (and
      (not (at ?v ?from))
      (at ?v ?to)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-road_0129-tw_00450
    :parameters (?v - vehicle ?r - road_type_road_0129 ?from - loc_type_loc_0080 ?to - loc_type_loc_0021 ?w - window_type_tw_00450)
    :precondition (and
      (at ?v ?from)
      (connects ?r ?from ?to)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
      (not (location-blocked ?from))
      (not (location-blocked ?to))
    )
    :effect (and
      (not (at ?v ?from))
      (at ?v ?to)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-road_0130-tw_00000
    :parameters (?v - vehicle ?r - road_type_road_0130 ?from - loc_type_loc_0080 ?to - loc_type_loc_0009 ?w - window_type_tw_00000)
    :precondition (and
      (at ?v ?from)
      (connects ?r ?from ?to)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
      (not (location-blocked ?from))
      (not (location-blocked ?to))
    )
    :effect (and
      (not (at ?v ?from))
      (at ?v ?to)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-road_0130-tw_00030
    :parameters (?v - vehicle ?r - road_type_road_0130 ?from - loc_type_loc_0080 ?to - loc_type_loc_0009 ?w - window_type_tw_00030)
    :precondition (and
      (at ?v ?from)
      (connects ?r ?from ?to)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
      (not (location-blocked ?from))
      (not (location-blocked ?to))
    )
    :effect (and
      (not (at ?v ?from))
      (at ?v ?to)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-road_0130-tw_00060
    :parameters (?v - vehicle ?r - road_type_road_0130 ?from - loc_type_loc_0080 ?to - loc_type_loc_0009 ?w - window_type_tw_00060)
    :precondition (and
      (at ?v ?from)
      (connects ?r ?from ?to)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
      (not (location-blocked ?from))
      (not (location-blocked ?to))
    )
    :effect (and
      (not (at ?v ?from))
      (at ?v ?to)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-road_0130-tw_00090
    :parameters (?v - vehicle ?r - road_type_road_0130 ?from - loc_type_loc_0080 ?to - loc_type_loc_0009 ?w - window_type_tw_00090)
    :precondition (and
      (at ?v ?from)
      (connects ?r ?from ?to)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
      (not (location-blocked ?from))
      (not (location-blocked ?to))
    )
    :effect (and
      (not (at ?v ?from))
      (at ?v ?to)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-road_0130-tw_00120
    :parameters (?v - vehicle ?r - road_type_road_0130 ?from - loc_type_loc_0080 ?to - loc_type_loc_0009 ?w - window_type_tw_00120)
    :precondition (and
      (at ?v ?from)
      (connects ?r ?from ?to)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
      (not (location-blocked ?from))
      (not (location-blocked ?to))
    )
    :effect (and
      (not (at ?v ?from))
      (at ?v ?to)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-road_0130-tw_00150
    :parameters (?v - vehicle ?r - road_type_road_0130 ?from - loc_type_loc_0080 ?to - loc_type_loc_0009 ?w - window_type_tw_00150)
    :precondition (and
      (at ?v ?from)
      (connects ?r ?from ?to)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
      (not (location-blocked ?from))
      (not (location-blocked ?to))
    )
    :effect (and
      (not (at ?v ?from))
      (at ?v ?to)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-road_0130-tw_00180
    :parameters (?v - vehicle ?r - road_type_road_0130 ?from - loc_type_loc_0080 ?to - loc_type_loc_0009 ?w - window_type_tw_00180)
    :precondition (and
      (at ?v ?from)
      (connects ?r ?from ?to)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
      (not (location-blocked ?from))
      (not (location-blocked ?to))
    )
    :effect (and
      (not (at ?v ?from))
      (at ?v ?to)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-road_0130-tw_00210
    :parameters (?v - vehicle ?r - road_type_road_0130 ?from - loc_type_loc_0080 ?to - loc_type_loc_0009 ?w - window_type_tw_00210)
    :precondition (and
      (at ?v ?from)
      (connects ?r ?from ?to)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
      (not (location-blocked ?from))
      (not (location-blocked ?to))
    )
    :effect (and
      (not (at ?v ?from))
      (at ?v ?to)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-road_0130-tw_00240
    :parameters (?v - vehicle ?r - road_type_road_0130 ?from - loc_type_loc_0080 ?to - loc_type_loc_0009 ?w - window_type_tw_00240)
    :precondition (and
      (at ?v ?from)
      (connects ?r ?from ?to)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
      (not (location-blocked ?from))
      (not (location-blocked ?to))
    )
    :effect (and
      (not (at ?v ?from))
      (at ?v ?to)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-road_0130-tw_00270
    :parameters (?v - vehicle ?r - road_type_road_0130 ?from - loc_type_loc_0080 ?to - loc_type_loc_0009 ?w - window_type_tw_00270)
    :precondition (and
      (at ?v ?from)
      (connects ?r ?from ?to)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
      (not (location-blocked ?from))
      (not (location-blocked ?to))
    )
    :effect (and
      (not (at ?v ?from))
      (at ?v ?to)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-road_0130-tw_00300
    :parameters (?v - vehicle ?r - road_type_road_0130 ?from - loc_type_loc_0080 ?to - loc_type_loc_0009 ?w - window_type_tw_00300)
    :precondition (and
      (at ?v ?from)
      (connects ?r ?from ?to)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
      (not (location-blocked ?from))
      (not (location-blocked ?to))
    )
    :effect (and
      (not (at ?v ?from))
      (at ?v ?to)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-road_0130-tw_00330
    :parameters (?v - vehicle ?r - road_type_road_0130 ?from - loc_type_loc_0080 ?to - loc_type_loc_0009 ?w - window_type_tw_00330)
    :precondition (and
      (at ?v ?from)
      (connects ?r ?from ?to)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
      (not (location-blocked ?from))
      (not (location-blocked ?to))
    )
    :effect (and
      (not (at ?v ?from))
      (at ?v ?to)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-road_0130-tw_00360
    :parameters (?v - vehicle ?r - road_type_road_0130 ?from - loc_type_loc_0080 ?to - loc_type_loc_0009 ?w - window_type_tw_00360)
    :precondition (and
      (at ?v ?from)
      (connects ?r ?from ?to)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
      (not (location-blocked ?from))
      (not (location-blocked ?to))
    )
    :effect (and
      (not (at ?v ?from))
      (at ?v ?to)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-road_0130-tw_00390
    :parameters (?v - vehicle ?r - road_type_road_0130 ?from - loc_type_loc_0080 ?to - loc_type_loc_0009 ?w - window_type_tw_00390)
    :precondition (and
      (at ?v ?from)
      (connects ?r ?from ?to)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
      (not (location-blocked ?from))
      (not (location-blocked ?to))
    )
    :effect (and
      (not (at ?v ?from))
      (at ?v ?to)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-road_0130-tw_00420
    :parameters (?v - vehicle ?r - road_type_road_0130 ?from - loc_type_loc_0080 ?to - loc_type_loc_0009 ?w - window_type_tw_00420)
    :precondition (and
      (at ?v ?from)
      (connects ?r ?from ?to)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
      (not (location-blocked ?from))
      (not (location-blocked ?to))
    )
    :effect (and
      (not (at ?v ?from))
      (at ?v ?to)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-road_0130-tw_00450
    :parameters (?v - vehicle ?r - road_type_road_0130 ?from - loc_type_loc_0080 ?to - loc_type_loc_0009 ?w - window_type_tw_00450)
    :precondition (and
      (at ?v ?from)
      (connects ?r ?from ?to)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
      (not (location-blocked ?from))
      (not (location-blocked ?to))
    )
    :effect (and
      (not (at ?v ?from))
      (at ?v ?to)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-road_0131-tw_00000
    :parameters (?v - vehicle ?r - road_type_road_0131 ?from - loc_type_loc_0081 ?to - loc_type_loc_0087 ?w - window_type_tw_00000)
    :precondition (and
      (at ?v ?from)
      (connects ?r ?from ?to)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
      (not (location-blocked ?from))
      (not (location-blocked ?to))
    )
    :effect (and
      (not (at ?v ?from))
      (at ?v ?to)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-road_0131-tw_00030
    :parameters (?v - vehicle ?r - road_type_road_0131 ?from - loc_type_loc_0081 ?to - loc_type_loc_0087 ?w - window_type_tw_00030)
    :precondition (and
      (at ?v ?from)
      (connects ?r ?from ?to)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
      (not (location-blocked ?from))
      (not (location-blocked ?to))
    )
    :effect (and
      (not (at ?v ?from))
      (at ?v ?to)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-road_0131-tw_00060
    :parameters (?v - vehicle ?r - road_type_road_0131 ?from - loc_type_loc_0081 ?to - loc_type_loc_0087 ?w - window_type_tw_00060)
    :precondition (and
      (at ?v ?from)
      (connects ?r ?from ?to)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
      (not (location-blocked ?from))
      (not (location-blocked ?to))
    )
    :effect (and
      (not (at ?v ?from))
      (at ?v ?to)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-road_0131-tw_00090
    :parameters (?v - vehicle ?r - road_type_road_0131 ?from - loc_type_loc_0081 ?to - loc_type_loc_0087 ?w - window_type_tw_00090)
    :precondition (and
      (at ?v ?from)
      (connects ?r ?from ?to)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
      (not (location-blocked ?from))
      (not (location-blocked ?to))
    )
    :effect (and
      (not (at ?v ?from))
      (at ?v ?to)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-road_0131-tw_00120
    :parameters (?v - vehicle ?r - road_type_road_0131 ?from - loc_type_loc_0081 ?to - loc_type_loc_0087 ?w - window_type_tw_00120)
    :precondition (and
      (at ?v ?from)
      (connects ?r ?from ?to)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
      (not (location-blocked ?from))
      (not (location-blocked ?to))
    )
    :effect (and
      (not (at ?v ?from))
      (at ?v ?to)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-road_0131-tw_00150
    :parameters (?v - vehicle ?r - road_type_road_0131 ?from - loc_type_loc_0081 ?to - loc_type_loc_0087 ?w - window_type_tw_00150)
    :precondition (and
      (at ?v ?from)
      (connects ?r ?from ?to)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
      (not (location-blocked ?from))
      (not (location-blocked ?to))
    )
    :effect (and
      (not (at ?v ?from))
      (at ?v ?to)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-road_0131-tw_00180
    :parameters (?v - vehicle ?r - road_type_road_0131 ?from - loc_type_loc_0081 ?to - loc_type_loc_0087 ?w - window_type_tw_00180)
    :precondition (and
      (at ?v ?from)
      (connects ?r ?from ?to)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
      (not (location-blocked ?from))
      (not (location-blocked ?to))
    )
    :effect (and
      (not (at ?v ?from))
      (at ?v ?to)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-road_0131-tw_00210
    :parameters (?v - vehicle ?r - road_type_road_0131 ?from - loc_type_loc_0081 ?to - loc_type_loc_0087 ?w - window_type_tw_00210)
    :precondition (and
      (at ?v ?from)
      (connects ?r ?from ?to)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
      (not (location-blocked ?from))
      (not (location-blocked ?to))
    )
    :effect (and
      (not (at ?v ?from))
      (at ?v ?to)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-road_0131-tw_00240
    :parameters (?v - vehicle ?r - road_type_road_0131 ?from - loc_type_loc_0081 ?to - loc_type_loc_0087 ?w - window_type_tw_00240)
    :precondition (and
      (at ?v ?from)
      (connects ?r ?from ?to)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
      (not (location-blocked ?from))
      (not (location-blocked ?to))
    )
    :effect (and
      (not (at ?v ?from))
      (at ?v ?to)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-road_0131-tw_00270
    :parameters (?v - vehicle ?r - road_type_road_0131 ?from - loc_type_loc_0081 ?to - loc_type_loc_0087 ?w - window_type_tw_00270)
    :precondition (and
      (at ?v ?from)
      (connects ?r ?from ?to)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
      (not (location-blocked ?from))
      (not (location-blocked ?to))
    )
    :effect (and
      (not (at ?v ?from))
      (at ?v ?to)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-road_0131-tw_00300
    :parameters (?v - vehicle ?r - road_type_road_0131 ?from - loc_type_loc_0081 ?to - loc_type_loc_0087 ?w - window_type_tw_00300)
    :precondition (and
      (at ?v ?from)
      (connects ?r ?from ?to)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
      (not (location-blocked ?from))
      (not (location-blocked ?to))
    )
    :effect (and
      (not (at ?v ?from))
      (at ?v ?to)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-road_0131-tw_00330
    :parameters (?v - vehicle ?r - road_type_road_0131 ?from - loc_type_loc_0081 ?to - loc_type_loc_0087 ?w - window_type_tw_00330)
    :precondition (and
      (at ?v ?from)
      (connects ?r ?from ?to)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
      (not (location-blocked ?from))
      (not (location-blocked ?to))
    )
    :effect (and
      (not (at ?v ?from))
      (at ?v ?to)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-road_0131-tw_00360
    :parameters (?v - vehicle ?r - road_type_road_0131 ?from - loc_type_loc_0081 ?to - loc_type_loc_0087 ?w - window_type_tw_00360)
    :precondition (and
      (at ?v ?from)
      (connects ?r ?from ?to)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
      (not (location-blocked ?from))
      (not (location-blocked ?to))
    )
    :effect (and
      (not (at ?v ?from))
      (at ?v ?to)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-road_0131-tw_00390
    :parameters (?v - vehicle ?r - road_type_road_0131 ?from - loc_type_loc_0081 ?to - loc_type_loc_0087 ?w - window_type_tw_00390)
    :precondition (and
      (at ?v ?from)
      (connects ?r ?from ?to)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
      (not (location-blocked ?from))
      (not (location-blocked ?to))
    )
    :effect (and
      (not (at ?v ?from))
      (at ?v ?to)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-road_0131-tw_00420
    :parameters (?v - vehicle ?r - road_type_road_0131 ?from - loc_type_loc_0081 ?to - loc_type_loc_0087 ?w - window_type_tw_00420)
    :precondition (and
      (at ?v ?from)
      (connects ?r ?from ?to)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
      (not (location-blocked ?from))
      (not (location-blocked ?to))
    )
    :effect (and
      (not (at ?v ?from))
      (at ?v ?to)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-road_0131-tw_00450
    :parameters (?v - vehicle ?r - road_type_road_0131 ?from - loc_type_loc_0081 ?to - loc_type_loc_0087 ?w - window_type_tw_00450)
    :precondition (and
      (at ?v ?from)
      (connects ?r ?from ?to)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
      (not (location-blocked ?from))
      (not (location-blocked ?to))
    )
    :effect (and
      (not (at ?v ?from))
      (at ?v ?to)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-road_0133-tw_00000
    :parameters (?v - vehicle ?r - road_type_road_0133 ?from - loc_type_loc_0081 ?to - loc_type_loc_0048 ?w - window_type_tw_00000)
    :precondition (and
      (at ?v ?from)
      (connects ?r ?from ?to)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
      (not (location-blocked ?from))
      (not (location-blocked ?to))
    )
    :effect (and
      (not (at ?v ?from))
      (at ?v ?to)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-road_0133-tw_00030
    :parameters (?v - vehicle ?r - road_type_road_0133 ?from - loc_type_loc_0081 ?to - loc_type_loc_0048 ?w - window_type_tw_00030)
    :precondition (and
      (at ?v ?from)
      (connects ?r ?from ?to)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
      (not (location-blocked ?from))
      (not (location-blocked ?to))
    )
    :effect (and
      (not (at ?v ?from))
      (at ?v ?to)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-road_0133-tw_00060
    :parameters (?v - vehicle ?r - road_type_road_0133 ?from - loc_type_loc_0081 ?to - loc_type_loc_0048 ?w - window_type_tw_00060)
    :precondition (and
      (at ?v ?from)
      (connects ?r ?from ?to)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
      (not (location-blocked ?from))
      (not (location-blocked ?to))
    )
    :effect (and
      (not (at ?v ?from))
      (at ?v ?to)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-road_0133-tw_00090
    :parameters (?v - vehicle ?r - road_type_road_0133 ?from - loc_type_loc_0081 ?to - loc_type_loc_0048 ?w - window_type_tw_00090)
    :precondition (and
      (at ?v ?from)
      (connects ?r ?from ?to)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
      (not (location-blocked ?from))
      (not (location-blocked ?to))
    )
    :effect (and
      (not (at ?v ?from))
      (at ?v ?to)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-road_0133-tw_00120
    :parameters (?v - vehicle ?r - road_type_road_0133 ?from - loc_type_loc_0081 ?to - loc_type_loc_0048 ?w - window_type_tw_00120)
    :precondition (and
      (at ?v ?from)
      (connects ?r ?from ?to)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
      (not (location-blocked ?from))
      (not (location-blocked ?to))
    )
    :effect (and
      (not (at ?v ?from))
      (at ?v ?to)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-road_0133-tw_00150
    :parameters (?v - vehicle ?r - road_type_road_0133 ?from - loc_type_loc_0081 ?to - loc_type_loc_0048 ?w - window_type_tw_00150)
    :precondition (and
      (at ?v ?from)
      (connects ?r ?from ?to)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
      (not (location-blocked ?from))
      (not (location-blocked ?to))
    )
    :effect (and
      (not (at ?v ?from))
      (at ?v ?to)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-road_0133-tw_00180
    :parameters (?v - vehicle ?r - road_type_road_0133 ?from - loc_type_loc_0081 ?to - loc_type_loc_0048 ?w - window_type_tw_00180)
    :precondition (and
      (at ?v ?from)
      (connects ?r ?from ?to)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
      (not (location-blocked ?from))
      (not (location-blocked ?to))
    )
    :effect (and
      (not (at ?v ?from))
      (at ?v ?to)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-road_0133-tw_00210
    :parameters (?v - vehicle ?r - road_type_road_0133 ?from - loc_type_loc_0081 ?to - loc_type_loc_0048 ?w - window_type_tw_00210)
    :precondition (and
      (at ?v ?from)
      (connects ?r ?from ?to)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
      (not (location-blocked ?from))
      (not (location-blocked ?to))
    )
    :effect (and
      (not (at ?v ?from))
      (at ?v ?to)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-road_0133-tw_00240
    :parameters (?v - vehicle ?r - road_type_road_0133 ?from - loc_type_loc_0081 ?to - loc_type_loc_0048 ?w - window_type_tw_00240)
    :precondition (and
      (at ?v ?from)
      (connects ?r ?from ?to)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
      (not (location-blocked ?from))
      (not (location-blocked ?to))
    )
    :effect (and
      (not (at ?v ?from))
      (at ?v ?to)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-road_0133-tw_00270
    :parameters (?v - vehicle ?r - road_type_road_0133 ?from - loc_type_loc_0081 ?to - loc_type_loc_0048 ?w - window_type_tw_00270)
    :precondition (and
      (at ?v ?from)
      (connects ?r ?from ?to)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
      (not (location-blocked ?from))
      (not (location-blocked ?to))
    )
    :effect (and
      (not (at ?v ?from))
      (at ?v ?to)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-road_0133-tw_00300
    :parameters (?v - vehicle ?r - road_type_road_0133 ?from - loc_type_loc_0081 ?to - loc_type_loc_0048 ?w - window_type_tw_00300)
    :precondition (and
      (at ?v ?from)
      (connects ?r ?from ?to)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
      (not (location-blocked ?from))
      (not (location-blocked ?to))
    )
    :effect (and
      (not (at ?v ?from))
      (at ?v ?to)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-road_0133-tw_00330
    :parameters (?v - vehicle ?r - road_type_road_0133 ?from - loc_type_loc_0081 ?to - loc_type_loc_0048 ?w - window_type_tw_00330)
    :precondition (and
      (at ?v ?from)
      (connects ?r ?from ?to)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
      (not (location-blocked ?from))
      (not (location-blocked ?to))
    )
    :effect (and
      (not (at ?v ?from))
      (at ?v ?to)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-road_0133-tw_00360
    :parameters (?v - vehicle ?r - road_type_road_0133 ?from - loc_type_loc_0081 ?to - loc_type_loc_0048 ?w - window_type_tw_00360)
    :precondition (and
      (at ?v ?from)
      (connects ?r ?from ?to)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
      (not (location-blocked ?from))
      (not (location-blocked ?to))
    )
    :effect (and
      (not (at ?v ?from))
      (at ?v ?to)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-road_0133-tw_00390
    :parameters (?v - vehicle ?r - road_type_road_0133 ?from - loc_type_loc_0081 ?to - loc_type_loc_0048 ?w - window_type_tw_00390)
    :precondition (and
      (at ?v ?from)
      (connects ?r ?from ?to)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
      (not (location-blocked ?from))
      (not (location-blocked ?to))
    )
    :effect (and
      (not (at ?v ?from))
      (at ?v ?to)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-road_0133-tw_00420
    :parameters (?v - vehicle ?r - road_type_road_0133 ?from - loc_type_loc_0081 ?to - loc_type_loc_0048 ?w - window_type_tw_00420)
    :precondition (and
      (at ?v ?from)
      (connects ?r ?from ?to)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
      (not (location-blocked ?from))
      (not (location-blocked ?to))
    )
    :effect (and
      (not (at ?v ?from))
      (at ?v ?to)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-road_0133-tw_00450
    :parameters (?v - vehicle ?r - road_type_road_0133 ?from - loc_type_loc_0081 ?to - loc_type_loc_0048 ?w - window_type_tw_00450)
    :precondition (and
      (at ?v ?from)
      (connects ?r ?from ?to)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
      (not (location-blocked ?from))
      (not (location-blocked ?to))
    )
    :effect (and
      (not (at ?v ?from))
      (at ?v ?to)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-road_0134-tw_00000
    :parameters (?v - vehicle ?r - road_type_road_0134 ?from - loc_type_loc_0082 ?to - loc_type_loc_0044 ?w - window_type_tw_00000)
    :precondition (and
      (at ?v ?from)
      (connects ?r ?from ?to)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
      (not (location-blocked ?from))
      (not (location-blocked ?to))
    )
    :effect (and
      (not (at ?v ?from))
      (at ?v ?to)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-road_0134-tw_00030
    :parameters (?v - vehicle ?r - road_type_road_0134 ?from - loc_type_loc_0082 ?to - loc_type_loc_0044 ?w - window_type_tw_00030)
    :precondition (and
      (at ?v ?from)
      (connects ?r ?from ?to)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
      (not (location-blocked ?from))
      (not (location-blocked ?to))
    )
    :effect (and
      (not (at ?v ?from))
      (at ?v ?to)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-road_0134-tw_00060
    :parameters (?v - vehicle ?r - road_type_road_0134 ?from - loc_type_loc_0082 ?to - loc_type_loc_0044 ?w - window_type_tw_00060)
    :precondition (and
      (at ?v ?from)
      (connects ?r ?from ?to)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
      (not (location-blocked ?from))
      (not (location-blocked ?to))
    )
    :effect (and
      (not (at ?v ?from))
      (at ?v ?to)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-road_0134-tw_00090
    :parameters (?v - vehicle ?r - road_type_road_0134 ?from - loc_type_loc_0082 ?to - loc_type_loc_0044 ?w - window_type_tw_00090)
    :precondition (and
      (at ?v ?from)
      (connects ?r ?from ?to)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
      (not (location-blocked ?from))
      (not (location-blocked ?to))
    )
    :effect (and
      (not (at ?v ?from))
      (at ?v ?to)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-road_0134-tw_00120
    :parameters (?v - vehicle ?r - road_type_road_0134 ?from - loc_type_loc_0082 ?to - loc_type_loc_0044 ?w - window_type_tw_00120)
    :precondition (and
      (at ?v ?from)
      (connects ?r ?from ?to)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
      (not (location-blocked ?from))
      (not (location-blocked ?to))
    )
    :effect (and
      (not (at ?v ?from))
      (at ?v ?to)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-road_0134-tw_00150
    :parameters (?v - vehicle ?r - road_type_road_0134 ?from - loc_type_loc_0082 ?to - loc_type_loc_0044 ?w - window_type_tw_00150)
    :precondition (and
      (at ?v ?from)
      (connects ?r ?from ?to)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
      (not (location-blocked ?from))
      (not (location-blocked ?to))
    )
    :effect (and
      (not (at ?v ?from))
      (at ?v ?to)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-road_0134-tw_00180
    :parameters (?v - vehicle ?r - road_type_road_0134 ?from - loc_type_loc_0082 ?to - loc_type_loc_0044 ?w - window_type_tw_00180)
    :precondition (and
      (at ?v ?from)
      (connects ?r ?from ?to)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
      (not (location-blocked ?from))
      (not (location-blocked ?to))
    )
    :effect (and
      (not (at ?v ?from))
      (at ?v ?to)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-road_0134-tw_00210
    :parameters (?v - vehicle ?r - road_type_road_0134 ?from - loc_type_loc_0082 ?to - loc_type_loc_0044 ?w - window_type_tw_00210)
    :precondition (and
      (at ?v ?from)
      (connects ?r ?from ?to)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
      (not (location-blocked ?from))
      (not (location-blocked ?to))
    )
    :effect (and
      (not (at ?v ?from))
      (at ?v ?to)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-road_0134-tw_00240
    :parameters (?v - vehicle ?r - road_type_road_0134 ?from - loc_type_loc_0082 ?to - loc_type_loc_0044 ?w - window_type_tw_00240)
    :precondition (and
      (at ?v ?from)
      (connects ?r ?from ?to)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
      (not (location-blocked ?from))
      (not (location-blocked ?to))
    )
    :effect (and
      (not (at ?v ?from))
      (at ?v ?to)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-road_0134-tw_00270
    :parameters (?v - vehicle ?r - road_type_road_0134 ?from - loc_type_loc_0082 ?to - loc_type_loc_0044 ?w - window_type_tw_00270)
    :precondition (and
      (at ?v ?from)
      (connects ?r ?from ?to)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
      (not (location-blocked ?from))
      (not (location-blocked ?to))
    )
    :effect (and
      (not (at ?v ?from))
      (at ?v ?to)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-road_0134-tw_00300
    :parameters (?v - vehicle ?r - road_type_road_0134 ?from - loc_type_loc_0082 ?to - loc_type_loc_0044 ?w - window_type_tw_00300)
    :precondition (and
      (at ?v ?from)
      (connects ?r ?from ?to)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
      (not (location-blocked ?from))
      (not (location-blocked ?to))
    )
    :effect (and
      (not (at ?v ?from))
      (at ?v ?to)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-road_0134-tw_00330
    :parameters (?v - vehicle ?r - road_type_road_0134 ?from - loc_type_loc_0082 ?to - loc_type_loc_0044 ?w - window_type_tw_00330)
    :precondition (and
      (at ?v ?from)
      (connects ?r ?from ?to)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
      (not (location-blocked ?from))
      (not (location-blocked ?to))
    )
    :effect (and
      (not (at ?v ?from))
      (at ?v ?to)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-road_0134-tw_00360
    :parameters (?v - vehicle ?r - road_type_road_0134 ?from - loc_type_loc_0082 ?to - loc_type_loc_0044 ?w - window_type_tw_00360)
    :precondition (and
      (at ?v ?from)
      (connects ?r ?from ?to)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
      (not (location-blocked ?from))
      (not (location-blocked ?to))
    )
    :effect (and
      (not (at ?v ?from))
      (at ?v ?to)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-road_0134-tw_00390
    :parameters (?v - vehicle ?r - road_type_road_0134 ?from - loc_type_loc_0082 ?to - loc_type_loc_0044 ?w - window_type_tw_00390)
    :precondition (and
      (at ?v ?from)
      (connects ?r ?from ?to)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
      (not (location-blocked ?from))
      (not (location-blocked ?to))
    )
    :effect (and
      (not (at ?v ?from))
      (at ?v ?to)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-road_0134-tw_00420
    :parameters (?v - vehicle ?r - road_type_road_0134 ?from - loc_type_loc_0082 ?to - loc_type_loc_0044 ?w - window_type_tw_00420)
    :precondition (and
      (at ?v ?from)
      (connects ?r ?from ?to)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
      (not (location-blocked ?from))
      (not (location-blocked ?to))
    )
    :effect (and
      (not (at ?v ?from))
      (at ?v ?to)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-road_0134-tw_00450
    :parameters (?v - vehicle ?r - road_type_road_0134 ?from - loc_type_loc_0082 ?to - loc_type_loc_0044 ?w - window_type_tw_00450)
    :precondition (and
      (at ?v ?from)
      (connects ?r ?from ?to)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
      (not (location-blocked ?from))
      (not (location-blocked ?to))
    )
    :effect (and
      (not (at ?v ?from))
      (at ?v ?to)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-road_0135-tw_00000
    :parameters (?v - vehicle ?r - road_type_road_0135 ?from - loc_type_loc_0083 ?to - loc_type_loc_0082 ?w - window_type_tw_00000)
    :precondition (and
      (at ?v ?from)
      (connects ?r ?from ?to)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
      (not (location-blocked ?from))
      (not (location-blocked ?to))
    )
    :effect (and
      (not (at ?v ?from))
      (at ?v ?to)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-road_0135-tw_00030
    :parameters (?v - vehicle ?r - road_type_road_0135 ?from - loc_type_loc_0083 ?to - loc_type_loc_0082 ?w - window_type_tw_00030)
    :precondition (and
      (at ?v ?from)
      (connects ?r ?from ?to)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
      (not (location-blocked ?from))
      (not (location-blocked ?to))
    )
    :effect (and
      (not (at ?v ?from))
      (at ?v ?to)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-road_0135-tw_00060
    :parameters (?v - vehicle ?r - road_type_road_0135 ?from - loc_type_loc_0083 ?to - loc_type_loc_0082 ?w - window_type_tw_00060)
    :precondition (and
      (at ?v ?from)
      (connects ?r ?from ?to)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
      (not (location-blocked ?from))
      (not (location-blocked ?to))
    )
    :effect (and
      (not (at ?v ?from))
      (at ?v ?to)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-road_0135-tw_00090
    :parameters (?v - vehicle ?r - road_type_road_0135 ?from - loc_type_loc_0083 ?to - loc_type_loc_0082 ?w - window_type_tw_00090)
    :precondition (and
      (at ?v ?from)
      (connects ?r ?from ?to)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
      (not (location-blocked ?from))
      (not (location-blocked ?to))
    )
    :effect (and
      (not (at ?v ?from))
      (at ?v ?to)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-road_0135-tw_00120
    :parameters (?v - vehicle ?r - road_type_road_0135 ?from - loc_type_loc_0083 ?to - loc_type_loc_0082 ?w - window_type_tw_00120)
    :precondition (and
      (at ?v ?from)
      (connects ?r ?from ?to)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
      (not (location-blocked ?from))
      (not (location-blocked ?to))
    )
    :effect (and
      (not (at ?v ?from))
      (at ?v ?to)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-road_0135-tw_00150
    :parameters (?v - vehicle ?r - road_type_road_0135 ?from - loc_type_loc_0083 ?to - loc_type_loc_0082 ?w - window_type_tw_00150)
    :precondition (and
      (at ?v ?from)
      (connects ?r ?from ?to)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
      (not (location-blocked ?from))
      (not (location-blocked ?to))
    )
    :effect (and
      (not (at ?v ?from))
      (at ?v ?to)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-road_0135-tw_00180
    :parameters (?v - vehicle ?r - road_type_road_0135 ?from - loc_type_loc_0083 ?to - loc_type_loc_0082 ?w - window_type_tw_00180)
    :precondition (and
      (at ?v ?from)
      (connects ?r ?from ?to)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
      (not (location-blocked ?from))
      (not (location-blocked ?to))
    )
    :effect (and
      (not (at ?v ?from))
      (at ?v ?to)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-road_0135-tw_00210
    :parameters (?v - vehicle ?r - road_type_road_0135 ?from - loc_type_loc_0083 ?to - loc_type_loc_0082 ?w - window_type_tw_00210)
    :precondition (and
      (at ?v ?from)
      (connects ?r ?from ?to)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
      (not (location-blocked ?from))
      (not (location-blocked ?to))
    )
    :effect (and
      (not (at ?v ?from))
      (at ?v ?to)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-road_0135-tw_00240
    :parameters (?v - vehicle ?r - road_type_road_0135 ?from - loc_type_loc_0083 ?to - loc_type_loc_0082 ?w - window_type_tw_00240)
    :precondition (and
      (at ?v ?from)
      (connects ?r ?from ?to)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
      (not (location-blocked ?from))
      (not (location-blocked ?to))
    )
    :effect (and
      (not (at ?v ?from))
      (at ?v ?to)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-road_0135-tw_00270
    :parameters (?v - vehicle ?r - road_type_road_0135 ?from - loc_type_loc_0083 ?to - loc_type_loc_0082 ?w - window_type_tw_00270)
    :precondition (and
      (at ?v ?from)
      (connects ?r ?from ?to)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
      (not (location-blocked ?from))
      (not (location-blocked ?to))
    )
    :effect (and
      (not (at ?v ?from))
      (at ?v ?to)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-road_0135-tw_00300
    :parameters (?v - vehicle ?r - road_type_road_0135 ?from - loc_type_loc_0083 ?to - loc_type_loc_0082 ?w - window_type_tw_00300)
    :precondition (and
      (at ?v ?from)
      (connects ?r ?from ?to)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
      (not (location-blocked ?from))
      (not (location-blocked ?to))
    )
    :effect (and
      (not (at ?v ?from))
      (at ?v ?to)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-road_0135-tw_00330
    :parameters (?v - vehicle ?r - road_type_road_0135 ?from - loc_type_loc_0083 ?to - loc_type_loc_0082 ?w - window_type_tw_00330)
    :precondition (and
      (at ?v ?from)
      (connects ?r ?from ?to)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
      (not (location-blocked ?from))
      (not (location-blocked ?to))
    )
    :effect (and
      (not (at ?v ?from))
      (at ?v ?to)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-road_0135-tw_00360
    :parameters (?v - vehicle ?r - road_type_road_0135 ?from - loc_type_loc_0083 ?to - loc_type_loc_0082 ?w - window_type_tw_00360)
    :precondition (and
      (at ?v ?from)
      (connects ?r ?from ?to)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
      (not (location-blocked ?from))
      (not (location-blocked ?to))
    )
    :effect (and
      (not (at ?v ?from))
      (at ?v ?to)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-road_0135-tw_00390
    :parameters (?v - vehicle ?r - road_type_road_0135 ?from - loc_type_loc_0083 ?to - loc_type_loc_0082 ?w - window_type_tw_00390)
    :precondition (and
      (at ?v ?from)
      (connects ?r ?from ?to)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
      (not (location-blocked ?from))
      (not (location-blocked ?to))
    )
    :effect (and
      (not (at ?v ?from))
      (at ?v ?to)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-road_0135-tw_00420
    :parameters (?v - vehicle ?r - road_type_road_0135 ?from - loc_type_loc_0083 ?to - loc_type_loc_0082 ?w - window_type_tw_00420)
    :precondition (and
      (at ?v ?from)
      (connects ?r ?from ?to)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
      (not (location-blocked ?from))
      (not (location-blocked ?to))
    )
    :effect (and
      (not (at ?v ?from))
      (at ?v ?to)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-road_0135-tw_00450
    :parameters (?v - vehicle ?r - road_type_road_0135 ?from - loc_type_loc_0083 ?to - loc_type_loc_0082 ?w - window_type_tw_00450)
    :precondition (and
      (at ?v ?from)
      (connects ?r ?from ?to)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
      (not (location-blocked ?from))
      (not (location-blocked ?to))
    )
    :effect (and
      (not (at ?v ?from))
      (at ?v ?to)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-road_0137-tw_00000
    :parameters (?v - vehicle ?r - road_type_road_0137 ?from - loc_type_loc_0084 ?to - loc_type_loc_0085 ?w - window_type_tw_00000)
    :precondition (and
      (at ?v ?from)
      (connects ?r ?from ?to)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
      (not (location-blocked ?from))
      (not (location-blocked ?to))
    )
    :effect (and
      (not (at ?v ?from))
      (at ?v ?to)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-road_0137-tw_00030
    :parameters (?v - vehicle ?r - road_type_road_0137 ?from - loc_type_loc_0084 ?to - loc_type_loc_0085 ?w - window_type_tw_00030)
    :precondition (and
      (at ?v ?from)
      (connects ?r ?from ?to)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
      (not (location-blocked ?from))
      (not (location-blocked ?to))
    )
    :effect (and
      (not (at ?v ?from))
      (at ?v ?to)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-road_0137-tw_00060
    :parameters (?v - vehicle ?r - road_type_road_0137 ?from - loc_type_loc_0084 ?to - loc_type_loc_0085 ?w - window_type_tw_00060)
    :precondition (and
      (at ?v ?from)
      (connects ?r ?from ?to)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
      (not (location-blocked ?from))
      (not (location-blocked ?to))
    )
    :effect (and
      (not (at ?v ?from))
      (at ?v ?to)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-road_0137-tw_00090
    :parameters (?v - vehicle ?r - road_type_road_0137 ?from - loc_type_loc_0084 ?to - loc_type_loc_0085 ?w - window_type_tw_00090)
    :precondition (and
      (at ?v ?from)
      (connects ?r ?from ?to)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
      (not (location-blocked ?from))
      (not (location-blocked ?to))
    )
    :effect (and
      (not (at ?v ?from))
      (at ?v ?to)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-road_0137-tw_00120
    :parameters (?v - vehicle ?r - road_type_road_0137 ?from - loc_type_loc_0084 ?to - loc_type_loc_0085 ?w - window_type_tw_00120)
    :precondition (and
      (at ?v ?from)
      (connects ?r ?from ?to)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
      (not (location-blocked ?from))
      (not (location-blocked ?to))
    )
    :effect (and
      (not (at ?v ?from))
      (at ?v ?to)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-road_0137-tw_00150
    :parameters (?v - vehicle ?r - road_type_road_0137 ?from - loc_type_loc_0084 ?to - loc_type_loc_0085 ?w - window_type_tw_00150)
    :precondition (and
      (at ?v ?from)
      (connects ?r ?from ?to)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
      (not (location-blocked ?from))
      (not (location-blocked ?to))
    )
    :effect (and
      (not (at ?v ?from))
      (at ?v ?to)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-road_0137-tw_00180
    :parameters (?v - vehicle ?r - road_type_road_0137 ?from - loc_type_loc_0084 ?to - loc_type_loc_0085 ?w - window_type_tw_00180)
    :precondition (and
      (at ?v ?from)
      (connects ?r ?from ?to)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
      (not (location-blocked ?from))
      (not (location-blocked ?to))
    )
    :effect (and
      (not (at ?v ?from))
      (at ?v ?to)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-road_0137-tw_00210
    :parameters (?v - vehicle ?r - road_type_road_0137 ?from - loc_type_loc_0084 ?to - loc_type_loc_0085 ?w - window_type_tw_00210)
    :precondition (and
      (at ?v ?from)
      (connects ?r ?from ?to)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
      (not (location-blocked ?from))
      (not (location-blocked ?to))
    )
    :effect (and
      (not (at ?v ?from))
      (at ?v ?to)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-road_0137-tw_00240
    :parameters (?v - vehicle ?r - road_type_road_0137 ?from - loc_type_loc_0084 ?to - loc_type_loc_0085 ?w - window_type_tw_00240)
    :precondition (and
      (at ?v ?from)
      (connects ?r ?from ?to)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
      (not (location-blocked ?from))
      (not (location-blocked ?to))
    )
    :effect (and
      (not (at ?v ?from))
      (at ?v ?to)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-road_0137-tw_00270
    :parameters (?v - vehicle ?r - road_type_road_0137 ?from - loc_type_loc_0084 ?to - loc_type_loc_0085 ?w - window_type_tw_00270)
    :precondition (and
      (at ?v ?from)
      (connects ?r ?from ?to)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
      (not (location-blocked ?from))
      (not (location-blocked ?to))
    )
    :effect (and
      (not (at ?v ?from))
      (at ?v ?to)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-road_0137-tw_00300
    :parameters (?v - vehicle ?r - road_type_road_0137 ?from - loc_type_loc_0084 ?to - loc_type_loc_0085 ?w - window_type_tw_00300)
    :precondition (and
      (at ?v ?from)
      (connects ?r ?from ?to)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
      (not (location-blocked ?from))
      (not (location-blocked ?to))
    )
    :effect (and
      (not (at ?v ?from))
      (at ?v ?to)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-road_0137-tw_00330
    :parameters (?v - vehicle ?r - road_type_road_0137 ?from - loc_type_loc_0084 ?to - loc_type_loc_0085 ?w - window_type_tw_00330)
    :precondition (and
      (at ?v ?from)
      (connects ?r ?from ?to)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
      (not (location-blocked ?from))
      (not (location-blocked ?to))
    )
    :effect (and
      (not (at ?v ?from))
      (at ?v ?to)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-road_0137-tw_00360
    :parameters (?v - vehicle ?r - road_type_road_0137 ?from - loc_type_loc_0084 ?to - loc_type_loc_0085 ?w - window_type_tw_00360)
    :precondition (and
      (at ?v ?from)
      (connects ?r ?from ?to)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
      (not (location-blocked ?from))
      (not (location-blocked ?to))
    )
    :effect (and
      (not (at ?v ?from))
      (at ?v ?to)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-road_0137-tw_00390
    :parameters (?v - vehicle ?r - road_type_road_0137 ?from - loc_type_loc_0084 ?to - loc_type_loc_0085 ?w - window_type_tw_00390)
    :precondition (and
      (at ?v ?from)
      (connects ?r ?from ?to)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
      (not (location-blocked ?from))
      (not (location-blocked ?to))
    )
    :effect (and
      (not (at ?v ?from))
      (at ?v ?to)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-road_0137-tw_00420
    :parameters (?v - vehicle ?r - road_type_road_0137 ?from - loc_type_loc_0084 ?to - loc_type_loc_0085 ?w - window_type_tw_00420)
    :precondition (and
      (at ?v ?from)
      (connects ?r ?from ?to)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
      (not (location-blocked ?from))
      (not (location-blocked ?to))
    )
    :effect (and
      (not (at ?v ?from))
      (at ?v ?to)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-road_0137-tw_00450
    :parameters (?v - vehicle ?r - road_type_road_0137 ?from - loc_type_loc_0084 ?to - loc_type_loc_0085 ?w - window_type_tw_00450)
    :precondition (and
      (at ?v ?from)
      (connects ?r ?from ?to)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
      (not (location-blocked ?from))
      (not (location-blocked ?to))
    )
    :effect (and
      (not (at ?v ?from))
      (at ?v ?to)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-road_0138-tw_00000
    :parameters (?v - vehicle ?r - road_type_road_0138 ?from - loc_type_loc_0084 ?to - loc_type_loc_0006 ?w - window_type_tw_00000)
    :precondition (and
      (at ?v ?from)
      (connects ?r ?from ?to)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
      (not (location-blocked ?from))
      (not (location-blocked ?to))
    )
    :effect (and
      (not (at ?v ?from))
      (at ?v ?to)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-road_0138-tw_00030
    :parameters (?v - vehicle ?r - road_type_road_0138 ?from - loc_type_loc_0084 ?to - loc_type_loc_0006 ?w - window_type_tw_00030)
    :precondition (and
      (at ?v ?from)
      (connects ?r ?from ?to)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
      (not (location-blocked ?from))
      (not (location-blocked ?to))
    )
    :effect (and
      (not (at ?v ?from))
      (at ?v ?to)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-road_0138-tw_00060
    :parameters (?v - vehicle ?r - road_type_road_0138 ?from - loc_type_loc_0084 ?to - loc_type_loc_0006 ?w - window_type_tw_00060)
    :precondition (and
      (at ?v ?from)
      (connects ?r ?from ?to)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
      (not (location-blocked ?from))
      (not (location-blocked ?to))
    )
    :effect (and
      (not (at ?v ?from))
      (at ?v ?to)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-road_0138-tw_00090
    :parameters (?v - vehicle ?r - road_type_road_0138 ?from - loc_type_loc_0084 ?to - loc_type_loc_0006 ?w - window_type_tw_00090)
    :precondition (and
      (at ?v ?from)
      (connects ?r ?from ?to)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
      (not (location-blocked ?from))
      (not (location-blocked ?to))
    )
    :effect (and
      (not (at ?v ?from))
      (at ?v ?to)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-road_0138-tw_00120
    :parameters (?v - vehicle ?r - road_type_road_0138 ?from - loc_type_loc_0084 ?to - loc_type_loc_0006 ?w - window_type_tw_00120)
    :precondition (and
      (at ?v ?from)
      (connects ?r ?from ?to)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
      (not (location-blocked ?from))
      (not (location-blocked ?to))
    )
    :effect (and
      (not (at ?v ?from))
      (at ?v ?to)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-road_0138-tw_00150
    :parameters (?v - vehicle ?r - road_type_road_0138 ?from - loc_type_loc_0084 ?to - loc_type_loc_0006 ?w - window_type_tw_00150)
    :precondition (and
      (at ?v ?from)
      (connects ?r ?from ?to)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
      (not (location-blocked ?from))
      (not (location-blocked ?to))
    )
    :effect (and
      (not (at ?v ?from))
      (at ?v ?to)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-road_0138-tw_00180
    :parameters (?v - vehicle ?r - road_type_road_0138 ?from - loc_type_loc_0084 ?to - loc_type_loc_0006 ?w - window_type_tw_00180)
    :precondition (and
      (at ?v ?from)
      (connects ?r ?from ?to)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
      (not (location-blocked ?from))
      (not (location-blocked ?to))
    )
    :effect (and
      (not (at ?v ?from))
      (at ?v ?to)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-road_0138-tw_00210
    :parameters (?v - vehicle ?r - road_type_road_0138 ?from - loc_type_loc_0084 ?to - loc_type_loc_0006 ?w - window_type_tw_00210)
    :precondition (and
      (at ?v ?from)
      (connects ?r ?from ?to)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
      (not (location-blocked ?from))
      (not (location-blocked ?to))
    )
    :effect (and
      (not (at ?v ?from))
      (at ?v ?to)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-road_0138-tw_00240
    :parameters (?v - vehicle ?r - road_type_road_0138 ?from - loc_type_loc_0084 ?to - loc_type_loc_0006 ?w - window_type_tw_00240)
    :precondition (and
      (at ?v ?from)
      (connects ?r ?from ?to)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
      (not (location-blocked ?from))
      (not (location-blocked ?to))
    )
    :effect (and
      (not (at ?v ?from))
      (at ?v ?to)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-road_0138-tw_00270
    :parameters (?v - vehicle ?r - road_type_road_0138 ?from - loc_type_loc_0084 ?to - loc_type_loc_0006 ?w - window_type_tw_00270)
    :precondition (and
      (at ?v ?from)
      (connects ?r ?from ?to)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
      (not (location-blocked ?from))
      (not (location-blocked ?to))
    )
    :effect (and
      (not (at ?v ?from))
      (at ?v ?to)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-road_0138-tw_00300
    :parameters (?v - vehicle ?r - road_type_road_0138 ?from - loc_type_loc_0084 ?to - loc_type_loc_0006 ?w - window_type_tw_00300)
    :precondition (and
      (at ?v ?from)
      (connects ?r ?from ?to)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
      (not (location-blocked ?from))
      (not (location-blocked ?to))
    )
    :effect (and
      (not (at ?v ?from))
      (at ?v ?to)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-road_0138-tw_00330
    :parameters (?v - vehicle ?r - road_type_road_0138 ?from - loc_type_loc_0084 ?to - loc_type_loc_0006 ?w - window_type_tw_00330)
    :precondition (and
      (at ?v ?from)
      (connects ?r ?from ?to)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
      (not (location-blocked ?from))
      (not (location-blocked ?to))
    )
    :effect (and
      (not (at ?v ?from))
      (at ?v ?to)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-road_0138-tw_00360
    :parameters (?v - vehicle ?r - road_type_road_0138 ?from - loc_type_loc_0084 ?to - loc_type_loc_0006 ?w - window_type_tw_00360)
    :precondition (and
      (at ?v ?from)
      (connects ?r ?from ?to)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
      (not (location-blocked ?from))
      (not (location-blocked ?to))
    )
    :effect (and
      (not (at ?v ?from))
      (at ?v ?to)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-road_0138-tw_00390
    :parameters (?v - vehicle ?r - road_type_road_0138 ?from - loc_type_loc_0084 ?to - loc_type_loc_0006 ?w - window_type_tw_00390)
    :precondition (and
      (at ?v ?from)
      (connects ?r ?from ?to)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
      (not (location-blocked ?from))
      (not (location-blocked ?to))
    )
    :effect (and
      (not (at ?v ?from))
      (at ?v ?to)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-road_0138-tw_00420
    :parameters (?v - vehicle ?r - road_type_road_0138 ?from - loc_type_loc_0084 ?to - loc_type_loc_0006 ?w - window_type_tw_00420)
    :precondition (and
      (at ?v ?from)
      (connects ?r ?from ?to)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
      (not (location-blocked ?from))
      (not (location-blocked ?to))
    )
    :effect (and
      (not (at ?v ?from))
      (at ?v ?to)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-road_0138-tw_00450
    :parameters (?v - vehicle ?r - road_type_road_0138 ?from - loc_type_loc_0084 ?to - loc_type_loc_0006 ?w - window_type_tw_00450)
    :precondition (and
      (at ?v ?from)
      (connects ?r ?from ?to)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
      (not (location-blocked ?from))
      (not (location-blocked ?to))
    )
    :effect (and
      (not (at ?v ?from))
      (at ?v ?to)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-road_0139-tw_00000
    :parameters (?v - vehicle ?r - road_type_road_0139 ?from - loc_type_loc_0085 ?to - loc_type_loc_0069 ?w - window_type_tw_00000)
    :precondition (and
      (at ?v ?from)
      (connects ?r ?from ?to)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
      (not (location-blocked ?from))
      (not (location-blocked ?to))
    )
    :effect (and
      (not (at ?v ?from))
      (at ?v ?to)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-road_0139-tw_00030
    :parameters (?v - vehicle ?r - road_type_road_0139 ?from - loc_type_loc_0085 ?to - loc_type_loc_0069 ?w - window_type_tw_00030)
    :precondition (and
      (at ?v ?from)
      (connects ?r ?from ?to)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
      (not (location-blocked ?from))
      (not (location-blocked ?to))
    )
    :effect (and
      (not (at ?v ?from))
      (at ?v ?to)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-road_0139-tw_00060
    :parameters (?v - vehicle ?r - road_type_road_0139 ?from - loc_type_loc_0085 ?to - loc_type_loc_0069 ?w - window_type_tw_00060)
    :precondition (and
      (at ?v ?from)
      (connects ?r ?from ?to)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
      (not (location-blocked ?from))
      (not (location-blocked ?to))
    )
    :effect (and
      (not (at ?v ?from))
      (at ?v ?to)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-road_0139-tw_00090
    :parameters (?v - vehicle ?r - road_type_road_0139 ?from - loc_type_loc_0085 ?to - loc_type_loc_0069 ?w - window_type_tw_00090)
    :precondition (and
      (at ?v ?from)
      (connects ?r ?from ?to)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
      (not (location-blocked ?from))
      (not (location-blocked ?to))
    )
    :effect (and
      (not (at ?v ?from))
      (at ?v ?to)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-road_0139-tw_00120
    :parameters (?v - vehicle ?r - road_type_road_0139 ?from - loc_type_loc_0085 ?to - loc_type_loc_0069 ?w - window_type_tw_00120)
    :precondition (and
      (at ?v ?from)
      (connects ?r ?from ?to)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
      (not (location-blocked ?from))
      (not (location-blocked ?to))
    )
    :effect (and
      (not (at ?v ?from))
      (at ?v ?to)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-road_0139-tw_00150
    :parameters (?v - vehicle ?r - road_type_road_0139 ?from - loc_type_loc_0085 ?to - loc_type_loc_0069 ?w - window_type_tw_00150)
    :precondition (and
      (at ?v ?from)
      (connects ?r ?from ?to)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
      (not (location-blocked ?from))
      (not (location-blocked ?to))
    )
    :effect (and
      (not (at ?v ?from))
      (at ?v ?to)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-road_0139-tw_00180
    :parameters (?v - vehicle ?r - road_type_road_0139 ?from - loc_type_loc_0085 ?to - loc_type_loc_0069 ?w - window_type_tw_00180)
    :precondition (and
      (at ?v ?from)
      (connects ?r ?from ?to)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
      (not (location-blocked ?from))
      (not (location-blocked ?to))
    )
    :effect (and
      (not (at ?v ?from))
      (at ?v ?to)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-road_0139-tw_00210
    :parameters (?v - vehicle ?r - road_type_road_0139 ?from - loc_type_loc_0085 ?to - loc_type_loc_0069 ?w - window_type_tw_00210)
    :precondition (and
      (at ?v ?from)
      (connects ?r ?from ?to)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
      (not (location-blocked ?from))
      (not (location-blocked ?to))
    )
    :effect (and
      (not (at ?v ?from))
      (at ?v ?to)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-road_0139-tw_00240
    :parameters (?v - vehicle ?r - road_type_road_0139 ?from - loc_type_loc_0085 ?to - loc_type_loc_0069 ?w - window_type_tw_00240)
    :precondition (and
      (at ?v ?from)
      (connects ?r ?from ?to)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
      (not (location-blocked ?from))
      (not (location-blocked ?to))
    )
    :effect (and
      (not (at ?v ?from))
      (at ?v ?to)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-road_0139-tw_00270
    :parameters (?v - vehicle ?r - road_type_road_0139 ?from - loc_type_loc_0085 ?to - loc_type_loc_0069 ?w - window_type_tw_00270)
    :precondition (and
      (at ?v ?from)
      (connects ?r ?from ?to)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
      (not (location-blocked ?from))
      (not (location-blocked ?to))
    )
    :effect (and
      (not (at ?v ?from))
      (at ?v ?to)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-road_0139-tw_00300
    :parameters (?v - vehicle ?r - road_type_road_0139 ?from - loc_type_loc_0085 ?to - loc_type_loc_0069 ?w - window_type_tw_00300)
    :precondition (and
      (at ?v ?from)
      (connects ?r ?from ?to)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
      (not (location-blocked ?from))
      (not (location-blocked ?to))
    )
    :effect (and
      (not (at ?v ?from))
      (at ?v ?to)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-road_0139-tw_00330
    :parameters (?v - vehicle ?r - road_type_road_0139 ?from - loc_type_loc_0085 ?to - loc_type_loc_0069 ?w - window_type_tw_00330)
    :precondition (and
      (at ?v ?from)
      (connects ?r ?from ?to)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
      (not (location-blocked ?from))
      (not (location-blocked ?to))
    )
    :effect (and
      (not (at ?v ?from))
      (at ?v ?to)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-road_0139-tw_00360
    :parameters (?v - vehicle ?r - road_type_road_0139 ?from - loc_type_loc_0085 ?to - loc_type_loc_0069 ?w - window_type_tw_00360)
    :precondition (and
      (at ?v ?from)
      (connects ?r ?from ?to)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
      (not (location-blocked ?from))
      (not (location-blocked ?to))
    )
    :effect (and
      (not (at ?v ?from))
      (at ?v ?to)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-road_0139-tw_00390
    :parameters (?v - vehicle ?r - road_type_road_0139 ?from - loc_type_loc_0085 ?to - loc_type_loc_0069 ?w - window_type_tw_00390)
    :precondition (and
      (at ?v ?from)
      (connects ?r ?from ?to)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
      (not (location-blocked ?from))
      (not (location-blocked ?to))
    )
    :effect (and
      (not (at ?v ?from))
      (at ?v ?to)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-road_0139-tw_00420
    :parameters (?v - vehicle ?r - road_type_road_0139 ?from - loc_type_loc_0085 ?to - loc_type_loc_0069 ?w - window_type_tw_00420)
    :precondition (and
      (at ?v ?from)
      (connects ?r ?from ?to)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
      (not (location-blocked ?from))
      (not (location-blocked ?to))
    )
    :effect (and
      (not (at ?v ?from))
      (at ?v ?to)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-road_0139-tw_00450
    :parameters (?v - vehicle ?r - road_type_road_0139 ?from - loc_type_loc_0085 ?to - loc_type_loc_0069 ?w - window_type_tw_00450)
    :precondition (and
      (at ?v ?from)
      (connects ?r ?from ?to)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
      (not (location-blocked ?from))
      (not (location-blocked ?to))
    )
    :effect (and
      (not (at ?v ?from))
      (at ?v ?to)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-road_0140-tw_00000
    :parameters (?v - vehicle ?r - road_type_road_0140 ?from - loc_type_loc_0087 ?to - loc_type_loc_0081 ?w - window_type_tw_00000)
    :precondition (and
      (at ?v ?from)
      (connects ?r ?from ?to)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
      (not (location-blocked ?from))
      (not (location-blocked ?to))
    )
    :effect (and
      (not (at ?v ?from))
      (at ?v ?to)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-road_0140-tw_00030
    :parameters (?v - vehicle ?r - road_type_road_0140 ?from - loc_type_loc_0087 ?to - loc_type_loc_0081 ?w - window_type_tw_00030)
    :precondition (and
      (at ?v ?from)
      (connects ?r ?from ?to)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
      (not (location-blocked ?from))
      (not (location-blocked ?to))
    )
    :effect (and
      (not (at ?v ?from))
      (at ?v ?to)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-road_0140-tw_00060
    :parameters (?v - vehicle ?r - road_type_road_0140 ?from - loc_type_loc_0087 ?to - loc_type_loc_0081 ?w - window_type_tw_00060)
    :precondition (and
      (at ?v ?from)
      (connects ?r ?from ?to)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
      (not (location-blocked ?from))
      (not (location-blocked ?to))
    )
    :effect (and
      (not (at ?v ?from))
      (at ?v ?to)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-road_0140-tw_00090
    :parameters (?v - vehicle ?r - road_type_road_0140 ?from - loc_type_loc_0087 ?to - loc_type_loc_0081 ?w - window_type_tw_00090)
    :precondition (and
      (at ?v ?from)
      (connects ?r ?from ?to)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
      (not (location-blocked ?from))
      (not (location-blocked ?to))
    )
    :effect (and
      (not (at ?v ?from))
      (at ?v ?to)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-road_0140-tw_00120
    :parameters (?v - vehicle ?r - road_type_road_0140 ?from - loc_type_loc_0087 ?to - loc_type_loc_0081 ?w - window_type_tw_00120)
    :precondition (and
      (at ?v ?from)
      (connects ?r ?from ?to)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
      (not (location-blocked ?from))
      (not (location-blocked ?to))
    )
    :effect (and
      (not (at ?v ?from))
      (at ?v ?to)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-road_0140-tw_00150
    :parameters (?v - vehicle ?r - road_type_road_0140 ?from - loc_type_loc_0087 ?to - loc_type_loc_0081 ?w - window_type_tw_00150)
    :precondition (and
      (at ?v ?from)
      (connects ?r ?from ?to)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
      (not (location-blocked ?from))
      (not (location-blocked ?to))
    )
    :effect (and
      (not (at ?v ?from))
      (at ?v ?to)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-road_0140-tw_00180
    :parameters (?v - vehicle ?r - road_type_road_0140 ?from - loc_type_loc_0087 ?to - loc_type_loc_0081 ?w - window_type_tw_00180)
    :precondition (and
      (at ?v ?from)
      (connects ?r ?from ?to)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
      (not (location-blocked ?from))
      (not (location-blocked ?to))
    )
    :effect (and
      (not (at ?v ?from))
      (at ?v ?to)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-road_0140-tw_00210
    :parameters (?v - vehicle ?r - road_type_road_0140 ?from - loc_type_loc_0087 ?to - loc_type_loc_0081 ?w - window_type_tw_00210)
    :precondition (and
      (at ?v ?from)
      (connects ?r ?from ?to)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
      (not (location-blocked ?from))
      (not (location-blocked ?to))
    )
    :effect (and
      (not (at ?v ?from))
      (at ?v ?to)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-road_0140-tw_00240
    :parameters (?v - vehicle ?r - road_type_road_0140 ?from - loc_type_loc_0087 ?to - loc_type_loc_0081 ?w - window_type_tw_00240)
    :precondition (and
      (at ?v ?from)
      (connects ?r ?from ?to)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
      (not (location-blocked ?from))
      (not (location-blocked ?to))
    )
    :effect (and
      (not (at ?v ?from))
      (at ?v ?to)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-road_0140-tw_00270
    :parameters (?v - vehicle ?r - road_type_road_0140 ?from - loc_type_loc_0087 ?to - loc_type_loc_0081 ?w - window_type_tw_00270)
    :precondition (and
      (at ?v ?from)
      (connects ?r ?from ?to)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
      (not (location-blocked ?from))
      (not (location-blocked ?to))
    )
    :effect (and
      (not (at ?v ?from))
      (at ?v ?to)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-road_0140-tw_00300
    :parameters (?v - vehicle ?r - road_type_road_0140 ?from - loc_type_loc_0087 ?to - loc_type_loc_0081 ?w - window_type_tw_00300)
    :precondition (and
      (at ?v ?from)
      (connects ?r ?from ?to)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
      (not (location-blocked ?from))
      (not (location-blocked ?to))
    )
    :effect (and
      (not (at ?v ?from))
      (at ?v ?to)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-road_0140-tw_00330
    :parameters (?v - vehicle ?r - road_type_road_0140 ?from - loc_type_loc_0087 ?to - loc_type_loc_0081 ?w - window_type_tw_00330)
    :precondition (and
      (at ?v ?from)
      (connects ?r ?from ?to)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
      (not (location-blocked ?from))
      (not (location-blocked ?to))
    )
    :effect (and
      (not (at ?v ?from))
      (at ?v ?to)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-road_0140-tw_00360
    :parameters (?v - vehicle ?r - road_type_road_0140 ?from - loc_type_loc_0087 ?to - loc_type_loc_0081 ?w - window_type_tw_00360)
    :precondition (and
      (at ?v ?from)
      (connects ?r ?from ?to)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
      (not (location-blocked ?from))
      (not (location-blocked ?to))
    )
    :effect (and
      (not (at ?v ?from))
      (at ?v ?to)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-road_0140-tw_00390
    :parameters (?v - vehicle ?r - road_type_road_0140 ?from - loc_type_loc_0087 ?to - loc_type_loc_0081 ?w - window_type_tw_00390)
    :precondition (and
      (at ?v ?from)
      (connects ?r ?from ?to)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
      (not (location-blocked ?from))
      (not (location-blocked ?to))
    )
    :effect (and
      (not (at ?v ?from))
      (at ?v ?to)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-road_0140-tw_00420
    :parameters (?v - vehicle ?r - road_type_road_0140 ?from - loc_type_loc_0087 ?to - loc_type_loc_0081 ?w - window_type_tw_00420)
    :precondition (and
      (at ?v ?from)
      (connects ?r ?from ?to)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
      (not (location-blocked ?from))
      (not (location-blocked ?to))
    )
    :effect (and
      (not (at ?v ?from))
      (at ?v ?to)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-road_0140-tw_00450
    :parameters (?v - vehicle ?r - road_type_road_0140 ?from - loc_type_loc_0087 ?to - loc_type_loc_0081 ?w - window_type_tw_00450)
    :precondition (and
      (at ?v ?from)
      (connects ?r ?from ?to)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
      (not (location-blocked ?from))
      (not (location-blocked ?to))
    )
    :effect (and
      (not (at ?v ?from))
      (at ?v ?to)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-road_0141-tw_00000
    :parameters (?v - vehicle ?r - road_type_road_0141 ?from - loc_type_loc_0087 ?to - loc_type_loc_0000 ?w - window_type_tw_00000)
    :precondition (and
      (at ?v ?from)
      (connects ?r ?from ?to)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
      (not (location-blocked ?from))
      (not (location-blocked ?to))
    )
    :effect (and
      (not (at ?v ?from))
      (at ?v ?to)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-road_0141-tw_00030
    :parameters (?v - vehicle ?r - road_type_road_0141 ?from - loc_type_loc_0087 ?to - loc_type_loc_0000 ?w - window_type_tw_00030)
    :precondition (and
      (at ?v ?from)
      (connects ?r ?from ?to)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
      (not (location-blocked ?from))
      (not (location-blocked ?to))
    )
    :effect (and
      (not (at ?v ?from))
      (at ?v ?to)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-road_0141-tw_00060
    :parameters (?v - vehicle ?r - road_type_road_0141 ?from - loc_type_loc_0087 ?to - loc_type_loc_0000 ?w - window_type_tw_00060)
    :precondition (and
      (at ?v ?from)
      (connects ?r ?from ?to)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
      (not (location-blocked ?from))
      (not (location-blocked ?to))
    )
    :effect (and
      (not (at ?v ?from))
      (at ?v ?to)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-road_0141-tw_00090
    :parameters (?v - vehicle ?r - road_type_road_0141 ?from - loc_type_loc_0087 ?to - loc_type_loc_0000 ?w - window_type_tw_00090)
    :precondition (and
      (at ?v ?from)
      (connects ?r ?from ?to)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
      (not (location-blocked ?from))
      (not (location-blocked ?to))
    )
    :effect (and
      (not (at ?v ?from))
      (at ?v ?to)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-road_0141-tw_00120
    :parameters (?v - vehicle ?r - road_type_road_0141 ?from - loc_type_loc_0087 ?to - loc_type_loc_0000 ?w - window_type_tw_00120)
    :precondition (and
      (at ?v ?from)
      (connects ?r ?from ?to)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
      (not (location-blocked ?from))
      (not (location-blocked ?to))
    )
    :effect (and
      (not (at ?v ?from))
      (at ?v ?to)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-road_0141-tw_00150
    :parameters (?v - vehicle ?r - road_type_road_0141 ?from - loc_type_loc_0087 ?to - loc_type_loc_0000 ?w - window_type_tw_00150)
    :precondition (and
      (at ?v ?from)
      (connects ?r ?from ?to)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
      (not (location-blocked ?from))
      (not (location-blocked ?to))
    )
    :effect (and
      (not (at ?v ?from))
      (at ?v ?to)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-road_0141-tw_00180
    :parameters (?v - vehicle ?r - road_type_road_0141 ?from - loc_type_loc_0087 ?to - loc_type_loc_0000 ?w - window_type_tw_00180)
    :precondition (and
      (at ?v ?from)
      (connects ?r ?from ?to)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
      (not (location-blocked ?from))
      (not (location-blocked ?to))
    )
    :effect (and
      (not (at ?v ?from))
      (at ?v ?to)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-road_0141-tw_00210
    :parameters (?v - vehicle ?r - road_type_road_0141 ?from - loc_type_loc_0087 ?to - loc_type_loc_0000 ?w - window_type_tw_00210)
    :precondition (and
      (at ?v ?from)
      (connects ?r ?from ?to)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
      (not (location-blocked ?from))
      (not (location-blocked ?to))
    )
    :effect (and
      (not (at ?v ?from))
      (at ?v ?to)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-road_0141-tw_00240
    :parameters (?v - vehicle ?r - road_type_road_0141 ?from - loc_type_loc_0087 ?to - loc_type_loc_0000 ?w - window_type_tw_00240)
    :precondition (and
      (at ?v ?from)
      (connects ?r ?from ?to)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
      (not (location-blocked ?from))
      (not (location-blocked ?to))
    )
    :effect (and
      (not (at ?v ?from))
      (at ?v ?to)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-road_0141-tw_00270
    :parameters (?v - vehicle ?r - road_type_road_0141 ?from - loc_type_loc_0087 ?to - loc_type_loc_0000 ?w - window_type_tw_00270)
    :precondition (and
      (at ?v ?from)
      (connects ?r ?from ?to)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
      (not (location-blocked ?from))
      (not (location-blocked ?to))
    )
    :effect (and
      (not (at ?v ?from))
      (at ?v ?to)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-road_0141-tw_00300
    :parameters (?v - vehicle ?r - road_type_road_0141 ?from - loc_type_loc_0087 ?to - loc_type_loc_0000 ?w - window_type_tw_00300)
    :precondition (and
      (at ?v ?from)
      (connects ?r ?from ?to)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
      (not (location-blocked ?from))
      (not (location-blocked ?to))
    )
    :effect (and
      (not (at ?v ?from))
      (at ?v ?to)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-road_0141-tw_00330
    :parameters (?v - vehicle ?r - road_type_road_0141 ?from - loc_type_loc_0087 ?to - loc_type_loc_0000 ?w - window_type_tw_00330)
    :precondition (and
      (at ?v ?from)
      (connects ?r ?from ?to)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
      (not (location-blocked ?from))
      (not (location-blocked ?to))
    )
    :effect (and
      (not (at ?v ?from))
      (at ?v ?to)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-road_0141-tw_00360
    :parameters (?v - vehicle ?r - road_type_road_0141 ?from - loc_type_loc_0087 ?to - loc_type_loc_0000 ?w - window_type_tw_00360)
    :precondition (and
      (at ?v ?from)
      (connects ?r ?from ?to)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
      (not (location-blocked ?from))
      (not (location-blocked ?to))
    )
    :effect (and
      (not (at ?v ?from))
      (at ?v ?to)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-road_0141-tw_00390
    :parameters (?v - vehicle ?r - road_type_road_0141 ?from - loc_type_loc_0087 ?to - loc_type_loc_0000 ?w - window_type_tw_00390)
    :precondition (and
      (at ?v ?from)
      (connects ?r ?from ?to)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
      (not (location-blocked ?from))
      (not (location-blocked ?to))
    )
    :effect (and
      (not (at ?v ?from))
      (at ?v ?to)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-road_0141-tw_00420
    :parameters (?v - vehicle ?r - road_type_road_0141 ?from - loc_type_loc_0087 ?to - loc_type_loc_0000 ?w - window_type_tw_00420)
    :precondition (and
      (at ?v ?from)
      (connects ?r ?from ?to)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
      (not (location-blocked ?from))
      (not (location-blocked ?to))
    )
    :effect (and
      (not (at ?v ?from))
      (at ?v ?to)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-road_0141-tw_00450
    :parameters (?v - vehicle ?r - road_type_road_0141 ?from - loc_type_loc_0087 ?to - loc_type_loc_0000 ?w - window_type_tw_00450)
    :precondition (and
      (at ?v ?from)
      (connects ?r ?from ?to)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
      (not (location-blocked ?from))
      (not (location-blocked ?to))
    )
    :effect (and
      (not (at ?v ?from))
      (at ?v ?to)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-road_0142-tw_00000
    :parameters (?v - vehicle ?r - road_type_road_0142 ?from - loc_type_loc_0087 ?to - loc_type_loc_0069 ?w - window_type_tw_00000)
    :precondition (and
      (at ?v ?from)
      (connects ?r ?from ?to)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
      (not (location-blocked ?from))
      (not (location-blocked ?to))
    )
    :effect (and
      (not (at ?v ?from))
      (at ?v ?to)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-road_0142-tw_00030
    :parameters (?v - vehicle ?r - road_type_road_0142 ?from - loc_type_loc_0087 ?to - loc_type_loc_0069 ?w - window_type_tw_00030)
    :precondition (and
      (at ?v ?from)
      (connects ?r ?from ?to)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
      (not (location-blocked ?from))
      (not (location-blocked ?to))
    )
    :effect (and
      (not (at ?v ?from))
      (at ?v ?to)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-road_0142-tw_00060
    :parameters (?v - vehicle ?r - road_type_road_0142 ?from - loc_type_loc_0087 ?to - loc_type_loc_0069 ?w - window_type_tw_00060)
    :precondition (and
      (at ?v ?from)
      (connects ?r ?from ?to)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
      (not (location-blocked ?from))
      (not (location-blocked ?to))
    )
    :effect (and
      (not (at ?v ?from))
      (at ?v ?to)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-road_0142-tw_00090
    :parameters (?v - vehicle ?r - road_type_road_0142 ?from - loc_type_loc_0087 ?to - loc_type_loc_0069 ?w - window_type_tw_00090)
    :precondition (and
      (at ?v ?from)
      (connects ?r ?from ?to)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
      (not (location-blocked ?from))
      (not (location-blocked ?to))
    )
    :effect (and
      (not (at ?v ?from))
      (at ?v ?to)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-road_0142-tw_00120
    :parameters (?v - vehicle ?r - road_type_road_0142 ?from - loc_type_loc_0087 ?to - loc_type_loc_0069 ?w - window_type_tw_00120)
    :precondition (and
      (at ?v ?from)
      (connects ?r ?from ?to)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
      (not (location-blocked ?from))
      (not (location-blocked ?to))
    )
    :effect (and
      (not (at ?v ?from))
      (at ?v ?to)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-road_0142-tw_00150
    :parameters (?v - vehicle ?r - road_type_road_0142 ?from - loc_type_loc_0087 ?to - loc_type_loc_0069 ?w - window_type_tw_00150)
    :precondition (and
      (at ?v ?from)
      (connects ?r ?from ?to)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
      (not (location-blocked ?from))
      (not (location-blocked ?to))
    )
    :effect (and
      (not (at ?v ?from))
      (at ?v ?to)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-road_0142-tw_00180
    :parameters (?v - vehicle ?r - road_type_road_0142 ?from - loc_type_loc_0087 ?to - loc_type_loc_0069 ?w - window_type_tw_00180)
    :precondition (and
      (at ?v ?from)
      (connects ?r ?from ?to)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
      (not (location-blocked ?from))
      (not (location-blocked ?to))
    )
    :effect (and
      (not (at ?v ?from))
      (at ?v ?to)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-road_0142-tw_00210
    :parameters (?v - vehicle ?r - road_type_road_0142 ?from - loc_type_loc_0087 ?to - loc_type_loc_0069 ?w - window_type_tw_00210)
    :precondition (and
      (at ?v ?from)
      (connects ?r ?from ?to)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
      (not (location-blocked ?from))
      (not (location-blocked ?to))
    )
    :effect (and
      (not (at ?v ?from))
      (at ?v ?to)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-road_0142-tw_00240
    :parameters (?v - vehicle ?r - road_type_road_0142 ?from - loc_type_loc_0087 ?to - loc_type_loc_0069 ?w - window_type_tw_00240)
    :precondition (and
      (at ?v ?from)
      (connects ?r ?from ?to)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
      (not (location-blocked ?from))
      (not (location-blocked ?to))
    )
    :effect (and
      (not (at ?v ?from))
      (at ?v ?to)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-road_0142-tw_00270
    :parameters (?v - vehicle ?r - road_type_road_0142 ?from - loc_type_loc_0087 ?to - loc_type_loc_0069 ?w - window_type_tw_00270)
    :precondition (and
      (at ?v ?from)
      (connects ?r ?from ?to)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
      (not (location-blocked ?from))
      (not (location-blocked ?to))
    )
    :effect (and
      (not (at ?v ?from))
      (at ?v ?to)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-road_0142-tw_00300
    :parameters (?v - vehicle ?r - road_type_road_0142 ?from - loc_type_loc_0087 ?to - loc_type_loc_0069 ?w - window_type_tw_00300)
    :precondition (and
      (at ?v ?from)
      (connects ?r ?from ?to)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
      (not (location-blocked ?from))
      (not (location-blocked ?to))
    )
    :effect (and
      (not (at ?v ?from))
      (at ?v ?to)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-road_0142-tw_00330
    :parameters (?v - vehicle ?r - road_type_road_0142 ?from - loc_type_loc_0087 ?to - loc_type_loc_0069 ?w - window_type_tw_00330)
    :precondition (and
      (at ?v ?from)
      (connects ?r ?from ?to)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
      (not (location-blocked ?from))
      (not (location-blocked ?to))
    )
    :effect (and
      (not (at ?v ?from))
      (at ?v ?to)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-road_0142-tw_00360
    :parameters (?v - vehicle ?r - road_type_road_0142 ?from - loc_type_loc_0087 ?to - loc_type_loc_0069 ?w - window_type_tw_00360)
    :precondition (and
      (at ?v ?from)
      (connects ?r ?from ?to)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
      (not (location-blocked ?from))
      (not (location-blocked ?to))
    )
    :effect (and
      (not (at ?v ?from))
      (at ?v ?to)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-road_0142-tw_00390
    :parameters (?v - vehicle ?r - road_type_road_0142 ?from - loc_type_loc_0087 ?to - loc_type_loc_0069 ?w - window_type_tw_00390)
    :precondition (and
      (at ?v ?from)
      (connects ?r ?from ?to)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
      (not (location-blocked ?from))
      (not (location-blocked ?to))
    )
    :effect (and
      (not (at ?v ?from))
      (at ?v ?to)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-road_0142-tw_00420
    :parameters (?v - vehicle ?r - road_type_road_0142 ?from - loc_type_loc_0087 ?to - loc_type_loc_0069 ?w - window_type_tw_00420)
    :precondition (and
      (at ?v ?from)
      (connects ?r ?from ?to)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
      (not (location-blocked ?from))
      (not (location-blocked ?to))
    )
    :effect (and
      (not (at ?v ?from))
      (at ?v ?to)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-road_0142-tw_00450
    :parameters (?v - vehicle ?r - road_type_road_0142 ?from - loc_type_loc_0087 ?to - loc_type_loc_0069 ?w - window_type_tw_00450)
    :precondition (and
      (at ?v ?from)
      (connects ?r ?from ?to)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
      (not (location-blocked ?from))
      (not (location-blocked ?to))
    )
    :effect (and
      (not (at ?v ?from))
      (at ?v ?to)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-road_0143-tw_00000
    :parameters (?v - vehicle ?r - road_type_road_0143 ?from - loc_type_loc_0088 ?to - loc_type_loc_0049 ?w - window_type_tw_00000)
    :precondition (and
      (at ?v ?from)
      (connects ?r ?from ?to)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
      (not (location-blocked ?from))
      (not (location-blocked ?to))
    )
    :effect (and
      (not (at ?v ?from))
      (at ?v ?to)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-road_0143-tw_00030
    :parameters (?v - vehicle ?r - road_type_road_0143 ?from - loc_type_loc_0088 ?to - loc_type_loc_0049 ?w - window_type_tw_00030)
    :precondition (and
      (at ?v ?from)
      (connects ?r ?from ?to)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
      (not (location-blocked ?from))
      (not (location-blocked ?to))
    )
    :effect (and
      (not (at ?v ?from))
      (at ?v ?to)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-road_0143-tw_00060
    :parameters (?v - vehicle ?r - road_type_road_0143 ?from - loc_type_loc_0088 ?to - loc_type_loc_0049 ?w - window_type_tw_00060)
    :precondition (and
      (at ?v ?from)
      (connects ?r ?from ?to)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
      (not (location-blocked ?from))
      (not (location-blocked ?to))
    )
    :effect (and
      (not (at ?v ?from))
      (at ?v ?to)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-road_0143-tw_00090
    :parameters (?v - vehicle ?r - road_type_road_0143 ?from - loc_type_loc_0088 ?to - loc_type_loc_0049 ?w - window_type_tw_00090)
    :precondition (and
      (at ?v ?from)
      (connects ?r ?from ?to)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
      (not (location-blocked ?from))
      (not (location-blocked ?to))
    )
    :effect (and
      (not (at ?v ?from))
      (at ?v ?to)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-road_0143-tw_00120
    :parameters (?v - vehicle ?r - road_type_road_0143 ?from - loc_type_loc_0088 ?to - loc_type_loc_0049 ?w - window_type_tw_00120)
    :precondition (and
      (at ?v ?from)
      (connects ?r ?from ?to)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
      (not (location-blocked ?from))
      (not (location-blocked ?to))
    )
    :effect (and
      (not (at ?v ?from))
      (at ?v ?to)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-road_0143-tw_00150
    :parameters (?v - vehicle ?r - road_type_road_0143 ?from - loc_type_loc_0088 ?to - loc_type_loc_0049 ?w - window_type_tw_00150)
    :precondition (and
      (at ?v ?from)
      (connects ?r ?from ?to)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
      (not (location-blocked ?from))
      (not (location-blocked ?to))
    )
    :effect (and
      (not (at ?v ?from))
      (at ?v ?to)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-road_0143-tw_00180
    :parameters (?v - vehicle ?r - road_type_road_0143 ?from - loc_type_loc_0088 ?to - loc_type_loc_0049 ?w - window_type_tw_00180)
    :precondition (and
      (at ?v ?from)
      (connects ?r ?from ?to)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
      (not (location-blocked ?from))
      (not (location-blocked ?to))
    )
    :effect (and
      (not (at ?v ?from))
      (at ?v ?to)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-road_0143-tw_00210
    :parameters (?v - vehicle ?r - road_type_road_0143 ?from - loc_type_loc_0088 ?to - loc_type_loc_0049 ?w - window_type_tw_00210)
    :precondition (and
      (at ?v ?from)
      (connects ?r ?from ?to)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
      (not (location-blocked ?from))
      (not (location-blocked ?to))
    )
    :effect (and
      (not (at ?v ?from))
      (at ?v ?to)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-road_0143-tw_00240
    :parameters (?v - vehicle ?r - road_type_road_0143 ?from - loc_type_loc_0088 ?to - loc_type_loc_0049 ?w - window_type_tw_00240)
    :precondition (and
      (at ?v ?from)
      (connects ?r ?from ?to)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
      (not (location-blocked ?from))
      (not (location-blocked ?to))
    )
    :effect (and
      (not (at ?v ?from))
      (at ?v ?to)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-road_0143-tw_00270
    :parameters (?v - vehicle ?r - road_type_road_0143 ?from - loc_type_loc_0088 ?to - loc_type_loc_0049 ?w - window_type_tw_00270)
    :precondition (and
      (at ?v ?from)
      (connects ?r ?from ?to)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
      (not (location-blocked ?from))
      (not (location-blocked ?to))
    )
    :effect (and
      (not (at ?v ?from))
      (at ?v ?to)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-road_0143-tw_00300
    :parameters (?v - vehicle ?r - road_type_road_0143 ?from - loc_type_loc_0088 ?to - loc_type_loc_0049 ?w - window_type_tw_00300)
    :precondition (and
      (at ?v ?from)
      (connects ?r ?from ?to)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
      (not (location-blocked ?from))
      (not (location-blocked ?to))
    )
    :effect (and
      (not (at ?v ?from))
      (at ?v ?to)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-road_0143-tw_00330
    :parameters (?v - vehicle ?r - road_type_road_0143 ?from - loc_type_loc_0088 ?to - loc_type_loc_0049 ?w - window_type_tw_00330)
    :precondition (and
      (at ?v ?from)
      (connects ?r ?from ?to)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
      (not (location-blocked ?from))
      (not (location-blocked ?to))
    )
    :effect (and
      (not (at ?v ?from))
      (at ?v ?to)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-road_0143-tw_00360
    :parameters (?v - vehicle ?r - road_type_road_0143 ?from - loc_type_loc_0088 ?to - loc_type_loc_0049 ?w - window_type_tw_00360)
    :precondition (and
      (at ?v ?from)
      (connects ?r ?from ?to)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
      (not (location-blocked ?from))
      (not (location-blocked ?to))
    )
    :effect (and
      (not (at ?v ?from))
      (at ?v ?to)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-road_0143-tw_00390
    :parameters (?v - vehicle ?r - road_type_road_0143 ?from - loc_type_loc_0088 ?to - loc_type_loc_0049 ?w - window_type_tw_00390)
    :precondition (and
      (at ?v ?from)
      (connects ?r ?from ?to)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
      (not (location-blocked ?from))
      (not (location-blocked ?to))
    )
    :effect (and
      (not (at ?v ?from))
      (at ?v ?to)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-road_0143-tw_00420
    :parameters (?v - vehicle ?r - road_type_road_0143 ?from - loc_type_loc_0088 ?to - loc_type_loc_0049 ?w - window_type_tw_00420)
    :precondition (and
      (at ?v ?from)
      (connects ?r ?from ?to)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
      (not (location-blocked ?from))
      (not (location-blocked ?to))
    )
    :effect (and
      (not (at ?v ?from))
      (at ?v ?to)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-road_0143-tw_00450
    :parameters (?v - vehicle ?r - road_type_road_0143 ?from - loc_type_loc_0088 ?to - loc_type_loc_0049 ?w - window_type_tw_00450)
    :precondition (and
      (at ?v ?from)
      (connects ?r ?from ?to)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
      (not (location-blocked ?from))
      (not (location-blocked ?to))
    )
    :effect (and
      (not (at ?v ?from))
      (at ?v ?to)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-road_0144-tw_00000
    :parameters (?v - vehicle ?r - road_type_road_0144 ?from - loc_type_loc_0089 ?to - loc_type_loc_0014 ?w - window_type_tw_00000)
    :precondition (and
      (at ?v ?from)
      (connects ?r ?from ?to)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
      (not (location-blocked ?from))
      (not (location-blocked ?to))
    )
    :effect (and
      (not (at ?v ?from))
      (at ?v ?to)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-road_0144-tw_00030
    :parameters (?v - vehicle ?r - road_type_road_0144 ?from - loc_type_loc_0089 ?to - loc_type_loc_0014 ?w - window_type_tw_00030)
    :precondition (and
      (at ?v ?from)
      (connects ?r ?from ?to)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
      (not (location-blocked ?from))
      (not (location-blocked ?to))
    )
    :effect (and
      (not (at ?v ?from))
      (at ?v ?to)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-road_0144-tw_00060
    :parameters (?v - vehicle ?r - road_type_road_0144 ?from - loc_type_loc_0089 ?to - loc_type_loc_0014 ?w - window_type_tw_00060)
    :precondition (and
      (at ?v ?from)
      (connects ?r ?from ?to)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
      (not (location-blocked ?from))
      (not (location-blocked ?to))
    )
    :effect (and
      (not (at ?v ?from))
      (at ?v ?to)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-road_0144-tw_00090
    :parameters (?v - vehicle ?r - road_type_road_0144 ?from - loc_type_loc_0089 ?to - loc_type_loc_0014 ?w - window_type_tw_00090)
    :precondition (and
      (at ?v ?from)
      (connects ?r ?from ?to)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
      (not (location-blocked ?from))
      (not (location-blocked ?to))
    )
    :effect (and
      (not (at ?v ?from))
      (at ?v ?to)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-road_0144-tw_00120
    :parameters (?v - vehicle ?r - road_type_road_0144 ?from - loc_type_loc_0089 ?to - loc_type_loc_0014 ?w - window_type_tw_00120)
    :precondition (and
      (at ?v ?from)
      (connects ?r ?from ?to)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
      (not (location-blocked ?from))
      (not (location-blocked ?to))
    )
    :effect (and
      (not (at ?v ?from))
      (at ?v ?to)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-road_0144-tw_00150
    :parameters (?v - vehicle ?r - road_type_road_0144 ?from - loc_type_loc_0089 ?to - loc_type_loc_0014 ?w - window_type_tw_00150)
    :precondition (and
      (at ?v ?from)
      (connects ?r ?from ?to)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
      (not (location-blocked ?from))
      (not (location-blocked ?to))
    )
    :effect (and
      (not (at ?v ?from))
      (at ?v ?to)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-road_0144-tw_00180
    :parameters (?v - vehicle ?r - road_type_road_0144 ?from - loc_type_loc_0089 ?to - loc_type_loc_0014 ?w - window_type_tw_00180)
    :precondition (and
      (at ?v ?from)
      (connects ?r ?from ?to)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
      (not (location-blocked ?from))
      (not (location-blocked ?to))
    )
    :effect (and
      (not (at ?v ?from))
      (at ?v ?to)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-road_0144-tw_00210
    :parameters (?v - vehicle ?r - road_type_road_0144 ?from - loc_type_loc_0089 ?to - loc_type_loc_0014 ?w - window_type_tw_00210)
    :precondition (and
      (at ?v ?from)
      (connects ?r ?from ?to)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
      (not (location-blocked ?from))
      (not (location-blocked ?to))
    )
    :effect (and
      (not (at ?v ?from))
      (at ?v ?to)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-road_0144-tw_00240
    :parameters (?v - vehicle ?r - road_type_road_0144 ?from - loc_type_loc_0089 ?to - loc_type_loc_0014 ?w - window_type_tw_00240)
    :precondition (and
      (at ?v ?from)
      (connects ?r ?from ?to)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
      (not (location-blocked ?from))
      (not (location-blocked ?to))
    )
    :effect (and
      (not (at ?v ?from))
      (at ?v ?to)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-road_0144-tw_00270
    :parameters (?v - vehicle ?r - road_type_road_0144 ?from - loc_type_loc_0089 ?to - loc_type_loc_0014 ?w - window_type_tw_00270)
    :precondition (and
      (at ?v ?from)
      (connects ?r ?from ?to)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
      (not (location-blocked ?from))
      (not (location-blocked ?to))
    )
    :effect (and
      (not (at ?v ?from))
      (at ?v ?to)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-road_0144-tw_00300
    :parameters (?v - vehicle ?r - road_type_road_0144 ?from - loc_type_loc_0089 ?to - loc_type_loc_0014 ?w - window_type_tw_00300)
    :precondition (and
      (at ?v ?from)
      (connects ?r ?from ?to)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
      (not (location-blocked ?from))
      (not (location-blocked ?to))
    )
    :effect (and
      (not (at ?v ?from))
      (at ?v ?to)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-road_0144-tw_00330
    :parameters (?v - vehicle ?r - road_type_road_0144 ?from - loc_type_loc_0089 ?to - loc_type_loc_0014 ?w - window_type_tw_00330)
    :precondition (and
      (at ?v ?from)
      (connects ?r ?from ?to)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
      (not (location-blocked ?from))
      (not (location-blocked ?to))
    )
    :effect (and
      (not (at ?v ?from))
      (at ?v ?to)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-road_0144-tw_00360
    :parameters (?v - vehicle ?r - road_type_road_0144 ?from - loc_type_loc_0089 ?to - loc_type_loc_0014 ?w - window_type_tw_00360)
    :precondition (and
      (at ?v ?from)
      (connects ?r ?from ?to)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
      (not (location-blocked ?from))
      (not (location-blocked ?to))
    )
    :effect (and
      (not (at ?v ?from))
      (at ?v ?to)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-road_0144-tw_00390
    :parameters (?v - vehicle ?r - road_type_road_0144 ?from - loc_type_loc_0089 ?to - loc_type_loc_0014 ?w - window_type_tw_00390)
    :precondition (and
      (at ?v ?from)
      (connects ?r ?from ?to)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
      (not (location-blocked ?from))
      (not (location-blocked ?to))
    )
    :effect (and
      (not (at ?v ?from))
      (at ?v ?to)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-road_0144-tw_00420
    :parameters (?v - vehicle ?r - road_type_road_0144 ?from - loc_type_loc_0089 ?to - loc_type_loc_0014 ?w - window_type_tw_00420)
    :precondition (and
      (at ?v ?from)
      (connects ?r ?from ?to)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
      (not (location-blocked ?from))
      (not (location-blocked ?to))
    )
    :effect (and
      (not (at ?v ?from))
      (at ?v ?to)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-road_0144-tw_00450
    :parameters (?v - vehicle ?r - road_type_road_0144 ?from - loc_type_loc_0089 ?to - loc_type_loc_0014 ?w - window_type_tw_00450)
    :precondition (and
      (at ?v ?from)
      (connects ?r ?from ?to)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
      (not (location-blocked ?from))
      (not (location-blocked ?to))
    )
    :effect (and
      (not (at ?v ?from))
      (at ?v ?to)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-road_0146-tw_00000
    :parameters (?v - vehicle ?r - road_type_road_0146 ?from - loc_type_loc_0091 ?to - loc_type_loc_0094 ?w - window_type_tw_00000)
    :precondition (and
      (at ?v ?from)
      (connects ?r ?from ?to)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
      (not (location-blocked ?from))
      (not (location-blocked ?to))
    )
    :effect (and
      (not (at ?v ?from))
      (at ?v ?to)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-road_0146-tw_00030
    :parameters (?v - vehicle ?r - road_type_road_0146 ?from - loc_type_loc_0091 ?to - loc_type_loc_0094 ?w - window_type_tw_00030)
    :precondition (and
      (at ?v ?from)
      (connects ?r ?from ?to)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
      (not (location-blocked ?from))
      (not (location-blocked ?to))
    )
    :effect (and
      (not (at ?v ?from))
      (at ?v ?to)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-road_0146-tw_00060
    :parameters (?v - vehicle ?r - road_type_road_0146 ?from - loc_type_loc_0091 ?to - loc_type_loc_0094 ?w - window_type_tw_00060)
    :precondition (and
      (at ?v ?from)
      (connects ?r ?from ?to)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
      (not (location-blocked ?from))
      (not (location-blocked ?to))
    )
    :effect (and
      (not (at ?v ?from))
      (at ?v ?to)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-road_0146-tw_00090
    :parameters (?v - vehicle ?r - road_type_road_0146 ?from - loc_type_loc_0091 ?to - loc_type_loc_0094 ?w - window_type_tw_00090)
    :precondition (and
      (at ?v ?from)
      (connects ?r ?from ?to)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
      (not (location-blocked ?from))
      (not (location-blocked ?to))
    )
    :effect (and
      (not (at ?v ?from))
      (at ?v ?to)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-road_0146-tw_00120
    :parameters (?v - vehicle ?r - road_type_road_0146 ?from - loc_type_loc_0091 ?to - loc_type_loc_0094 ?w - window_type_tw_00120)
    :precondition (and
      (at ?v ?from)
      (connects ?r ?from ?to)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
      (not (location-blocked ?from))
      (not (location-blocked ?to))
    )
    :effect (and
      (not (at ?v ?from))
      (at ?v ?to)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-road_0146-tw_00150
    :parameters (?v - vehicle ?r - road_type_road_0146 ?from - loc_type_loc_0091 ?to - loc_type_loc_0094 ?w - window_type_tw_00150)
    :precondition (and
      (at ?v ?from)
      (connects ?r ?from ?to)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
      (not (location-blocked ?from))
      (not (location-blocked ?to))
    )
    :effect (and
      (not (at ?v ?from))
      (at ?v ?to)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-road_0146-tw_00180
    :parameters (?v - vehicle ?r - road_type_road_0146 ?from - loc_type_loc_0091 ?to - loc_type_loc_0094 ?w - window_type_tw_00180)
    :precondition (and
      (at ?v ?from)
      (connects ?r ?from ?to)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
      (not (location-blocked ?from))
      (not (location-blocked ?to))
    )
    :effect (and
      (not (at ?v ?from))
      (at ?v ?to)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-road_0146-tw_00210
    :parameters (?v - vehicle ?r - road_type_road_0146 ?from - loc_type_loc_0091 ?to - loc_type_loc_0094 ?w - window_type_tw_00210)
    :precondition (and
      (at ?v ?from)
      (connects ?r ?from ?to)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
      (not (location-blocked ?from))
      (not (location-blocked ?to))
    )
    :effect (and
      (not (at ?v ?from))
      (at ?v ?to)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-road_0146-tw_00240
    :parameters (?v - vehicle ?r - road_type_road_0146 ?from - loc_type_loc_0091 ?to - loc_type_loc_0094 ?w - window_type_tw_00240)
    :precondition (and
      (at ?v ?from)
      (connects ?r ?from ?to)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
      (not (location-blocked ?from))
      (not (location-blocked ?to))
    )
    :effect (and
      (not (at ?v ?from))
      (at ?v ?to)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-road_0146-tw_00270
    :parameters (?v - vehicle ?r - road_type_road_0146 ?from - loc_type_loc_0091 ?to - loc_type_loc_0094 ?w - window_type_tw_00270)
    :precondition (and
      (at ?v ?from)
      (connects ?r ?from ?to)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
      (not (location-blocked ?from))
      (not (location-blocked ?to))
    )
    :effect (and
      (not (at ?v ?from))
      (at ?v ?to)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-road_0146-tw_00300
    :parameters (?v - vehicle ?r - road_type_road_0146 ?from - loc_type_loc_0091 ?to - loc_type_loc_0094 ?w - window_type_tw_00300)
    :precondition (and
      (at ?v ?from)
      (connects ?r ?from ?to)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
      (not (location-blocked ?from))
      (not (location-blocked ?to))
    )
    :effect (and
      (not (at ?v ?from))
      (at ?v ?to)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-road_0146-tw_00330
    :parameters (?v - vehicle ?r - road_type_road_0146 ?from - loc_type_loc_0091 ?to - loc_type_loc_0094 ?w - window_type_tw_00330)
    :precondition (and
      (at ?v ?from)
      (connects ?r ?from ?to)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
      (not (location-blocked ?from))
      (not (location-blocked ?to))
    )
    :effect (and
      (not (at ?v ?from))
      (at ?v ?to)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-road_0146-tw_00360
    :parameters (?v - vehicle ?r - road_type_road_0146 ?from - loc_type_loc_0091 ?to - loc_type_loc_0094 ?w - window_type_tw_00360)
    :precondition (and
      (at ?v ?from)
      (connects ?r ?from ?to)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
      (not (location-blocked ?from))
      (not (location-blocked ?to))
    )
    :effect (and
      (not (at ?v ?from))
      (at ?v ?to)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-road_0146-tw_00390
    :parameters (?v - vehicle ?r - road_type_road_0146 ?from - loc_type_loc_0091 ?to - loc_type_loc_0094 ?w - window_type_tw_00390)
    :precondition (and
      (at ?v ?from)
      (connects ?r ?from ?to)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
      (not (location-blocked ?from))
      (not (location-blocked ?to))
    )
    :effect (and
      (not (at ?v ?from))
      (at ?v ?to)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-road_0146-tw_00420
    :parameters (?v - vehicle ?r - road_type_road_0146 ?from - loc_type_loc_0091 ?to - loc_type_loc_0094 ?w - window_type_tw_00420)
    :precondition (and
      (at ?v ?from)
      (connects ?r ?from ?to)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
      (not (location-blocked ?from))
      (not (location-blocked ?to))
    )
    :effect (and
      (not (at ?v ?from))
      (at ?v ?to)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-road_0146-tw_00450
    :parameters (?v - vehicle ?r - road_type_road_0146 ?from - loc_type_loc_0091 ?to - loc_type_loc_0094 ?w - window_type_tw_00450)
    :precondition (and
      (at ?v ?from)
      (connects ?r ?from ?to)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
      (not (location-blocked ?from))
      (not (location-blocked ?to))
    )
    :effect (and
      (not (at ?v ?from))
      (at ?v ?to)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-road_0147-tw_00000
    :parameters (?v - vehicle ?r - road_type_road_0147 ?from - loc_type_loc_0091 ?to - loc_type_loc_0011 ?w - window_type_tw_00000)
    :precondition (and
      (at ?v ?from)
      (connects ?r ?from ?to)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
      (not (location-blocked ?from))
      (not (location-blocked ?to))
    )
    :effect (and
      (not (at ?v ?from))
      (at ?v ?to)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-road_0147-tw_00030
    :parameters (?v - vehicle ?r - road_type_road_0147 ?from - loc_type_loc_0091 ?to - loc_type_loc_0011 ?w - window_type_tw_00030)
    :precondition (and
      (at ?v ?from)
      (connects ?r ?from ?to)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
      (not (location-blocked ?from))
      (not (location-blocked ?to))
    )
    :effect (and
      (not (at ?v ?from))
      (at ?v ?to)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-road_0147-tw_00060
    :parameters (?v - vehicle ?r - road_type_road_0147 ?from - loc_type_loc_0091 ?to - loc_type_loc_0011 ?w - window_type_tw_00060)
    :precondition (and
      (at ?v ?from)
      (connects ?r ?from ?to)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
      (not (location-blocked ?from))
      (not (location-blocked ?to))
    )
    :effect (and
      (not (at ?v ?from))
      (at ?v ?to)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-road_0147-tw_00090
    :parameters (?v - vehicle ?r - road_type_road_0147 ?from - loc_type_loc_0091 ?to - loc_type_loc_0011 ?w - window_type_tw_00090)
    :precondition (and
      (at ?v ?from)
      (connects ?r ?from ?to)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
      (not (location-blocked ?from))
      (not (location-blocked ?to))
    )
    :effect (and
      (not (at ?v ?from))
      (at ?v ?to)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-road_0147-tw_00120
    :parameters (?v - vehicle ?r - road_type_road_0147 ?from - loc_type_loc_0091 ?to - loc_type_loc_0011 ?w - window_type_tw_00120)
    :precondition (and
      (at ?v ?from)
      (connects ?r ?from ?to)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
      (not (location-blocked ?from))
      (not (location-blocked ?to))
    )
    :effect (and
      (not (at ?v ?from))
      (at ?v ?to)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-road_0147-tw_00150
    :parameters (?v - vehicle ?r - road_type_road_0147 ?from - loc_type_loc_0091 ?to - loc_type_loc_0011 ?w - window_type_tw_00150)
    :precondition (and
      (at ?v ?from)
      (connects ?r ?from ?to)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
      (not (location-blocked ?from))
      (not (location-blocked ?to))
    )
    :effect (and
      (not (at ?v ?from))
      (at ?v ?to)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-road_0147-tw_00180
    :parameters (?v - vehicle ?r - road_type_road_0147 ?from - loc_type_loc_0091 ?to - loc_type_loc_0011 ?w - window_type_tw_00180)
    :precondition (and
      (at ?v ?from)
      (connects ?r ?from ?to)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
      (not (location-blocked ?from))
      (not (location-blocked ?to))
    )
    :effect (and
      (not (at ?v ?from))
      (at ?v ?to)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-road_0147-tw_00210
    :parameters (?v - vehicle ?r - road_type_road_0147 ?from - loc_type_loc_0091 ?to - loc_type_loc_0011 ?w - window_type_tw_00210)
    :precondition (and
      (at ?v ?from)
      (connects ?r ?from ?to)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
      (not (location-blocked ?from))
      (not (location-blocked ?to))
    )
    :effect (and
      (not (at ?v ?from))
      (at ?v ?to)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-road_0147-tw_00240
    :parameters (?v - vehicle ?r - road_type_road_0147 ?from - loc_type_loc_0091 ?to - loc_type_loc_0011 ?w - window_type_tw_00240)
    :precondition (and
      (at ?v ?from)
      (connects ?r ?from ?to)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
      (not (location-blocked ?from))
      (not (location-blocked ?to))
    )
    :effect (and
      (not (at ?v ?from))
      (at ?v ?to)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-road_0147-tw_00270
    :parameters (?v - vehicle ?r - road_type_road_0147 ?from - loc_type_loc_0091 ?to - loc_type_loc_0011 ?w - window_type_tw_00270)
    :precondition (and
      (at ?v ?from)
      (connects ?r ?from ?to)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
      (not (location-blocked ?from))
      (not (location-blocked ?to))
    )
    :effect (and
      (not (at ?v ?from))
      (at ?v ?to)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-road_0147-tw_00300
    :parameters (?v - vehicle ?r - road_type_road_0147 ?from - loc_type_loc_0091 ?to - loc_type_loc_0011 ?w - window_type_tw_00300)
    :precondition (and
      (at ?v ?from)
      (connects ?r ?from ?to)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
      (not (location-blocked ?from))
      (not (location-blocked ?to))
    )
    :effect (and
      (not (at ?v ?from))
      (at ?v ?to)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-road_0147-tw_00330
    :parameters (?v - vehicle ?r - road_type_road_0147 ?from - loc_type_loc_0091 ?to - loc_type_loc_0011 ?w - window_type_tw_00330)
    :precondition (and
      (at ?v ?from)
      (connects ?r ?from ?to)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
      (not (location-blocked ?from))
      (not (location-blocked ?to))
    )
    :effect (and
      (not (at ?v ?from))
      (at ?v ?to)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-road_0147-tw_00360
    :parameters (?v - vehicle ?r - road_type_road_0147 ?from - loc_type_loc_0091 ?to - loc_type_loc_0011 ?w - window_type_tw_00360)
    :precondition (and
      (at ?v ?from)
      (connects ?r ?from ?to)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
      (not (location-blocked ?from))
      (not (location-blocked ?to))
    )
    :effect (and
      (not (at ?v ?from))
      (at ?v ?to)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-road_0147-tw_00390
    :parameters (?v - vehicle ?r - road_type_road_0147 ?from - loc_type_loc_0091 ?to - loc_type_loc_0011 ?w - window_type_tw_00390)
    :precondition (and
      (at ?v ?from)
      (connects ?r ?from ?to)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
      (not (location-blocked ?from))
      (not (location-blocked ?to))
    )
    :effect (and
      (not (at ?v ?from))
      (at ?v ?to)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-road_0147-tw_00420
    :parameters (?v - vehicle ?r - road_type_road_0147 ?from - loc_type_loc_0091 ?to - loc_type_loc_0011 ?w - window_type_tw_00420)
    :precondition (and
      (at ?v ?from)
      (connects ?r ?from ?to)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
      (not (location-blocked ?from))
      (not (location-blocked ?to))
    )
    :effect (and
      (not (at ?v ?from))
      (at ?v ?to)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-road_0147-tw_00450
    :parameters (?v - vehicle ?r - road_type_road_0147 ?from - loc_type_loc_0091 ?to - loc_type_loc_0011 ?w - window_type_tw_00450)
    :precondition (and
      (at ?v ?from)
      (connects ?r ?from ?to)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
      (not (location-blocked ?from))
      (not (location-blocked ?to))
    )
    :effect (and
      (not (at ?v ?from))
      (at ?v ?to)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-road_0148-tw_00000
    :parameters (?v - vehicle ?r - road_type_road_0148 ?from - loc_type_loc_0092 ?to - loc_type_loc_0032 ?w - window_type_tw_00000)
    :precondition (and
      (at ?v ?from)
      (connects ?r ?from ?to)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
      (not (location-blocked ?from))
      (not (location-blocked ?to))
    )
    :effect (and
      (not (at ?v ?from))
      (at ?v ?to)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-road_0148-tw_00030
    :parameters (?v - vehicle ?r - road_type_road_0148 ?from - loc_type_loc_0092 ?to - loc_type_loc_0032 ?w - window_type_tw_00030)
    :precondition (and
      (at ?v ?from)
      (connects ?r ?from ?to)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
      (not (location-blocked ?from))
      (not (location-blocked ?to))
    )
    :effect (and
      (not (at ?v ?from))
      (at ?v ?to)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-road_0148-tw_00060
    :parameters (?v - vehicle ?r - road_type_road_0148 ?from - loc_type_loc_0092 ?to - loc_type_loc_0032 ?w - window_type_tw_00060)
    :precondition (and
      (at ?v ?from)
      (connects ?r ?from ?to)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
      (not (location-blocked ?from))
      (not (location-blocked ?to))
    )
    :effect (and
      (not (at ?v ?from))
      (at ?v ?to)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-road_0148-tw_00090
    :parameters (?v - vehicle ?r - road_type_road_0148 ?from - loc_type_loc_0092 ?to - loc_type_loc_0032 ?w - window_type_tw_00090)
    :precondition (and
      (at ?v ?from)
      (connects ?r ?from ?to)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
      (not (location-blocked ?from))
      (not (location-blocked ?to))
    )
    :effect (and
      (not (at ?v ?from))
      (at ?v ?to)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-road_0148-tw_00120
    :parameters (?v - vehicle ?r - road_type_road_0148 ?from - loc_type_loc_0092 ?to - loc_type_loc_0032 ?w - window_type_tw_00120)
    :precondition (and
      (at ?v ?from)
      (connects ?r ?from ?to)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
      (not (location-blocked ?from))
      (not (location-blocked ?to))
    )
    :effect (and
      (not (at ?v ?from))
      (at ?v ?to)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-road_0148-tw_00150
    :parameters (?v - vehicle ?r - road_type_road_0148 ?from - loc_type_loc_0092 ?to - loc_type_loc_0032 ?w - window_type_tw_00150)
    :precondition (and
      (at ?v ?from)
      (connects ?r ?from ?to)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
      (not (location-blocked ?from))
      (not (location-blocked ?to))
    )
    :effect (and
      (not (at ?v ?from))
      (at ?v ?to)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-road_0148-tw_00180
    :parameters (?v - vehicle ?r - road_type_road_0148 ?from - loc_type_loc_0092 ?to - loc_type_loc_0032 ?w - window_type_tw_00180)
    :precondition (and
      (at ?v ?from)
      (connects ?r ?from ?to)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
      (not (location-blocked ?from))
      (not (location-blocked ?to))
    )
    :effect (and
      (not (at ?v ?from))
      (at ?v ?to)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-road_0148-tw_00210
    :parameters (?v - vehicle ?r - road_type_road_0148 ?from - loc_type_loc_0092 ?to - loc_type_loc_0032 ?w - window_type_tw_00210)
    :precondition (and
      (at ?v ?from)
      (connects ?r ?from ?to)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
      (not (location-blocked ?from))
      (not (location-blocked ?to))
    )
    :effect (and
      (not (at ?v ?from))
      (at ?v ?to)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-road_0148-tw_00240
    :parameters (?v - vehicle ?r - road_type_road_0148 ?from - loc_type_loc_0092 ?to - loc_type_loc_0032 ?w - window_type_tw_00240)
    :precondition (and
      (at ?v ?from)
      (connects ?r ?from ?to)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
      (not (location-blocked ?from))
      (not (location-blocked ?to))
    )
    :effect (and
      (not (at ?v ?from))
      (at ?v ?to)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-road_0148-tw_00270
    :parameters (?v - vehicle ?r - road_type_road_0148 ?from - loc_type_loc_0092 ?to - loc_type_loc_0032 ?w - window_type_tw_00270)
    :precondition (and
      (at ?v ?from)
      (connects ?r ?from ?to)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
      (not (location-blocked ?from))
      (not (location-blocked ?to))
    )
    :effect (and
      (not (at ?v ?from))
      (at ?v ?to)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-road_0148-tw_00300
    :parameters (?v - vehicle ?r - road_type_road_0148 ?from - loc_type_loc_0092 ?to - loc_type_loc_0032 ?w - window_type_tw_00300)
    :precondition (and
      (at ?v ?from)
      (connects ?r ?from ?to)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
      (not (location-blocked ?from))
      (not (location-blocked ?to))
    )
    :effect (and
      (not (at ?v ?from))
      (at ?v ?to)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-road_0148-tw_00330
    :parameters (?v - vehicle ?r - road_type_road_0148 ?from - loc_type_loc_0092 ?to - loc_type_loc_0032 ?w - window_type_tw_00330)
    :precondition (and
      (at ?v ?from)
      (connects ?r ?from ?to)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
      (not (location-blocked ?from))
      (not (location-blocked ?to))
    )
    :effect (and
      (not (at ?v ?from))
      (at ?v ?to)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-road_0148-tw_00360
    :parameters (?v - vehicle ?r - road_type_road_0148 ?from - loc_type_loc_0092 ?to - loc_type_loc_0032 ?w - window_type_tw_00360)
    :precondition (and
      (at ?v ?from)
      (connects ?r ?from ?to)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
      (not (location-blocked ?from))
      (not (location-blocked ?to))
    )
    :effect (and
      (not (at ?v ?from))
      (at ?v ?to)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-road_0148-tw_00390
    :parameters (?v - vehicle ?r - road_type_road_0148 ?from - loc_type_loc_0092 ?to - loc_type_loc_0032 ?w - window_type_tw_00390)
    :precondition (and
      (at ?v ?from)
      (connects ?r ?from ?to)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
      (not (location-blocked ?from))
      (not (location-blocked ?to))
    )
    :effect (and
      (not (at ?v ?from))
      (at ?v ?to)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-road_0148-tw_00420
    :parameters (?v - vehicle ?r - road_type_road_0148 ?from - loc_type_loc_0092 ?to - loc_type_loc_0032 ?w - window_type_tw_00420)
    :precondition (and
      (at ?v ?from)
      (connects ?r ?from ?to)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
      (not (location-blocked ?from))
      (not (location-blocked ?to))
    )
    :effect (and
      (not (at ?v ?from))
      (at ?v ?to)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-road_0148-tw_00450
    :parameters (?v - vehicle ?r - road_type_road_0148 ?from - loc_type_loc_0092 ?to - loc_type_loc_0032 ?w - window_type_tw_00450)
    :precondition (and
      (at ?v ?from)
      (connects ?r ?from ?to)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
      (not (location-blocked ?from))
      (not (location-blocked ?to))
    )
    :effect (and
      (not (at ?v ?from))
      (at ?v ?to)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-road_0149-tw_00000
    :parameters (?v - vehicle ?r - road_type_road_0149 ?from - loc_type_loc_0092 ?to - loc_type_loc_0007 ?w - window_type_tw_00000)
    :precondition (and
      (at ?v ?from)
      (connects ?r ?from ?to)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
      (not (location-blocked ?from))
      (not (location-blocked ?to))
    )
    :effect (and
      (not (at ?v ?from))
      (at ?v ?to)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-road_0149-tw_00030
    :parameters (?v - vehicle ?r - road_type_road_0149 ?from - loc_type_loc_0092 ?to - loc_type_loc_0007 ?w - window_type_tw_00030)
    :precondition (and
      (at ?v ?from)
      (connects ?r ?from ?to)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
      (not (location-blocked ?from))
      (not (location-blocked ?to))
    )
    :effect (and
      (not (at ?v ?from))
      (at ?v ?to)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-road_0149-tw_00060
    :parameters (?v - vehicle ?r - road_type_road_0149 ?from - loc_type_loc_0092 ?to - loc_type_loc_0007 ?w - window_type_tw_00060)
    :precondition (and
      (at ?v ?from)
      (connects ?r ?from ?to)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
      (not (location-blocked ?from))
      (not (location-blocked ?to))
    )
    :effect (and
      (not (at ?v ?from))
      (at ?v ?to)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-road_0149-tw_00090
    :parameters (?v - vehicle ?r - road_type_road_0149 ?from - loc_type_loc_0092 ?to - loc_type_loc_0007 ?w - window_type_tw_00090)
    :precondition (and
      (at ?v ?from)
      (connects ?r ?from ?to)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
      (not (location-blocked ?from))
      (not (location-blocked ?to))
    )
    :effect (and
      (not (at ?v ?from))
      (at ?v ?to)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-road_0149-tw_00120
    :parameters (?v - vehicle ?r - road_type_road_0149 ?from - loc_type_loc_0092 ?to - loc_type_loc_0007 ?w - window_type_tw_00120)
    :precondition (and
      (at ?v ?from)
      (connects ?r ?from ?to)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
      (not (location-blocked ?from))
      (not (location-blocked ?to))
    )
    :effect (and
      (not (at ?v ?from))
      (at ?v ?to)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-road_0149-tw_00150
    :parameters (?v - vehicle ?r - road_type_road_0149 ?from - loc_type_loc_0092 ?to - loc_type_loc_0007 ?w - window_type_tw_00150)
    :precondition (and
      (at ?v ?from)
      (connects ?r ?from ?to)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
      (not (location-blocked ?from))
      (not (location-blocked ?to))
    )
    :effect (and
      (not (at ?v ?from))
      (at ?v ?to)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-road_0149-tw_00180
    :parameters (?v - vehicle ?r - road_type_road_0149 ?from - loc_type_loc_0092 ?to - loc_type_loc_0007 ?w - window_type_tw_00180)
    :precondition (and
      (at ?v ?from)
      (connects ?r ?from ?to)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
      (not (location-blocked ?from))
      (not (location-blocked ?to))
    )
    :effect (and
      (not (at ?v ?from))
      (at ?v ?to)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-road_0149-tw_00210
    :parameters (?v - vehicle ?r - road_type_road_0149 ?from - loc_type_loc_0092 ?to - loc_type_loc_0007 ?w - window_type_tw_00210)
    :precondition (and
      (at ?v ?from)
      (connects ?r ?from ?to)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
      (not (location-blocked ?from))
      (not (location-blocked ?to))
    )
    :effect (and
      (not (at ?v ?from))
      (at ?v ?to)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-road_0149-tw_00240
    :parameters (?v - vehicle ?r - road_type_road_0149 ?from - loc_type_loc_0092 ?to - loc_type_loc_0007 ?w - window_type_tw_00240)
    :precondition (and
      (at ?v ?from)
      (connects ?r ?from ?to)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
      (not (location-blocked ?from))
      (not (location-blocked ?to))
    )
    :effect (and
      (not (at ?v ?from))
      (at ?v ?to)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-road_0149-tw_00270
    :parameters (?v - vehicle ?r - road_type_road_0149 ?from - loc_type_loc_0092 ?to - loc_type_loc_0007 ?w - window_type_tw_00270)
    :precondition (and
      (at ?v ?from)
      (connects ?r ?from ?to)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
      (not (location-blocked ?from))
      (not (location-blocked ?to))
    )
    :effect (and
      (not (at ?v ?from))
      (at ?v ?to)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-road_0149-tw_00300
    :parameters (?v - vehicle ?r - road_type_road_0149 ?from - loc_type_loc_0092 ?to - loc_type_loc_0007 ?w - window_type_tw_00300)
    :precondition (and
      (at ?v ?from)
      (connects ?r ?from ?to)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
      (not (location-blocked ?from))
      (not (location-blocked ?to))
    )
    :effect (and
      (not (at ?v ?from))
      (at ?v ?to)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-road_0149-tw_00330
    :parameters (?v - vehicle ?r - road_type_road_0149 ?from - loc_type_loc_0092 ?to - loc_type_loc_0007 ?w - window_type_tw_00330)
    :precondition (and
      (at ?v ?from)
      (connects ?r ?from ?to)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
      (not (location-blocked ?from))
      (not (location-blocked ?to))
    )
    :effect (and
      (not (at ?v ?from))
      (at ?v ?to)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-road_0149-tw_00360
    :parameters (?v - vehicle ?r - road_type_road_0149 ?from - loc_type_loc_0092 ?to - loc_type_loc_0007 ?w - window_type_tw_00360)
    :precondition (and
      (at ?v ?from)
      (connects ?r ?from ?to)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
      (not (location-blocked ?from))
      (not (location-blocked ?to))
    )
    :effect (and
      (not (at ?v ?from))
      (at ?v ?to)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-road_0149-tw_00390
    :parameters (?v - vehicle ?r - road_type_road_0149 ?from - loc_type_loc_0092 ?to - loc_type_loc_0007 ?w - window_type_tw_00390)
    :precondition (and
      (at ?v ?from)
      (connects ?r ?from ?to)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
      (not (location-blocked ?from))
      (not (location-blocked ?to))
    )
    :effect (and
      (not (at ?v ?from))
      (at ?v ?to)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-road_0149-tw_00420
    :parameters (?v - vehicle ?r - road_type_road_0149 ?from - loc_type_loc_0092 ?to - loc_type_loc_0007 ?w - window_type_tw_00420)
    :precondition (and
      (at ?v ?from)
      (connects ?r ?from ?to)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
      (not (location-blocked ?from))
      (not (location-blocked ?to))
    )
    :effect (and
      (not (at ?v ?from))
      (at ?v ?to)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-road_0149-tw_00450
    :parameters (?v - vehicle ?r - road_type_road_0149 ?from - loc_type_loc_0092 ?to - loc_type_loc_0007 ?w - window_type_tw_00450)
    :precondition (and
      (at ?v ?from)
      (connects ?r ?from ?to)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
      (not (location-blocked ?from))
      (not (location-blocked ?to))
    )
    :effect (and
      (not (at ?v ?from))
      (at ?v ?to)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-road_0150-tw_00000
    :parameters (?v - vehicle ?r - road_type_road_0150 ?from - loc_type_loc_0092 ?to - loc_type_loc_0016 ?w - window_type_tw_00000)
    :precondition (and
      (at ?v ?from)
      (connects ?r ?from ?to)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
      (not (location-blocked ?from))
      (not (location-blocked ?to))
    )
    :effect (and
      (not (at ?v ?from))
      (at ?v ?to)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-road_0150-tw_00030
    :parameters (?v - vehicle ?r - road_type_road_0150 ?from - loc_type_loc_0092 ?to - loc_type_loc_0016 ?w - window_type_tw_00030)
    :precondition (and
      (at ?v ?from)
      (connects ?r ?from ?to)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
      (not (location-blocked ?from))
      (not (location-blocked ?to))
    )
    :effect (and
      (not (at ?v ?from))
      (at ?v ?to)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-road_0150-tw_00060
    :parameters (?v - vehicle ?r - road_type_road_0150 ?from - loc_type_loc_0092 ?to - loc_type_loc_0016 ?w - window_type_tw_00060)
    :precondition (and
      (at ?v ?from)
      (connects ?r ?from ?to)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
      (not (location-blocked ?from))
      (not (location-blocked ?to))
    )
    :effect (and
      (not (at ?v ?from))
      (at ?v ?to)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-road_0150-tw_00090
    :parameters (?v - vehicle ?r - road_type_road_0150 ?from - loc_type_loc_0092 ?to - loc_type_loc_0016 ?w - window_type_tw_00090)
    :precondition (and
      (at ?v ?from)
      (connects ?r ?from ?to)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
      (not (location-blocked ?from))
      (not (location-blocked ?to))
    )
    :effect (and
      (not (at ?v ?from))
      (at ?v ?to)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-road_0150-tw_00120
    :parameters (?v - vehicle ?r - road_type_road_0150 ?from - loc_type_loc_0092 ?to - loc_type_loc_0016 ?w - window_type_tw_00120)
    :precondition (and
      (at ?v ?from)
      (connects ?r ?from ?to)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
      (not (location-blocked ?from))
      (not (location-blocked ?to))
    )
    :effect (and
      (not (at ?v ?from))
      (at ?v ?to)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-road_0150-tw_00150
    :parameters (?v - vehicle ?r - road_type_road_0150 ?from - loc_type_loc_0092 ?to - loc_type_loc_0016 ?w - window_type_tw_00150)
    :precondition (and
      (at ?v ?from)
      (connects ?r ?from ?to)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
      (not (location-blocked ?from))
      (not (location-blocked ?to))
    )
    :effect (and
      (not (at ?v ?from))
      (at ?v ?to)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-road_0150-tw_00180
    :parameters (?v - vehicle ?r - road_type_road_0150 ?from - loc_type_loc_0092 ?to - loc_type_loc_0016 ?w - window_type_tw_00180)
    :precondition (and
      (at ?v ?from)
      (connects ?r ?from ?to)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
      (not (location-blocked ?from))
      (not (location-blocked ?to))
    )
    :effect (and
      (not (at ?v ?from))
      (at ?v ?to)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-road_0150-tw_00210
    :parameters (?v - vehicle ?r - road_type_road_0150 ?from - loc_type_loc_0092 ?to - loc_type_loc_0016 ?w - window_type_tw_00210)
    :precondition (and
      (at ?v ?from)
      (connects ?r ?from ?to)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
      (not (location-blocked ?from))
      (not (location-blocked ?to))
    )
    :effect (and
      (not (at ?v ?from))
      (at ?v ?to)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-road_0150-tw_00240
    :parameters (?v - vehicle ?r - road_type_road_0150 ?from - loc_type_loc_0092 ?to - loc_type_loc_0016 ?w - window_type_tw_00240)
    :precondition (and
      (at ?v ?from)
      (connects ?r ?from ?to)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
      (not (location-blocked ?from))
      (not (location-blocked ?to))
    )
    :effect (and
      (not (at ?v ?from))
      (at ?v ?to)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-road_0150-tw_00270
    :parameters (?v - vehicle ?r - road_type_road_0150 ?from - loc_type_loc_0092 ?to - loc_type_loc_0016 ?w - window_type_tw_00270)
    :precondition (and
      (at ?v ?from)
      (connects ?r ?from ?to)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
      (not (location-blocked ?from))
      (not (location-blocked ?to))
    )
    :effect (and
      (not (at ?v ?from))
      (at ?v ?to)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-road_0150-tw_00300
    :parameters (?v - vehicle ?r - road_type_road_0150 ?from - loc_type_loc_0092 ?to - loc_type_loc_0016 ?w - window_type_tw_00300)
    :precondition (and
      (at ?v ?from)
      (connects ?r ?from ?to)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
      (not (location-blocked ?from))
      (not (location-blocked ?to))
    )
    :effect (and
      (not (at ?v ?from))
      (at ?v ?to)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-road_0150-tw_00330
    :parameters (?v - vehicle ?r - road_type_road_0150 ?from - loc_type_loc_0092 ?to - loc_type_loc_0016 ?w - window_type_tw_00330)
    :precondition (and
      (at ?v ?from)
      (connects ?r ?from ?to)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
      (not (location-blocked ?from))
      (not (location-blocked ?to))
    )
    :effect (and
      (not (at ?v ?from))
      (at ?v ?to)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-road_0150-tw_00360
    :parameters (?v - vehicle ?r - road_type_road_0150 ?from - loc_type_loc_0092 ?to - loc_type_loc_0016 ?w - window_type_tw_00360)
    :precondition (and
      (at ?v ?from)
      (connects ?r ?from ?to)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
      (not (location-blocked ?from))
      (not (location-blocked ?to))
    )
    :effect (and
      (not (at ?v ?from))
      (at ?v ?to)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-road_0150-tw_00390
    :parameters (?v - vehicle ?r - road_type_road_0150 ?from - loc_type_loc_0092 ?to - loc_type_loc_0016 ?w - window_type_tw_00390)
    :precondition (and
      (at ?v ?from)
      (connects ?r ?from ?to)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
      (not (location-blocked ?from))
      (not (location-blocked ?to))
    )
    :effect (and
      (not (at ?v ?from))
      (at ?v ?to)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-road_0150-tw_00420
    :parameters (?v - vehicle ?r - road_type_road_0150 ?from - loc_type_loc_0092 ?to - loc_type_loc_0016 ?w - window_type_tw_00420)
    :precondition (and
      (at ?v ?from)
      (connects ?r ?from ?to)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
      (not (location-blocked ?from))
      (not (location-blocked ?to))
    )
    :effect (and
      (not (at ?v ?from))
      (at ?v ?to)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-road_0150-tw_00450
    :parameters (?v - vehicle ?r - road_type_road_0150 ?from - loc_type_loc_0092 ?to - loc_type_loc_0016 ?w - window_type_tw_00450)
    :precondition (and
      (at ?v ?from)
      (connects ?r ?from ?to)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
      (not (location-blocked ?from))
      (not (location-blocked ?to))
    )
    :effect (and
      (not (at ?v ?from))
      (at ?v ?to)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-road_0151-tw_00000
    :parameters (?v - vehicle ?r - road_type_road_0151 ?from - loc_type_loc_0093 ?to - loc_type_loc_0077 ?w - window_type_tw_00000)
    :precondition (and
      (at ?v ?from)
      (connects ?r ?from ?to)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
      (not (location-blocked ?from))
      (not (location-blocked ?to))
    )
    :effect (and
      (not (at ?v ?from))
      (at ?v ?to)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-road_0151-tw_00030
    :parameters (?v - vehicle ?r - road_type_road_0151 ?from - loc_type_loc_0093 ?to - loc_type_loc_0077 ?w - window_type_tw_00030)
    :precondition (and
      (at ?v ?from)
      (connects ?r ?from ?to)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
      (not (location-blocked ?from))
      (not (location-blocked ?to))
    )
    :effect (and
      (not (at ?v ?from))
      (at ?v ?to)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-road_0151-tw_00060
    :parameters (?v - vehicle ?r - road_type_road_0151 ?from - loc_type_loc_0093 ?to - loc_type_loc_0077 ?w - window_type_tw_00060)
    :precondition (and
      (at ?v ?from)
      (connects ?r ?from ?to)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
      (not (location-blocked ?from))
      (not (location-blocked ?to))
    )
    :effect (and
      (not (at ?v ?from))
      (at ?v ?to)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-road_0151-tw_00090
    :parameters (?v - vehicle ?r - road_type_road_0151 ?from - loc_type_loc_0093 ?to - loc_type_loc_0077 ?w - window_type_tw_00090)
    :precondition (and
      (at ?v ?from)
      (connects ?r ?from ?to)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
      (not (location-blocked ?from))
      (not (location-blocked ?to))
    )
    :effect (and
      (not (at ?v ?from))
      (at ?v ?to)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-road_0151-tw_00120
    :parameters (?v - vehicle ?r - road_type_road_0151 ?from - loc_type_loc_0093 ?to - loc_type_loc_0077 ?w - window_type_tw_00120)
    :precondition (and
      (at ?v ?from)
      (connects ?r ?from ?to)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
      (not (location-blocked ?from))
      (not (location-blocked ?to))
    )
    :effect (and
      (not (at ?v ?from))
      (at ?v ?to)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-road_0151-tw_00150
    :parameters (?v - vehicle ?r - road_type_road_0151 ?from - loc_type_loc_0093 ?to - loc_type_loc_0077 ?w - window_type_tw_00150)
    :precondition (and
      (at ?v ?from)
      (connects ?r ?from ?to)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
      (not (location-blocked ?from))
      (not (location-blocked ?to))
    )
    :effect (and
      (not (at ?v ?from))
      (at ?v ?to)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-road_0151-tw_00180
    :parameters (?v - vehicle ?r - road_type_road_0151 ?from - loc_type_loc_0093 ?to - loc_type_loc_0077 ?w - window_type_tw_00180)
    :precondition (and
      (at ?v ?from)
      (connects ?r ?from ?to)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
      (not (location-blocked ?from))
      (not (location-blocked ?to))
    )
    :effect (and
      (not (at ?v ?from))
      (at ?v ?to)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-road_0151-tw_00210
    :parameters (?v - vehicle ?r - road_type_road_0151 ?from - loc_type_loc_0093 ?to - loc_type_loc_0077 ?w - window_type_tw_00210)
    :precondition (and
      (at ?v ?from)
      (connects ?r ?from ?to)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
      (not (location-blocked ?from))
      (not (location-blocked ?to))
    )
    :effect (and
      (not (at ?v ?from))
      (at ?v ?to)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-road_0151-tw_00240
    :parameters (?v - vehicle ?r - road_type_road_0151 ?from - loc_type_loc_0093 ?to - loc_type_loc_0077 ?w - window_type_tw_00240)
    :precondition (and
      (at ?v ?from)
      (connects ?r ?from ?to)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
      (not (location-blocked ?from))
      (not (location-blocked ?to))
    )
    :effect (and
      (not (at ?v ?from))
      (at ?v ?to)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-road_0151-tw_00270
    :parameters (?v - vehicle ?r - road_type_road_0151 ?from - loc_type_loc_0093 ?to - loc_type_loc_0077 ?w - window_type_tw_00270)
    :precondition (and
      (at ?v ?from)
      (connects ?r ?from ?to)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
      (not (location-blocked ?from))
      (not (location-blocked ?to))
    )
    :effect (and
      (not (at ?v ?from))
      (at ?v ?to)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-road_0151-tw_00300
    :parameters (?v - vehicle ?r - road_type_road_0151 ?from - loc_type_loc_0093 ?to - loc_type_loc_0077 ?w - window_type_tw_00300)
    :precondition (and
      (at ?v ?from)
      (connects ?r ?from ?to)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
      (not (location-blocked ?from))
      (not (location-blocked ?to))
    )
    :effect (and
      (not (at ?v ?from))
      (at ?v ?to)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-road_0151-tw_00330
    :parameters (?v - vehicle ?r - road_type_road_0151 ?from - loc_type_loc_0093 ?to - loc_type_loc_0077 ?w - window_type_tw_00330)
    :precondition (and
      (at ?v ?from)
      (connects ?r ?from ?to)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
      (not (location-blocked ?from))
      (not (location-blocked ?to))
    )
    :effect (and
      (not (at ?v ?from))
      (at ?v ?to)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-road_0151-tw_00360
    :parameters (?v - vehicle ?r - road_type_road_0151 ?from - loc_type_loc_0093 ?to - loc_type_loc_0077 ?w - window_type_tw_00360)
    :precondition (and
      (at ?v ?from)
      (connects ?r ?from ?to)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
      (not (location-blocked ?from))
      (not (location-blocked ?to))
    )
    :effect (and
      (not (at ?v ?from))
      (at ?v ?to)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-road_0151-tw_00390
    :parameters (?v - vehicle ?r - road_type_road_0151 ?from - loc_type_loc_0093 ?to - loc_type_loc_0077 ?w - window_type_tw_00390)
    :precondition (and
      (at ?v ?from)
      (connects ?r ?from ?to)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
      (not (location-blocked ?from))
      (not (location-blocked ?to))
    )
    :effect (and
      (not (at ?v ?from))
      (at ?v ?to)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-road_0151-tw_00420
    :parameters (?v - vehicle ?r - road_type_road_0151 ?from - loc_type_loc_0093 ?to - loc_type_loc_0077 ?w - window_type_tw_00420)
    :precondition (and
      (at ?v ?from)
      (connects ?r ?from ?to)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
      (not (location-blocked ?from))
      (not (location-blocked ?to))
    )
    :effect (and
      (not (at ?v ?from))
      (at ?v ?to)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-road_0151-tw_00450
    :parameters (?v - vehicle ?r - road_type_road_0151 ?from - loc_type_loc_0093 ?to - loc_type_loc_0077 ?w - window_type_tw_00450)
    :precondition (and
      (at ?v ?from)
      (connects ?r ?from ?to)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
      (not (location-blocked ?from))
      (not (location-blocked ?to))
    )
    :effect (and
      (not (at ?v ?from))
      (at ?v ?to)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-road_0152-tw_00000
    :parameters (?v - vehicle ?r - road_type_road_0152 ?from - loc_type_loc_0093 ?to - loc_type_loc_0027 ?w - window_type_tw_00000)
    :precondition (and
      (at ?v ?from)
      (connects ?r ?from ?to)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
      (not (location-blocked ?from))
      (not (location-blocked ?to))
    )
    :effect (and
      (not (at ?v ?from))
      (at ?v ?to)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-road_0152-tw_00030
    :parameters (?v - vehicle ?r - road_type_road_0152 ?from - loc_type_loc_0093 ?to - loc_type_loc_0027 ?w - window_type_tw_00030)
    :precondition (and
      (at ?v ?from)
      (connects ?r ?from ?to)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
      (not (location-blocked ?from))
      (not (location-blocked ?to))
    )
    :effect (and
      (not (at ?v ?from))
      (at ?v ?to)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-road_0152-tw_00060
    :parameters (?v - vehicle ?r - road_type_road_0152 ?from - loc_type_loc_0093 ?to - loc_type_loc_0027 ?w - window_type_tw_00060)
    :precondition (and
      (at ?v ?from)
      (connects ?r ?from ?to)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
      (not (location-blocked ?from))
      (not (location-blocked ?to))
    )
    :effect (and
      (not (at ?v ?from))
      (at ?v ?to)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-road_0152-tw_00090
    :parameters (?v - vehicle ?r - road_type_road_0152 ?from - loc_type_loc_0093 ?to - loc_type_loc_0027 ?w - window_type_tw_00090)
    :precondition (and
      (at ?v ?from)
      (connects ?r ?from ?to)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
      (not (location-blocked ?from))
      (not (location-blocked ?to))
    )
    :effect (and
      (not (at ?v ?from))
      (at ?v ?to)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-road_0152-tw_00120
    :parameters (?v - vehicle ?r - road_type_road_0152 ?from - loc_type_loc_0093 ?to - loc_type_loc_0027 ?w - window_type_tw_00120)
    :precondition (and
      (at ?v ?from)
      (connects ?r ?from ?to)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
      (not (location-blocked ?from))
      (not (location-blocked ?to))
    )
    :effect (and
      (not (at ?v ?from))
      (at ?v ?to)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-road_0152-tw_00150
    :parameters (?v - vehicle ?r - road_type_road_0152 ?from - loc_type_loc_0093 ?to - loc_type_loc_0027 ?w - window_type_tw_00150)
    :precondition (and
      (at ?v ?from)
      (connects ?r ?from ?to)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
      (not (location-blocked ?from))
      (not (location-blocked ?to))
    )
    :effect (and
      (not (at ?v ?from))
      (at ?v ?to)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-road_0152-tw_00180
    :parameters (?v - vehicle ?r - road_type_road_0152 ?from - loc_type_loc_0093 ?to - loc_type_loc_0027 ?w - window_type_tw_00180)
    :precondition (and
      (at ?v ?from)
      (connects ?r ?from ?to)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
      (not (location-blocked ?from))
      (not (location-blocked ?to))
    )
    :effect (and
      (not (at ?v ?from))
      (at ?v ?to)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-road_0152-tw_00210
    :parameters (?v - vehicle ?r - road_type_road_0152 ?from - loc_type_loc_0093 ?to - loc_type_loc_0027 ?w - window_type_tw_00210)
    :precondition (and
      (at ?v ?from)
      (connects ?r ?from ?to)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
      (not (location-blocked ?from))
      (not (location-blocked ?to))
    )
    :effect (and
      (not (at ?v ?from))
      (at ?v ?to)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-road_0152-tw_00240
    :parameters (?v - vehicle ?r - road_type_road_0152 ?from - loc_type_loc_0093 ?to - loc_type_loc_0027 ?w - window_type_tw_00240)
    :precondition (and
      (at ?v ?from)
      (connects ?r ?from ?to)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
      (not (location-blocked ?from))
      (not (location-blocked ?to))
    )
    :effect (and
      (not (at ?v ?from))
      (at ?v ?to)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-road_0152-tw_00270
    :parameters (?v - vehicle ?r - road_type_road_0152 ?from - loc_type_loc_0093 ?to - loc_type_loc_0027 ?w - window_type_tw_00270)
    :precondition (and
      (at ?v ?from)
      (connects ?r ?from ?to)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
      (not (location-blocked ?from))
      (not (location-blocked ?to))
    )
    :effect (and
      (not (at ?v ?from))
      (at ?v ?to)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-road_0152-tw_00300
    :parameters (?v - vehicle ?r - road_type_road_0152 ?from - loc_type_loc_0093 ?to - loc_type_loc_0027 ?w - window_type_tw_00300)
    :precondition (and
      (at ?v ?from)
      (connects ?r ?from ?to)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
      (not (location-blocked ?from))
      (not (location-blocked ?to))
    )
    :effect (and
      (not (at ?v ?from))
      (at ?v ?to)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-road_0152-tw_00330
    :parameters (?v - vehicle ?r - road_type_road_0152 ?from - loc_type_loc_0093 ?to - loc_type_loc_0027 ?w - window_type_tw_00330)
    :precondition (and
      (at ?v ?from)
      (connects ?r ?from ?to)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
      (not (location-blocked ?from))
      (not (location-blocked ?to))
    )
    :effect (and
      (not (at ?v ?from))
      (at ?v ?to)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-road_0152-tw_00360
    :parameters (?v - vehicle ?r - road_type_road_0152 ?from - loc_type_loc_0093 ?to - loc_type_loc_0027 ?w - window_type_tw_00360)
    :precondition (and
      (at ?v ?from)
      (connects ?r ?from ?to)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
      (not (location-blocked ?from))
      (not (location-blocked ?to))
    )
    :effect (and
      (not (at ?v ?from))
      (at ?v ?to)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-road_0152-tw_00390
    :parameters (?v - vehicle ?r - road_type_road_0152 ?from - loc_type_loc_0093 ?to - loc_type_loc_0027 ?w - window_type_tw_00390)
    :precondition (and
      (at ?v ?from)
      (connects ?r ?from ?to)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
      (not (location-blocked ?from))
      (not (location-blocked ?to))
    )
    :effect (and
      (not (at ?v ?from))
      (at ?v ?to)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-road_0152-tw_00420
    :parameters (?v - vehicle ?r - road_type_road_0152 ?from - loc_type_loc_0093 ?to - loc_type_loc_0027 ?w - window_type_tw_00420)
    :precondition (and
      (at ?v ?from)
      (connects ?r ?from ?to)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
      (not (location-blocked ?from))
      (not (location-blocked ?to))
    )
    :effect (and
      (not (at ?v ?from))
      (at ?v ?to)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-road_0152-tw_00450
    :parameters (?v - vehicle ?r - road_type_road_0152 ?from - loc_type_loc_0093 ?to - loc_type_loc_0027 ?w - window_type_tw_00450)
    :precondition (and
      (at ?v ?from)
      (connects ?r ?from ?to)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
      (not (location-blocked ?from))
      (not (location-blocked ?to))
    )
    :effect (and
      (not (at ?v ?from))
      (at ?v ?to)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-road_0154-tw_00000
    :parameters (?v - vehicle ?r - road_type_road_0154 ?from - loc_type_loc_0094 ?to - loc_type_loc_0064 ?w - window_type_tw_00000)
    :precondition (and
      (at ?v ?from)
      (connects ?r ?from ?to)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
      (not (location-blocked ?from))
      (not (location-blocked ?to))
    )
    :effect (and
      (not (at ?v ?from))
      (at ?v ?to)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-road_0154-tw_00030
    :parameters (?v - vehicle ?r - road_type_road_0154 ?from - loc_type_loc_0094 ?to - loc_type_loc_0064 ?w - window_type_tw_00030)
    :precondition (and
      (at ?v ?from)
      (connects ?r ?from ?to)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
      (not (location-blocked ?from))
      (not (location-blocked ?to))
    )
    :effect (and
      (not (at ?v ?from))
      (at ?v ?to)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-road_0154-tw_00060
    :parameters (?v - vehicle ?r - road_type_road_0154 ?from - loc_type_loc_0094 ?to - loc_type_loc_0064 ?w - window_type_tw_00060)
    :precondition (and
      (at ?v ?from)
      (connects ?r ?from ?to)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
      (not (location-blocked ?from))
      (not (location-blocked ?to))
    )
    :effect (and
      (not (at ?v ?from))
      (at ?v ?to)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-road_0154-tw_00090
    :parameters (?v - vehicle ?r - road_type_road_0154 ?from - loc_type_loc_0094 ?to - loc_type_loc_0064 ?w - window_type_tw_00090)
    :precondition (and
      (at ?v ?from)
      (connects ?r ?from ?to)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
      (not (location-blocked ?from))
      (not (location-blocked ?to))
    )
    :effect (and
      (not (at ?v ?from))
      (at ?v ?to)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-road_0154-tw_00120
    :parameters (?v - vehicle ?r - road_type_road_0154 ?from - loc_type_loc_0094 ?to - loc_type_loc_0064 ?w - window_type_tw_00120)
    :precondition (and
      (at ?v ?from)
      (connects ?r ?from ?to)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
      (not (location-blocked ?from))
      (not (location-blocked ?to))
    )
    :effect (and
      (not (at ?v ?from))
      (at ?v ?to)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-road_0154-tw_00150
    :parameters (?v - vehicle ?r - road_type_road_0154 ?from - loc_type_loc_0094 ?to - loc_type_loc_0064 ?w - window_type_tw_00150)
    :precondition (and
      (at ?v ?from)
      (connects ?r ?from ?to)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
      (not (location-blocked ?from))
      (not (location-blocked ?to))
    )
    :effect (and
      (not (at ?v ?from))
      (at ?v ?to)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-road_0154-tw_00180
    :parameters (?v - vehicle ?r - road_type_road_0154 ?from - loc_type_loc_0094 ?to - loc_type_loc_0064 ?w - window_type_tw_00180)
    :precondition (and
      (at ?v ?from)
      (connects ?r ?from ?to)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
      (not (location-blocked ?from))
      (not (location-blocked ?to))
    )
    :effect (and
      (not (at ?v ?from))
      (at ?v ?to)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-road_0154-tw_00210
    :parameters (?v - vehicle ?r - road_type_road_0154 ?from - loc_type_loc_0094 ?to - loc_type_loc_0064 ?w - window_type_tw_00210)
    :precondition (and
      (at ?v ?from)
      (connects ?r ?from ?to)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
      (not (location-blocked ?from))
      (not (location-blocked ?to))
    )
    :effect (and
      (not (at ?v ?from))
      (at ?v ?to)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-road_0154-tw_00240
    :parameters (?v - vehicle ?r - road_type_road_0154 ?from - loc_type_loc_0094 ?to - loc_type_loc_0064 ?w - window_type_tw_00240)
    :precondition (and
      (at ?v ?from)
      (connects ?r ?from ?to)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
      (not (location-blocked ?from))
      (not (location-blocked ?to))
    )
    :effect (and
      (not (at ?v ?from))
      (at ?v ?to)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-road_0154-tw_00270
    :parameters (?v - vehicle ?r - road_type_road_0154 ?from - loc_type_loc_0094 ?to - loc_type_loc_0064 ?w - window_type_tw_00270)
    :precondition (and
      (at ?v ?from)
      (connects ?r ?from ?to)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
      (not (location-blocked ?from))
      (not (location-blocked ?to))
    )
    :effect (and
      (not (at ?v ?from))
      (at ?v ?to)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-road_0154-tw_00300
    :parameters (?v - vehicle ?r - road_type_road_0154 ?from - loc_type_loc_0094 ?to - loc_type_loc_0064 ?w - window_type_tw_00300)
    :precondition (and
      (at ?v ?from)
      (connects ?r ?from ?to)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
      (not (location-blocked ?from))
      (not (location-blocked ?to))
    )
    :effect (and
      (not (at ?v ?from))
      (at ?v ?to)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-road_0154-tw_00330
    :parameters (?v - vehicle ?r - road_type_road_0154 ?from - loc_type_loc_0094 ?to - loc_type_loc_0064 ?w - window_type_tw_00330)
    :precondition (and
      (at ?v ?from)
      (connects ?r ?from ?to)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
      (not (location-blocked ?from))
      (not (location-blocked ?to))
    )
    :effect (and
      (not (at ?v ?from))
      (at ?v ?to)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-road_0154-tw_00360
    :parameters (?v - vehicle ?r - road_type_road_0154 ?from - loc_type_loc_0094 ?to - loc_type_loc_0064 ?w - window_type_tw_00360)
    :precondition (and
      (at ?v ?from)
      (connects ?r ?from ?to)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
      (not (location-blocked ?from))
      (not (location-blocked ?to))
    )
    :effect (and
      (not (at ?v ?from))
      (at ?v ?to)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-road_0154-tw_00390
    :parameters (?v - vehicle ?r - road_type_road_0154 ?from - loc_type_loc_0094 ?to - loc_type_loc_0064 ?w - window_type_tw_00390)
    :precondition (and
      (at ?v ?from)
      (connects ?r ?from ?to)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
      (not (location-blocked ?from))
      (not (location-blocked ?to))
    )
    :effect (and
      (not (at ?v ?from))
      (at ?v ?to)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-road_0154-tw_00420
    :parameters (?v - vehicle ?r - road_type_road_0154 ?from - loc_type_loc_0094 ?to - loc_type_loc_0064 ?w - window_type_tw_00420)
    :precondition (and
      (at ?v ?from)
      (connects ?r ?from ?to)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
      (not (location-blocked ?from))
      (not (location-blocked ?to))
    )
    :effect (and
      (not (at ?v ?from))
      (at ?v ?to)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-road_0154-tw_00450
    :parameters (?v - vehicle ?r - road_type_road_0154 ?from - loc_type_loc_0094 ?to - loc_type_loc_0064 ?w - window_type_tw_00450)
    :precondition (and
      (at ?v ?from)
      (connects ?r ?from ?to)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
      (not (location-blocked ?from))
      (not (location-blocked ?to))
    )
    :effect (and
      (not (at ?v ?from))
      (at ?v ?to)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-road_0156-tw_00000
    :parameters (?v - vehicle ?r - road_type_road_0156 ?from - loc_type_loc_0096 ?to - loc_type_loc_0076 ?w - window_type_tw_00000)
    :precondition (and
      (at ?v ?from)
      (connects ?r ?from ?to)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
      (not (location-blocked ?from))
      (not (location-blocked ?to))
    )
    :effect (and
      (not (at ?v ?from))
      (at ?v ?to)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-road_0156-tw_00030
    :parameters (?v - vehicle ?r - road_type_road_0156 ?from - loc_type_loc_0096 ?to - loc_type_loc_0076 ?w - window_type_tw_00030)
    :precondition (and
      (at ?v ?from)
      (connects ?r ?from ?to)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
      (not (location-blocked ?from))
      (not (location-blocked ?to))
    )
    :effect (and
      (not (at ?v ?from))
      (at ?v ?to)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-road_0156-tw_00060
    :parameters (?v - vehicle ?r - road_type_road_0156 ?from - loc_type_loc_0096 ?to - loc_type_loc_0076 ?w - window_type_tw_00060)
    :precondition (and
      (at ?v ?from)
      (connects ?r ?from ?to)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
      (not (location-blocked ?from))
      (not (location-blocked ?to))
    )
    :effect (and
      (not (at ?v ?from))
      (at ?v ?to)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-road_0156-tw_00090
    :parameters (?v - vehicle ?r - road_type_road_0156 ?from - loc_type_loc_0096 ?to - loc_type_loc_0076 ?w - window_type_tw_00090)
    :precondition (and
      (at ?v ?from)
      (connects ?r ?from ?to)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
      (not (location-blocked ?from))
      (not (location-blocked ?to))
    )
    :effect (and
      (not (at ?v ?from))
      (at ?v ?to)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-road_0156-tw_00120
    :parameters (?v - vehicle ?r - road_type_road_0156 ?from - loc_type_loc_0096 ?to - loc_type_loc_0076 ?w - window_type_tw_00120)
    :precondition (and
      (at ?v ?from)
      (connects ?r ?from ?to)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
      (not (location-blocked ?from))
      (not (location-blocked ?to))
    )
    :effect (and
      (not (at ?v ?from))
      (at ?v ?to)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-road_0156-tw_00150
    :parameters (?v - vehicle ?r - road_type_road_0156 ?from - loc_type_loc_0096 ?to - loc_type_loc_0076 ?w - window_type_tw_00150)
    :precondition (and
      (at ?v ?from)
      (connects ?r ?from ?to)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
      (not (location-blocked ?from))
      (not (location-blocked ?to))
    )
    :effect (and
      (not (at ?v ?from))
      (at ?v ?to)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-road_0156-tw_00180
    :parameters (?v - vehicle ?r - road_type_road_0156 ?from - loc_type_loc_0096 ?to - loc_type_loc_0076 ?w - window_type_tw_00180)
    :precondition (and
      (at ?v ?from)
      (connects ?r ?from ?to)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
      (not (location-blocked ?from))
      (not (location-blocked ?to))
    )
    :effect (and
      (not (at ?v ?from))
      (at ?v ?to)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-road_0156-tw_00210
    :parameters (?v - vehicle ?r - road_type_road_0156 ?from - loc_type_loc_0096 ?to - loc_type_loc_0076 ?w - window_type_tw_00210)
    :precondition (and
      (at ?v ?from)
      (connects ?r ?from ?to)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
      (not (location-blocked ?from))
      (not (location-blocked ?to))
    )
    :effect (and
      (not (at ?v ?from))
      (at ?v ?to)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-road_0156-tw_00240
    :parameters (?v - vehicle ?r - road_type_road_0156 ?from - loc_type_loc_0096 ?to - loc_type_loc_0076 ?w - window_type_tw_00240)
    :precondition (and
      (at ?v ?from)
      (connects ?r ?from ?to)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
      (not (location-blocked ?from))
      (not (location-blocked ?to))
    )
    :effect (and
      (not (at ?v ?from))
      (at ?v ?to)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-road_0156-tw_00270
    :parameters (?v - vehicle ?r - road_type_road_0156 ?from - loc_type_loc_0096 ?to - loc_type_loc_0076 ?w - window_type_tw_00270)
    :precondition (and
      (at ?v ?from)
      (connects ?r ?from ?to)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
      (not (location-blocked ?from))
      (not (location-blocked ?to))
    )
    :effect (and
      (not (at ?v ?from))
      (at ?v ?to)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-road_0156-tw_00300
    :parameters (?v - vehicle ?r - road_type_road_0156 ?from - loc_type_loc_0096 ?to - loc_type_loc_0076 ?w - window_type_tw_00300)
    :precondition (and
      (at ?v ?from)
      (connects ?r ?from ?to)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
      (not (location-blocked ?from))
      (not (location-blocked ?to))
    )
    :effect (and
      (not (at ?v ?from))
      (at ?v ?to)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-road_0156-tw_00330
    :parameters (?v - vehicle ?r - road_type_road_0156 ?from - loc_type_loc_0096 ?to - loc_type_loc_0076 ?w - window_type_tw_00330)
    :precondition (and
      (at ?v ?from)
      (connects ?r ?from ?to)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
      (not (location-blocked ?from))
      (not (location-blocked ?to))
    )
    :effect (and
      (not (at ?v ?from))
      (at ?v ?to)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-road_0156-tw_00360
    :parameters (?v - vehicle ?r - road_type_road_0156 ?from - loc_type_loc_0096 ?to - loc_type_loc_0076 ?w - window_type_tw_00360)
    :precondition (and
      (at ?v ?from)
      (connects ?r ?from ?to)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
      (not (location-blocked ?from))
      (not (location-blocked ?to))
    )
    :effect (and
      (not (at ?v ?from))
      (at ?v ?to)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-road_0156-tw_00390
    :parameters (?v - vehicle ?r - road_type_road_0156 ?from - loc_type_loc_0096 ?to - loc_type_loc_0076 ?w - window_type_tw_00390)
    :precondition (and
      (at ?v ?from)
      (connects ?r ?from ?to)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
      (not (location-blocked ?from))
      (not (location-blocked ?to))
    )
    :effect (and
      (not (at ?v ?from))
      (at ?v ?to)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-road_0156-tw_00420
    :parameters (?v - vehicle ?r - road_type_road_0156 ?from - loc_type_loc_0096 ?to - loc_type_loc_0076 ?w - window_type_tw_00420)
    :precondition (and
      (at ?v ?from)
      (connects ?r ?from ?to)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
      (not (location-blocked ?from))
      (not (location-blocked ?to))
    )
    :effect (and
      (not (at ?v ?from))
      (at ?v ?to)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-road_0156-tw_00450
    :parameters (?v - vehicle ?r - road_type_road_0156 ?from - loc_type_loc_0096 ?to - loc_type_loc_0076 ?w - window_type_tw_00450)
    :precondition (and
      (at ?v ?from)
      (connects ?r ?from ?to)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
      (not (location-blocked ?from))
      (not (location-blocked ?to))
    )
    :effect (and
      (not (at ?v ?from))
      (at ?v ?to)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-road_0157-tw_00000
    :parameters (?v - vehicle ?r - road_type_road_0157 ?from - loc_type_loc_0096 ?to - loc_type_loc_0013 ?w - window_type_tw_00000)
    :precondition (and
      (at ?v ?from)
      (connects ?r ?from ?to)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
      (not (location-blocked ?from))
      (not (location-blocked ?to))
    )
    :effect (and
      (not (at ?v ?from))
      (at ?v ?to)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-road_0157-tw_00030
    :parameters (?v - vehicle ?r - road_type_road_0157 ?from - loc_type_loc_0096 ?to - loc_type_loc_0013 ?w - window_type_tw_00030)
    :precondition (and
      (at ?v ?from)
      (connects ?r ?from ?to)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
      (not (location-blocked ?from))
      (not (location-blocked ?to))
    )
    :effect (and
      (not (at ?v ?from))
      (at ?v ?to)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-road_0157-tw_00060
    :parameters (?v - vehicle ?r - road_type_road_0157 ?from - loc_type_loc_0096 ?to - loc_type_loc_0013 ?w - window_type_tw_00060)
    :precondition (and
      (at ?v ?from)
      (connects ?r ?from ?to)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
      (not (location-blocked ?from))
      (not (location-blocked ?to))
    )
    :effect (and
      (not (at ?v ?from))
      (at ?v ?to)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-road_0157-tw_00090
    :parameters (?v - vehicle ?r - road_type_road_0157 ?from - loc_type_loc_0096 ?to - loc_type_loc_0013 ?w - window_type_tw_00090)
    :precondition (and
      (at ?v ?from)
      (connects ?r ?from ?to)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
      (not (location-blocked ?from))
      (not (location-blocked ?to))
    )
    :effect (and
      (not (at ?v ?from))
      (at ?v ?to)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-road_0157-tw_00120
    :parameters (?v - vehicle ?r - road_type_road_0157 ?from - loc_type_loc_0096 ?to - loc_type_loc_0013 ?w - window_type_tw_00120)
    :precondition (and
      (at ?v ?from)
      (connects ?r ?from ?to)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
      (not (location-blocked ?from))
      (not (location-blocked ?to))
    )
    :effect (and
      (not (at ?v ?from))
      (at ?v ?to)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-road_0157-tw_00150
    :parameters (?v - vehicle ?r - road_type_road_0157 ?from - loc_type_loc_0096 ?to - loc_type_loc_0013 ?w - window_type_tw_00150)
    :precondition (and
      (at ?v ?from)
      (connects ?r ?from ?to)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
      (not (location-blocked ?from))
      (not (location-blocked ?to))
    )
    :effect (and
      (not (at ?v ?from))
      (at ?v ?to)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-road_0157-tw_00180
    :parameters (?v - vehicle ?r - road_type_road_0157 ?from - loc_type_loc_0096 ?to - loc_type_loc_0013 ?w - window_type_tw_00180)
    :precondition (and
      (at ?v ?from)
      (connects ?r ?from ?to)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
      (not (location-blocked ?from))
      (not (location-blocked ?to))
    )
    :effect (and
      (not (at ?v ?from))
      (at ?v ?to)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-road_0157-tw_00210
    :parameters (?v - vehicle ?r - road_type_road_0157 ?from - loc_type_loc_0096 ?to - loc_type_loc_0013 ?w - window_type_tw_00210)
    :precondition (and
      (at ?v ?from)
      (connects ?r ?from ?to)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
      (not (location-blocked ?from))
      (not (location-blocked ?to))
    )
    :effect (and
      (not (at ?v ?from))
      (at ?v ?to)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-road_0157-tw_00240
    :parameters (?v - vehicle ?r - road_type_road_0157 ?from - loc_type_loc_0096 ?to - loc_type_loc_0013 ?w - window_type_tw_00240)
    :precondition (and
      (at ?v ?from)
      (connects ?r ?from ?to)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
      (not (location-blocked ?from))
      (not (location-blocked ?to))
    )
    :effect (and
      (not (at ?v ?from))
      (at ?v ?to)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-road_0157-tw_00270
    :parameters (?v - vehicle ?r - road_type_road_0157 ?from - loc_type_loc_0096 ?to - loc_type_loc_0013 ?w - window_type_tw_00270)
    :precondition (and
      (at ?v ?from)
      (connects ?r ?from ?to)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
      (not (location-blocked ?from))
      (not (location-blocked ?to))
    )
    :effect (and
      (not (at ?v ?from))
      (at ?v ?to)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-road_0157-tw_00300
    :parameters (?v - vehicle ?r - road_type_road_0157 ?from - loc_type_loc_0096 ?to - loc_type_loc_0013 ?w - window_type_tw_00300)
    :precondition (and
      (at ?v ?from)
      (connects ?r ?from ?to)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
      (not (location-blocked ?from))
      (not (location-blocked ?to))
    )
    :effect (and
      (not (at ?v ?from))
      (at ?v ?to)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-road_0157-tw_00330
    :parameters (?v - vehicle ?r - road_type_road_0157 ?from - loc_type_loc_0096 ?to - loc_type_loc_0013 ?w - window_type_tw_00330)
    :precondition (and
      (at ?v ?from)
      (connects ?r ?from ?to)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
      (not (location-blocked ?from))
      (not (location-blocked ?to))
    )
    :effect (and
      (not (at ?v ?from))
      (at ?v ?to)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-road_0157-tw_00360
    :parameters (?v - vehicle ?r - road_type_road_0157 ?from - loc_type_loc_0096 ?to - loc_type_loc_0013 ?w - window_type_tw_00360)
    :precondition (and
      (at ?v ?from)
      (connects ?r ?from ?to)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
      (not (location-blocked ?from))
      (not (location-blocked ?to))
    )
    :effect (and
      (not (at ?v ?from))
      (at ?v ?to)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-road_0157-tw_00390
    :parameters (?v - vehicle ?r - road_type_road_0157 ?from - loc_type_loc_0096 ?to - loc_type_loc_0013 ?w - window_type_tw_00390)
    :precondition (and
      (at ?v ?from)
      (connects ?r ?from ?to)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
      (not (location-blocked ?from))
      (not (location-blocked ?to))
    )
    :effect (and
      (not (at ?v ?from))
      (at ?v ?to)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-road_0157-tw_00420
    :parameters (?v - vehicle ?r - road_type_road_0157 ?from - loc_type_loc_0096 ?to - loc_type_loc_0013 ?w - window_type_tw_00420)
    :precondition (and
      (at ?v ?from)
      (connects ?r ?from ?to)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
      (not (location-blocked ?from))
      (not (location-blocked ?to))
    )
    :effect (and
      (not (at ?v ?from))
      (at ?v ?to)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-road_0157-tw_00450
    :parameters (?v - vehicle ?r - road_type_road_0157 ?from - loc_type_loc_0096 ?to - loc_type_loc_0013 ?w - window_type_tw_00450)
    :precondition (and
      (at ?v ?from)
      (connects ?r ?from ?to)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
      (not (location-blocked ?from))
      (not (location-blocked ?to))
    )
    :effect (and
      (not (at ?v ?from))
      (at ?v ?to)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-road_0158-tw_00000
    :parameters (?v - vehicle ?r - road_type_road_0158 ?from - loc_type_loc_0097 ?to - loc_type_loc_0047 ?w - window_type_tw_00000)
    :precondition (and
      (at ?v ?from)
      (connects ?r ?from ?to)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
      (not (location-blocked ?from))
      (not (location-blocked ?to))
    )
    :effect (and
      (not (at ?v ?from))
      (at ?v ?to)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-road_0158-tw_00030
    :parameters (?v - vehicle ?r - road_type_road_0158 ?from - loc_type_loc_0097 ?to - loc_type_loc_0047 ?w - window_type_tw_00030)
    :precondition (and
      (at ?v ?from)
      (connects ?r ?from ?to)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
      (not (location-blocked ?from))
      (not (location-blocked ?to))
    )
    :effect (and
      (not (at ?v ?from))
      (at ?v ?to)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-road_0158-tw_00060
    :parameters (?v - vehicle ?r - road_type_road_0158 ?from - loc_type_loc_0097 ?to - loc_type_loc_0047 ?w - window_type_tw_00060)
    :precondition (and
      (at ?v ?from)
      (connects ?r ?from ?to)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
      (not (location-blocked ?from))
      (not (location-blocked ?to))
    )
    :effect (and
      (not (at ?v ?from))
      (at ?v ?to)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-road_0158-tw_00090
    :parameters (?v - vehicle ?r - road_type_road_0158 ?from - loc_type_loc_0097 ?to - loc_type_loc_0047 ?w - window_type_tw_00090)
    :precondition (and
      (at ?v ?from)
      (connects ?r ?from ?to)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
      (not (location-blocked ?from))
      (not (location-blocked ?to))
    )
    :effect (and
      (not (at ?v ?from))
      (at ?v ?to)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-road_0158-tw_00120
    :parameters (?v - vehicle ?r - road_type_road_0158 ?from - loc_type_loc_0097 ?to - loc_type_loc_0047 ?w - window_type_tw_00120)
    :precondition (and
      (at ?v ?from)
      (connects ?r ?from ?to)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
      (not (location-blocked ?from))
      (not (location-blocked ?to))
    )
    :effect (and
      (not (at ?v ?from))
      (at ?v ?to)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-road_0158-tw_00150
    :parameters (?v - vehicle ?r - road_type_road_0158 ?from - loc_type_loc_0097 ?to - loc_type_loc_0047 ?w - window_type_tw_00150)
    :precondition (and
      (at ?v ?from)
      (connects ?r ?from ?to)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
      (not (location-blocked ?from))
      (not (location-blocked ?to))
    )
    :effect (and
      (not (at ?v ?from))
      (at ?v ?to)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-road_0158-tw_00180
    :parameters (?v - vehicle ?r - road_type_road_0158 ?from - loc_type_loc_0097 ?to - loc_type_loc_0047 ?w - window_type_tw_00180)
    :precondition (and
      (at ?v ?from)
      (connects ?r ?from ?to)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
      (not (location-blocked ?from))
      (not (location-blocked ?to))
    )
    :effect (and
      (not (at ?v ?from))
      (at ?v ?to)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-road_0158-tw_00210
    :parameters (?v - vehicle ?r - road_type_road_0158 ?from - loc_type_loc_0097 ?to - loc_type_loc_0047 ?w - window_type_tw_00210)
    :precondition (and
      (at ?v ?from)
      (connects ?r ?from ?to)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
      (not (location-blocked ?from))
      (not (location-blocked ?to))
    )
    :effect (and
      (not (at ?v ?from))
      (at ?v ?to)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-road_0158-tw_00240
    :parameters (?v - vehicle ?r - road_type_road_0158 ?from - loc_type_loc_0097 ?to - loc_type_loc_0047 ?w - window_type_tw_00240)
    :precondition (and
      (at ?v ?from)
      (connects ?r ?from ?to)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
      (not (location-blocked ?from))
      (not (location-blocked ?to))
    )
    :effect (and
      (not (at ?v ?from))
      (at ?v ?to)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-road_0158-tw_00270
    :parameters (?v - vehicle ?r - road_type_road_0158 ?from - loc_type_loc_0097 ?to - loc_type_loc_0047 ?w - window_type_tw_00270)
    :precondition (and
      (at ?v ?from)
      (connects ?r ?from ?to)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
      (not (location-blocked ?from))
      (not (location-blocked ?to))
    )
    :effect (and
      (not (at ?v ?from))
      (at ?v ?to)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-road_0158-tw_00300
    :parameters (?v - vehicle ?r - road_type_road_0158 ?from - loc_type_loc_0097 ?to - loc_type_loc_0047 ?w - window_type_tw_00300)
    :precondition (and
      (at ?v ?from)
      (connects ?r ?from ?to)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
      (not (location-blocked ?from))
      (not (location-blocked ?to))
    )
    :effect (and
      (not (at ?v ?from))
      (at ?v ?to)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-road_0158-tw_00330
    :parameters (?v - vehicle ?r - road_type_road_0158 ?from - loc_type_loc_0097 ?to - loc_type_loc_0047 ?w - window_type_tw_00330)
    :precondition (and
      (at ?v ?from)
      (connects ?r ?from ?to)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
      (not (location-blocked ?from))
      (not (location-blocked ?to))
    )
    :effect (and
      (not (at ?v ?from))
      (at ?v ?to)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-road_0158-tw_00360
    :parameters (?v - vehicle ?r - road_type_road_0158 ?from - loc_type_loc_0097 ?to - loc_type_loc_0047 ?w - window_type_tw_00360)
    :precondition (and
      (at ?v ?from)
      (connects ?r ?from ?to)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
      (not (location-blocked ?from))
      (not (location-blocked ?to))
    )
    :effect (and
      (not (at ?v ?from))
      (at ?v ?to)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-road_0158-tw_00390
    :parameters (?v - vehicle ?r - road_type_road_0158 ?from - loc_type_loc_0097 ?to - loc_type_loc_0047 ?w - window_type_tw_00390)
    :precondition (and
      (at ?v ?from)
      (connects ?r ?from ?to)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
      (not (location-blocked ?from))
      (not (location-blocked ?to))
    )
    :effect (and
      (not (at ?v ?from))
      (at ?v ?to)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-road_0158-tw_00420
    :parameters (?v - vehicle ?r - road_type_road_0158 ?from - loc_type_loc_0097 ?to - loc_type_loc_0047 ?w - window_type_tw_00420)
    :precondition (and
      (at ?v ?from)
      (connects ?r ?from ?to)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
      (not (location-blocked ?from))
      (not (location-blocked ?to))
    )
    :effect (and
      (not (at ?v ?from))
      (at ?v ?to)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-road_0158-tw_00450
    :parameters (?v - vehicle ?r - road_type_road_0158 ?from - loc_type_loc_0097 ?to - loc_type_loc_0047 ?w - window_type_tw_00450)
    :precondition (and
      (at ?v ?from)
      (connects ?r ?from ?to)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
      (not (location-blocked ?from))
      (not (location-blocked ?to))
    )
    :effect (and
      (not (at ?v ?from))
      (at ?v ?to)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-road_0159-tw_00000
    :parameters (?v - vehicle ?r - road_type_road_0159 ?from - loc_type_loc_0097 ?to - loc_type_loc_0098 ?w - window_type_tw_00000)
    :precondition (and
      (at ?v ?from)
      (connects ?r ?from ?to)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
      (not (location-blocked ?from))
      (not (location-blocked ?to))
    )
    :effect (and
      (not (at ?v ?from))
      (at ?v ?to)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-road_0159-tw_00030
    :parameters (?v - vehicle ?r - road_type_road_0159 ?from - loc_type_loc_0097 ?to - loc_type_loc_0098 ?w - window_type_tw_00030)
    :precondition (and
      (at ?v ?from)
      (connects ?r ?from ?to)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
      (not (location-blocked ?from))
      (not (location-blocked ?to))
    )
    :effect (and
      (not (at ?v ?from))
      (at ?v ?to)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-road_0159-tw_00060
    :parameters (?v - vehicle ?r - road_type_road_0159 ?from - loc_type_loc_0097 ?to - loc_type_loc_0098 ?w - window_type_tw_00060)
    :precondition (and
      (at ?v ?from)
      (connects ?r ?from ?to)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
      (not (location-blocked ?from))
      (not (location-blocked ?to))
    )
    :effect (and
      (not (at ?v ?from))
      (at ?v ?to)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-road_0159-tw_00090
    :parameters (?v - vehicle ?r - road_type_road_0159 ?from - loc_type_loc_0097 ?to - loc_type_loc_0098 ?w - window_type_tw_00090)
    :precondition (and
      (at ?v ?from)
      (connects ?r ?from ?to)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
      (not (location-blocked ?from))
      (not (location-blocked ?to))
    )
    :effect (and
      (not (at ?v ?from))
      (at ?v ?to)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-road_0159-tw_00120
    :parameters (?v - vehicle ?r - road_type_road_0159 ?from - loc_type_loc_0097 ?to - loc_type_loc_0098 ?w - window_type_tw_00120)
    :precondition (and
      (at ?v ?from)
      (connects ?r ?from ?to)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
      (not (location-blocked ?from))
      (not (location-blocked ?to))
    )
    :effect (and
      (not (at ?v ?from))
      (at ?v ?to)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-road_0159-tw_00150
    :parameters (?v - vehicle ?r - road_type_road_0159 ?from - loc_type_loc_0097 ?to - loc_type_loc_0098 ?w - window_type_tw_00150)
    :precondition (and
      (at ?v ?from)
      (connects ?r ?from ?to)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
      (not (location-blocked ?from))
      (not (location-blocked ?to))
    )
    :effect (and
      (not (at ?v ?from))
      (at ?v ?to)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-road_0159-tw_00180
    :parameters (?v - vehicle ?r - road_type_road_0159 ?from - loc_type_loc_0097 ?to - loc_type_loc_0098 ?w - window_type_tw_00180)
    :precondition (and
      (at ?v ?from)
      (connects ?r ?from ?to)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
      (not (location-blocked ?from))
      (not (location-blocked ?to))
    )
    :effect (and
      (not (at ?v ?from))
      (at ?v ?to)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-road_0159-tw_00210
    :parameters (?v - vehicle ?r - road_type_road_0159 ?from - loc_type_loc_0097 ?to - loc_type_loc_0098 ?w - window_type_tw_00210)
    :precondition (and
      (at ?v ?from)
      (connects ?r ?from ?to)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
      (not (location-blocked ?from))
      (not (location-blocked ?to))
    )
    :effect (and
      (not (at ?v ?from))
      (at ?v ?to)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-road_0159-tw_00240
    :parameters (?v - vehicle ?r - road_type_road_0159 ?from - loc_type_loc_0097 ?to - loc_type_loc_0098 ?w - window_type_tw_00240)
    :precondition (and
      (at ?v ?from)
      (connects ?r ?from ?to)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
      (not (location-blocked ?from))
      (not (location-blocked ?to))
    )
    :effect (and
      (not (at ?v ?from))
      (at ?v ?to)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-road_0159-tw_00270
    :parameters (?v - vehicle ?r - road_type_road_0159 ?from - loc_type_loc_0097 ?to - loc_type_loc_0098 ?w - window_type_tw_00270)
    :precondition (and
      (at ?v ?from)
      (connects ?r ?from ?to)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
      (not (location-blocked ?from))
      (not (location-blocked ?to))
    )
    :effect (and
      (not (at ?v ?from))
      (at ?v ?to)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-road_0159-tw_00300
    :parameters (?v - vehicle ?r - road_type_road_0159 ?from - loc_type_loc_0097 ?to - loc_type_loc_0098 ?w - window_type_tw_00300)
    :precondition (and
      (at ?v ?from)
      (connects ?r ?from ?to)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
      (not (location-blocked ?from))
      (not (location-blocked ?to))
    )
    :effect (and
      (not (at ?v ?from))
      (at ?v ?to)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-road_0159-tw_00330
    :parameters (?v - vehicle ?r - road_type_road_0159 ?from - loc_type_loc_0097 ?to - loc_type_loc_0098 ?w - window_type_tw_00330)
    :precondition (and
      (at ?v ?from)
      (connects ?r ?from ?to)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
      (not (location-blocked ?from))
      (not (location-blocked ?to))
    )
    :effect (and
      (not (at ?v ?from))
      (at ?v ?to)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-road_0159-tw_00360
    :parameters (?v - vehicle ?r - road_type_road_0159 ?from - loc_type_loc_0097 ?to - loc_type_loc_0098 ?w - window_type_tw_00360)
    :precondition (and
      (at ?v ?from)
      (connects ?r ?from ?to)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
      (not (location-blocked ?from))
      (not (location-blocked ?to))
    )
    :effect (and
      (not (at ?v ?from))
      (at ?v ?to)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-road_0159-tw_00390
    :parameters (?v - vehicle ?r - road_type_road_0159 ?from - loc_type_loc_0097 ?to - loc_type_loc_0098 ?w - window_type_tw_00390)
    :precondition (and
      (at ?v ?from)
      (connects ?r ?from ?to)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
      (not (location-blocked ?from))
      (not (location-blocked ?to))
    )
    :effect (and
      (not (at ?v ?from))
      (at ?v ?to)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-road_0159-tw_00420
    :parameters (?v - vehicle ?r - road_type_road_0159 ?from - loc_type_loc_0097 ?to - loc_type_loc_0098 ?w - window_type_tw_00420)
    :precondition (and
      (at ?v ?from)
      (connects ?r ?from ?to)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
      (not (location-blocked ?from))
      (not (location-blocked ?to))
    )
    :effect (and
      (not (at ?v ?from))
      (at ?v ?to)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-road_0159-tw_00450
    :parameters (?v - vehicle ?r - road_type_road_0159 ?from - loc_type_loc_0097 ?to - loc_type_loc_0098 ?w - window_type_tw_00450)
    :precondition (and
      (at ?v ?from)
      (connects ?r ?from ?to)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
      (not (location-blocked ?from))
      (not (location-blocked ?to))
    )
    :effect (and
      (not (at ?v ?from))
      (at ?v ?to)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-road_0160-tw_00000
    :parameters (?v - vehicle ?r - road_type_road_0160 ?from - loc_type_loc_0098 ?to - loc_type_loc_0097 ?w - window_type_tw_00000)
    :precondition (and
      (at ?v ?from)
      (connects ?r ?from ?to)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
      (not (location-blocked ?from))
      (not (location-blocked ?to))
    )
    :effect (and
      (not (at ?v ?from))
      (at ?v ?to)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-road_0160-tw_00030
    :parameters (?v - vehicle ?r - road_type_road_0160 ?from - loc_type_loc_0098 ?to - loc_type_loc_0097 ?w - window_type_tw_00030)
    :precondition (and
      (at ?v ?from)
      (connects ?r ?from ?to)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
      (not (location-blocked ?from))
      (not (location-blocked ?to))
    )
    :effect (and
      (not (at ?v ?from))
      (at ?v ?to)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-road_0160-tw_00060
    :parameters (?v - vehicle ?r - road_type_road_0160 ?from - loc_type_loc_0098 ?to - loc_type_loc_0097 ?w - window_type_tw_00060)
    :precondition (and
      (at ?v ?from)
      (connects ?r ?from ?to)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
      (not (location-blocked ?from))
      (not (location-blocked ?to))
    )
    :effect (and
      (not (at ?v ?from))
      (at ?v ?to)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-road_0160-tw_00090
    :parameters (?v - vehicle ?r - road_type_road_0160 ?from - loc_type_loc_0098 ?to - loc_type_loc_0097 ?w - window_type_tw_00090)
    :precondition (and
      (at ?v ?from)
      (connects ?r ?from ?to)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
      (not (location-blocked ?from))
      (not (location-blocked ?to))
    )
    :effect (and
      (not (at ?v ?from))
      (at ?v ?to)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-road_0160-tw_00120
    :parameters (?v - vehicle ?r - road_type_road_0160 ?from - loc_type_loc_0098 ?to - loc_type_loc_0097 ?w - window_type_tw_00120)
    :precondition (and
      (at ?v ?from)
      (connects ?r ?from ?to)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
      (not (location-blocked ?from))
      (not (location-blocked ?to))
    )
    :effect (and
      (not (at ?v ?from))
      (at ?v ?to)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-road_0160-tw_00150
    :parameters (?v - vehicle ?r - road_type_road_0160 ?from - loc_type_loc_0098 ?to - loc_type_loc_0097 ?w - window_type_tw_00150)
    :precondition (and
      (at ?v ?from)
      (connects ?r ?from ?to)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
      (not (location-blocked ?from))
      (not (location-blocked ?to))
    )
    :effect (and
      (not (at ?v ?from))
      (at ?v ?to)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-road_0160-tw_00180
    :parameters (?v - vehicle ?r - road_type_road_0160 ?from - loc_type_loc_0098 ?to - loc_type_loc_0097 ?w - window_type_tw_00180)
    :precondition (and
      (at ?v ?from)
      (connects ?r ?from ?to)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
      (not (location-blocked ?from))
      (not (location-blocked ?to))
    )
    :effect (and
      (not (at ?v ?from))
      (at ?v ?to)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-road_0160-tw_00210
    :parameters (?v - vehicle ?r - road_type_road_0160 ?from - loc_type_loc_0098 ?to - loc_type_loc_0097 ?w - window_type_tw_00210)
    :precondition (and
      (at ?v ?from)
      (connects ?r ?from ?to)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
      (not (location-blocked ?from))
      (not (location-blocked ?to))
    )
    :effect (and
      (not (at ?v ?from))
      (at ?v ?to)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-road_0160-tw_00240
    :parameters (?v - vehicle ?r - road_type_road_0160 ?from - loc_type_loc_0098 ?to - loc_type_loc_0097 ?w - window_type_tw_00240)
    :precondition (and
      (at ?v ?from)
      (connects ?r ?from ?to)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
      (not (location-blocked ?from))
      (not (location-blocked ?to))
    )
    :effect (and
      (not (at ?v ?from))
      (at ?v ?to)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-road_0160-tw_00270
    :parameters (?v - vehicle ?r - road_type_road_0160 ?from - loc_type_loc_0098 ?to - loc_type_loc_0097 ?w - window_type_tw_00270)
    :precondition (and
      (at ?v ?from)
      (connects ?r ?from ?to)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
      (not (location-blocked ?from))
      (not (location-blocked ?to))
    )
    :effect (and
      (not (at ?v ?from))
      (at ?v ?to)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-road_0160-tw_00300
    :parameters (?v - vehicle ?r - road_type_road_0160 ?from - loc_type_loc_0098 ?to - loc_type_loc_0097 ?w - window_type_tw_00300)
    :precondition (and
      (at ?v ?from)
      (connects ?r ?from ?to)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
      (not (location-blocked ?from))
      (not (location-blocked ?to))
    )
    :effect (and
      (not (at ?v ?from))
      (at ?v ?to)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-road_0160-tw_00330
    :parameters (?v - vehicle ?r - road_type_road_0160 ?from - loc_type_loc_0098 ?to - loc_type_loc_0097 ?w - window_type_tw_00330)
    :precondition (and
      (at ?v ?from)
      (connects ?r ?from ?to)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
      (not (location-blocked ?from))
      (not (location-blocked ?to))
    )
    :effect (and
      (not (at ?v ?from))
      (at ?v ?to)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-road_0160-tw_00360
    :parameters (?v - vehicle ?r - road_type_road_0160 ?from - loc_type_loc_0098 ?to - loc_type_loc_0097 ?w - window_type_tw_00360)
    :precondition (and
      (at ?v ?from)
      (connects ?r ?from ?to)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
      (not (location-blocked ?from))
      (not (location-blocked ?to))
    )
    :effect (and
      (not (at ?v ?from))
      (at ?v ?to)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-road_0160-tw_00390
    :parameters (?v - vehicle ?r - road_type_road_0160 ?from - loc_type_loc_0098 ?to - loc_type_loc_0097 ?w - window_type_tw_00390)
    :precondition (and
      (at ?v ?from)
      (connects ?r ?from ?to)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
      (not (location-blocked ?from))
      (not (location-blocked ?to))
    )
    :effect (and
      (not (at ?v ?from))
      (at ?v ?to)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-road_0160-tw_00420
    :parameters (?v - vehicle ?r - road_type_road_0160 ?from - loc_type_loc_0098 ?to - loc_type_loc_0097 ?w - window_type_tw_00420)
    :precondition (and
      (at ?v ?from)
      (connects ?r ?from ?to)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
      (not (location-blocked ?from))
      (not (location-blocked ?to))
    )
    :effect (and
      (not (at ?v ?from))
      (at ?v ?to)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-road_0160-tw_00450
    :parameters (?v - vehicle ?r - road_type_road_0160 ?from - loc_type_loc_0098 ?to - loc_type_loc_0097 ?w - window_type_tw_00450)
    :precondition (and
      (at ?v ?from)
      (connects ?r ?from ?to)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
      (not (location-blocked ?from))
      (not (location-blocked ?to))
    )
    :effect (and
      (not (at ?v ?from))
      (at ?v ?to)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-road_0161-tw_00000
    :parameters (?v - vehicle ?r - road_type_road_0161 ?from - loc_type_loc_0099 ?to - loc_type_loc_0083 ?w - window_type_tw_00000)
    :precondition (and
      (at ?v ?from)
      (connects ?r ?from ?to)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
      (not (location-blocked ?from))
      (not (location-blocked ?to))
    )
    :effect (and
      (not (at ?v ?from))
      (at ?v ?to)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-road_0161-tw_00030
    :parameters (?v - vehicle ?r - road_type_road_0161 ?from - loc_type_loc_0099 ?to - loc_type_loc_0083 ?w - window_type_tw_00030)
    :precondition (and
      (at ?v ?from)
      (connects ?r ?from ?to)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
      (not (location-blocked ?from))
      (not (location-blocked ?to))
    )
    :effect (and
      (not (at ?v ?from))
      (at ?v ?to)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-road_0161-tw_00060
    :parameters (?v - vehicle ?r - road_type_road_0161 ?from - loc_type_loc_0099 ?to - loc_type_loc_0083 ?w - window_type_tw_00060)
    :precondition (and
      (at ?v ?from)
      (connects ?r ?from ?to)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
      (not (location-blocked ?from))
      (not (location-blocked ?to))
    )
    :effect (and
      (not (at ?v ?from))
      (at ?v ?to)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-road_0161-tw_00090
    :parameters (?v - vehicle ?r - road_type_road_0161 ?from - loc_type_loc_0099 ?to - loc_type_loc_0083 ?w - window_type_tw_00090)
    :precondition (and
      (at ?v ?from)
      (connects ?r ?from ?to)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
      (not (location-blocked ?from))
      (not (location-blocked ?to))
    )
    :effect (and
      (not (at ?v ?from))
      (at ?v ?to)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-road_0161-tw_00120
    :parameters (?v - vehicle ?r - road_type_road_0161 ?from - loc_type_loc_0099 ?to - loc_type_loc_0083 ?w - window_type_tw_00120)
    :precondition (and
      (at ?v ?from)
      (connects ?r ?from ?to)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
      (not (location-blocked ?from))
      (not (location-blocked ?to))
    )
    :effect (and
      (not (at ?v ?from))
      (at ?v ?to)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-road_0161-tw_00150
    :parameters (?v - vehicle ?r - road_type_road_0161 ?from - loc_type_loc_0099 ?to - loc_type_loc_0083 ?w - window_type_tw_00150)
    :precondition (and
      (at ?v ?from)
      (connects ?r ?from ?to)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
      (not (location-blocked ?from))
      (not (location-blocked ?to))
    )
    :effect (and
      (not (at ?v ?from))
      (at ?v ?to)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-road_0161-tw_00180
    :parameters (?v - vehicle ?r - road_type_road_0161 ?from - loc_type_loc_0099 ?to - loc_type_loc_0083 ?w - window_type_tw_00180)
    :precondition (and
      (at ?v ?from)
      (connects ?r ?from ?to)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
      (not (location-blocked ?from))
      (not (location-blocked ?to))
    )
    :effect (and
      (not (at ?v ?from))
      (at ?v ?to)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-road_0161-tw_00210
    :parameters (?v - vehicle ?r - road_type_road_0161 ?from - loc_type_loc_0099 ?to - loc_type_loc_0083 ?w - window_type_tw_00210)
    :precondition (and
      (at ?v ?from)
      (connects ?r ?from ?to)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
      (not (location-blocked ?from))
      (not (location-blocked ?to))
    )
    :effect (and
      (not (at ?v ?from))
      (at ?v ?to)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-road_0161-tw_00240
    :parameters (?v - vehicle ?r - road_type_road_0161 ?from - loc_type_loc_0099 ?to - loc_type_loc_0083 ?w - window_type_tw_00240)
    :precondition (and
      (at ?v ?from)
      (connects ?r ?from ?to)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
      (not (location-blocked ?from))
      (not (location-blocked ?to))
    )
    :effect (and
      (not (at ?v ?from))
      (at ?v ?to)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-road_0161-tw_00270
    :parameters (?v - vehicle ?r - road_type_road_0161 ?from - loc_type_loc_0099 ?to - loc_type_loc_0083 ?w - window_type_tw_00270)
    :precondition (and
      (at ?v ?from)
      (connects ?r ?from ?to)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
      (not (location-blocked ?from))
      (not (location-blocked ?to))
    )
    :effect (and
      (not (at ?v ?from))
      (at ?v ?to)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-road_0161-tw_00300
    :parameters (?v - vehicle ?r - road_type_road_0161 ?from - loc_type_loc_0099 ?to - loc_type_loc_0083 ?w - window_type_tw_00300)
    :precondition (and
      (at ?v ?from)
      (connects ?r ?from ?to)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
      (not (location-blocked ?from))
      (not (location-blocked ?to))
    )
    :effect (and
      (not (at ?v ?from))
      (at ?v ?to)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-road_0161-tw_00330
    :parameters (?v - vehicle ?r - road_type_road_0161 ?from - loc_type_loc_0099 ?to - loc_type_loc_0083 ?w - window_type_tw_00330)
    :precondition (and
      (at ?v ?from)
      (connects ?r ?from ?to)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
      (not (location-blocked ?from))
      (not (location-blocked ?to))
    )
    :effect (and
      (not (at ?v ?from))
      (at ?v ?to)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-road_0161-tw_00360
    :parameters (?v - vehicle ?r - road_type_road_0161 ?from - loc_type_loc_0099 ?to - loc_type_loc_0083 ?w - window_type_tw_00360)
    :precondition (and
      (at ?v ?from)
      (connects ?r ?from ?to)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
      (not (location-blocked ?from))
      (not (location-blocked ?to))
    )
    :effect (and
      (not (at ?v ?from))
      (at ?v ?to)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-road_0161-tw_00390
    :parameters (?v - vehicle ?r - road_type_road_0161 ?from - loc_type_loc_0099 ?to - loc_type_loc_0083 ?w - window_type_tw_00390)
    :precondition (and
      (at ?v ?from)
      (connects ?r ?from ?to)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
      (not (location-blocked ?from))
      (not (location-blocked ?to))
    )
    :effect (and
      (not (at ?v ?from))
      (at ?v ?to)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-road_0161-tw_00420
    :parameters (?v - vehicle ?r - road_type_road_0161 ?from - loc_type_loc_0099 ?to - loc_type_loc_0083 ?w - window_type_tw_00420)
    :precondition (and
      (at ?v ?from)
      (connects ?r ?from ?to)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
      (not (location-blocked ?from))
      (not (location-blocked ?to))
    )
    :effect (and
      (not (at ?v ?from))
      (at ?v ?to)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-road_0161-tw_00450
    :parameters (?v - vehicle ?r - road_type_road_0161 ?from - loc_type_loc_0099 ?to - loc_type_loc_0083 ?w - window_type_tw_00450)
    :precondition (and
      (at ?v ?from)
      (connects ?r ?from ?to)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
      (not (location-blocked ?from))
      (not (location-blocked ?to))
    )
    :effect (and
      (not (at ?v ?from))
      (at ?v ?to)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  ;; Advances the single global congestion window.
  (:event advance-window
    :parameters (?from - time-window ?to - time-window)
    :precondition (and
      (current-window ?from)
      (next-window ?from ?to)
      (>= (sim-time) (window-start ?to))
    )
    :effect (and
      (not (current-window ?from))
      (current-window ?to)
    )
  )
)
