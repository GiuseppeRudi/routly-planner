;; ============================================================
;;  DOMAIN: road-network
;;  Features: tl_cong-pddl-hybrid_llm_macro
;;    traversal             : compiled_duration
;;    state representation  : line_graph
;;    action generation     : compiled
;;    traffic_lights  : True
;;    congestion      : enabled=True, mode=pddl, type=hybrid
;;    llm_events      : True
;;    fuel            : enabled=False, replanning=False, consumption=continuous
;; ============================================================

(define (domain road-network)
  (:requirements :typing :numeric-fluents :time)
  (:types
    vehicle location road time-window - object
    road_type_macro_0000 road_type_macro_0001 road_type_macro_0002 road_type_macro_0003 road_type_macro_0004 road_type_macro_0005 road_type_macro_0006 road_type_macro_0007 road_type_macro_0008 road_type_macro_0009 road_type_macro_0010 road_type_macro_0011 road_type_macro_0012 road_type_macro_0013 road_type_macro_0014 road_type_macro_0015 road_type_macro_0016 road_type_macro_0017 road_type_macro_0018 road_type_macro_0019 road_type_macro_0020 road_type_macro_0021 road_type_macro_0022 road_type_macro_0023 road_type_macro_0024 road_type_macro_0025 road_type_macro_0026 road_type_macro_0027 road_type_macro_0028 road_type_macro_0029 road_type_macro_0030 road_type_macro_0031 road_type_macro_0032 road_type_macro_0033 road_type_macro_0034 road_type_macro_0035 road_type_macro_0036 road_type_macro_0037 road_type_macro_0038 road_type_macro_0039 road_type_macro_0040 road_type_macro_0041 road_type_macro_0042 road_type_macro_0043 road_type_macro_0044 road_type_macro_0045 road_type_macro_0046 road_type_macro_0047 road_type_macro_0048 road_type_macro_0049 road_type_macro_0050 road_type_macro_0051 road_type_macro_0052 road_type_macro_0053 road_type_macro_0054 road_type_macro_0055 road_type_macro_0056 road_type_macro_0057 road_type_macro_0058 road_type_road_0000 road_type_road_0001 road_type_road_0002 road_type_road_0003 road_type_road_0004 road_type_road_0006 road_type_road_0007 road_type_road_0008 road_type_road_0009 road_type_road_0010 road_type_road_0011 road_type_road_0012 road_type_road_0013 road_type_road_0014 road_type_road_0015 road_type_road_0016 road_type_road_0017 road_type_road_0018 road_type_road_0019 road_type_road_0020 road_type_road_0021 road_type_road_0022 road_type_road_0023 road_type_road_0024 road_type_road_0025 road_type_road_0026 road_type_road_0027 road_type_road_0028 road_type_road_0029 road_type_road_0030 road_type_road_0031 road_type_road_0032 road_type_road_0033 road_type_road_0034 road_type_road_0035 road_type_road_0036 road_type_road_0037 road_type_road_0038 road_type_road_0039 road_type_road_0042 road_type_road_0043 road_type_road_0044 road_type_road_0045 road_type_road_0046 road_type_road_0047 road_type_road_0050 road_type_road_0051 road_type_road_0052 road_type_road_0053 road_type_road_0054 road_type_road_0056 road_type_road_0057 road_type_road_0058 road_type_road_0061 road_type_road_0062 road_type_road_0063 road_type_road_0064 road_type_road_0065 road_type_road_0066 road_type_road_0067 road_type_road_0069 road_type_road_0070 road_type_road_0071 road_type_road_0072 road_type_road_0074 road_type_road_0075 road_type_road_0076 road_type_road_0077 road_type_road_0078 road_type_road_0079 road_type_road_0080 road_type_road_0081 road_type_road_0082 road_type_road_0083 road_type_road_0084 road_type_road_0086 road_type_road_0087 road_type_road_0088 road_type_road_0089 road_type_road_0090 road_type_road_0091 road_type_road_0092 road_type_road_0093 road_type_road_0094 road_type_road_0095 road_type_road_0097 road_type_road_0098 road_type_road_0099 road_type_road_0100 road_type_road_0101 road_type_road_0102 road_type_road_0104 road_type_road_0105 road_type_road_0106 road_type_road_0107 road_type_road_0108 road_type_road_0109 road_type_road_0110 road_type_road_0111 road_type_road_0112 road_type_road_0114 road_type_road_0115 road_type_road_0116 road_type_road_0117 road_type_road_0118 road_type_road_0119 road_type_road_0120 road_type_road_0121 road_type_road_0122 road_type_road_0123 road_type_road_0124 road_type_road_0125 road_type_road_0126 road_type_road_0127 road_type_road_0128 road_type_road_0129 road_type_road_0131 road_type_road_0132 road_type_road_0133 road_type_road_0134 road_type_road_0135 road_type_road_0136 road_type_road_0137 road_type_road_0138 road_type_road_0139 road_type_road_0140 road_type_road_0141 road_type_road_0142 road_type_road_0143 road_type_road_0144 road_type_road_0145 road_type_road_0146 road_type_road_0147 road_type_road_0148 road_type_road_0149 road_type_road_0150 road_type_road_0151 road_type_road_0152 road_type_road_0153 road_type_road_0154 road_type_road_0155 road_type_road_0156 road_type_road_0157 road_type_road_0159 road_type_road_0160 road_type_road_0161 road_type_road_0162 road_type_road_0163 road_type_road_0164 road_type_road_0165 road_type_road_0166 road_type_road_0167 road_type_road_0169 road_type_road_0170 road_type_road_0171 road_type_road_0172 road_type_road_0173 road_type_road_0174 road_type_road_0175 road_type_road_0176 road_type_road_0177 road_type_road_0178 road_type_road_0179 road_type_road_0180 road_type_road_0181 road_type_road_0182 road_type_road_0183 road_type_road_0184 road_type_road_0187 road_type_road_0188 road_type_road_0189 road_type_road_0191 road_type_road_0192 road_type_road_0193 road_type_road_0194 road_type_road_0195 road_type_road_0196 road_type_road_0197 road_type_road_0198 road_type_road_0199 road_type_road_0200 road_type_road_0201 road_type_road_0202 road_type_road_0203 road_type_road_0204 road_type_road_0205 road_type_road_0206 road_type_road_0207 road_type_road_0208 road_type_road_0209 road_type_road_0210 road_type_road_0211 road_type_road_0212 road_type_road_0213 road_type_road_0214 road_type_road_0215 road_type_road_0216 road_type_road_0217 road_type_road_0219 road_type_road_0220 road_type_road_0221 road_type_road_0222 road_type_road_0223 road_type_road_0224 road_type_road_0225 road_type_road_0227 road_type_road_0228 road_type_road_0230 road_type_road_0232 road_type_road_0233 road_type_road_0234 road_type_road_0235 road_type_road_0236 road_type_road_0237 road_type_road_0238 road_type_road_0239 road_type_road_0240 road_type_road_0241 road_type_road_0242 road_type_road_0243 road_type_road_0244 road_type_road_0245 road_type_road_0246 road_type_road_0247 road_type_road_0248 road_type_road_0249 road_type_road_0250 road_type_road_0251 road_type_road_0252 road_type_road_0253 road_type_road_0254 road_type_road_0257 road_type_road_0258 road_type_road_0259 road_type_road_0260 road_type_road_0261 road_type_road_0262 road_type_road_0263 road_type_road_0264 road_type_road_0265 road_type_road_0266 road_type_road_0267 road_type_road_0268 road_type_road_0269 road_type_road_0270 road_type_road_0271 road_type_road_0272 road_type_road_0273 road_type_road_0274 road_type_road_0275 road_type_road_0276 road_type_road_0277 road_type_road_0280 road_type_road_0281 road_type_road_0282 road_type_road_0283 road_type_road_0286 road_type_road_0287 road_type_road_0288 road_type_road_0289 road_type_road_0290 road_type_road_0291 road_type_road_0292 road_type_road_0294 road_type_road_0295 road_type_road_0296 road_type_road_0297 road_type_road_0298 road_type_road_0299 road_type_road_0300 road_type_road_0301 road_type_road_0302 road_type_road_0303 road_type_road_0304 road_type_road_0305 road_type_road_0307 road_type_road_0309 road_type_road_0310 road_type_road_0311 road_type_road_0312 road_type_road_0313 road_type_road_0314 road_type_road_0315 road_type_road_0316 road_type_road_0317 road_type_road_0318 road_type_road_0319 road_type_road_0320 road_type_road_0321 road_type_road_0322 road_type_road_0323 road_type_road_0324 road_type_road_0325 road_type_road_0326 road_type_road_0327 road_type_road_0328 road_type_road_0329 road_type_road_0330 road_type_road_0332 road_type_road_0333 road_type_road_0334 road_type_road_0335 road_type_road_0336 road_type_road_0337 road_type_road_0338 road_type_road_0339 road_type_road_0340 road_type_road_0341 road_type_road_0342 road_type_road_0343 road_type_road_0344 road_type_road_0345 road_type_road_0346 road_type_road_0347 road_type_road_0348 road_type_road_0349 road_type_road_0350 road_type_road_0351 road_type_road_0352 road_type_road_0354 road_type_road_0355 road_type_road_0356 road_type_road_0357 road_type_road_0358 road_type_road_0359 road_type_road_0360 road_type_road_0361 road_type_road_0362 road_type_road_0364 road_type_road_0365 road_type_road_0367 road_type_road_0369 road_type_road_0370 road_type_road_0371 road_type_road_0372 road_type_road_0373 road_type_road_0374 road_type_road_0375 road_type_road_0376 road_type_road_0377 road_type_road_0378 road_type_road_0379 road_type_road_0380 road_type_road_0381 road_type_road_0382 road_type_road_0383 road_type_road_0384 road_type_road_0385 road_type_road_0386 road_type_road_0387 road_type_road_0388 road_type_road_0389 road_type_road_0390 road_type_road_0391 road_type_road_0392 road_type_road_0393 road_type_road_0394 road_type_road_0395 road_type_road_0396 road_type_road_0397 road_type_road_0398 road_type_road_0399 road_type_road_0400 road_type_road_0401 road_type_road_0403 road_type_road_0404 road_type_road_0405 road_type_road_0406 road_type_road_0407 road_type_road_0408 road_type_road_0410 road_type_road_0411 road_type_road_0412 road_type_road_0413 road_type_road_0414 road_type_road_0416 road_type_road_0417 road_type_road_0418 road_type_road_0419 road_type_road_0420 road_type_road_0421 road_type_road_0422 road_type_road_0423 road_type_road_0424 road_type_road_0425 road_type_road_0426 road_type_road_0427 road_type_road_0428 road_type_road_0430 road_type_road_0433 road_type_road_0434 road_type_road_0436 road_type_road_0437 road_type_road_0438 road_type_road_0439 road_type_road_0440 road_type_road_0441 road_type_road_0442 road_type_road_0443 road_type_road_0444 road_type_road_0445 road_type_road_0446 road_type_road_0447 road_type_road_0448 road_type_road_0449 road_type_road_0450 road_type_road_0451 road_type_road_0452 road_type_road_0453 road_type_road_0454 road_type_road_0455 road_type_road_0456 road_type_road_0457 road_type_road_0458 road_type_road_0459 road_type_road_0460 road_type_road_0461 road_type_road_0462 road_type_road_0463 road_type_road_0464 road_type_road_0465 road_type_road_0466 road_type_road_0467 road_type_road_0468 road_type_road_0469 road_type_road_0470 road_type_road_0471 road_type_road_0472 road_type_road_0473 road_type_road_0475 road_type_road_0476 road_type_road_0479 road_type_road_0480 road_type_road_0481 road_type_road_0482 road_type_road_0483 road_type_road_0484 road_type_road_0485 road_type_road_0486 road_type_road_0487 road_type_road_0488 road_type_road_0490 road_type_road_0491 road_type_road_0492 road_type_road_0493 road_type_road_0494 road_type_road_0495 road_type_road_0496 road_type_road_0497 road_type_road_0498 road_type_road_0499 road_type_road_0500 road_type_road_0501 road_type_road_0502 road_type_road_0505 road_type_road_0506 road_type_road_0507 road_type_road_0508 road_type_road_0511 road_type_road_0512 road_type_road_0513 road_type_road_0514 road_type_road_0515 road_type_road_0516 road_type_road_0517 road_type_road_0518 road_type_road_0519 road_type_road_0520 road_type_road_0521 road_type_road_0522 road_type_road_0523 road_type_road_0524 road_type_road_0525 road_type_road_0526 road_type_road_0527 road_type_road_0529 road_type_road_0530 road_type_road_0532 road_type_road_0533 road_type_road_0534 road_type_road_0535 road_type_road_0536 road_type_road_0537 road_type_road_0539 road_type_road_0540 road_type_road_0541 road_type_road_0542 road_type_road_0543 road_type_road_0544 road_type_road_0545 road_type_road_0546 road_type_road_0547 road_type_road_0548 road_type_road_0549 road_type_road_0550 road_type_road_0551 road_type_road_0552 road_type_road_0553 road_type_road_0554 road_type_road_0555 road_type_road_0556 road_type_road_0557 road_type_road_0558 road_type_road_0559 road_type_road_0561 road_type_road_0563 road_type_road_0564 road_type_road_0566 road_type_road_0567 road_type_road_0568 road_type_road_0569 road_type_road_0572 road_type_road_0573 road_type_road_0574 road_type_road_0575 road_type_road_0576 road_type_road_0577 road_type_road_0578 road_type_road_0579 road_type_road_0580 road_type_road_0581 road_type_road_0582 road_type_road_0583 road_type_road_0584 road_type_road_0586 road_type_road_0587 road_type_road_0588 road_type_road_0589 road_type_road_0590 road_type_road_0591 road_type_road_0592 road_type_road_0593 road_type_road_0594 road_type_road_0595 road_type_road_0596 road_type_road_0598 road_type_road_0599 road_type_road_0600 road_type_road_0601 road_type_road_0602 road_type_road_0603 road_type_road_0604 road_type_road_0606 road_type_road_0607 road_type_road_0608 road_type_road_0609 road_type_road_0610 road_type_road_0611 road_type_road_0612 road_type_road_0614 road_type_road_0615 road_type_road_0616 road_type_road_0617 road_type_road_0618 road_type_road_0619 road_type_road_0620 road_type_road_0621 road_type_road_0622 road_type_road_0623 road_type_road_0624 road_type_road_0625 road_type_road_0626 road_type_road_0627 road_type_road_0628 road_type_road_0629 road_type_road_0630 road_type_road_0632 road_type_road_0633 road_type_road_0634 road_type_road_0635 road_type_road_0636 road_type_road_0637 road_type_road_0639 road_type_road_0640 road_type_road_0641 road_type_road_0642 road_type_road_0643 road_type_road_0644 road_type_road_0645 road_type_road_0647 road_type_road_0648 road_type_road_0649 road_type_road_0650 road_type_road_0651 road_type_road_0652 road_type_road_0653 road_type_road_0654 road_type_road_0655 road_type_road_0656 road_type_road_0657 road_type_road_0658 road_type_road_0659 road_type_road_0661 road_type_road_0662 road_type_road_0664 road_type_road_0665 road_type_road_0666 road_type_road_0667 road_type_road_0668 road_type_road_0669 road_type_road_0670 road_type_road_0671 road_type_road_0672 road_type_road_0673 road_type_road_0674 road_type_road_0675 road_type_road_0676 road_type_road_0677 road_type_road_0678 road_type_road_0679 road_type_road_0680 road_type_road_0681 road_type_road_0682 road_type_road_0683 road_type_road_0684 road_type_road_0685 road_type_road_0686 road_type_road_0687 road_type_road_0688 road_type_road_0689 road_type_road_0690 road_type_road_0691 road_type_road_0692 road_type_road_0693 road_type_road_0694 road_type_road_0696 road_type_road_0697 road_type_road_0698 road_type_road_0700 road_type_road_0701 road_type_road_0702 road_type_road_0703 road_type_road_0704 road_type_road_0705 road_type_road_0706 road_type_road_0707 road_type_road_0708 road_type_road_0709 road_type_road_0711 road_type_road_0712 road_type_road_0713 road_type_road_0714 road_type_road_0715 road_type_road_0716 road_type_road_0717 road_type_road_0718 road_type_road_0719 road_type_road_0720 road_type_road_0722 road_type_road_0723 road_type_road_0724 road_type_road_0725 road_type_road_0726 road_type_road_0727 road_type_road_0728 road_type_road_0729 road_type_road_0730 road_type_road_0732 road_type_road_0733 road_type_road_0734 road_type_road_0735 road_type_road_0736 road_type_road_0738 road_type_road_0739 road_type_road_0740 road_type_road_0741 road_type_road_0742 road_type_road_0743 road_type_road_0744 road_type_road_0745 road_type_road_0746 road_type_road_0747 road_type_road_0749 road_type_road_0750 road_type_road_0751 road_type_road_0752 road_type_road_0753 road_type_road_0754 road_type_road_0756 road_type_road_0757 road_type_road_0758 road_type_road_0759 road_type_road_0760 road_type_road_0761 road_type_road_0762 road_type_road_0763 road_type_road_0765 road_type_road_0766 road_type_road_0767 road_type_road_0768 road_type_road_0769 road_type_road_0770 road_type_road_0771 road_type_road_0772 road_type_road_0773 road_type_road_0774 road_type_road_0775 road_type_road_0776 road_type_road_0777 road_type_road_0778 road_type_road_0779 road_type_road_0780 road_type_road_0781 road_type_road_0782 road_type_road_0783 road_type_road_0784 road_type_road_0785 road_type_road_0786 road_type_road_0787 road_type_road_0789 road_type_road_0790 road_type_road_0791 road_type_road_0793 road_type_road_0794 road_type_road_0795 road_type_road_0796 road_type_road_0797 road_type_road_0798 road_type_road_0799 road_type_road_0801 road_type_road_0802 road_type_road_0803 road_type_road_0804 road_type_road_0805 road_type_road_0806 road_type_road_0807 road_type_road_0808 road_type_road_0809 road_type_road_0810 road_type_road_0811 road_type_road_0812 road_type_road_0813 road_type_road_0814 road_type_road_0815 road_type_road_0816 road_type_road_0817 road_type_road_0818 road_type_road_0819 road_type_road_0820 road_type_road_0821 road_type_road_0822 road_type_road_0823 road_type_road_0824 road_type_road_0825 road_type_road_0826 road_type_road_0827 road_type_road_0828 road_type_road_0834 road_type_road_0835 road_type_road_0836 road_type_road_0838 road_type_road_0839 road_type_road_0843 road_type_road_0844 road_type_road_0845 road_type_road_0846 road_type_road_0847 road_type_road_0848 road_type_road_0849 road_type_road_0851 road_type_road_0852 road_type_road_0853 road_type_road_0854 road_type_road_0855 road_type_road_0856 road_type_road_0857 road_type_road_0858 road_type_road_0859 road_type_road_0860 road_type_road_0861
    - road
    window_type_tw_00000 window_type_tw_00030 window_type_tw_00060 window_type_tw_00090 window_type_tw_00120 window_type_tw_00150 window_type_tw_00180 window_type_tw_00210 window_type_tw_00240 window_type_tw_00270 window_type_tw_00300 window_type_tw_00330 window_type_tw_00360 window_type_tw_00390 window_type_tw_00420 window_type_tw_00450 window_type_tw_00480 window_type_tw_00510 window_type_tw_00540 window_type_tw_00570 window_type_tw_00600
    - time-window
  )

  (:predicates
    (connects  ?r - road  ?from - location  ?to - location)
    (road-open ?r - road)
    (has-traffic-light ?l - location)
    (ready-road ?v - vehicle ?r - road)
    (road-next ?r - road ?next - road)
    (goal-road ?r - road)
    (reached-goal ?v - vehicle)
    (static-road  ?r - road)
    (dynamic-road ?r - road)
    (current-window ?w - time-window)
    (next-window ?from - time-window ?to - time-window)
    (road-blocked ?r - road)   ;; set by LLM event generator
    (location-blocked ?l - location)   ;; derived from blocked roads
    (at       ?v - vehicle  ?l - location)
  )

  (:functions
    (travel-duration      ?r - road)  ;; precomputed seconds to traverse
    (travel-time          ?v - vehicle)
    (congestion-factor    ?r - road)  ;; 1.0=free, 2.0=half speed
    (sim-time)
    (window-start         ?w - time-window)
    (travel-duration-window ?r - road ?w - time-window)
  )

  (:action traverse-road-static-macro_0000-macro_0033
    :parameters (?v - vehicle ?r - road_type_macro_0000 ?next - road_type_macro_0033)
    :precondition (and
      (ready-road ?v ?r)
      (road-next ?r ?next)
      (road-open ?r)
      (static-road ?r)
      (not (road-blocked ?r))
    )
    :effect (and
      (not (ready-road ?v ?r))
      (ready-road ?v ?next)
      (increase (travel-time ?v) (travel-duration ?r))
      (increase (sim-time) (travel-duration ?r))
    )
  )

  (:action traverse-road-static-macro_0000-road_0428
    :parameters (?v - vehicle ?r - road_type_macro_0000 ?next - road_type_road_0428)
    :precondition (and
      (ready-road ?v ?r)
      (road-next ?r ?next)
      (road-open ?r)
      (static-road ?r)
      (not (road-blocked ?r))
    )
    :effect (and
      (not (ready-road ?v ?r))
      (ready-road ?v ?next)
      (increase (travel-time ?v) (travel-duration ?r))
      (increase (sim-time) (travel-duration ?r))
    )
  )

  (:action traverse-road-static-macro_0000-road_0430
    :parameters (?v - vehicle ?r - road_type_macro_0000 ?next - road_type_road_0430)
    :precondition (and
      (ready-road ?v ?r)
      (road-next ?r ?next)
      (road-open ?r)
      (static-road ?r)
      (not (road-blocked ?r))
    )
    :effect (and
      (not (ready-road ?v ?r))
      (ready-road ?v ?next)
      (increase (travel-time ?v) (travel-duration ?r))
      (increase (sim-time) (travel-duration ?r))
    )
  )

  (:action traverse-road-static-macro_0001-road_0022
    :parameters (?v - vehicle ?r - road_type_macro_0001 ?next - road_type_road_0022)
    :precondition (and
      (ready-road ?v ?r)
      (road-next ?r ?next)
      (road-open ?r)
      (static-road ?r)
      (not (road-blocked ?r))
    )
    :effect (and
      (not (ready-road ?v ?r))
      (ready-road ?v ?next)
      (increase (travel-time ?v) (travel-duration ?r))
      (increase (sim-time) (travel-duration ?r))
    )
  )

  (:action traverse-road-static-macro_0001-road_0023
    :parameters (?v - vehicle ?r - road_type_macro_0001 ?next - road_type_road_0023)
    :precondition (and
      (ready-road ?v ?r)
      (road-next ?r ?next)
      (road-open ?r)
      (static-road ?r)
      (not (road-blocked ?r))
    )
    :effect (and
      (not (ready-road ?v ?r))
      (ready-road ?v ?next)
      (increase (travel-time ?v) (travel-duration ?r))
      (increase (sim-time) (travel-duration ?r))
    )
  )

  (:action traverse-road-static-macro_0002-road_0357
    :parameters (?v - vehicle ?r - road_type_macro_0002 ?next - road_type_road_0357)
    :precondition (and
      (ready-road ?v ?r)
      (road-next ?r ?next)
      (road-open ?r)
      (static-road ?r)
      (not (road-blocked ?r))
    )
    :effect (and
      (not (ready-road ?v ?r))
      (ready-road ?v ?next)
      (increase (travel-time ?v) (travel-duration ?r))
      (increase (sim-time) (travel-duration ?r))
    )
  )

  (:action traverse-road-static-macro_0002-road_0358
    :parameters (?v - vehicle ?r - road_type_macro_0002 ?next - road_type_road_0358)
    :precondition (and
      (ready-road ?v ?r)
      (road-next ?r ?next)
      (road-open ?r)
      (static-road ?r)
      (not (road-blocked ?r))
    )
    :effect (and
      (not (ready-road ?v ?r))
      (ready-road ?v ?next)
      (increase (travel-time ?v) (travel-duration ?r))
      (increase (sim-time) (travel-duration ?r))
    )
  )

  (:action traverse-road-dynamic-macro_0003-road_0449-tw_00000
    :parameters (?v - vehicle ?r - road_type_macro_0003 ?next - road_type_road_0449 ?w - window_type_tw_00000)
    :precondition (and
      (ready-road ?v ?r)
      (road-next ?r ?next)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
    )
    :effect (and
      (not (ready-road ?v ?r))
      (ready-road ?v ?next)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-macro_0003-road_0449-tw_00030
    :parameters (?v - vehicle ?r - road_type_macro_0003 ?next - road_type_road_0449 ?w - window_type_tw_00030)
    :precondition (and
      (ready-road ?v ?r)
      (road-next ?r ?next)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
    )
    :effect (and
      (not (ready-road ?v ?r))
      (ready-road ?v ?next)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-macro_0003-road_0449-tw_00060
    :parameters (?v - vehicle ?r - road_type_macro_0003 ?next - road_type_road_0449 ?w - window_type_tw_00060)
    :precondition (and
      (ready-road ?v ?r)
      (road-next ?r ?next)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
    )
    :effect (and
      (not (ready-road ?v ?r))
      (ready-road ?v ?next)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-macro_0003-road_0449-tw_00090
    :parameters (?v - vehicle ?r - road_type_macro_0003 ?next - road_type_road_0449 ?w - window_type_tw_00090)
    :precondition (and
      (ready-road ?v ?r)
      (road-next ?r ?next)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
    )
    :effect (and
      (not (ready-road ?v ?r))
      (ready-road ?v ?next)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-macro_0003-road_0449-tw_00120
    :parameters (?v - vehicle ?r - road_type_macro_0003 ?next - road_type_road_0449 ?w - window_type_tw_00120)
    :precondition (and
      (ready-road ?v ?r)
      (road-next ?r ?next)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
    )
    :effect (and
      (not (ready-road ?v ?r))
      (ready-road ?v ?next)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-macro_0003-road_0449-tw_00150
    :parameters (?v - vehicle ?r - road_type_macro_0003 ?next - road_type_road_0449 ?w - window_type_tw_00150)
    :precondition (and
      (ready-road ?v ?r)
      (road-next ?r ?next)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
    )
    :effect (and
      (not (ready-road ?v ?r))
      (ready-road ?v ?next)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-macro_0003-road_0449-tw_00180
    :parameters (?v - vehicle ?r - road_type_macro_0003 ?next - road_type_road_0449 ?w - window_type_tw_00180)
    :precondition (and
      (ready-road ?v ?r)
      (road-next ?r ?next)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
    )
    :effect (and
      (not (ready-road ?v ?r))
      (ready-road ?v ?next)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-macro_0003-road_0449-tw_00210
    :parameters (?v - vehicle ?r - road_type_macro_0003 ?next - road_type_road_0449 ?w - window_type_tw_00210)
    :precondition (and
      (ready-road ?v ?r)
      (road-next ?r ?next)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
    )
    :effect (and
      (not (ready-road ?v ?r))
      (ready-road ?v ?next)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-macro_0003-road_0449-tw_00240
    :parameters (?v - vehicle ?r - road_type_macro_0003 ?next - road_type_road_0449 ?w - window_type_tw_00240)
    :precondition (and
      (ready-road ?v ?r)
      (road-next ?r ?next)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
    )
    :effect (and
      (not (ready-road ?v ?r))
      (ready-road ?v ?next)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-macro_0003-road_0449-tw_00270
    :parameters (?v - vehicle ?r - road_type_macro_0003 ?next - road_type_road_0449 ?w - window_type_tw_00270)
    :precondition (and
      (ready-road ?v ?r)
      (road-next ?r ?next)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
    )
    :effect (and
      (not (ready-road ?v ?r))
      (ready-road ?v ?next)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-macro_0003-road_0449-tw_00300
    :parameters (?v - vehicle ?r - road_type_macro_0003 ?next - road_type_road_0449 ?w - window_type_tw_00300)
    :precondition (and
      (ready-road ?v ?r)
      (road-next ?r ?next)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
    )
    :effect (and
      (not (ready-road ?v ?r))
      (ready-road ?v ?next)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-macro_0003-road_0449-tw_00330
    :parameters (?v - vehicle ?r - road_type_macro_0003 ?next - road_type_road_0449 ?w - window_type_tw_00330)
    :precondition (and
      (ready-road ?v ?r)
      (road-next ?r ?next)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
    )
    :effect (and
      (not (ready-road ?v ?r))
      (ready-road ?v ?next)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-macro_0003-road_0449-tw_00360
    :parameters (?v - vehicle ?r - road_type_macro_0003 ?next - road_type_road_0449 ?w - window_type_tw_00360)
    :precondition (and
      (ready-road ?v ?r)
      (road-next ?r ?next)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
    )
    :effect (and
      (not (ready-road ?v ?r))
      (ready-road ?v ?next)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-macro_0003-road_0449-tw_00390
    :parameters (?v - vehicle ?r - road_type_macro_0003 ?next - road_type_road_0449 ?w - window_type_tw_00390)
    :precondition (and
      (ready-road ?v ?r)
      (road-next ?r ?next)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
    )
    :effect (and
      (not (ready-road ?v ?r))
      (ready-road ?v ?next)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-macro_0003-road_0449-tw_00420
    :parameters (?v - vehicle ?r - road_type_macro_0003 ?next - road_type_road_0449 ?w - window_type_tw_00420)
    :precondition (and
      (ready-road ?v ?r)
      (road-next ?r ?next)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
    )
    :effect (and
      (not (ready-road ?v ?r))
      (ready-road ?v ?next)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-macro_0003-road_0449-tw_00450
    :parameters (?v - vehicle ?r - road_type_macro_0003 ?next - road_type_road_0449 ?w - window_type_tw_00450)
    :precondition (and
      (ready-road ?v ?r)
      (road-next ?r ?next)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
    )
    :effect (and
      (not (ready-road ?v ?r))
      (ready-road ?v ?next)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-macro_0003-road_0449-tw_00480
    :parameters (?v - vehicle ?r - road_type_macro_0003 ?next - road_type_road_0449 ?w - window_type_tw_00480)
    :precondition (and
      (ready-road ?v ?r)
      (road-next ?r ?next)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
    )
    :effect (and
      (not (ready-road ?v ?r))
      (ready-road ?v ?next)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-macro_0003-road_0449-tw_00510
    :parameters (?v - vehicle ?r - road_type_macro_0003 ?next - road_type_road_0449 ?w - window_type_tw_00510)
    :precondition (and
      (ready-road ?v ?r)
      (road-next ?r ?next)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
    )
    :effect (and
      (not (ready-road ?v ?r))
      (ready-road ?v ?next)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-macro_0003-road_0449-tw_00540
    :parameters (?v - vehicle ?r - road_type_macro_0003 ?next - road_type_road_0449 ?w - window_type_tw_00540)
    :precondition (and
      (ready-road ?v ?r)
      (road-next ?r ?next)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
    )
    :effect (and
      (not (ready-road ?v ?r))
      (ready-road ?v ?next)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-macro_0003-road_0449-tw_00570
    :parameters (?v - vehicle ?r - road_type_macro_0003 ?next - road_type_road_0449 ?w - window_type_tw_00570)
    :precondition (and
      (ready-road ?v ?r)
      (road-next ?r ?next)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
    )
    :effect (and
      (not (ready-road ?v ?r))
      (ready-road ?v ?next)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-macro_0003-road_0449-tw_00600
    :parameters (?v - vehicle ?r - road_type_macro_0003 ?next - road_type_road_0449 ?w - window_type_tw_00600)
    :precondition (and
      (ready-road ?v ?r)
      (road-next ?r ?next)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
    )
    :effect (and
      (not (ready-road ?v ?r))
      (ready-road ?v ?next)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-macro_0003-road_0450-tw_00000
    :parameters (?v - vehicle ?r - road_type_macro_0003 ?next - road_type_road_0450 ?w - window_type_tw_00000)
    :precondition (and
      (ready-road ?v ?r)
      (road-next ?r ?next)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
    )
    :effect (and
      (not (ready-road ?v ?r))
      (ready-road ?v ?next)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-macro_0003-road_0450-tw_00030
    :parameters (?v - vehicle ?r - road_type_macro_0003 ?next - road_type_road_0450 ?w - window_type_tw_00030)
    :precondition (and
      (ready-road ?v ?r)
      (road-next ?r ?next)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
    )
    :effect (and
      (not (ready-road ?v ?r))
      (ready-road ?v ?next)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-macro_0003-road_0450-tw_00060
    :parameters (?v - vehicle ?r - road_type_macro_0003 ?next - road_type_road_0450 ?w - window_type_tw_00060)
    :precondition (and
      (ready-road ?v ?r)
      (road-next ?r ?next)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
    )
    :effect (and
      (not (ready-road ?v ?r))
      (ready-road ?v ?next)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-macro_0003-road_0450-tw_00090
    :parameters (?v - vehicle ?r - road_type_macro_0003 ?next - road_type_road_0450 ?w - window_type_tw_00090)
    :precondition (and
      (ready-road ?v ?r)
      (road-next ?r ?next)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
    )
    :effect (and
      (not (ready-road ?v ?r))
      (ready-road ?v ?next)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-macro_0003-road_0450-tw_00120
    :parameters (?v - vehicle ?r - road_type_macro_0003 ?next - road_type_road_0450 ?w - window_type_tw_00120)
    :precondition (and
      (ready-road ?v ?r)
      (road-next ?r ?next)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
    )
    :effect (and
      (not (ready-road ?v ?r))
      (ready-road ?v ?next)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-macro_0003-road_0450-tw_00150
    :parameters (?v - vehicle ?r - road_type_macro_0003 ?next - road_type_road_0450 ?w - window_type_tw_00150)
    :precondition (and
      (ready-road ?v ?r)
      (road-next ?r ?next)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
    )
    :effect (and
      (not (ready-road ?v ?r))
      (ready-road ?v ?next)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-macro_0003-road_0450-tw_00180
    :parameters (?v - vehicle ?r - road_type_macro_0003 ?next - road_type_road_0450 ?w - window_type_tw_00180)
    :precondition (and
      (ready-road ?v ?r)
      (road-next ?r ?next)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
    )
    :effect (and
      (not (ready-road ?v ?r))
      (ready-road ?v ?next)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-macro_0003-road_0450-tw_00210
    :parameters (?v - vehicle ?r - road_type_macro_0003 ?next - road_type_road_0450 ?w - window_type_tw_00210)
    :precondition (and
      (ready-road ?v ?r)
      (road-next ?r ?next)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
    )
    :effect (and
      (not (ready-road ?v ?r))
      (ready-road ?v ?next)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-macro_0003-road_0450-tw_00240
    :parameters (?v - vehicle ?r - road_type_macro_0003 ?next - road_type_road_0450 ?w - window_type_tw_00240)
    :precondition (and
      (ready-road ?v ?r)
      (road-next ?r ?next)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
    )
    :effect (and
      (not (ready-road ?v ?r))
      (ready-road ?v ?next)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-macro_0003-road_0450-tw_00270
    :parameters (?v - vehicle ?r - road_type_macro_0003 ?next - road_type_road_0450 ?w - window_type_tw_00270)
    :precondition (and
      (ready-road ?v ?r)
      (road-next ?r ?next)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
    )
    :effect (and
      (not (ready-road ?v ?r))
      (ready-road ?v ?next)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-macro_0003-road_0450-tw_00300
    :parameters (?v - vehicle ?r - road_type_macro_0003 ?next - road_type_road_0450 ?w - window_type_tw_00300)
    :precondition (and
      (ready-road ?v ?r)
      (road-next ?r ?next)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
    )
    :effect (and
      (not (ready-road ?v ?r))
      (ready-road ?v ?next)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-macro_0003-road_0450-tw_00330
    :parameters (?v - vehicle ?r - road_type_macro_0003 ?next - road_type_road_0450 ?w - window_type_tw_00330)
    :precondition (and
      (ready-road ?v ?r)
      (road-next ?r ?next)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
    )
    :effect (and
      (not (ready-road ?v ?r))
      (ready-road ?v ?next)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-macro_0003-road_0450-tw_00360
    :parameters (?v - vehicle ?r - road_type_macro_0003 ?next - road_type_road_0450 ?w - window_type_tw_00360)
    :precondition (and
      (ready-road ?v ?r)
      (road-next ?r ?next)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
    )
    :effect (and
      (not (ready-road ?v ?r))
      (ready-road ?v ?next)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-macro_0003-road_0450-tw_00390
    :parameters (?v - vehicle ?r - road_type_macro_0003 ?next - road_type_road_0450 ?w - window_type_tw_00390)
    :precondition (and
      (ready-road ?v ?r)
      (road-next ?r ?next)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
    )
    :effect (and
      (not (ready-road ?v ?r))
      (ready-road ?v ?next)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-macro_0003-road_0450-tw_00420
    :parameters (?v - vehicle ?r - road_type_macro_0003 ?next - road_type_road_0450 ?w - window_type_tw_00420)
    :precondition (and
      (ready-road ?v ?r)
      (road-next ?r ?next)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
    )
    :effect (and
      (not (ready-road ?v ?r))
      (ready-road ?v ?next)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-macro_0003-road_0450-tw_00450
    :parameters (?v - vehicle ?r - road_type_macro_0003 ?next - road_type_road_0450 ?w - window_type_tw_00450)
    :precondition (and
      (ready-road ?v ?r)
      (road-next ?r ?next)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
    )
    :effect (and
      (not (ready-road ?v ?r))
      (ready-road ?v ?next)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-macro_0003-road_0450-tw_00480
    :parameters (?v - vehicle ?r - road_type_macro_0003 ?next - road_type_road_0450 ?w - window_type_tw_00480)
    :precondition (and
      (ready-road ?v ?r)
      (road-next ?r ?next)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
    )
    :effect (and
      (not (ready-road ?v ?r))
      (ready-road ?v ?next)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-macro_0003-road_0450-tw_00510
    :parameters (?v - vehicle ?r - road_type_macro_0003 ?next - road_type_road_0450 ?w - window_type_tw_00510)
    :precondition (and
      (ready-road ?v ?r)
      (road-next ?r ?next)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
    )
    :effect (and
      (not (ready-road ?v ?r))
      (ready-road ?v ?next)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-macro_0003-road_0450-tw_00540
    :parameters (?v - vehicle ?r - road_type_macro_0003 ?next - road_type_road_0450 ?w - window_type_tw_00540)
    :precondition (and
      (ready-road ?v ?r)
      (road-next ?r ?next)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
    )
    :effect (and
      (not (ready-road ?v ?r))
      (ready-road ?v ?next)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-macro_0003-road_0450-tw_00570
    :parameters (?v - vehicle ?r - road_type_macro_0003 ?next - road_type_road_0450 ?w - window_type_tw_00570)
    :precondition (and
      (ready-road ?v ?r)
      (road-next ?r ?next)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
    )
    :effect (and
      (not (ready-road ?v ?r))
      (ready-road ?v ?next)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-macro_0003-road_0450-tw_00600
    :parameters (?v - vehicle ?r - road_type_macro_0003 ?next - road_type_road_0450 ?w - window_type_tw_00600)
    :precondition (and
      (ready-road ?v ?r)
      (road-next ?r ?next)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
    )
    :effect (and
      (not (ready-road ?v ?r))
      (ready-road ?v ?next)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-static-macro_0004-road_0242
    :parameters (?v - vehicle ?r - road_type_macro_0004 ?next - road_type_road_0242)
    :precondition (and
      (ready-road ?v ?r)
      (road-next ?r ?next)
      (road-open ?r)
      (static-road ?r)
      (not (road-blocked ?r))
    )
    :effect (and
      (not (ready-road ?v ?r))
      (ready-road ?v ?next)
      (increase (travel-time ?v) (travel-duration ?r))
      (increase (sim-time) (travel-duration ?r))
    )
  )

  (:action traverse-road-static-macro_0004-road_0243
    :parameters (?v - vehicle ?r - road_type_macro_0004 ?next - road_type_road_0243)
    :precondition (and
      (ready-road ?v ?r)
      (road-next ?r ?next)
      (road-open ?r)
      (static-road ?r)
      (not (road-blocked ?r))
    )
    :effect (and
      (not (ready-road ?v ?r))
      (ready-road ?v ?next)
      (increase (travel-time ?v) (travel-duration ?r))
      (increase (sim-time) (travel-duration ?r))
    )
  )

  (:action traverse-road-static-macro_0005-road_0266
    :parameters (?v - vehicle ?r - road_type_macro_0005 ?next - road_type_road_0266)
    :precondition (and
      (ready-road ?v ?r)
      (road-next ?r ?next)
      (road-open ?r)
      (static-road ?r)
      (not (road-blocked ?r))
    )
    :effect (and
      (not (ready-road ?v ?r))
      (ready-road ?v ?next)
      (increase (travel-time ?v) (travel-duration ?r))
      (increase (sim-time) (travel-duration ?r))
    )
  )

  (:action traverse-road-static-macro_0006-macro_0007
    :parameters (?v - vehicle ?r - road_type_macro_0006 ?next - road_type_macro_0007)
    :precondition (and
      (ready-road ?v ?r)
      (road-next ?r ?next)
      (road-open ?r)
      (static-road ?r)
      (not (road-blocked ?r))
    )
    :effect (and
      (not (ready-road ?v ?r))
      (ready-road ?v ?next)
      (increase (travel-time ?v) (travel-duration ?r))
      (increase (sim-time) (travel-duration ?r))
    )
  )

  (:action traverse-road-static-macro_0006-road_0072
    :parameters (?v - vehicle ?r - road_type_macro_0006 ?next - road_type_road_0072)
    :precondition (and
      (ready-road ?v ?r)
      (road-next ?r ?next)
      (road-open ?r)
      (static-road ?r)
      (not (road-blocked ?r))
    )
    :effect (and
      (not (ready-road ?v ?r))
      (ready-road ?v ?next)
      (increase (travel-time ?v) (travel-duration ?r))
      (increase (sim-time) (travel-duration ?r))
    )
  )

  (:action traverse-road-static-macro_0006-road_0074
    :parameters (?v - vehicle ?r - road_type_macro_0006 ?next - road_type_road_0074)
    :precondition (and
      (ready-road ?v ?r)
      (road-next ?r ?next)
      (road-open ?r)
      (static-road ?r)
      (not (road-blocked ?r))
    )
    :effect (and
      (not (ready-road ?v ?r))
      (ready-road ?v ?next)
      (increase (travel-time ?v) (travel-duration ?r))
      (increase (sim-time) (travel-duration ?r))
    )
  )

  (:action traverse-road-static-macro_0007-macro_0006
    :parameters (?v - vehicle ?r - road_type_macro_0007 ?next - road_type_macro_0006)
    :precondition (and
      (ready-road ?v ?r)
      (road-next ?r ?next)
      (road-open ?r)
      (static-road ?r)
      (not (road-blocked ?r))
    )
    :effect (and
      (not (ready-road ?v ?r))
      (ready-road ?v ?next)
      (increase (travel-time ?v) (travel-duration ?r))
      (increase (sim-time) (travel-duration ?r))
    )
  )

  (:action traverse-road-static-macro_0007-road_0066
    :parameters (?v - vehicle ?r - road_type_macro_0007 ?next - road_type_road_0066)
    :precondition (and
      (ready-road ?v ?r)
      (road-next ?r ?next)
      (road-open ?r)
      (static-road ?r)
      (not (road-blocked ?r))
    )
    :effect (and
      (not (ready-road ?v ?r))
      (ready-road ?v ?next)
      (increase (travel-time ?v) (travel-duration ?r))
      (increase (sim-time) (travel-duration ?r))
    )
  )

  (:action traverse-road-static-macro_0007-road_0067
    :parameters (?v - vehicle ?r - road_type_macro_0007 ?next - road_type_road_0067)
    :precondition (and
      (ready-road ?v ?r)
      (road-next ?r ?next)
      (road-open ?r)
      (static-road ?r)
      (not (road-blocked ?r))
    )
    :effect (and
      (not (ready-road ?v ?r))
      (ready-road ?v ?next)
      (increase (travel-time ?v) (travel-duration ?r))
      (increase (sim-time) (travel-duration ?r))
    )
  )

  (:action traverse-road-static-macro_0008-road_0093
    :parameters (?v - vehicle ?r - road_type_macro_0008 ?next - road_type_road_0093)
    :precondition (and
      (ready-road ?v ?r)
      (road-next ?r ?next)
      (road-open ?r)
      (static-road ?r)
      (not (road-blocked ?r))
    )
    :effect (and
      (not (ready-road ?v ?r))
      (ready-road ?v ?next)
      (increase (travel-time ?v) (travel-duration ?r))
      (increase (sim-time) (travel-duration ?r))
    )
  )

  (:action traverse-road-static-macro_0008-road_0094
    :parameters (?v - vehicle ?r - road_type_macro_0008 ?next - road_type_road_0094)
    :precondition (and
      (ready-road ?v ?r)
      (road-next ?r ?next)
      (road-open ?r)
      (static-road ?r)
      (not (road-blocked ?r))
    )
    :effect (and
      (not (ready-road ?v ?r))
      (ready-road ?v ?next)
      (increase (travel-time ?v) (travel-duration ?r))
      (increase (sim-time) (travel-duration ?r))
    )
  )

  (:action traverse-road-static-macro_0009-road_0642
    :parameters (?v - vehicle ?r - road_type_macro_0009 ?next - road_type_road_0642)
    :precondition (and
      (ready-road ?v ?r)
      (road-next ?r ?next)
      (road-open ?r)
      (static-road ?r)
      (not (road-blocked ?r))
    )
    :effect (and
      (not (ready-road ?v ?r))
      (ready-road ?v ?next)
      (increase (travel-time ?v) (travel-duration ?r))
      (increase (sim-time) (travel-duration ?r))
    )
  )

  (:action traverse-road-static-macro_0010-macro_0018
    :parameters (?v - vehicle ?r - road_type_macro_0010 ?next - road_type_macro_0018)
    :precondition (and
      (ready-road ?v ?r)
      (road-next ?r ?next)
      (road-open ?r)
      (static-road ?r)
      (not (road-blocked ?r))
    )
    :effect (and
      (not (ready-road ?v ?r))
      (ready-road ?v ?next)
      (increase (travel-time ?v) (travel-duration ?r))
      (increase (sim-time) (travel-duration ?r))
    )
  )

  (:action traverse-road-static-macro_0010-road_0232
    :parameters (?v - vehicle ?r - road_type_macro_0010 ?next - road_type_road_0232)
    :precondition (and
      (ready-road ?v ?r)
      (road-next ?r ?next)
      (road-open ?r)
      (static-road ?r)
      (not (road-blocked ?r))
    )
    :effect (and
      (not (ready-road ?v ?r))
      (ready-road ?v ?next)
      (increase (travel-time ?v) (travel-duration ?r))
      (increase (sim-time) (travel-duration ?r))
    )
  )

  (:action traverse-road-static-macro_0011-road_0106
    :parameters (?v - vehicle ?r - road_type_macro_0011 ?next - road_type_road_0106)
    :precondition (and
      (ready-road ?v ?r)
      (road-next ?r ?next)
      (road-open ?r)
      (static-road ?r)
      (not (road-blocked ?r))
    )
    :effect (and
      (not (ready-road ?v ?r))
      (ready-road ?v ?next)
      (increase (travel-time ?v) (travel-duration ?r))
      (increase (sim-time) (travel-duration ?r))
    )
  )

  (:action traverse-road-static-macro_0011-road_0107
    :parameters (?v - vehicle ?r - road_type_macro_0011 ?next - road_type_road_0107)
    :precondition (and
      (ready-road ?v ?r)
      (road-next ?r ?next)
      (road-open ?r)
      (static-road ?r)
      (not (road-blocked ?r))
    )
    :effect (and
      (not (ready-road ?v ?r))
      (ready-road ?v ?next)
      (increase (travel-time ?v) (travel-duration ?r))
      (increase (sim-time) (travel-duration ?r))
    )
  )

  (:action traverse-road-static-macro_0012-road_0793
    :parameters (?v - vehicle ?r - road_type_macro_0012 ?next - road_type_road_0793)
    :precondition (and
      (ready-road ?v ?r)
      (road-next ?r ?next)
      (road-open ?r)
      (static-road ?r)
      (not (road-blocked ?r))
    )
    :effect (and
      (not (ready-road ?v ?r))
      (ready-road ?v ?next)
      (increase (travel-time ?v) (travel-duration ?r))
      (increase (sim-time) (travel-duration ?r))
    )
  )

  (:action traverse-road-static-macro_0012-road_0794
    :parameters (?v - vehicle ?r - road_type_macro_0012 ?next - road_type_road_0794)
    :precondition (and
      (ready-road ?v ?r)
      (road-next ?r ?next)
      (road-open ?r)
      (static-road ?r)
      (not (road-blocked ?r))
    )
    :effect (and
      (not (ready-road ?v ?r))
      (ready-road ?v ?next)
      (increase (travel-time ?v) (travel-duration ?r))
      (increase (sim-time) (travel-duration ?r))
    )
  )

  (:action traverse-road-dynamic-macro_0013-road_0281-tw_00000
    :parameters (?v - vehicle ?r - road_type_macro_0013 ?next - road_type_road_0281 ?w - window_type_tw_00000)
    :precondition (and
      (ready-road ?v ?r)
      (road-next ?r ?next)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
    )
    :effect (and
      (not (ready-road ?v ?r))
      (ready-road ?v ?next)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-macro_0013-road_0281-tw_00030
    :parameters (?v - vehicle ?r - road_type_macro_0013 ?next - road_type_road_0281 ?w - window_type_tw_00030)
    :precondition (and
      (ready-road ?v ?r)
      (road-next ?r ?next)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
    )
    :effect (and
      (not (ready-road ?v ?r))
      (ready-road ?v ?next)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-macro_0013-road_0281-tw_00060
    :parameters (?v - vehicle ?r - road_type_macro_0013 ?next - road_type_road_0281 ?w - window_type_tw_00060)
    :precondition (and
      (ready-road ?v ?r)
      (road-next ?r ?next)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
    )
    :effect (and
      (not (ready-road ?v ?r))
      (ready-road ?v ?next)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-macro_0013-road_0281-tw_00090
    :parameters (?v - vehicle ?r - road_type_macro_0013 ?next - road_type_road_0281 ?w - window_type_tw_00090)
    :precondition (and
      (ready-road ?v ?r)
      (road-next ?r ?next)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
    )
    :effect (and
      (not (ready-road ?v ?r))
      (ready-road ?v ?next)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-macro_0013-road_0281-tw_00120
    :parameters (?v - vehicle ?r - road_type_macro_0013 ?next - road_type_road_0281 ?w - window_type_tw_00120)
    :precondition (and
      (ready-road ?v ?r)
      (road-next ?r ?next)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
    )
    :effect (and
      (not (ready-road ?v ?r))
      (ready-road ?v ?next)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-macro_0013-road_0281-tw_00150
    :parameters (?v - vehicle ?r - road_type_macro_0013 ?next - road_type_road_0281 ?w - window_type_tw_00150)
    :precondition (and
      (ready-road ?v ?r)
      (road-next ?r ?next)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
    )
    :effect (and
      (not (ready-road ?v ?r))
      (ready-road ?v ?next)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-macro_0013-road_0281-tw_00180
    :parameters (?v - vehicle ?r - road_type_macro_0013 ?next - road_type_road_0281 ?w - window_type_tw_00180)
    :precondition (and
      (ready-road ?v ?r)
      (road-next ?r ?next)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
    )
    :effect (and
      (not (ready-road ?v ?r))
      (ready-road ?v ?next)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-macro_0013-road_0281-tw_00210
    :parameters (?v - vehicle ?r - road_type_macro_0013 ?next - road_type_road_0281 ?w - window_type_tw_00210)
    :precondition (and
      (ready-road ?v ?r)
      (road-next ?r ?next)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
    )
    :effect (and
      (not (ready-road ?v ?r))
      (ready-road ?v ?next)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-macro_0013-road_0281-tw_00240
    :parameters (?v - vehicle ?r - road_type_macro_0013 ?next - road_type_road_0281 ?w - window_type_tw_00240)
    :precondition (and
      (ready-road ?v ?r)
      (road-next ?r ?next)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
    )
    :effect (and
      (not (ready-road ?v ?r))
      (ready-road ?v ?next)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-macro_0013-road_0281-tw_00270
    :parameters (?v - vehicle ?r - road_type_macro_0013 ?next - road_type_road_0281 ?w - window_type_tw_00270)
    :precondition (and
      (ready-road ?v ?r)
      (road-next ?r ?next)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
    )
    :effect (and
      (not (ready-road ?v ?r))
      (ready-road ?v ?next)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-macro_0013-road_0281-tw_00300
    :parameters (?v - vehicle ?r - road_type_macro_0013 ?next - road_type_road_0281 ?w - window_type_tw_00300)
    :precondition (and
      (ready-road ?v ?r)
      (road-next ?r ?next)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
    )
    :effect (and
      (not (ready-road ?v ?r))
      (ready-road ?v ?next)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-macro_0013-road_0281-tw_00330
    :parameters (?v - vehicle ?r - road_type_macro_0013 ?next - road_type_road_0281 ?w - window_type_tw_00330)
    :precondition (and
      (ready-road ?v ?r)
      (road-next ?r ?next)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
    )
    :effect (and
      (not (ready-road ?v ?r))
      (ready-road ?v ?next)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-macro_0013-road_0281-tw_00360
    :parameters (?v - vehicle ?r - road_type_macro_0013 ?next - road_type_road_0281 ?w - window_type_tw_00360)
    :precondition (and
      (ready-road ?v ?r)
      (road-next ?r ?next)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
    )
    :effect (and
      (not (ready-road ?v ?r))
      (ready-road ?v ?next)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-macro_0013-road_0281-tw_00390
    :parameters (?v - vehicle ?r - road_type_macro_0013 ?next - road_type_road_0281 ?w - window_type_tw_00390)
    :precondition (and
      (ready-road ?v ?r)
      (road-next ?r ?next)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
    )
    :effect (and
      (not (ready-road ?v ?r))
      (ready-road ?v ?next)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-macro_0013-road_0281-tw_00420
    :parameters (?v - vehicle ?r - road_type_macro_0013 ?next - road_type_road_0281 ?w - window_type_tw_00420)
    :precondition (and
      (ready-road ?v ?r)
      (road-next ?r ?next)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
    )
    :effect (and
      (not (ready-road ?v ?r))
      (ready-road ?v ?next)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-macro_0013-road_0281-tw_00450
    :parameters (?v - vehicle ?r - road_type_macro_0013 ?next - road_type_road_0281 ?w - window_type_tw_00450)
    :precondition (and
      (ready-road ?v ?r)
      (road-next ?r ?next)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
    )
    :effect (and
      (not (ready-road ?v ?r))
      (ready-road ?v ?next)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-macro_0013-road_0281-tw_00480
    :parameters (?v - vehicle ?r - road_type_macro_0013 ?next - road_type_road_0281 ?w - window_type_tw_00480)
    :precondition (and
      (ready-road ?v ?r)
      (road-next ?r ?next)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
    )
    :effect (and
      (not (ready-road ?v ?r))
      (ready-road ?v ?next)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-macro_0013-road_0281-tw_00510
    :parameters (?v - vehicle ?r - road_type_macro_0013 ?next - road_type_road_0281 ?w - window_type_tw_00510)
    :precondition (and
      (ready-road ?v ?r)
      (road-next ?r ?next)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
    )
    :effect (and
      (not (ready-road ?v ?r))
      (ready-road ?v ?next)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-macro_0013-road_0281-tw_00540
    :parameters (?v - vehicle ?r - road_type_macro_0013 ?next - road_type_road_0281 ?w - window_type_tw_00540)
    :precondition (and
      (ready-road ?v ?r)
      (road-next ?r ?next)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
    )
    :effect (and
      (not (ready-road ?v ?r))
      (ready-road ?v ?next)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-macro_0013-road_0281-tw_00570
    :parameters (?v - vehicle ?r - road_type_macro_0013 ?next - road_type_road_0281 ?w - window_type_tw_00570)
    :precondition (and
      (ready-road ?v ?r)
      (road-next ?r ?next)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
    )
    :effect (and
      (not (ready-road ?v ?r))
      (ready-road ?v ?next)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-macro_0013-road_0281-tw_00600
    :parameters (?v - vehicle ?r - road_type_macro_0013 ?next - road_type_road_0281 ?w - window_type_tw_00600)
    :precondition (and
      (ready-road ?v ?r)
      (road-next ?r ?next)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
    )
    :effect (and
      (not (ready-road ?v ?r))
      (ready-road ?v ?next)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-macro_0013-road_0282-tw_00000
    :parameters (?v - vehicle ?r - road_type_macro_0013 ?next - road_type_road_0282 ?w - window_type_tw_00000)
    :precondition (and
      (ready-road ?v ?r)
      (road-next ?r ?next)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
    )
    :effect (and
      (not (ready-road ?v ?r))
      (ready-road ?v ?next)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-macro_0013-road_0282-tw_00030
    :parameters (?v - vehicle ?r - road_type_macro_0013 ?next - road_type_road_0282 ?w - window_type_tw_00030)
    :precondition (and
      (ready-road ?v ?r)
      (road-next ?r ?next)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
    )
    :effect (and
      (not (ready-road ?v ?r))
      (ready-road ?v ?next)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-macro_0013-road_0282-tw_00060
    :parameters (?v - vehicle ?r - road_type_macro_0013 ?next - road_type_road_0282 ?w - window_type_tw_00060)
    :precondition (and
      (ready-road ?v ?r)
      (road-next ?r ?next)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
    )
    :effect (and
      (not (ready-road ?v ?r))
      (ready-road ?v ?next)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-macro_0013-road_0282-tw_00090
    :parameters (?v - vehicle ?r - road_type_macro_0013 ?next - road_type_road_0282 ?w - window_type_tw_00090)
    :precondition (and
      (ready-road ?v ?r)
      (road-next ?r ?next)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
    )
    :effect (and
      (not (ready-road ?v ?r))
      (ready-road ?v ?next)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-macro_0013-road_0282-tw_00120
    :parameters (?v - vehicle ?r - road_type_macro_0013 ?next - road_type_road_0282 ?w - window_type_tw_00120)
    :precondition (and
      (ready-road ?v ?r)
      (road-next ?r ?next)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
    )
    :effect (and
      (not (ready-road ?v ?r))
      (ready-road ?v ?next)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-macro_0013-road_0282-tw_00150
    :parameters (?v - vehicle ?r - road_type_macro_0013 ?next - road_type_road_0282 ?w - window_type_tw_00150)
    :precondition (and
      (ready-road ?v ?r)
      (road-next ?r ?next)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
    )
    :effect (and
      (not (ready-road ?v ?r))
      (ready-road ?v ?next)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-macro_0013-road_0282-tw_00180
    :parameters (?v - vehicle ?r - road_type_macro_0013 ?next - road_type_road_0282 ?w - window_type_tw_00180)
    :precondition (and
      (ready-road ?v ?r)
      (road-next ?r ?next)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
    )
    :effect (and
      (not (ready-road ?v ?r))
      (ready-road ?v ?next)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-macro_0013-road_0282-tw_00210
    :parameters (?v - vehicle ?r - road_type_macro_0013 ?next - road_type_road_0282 ?w - window_type_tw_00210)
    :precondition (and
      (ready-road ?v ?r)
      (road-next ?r ?next)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
    )
    :effect (and
      (not (ready-road ?v ?r))
      (ready-road ?v ?next)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-macro_0013-road_0282-tw_00240
    :parameters (?v - vehicle ?r - road_type_macro_0013 ?next - road_type_road_0282 ?w - window_type_tw_00240)
    :precondition (and
      (ready-road ?v ?r)
      (road-next ?r ?next)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
    )
    :effect (and
      (not (ready-road ?v ?r))
      (ready-road ?v ?next)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-macro_0013-road_0282-tw_00270
    :parameters (?v - vehicle ?r - road_type_macro_0013 ?next - road_type_road_0282 ?w - window_type_tw_00270)
    :precondition (and
      (ready-road ?v ?r)
      (road-next ?r ?next)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
    )
    :effect (and
      (not (ready-road ?v ?r))
      (ready-road ?v ?next)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-macro_0013-road_0282-tw_00300
    :parameters (?v - vehicle ?r - road_type_macro_0013 ?next - road_type_road_0282 ?w - window_type_tw_00300)
    :precondition (and
      (ready-road ?v ?r)
      (road-next ?r ?next)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
    )
    :effect (and
      (not (ready-road ?v ?r))
      (ready-road ?v ?next)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-macro_0013-road_0282-tw_00330
    :parameters (?v - vehicle ?r - road_type_macro_0013 ?next - road_type_road_0282 ?w - window_type_tw_00330)
    :precondition (and
      (ready-road ?v ?r)
      (road-next ?r ?next)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
    )
    :effect (and
      (not (ready-road ?v ?r))
      (ready-road ?v ?next)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-macro_0013-road_0282-tw_00360
    :parameters (?v - vehicle ?r - road_type_macro_0013 ?next - road_type_road_0282 ?w - window_type_tw_00360)
    :precondition (and
      (ready-road ?v ?r)
      (road-next ?r ?next)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
    )
    :effect (and
      (not (ready-road ?v ?r))
      (ready-road ?v ?next)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-macro_0013-road_0282-tw_00390
    :parameters (?v - vehicle ?r - road_type_macro_0013 ?next - road_type_road_0282 ?w - window_type_tw_00390)
    :precondition (and
      (ready-road ?v ?r)
      (road-next ?r ?next)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
    )
    :effect (and
      (not (ready-road ?v ?r))
      (ready-road ?v ?next)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-macro_0013-road_0282-tw_00420
    :parameters (?v - vehicle ?r - road_type_macro_0013 ?next - road_type_road_0282 ?w - window_type_tw_00420)
    :precondition (and
      (ready-road ?v ?r)
      (road-next ?r ?next)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
    )
    :effect (and
      (not (ready-road ?v ?r))
      (ready-road ?v ?next)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-macro_0013-road_0282-tw_00450
    :parameters (?v - vehicle ?r - road_type_macro_0013 ?next - road_type_road_0282 ?w - window_type_tw_00450)
    :precondition (and
      (ready-road ?v ?r)
      (road-next ?r ?next)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
    )
    :effect (and
      (not (ready-road ?v ?r))
      (ready-road ?v ?next)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-macro_0013-road_0282-tw_00480
    :parameters (?v - vehicle ?r - road_type_macro_0013 ?next - road_type_road_0282 ?w - window_type_tw_00480)
    :precondition (and
      (ready-road ?v ?r)
      (road-next ?r ?next)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
    )
    :effect (and
      (not (ready-road ?v ?r))
      (ready-road ?v ?next)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-macro_0013-road_0282-tw_00510
    :parameters (?v - vehicle ?r - road_type_macro_0013 ?next - road_type_road_0282 ?w - window_type_tw_00510)
    :precondition (and
      (ready-road ?v ?r)
      (road-next ?r ?next)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
    )
    :effect (and
      (not (ready-road ?v ?r))
      (ready-road ?v ?next)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-macro_0013-road_0282-tw_00540
    :parameters (?v - vehicle ?r - road_type_macro_0013 ?next - road_type_road_0282 ?w - window_type_tw_00540)
    :precondition (and
      (ready-road ?v ?r)
      (road-next ?r ?next)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
    )
    :effect (and
      (not (ready-road ?v ?r))
      (ready-road ?v ?next)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-macro_0013-road_0282-tw_00570
    :parameters (?v - vehicle ?r - road_type_macro_0013 ?next - road_type_road_0282 ?w - window_type_tw_00570)
    :precondition (and
      (ready-road ?v ?r)
      (road-next ?r ?next)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
    )
    :effect (and
      (not (ready-road ?v ?r))
      (ready-road ?v ?next)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-macro_0013-road_0282-tw_00600
    :parameters (?v - vehicle ?r - road_type_macro_0013 ?next - road_type_road_0282 ?w - window_type_tw_00600)
    :precondition (and
      (ready-road ?v ?r)
      (road-next ?r ?next)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
    )
    :effect (and
      (not (ready-road ?v ?r))
      (ready-road ?v ?next)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-static-macro_0014-macro_0042
    :parameters (?v - vehicle ?r - road_type_macro_0014 ?next - road_type_macro_0042)
    :precondition (and
      (ready-road ?v ?r)
      (road-next ?r ?next)
      (road-open ?r)
      (static-road ?r)
      (not (road-blocked ?r))
    )
    :effect (and
      (not (ready-road ?v ?r))
      (ready-road ?v ?next)
      (increase (travel-time ?v) (travel-duration ?r))
      (increase (sim-time) (travel-duration ?r))
    )
  )

  (:action traverse-road-static-macro_0014-road_0583
    :parameters (?v - vehicle ?r - road_type_macro_0014 ?next - road_type_road_0583)
    :precondition (and
      (ready-road ?v ?r)
      (road-next ?r ?next)
      (road-open ?r)
      (static-road ?r)
      (not (road-blocked ?r))
    )
    :effect (and
      (not (ready-road ?v ?r))
      (ready-road ?v ?next)
      (increase (travel-time ?v) (travel-duration ?r))
      (increase (sim-time) (travel-duration ?r))
    )
  )

  (:action traverse-road-static-macro_0014-road_0584
    :parameters (?v - vehicle ?r - road_type_macro_0014 ?next - road_type_road_0584)
    :precondition (and
      (ready-road ?v ?r)
      (road-next ?r ?next)
      (road-open ?r)
      (static-road ?r)
      (not (road-blocked ?r))
    )
    :effect (and
      (not (ready-road ?v ?r))
      (ready-road ?v ?next)
      (increase (travel-time ?v) (travel-duration ?r))
      (increase (sim-time) (travel-duration ?r))
    )
  )

  (:action traverse-road-static-macro_0015-road_0045
    :parameters (?v - vehicle ?r - road_type_macro_0015 ?next - road_type_road_0045)
    :precondition (and
      (ready-road ?v ?r)
      (road-next ?r ?next)
      (road-open ?r)
      (static-road ?r)
      (not (road-blocked ?r))
    )
    :effect (and
      (not (ready-road ?v ?r))
      (ready-road ?v ?next)
      (increase (travel-time ?v) (travel-duration ?r))
      (increase (sim-time) (travel-duration ?r))
    )
  )

  (:action traverse-road-static-macro_0015-road_0046
    :parameters (?v - vehicle ?r - road_type_macro_0015 ?next - road_type_road_0046)
    :precondition (and
      (ready-road ?v ?r)
      (road-next ?r ?next)
      (road-open ?r)
      (static-road ?r)
      (not (road-blocked ?r))
    )
    :effect (and
      (not (ready-road ?v ?r))
      (ready-road ?v ?next)
      (increase (travel-time ?v) (travel-duration ?r))
      (increase (sim-time) (travel-duration ?r))
    )
  )

  (:action traverse-road-static-macro_0016-road_0239
    :parameters (?v - vehicle ?r - road_type_macro_0016 ?next - road_type_road_0239)
    :precondition (and
      (ready-road ?v ?r)
      (road-next ?r ?next)
      (road-open ?r)
      (static-road ?r)
      (not (road-blocked ?r))
    )
    :effect (and
      (not (ready-road ?v ?r))
      (ready-road ?v ?next)
      (increase (travel-time ?v) (travel-duration ?r))
      (increase (sim-time) (travel-duration ?r))
    )
  )

  (:action traverse-road-static-macro_0017-road_0205
    :parameters (?v - vehicle ?r - road_type_macro_0017 ?next - road_type_road_0205)
    :precondition (and
      (ready-road ?v ?r)
      (road-next ?r ?next)
      (road-open ?r)
      (static-road ?r)
      (not (road-blocked ?r))
    )
    :effect (and
      (not (ready-road ?v ?r))
      (ready-road ?v ?next)
      (increase (travel-time ?v) (travel-duration ?r))
      (increase (sim-time) (travel-duration ?r))
    )
  )

  (:action traverse-road-static-macro_0018-macro_0010
    :parameters (?v - vehicle ?r - road_type_macro_0018 ?next - road_type_macro_0010)
    :precondition (and
      (ready-road ?v ?r)
      (road-next ?r ?next)
      (road-open ?r)
      (static-road ?r)
      (not (road-blocked ?r))
    )
    :effect (and
      (not (ready-road ?v ?r))
      (ready-road ?v ?next)
      (increase (travel-time ?v) (travel-duration ?r))
      (increase (sim-time) (travel-duration ?r))
    )
  )

  (:action traverse-road-static-macro_0018-road_0102
    :parameters (?v - vehicle ?r - road_type_macro_0018 ?next - road_type_road_0102)
    :precondition (and
      (ready-road ?v ?r)
      (road-next ?r ?next)
      (road-open ?r)
      (static-road ?r)
      (not (road-blocked ?r))
    )
    :effect (and
      (not (ready-road ?v ?r))
      (ready-road ?v ?next)
      (increase (travel-time ?v) (travel-duration ?r))
      (increase (sim-time) (travel-duration ?r))
    )
  )

  (:action traverse-road-static-macro_0018-road_0104
    :parameters (?v - vehicle ?r - road_type_macro_0018 ?next - road_type_road_0104)
    :precondition (and
      (ready-road ?v ?r)
      (road-next ?r ?next)
      (road-open ?r)
      (static-road ?r)
      (not (road-blocked ?r))
    )
    :effect (and
      (not (ready-road ?v ?r))
      (ready-road ?v ?next)
      (increase (travel-time ?v) (travel-duration ?r))
      (increase (sim-time) (travel-duration ?r))
    )
  )

  (:action traverse-road-static-macro_0019-road_0249
    :parameters (?v - vehicle ?r - road_type_macro_0019 ?next - road_type_road_0249)
    :precondition (and
      (ready-road ?v ?r)
      (road-next ?r ?next)
      (road-open ?r)
      (static-road ?r)
      (not (road-blocked ?r))
    )
    :effect (and
      (not (ready-road ?v ?r))
      (ready-road ?v ?next)
      (increase (travel-time ?v) (travel-duration ?r))
      (increase (sim-time) (travel-duration ?r))
    )
  )

  (:action traverse-road-static-macro_0019-road_0250
    :parameters (?v - vehicle ?r - road_type_macro_0019 ?next - road_type_road_0250)
    :precondition (and
      (ready-road ?v ?r)
      (road-next ?r ?next)
      (road-open ?r)
      (static-road ?r)
      (not (road-blocked ?r))
    )
    :effect (and
      (not (ready-road ?v ?r))
      (ready-road ?v ?next)
      (increase (travel-time ?v) (travel-duration ?r))
      (increase (sim-time) (travel-duration ?r))
    )
  )

  (:action traverse-road-static-macro_0019-road_0251
    :parameters (?v - vehicle ?r - road_type_macro_0019 ?next - road_type_road_0251)
    :precondition (and
      (ready-road ?v ?r)
      (road-next ?r ?next)
      (road-open ?r)
      (static-road ?r)
      (not (road-blocked ?r))
    )
    :effect (and
      (not (ready-road ?v ?r))
      (ready-road ?v ?next)
      (increase (travel-time ?v) (travel-duration ?r))
      (increase (sim-time) (travel-duration ?r))
    )
  )

  (:action traverse-road-static-macro_0020-road_0249
    :parameters (?v - vehicle ?r - road_type_macro_0020 ?next - road_type_road_0249)
    :precondition (and
      (ready-road ?v ?r)
      (road-next ?r ?next)
      (road-open ?r)
      (static-road ?r)
      (not (road-blocked ?r))
    )
    :effect (and
      (not (ready-road ?v ?r))
      (ready-road ?v ?next)
      (increase (travel-time ?v) (travel-duration ?r))
      (increase (sim-time) (travel-duration ?r))
    )
  )

  (:action traverse-road-static-macro_0020-road_0250
    :parameters (?v - vehicle ?r - road_type_macro_0020 ?next - road_type_road_0250)
    :precondition (and
      (ready-road ?v ?r)
      (road-next ?r ?next)
      (road-open ?r)
      (static-road ?r)
      (not (road-blocked ?r))
    )
    :effect (and
      (not (ready-road ?v ?r))
      (ready-road ?v ?next)
      (increase (travel-time ?v) (travel-duration ?r))
      (increase (sim-time) (travel-duration ?r))
    )
  )

  (:action traverse-road-static-macro_0020-road_0251
    :parameters (?v - vehicle ?r - road_type_macro_0020 ?next - road_type_road_0251)
    :precondition (and
      (ready-road ?v ?r)
      (road-next ?r ?next)
      (road-open ?r)
      (static-road ?r)
      (not (road-blocked ?r))
    )
    :effect (and
      (not (ready-road ?v ?r))
      (ready-road ?v ?next)
      (increase (travel-time ?v) (travel-duration ?r))
      (increase (sim-time) (travel-duration ?r))
    )
  )

  (:action traverse-road-dynamic-macro_0023-road_0077-tw_00000
    :parameters (?v - vehicle ?r - road_type_macro_0023 ?next - road_type_road_0077 ?w - window_type_tw_00000)
    :precondition (and
      (ready-road ?v ?r)
      (road-next ?r ?next)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
    )
    :effect (and
      (not (ready-road ?v ?r))
      (ready-road ?v ?next)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-macro_0023-road_0077-tw_00030
    :parameters (?v - vehicle ?r - road_type_macro_0023 ?next - road_type_road_0077 ?w - window_type_tw_00030)
    :precondition (and
      (ready-road ?v ?r)
      (road-next ?r ?next)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
    )
    :effect (and
      (not (ready-road ?v ?r))
      (ready-road ?v ?next)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-macro_0023-road_0077-tw_00060
    :parameters (?v - vehicle ?r - road_type_macro_0023 ?next - road_type_road_0077 ?w - window_type_tw_00060)
    :precondition (and
      (ready-road ?v ?r)
      (road-next ?r ?next)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
    )
    :effect (and
      (not (ready-road ?v ?r))
      (ready-road ?v ?next)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-macro_0023-road_0077-tw_00090
    :parameters (?v - vehicle ?r - road_type_macro_0023 ?next - road_type_road_0077 ?w - window_type_tw_00090)
    :precondition (and
      (ready-road ?v ?r)
      (road-next ?r ?next)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
    )
    :effect (and
      (not (ready-road ?v ?r))
      (ready-road ?v ?next)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-macro_0023-road_0077-tw_00120
    :parameters (?v - vehicle ?r - road_type_macro_0023 ?next - road_type_road_0077 ?w - window_type_tw_00120)
    :precondition (and
      (ready-road ?v ?r)
      (road-next ?r ?next)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
    )
    :effect (and
      (not (ready-road ?v ?r))
      (ready-road ?v ?next)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-macro_0023-road_0077-tw_00150
    :parameters (?v - vehicle ?r - road_type_macro_0023 ?next - road_type_road_0077 ?w - window_type_tw_00150)
    :precondition (and
      (ready-road ?v ?r)
      (road-next ?r ?next)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
    )
    :effect (and
      (not (ready-road ?v ?r))
      (ready-road ?v ?next)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-macro_0023-road_0077-tw_00180
    :parameters (?v - vehicle ?r - road_type_macro_0023 ?next - road_type_road_0077 ?w - window_type_tw_00180)
    :precondition (and
      (ready-road ?v ?r)
      (road-next ?r ?next)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
    )
    :effect (and
      (not (ready-road ?v ?r))
      (ready-road ?v ?next)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-macro_0023-road_0077-tw_00210
    :parameters (?v - vehicle ?r - road_type_macro_0023 ?next - road_type_road_0077 ?w - window_type_tw_00210)
    :precondition (and
      (ready-road ?v ?r)
      (road-next ?r ?next)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
    )
    :effect (and
      (not (ready-road ?v ?r))
      (ready-road ?v ?next)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-macro_0023-road_0077-tw_00240
    :parameters (?v - vehicle ?r - road_type_macro_0023 ?next - road_type_road_0077 ?w - window_type_tw_00240)
    :precondition (and
      (ready-road ?v ?r)
      (road-next ?r ?next)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
    )
    :effect (and
      (not (ready-road ?v ?r))
      (ready-road ?v ?next)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-macro_0023-road_0077-tw_00270
    :parameters (?v - vehicle ?r - road_type_macro_0023 ?next - road_type_road_0077 ?w - window_type_tw_00270)
    :precondition (and
      (ready-road ?v ?r)
      (road-next ?r ?next)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
    )
    :effect (and
      (not (ready-road ?v ?r))
      (ready-road ?v ?next)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-macro_0023-road_0077-tw_00300
    :parameters (?v - vehicle ?r - road_type_macro_0023 ?next - road_type_road_0077 ?w - window_type_tw_00300)
    :precondition (and
      (ready-road ?v ?r)
      (road-next ?r ?next)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
    )
    :effect (and
      (not (ready-road ?v ?r))
      (ready-road ?v ?next)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-macro_0023-road_0077-tw_00330
    :parameters (?v - vehicle ?r - road_type_macro_0023 ?next - road_type_road_0077 ?w - window_type_tw_00330)
    :precondition (and
      (ready-road ?v ?r)
      (road-next ?r ?next)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
    )
    :effect (and
      (not (ready-road ?v ?r))
      (ready-road ?v ?next)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-macro_0023-road_0077-tw_00360
    :parameters (?v - vehicle ?r - road_type_macro_0023 ?next - road_type_road_0077 ?w - window_type_tw_00360)
    :precondition (and
      (ready-road ?v ?r)
      (road-next ?r ?next)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
    )
    :effect (and
      (not (ready-road ?v ?r))
      (ready-road ?v ?next)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-macro_0023-road_0077-tw_00390
    :parameters (?v - vehicle ?r - road_type_macro_0023 ?next - road_type_road_0077 ?w - window_type_tw_00390)
    :precondition (and
      (ready-road ?v ?r)
      (road-next ?r ?next)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
    )
    :effect (and
      (not (ready-road ?v ?r))
      (ready-road ?v ?next)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-macro_0023-road_0077-tw_00420
    :parameters (?v - vehicle ?r - road_type_macro_0023 ?next - road_type_road_0077 ?w - window_type_tw_00420)
    :precondition (and
      (ready-road ?v ?r)
      (road-next ?r ?next)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
    )
    :effect (and
      (not (ready-road ?v ?r))
      (ready-road ?v ?next)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-macro_0023-road_0077-tw_00450
    :parameters (?v - vehicle ?r - road_type_macro_0023 ?next - road_type_road_0077 ?w - window_type_tw_00450)
    :precondition (and
      (ready-road ?v ?r)
      (road-next ?r ?next)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
    )
    :effect (and
      (not (ready-road ?v ?r))
      (ready-road ?v ?next)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-macro_0023-road_0077-tw_00480
    :parameters (?v - vehicle ?r - road_type_macro_0023 ?next - road_type_road_0077 ?w - window_type_tw_00480)
    :precondition (and
      (ready-road ?v ?r)
      (road-next ?r ?next)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
    )
    :effect (and
      (not (ready-road ?v ?r))
      (ready-road ?v ?next)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-macro_0023-road_0077-tw_00510
    :parameters (?v - vehicle ?r - road_type_macro_0023 ?next - road_type_road_0077 ?w - window_type_tw_00510)
    :precondition (and
      (ready-road ?v ?r)
      (road-next ?r ?next)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
    )
    :effect (and
      (not (ready-road ?v ?r))
      (ready-road ?v ?next)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-macro_0023-road_0077-tw_00540
    :parameters (?v - vehicle ?r - road_type_macro_0023 ?next - road_type_road_0077 ?w - window_type_tw_00540)
    :precondition (and
      (ready-road ?v ?r)
      (road-next ?r ?next)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
    )
    :effect (and
      (not (ready-road ?v ?r))
      (ready-road ?v ?next)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-macro_0023-road_0077-tw_00570
    :parameters (?v - vehicle ?r - road_type_macro_0023 ?next - road_type_road_0077 ?w - window_type_tw_00570)
    :precondition (and
      (ready-road ?v ?r)
      (road-next ?r ?next)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
    )
    :effect (and
      (not (ready-road ?v ?r))
      (ready-road ?v ?next)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-macro_0023-road_0077-tw_00600
    :parameters (?v - vehicle ?r - road_type_macro_0023 ?next - road_type_road_0077 ?w - window_type_tw_00600)
    :precondition (and
      (ready-road ?v ?r)
      (road-next ?r ?next)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
    )
    :effect (and
      (not (ready-road ?v ?r))
      (ready-road ?v ?next)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-macro_0023-road_0078-tw_00000
    :parameters (?v - vehicle ?r - road_type_macro_0023 ?next - road_type_road_0078 ?w - window_type_tw_00000)
    :precondition (and
      (ready-road ?v ?r)
      (road-next ?r ?next)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
    )
    :effect (and
      (not (ready-road ?v ?r))
      (ready-road ?v ?next)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-macro_0023-road_0078-tw_00030
    :parameters (?v - vehicle ?r - road_type_macro_0023 ?next - road_type_road_0078 ?w - window_type_tw_00030)
    :precondition (and
      (ready-road ?v ?r)
      (road-next ?r ?next)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
    )
    :effect (and
      (not (ready-road ?v ?r))
      (ready-road ?v ?next)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-macro_0023-road_0078-tw_00060
    :parameters (?v - vehicle ?r - road_type_macro_0023 ?next - road_type_road_0078 ?w - window_type_tw_00060)
    :precondition (and
      (ready-road ?v ?r)
      (road-next ?r ?next)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
    )
    :effect (and
      (not (ready-road ?v ?r))
      (ready-road ?v ?next)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-macro_0023-road_0078-tw_00090
    :parameters (?v - vehicle ?r - road_type_macro_0023 ?next - road_type_road_0078 ?w - window_type_tw_00090)
    :precondition (and
      (ready-road ?v ?r)
      (road-next ?r ?next)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
    )
    :effect (and
      (not (ready-road ?v ?r))
      (ready-road ?v ?next)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-macro_0023-road_0078-tw_00120
    :parameters (?v - vehicle ?r - road_type_macro_0023 ?next - road_type_road_0078 ?w - window_type_tw_00120)
    :precondition (and
      (ready-road ?v ?r)
      (road-next ?r ?next)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
    )
    :effect (and
      (not (ready-road ?v ?r))
      (ready-road ?v ?next)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-macro_0023-road_0078-tw_00150
    :parameters (?v - vehicle ?r - road_type_macro_0023 ?next - road_type_road_0078 ?w - window_type_tw_00150)
    :precondition (and
      (ready-road ?v ?r)
      (road-next ?r ?next)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
    )
    :effect (and
      (not (ready-road ?v ?r))
      (ready-road ?v ?next)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-macro_0023-road_0078-tw_00180
    :parameters (?v - vehicle ?r - road_type_macro_0023 ?next - road_type_road_0078 ?w - window_type_tw_00180)
    :precondition (and
      (ready-road ?v ?r)
      (road-next ?r ?next)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
    )
    :effect (and
      (not (ready-road ?v ?r))
      (ready-road ?v ?next)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-macro_0023-road_0078-tw_00210
    :parameters (?v - vehicle ?r - road_type_macro_0023 ?next - road_type_road_0078 ?w - window_type_tw_00210)
    :precondition (and
      (ready-road ?v ?r)
      (road-next ?r ?next)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
    )
    :effect (and
      (not (ready-road ?v ?r))
      (ready-road ?v ?next)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-macro_0023-road_0078-tw_00240
    :parameters (?v - vehicle ?r - road_type_macro_0023 ?next - road_type_road_0078 ?w - window_type_tw_00240)
    :precondition (and
      (ready-road ?v ?r)
      (road-next ?r ?next)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
    )
    :effect (and
      (not (ready-road ?v ?r))
      (ready-road ?v ?next)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-macro_0023-road_0078-tw_00270
    :parameters (?v - vehicle ?r - road_type_macro_0023 ?next - road_type_road_0078 ?w - window_type_tw_00270)
    :precondition (and
      (ready-road ?v ?r)
      (road-next ?r ?next)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
    )
    :effect (and
      (not (ready-road ?v ?r))
      (ready-road ?v ?next)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-macro_0023-road_0078-tw_00300
    :parameters (?v - vehicle ?r - road_type_macro_0023 ?next - road_type_road_0078 ?w - window_type_tw_00300)
    :precondition (and
      (ready-road ?v ?r)
      (road-next ?r ?next)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
    )
    :effect (and
      (not (ready-road ?v ?r))
      (ready-road ?v ?next)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-macro_0023-road_0078-tw_00330
    :parameters (?v - vehicle ?r - road_type_macro_0023 ?next - road_type_road_0078 ?w - window_type_tw_00330)
    :precondition (and
      (ready-road ?v ?r)
      (road-next ?r ?next)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
    )
    :effect (and
      (not (ready-road ?v ?r))
      (ready-road ?v ?next)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-macro_0023-road_0078-tw_00360
    :parameters (?v - vehicle ?r - road_type_macro_0023 ?next - road_type_road_0078 ?w - window_type_tw_00360)
    :precondition (and
      (ready-road ?v ?r)
      (road-next ?r ?next)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
    )
    :effect (and
      (not (ready-road ?v ?r))
      (ready-road ?v ?next)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-macro_0023-road_0078-tw_00390
    :parameters (?v - vehicle ?r - road_type_macro_0023 ?next - road_type_road_0078 ?w - window_type_tw_00390)
    :precondition (and
      (ready-road ?v ?r)
      (road-next ?r ?next)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
    )
    :effect (and
      (not (ready-road ?v ?r))
      (ready-road ?v ?next)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-macro_0023-road_0078-tw_00420
    :parameters (?v - vehicle ?r - road_type_macro_0023 ?next - road_type_road_0078 ?w - window_type_tw_00420)
    :precondition (and
      (ready-road ?v ?r)
      (road-next ?r ?next)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
    )
    :effect (and
      (not (ready-road ?v ?r))
      (ready-road ?v ?next)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-macro_0023-road_0078-tw_00450
    :parameters (?v - vehicle ?r - road_type_macro_0023 ?next - road_type_road_0078 ?w - window_type_tw_00450)
    :precondition (and
      (ready-road ?v ?r)
      (road-next ?r ?next)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
    )
    :effect (and
      (not (ready-road ?v ?r))
      (ready-road ?v ?next)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-macro_0023-road_0078-tw_00480
    :parameters (?v - vehicle ?r - road_type_macro_0023 ?next - road_type_road_0078 ?w - window_type_tw_00480)
    :precondition (and
      (ready-road ?v ?r)
      (road-next ?r ?next)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
    )
    :effect (and
      (not (ready-road ?v ?r))
      (ready-road ?v ?next)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-macro_0023-road_0078-tw_00510
    :parameters (?v - vehicle ?r - road_type_macro_0023 ?next - road_type_road_0078 ?w - window_type_tw_00510)
    :precondition (and
      (ready-road ?v ?r)
      (road-next ?r ?next)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
    )
    :effect (and
      (not (ready-road ?v ?r))
      (ready-road ?v ?next)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-macro_0023-road_0078-tw_00540
    :parameters (?v - vehicle ?r - road_type_macro_0023 ?next - road_type_road_0078 ?w - window_type_tw_00540)
    :precondition (and
      (ready-road ?v ?r)
      (road-next ?r ?next)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
    )
    :effect (and
      (not (ready-road ?v ?r))
      (ready-road ?v ?next)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-macro_0023-road_0078-tw_00570
    :parameters (?v - vehicle ?r - road_type_macro_0023 ?next - road_type_road_0078 ?w - window_type_tw_00570)
    :precondition (and
      (ready-road ?v ?r)
      (road-next ?r ?next)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
    )
    :effect (and
      (not (ready-road ?v ?r))
      (ready-road ?v ?next)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-macro_0023-road_0078-tw_00600
    :parameters (?v - vehicle ?r - road_type_macro_0023 ?next - road_type_road_0078 ?w - window_type_tw_00600)
    :precondition (and
      (ready-road ?v ?r)
      (road-next ?r ?next)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
    )
    :effect (and
      (not (ready-road ?v ?r))
      (ready-road ?v ?next)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-static-macro_0024-road_0317
    :parameters (?v - vehicle ?r - road_type_macro_0024 ?next - road_type_road_0317)
    :precondition (and
      (ready-road ?v ?r)
      (road-next ?r ?next)
      (road-open ?r)
      (static-road ?r)
      (not (road-blocked ?r))
    )
    :effect (and
      (not (ready-road ?v ?r))
      (ready-road ?v ?next)
      (increase (travel-time ?v) (travel-duration ?r))
      (increase (sim-time) (travel-duration ?r))
    )
  )

  (:action traverse-road-dynamic-macro_0027-road_0736-tw_00000
    :parameters (?v - vehicle ?r - road_type_macro_0027 ?next - road_type_road_0736 ?w - window_type_tw_00000)
    :precondition (and
      (ready-road ?v ?r)
      (road-next ?r ?next)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
    )
    :effect (and
      (not (ready-road ?v ?r))
      (ready-road ?v ?next)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-macro_0027-road_0736-tw_00030
    :parameters (?v - vehicle ?r - road_type_macro_0027 ?next - road_type_road_0736 ?w - window_type_tw_00030)
    :precondition (and
      (ready-road ?v ?r)
      (road-next ?r ?next)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
    )
    :effect (and
      (not (ready-road ?v ?r))
      (ready-road ?v ?next)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-macro_0027-road_0736-tw_00060
    :parameters (?v - vehicle ?r - road_type_macro_0027 ?next - road_type_road_0736 ?w - window_type_tw_00060)
    :precondition (and
      (ready-road ?v ?r)
      (road-next ?r ?next)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
    )
    :effect (and
      (not (ready-road ?v ?r))
      (ready-road ?v ?next)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-macro_0027-road_0736-tw_00090
    :parameters (?v - vehicle ?r - road_type_macro_0027 ?next - road_type_road_0736 ?w - window_type_tw_00090)
    :precondition (and
      (ready-road ?v ?r)
      (road-next ?r ?next)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
    )
    :effect (and
      (not (ready-road ?v ?r))
      (ready-road ?v ?next)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-macro_0027-road_0736-tw_00120
    :parameters (?v - vehicle ?r - road_type_macro_0027 ?next - road_type_road_0736 ?w - window_type_tw_00120)
    :precondition (and
      (ready-road ?v ?r)
      (road-next ?r ?next)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
    )
    :effect (and
      (not (ready-road ?v ?r))
      (ready-road ?v ?next)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-macro_0027-road_0736-tw_00150
    :parameters (?v - vehicle ?r - road_type_macro_0027 ?next - road_type_road_0736 ?w - window_type_tw_00150)
    :precondition (and
      (ready-road ?v ?r)
      (road-next ?r ?next)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
    )
    :effect (and
      (not (ready-road ?v ?r))
      (ready-road ?v ?next)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-macro_0027-road_0736-tw_00180
    :parameters (?v - vehicle ?r - road_type_macro_0027 ?next - road_type_road_0736 ?w - window_type_tw_00180)
    :precondition (and
      (ready-road ?v ?r)
      (road-next ?r ?next)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
    )
    :effect (and
      (not (ready-road ?v ?r))
      (ready-road ?v ?next)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-macro_0027-road_0736-tw_00210
    :parameters (?v - vehicle ?r - road_type_macro_0027 ?next - road_type_road_0736 ?w - window_type_tw_00210)
    :precondition (and
      (ready-road ?v ?r)
      (road-next ?r ?next)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
    )
    :effect (and
      (not (ready-road ?v ?r))
      (ready-road ?v ?next)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-macro_0027-road_0736-tw_00240
    :parameters (?v - vehicle ?r - road_type_macro_0027 ?next - road_type_road_0736 ?w - window_type_tw_00240)
    :precondition (and
      (ready-road ?v ?r)
      (road-next ?r ?next)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
    )
    :effect (and
      (not (ready-road ?v ?r))
      (ready-road ?v ?next)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-macro_0027-road_0736-tw_00270
    :parameters (?v - vehicle ?r - road_type_macro_0027 ?next - road_type_road_0736 ?w - window_type_tw_00270)
    :precondition (and
      (ready-road ?v ?r)
      (road-next ?r ?next)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
    )
    :effect (and
      (not (ready-road ?v ?r))
      (ready-road ?v ?next)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-macro_0027-road_0736-tw_00300
    :parameters (?v - vehicle ?r - road_type_macro_0027 ?next - road_type_road_0736 ?w - window_type_tw_00300)
    :precondition (and
      (ready-road ?v ?r)
      (road-next ?r ?next)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
    )
    :effect (and
      (not (ready-road ?v ?r))
      (ready-road ?v ?next)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-macro_0027-road_0736-tw_00330
    :parameters (?v - vehicle ?r - road_type_macro_0027 ?next - road_type_road_0736 ?w - window_type_tw_00330)
    :precondition (and
      (ready-road ?v ?r)
      (road-next ?r ?next)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
    )
    :effect (and
      (not (ready-road ?v ?r))
      (ready-road ?v ?next)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-macro_0027-road_0736-tw_00360
    :parameters (?v - vehicle ?r - road_type_macro_0027 ?next - road_type_road_0736 ?w - window_type_tw_00360)
    :precondition (and
      (ready-road ?v ?r)
      (road-next ?r ?next)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
    )
    :effect (and
      (not (ready-road ?v ?r))
      (ready-road ?v ?next)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-macro_0027-road_0736-tw_00390
    :parameters (?v - vehicle ?r - road_type_macro_0027 ?next - road_type_road_0736 ?w - window_type_tw_00390)
    :precondition (and
      (ready-road ?v ?r)
      (road-next ?r ?next)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
    )
    :effect (and
      (not (ready-road ?v ?r))
      (ready-road ?v ?next)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-macro_0027-road_0736-tw_00420
    :parameters (?v - vehicle ?r - road_type_macro_0027 ?next - road_type_road_0736 ?w - window_type_tw_00420)
    :precondition (and
      (ready-road ?v ?r)
      (road-next ?r ?next)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
    )
    :effect (and
      (not (ready-road ?v ?r))
      (ready-road ?v ?next)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-macro_0027-road_0736-tw_00450
    :parameters (?v - vehicle ?r - road_type_macro_0027 ?next - road_type_road_0736 ?w - window_type_tw_00450)
    :precondition (and
      (ready-road ?v ?r)
      (road-next ?r ?next)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
    )
    :effect (and
      (not (ready-road ?v ?r))
      (ready-road ?v ?next)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-macro_0027-road_0736-tw_00480
    :parameters (?v - vehicle ?r - road_type_macro_0027 ?next - road_type_road_0736 ?w - window_type_tw_00480)
    :precondition (and
      (ready-road ?v ?r)
      (road-next ?r ?next)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
    )
    :effect (and
      (not (ready-road ?v ?r))
      (ready-road ?v ?next)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-macro_0027-road_0736-tw_00510
    :parameters (?v - vehicle ?r - road_type_macro_0027 ?next - road_type_road_0736 ?w - window_type_tw_00510)
    :precondition (and
      (ready-road ?v ?r)
      (road-next ?r ?next)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
    )
    :effect (and
      (not (ready-road ?v ?r))
      (ready-road ?v ?next)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-macro_0027-road_0736-tw_00540
    :parameters (?v - vehicle ?r - road_type_macro_0027 ?next - road_type_road_0736 ?w - window_type_tw_00540)
    :precondition (and
      (ready-road ?v ?r)
      (road-next ?r ?next)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
    )
    :effect (and
      (not (ready-road ?v ?r))
      (ready-road ?v ?next)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-macro_0027-road_0736-tw_00570
    :parameters (?v - vehicle ?r - road_type_macro_0027 ?next - road_type_road_0736 ?w - window_type_tw_00570)
    :precondition (and
      (ready-road ?v ?r)
      (road-next ?r ?next)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
    )
    :effect (and
      (not (ready-road ?v ?r))
      (ready-road ?v ?next)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-macro_0027-road_0736-tw_00600
    :parameters (?v - vehicle ?r - road_type_macro_0027 ?next - road_type_road_0736 ?w - window_type_tw_00600)
    :precondition (and
      (ready-road ?v ?r)
      (road-next ?r ?next)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
    )
    :effect (and
      (not (ready-road ?v ?r))
      (ready-road ?v ?next)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-static-macro_0028-road_0235
    :parameters (?v - vehicle ?r - road_type_macro_0028 ?next - road_type_road_0235)
    :precondition (and
      (ready-road ?v ?r)
      (road-next ?r ?next)
      (road-open ?r)
      (static-road ?r)
      (not (road-blocked ?r))
    )
    :effect (and
      (not (ready-road ?v ?r))
      (ready-road ?v ?next)
      (increase (travel-time ?v) (travel-duration ?r))
      (increase (sim-time) (travel-duration ?r))
    )
  )

  (:action traverse-road-static-macro_0028-road_0236
    :parameters (?v - vehicle ?r - road_type_macro_0028 ?next - road_type_road_0236)
    :precondition (and
      (ready-road ?v ?r)
      (road-next ?r ?next)
      (road-open ?r)
      (static-road ?r)
      (not (road-blocked ?r))
    )
    :effect (and
      (not (ready-road ?v ?r))
      (ready-road ?v ?next)
      (increase (travel-time ?v) (travel-duration ?r))
      (increase (sim-time) (travel-duration ?r))
    )
  )

  (:action traverse-road-static-macro_0030-road_0247
    :parameters (?v - vehicle ?r - road_type_macro_0030 ?next - road_type_road_0247)
    :precondition (and
      (ready-road ?v ?r)
      (road-next ?r ?next)
      (road-open ?r)
      (static-road ?r)
      (not (road-blocked ?r))
    )
    :effect (and
      (not (ready-road ?v ?r))
      (ready-road ?v ?next)
      (increase (travel-time ?v) (travel-duration ?r))
      (increase (sim-time) (travel-duration ?r))
    )
  )

  (:action traverse-road-static-macro_0030-road_0248
    :parameters (?v - vehicle ?r - road_type_macro_0030 ?next - road_type_road_0248)
    :precondition (and
      (ready-road ?v ?r)
      (road-next ?r ?next)
      (road-open ?r)
      (static-road ?r)
      (not (road-blocked ?r))
    )
    :effect (and
      (not (ready-road ?v ?r))
      (ready-road ?v ?next)
      (increase (travel-time ?v) (travel-duration ?r))
      (increase (sim-time) (travel-duration ?r))
    )
  )

  (:action traverse-road-static-macro_0031-road_0579
    :parameters (?v - vehicle ?r - road_type_macro_0031 ?next - road_type_road_0579)
    :precondition (and
      (ready-road ?v ?r)
      (road-next ?r ?next)
      (road-open ?r)
      (static-road ?r)
      (not (road-blocked ?r))
    )
    :effect (and
      (not (ready-road ?v ?r))
      (ready-road ?v ?next)
      (increase (travel-time ?v) (travel-duration ?r))
      (increase (sim-time) (travel-duration ?r))
    )
  )

  (:action traverse-road-static-macro_0031-road_0580
    :parameters (?v - vehicle ?r - road_type_macro_0031 ?next - road_type_road_0580)
    :precondition (and
      (ready-road ?v ?r)
      (road-next ?r ?next)
      (road-open ?r)
      (static-road ?r)
      (not (road-blocked ?r))
    )
    :effect (and
      (not (ready-road ?v ?r))
      (ready-road ?v ?next)
      (increase (travel-time ?v) (travel-duration ?r))
      (increase (sim-time) (travel-duration ?r))
    )
  )

  (:action traverse-road-static-macro_0032-road_0598
    :parameters (?v - vehicle ?r - road_type_macro_0032 ?next - road_type_road_0598)
    :precondition (and
      (ready-road ?v ?r)
      (road-next ?r ?next)
      (road-open ?r)
      (static-road ?r)
      (not (road-blocked ?r))
    )
    :effect (and
      (not (ready-road ?v ?r))
      (ready-road ?v ?next)
      (increase (travel-time ?v) (travel-duration ?r))
      (increase (sim-time) (travel-duration ?r))
    )
  )

  (:action traverse-road-static-macro_0032-road_0599
    :parameters (?v - vehicle ?r - road_type_macro_0032 ?next - road_type_road_0599)
    :precondition (and
      (ready-road ?v ?r)
      (road-next ?r ?next)
      (road-open ?r)
      (static-road ?r)
      (not (road-blocked ?r))
    )
    :effect (and
      (not (ready-road ?v ?r))
      (ready-road ?v ?next)
      (increase (travel-time ?v) (travel-duration ?r))
      (increase (sim-time) (travel-duration ?r))
    )
  )

  (:action traverse-road-static-macro_0033-macro_0000
    :parameters (?v - vehicle ?r - road_type_macro_0033 ?next - road_type_macro_0000)
    :precondition (and
      (ready-road ?v ?r)
      (road-next ?r ?next)
      (road-open ?r)
      (static-road ?r)
      (not (road-blocked ?r))
    )
    :effect (and
      (not (ready-road ?v ?r))
      (ready-road ?v ?next)
      (increase (travel-time ?v) (travel-duration ?r))
      (increase (sim-time) (travel-duration ?r))
    )
  )

  (:action traverse-road-static-macro_0033-road_0003
    :parameters (?v - vehicle ?r - road_type_macro_0033 ?next - road_type_road_0003)
    :precondition (and
      (ready-road ?v ?r)
      (road-next ?r ?next)
      (road-open ?r)
      (static-road ?r)
      (not (road-blocked ?r))
    )
    :effect (and
      (not (ready-road ?v ?r))
      (ready-road ?v ?next)
      (increase (travel-time ?v) (travel-duration ?r))
      (increase (sim-time) (travel-duration ?r))
    )
  )

  (:action traverse-road-static-macro_0033-road_0004
    :parameters (?v - vehicle ?r - road_type_macro_0033 ?next - road_type_road_0004)
    :precondition (and
      (ready-road ?v ?r)
      (road-next ?r ?next)
      (road-open ?r)
      (static-road ?r)
      (not (road-blocked ?r))
    )
    :effect (and
      (not (ready-road ?v ?r))
      (ready-road ?v ?next)
      (increase (travel-time ?v) (travel-duration ?r))
      (increase (sim-time) (travel-duration ?r))
    )
  )

  (:action traverse-road-static-macro_0034-road_0497
    :parameters (?v - vehicle ?r - road_type_macro_0034 ?next - road_type_road_0497)
    :precondition (and
      (ready-road ?v ?r)
      (road-next ?r ?next)
      (road-open ?r)
      (static-road ?r)
      (not (road-blocked ?r))
    )
    :effect (and
      (not (ready-road ?v ?r))
      (ready-road ?v ?next)
      (increase (travel-time ?v) (travel-duration ?r))
      (increase (sim-time) (travel-duration ?r))
    )
  )

  (:action traverse-road-static-macro_0034-road_0498
    :parameters (?v - vehicle ?r - road_type_macro_0034 ?next - road_type_road_0498)
    :precondition (and
      (ready-road ?v ?r)
      (road-next ?r ?next)
      (road-open ?r)
      (static-road ?r)
      (not (road-blocked ?r))
    )
    :effect (and
      (not (ready-road ?v ?r))
      (ready-road ?v ?next)
      (increase (travel-time ?v) (travel-duration ?r))
      (increase (sim-time) (travel-duration ?r))
    )
  )

  (:action traverse-road-static-macro_0035-road_0196
    :parameters (?v - vehicle ?r - road_type_macro_0035 ?next - road_type_road_0196)
    :precondition (and
      (ready-road ?v ?r)
      (road-next ?r ?next)
      (road-open ?r)
      (static-road ?r)
      (not (road-blocked ?r))
    )
    :effect (and
      (not (ready-road ?v ?r))
      (ready-road ?v ?next)
      (increase (travel-time ?v) (travel-duration ?r))
      (increase (sim-time) (travel-duration ?r))
    )
  )

  (:action traverse-road-static-macro_0037-road_0744
    :parameters (?v - vehicle ?r - road_type_macro_0037 ?next - road_type_road_0744)
    :precondition (and
      (ready-road ?v ?r)
      (road-next ?r ?next)
      (road-open ?r)
      (static-road ?r)
      (not (road-blocked ?r))
    )
    :effect (and
      (not (ready-road ?v ?r))
      (ready-road ?v ?next)
      (increase (travel-time ?v) (travel-duration ?r))
      (increase (sim-time) (travel-duration ?r))
    )
  )

  (:action traverse-road-static-macro_0038-road_0563
    :parameters (?v - vehicle ?r - road_type_macro_0038 ?next - road_type_road_0563)
    :precondition (and
      (ready-road ?v ?r)
      (road-next ?r ?next)
      (road-open ?r)
      (static-road ?r)
      (not (road-blocked ?r))
    )
    :effect (and
      (not (ready-road ?v ?r))
      (ready-road ?v ?next)
      (increase (travel-time ?v) (travel-duration ?r))
      (increase (sim-time) (travel-duration ?r))
    )
  )

  (:action traverse-road-static-macro_0038-road_0564
    :parameters (?v - vehicle ?r - road_type_macro_0038 ?next - road_type_road_0564)
    :precondition (and
      (ready-road ?v ?r)
      (road-next ?r ?next)
      (road-open ?r)
      (static-road ?r)
      (not (road-blocked ?r))
    )
    :effect (and
      (not (ready-road ?v ?r))
      (ready-road ?v ?next)
      (increase (travel-time ?v) (travel-duration ?r))
      (increase (sim-time) (travel-duration ?r))
    )
  )

  (:action traverse-road-static-macro_0039-macro_0026
    :parameters (?v - vehicle ?r - road_type_macro_0039 ?next - road_type_macro_0026)
    :precondition (and
      (ready-road ?v ?r)
      (road-next ?r ?next)
      (road-open ?r)
      (static-road ?r)
      (not (road-blocked ?r))
    )
    :effect (and
      (not (ready-road ?v ?r))
      (ready-road ?v ?next)
      (increase (travel-time ?v) (travel-duration ?r))
      (increase (sim-time) (travel-duration ?r))
    )
  )

  (:action traverse-road-static-macro_0039-road_0307
    :parameters (?v - vehicle ?r - road_type_macro_0039 ?next - road_type_road_0307)
    :precondition (and
      (ready-road ?v ?r)
      (road-next ?r ?next)
      (road-open ?r)
      (static-road ?r)
      (not (road-blocked ?r))
    )
    :effect (and
      (not (ready-road ?v ?r))
      (ready-road ?v ?next)
      (increase (travel-time ?v) (travel-duration ?r))
      (increase (sim-time) (travel-duration ?r))
    )
  )

  (:action traverse-road-static-macro_0041-road_0546
    :parameters (?v - vehicle ?r - road_type_macro_0041 ?next - road_type_road_0546)
    :precondition (and
      (ready-road ?v ?r)
      (road-next ?r ?next)
      (road-open ?r)
      (static-road ?r)
      (not (road-blocked ?r))
    )
    :effect (and
      (not (ready-road ?v ?r))
      (ready-road ?v ?next)
      (increase (travel-time ?v) (travel-duration ?r))
      (increase (sim-time) (travel-duration ?r))
    )
  )

  (:action traverse-road-static-macro_0041-road_0547
    :parameters (?v - vehicle ?r - road_type_macro_0041 ?next - road_type_road_0547)
    :precondition (and
      (ready-road ?v ?r)
      (road-next ?r ?next)
      (road-open ?r)
      (static-road ?r)
      (not (road-blocked ?r))
    )
    :effect (and
      (not (ready-road ?v ?r))
      (ready-road ?v ?next)
      (increase (travel-time ?v) (travel-duration ?r))
      (increase (sim-time) (travel-duration ?r))
    )
  )

  (:action traverse-road-static-macro_0042-macro_0014
    :parameters (?v - vehicle ?r - road_type_macro_0042 ?next - road_type_macro_0014)
    :precondition (and
      (ready-road ?v ?r)
      (road-next ?r ?next)
      (road-open ?r)
      (static-road ?r)
      (not (road-blocked ?r))
    )
    :effect (and
      (not (ready-road ?v ?r))
      (ready-road ?v ?next)
      (increase (travel-time ?v) (travel-duration ?r))
      (increase (sim-time) (travel-duration ?r))
    )
  )

  (:action traverse-road-static-macro_0042-macro_0015
    :parameters (?v - vehicle ?r - road_type_macro_0042 ?next - road_type_macro_0015)
    :precondition (and
      (ready-road ?v ?r)
      (road-next ?r ?next)
      (road-open ?r)
      (static-road ?r)
      (not (road-blocked ?r))
    )
    :effect (and
      (not (ready-road ?v ?r))
      (ready-road ?v ?next)
      (increase (travel-time ?v) (travel-duration ?r))
      (increase (sim-time) (travel-duration ?r))
    )
  )

  (:action traverse-road-static-macro_0043-road_0156
    :parameters (?v - vehicle ?r - road_type_macro_0043 ?next - road_type_road_0156)
    :precondition (and
      (ready-road ?v ?r)
      (road-next ?r ?next)
      (road-open ?r)
      (static-road ?r)
      (not (road-blocked ?r))
    )
    :effect (and
      (not (ready-road ?v ?r))
      (ready-road ?v ?next)
      (increase (travel-time ?v) (travel-duration ?r))
      (increase (sim-time) (travel-duration ?r))
    )
  )

  (:action traverse-road-static-macro_0043-road_0157
    :parameters (?v - vehicle ?r - road_type_macro_0043 ?next - road_type_road_0157)
    :precondition (and
      (ready-road ?v ?r)
      (road-next ?r ?next)
      (road-open ?r)
      (static-road ?r)
      (not (road-blocked ?r))
    )
    :effect (and
      (not (ready-road ?v ?r))
      (ready-road ?v ?next)
      (increase (travel-time ?v) (travel-duration ?r))
      (increase (sim-time) (travel-duration ?r))
    )
  )

  (:action traverse-road-static-macro_0044-road_0725
    :parameters (?v - vehicle ?r - road_type_macro_0044 ?next - road_type_road_0725)
    :precondition (and
      (ready-road ?v ?r)
      (road-next ?r ?next)
      (road-open ?r)
      (static-road ?r)
      (not (road-blocked ?r))
    )
    :effect (and
      (not (ready-road ?v ?r))
      (ready-road ?v ?next)
      (increase (travel-time ?v) (travel-duration ?r))
      (increase (sim-time) (travel-duration ?r))
    )
  )

  (:action traverse-road-static-macro_0044-road_0726
    :parameters (?v - vehicle ?r - road_type_macro_0044 ?next - road_type_road_0726)
    :precondition (and
      (ready-road ?v ?r)
      (road-next ?r ?next)
      (road-open ?r)
      (static-road ?r)
      (not (road-blocked ?r))
    )
    :effect (and
      (not (ready-road ?v ?r))
      (ready-road ?v ?next)
      (increase (travel-time ?v) (travel-duration ?r))
      (increase (sim-time) (travel-duration ?r))
    )
  )

  (:action traverse-road-static-macro_0044-road_0727
    :parameters (?v - vehicle ?r - road_type_macro_0044 ?next - road_type_road_0727)
    :precondition (and
      (ready-road ?v ?r)
      (road-next ?r ?next)
      (road-open ?r)
      (static-road ?r)
      (not (road-blocked ?r))
    )
    :effect (and
      (not (ready-road ?v ?r))
      (ready-road ?v ?next)
      (increase (travel-time ?v) (travel-duration ?r))
      (increase (sim-time) (travel-duration ?r))
    )
  )

  (:action traverse-road-dynamic-macro_0045-road_0301-tw_00000
    :parameters (?v - vehicle ?r - road_type_macro_0045 ?next - road_type_road_0301 ?w - window_type_tw_00000)
    :precondition (and
      (ready-road ?v ?r)
      (road-next ?r ?next)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
    )
    :effect (and
      (not (ready-road ?v ?r))
      (ready-road ?v ?next)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-macro_0045-road_0301-tw_00030
    :parameters (?v - vehicle ?r - road_type_macro_0045 ?next - road_type_road_0301 ?w - window_type_tw_00030)
    :precondition (and
      (ready-road ?v ?r)
      (road-next ?r ?next)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
    )
    :effect (and
      (not (ready-road ?v ?r))
      (ready-road ?v ?next)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-macro_0045-road_0301-tw_00060
    :parameters (?v - vehicle ?r - road_type_macro_0045 ?next - road_type_road_0301 ?w - window_type_tw_00060)
    :precondition (and
      (ready-road ?v ?r)
      (road-next ?r ?next)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
    )
    :effect (and
      (not (ready-road ?v ?r))
      (ready-road ?v ?next)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-macro_0045-road_0301-tw_00090
    :parameters (?v - vehicle ?r - road_type_macro_0045 ?next - road_type_road_0301 ?w - window_type_tw_00090)
    :precondition (and
      (ready-road ?v ?r)
      (road-next ?r ?next)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
    )
    :effect (and
      (not (ready-road ?v ?r))
      (ready-road ?v ?next)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-macro_0045-road_0301-tw_00120
    :parameters (?v - vehicle ?r - road_type_macro_0045 ?next - road_type_road_0301 ?w - window_type_tw_00120)
    :precondition (and
      (ready-road ?v ?r)
      (road-next ?r ?next)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
    )
    :effect (and
      (not (ready-road ?v ?r))
      (ready-road ?v ?next)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-macro_0045-road_0301-tw_00150
    :parameters (?v - vehicle ?r - road_type_macro_0045 ?next - road_type_road_0301 ?w - window_type_tw_00150)
    :precondition (and
      (ready-road ?v ?r)
      (road-next ?r ?next)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
    )
    :effect (and
      (not (ready-road ?v ?r))
      (ready-road ?v ?next)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-macro_0045-road_0301-tw_00180
    :parameters (?v - vehicle ?r - road_type_macro_0045 ?next - road_type_road_0301 ?w - window_type_tw_00180)
    :precondition (and
      (ready-road ?v ?r)
      (road-next ?r ?next)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
    )
    :effect (and
      (not (ready-road ?v ?r))
      (ready-road ?v ?next)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-macro_0045-road_0301-tw_00210
    :parameters (?v - vehicle ?r - road_type_macro_0045 ?next - road_type_road_0301 ?w - window_type_tw_00210)
    :precondition (and
      (ready-road ?v ?r)
      (road-next ?r ?next)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
    )
    :effect (and
      (not (ready-road ?v ?r))
      (ready-road ?v ?next)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-macro_0045-road_0301-tw_00240
    :parameters (?v - vehicle ?r - road_type_macro_0045 ?next - road_type_road_0301 ?w - window_type_tw_00240)
    :precondition (and
      (ready-road ?v ?r)
      (road-next ?r ?next)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
    )
    :effect (and
      (not (ready-road ?v ?r))
      (ready-road ?v ?next)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-macro_0045-road_0301-tw_00270
    :parameters (?v - vehicle ?r - road_type_macro_0045 ?next - road_type_road_0301 ?w - window_type_tw_00270)
    :precondition (and
      (ready-road ?v ?r)
      (road-next ?r ?next)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
    )
    :effect (and
      (not (ready-road ?v ?r))
      (ready-road ?v ?next)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-macro_0045-road_0301-tw_00300
    :parameters (?v - vehicle ?r - road_type_macro_0045 ?next - road_type_road_0301 ?w - window_type_tw_00300)
    :precondition (and
      (ready-road ?v ?r)
      (road-next ?r ?next)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
    )
    :effect (and
      (not (ready-road ?v ?r))
      (ready-road ?v ?next)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-macro_0045-road_0301-tw_00330
    :parameters (?v - vehicle ?r - road_type_macro_0045 ?next - road_type_road_0301 ?w - window_type_tw_00330)
    :precondition (and
      (ready-road ?v ?r)
      (road-next ?r ?next)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
    )
    :effect (and
      (not (ready-road ?v ?r))
      (ready-road ?v ?next)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-macro_0045-road_0301-tw_00360
    :parameters (?v - vehicle ?r - road_type_macro_0045 ?next - road_type_road_0301 ?w - window_type_tw_00360)
    :precondition (and
      (ready-road ?v ?r)
      (road-next ?r ?next)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
    )
    :effect (and
      (not (ready-road ?v ?r))
      (ready-road ?v ?next)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-macro_0045-road_0301-tw_00390
    :parameters (?v - vehicle ?r - road_type_macro_0045 ?next - road_type_road_0301 ?w - window_type_tw_00390)
    :precondition (and
      (ready-road ?v ?r)
      (road-next ?r ?next)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
    )
    :effect (and
      (not (ready-road ?v ?r))
      (ready-road ?v ?next)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-macro_0045-road_0301-tw_00420
    :parameters (?v - vehicle ?r - road_type_macro_0045 ?next - road_type_road_0301 ?w - window_type_tw_00420)
    :precondition (and
      (ready-road ?v ?r)
      (road-next ?r ?next)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
    )
    :effect (and
      (not (ready-road ?v ?r))
      (ready-road ?v ?next)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-macro_0045-road_0301-tw_00450
    :parameters (?v - vehicle ?r - road_type_macro_0045 ?next - road_type_road_0301 ?w - window_type_tw_00450)
    :precondition (and
      (ready-road ?v ?r)
      (road-next ?r ?next)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
    )
    :effect (and
      (not (ready-road ?v ?r))
      (ready-road ?v ?next)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-macro_0045-road_0301-tw_00480
    :parameters (?v - vehicle ?r - road_type_macro_0045 ?next - road_type_road_0301 ?w - window_type_tw_00480)
    :precondition (and
      (ready-road ?v ?r)
      (road-next ?r ?next)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
    )
    :effect (and
      (not (ready-road ?v ?r))
      (ready-road ?v ?next)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-macro_0045-road_0301-tw_00510
    :parameters (?v - vehicle ?r - road_type_macro_0045 ?next - road_type_road_0301 ?w - window_type_tw_00510)
    :precondition (and
      (ready-road ?v ?r)
      (road-next ?r ?next)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
    )
    :effect (and
      (not (ready-road ?v ?r))
      (ready-road ?v ?next)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-macro_0045-road_0301-tw_00540
    :parameters (?v - vehicle ?r - road_type_macro_0045 ?next - road_type_road_0301 ?w - window_type_tw_00540)
    :precondition (and
      (ready-road ?v ?r)
      (road-next ?r ?next)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
    )
    :effect (and
      (not (ready-road ?v ?r))
      (ready-road ?v ?next)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-macro_0045-road_0301-tw_00570
    :parameters (?v - vehicle ?r - road_type_macro_0045 ?next - road_type_road_0301 ?w - window_type_tw_00570)
    :precondition (and
      (ready-road ?v ?r)
      (road-next ?r ?next)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
    )
    :effect (and
      (not (ready-road ?v ?r))
      (ready-road ?v ?next)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-macro_0045-road_0301-tw_00600
    :parameters (?v - vehicle ?r - road_type_macro_0045 ?next - road_type_road_0301 ?w - window_type_tw_00600)
    :precondition (and
      (ready-road ?v ?r)
      (road-next ?r ?next)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
    )
    :effect (and
      (not (ready-road ?v ?r))
      (ready-road ?v ?next)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-macro_0045-road_0302-tw_00000
    :parameters (?v - vehicle ?r - road_type_macro_0045 ?next - road_type_road_0302 ?w - window_type_tw_00000)
    :precondition (and
      (ready-road ?v ?r)
      (road-next ?r ?next)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
    )
    :effect (and
      (not (ready-road ?v ?r))
      (ready-road ?v ?next)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-macro_0045-road_0302-tw_00030
    :parameters (?v - vehicle ?r - road_type_macro_0045 ?next - road_type_road_0302 ?w - window_type_tw_00030)
    :precondition (and
      (ready-road ?v ?r)
      (road-next ?r ?next)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
    )
    :effect (and
      (not (ready-road ?v ?r))
      (ready-road ?v ?next)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-macro_0045-road_0302-tw_00060
    :parameters (?v - vehicle ?r - road_type_macro_0045 ?next - road_type_road_0302 ?w - window_type_tw_00060)
    :precondition (and
      (ready-road ?v ?r)
      (road-next ?r ?next)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
    )
    :effect (and
      (not (ready-road ?v ?r))
      (ready-road ?v ?next)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-macro_0045-road_0302-tw_00090
    :parameters (?v - vehicle ?r - road_type_macro_0045 ?next - road_type_road_0302 ?w - window_type_tw_00090)
    :precondition (and
      (ready-road ?v ?r)
      (road-next ?r ?next)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
    )
    :effect (and
      (not (ready-road ?v ?r))
      (ready-road ?v ?next)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-macro_0045-road_0302-tw_00120
    :parameters (?v - vehicle ?r - road_type_macro_0045 ?next - road_type_road_0302 ?w - window_type_tw_00120)
    :precondition (and
      (ready-road ?v ?r)
      (road-next ?r ?next)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
    )
    :effect (and
      (not (ready-road ?v ?r))
      (ready-road ?v ?next)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-macro_0045-road_0302-tw_00150
    :parameters (?v - vehicle ?r - road_type_macro_0045 ?next - road_type_road_0302 ?w - window_type_tw_00150)
    :precondition (and
      (ready-road ?v ?r)
      (road-next ?r ?next)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
    )
    :effect (and
      (not (ready-road ?v ?r))
      (ready-road ?v ?next)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-macro_0045-road_0302-tw_00180
    :parameters (?v - vehicle ?r - road_type_macro_0045 ?next - road_type_road_0302 ?w - window_type_tw_00180)
    :precondition (and
      (ready-road ?v ?r)
      (road-next ?r ?next)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
    )
    :effect (and
      (not (ready-road ?v ?r))
      (ready-road ?v ?next)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-macro_0045-road_0302-tw_00210
    :parameters (?v - vehicle ?r - road_type_macro_0045 ?next - road_type_road_0302 ?w - window_type_tw_00210)
    :precondition (and
      (ready-road ?v ?r)
      (road-next ?r ?next)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
    )
    :effect (and
      (not (ready-road ?v ?r))
      (ready-road ?v ?next)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-macro_0045-road_0302-tw_00240
    :parameters (?v - vehicle ?r - road_type_macro_0045 ?next - road_type_road_0302 ?w - window_type_tw_00240)
    :precondition (and
      (ready-road ?v ?r)
      (road-next ?r ?next)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
    )
    :effect (and
      (not (ready-road ?v ?r))
      (ready-road ?v ?next)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-macro_0045-road_0302-tw_00270
    :parameters (?v - vehicle ?r - road_type_macro_0045 ?next - road_type_road_0302 ?w - window_type_tw_00270)
    :precondition (and
      (ready-road ?v ?r)
      (road-next ?r ?next)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
    )
    :effect (and
      (not (ready-road ?v ?r))
      (ready-road ?v ?next)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-macro_0045-road_0302-tw_00300
    :parameters (?v - vehicle ?r - road_type_macro_0045 ?next - road_type_road_0302 ?w - window_type_tw_00300)
    :precondition (and
      (ready-road ?v ?r)
      (road-next ?r ?next)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
    )
    :effect (and
      (not (ready-road ?v ?r))
      (ready-road ?v ?next)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-macro_0045-road_0302-tw_00330
    :parameters (?v - vehicle ?r - road_type_macro_0045 ?next - road_type_road_0302 ?w - window_type_tw_00330)
    :precondition (and
      (ready-road ?v ?r)
      (road-next ?r ?next)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
    )
    :effect (and
      (not (ready-road ?v ?r))
      (ready-road ?v ?next)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-macro_0045-road_0302-tw_00360
    :parameters (?v - vehicle ?r - road_type_macro_0045 ?next - road_type_road_0302 ?w - window_type_tw_00360)
    :precondition (and
      (ready-road ?v ?r)
      (road-next ?r ?next)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
    )
    :effect (and
      (not (ready-road ?v ?r))
      (ready-road ?v ?next)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-macro_0045-road_0302-tw_00390
    :parameters (?v - vehicle ?r - road_type_macro_0045 ?next - road_type_road_0302 ?w - window_type_tw_00390)
    :precondition (and
      (ready-road ?v ?r)
      (road-next ?r ?next)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
    )
    :effect (and
      (not (ready-road ?v ?r))
      (ready-road ?v ?next)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-macro_0045-road_0302-tw_00420
    :parameters (?v - vehicle ?r - road_type_macro_0045 ?next - road_type_road_0302 ?w - window_type_tw_00420)
    :precondition (and
      (ready-road ?v ?r)
      (road-next ?r ?next)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
    )
    :effect (and
      (not (ready-road ?v ?r))
      (ready-road ?v ?next)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-macro_0045-road_0302-tw_00450
    :parameters (?v - vehicle ?r - road_type_macro_0045 ?next - road_type_road_0302 ?w - window_type_tw_00450)
    :precondition (and
      (ready-road ?v ?r)
      (road-next ?r ?next)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
    )
    :effect (and
      (not (ready-road ?v ?r))
      (ready-road ?v ?next)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-macro_0045-road_0302-tw_00480
    :parameters (?v - vehicle ?r - road_type_macro_0045 ?next - road_type_road_0302 ?w - window_type_tw_00480)
    :precondition (and
      (ready-road ?v ?r)
      (road-next ?r ?next)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
    )
    :effect (and
      (not (ready-road ?v ?r))
      (ready-road ?v ?next)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-macro_0045-road_0302-tw_00510
    :parameters (?v - vehicle ?r - road_type_macro_0045 ?next - road_type_road_0302 ?w - window_type_tw_00510)
    :precondition (and
      (ready-road ?v ?r)
      (road-next ?r ?next)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
    )
    :effect (and
      (not (ready-road ?v ?r))
      (ready-road ?v ?next)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-macro_0045-road_0302-tw_00540
    :parameters (?v - vehicle ?r - road_type_macro_0045 ?next - road_type_road_0302 ?w - window_type_tw_00540)
    :precondition (and
      (ready-road ?v ?r)
      (road-next ?r ?next)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
    )
    :effect (and
      (not (ready-road ?v ?r))
      (ready-road ?v ?next)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-macro_0045-road_0302-tw_00570
    :parameters (?v - vehicle ?r - road_type_macro_0045 ?next - road_type_road_0302 ?w - window_type_tw_00570)
    :precondition (and
      (ready-road ?v ?r)
      (road-next ?r ?next)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
    )
    :effect (and
      (not (ready-road ?v ?r))
      (ready-road ?v ?next)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-macro_0045-road_0302-tw_00600
    :parameters (?v - vehicle ?r - road_type_macro_0045 ?next - road_type_road_0302 ?w - window_type_tw_00600)
    :precondition (and
      (ready-road ?v ?r)
      (road-next ?r ?next)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
    )
    :effect (and
      (not (ready-road ?v ?r))
      (ready-road ?v ?next)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-static-macro_0046-road_0618
    :parameters (?v - vehicle ?r - road_type_macro_0046 ?next - road_type_road_0618)
    :precondition (and
      (ready-road ?v ?r)
      (road-next ?r ?next)
      (road-open ?r)
      (static-road ?r)
      (not (road-blocked ?r))
    )
    :effect (and
      (not (ready-road ?v ?r))
      (ready-road ?v ?next)
      (increase (travel-time ?v) (travel-duration ?r))
      (increase (sim-time) (travel-duration ?r))
    )
  )

  (:action traverse-road-static-macro_0046-road_0619
    :parameters (?v - vehicle ?r - road_type_macro_0046 ?next - road_type_road_0619)
    :precondition (and
      (ready-road ?v ?r)
      (road-next ?r ?next)
      (road-open ?r)
      (static-road ?r)
      (not (road-blocked ?r))
    )
    :effect (and
      (not (ready-road ?v ?r))
      (ready-road ?v ?next)
      (increase (travel-time ?v) (travel-duration ?r))
      (increase (sim-time) (travel-duration ?r))
    )
  )

  (:action traverse-road-static-macro_0046-road_0620
    :parameters (?v - vehicle ?r - road_type_macro_0046 ?next - road_type_road_0620)
    :precondition (and
      (ready-road ?v ?r)
      (road-next ?r ?next)
      (road-open ?r)
      (static-road ?r)
      (not (road-blocked ?r))
    )
    :effect (and
      (not (ready-road ?v ?r))
      (ready-road ?v ?next)
      (increase (travel-time ?v) (travel-duration ?r))
      (increase (sim-time) (travel-duration ?r))
    )
  )

  (:action traverse-road-static-macro_0047-road_0627
    :parameters (?v - vehicle ?r - road_type_macro_0047 ?next - road_type_road_0627)
    :precondition (and
      (ready-road ?v ?r)
      (road-next ?r ?next)
      (road-open ?r)
      (static-road ?r)
      (not (road-blocked ?r))
    )
    :effect (and
      (not (ready-road ?v ?r))
      (ready-road ?v ?next)
      (increase (travel-time ?v) (travel-duration ?r))
      (increase (sim-time) (travel-duration ?r))
    )
  )

  (:action traverse-road-static-macro_0048-road_0730
    :parameters (?v - vehicle ?r - road_type_macro_0048 ?next - road_type_road_0730)
    :precondition (and
      (ready-road ?v ?r)
      (road-next ?r ?next)
      (road-open ?r)
      (static-road ?r)
      (not (road-blocked ?r))
    )
    :effect (and
      (not (ready-road ?v ?r))
      (ready-road ?v ?next)
      (increase (travel-time ?v) (travel-duration ?r))
      (increase (sim-time) (travel-duration ?r))
    )
  )

  (:action traverse-road-static-macro_0049-road_0379
    :parameters (?v - vehicle ?r - road_type_macro_0049 ?next - road_type_road_0379)
    :precondition (and
      (ready-road ?v ?r)
      (road-next ?r ?next)
      (road-open ?r)
      (static-road ?r)
      (not (road-blocked ?r))
    )
    :effect (and
      (not (ready-road ?v ?r))
      (ready-road ?v ?next)
      (increase (travel-time ?v) (travel-duration ?r))
      (increase (sim-time) (travel-duration ?r))
    )
  )

  (:action traverse-road-static-macro_0049-road_0380
    :parameters (?v - vehicle ?r - road_type_macro_0049 ?next - road_type_road_0380)
    :precondition (and
      (ready-road ?v ?r)
      (road-next ?r ?next)
      (road-open ?r)
      (static-road ?r)
      (not (road-blocked ?r))
    )
    :effect (and
      (not (ready-road ?v ?r))
      (ready-road ?v ?next)
      (increase (travel-time ?v) (travel-duration ?r))
      (increase (sim-time) (travel-duration ?r))
    )
  )

  (:action traverse-road-static-macro_0050-road_0349
    :parameters (?v - vehicle ?r - road_type_macro_0050 ?next - road_type_road_0349)
    :precondition (and
      (ready-road ?v ?r)
      (road-next ?r ?next)
      (road-open ?r)
      (static-road ?r)
      (not (road-blocked ?r))
    )
    :effect (and
      (not (ready-road ?v ?r))
      (ready-road ?v ?next)
      (increase (travel-time ?v) (travel-duration ?r))
      (increase (sim-time) (travel-duration ?r))
    )
  )

  (:action traverse-road-static-macro_0050-road_0350
    :parameters (?v - vehicle ?r - road_type_macro_0050 ?next - road_type_road_0350)
    :precondition (and
      (ready-road ?v ?r)
      (road-next ?r ?next)
      (road-open ?r)
      (static-road ?r)
      (not (road-blocked ?r))
    )
    :effect (and
      (not (ready-road ?v ?r))
      (ready-road ?v ?next)
      (increase (travel-time ?v) (travel-duration ?r))
      (increase (sim-time) (travel-duration ?r))
    )
  )

  (:action traverse-road-static-macro_0051-road_0338
    :parameters (?v - vehicle ?r - road_type_macro_0051 ?next - road_type_road_0338)
    :precondition (and
      (ready-road ?v ?r)
      (road-next ?r ?next)
      (road-open ?r)
      (static-road ?r)
      (not (road-blocked ?r))
    )
    :effect (and
      (not (ready-road ?v ?r))
      (ready-road ?v ?next)
      (increase (travel-time ?v) (travel-duration ?r))
      (increase (sim-time) (travel-duration ?r))
    )
  )

  (:action traverse-road-static-macro_0051-road_0339
    :parameters (?v - vehicle ?r - road_type_macro_0051 ?next - road_type_road_0339)
    :precondition (and
      (ready-road ?v ?r)
      (road-next ?r ?next)
      (road-open ?r)
      (static-road ?r)
      (not (road-blocked ?r))
    )
    :effect (and
      (not (ready-road ?v ?r))
      (ready-road ?v ?next)
      (increase (travel-time ?v) (travel-duration ?r))
      (increase (sim-time) (travel-duration ?r))
    )
  )

  (:action traverse-road-static-macro_0052-macro_0053
    :parameters (?v - vehicle ?r - road_type_macro_0052 ?next - road_type_macro_0053)
    :precondition (and
      (ready-road ?v ?r)
      (road-next ?r ?next)
      (road-open ?r)
      (static-road ?r)
      (not (road-blocked ?r))
    )
    :effect (and
      (not (ready-road ?v ?r))
      (ready-road ?v ?next)
      (increase (travel-time ?v) (travel-duration ?r))
      (increase (sim-time) (travel-duration ?r))
    )
  )

  (:action traverse-road-static-macro_0052-macro_0054
    :parameters (?v - vehicle ?r - road_type_macro_0052 ?next - road_type_macro_0054)
    :precondition (and
      (ready-road ?v ?r)
      (road-next ?r ?next)
      (road-open ?r)
      (static-road ?r)
      (not (road-blocked ?r))
    )
    :effect (and
      (not (ready-road ?v ?r))
      (ready-road ?v ?next)
      (increase (travel-time ?v) (travel-duration ?r))
      (increase (sim-time) (travel-duration ?r))
    )
  )

  (:action traverse-road-static-macro_0053-road_0399
    :parameters (?v - vehicle ?r - road_type_macro_0053 ?next - road_type_road_0399)
    :precondition (and
      (ready-road ?v ?r)
      (road-next ?r ?next)
      (road-open ?r)
      (static-road ?r)
      (not (road-blocked ?r))
    )
    :effect (and
      (not (ready-road ?v ?r))
      (ready-road ?v ?next)
      (increase (travel-time ?v) (travel-duration ?r))
      (increase (sim-time) (travel-duration ?r))
    )
  )

  (:action traverse-road-static-macro_0054-road_0627
    :parameters (?v - vehicle ?r - road_type_macro_0054 ?next - road_type_road_0627)
    :precondition (and
      (ready-road ?v ?r)
      (road-next ?r ?next)
      (road-open ?r)
      (static-road ?r)
      (not (road-blocked ?r))
    )
    :effect (and
      (not (ready-road ?v ?r))
      (ready-road ?v ?next)
      (increase (travel-time ?v) (travel-duration ?r))
      (increase (sim-time) (travel-duration ?r))
    )
  )

  (:action traverse-road-static-macro_0056-road_0258
    :parameters (?v - vehicle ?r - road_type_macro_0056 ?next - road_type_road_0258)
    :precondition (and
      (ready-road ?v ?r)
      (road-next ?r ?next)
      (road-open ?r)
      (static-road ?r)
      (not (road-blocked ?r))
    )
    :effect (and
      (not (ready-road ?v ?r))
      (ready-road ?v ?next)
      (increase (travel-time ?v) (travel-duration ?r))
      (increase (sim-time) (travel-duration ?r))
    )
  )

  (:action traverse-road-static-macro_0056-road_0259
    :parameters (?v - vehicle ?r - road_type_macro_0056 ?next - road_type_road_0259)
    :precondition (and
      (ready-road ?v ?r)
      (road-next ?r ?next)
      (road-open ?r)
      (static-road ?r)
      (not (road-blocked ?r))
    )
    :effect (and
      (not (ready-road ?v ?r))
      (ready-road ?v ?next)
      (increase (travel-time ?v) (travel-duration ?r))
      (increase (sim-time) (travel-duration ?r))
    )
  )

  (:action traverse-road-static-macro_0057-road_0258
    :parameters (?v - vehicle ?r - road_type_macro_0057 ?next - road_type_road_0258)
    :precondition (and
      (ready-road ?v ?r)
      (road-next ?r ?next)
      (road-open ?r)
      (static-road ?r)
      (not (road-blocked ?r))
    )
    :effect (and
      (not (ready-road ?v ?r))
      (ready-road ?v ?next)
      (increase (travel-time ?v) (travel-duration ?r))
      (increase (sim-time) (travel-duration ?r))
    )
  )

  (:action traverse-road-static-macro_0057-road_0259
    :parameters (?v - vehicle ?r - road_type_macro_0057 ?next - road_type_road_0259)
    :precondition (and
      (ready-road ?v ?r)
      (road-next ?r ?next)
      (road-open ?r)
      (static-road ?r)
      (not (road-blocked ?r))
    )
    :effect (and
      (not (ready-road ?v ?r))
      (ready-road ?v ?next)
      (increase (travel-time ?v) (travel-duration ?r))
      (increase (sim-time) (travel-duration ?r))
    )
  )

  (:action traverse-road-static-road_0000-road_0745
    :parameters (?v - vehicle ?r - road_type_road_0000 ?next - road_type_road_0745)
    :precondition (and
      (ready-road ?v ?r)
      (road-next ?r ?next)
      (road-open ?r)
      (static-road ?r)
      (not (road-blocked ?r))
    )
    :effect (and
      (not (ready-road ?v ?r))
      (ready-road ?v ?next)
      (increase (travel-time ?v) (travel-duration ?r))
      (increase (sim-time) (travel-duration ?r))
    )
  )

  (:action traverse-road-static-road_0000-road_0746
    :parameters (?v - vehicle ?r - road_type_road_0000 ?next - road_type_road_0746)
    :precondition (and
      (ready-road ?v ?r)
      (road-next ?r ?next)
      (road-open ?r)
      (static-road ?r)
      (not (road-blocked ?r))
    )
    :effect (and
      (not (ready-road ?v ?r))
      (ready-road ?v ?next)
      (increase (travel-time ?v) (travel-duration ?r))
      (increase (sim-time) (travel-duration ?r))
    )
  )

  (:action traverse-road-static-road_0001-road_0016
    :parameters (?v - vehicle ?r - road_type_road_0001 ?next - road_type_road_0016)
    :precondition (and
      (ready-road ?v ?r)
      (road-next ?r ?next)
      (road-open ?r)
      (static-road ?r)
      (not (road-blocked ?r))
    )
    :effect (and
      (not (ready-road ?v ?r))
      (ready-road ?v ?next)
      (increase (travel-time ?v) (travel-duration ?r))
      (increase (sim-time) (travel-duration ?r))
    )
  )

  (:action traverse-road-static-road_0001-road_0017
    :parameters (?v - vehicle ?r - road_type_road_0001 ?next - road_type_road_0017)
    :precondition (and
      (ready-road ?v ?r)
      (road-next ?r ?next)
      (road-open ?r)
      (static-road ?r)
      (not (road-blocked ?r))
    )
    :effect (and
      (not (ready-road ?v ?r))
      (ready-road ?v ?next)
      (increase (travel-time ?v) (travel-duration ?r))
      (increase (sim-time) (travel-duration ?r))
    )
  )

  (:action traverse-road-dynamic-road_0002-road_0056-tw_00000
    :parameters (?v - vehicle ?r - road_type_road_0002 ?next - road_type_road_0056 ?w - window_type_tw_00000)
    :precondition (and
      (ready-road ?v ?r)
      (road-next ?r ?next)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
    )
    :effect (and
      (not (ready-road ?v ?r))
      (ready-road ?v ?next)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-road_0002-road_0056-tw_00030
    :parameters (?v - vehicle ?r - road_type_road_0002 ?next - road_type_road_0056 ?w - window_type_tw_00030)
    :precondition (and
      (ready-road ?v ?r)
      (road-next ?r ?next)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
    )
    :effect (and
      (not (ready-road ?v ?r))
      (ready-road ?v ?next)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-road_0002-road_0056-tw_00060
    :parameters (?v - vehicle ?r - road_type_road_0002 ?next - road_type_road_0056 ?w - window_type_tw_00060)
    :precondition (and
      (ready-road ?v ?r)
      (road-next ?r ?next)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
    )
    :effect (and
      (not (ready-road ?v ?r))
      (ready-road ?v ?next)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-road_0002-road_0056-tw_00090
    :parameters (?v - vehicle ?r - road_type_road_0002 ?next - road_type_road_0056 ?w - window_type_tw_00090)
    :precondition (and
      (ready-road ?v ?r)
      (road-next ?r ?next)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
    )
    :effect (and
      (not (ready-road ?v ?r))
      (ready-road ?v ?next)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-road_0002-road_0056-tw_00120
    :parameters (?v - vehicle ?r - road_type_road_0002 ?next - road_type_road_0056 ?w - window_type_tw_00120)
    :precondition (and
      (ready-road ?v ?r)
      (road-next ?r ?next)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
    )
    :effect (and
      (not (ready-road ?v ?r))
      (ready-road ?v ?next)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-road_0002-road_0056-tw_00150
    :parameters (?v - vehicle ?r - road_type_road_0002 ?next - road_type_road_0056 ?w - window_type_tw_00150)
    :precondition (and
      (ready-road ?v ?r)
      (road-next ?r ?next)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
    )
    :effect (and
      (not (ready-road ?v ?r))
      (ready-road ?v ?next)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-road_0002-road_0056-tw_00180
    :parameters (?v - vehicle ?r - road_type_road_0002 ?next - road_type_road_0056 ?w - window_type_tw_00180)
    :precondition (and
      (ready-road ?v ?r)
      (road-next ?r ?next)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
    )
    :effect (and
      (not (ready-road ?v ?r))
      (ready-road ?v ?next)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-road_0002-road_0056-tw_00210
    :parameters (?v - vehicle ?r - road_type_road_0002 ?next - road_type_road_0056 ?w - window_type_tw_00210)
    :precondition (and
      (ready-road ?v ?r)
      (road-next ?r ?next)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
    )
    :effect (and
      (not (ready-road ?v ?r))
      (ready-road ?v ?next)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-road_0002-road_0056-tw_00240
    :parameters (?v - vehicle ?r - road_type_road_0002 ?next - road_type_road_0056 ?w - window_type_tw_00240)
    :precondition (and
      (ready-road ?v ?r)
      (road-next ?r ?next)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
    )
    :effect (and
      (not (ready-road ?v ?r))
      (ready-road ?v ?next)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-road_0002-road_0056-tw_00270
    :parameters (?v - vehicle ?r - road_type_road_0002 ?next - road_type_road_0056 ?w - window_type_tw_00270)
    :precondition (and
      (ready-road ?v ?r)
      (road-next ?r ?next)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
    )
    :effect (and
      (not (ready-road ?v ?r))
      (ready-road ?v ?next)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-road_0002-road_0056-tw_00300
    :parameters (?v - vehicle ?r - road_type_road_0002 ?next - road_type_road_0056 ?w - window_type_tw_00300)
    :precondition (and
      (ready-road ?v ?r)
      (road-next ?r ?next)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
    )
    :effect (and
      (not (ready-road ?v ?r))
      (ready-road ?v ?next)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-road_0002-road_0056-tw_00330
    :parameters (?v - vehicle ?r - road_type_road_0002 ?next - road_type_road_0056 ?w - window_type_tw_00330)
    :precondition (and
      (ready-road ?v ?r)
      (road-next ?r ?next)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
    )
    :effect (and
      (not (ready-road ?v ?r))
      (ready-road ?v ?next)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-road_0002-road_0056-tw_00360
    :parameters (?v - vehicle ?r - road_type_road_0002 ?next - road_type_road_0056 ?w - window_type_tw_00360)
    :precondition (and
      (ready-road ?v ?r)
      (road-next ?r ?next)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
    )
    :effect (and
      (not (ready-road ?v ?r))
      (ready-road ?v ?next)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-road_0002-road_0056-tw_00390
    :parameters (?v - vehicle ?r - road_type_road_0002 ?next - road_type_road_0056 ?w - window_type_tw_00390)
    :precondition (and
      (ready-road ?v ?r)
      (road-next ?r ?next)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
    )
    :effect (and
      (not (ready-road ?v ?r))
      (ready-road ?v ?next)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-road_0002-road_0056-tw_00420
    :parameters (?v - vehicle ?r - road_type_road_0002 ?next - road_type_road_0056 ?w - window_type_tw_00420)
    :precondition (and
      (ready-road ?v ?r)
      (road-next ?r ?next)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
    )
    :effect (and
      (not (ready-road ?v ?r))
      (ready-road ?v ?next)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-road_0002-road_0056-tw_00450
    :parameters (?v - vehicle ?r - road_type_road_0002 ?next - road_type_road_0056 ?w - window_type_tw_00450)
    :precondition (and
      (ready-road ?v ?r)
      (road-next ?r ?next)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
    )
    :effect (and
      (not (ready-road ?v ?r))
      (ready-road ?v ?next)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-road_0002-road_0056-tw_00480
    :parameters (?v - vehicle ?r - road_type_road_0002 ?next - road_type_road_0056 ?w - window_type_tw_00480)
    :precondition (and
      (ready-road ?v ?r)
      (road-next ?r ?next)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
    )
    :effect (and
      (not (ready-road ?v ?r))
      (ready-road ?v ?next)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-road_0002-road_0056-tw_00510
    :parameters (?v - vehicle ?r - road_type_road_0002 ?next - road_type_road_0056 ?w - window_type_tw_00510)
    :precondition (and
      (ready-road ?v ?r)
      (road-next ?r ?next)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
    )
    :effect (and
      (not (ready-road ?v ?r))
      (ready-road ?v ?next)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-road_0002-road_0056-tw_00540
    :parameters (?v - vehicle ?r - road_type_road_0002 ?next - road_type_road_0056 ?w - window_type_tw_00540)
    :precondition (and
      (ready-road ?v ?r)
      (road-next ?r ?next)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
    )
    :effect (and
      (not (ready-road ?v ?r))
      (ready-road ?v ?next)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-road_0002-road_0056-tw_00570
    :parameters (?v - vehicle ?r - road_type_road_0002 ?next - road_type_road_0056 ?w - window_type_tw_00570)
    :precondition (and
      (ready-road ?v ?r)
      (road-next ?r ?next)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
    )
    :effect (and
      (not (ready-road ?v ?r))
      (ready-road ?v ?next)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-road_0002-road_0056-tw_00600
    :parameters (?v - vehicle ?r - road_type_road_0002 ?next - road_type_road_0056 ?w - window_type_tw_00600)
    :precondition (and
      (ready-road ?v ?r)
      (road-next ?r ?next)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
    )
    :effect (and
      (not (ready-road ?v ?r))
      (ready-road ?v ?next)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-road_0002-road_0057-tw_00000
    :parameters (?v - vehicle ?r - road_type_road_0002 ?next - road_type_road_0057 ?w - window_type_tw_00000)
    :precondition (and
      (ready-road ?v ?r)
      (road-next ?r ?next)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
    )
    :effect (and
      (not (ready-road ?v ?r))
      (ready-road ?v ?next)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-road_0002-road_0057-tw_00030
    :parameters (?v - vehicle ?r - road_type_road_0002 ?next - road_type_road_0057 ?w - window_type_tw_00030)
    :precondition (and
      (ready-road ?v ?r)
      (road-next ?r ?next)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
    )
    :effect (and
      (not (ready-road ?v ?r))
      (ready-road ?v ?next)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-road_0002-road_0057-tw_00060
    :parameters (?v - vehicle ?r - road_type_road_0002 ?next - road_type_road_0057 ?w - window_type_tw_00060)
    :precondition (and
      (ready-road ?v ?r)
      (road-next ?r ?next)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
    )
    :effect (and
      (not (ready-road ?v ?r))
      (ready-road ?v ?next)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-road_0002-road_0057-tw_00090
    :parameters (?v - vehicle ?r - road_type_road_0002 ?next - road_type_road_0057 ?w - window_type_tw_00090)
    :precondition (and
      (ready-road ?v ?r)
      (road-next ?r ?next)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
    )
    :effect (and
      (not (ready-road ?v ?r))
      (ready-road ?v ?next)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-road_0002-road_0057-tw_00120
    :parameters (?v - vehicle ?r - road_type_road_0002 ?next - road_type_road_0057 ?w - window_type_tw_00120)
    :precondition (and
      (ready-road ?v ?r)
      (road-next ?r ?next)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
    )
    :effect (and
      (not (ready-road ?v ?r))
      (ready-road ?v ?next)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-road_0002-road_0057-tw_00150
    :parameters (?v - vehicle ?r - road_type_road_0002 ?next - road_type_road_0057 ?w - window_type_tw_00150)
    :precondition (and
      (ready-road ?v ?r)
      (road-next ?r ?next)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
    )
    :effect (and
      (not (ready-road ?v ?r))
      (ready-road ?v ?next)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-road_0002-road_0057-tw_00180
    :parameters (?v - vehicle ?r - road_type_road_0002 ?next - road_type_road_0057 ?w - window_type_tw_00180)
    :precondition (and
      (ready-road ?v ?r)
      (road-next ?r ?next)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
    )
    :effect (and
      (not (ready-road ?v ?r))
      (ready-road ?v ?next)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-road_0002-road_0057-tw_00210
    :parameters (?v - vehicle ?r - road_type_road_0002 ?next - road_type_road_0057 ?w - window_type_tw_00210)
    :precondition (and
      (ready-road ?v ?r)
      (road-next ?r ?next)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
    )
    :effect (and
      (not (ready-road ?v ?r))
      (ready-road ?v ?next)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-road_0002-road_0057-tw_00240
    :parameters (?v - vehicle ?r - road_type_road_0002 ?next - road_type_road_0057 ?w - window_type_tw_00240)
    :precondition (and
      (ready-road ?v ?r)
      (road-next ?r ?next)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
    )
    :effect (and
      (not (ready-road ?v ?r))
      (ready-road ?v ?next)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-road_0002-road_0057-tw_00270
    :parameters (?v - vehicle ?r - road_type_road_0002 ?next - road_type_road_0057 ?w - window_type_tw_00270)
    :precondition (and
      (ready-road ?v ?r)
      (road-next ?r ?next)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
    )
    :effect (and
      (not (ready-road ?v ?r))
      (ready-road ?v ?next)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-road_0002-road_0057-tw_00300
    :parameters (?v - vehicle ?r - road_type_road_0002 ?next - road_type_road_0057 ?w - window_type_tw_00300)
    :precondition (and
      (ready-road ?v ?r)
      (road-next ?r ?next)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
    )
    :effect (and
      (not (ready-road ?v ?r))
      (ready-road ?v ?next)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-road_0002-road_0057-tw_00330
    :parameters (?v - vehicle ?r - road_type_road_0002 ?next - road_type_road_0057 ?w - window_type_tw_00330)
    :precondition (and
      (ready-road ?v ?r)
      (road-next ?r ?next)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
    )
    :effect (and
      (not (ready-road ?v ?r))
      (ready-road ?v ?next)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-road_0002-road_0057-tw_00360
    :parameters (?v - vehicle ?r - road_type_road_0002 ?next - road_type_road_0057 ?w - window_type_tw_00360)
    :precondition (and
      (ready-road ?v ?r)
      (road-next ?r ?next)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
    )
    :effect (and
      (not (ready-road ?v ?r))
      (ready-road ?v ?next)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-road_0002-road_0057-tw_00390
    :parameters (?v - vehicle ?r - road_type_road_0002 ?next - road_type_road_0057 ?w - window_type_tw_00390)
    :precondition (and
      (ready-road ?v ?r)
      (road-next ?r ?next)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
    )
    :effect (and
      (not (ready-road ?v ?r))
      (ready-road ?v ?next)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-road_0002-road_0057-tw_00420
    :parameters (?v - vehicle ?r - road_type_road_0002 ?next - road_type_road_0057 ?w - window_type_tw_00420)
    :precondition (and
      (ready-road ?v ?r)
      (road-next ?r ?next)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
    )
    :effect (and
      (not (ready-road ?v ?r))
      (ready-road ?v ?next)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-road_0002-road_0057-tw_00450
    :parameters (?v - vehicle ?r - road_type_road_0002 ?next - road_type_road_0057 ?w - window_type_tw_00450)
    :precondition (and
      (ready-road ?v ?r)
      (road-next ?r ?next)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
    )
    :effect (and
      (not (ready-road ?v ?r))
      (ready-road ?v ?next)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-road_0002-road_0057-tw_00480
    :parameters (?v - vehicle ?r - road_type_road_0002 ?next - road_type_road_0057 ?w - window_type_tw_00480)
    :precondition (and
      (ready-road ?v ?r)
      (road-next ?r ?next)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
    )
    :effect (and
      (not (ready-road ?v ?r))
      (ready-road ?v ?next)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-road_0002-road_0057-tw_00510
    :parameters (?v - vehicle ?r - road_type_road_0002 ?next - road_type_road_0057 ?w - window_type_tw_00510)
    :precondition (and
      (ready-road ?v ?r)
      (road-next ?r ?next)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
    )
    :effect (and
      (not (ready-road ?v ?r))
      (ready-road ?v ?next)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-road_0002-road_0057-tw_00540
    :parameters (?v - vehicle ?r - road_type_road_0002 ?next - road_type_road_0057 ?w - window_type_tw_00540)
    :precondition (and
      (ready-road ?v ?r)
      (road-next ?r ?next)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
    )
    :effect (and
      (not (ready-road ?v ?r))
      (ready-road ?v ?next)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-road_0002-road_0057-tw_00570
    :parameters (?v - vehicle ?r - road_type_road_0002 ?next - road_type_road_0057 ?w - window_type_tw_00570)
    :precondition (and
      (ready-road ?v ?r)
      (road-next ?r ?next)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
    )
    :effect (and
      (not (ready-road ?v ?r))
      (ready-road ?v ?next)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-road_0002-road_0057-tw_00600
    :parameters (?v - vehicle ?r - road_type_road_0002 ?next - road_type_road_0057 ?w - window_type_tw_00600)
    :precondition (and
      (ready-road ?v ?r)
      (road-next ?r ?next)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
    )
    :effect (and
      (not (ready-road ?v ?r))
      (ready-road ?v ?next)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-static-road_0003-road_0395
    :parameters (?v - vehicle ?r - road_type_road_0003 ?next - road_type_road_0395)
    :precondition (and
      (ready-road ?v ?r)
      (road-next ?r ?next)
      (road-open ?r)
      (static-road ?r)
      (not (road-blocked ?r))
    )
    :effect (and
      (not (ready-road ?v ?r))
      (ready-road ?v ?next)
      (increase (travel-time ?v) (travel-duration ?r))
      (increase (sim-time) (travel-duration ?r))
    )
  )

  (:action traverse-road-static-road_0003-road_0396
    :parameters (?v - vehicle ?r - road_type_road_0003 ?next - road_type_road_0396)
    :precondition (and
      (ready-road ?v ?r)
      (road-next ?r ?next)
      (road-open ?r)
      (static-road ?r)
      (not (road-blocked ?r))
    )
    :effect (and
      (not (ready-road ?v ?r))
      (ready-road ?v ?next)
      (increase (travel-time ?v) (travel-duration ?r))
      (increase (sim-time) (travel-duration ?r))
    )
  )

  (:action traverse-road-static-road_0004-road_0602
    :parameters (?v - vehicle ?r - road_type_road_0004 ?next - road_type_road_0602)
    :precondition (and
      (ready-road ?v ?r)
      (road-next ?r ?next)
      (road-open ?r)
      (static-road ?r)
      (not (road-blocked ?r))
    )
    :effect (and
      (not (ready-road ?v ?r))
      (ready-road ?v ?next)
      (increase (travel-time ?v) (travel-duration ?r))
      (increase (sim-time) (travel-duration ?r))
    )
  )

  (:action traverse-road-static-road_0004-road_0603
    :parameters (?v - vehicle ?r - road_type_road_0004 ?next - road_type_road_0603)
    :precondition (and
      (ready-road ?v ?r)
      (road-next ?r ?next)
      (road-open ?r)
      (static-road ?r)
      (not (road-blocked ?r))
    )
    :effect (and
      (not (ready-road ?v ?r))
      (ready-road ?v ?next)
      (increase (travel-time ?v) (travel-duration ?r))
      (increase (sim-time) (travel-duration ?r))
    )
  )

  (:action traverse-road-static-road_0004-road_0604
    :parameters (?v - vehicle ?r - road_type_road_0004 ?next - road_type_road_0604)
    :precondition (and
      (ready-road ?v ?r)
      (road-next ?r ?next)
      (road-open ?r)
      (static-road ?r)
      (not (road-blocked ?r))
    )
    :effect (and
      (not (ready-road ?v ?r))
      (ready-road ?v ?next)
      (increase (travel-time ?v) (travel-duration ?r))
      (increase (sim-time) (travel-duration ?r))
    )
  )

  (:action traverse-road-static-road_0006-road_0187
    :parameters (?v - vehicle ?r - road_type_road_0006 ?next - road_type_road_0187)
    :precondition (and
      (ready-road ?v ?r)
      (road-next ?r ?next)
      (road-open ?r)
      (static-road ?r)
      (not (road-blocked ?r))
    )
    :effect (and
      (not (ready-road ?v ?r))
      (ready-road ?v ?next)
      (increase (travel-time ?v) (travel-duration ?r))
      (increase (sim-time) (travel-duration ?r))
    )
  )

  (:action traverse-road-static-road_0006-road_0188
    :parameters (?v - vehicle ?r - road_type_road_0006 ?next - road_type_road_0188)
    :precondition (and
      (ready-road ?v ?r)
      (road-next ?r ?next)
      (road-open ?r)
      (static-road ?r)
      (not (road-blocked ?r))
    )
    :effect (and
      (not (ready-road ?v ?r))
      (ready-road ?v ?next)
      (increase (travel-time ?v) (travel-duration ?r))
      (increase (sim-time) (travel-duration ?r))
    )
  )

  (:action traverse-road-static-road_0007-road_0002
    :parameters (?v - vehicle ?r - road_type_road_0007 ?next - road_type_road_0002)
    :precondition (and
      (ready-road ?v ?r)
      (road-next ?r ?next)
      (road-open ?r)
      (static-road ?r)
      (not (road-blocked ?r))
    )
    :effect (and
      (not (ready-road ?v ?r))
      (ready-road ?v ?next)
      (increase (travel-time ?v) (travel-duration ?r))
      (increase (sim-time) (travel-duration ?r))
    )
  )

  (:action traverse-road-static-road_0008-road_0011
    :parameters (?v - vehicle ?r - road_type_road_0008 ?next - road_type_road_0011)
    :precondition (and
      (ready-road ?v ?r)
      (road-next ?r ?next)
      (road-open ?r)
      (static-road ?r)
      (not (road-blocked ?r))
    )
    :effect (and
      (not (ready-road ?v ?r))
      (ready-road ?v ?next)
      (increase (travel-time ?v) (travel-duration ?r))
      (increase (sim-time) (travel-duration ?r))
    )
  )

  (:action traverse-road-static-road_0009-road_0006
    :parameters (?v - vehicle ?r - road_type_road_0009 ?next - road_type_road_0006)
    :precondition (and
      (ready-road ?v ?r)
      (road-next ?r ?next)
      (road-open ?r)
      (static-road ?r)
      (not (road-blocked ?r))
    )
    :effect (and
      (not (ready-road ?v ?r))
      (ready-road ?v ?next)
      (increase (travel-time ?v) (travel-duration ?r))
      (increase (sim-time) (travel-duration ?r))
    )
  )

  (:action traverse-road-static-road_0009-road_0007
    :parameters (?v - vehicle ?r - road_type_road_0009 ?next - road_type_road_0007)
    :precondition (and
      (ready-road ?v ?r)
      (road-next ?r ?next)
      (road-open ?r)
      (static-road ?r)
      (not (road-blocked ?r))
    )
    :effect (and
      (not (ready-road ?v ?r))
      (ready-road ?v ?next)
      (increase (travel-time ?v) (travel-duration ?r))
      (increase (sim-time) (travel-duration ?r))
    )
  )

  (:action traverse-road-static-road_0010-road_0211
    :parameters (?v - vehicle ?r - road_type_road_0010 ?next - road_type_road_0211)
    :precondition (and
      (ready-road ?v ?r)
      (road-next ?r ?next)
      (road-open ?r)
      (static-road ?r)
      (not (road-blocked ?r))
    )
    :effect (and
      (not (ready-road ?v ?r))
      (ready-road ?v ?next)
      (increase (travel-time ?v) (travel-duration ?r))
      (increase (sim-time) (travel-duration ?r))
    )
  )

  (:action traverse-road-static-road_0010-road_0212
    :parameters (?v - vehicle ?r - road_type_road_0010 ?next - road_type_road_0212)
    :precondition (and
      (ready-road ?v ?r)
      (road-next ?r ?next)
      (road-open ?r)
      (static-road ?r)
      (not (road-blocked ?r))
    )
    :effect (and
      (not (ready-road ?v ?r))
      (ready-road ?v ?next)
      (increase (travel-time ?v) (travel-duration ?r))
      (increase (sim-time) (travel-duration ?r))
    )
  )

  (:action traverse-road-static-road_0011-road_0548
    :parameters (?v - vehicle ?r - road_type_road_0011 ?next - road_type_road_0548)
    :precondition (and
      (ready-road ?v ?r)
      (road-next ?r ?next)
      (road-open ?r)
      (static-road ?r)
      (not (road-blocked ?r))
    )
    :effect (and
      (not (ready-road ?v ?r))
      (ready-road ?v ?next)
      (increase (travel-time ?v) (travel-duration ?r))
      (increase (sim-time) (travel-duration ?r))
    )
  )

  (:action traverse-road-static-road_0011-road_0549
    :parameters (?v - vehicle ?r - road_type_road_0011 ?next - road_type_road_0549)
    :precondition (and
      (ready-road ?v ?r)
      (road-next ?r ?next)
      (road-open ?r)
      (static-road ?r)
      (not (road-blocked ?r))
    )
    :effect (and
      (not (ready-road ?v ?r))
      (ready-road ?v ?next)
      (increase (travel-time ?v) (travel-duration ?r))
      (increase (sim-time) (travel-duration ?r))
    )
  )

  (:action traverse-road-static-road_0012-road_0192
    :parameters (?v - vehicle ?r - road_type_road_0012 ?next - road_type_road_0192)
    :precondition (and
      (ready-road ?v ?r)
      (road-next ?r ?next)
      (road-open ?r)
      (static-road ?r)
      (not (road-blocked ?r))
    )
    :effect (and
      (not (ready-road ?v ?r))
      (ready-road ?v ?next)
      (increase (travel-time ?v) (travel-duration ?r))
      (increase (sim-time) (travel-duration ?r))
    )
  )

  (:action traverse-road-static-road_0013-road_0011
    :parameters (?v - vehicle ?r - road_type_road_0013 ?next - road_type_road_0011)
    :precondition (and
      (ready-road ?v ?r)
      (road-next ?r ?next)
      (road-open ?r)
      (static-road ?r)
      (not (road-blocked ?r))
    )
    :effect (and
      (not (ready-road ?v ?r))
      (ready-road ?v ?next)
      (increase (travel-time ?v) (travel-duration ?r))
      (increase (sim-time) (travel-duration ?r))
    )
  )

  (:action traverse-road-static-road_0014-macro_0046
    :parameters (?v - vehicle ?r - road_type_road_0014 ?next - road_type_macro_0046)
    :precondition (and
      (ready-road ?v ?r)
      (road-next ?r ?next)
      (road-open ?r)
      (static-road ?r)
      (not (road-blocked ?r))
    )
    :effect (and
      (not (ready-road ?v ?r))
      (ready-road ?v ?next)
      (increase (travel-time ?v) (travel-duration ?r))
      (increase (sim-time) (travel-duration ?r))
    )
  )

  (:action traverse-road-static-road_0014-road_0647
    :parameters (?v - vehicle ?r - road_type_road_0014 ?next - road_type_road_0647)
    :precondition (and
      (ready-road ?v ?r)
      (road-next ?r ?next)
      (road-open ?r)
      (static-road ?r)
      (not (road-blocked ?r))
    )
    :effect (and
      (not (ready-road ?v ?r))
      (ready-road ?v ?next)
      (increase (travel-time ?v) (travel-duration ?r))
      (increase (sim-time) (travel-duration ?r))
    )
  )

  (:action traverse-road-static-road_0016-road_0854
    :parameters (?v - vehicle ?r - road_type_road_0016 ?next - road_type_road_0854)
    :precondition (and
      (ready-road ?v ?r)
      (road-next ?r ?next)
      (road-open ?r)
      (static-road ?r)
      (not (road-blocked ?r))
    )
    :effect (and
      (not (ready-road ?v ?r))
      (ready-road ?v ?next)
      (increase (travel-time ?v) (travel-duration ?r))
      (increase (sim-time) (travel-duration ?r))
    )
  )

  (:action traverse-road-static-road_0016-road_0855
    :parameters (?v - vehicle ?r - road_type_road_0016 ?next - road_type_road_0855)
    :precondition (and
      (ready-road ?v ?r)
      (road-next ?r ?next)
      (road-open ?r)
      (static-road ?r)
      (not (road-blocked ?r))
    )
    :effect (and
      (not (ready-road ?v ?r))
      (ready-road ?v ?next)
      (increase (travel-time ?v) (travel-duration ?r))
      (increase (sim-time) (travel-duration ?r))
    )
  )

  (:action traverse-road-static-road_0017-road_0000
    :parameters (?v - vehicle ?r - road_type_road_0017 ?next - road_type_road_0000)
    :precondition (and
      (ready-road ?v ?r)
      (road-next ?r ?next)
      (road-open ?r)
      (static-road ?r)
      (not (road-blocked ?r))
    )
    :effect (and
      (not (ready-road ?v ?r))
      (ready-road ?v ?next)
      (increase (travel-time ?v) (travel-duration ?r))
      (increase (sim-time) (travel-duration ?r))
    )
  )

  (:action traverse-road-static-road_0017-road_0001
    :parameters (?v - vehicle ?r - road_type_road_0017 ?next - road_type_road_0001)
    :precondition (and
      (ready-road ?v ?r)
      (road-next ?r ?next)
      (road-open ?r)
      (static-road ?r)
      (not (road-blocked ?r))
    )
    :effect (and
      (not (ready-road ?v ?r))
      (ready-road ?v ?next)
      (increase (travel-time ?v) (travel-duration ?r))
      (increase (sim-time) (travel-duration ?r))
    )
  )

  (:action traverse-road-static-road_0018-road_0810
    :parameters (?v - vehicle ?r - road_type_road_0018 ?next - road_type_road_0810)
    :precondition (and
      (ready-road ?v ?r)
      (road-next ?r ?next)
      (road-open ?r)
      (static-road ?r)
      (not (road-blocked ?r))
    )
    :effect (and
      (not (ready-road ?v ?r))
      (ready-road ?v ?next)
      (increase (travel-time ?v) (travel-duration ?r))
      (increase (sim-time) (travel-duration ?r))
    )
  )

  (:action traverse-road-static-road_0018-road_0811
    :parameters (?v - vehicle ?r - road_type_road_0018 ?next - road_type_road_0811)
    :precondition (and
      (ready-road ?v ?r)
      (road-next ?r ?next)
      (road-open ?r)
      (static-road ?r)
      (not (road-blocked ?r))
    )
    :effect (and
      (not (ready-road ?v ?r))
      (ready-road ?v ?next)
      (increase (travel-time ?v) (travel-duration ?r))
      (increase (sim-time) (travel-duration ?r))
    )
  )

  (:action traverse-road-static-road_0019-macro_0026
    :parameters (?v - vehicle ?r - road_type_road_0019 ?next - road_type_macro_0026)
    :precondition (and
      (ready-road ?v ?r)
      (road-next ?r ?next)
      (road-open ?r)
      (static-road ?r)
      (not (road-blocked ?r))
    )
    :effect (and
      (not (ready-road ?v ?r))
      (ready-road ?v ?next)
      (increase (travel-time ?v) (travel-duration ?r))
      (increase (sim-time) (travel-duration ?r))
    )
  )

  (:action traverse-road-static-road_0019-road_0307
    :parameters (?v - vehicle ?r - road_type_road_0019 ?next - road_type_road_0307)
    :precondition (and
      (ready-road ?v ?r)
      (road-next ?r ?next)
      (road-open ?r)
      (static-road ?r)
      (not (road-blocked ?r))
    )
    :effect (and
      (not (ready-road ?v ?r))
      (ready-road ?v ?next)
      (increase (travel-time ?v) (travel-duration ?r))
      (increase (sim-time) (travel-duration ?r))
    )
  )

  (:action traverse-road-static-road_0020-road_0556
    :parameters (?v - vehicle ?r - road_type_road_0020 ?next - road_type_road_0556)
    :precondition (and
      (ready-road ?v ?r)
      (road-next ?r ?next)
      (road-open ?r)
      (static-road ?r)
      (not (road-blocked ?r))
    )
    :effect (and
      (not (ready-road ?v ?r))
      (ready-road ?v ?next)
      (increase (travel-time ?v) (travel-duration ?r))
      (increase (sim-time) (travel-duration ?r))
    )
  )

  (:action traverse-road-static-road_0020-road_0557
    :parameters (?v - vehicle ?r - road_type_road_0020 ?next - road_type_road_0557)
    :precondition (and
      (ready-road ?v ?r)
      (road-next ?r ?next)
      (road-open ?r)
      (static-road ?r)
      (not (road-blocked ?r))
    )
    :effect (and
      (not (ready-road ?v ?r))
      (ready-road ?v ?next)
      (increase (travel-time ?v) (travel-duration ?r))
      (increase (sim-time) (travel-duration ?r))
    )
  )

  (:action traverse-road-static-road_0021-road_0643
    :parameters (?v - vehicle ?r - road_type_road_0021 ?next - road_type_road_0643)
    :precondition (and
      (ready-road ?v ?r)
      (road-next ?r ?next)
      (road-open ?r)
      (static-road ?r)
      (not (road-blocked ?r))
    )
    :effect (and
      (not (ready-road ?v ?r))
      (ready-road ?v ?next)
      (increase (travel-time ?v) (travel-duration ?r))
      (increase (sim-time) (travel-duration ?r))
    )
  )

  (:action traverse-road-static-road_0021-road_0644
    :parameters (?v - vehicle ?r - road_type_road_0021 ?next - road_type_road_0644)
    :precondition (and
      (ready-road ?v ?r)
      (road-next ?r ?next)
      (road-open ?r)
      (static-road ?r)
      (not (road-blocked ?r))
    )
    :effect (and
      (not (ready-road ?v ?r))
      (ready-road ?v ?next)
      (increase (travel-time ?v) (travel-duration ?r))
      (increase (sim-time) (travel-duration ?r))
    )
  )

  (:action traverse-road-static-road_0021-road_0645
    :parameters (?v - vehicle ?r - road_type_road_0021 ?next - road_type_road_0645)
    :precondition (and
      (ready-road ?v ?r)
      (road-next ?r ?next)
      (road-open ?r)
      (static-road ?r)
      (not (road-blocked ?r))
    )
    :effect (and
      (not (ready-road ?v ?r))
      (ready-road ?v ?next)
      (increase (travel-time ?v) (travel-duration ?r))
      (increase (sim-time) (travel-duration ?r))
    )
  )

  (:action traverse-road-static-road_0023-road_0028
    :parameters (?v - vehicle ?r - road_type_road_0023 ?next - road_type_road_0028)
    :precondition (and
      (ready-road ?v ?r)
      (road-next ?r ?next)
      (road-open ?r)
      (static-road ?r)
      (not (road-blocked ?r))
    )
    :effect (and
      (not (ready-road ?v ?r))
      (ready-road ?v ?next)
      (increase (travel-time ?v) (travel-duration ?r))
      (increase (sim-time) (travel-duration ?r))
    )
  )

  (:action traverse-road-static-road_0024-road_0027
    :parameters (?v - vehicle ?r - road_type_road_0024 ?next - road_type_road_0027)
    :precondition (and
      (ready-road ?v ?r)
      (road-next ?r ?next)
      (road-open ?r)
      (static-road ?r)
      (not (road-blocked ?r))
    )
    :effect (and
      (not (ready-road ?v ?r))
      (ready-road ?v ?next)
      (increase (travel-time ?v) (travel-duration ?r))
      (increase (sim-time) (travel-duration ?r))
    )
  )

  (:action traverse-road-static-road_0025-macro_0034
    :parameters (?v - vehicle ?r - road_type_road_0025 ?next - road_type_macro_0034)
    :precondition (and
      (ready-road ?v ?r)
      (road-next ?r ?next)
      (road-open ?r)
      (static-road ?r)
      (not (road-blocked ?r))
    )
    :effect (and
      (not (ready-road ?v ?r))
      (ready-road ?v ?next)
      (increase (travel-time ?v) (travel-duration ?r))
      (increase (sim-time) (travel-duration ?r))
    )
  )

  (:action traverse-road-static-road_0025-road_0434
    :parameters (?v - vehicle ?r - road_type_road_0025 ?next - road_type_road_0434)
    :precondition (and
      (ready-road ?v ?r)
      (road-next ?r ?next)
      (road-open ?r)
      (static-road ?r)
      (not (road-blocked ?r))
    )
    :effect (and
      (not (ready-road ?v ?r))
      (ready-road ?v ?next)
      (increase (travel-time ?v) (travel-duration ?r))
      (increase (sim-time) (travel-duration ?r))
    )
  )

  (:action traverse-road-static-road_0025-road_0436
    :parameters (?v - vehicle ?r - road_type_road_0025 ?next - road_type_road_0436)
    :precondition (and
      (ready-road ?v ?r)
      (road-next ?r ?next)
      (road-open ?r)
      (static-road ?r)
      (not (road-blocked ?r))
    )
    :effect (and
      (not (ready-road ?v ?r))
      (ready-road ?v ?next)
      (increase (travel-time ?v) (travel-duration ?r))
      (increase (sim-time) (travel-duration ?r))
    )
  )

  (:action traverse-road-static-road_0026-road_0137
    :parameters (?v - vehicle ?r - road_type_road_0026 ?next - road_type_road_0137)
    :precondition (and
      (ready-road ?v ?r)
      (road-next ?r ?next)
      (road-open ?r)
      (static-road ?r)
      (not (road-blocked ?r))
    )
    :effect (and
      (not (ready-road ?v ?r))
      (ready-road ?v ?next)
      (increase (travel-time ?v) (travel-duration ?r))
      (increase (sim-time) (travel-duration ?r))
    )
  )

  (:action traverse-road-static-road_0026-road_0138
    :parameters (?v - vehicle ?r - road_type_road_0026 ?next - road_type_road_0138)
    :precondition (and
      (ready-road ?v ?r)
      (road-next ?r ?next)
      (road-open ?r)
      (static-road ?r)
      (not (road-blocked ?r))
    )
    :effect (and
      (not (ready-road ?v ?r))
      (ready-road ?v ?next)
      (increase (travel-time ?v) (travel-duration ?r))
      (increase (sim-time) (travel-duration ?r))
    )
  )

  (:action traverse-road-static-road_0027-road_0182
    :parameters (?v - vehicle ?r - road_type_road_0027 ?next - road_type_road_0182)
    :precondition (and
      (ready-road ?v ?r)
      (road-next ?r ?next)
      (road-open ?r)
      (static-road ?r)
      (not (road-blocked ?r))
    )
    :effect (and
      (not (ready-road ?v ?r))
      (ready-road ?v ?next)
      (increase (travel-time ?v) (travel-duration ?r))
      (increase (sim-time) (travel-duration ?r))
    )
  )

  (:action traverse-road-static-road_0027-road_0183
    :parameters (?v - vehicle ?r - road_type_road_0027 ?next - road_type_road_0183)
    :precondition (and
      (ready-road ?v ?r)
      (road-next ?r ?next)
      (road-open ?r)
      (static-road ?r)
      (not (road-blocked ?r))
    )
    :effect (and
      (not (ready-road ?v ?r))
      (ready-road ?v ?next)
      (increase (travel-time ?v) (travel-duration ?r))
      (increase (sim-time) (travel-duration ?r))
    )
  )

  (:action traverse-road-static-road_0028-road_0022
    :parameters (?v - vehicle ?r - road_type_road_0028 ?next - road_type_road_0022)
    :precondition (and
      (ready-road ?v ?r)
      (road-next ?r ?next)
      (road-open ?r)
      (static-road ?r)
      (not (road-blocked ?r))
    )
    :effect (and
      (not (ready-road ?v ?r))
      (ready-road ?v ?next)
      (increase (travel-time ?v) (travel-duration ?r))
      (increase (sim-time) (travel-duration ?r))
    )
  )

  (:action traverse-road-static-road_0028-road_0023
    :parameters (?v - vehicle ?r - road_type_road_0028 ?next - road_type_road_0023)
    :precondition (and
      (ready-road ?v ?r)
      (road-next ?r ?next)
      (road-open ?r)
      (static-road ?r)
      (not (road-blocked ?r))
    )
    :effect (and
      (not (ready-road ?v ?r))
      (ready-road ?v ?next)
      (increase (travel-time ?v) (travel-duration ?r))
      (increase (sim-time) (travel-duration ?r))
    )
  )

  (:action traverse-road-static-road_0029-road_0785
    :parameters (?v - vehicle ?r - road_type_road_0029 ?next - road_type_road_0785)
    :precondition (and
      (ready-road ?v ?r)
      (road-next ?r ?next)
      (road-open ?r)
      (static-road ?r)
      (not (road-blocked ?r))
    )
    :effect (and
      (not (ready-road ?v ?r))
      (ready-road ?v ?next)
      (increase (travel-time ?v) (travel-duration ?r))
      (increase (sim-time) (travel-duration ?r))
    )
  )

  (:action traverse-road-static-road_0030-road_0018
    :parameters (?v - vehicle ?r - road_type_road_0030 ?next - road_type_road_0018)
    :precondition (and
      (ready-road ?v ?r)
      (road-next ?r ?next)
      (road-open ?r)
      (static-road ?r)
      (not (road-blocked ?r))
    )
    :effect (and
      (not (ready-road ?v ?r))
      (ready-road ?v ?next)
      (increase (travel-time ?v) (travel-duration ?r))
      (increase (sim-time) (travel-duration ?r))
    )
  )

  (:action traverse-road-static-road_0031-macro_0033
    :parameters (?v - vehicle ?r - road_type_road_0031 ?next - road_type_macro_0033)
    :precondition (and
      (ready-road ?v ?r)
      (road-next ?r ?next)
      (road-open ?r)
      (static-road ?r)
      (not (road-blocked ?r))
    )
    :effect (and
      (not (ready-road ?v ?r))
      (ready-road ?v ?next)
      (increase (travel-time ?v) (travel-duration ?r))
      (increase (sim-time) (travel-duration ?r))
    )
  )

  (:action traverse-road-static-road_0031-road_0428
    :parameters (?v - vehicle ?r - road_type_road_0031 ?next - road_type_road_0428)
    :precondition (and
      (ready-road ?v ?r)
      (road-next ?r ?next)
      (road-open ?r)
      (static-road ?r)
      (not (road-blocked ?r))
    )
    :effect (and
      (not (ready-road ?v ?r))
      (ready-road ?v ?next)
      (increase (travel-time ?v) (travel-duration ?r))
      (increase (sim-time) (travel-duration ?r))
    )
  )

  (:action traverse-road-static-road_0031-road_0430
    :parameters (?v - vehicle ?r - road_type_road_0031 ?next - road_type_road_0430)
    :precondition (and
      (ready-road ?v ?r)
      (road-next ?r ?next)
      (road-open ?r)
      (static-road ?r)
      (not (road-blocked ?r))
    )
    :effect (and
      (not (ready-road ?v ?r))
      (ready-road ?v ?next)
      (increase (travel-time ?v) (travel-duration ?r))
      (increase (sim-time) (travel-duration ?r))
    )
  )

  (:action traverse-road-static-road_0032-road_0424
    :parameters (?v - vehicle ?r - road_type_road_0032 ?next - road_type_road_0424)
    :precondition (and
      (ready-road ?v ?r)
      (road-next ?r ?next)
      (road-open ?r)
      (static-road ?r)
      (not (road-blocked ?r))
    )
    :effect (and
      (not (ready-road ?v ?r))
      (ready-road ?v ?next)
      (increase (travel-time ?v) (travel-duration ?r))
      (increase (sim-time) (travel-duration ?r))
    )
  )

  (:action traverse-road-static-road_0032-road_0425
    :parameters (?v - vehicle ?r - road_type_road_0032 ?next - road_type_road_0425)
    :precondition (and
      (ready-road ?v ?r)
      (road-next ?r ?next)
      (road-open ?r)
      (static-road ?r)
      (not (road-blocked ?r))
    )
    :effect (and
      (not (ready-road ?v ?r))
      (ready-road ?v ?next)
      (increase (travel-time ?v) (travel-duration ?r))
      (increase (sim-time) (travel-duration ?r))
    )
  )

  (:action traverse-road-static-road_0033-road_0655
    :parameters (?v - vehicle ?r - road_type_road_0033 ?next - road_type_road_0655)
    :precondition (and
      (ready-road ?v ?r)
      (road-next ?r ?next)
      (road-open ?r)
      (static-road ?r)
      (not (road-blocked ?r))
    )
    :effect (and
      (not (ready-road ?v ?r))
      (ready-road ?v ?next)
      (increase (travel-time ?v) (travel-duration ?r))
      (increase (sim-time) (travel-duration ?r))
    )
  )

  (:action traverse-road-static-road_0033-road_0656
    :parameters (?v - vehicle ?r - road_type_road_0033 ?next - road_type_road_0656)
    :precondition (and
      (ready-road ?v ?r)
      (road-next ?r ?next)
      (road-open ?r)
      (static-road ?r)
      (not (road-blocked ?r))
    )
    :effect (and
      (not (ready-road ?v ?r))
      (ready-road ?v ?next)
      (increase (travel-time ?v) (travel-duration ?r))
      (increase (sim-time) (travel-duration ?r))
    )
  )

  (:action traverse-road-static-road_0034-road_0444
    :parameters (?v - vehicle ?r - road_type_road_0034 ?next - road_type_road_0444)
    :precondition (and
      (ready-road ?v ?r)
      (road-next ?r ?next)
      (road-open ?r)
      (static-road ?r)
      (not (road-blocked ?r))
    )
    :effect (and
      (not (ready-road ?v ?r))
      (ready-road ?v ?next)
      (increase (travel-time ?v) (travel-duration ?r))
      (increase (sim-time) (travel-duration ?r))
    )
  )

  (:action traverse-road-static-road_0034-road_0445
    :parameters (?v - vehicle ?r - road_type_road_0034 ?next - road_type_road_0445)
    :precondition (and
      (ready-road ?v ?r)
      (road-next ?r ?next)
      (road-open ?r)
      (static-road ?r)
      (not (road-blocked ?r))
    )
    :effect (and
      (not (ready-road ?v ?r))
      (ready-road ?v ?next)
      (increase (travel-time ?v) (travel-duration ?r))
      (increase (sim-time) (travel-duration ?r))
    )
  )

  (:action traverse-road-static-road_0034-road_0446
    :parameters (?v - vehicle ?r - road_type_road_0034 ?next - road_type_road_0446)
    :precondition (and
      (ready-road ?v ?r)
      (road-next ?r ?next)
      (road-open ?r)
      (static-road ?r)
      (not (road-blocked ?r))
    )
    :effect (and
      (not (ready-road ?v ?r))
      (ready-road ?v ?next)
      (increase (travel-time ?v) (travel-duration ?r))
      (increase (sim-time) (travel-duration ?r))
    )
  )

  (:action traverse-road-dynamic-road_0035-road_0135-tw_00000
    :parameters (?v - vehicle ?r - road_type_road_0035 ?next - road_type_road_0135 ?w - window_type_tw_00000)
    :precondition (and
      (ready-road ?v ?r)
      (road-next ?r ?next)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
    )
    :effect (and
      (not (ready-road ?v ?r))
      (ready-road ?v ?next)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-road_0035-road_0135-tw_00030
    :parameters (?v - vehicle ?r - road_type_road_0035 ?next - road_type_road_0135 ?w - window_type_tw_00030)
    :precondition (and
      (ready-road ?v ?r)
      (road-next ?r ?next)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
    )
    :effect (and
      (not (ready-road ?v ?r))
      (ready-road ?v ?next)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-road_0035-road_0135-tw_00060
    :parameters (?v - vehicle ?r - road_type_road_0035 ?next - road_type_road_0135 ?w - window_type_tw_00060)
    :precondition (and
      (ready-road ?v ?r)
      (road-next ?r ?next)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
    )
    :effect (and
      (not (ready-road ?v ?r))
      (ready-road ?v ?next)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-road_0035-road_0135-tw_00090
    :parameters (?v - vehicle ?r - road_type_road_0035 ?next - road_type_road_0135 ?w - window_type_tw_00090)
    :precondition (and
      (ready-road ?v ?r)
      (road-next ?r ?next)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
    )
    :effect (and
      (not (ready-road ?v ?r))
      (ready-road ?v ?next)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-road_0035-road_0135-tw_00120
    :parameters (?v - vehicle ?r - road_type_road_0035 ?next - road_type_road_0135 ?w - window_type_tw_00120)
    :precondition (and
      (ready-road ?v ?r)
      (road-next ?r ?next)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
    )
    :effect (and
      (not (ready-road ?v ?r))
      (ready-road ?v ?next)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-road_0035-road_0135-tw_00150
    :parameters (?v - vehicle ?r - road_type_road_0035 ?next - road_type_road_0135 ?w - window_type_tw_00150)
    :precondition (and
      (ready-road ?v ?r)
      (road-next ?r ?next)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
    )
    :effect (and
      (not (ready-road ?v ?r))
      (ready-road ?v ?next)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-road_0035-road_0135-tw_00180
    :parameters (?v - vehicle ?r - road_type_road_0035 ?next - road_type_road_0135 ?w - window_type_tw_00180)
    :precondition (and
      (ready-road ?v ?r)
      (road-next ?r ?next)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
    )
    :effect (and
      (not (ready-road ?v ?r))
      (ready-road ?v ?next)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-road_0035-road_0135-tw_00210
    :parameters (?v - vehicle ?r - road_type_road_0035 ?next - road_type_road_0135 ?w - window_type_tw_00210)
    :precondition (and
      (ready-road ?v ?r)
      (road-next ?r ?next)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
    )
    :effect (and
      (not (ready-road ?v ?r))
      (ready-road ?v ?next)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-road_0035-road_0135-tw_00240
    :parameters (?v - vehicle ?r - road_type_road_0035 ?next - road_type_road_0135 ?w - window_type_tw_00240)
    :precondition (and
      (ready-road ?v ?r)
      (road-next ?r ?next)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
    )
    :effect (and
      (not (ready-road ?v ?r))
      (ready-road ?v ?next)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-road_0035-road_0135-tw_00270
    :parameters (?v - vehicle ?r - road_type_road_0035 ?next - road_type_road_0135 ?w - window_type_tw_00270)
    :precondition (and
      (ready-road ?v ?r)
      (road-next ?r ?next)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
    )
    :effect (and
      (not (ready-road ?v ?r))
      (ready-road ?v ?next)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-road_0035-road_0135-tw_00300
    :parameters (?v - vehicle ?r - road_type_road_0035 ?next - road_type_road_0135 ?w - window_type_tw_00300)
    :precondition (and
      (ready-road ?v ?r)
      (road-next ?r ?next)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
    )
    :effect (and
      (not (ready-road ?v ?r))
      (ready-road ?v ?next)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-road_0035-road_0135-tw_00330
    :parameters (?v - vehicle ?r - road_type_road_0035 ?next - road_type_road_0135 ?w - window_type_tw_00330)
    :precondition (and
      (ready-road ?v ?r)
      (road-next ?r ?next)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
    )
    :effect (and
      (not (ready-road ?v ?r))
      (ready-road ?v ?next)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-road_0035-road_0135-tw_00360
    :parameters (?v - vehicle ?r - road_type_road_0035 ?next - road_type_road_0135 ?w - window_type_tw_00360)
    :precondition (and
      (ready-road ?v ?r)
      (road-next ?r ?next)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
    )
    :effect (and
      (not (ready-road ?v ?r))
      (ready-road ?v ?next)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-road_0035-road_0135-tw_00390
    :parameters (?v - vehicle ?r - road_type_road_0035 ?next - road_type_road_0135 ?w - window_type_tw_00390)
    :precondition (and
      (ready-road ?v ?r)
      (road-next ?r ?next)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
    )
    :effect (and
      (not (ready-road ?v ?r))
      (ready-road ?v ?next)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-road_0035-road_0135-tw_00420
    :parameters (?v - vehicle ?r - road_type_road_0035 ?next - road_type_road_0135 ?w - window_type_tw_00420)
    :precondition (and
      (ready-road ?v ?r)
      (road-next ?r ?next)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
    )
    :effect (and
      (not (ready-road ?v ?r))
      (ready-road ?v ?next)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-road_0035-road_0135-tw_00450
    :parameters (?v - vehicle ?r - road_type_road_0035 ?next - road_type_road_0135 ?w - window_type_tw_00450)
    :precondition (and
      (ready-road ?v ?r)
      (road-next ?r ?next)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
    )
    :effect (and
      (not (ready-road ?v ?r))
      (ready-road ?v ?next)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-road_0035-road_0135-tw_00480
    :parameters (?v - vehicle ?r - road_type_road_0035 ?next - road_type_road_0135 ?w - window_type_tw_00480)
    :precondition (and
      (ready-road ?v ?r)
      (road-next ?r ?next)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
    )
    :effect (and
      (not (ready-road ?v ?r))
      (ready-road ?v ?next)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-road_0035-road_0135-tw_00510
    :parameters (?v - vehicle ?r - road_type_road_0035 ?next - road_type_road_0135 ?w - window_type_tw_00510)
    :precondition (and
      (ready-road ?v ?r)
      (road-next ?r ?next)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
    )
    :effect (and
      (not (ready-road ?v ?r))
      (ready-road ?v ?next)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-road_0035-road_0135-tw_00540
    :parameters (?v - vehicle ?r - road_type_road_0035 ?next - road_type_road_0135 ?w - window_type_tw_00540)
    :precondition (and
      (ready-road ?v ?r)
      (road-next ?r ?next)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
    )
    :effect (and
      (not (ready-road ?v ?r))
      (ready-road ?v ?next)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-road_0035-road_0135-tw_00570
    :parameters (?v - vehicle ?r - road_type_road_0035 ?next - road_type_road_0135 ?w - window_type_tw_00570)
    :precondition (and
      (ready-road ?v ?r)
      (road-next ?r ?next)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
    )
    :effect (and
      (not (ready-road ?v ?r))
      (ready-road ?v ?next)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-road_0035-road_0135-tw_00600
    :parameters (?v - vehicle ?r - road_type_road_0035 ?next - road_type_road_0135 ?w - window_type_tw_00600)
    :precondition (and
      (ready-road ?v ?r)
      (road-next ?r ?next)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
    )
    :effect (and
      (not (ready-road ?v ?r))
      (ready-road ?v ?next)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-road_0035-road_0136-tw_00000
    :parameters (?v - vehicle ?r - road_type_road_0035 ?next - road_type_road_0136 ?w - window_type_tw_00000)
    :precondition (and
      (ready-road ?v ?r)
      (road-next ?r ?next)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
    )
    :effect (and
      (not (ready-road ?v ?r))
      (ready-road ?v ?next)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-road_0035-road_0136-tw_00030
    :parameters (?v - vehicle ?r - road_type_road_0035 ?next - road_type_road_0136 ?w - window_type_tw_00030)
    :precondition (and
      (ready-road ?v ?r)
      (road-next ?r ?next)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
    )
    :effect (and
      (not (ready-road ?v ?r))
      (ready-road ?v ?next)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-road_0035-road_0136-tw_00060
    :parameters (?v - vehicle ?r - road_type_road_0035 ?next - road_type_road_0136 ?w - window_type_tw_00060)
    :precondition (and
      (ready-road ?v ?r)
      (road-next ?r ?next)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
    )
    :effect (and
      (not (ready-road ?v ?r))
      (ready-road ?v ?next)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-road_0035-road_0136-tw_00090
    :parameters (?v - vehicle ?r - road_type_road_0035 ?next - road_type_road_0136 ?w - window_type_tw_00090)
    :precondition (and
      (ready-road ?v ?r)
      (road-next ?r ?next)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
    )
    :effect (and
      (not (ready-road ?v ?r))
      (ready-road ?v ?next)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-road_0035-road_0136-tw_00120
    :parameters (?v - vehicle ?r - road_type_road_0035 ?next - road_type_road_0136 ?w - window_type_tw_00120)
    :precondition (and
      (ready-road ?v ?r)
      (road-next ?r ?next)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
    )
    :effect (and
      (not (ready-road ?v ?r))
      (ready-road ?v ?next)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-road_0035-road_0136-tw_00150
    :parameters (?v - vehicle ?r - road_type_road_0035 ?next - road_type_road_0136 ?w - window_type_tw_00150)
    :precondition (and
      (ready-road ?v ?r)
      (road-next ?r ?next)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
    )
    :effect (and
      (not (ready-road ?v ?r))
      (ready-road ?v ?next)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-road_0035-road_0136-tw_00180
    :parameters (?v - vehicle ?r - road_type_road_0035 ?next - road_type_road_0136 ?w - window_type_tw_00180)
    :precondition (and
      (ready-road ?v ?r)
      (road-next ?r ?next)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
    )
    :effect (and
      (not (ready-road ?v ?r))
      (ready-road ?v ?next)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-road_0035-road_0136-tw_00210
    :parameters (?v - vehicle ?r - road_type_road_0035 ?next - road_type_road_0136 ?w - window_type_tw_00210)
    :precondition (and
      (ready-road ?v ?r)
      (road-next ?r ?next)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
    )
    :effect (and
      (not (ready-road ?v ?r))
      (ready-road ?v ?next)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-road_0035-road_0136-tw_00240
    :parameters (?v - vehicle ?r - road_type_road_0035 ?next - road_type_road_0136 ?w - window_type_tw_00240)
    :precondition (and
      (ready-road ?v ?r)
      (road-next ?r ?next)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
    )
    :effect (and
      (not (ready-road ?v ?r))
      (ready-road ?v ?next)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-road_0035-road_0136-tw_00270
    :parameters (?v - vehicle ?r - road_type_road_0035 ?next - road_type_road_0136 ?w - window_type_tw_00270)
    :precondition (and
      (ready-road ?v ?r)
      (road-next ?r ?next)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
    )
    :effect (and
      (not (ready-road ?v ?r))
      (ready-road ?v ?next)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-road_0035-road_0136-tw_00300
    :parameters (?v - vehicle ?r - road_type_road_0035 ?next - road_type_road_0136 ?w - window_type_tw_00300)
    :precondition (and
      (ready-road ?v ?r)
      (road-next ?r ?next)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
    )
    :effect (and
      (not (ready-road ?v ?r))
      (ready-road ?v ?next)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-road_0035-road_0136-tw_00330
    :parameters (?v - vehicle ?r - road_type_road_0035 ?next - road_type_road_0136 ?w - window_type_tw_00330)
    :precondition (and
      (ready-road ?v ?r)
      (road-next ?r ?next)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
    )
    :effect (and
      (not (ready-road ?v ?r))
      (ready-road ?v ?next)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-road_0035-road_0136-tw_00360
    :parameters (?v - vehicle ?r - road_type_road_0035 ?next - road_type_road_0136 ?w - window_type_tw_00360)
    :precondition (and
      (ready-road ?v ?r)
      (road-next ?r ?next)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
    )
    :effect (and
      (not (ready-road ?v ?r))
      (ready-road ?v ?next)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-road_0035-road_0136-tw_00390
    :parameters (?v - vehicle ?r - road_type_road_0035 ?next - road_type_road_0136 ?w - window_type_tw_00390)
    :precondition (and
      (ready-road ?v ?r)
      (road-next ?r ?next)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
    )
    :effect (and
      (not (ready-road ?v ?r))
      (ready-road ?v ?next)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-road_0035-road_0136-tw_00420
    :parameters (?v - vehicle ?r - road_type_road_0035 ?next - road_type_road_0136 ?w - window_type_tw_00420)
    :precondition (and
      (ready-road ?v ?r)
      (road-next ?r ?next)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
    )
    :effect (and
      (not (ready-road ?v ?r))
      (ready-road ?v ?next)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-road_0035-road_0136-tw_00450
    :parameters (?v - vehicle ?r - road_type_road_0035 ?next - road_type_road_0136 ?w - window_type_tw_00450)
    :precondition (and
      (ready-road ?v ?r)
      (road-next ?r ?next)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
    )
    :effect (and
      (not (ready-road ?v ?r))
      (ready-road ?v ?next)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-road_0035-road_0136-tw_00480
    :parameters (?v - vehicle ?r - road_type_road_0035 ?next - road_type_road_0136 ?w - window_type_tw_00480)
    :precondition (and
      (ready-road ?v ?r)
      (road-next ?r ?next)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
    )
    :effect (and
      (not (ready-road ?v ?r))
      (ready-road ?v ?next)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-road_0035-road_0136-tw_00510
    :parameters (?v - vehicle ?r - road_type_road_0035 ?next - road_type_road_0136 ?w - window_type_tw_00510)
    :precondition (and
      (ready-road ?v ?r)
      (road-next ?r ?next)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
    )
    :effect (and
      (not (ready-road ?v ?r))
      (ready-road ?v ?next)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-road_0035-road_0136-tw_00540
    :parameters (?v - vehicle ?r - road_type_road_0035 ?next - road_type_road_0136 ?w - window_type_tw_00540)
    :precondition (and
      (ready-road ?v ?r)
      (road-next ?r ?next)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
    )
    :effect (and
      (not (ready-road ?v ?r))
      (ready-road ?v ?next)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-road_0035-road_0136-tw_00570
    :parameters (?v - vehicle ?r - road_type_road_0035 ?next - road_type_road_0136 ?w - window_type_tw_00570)
    :precondition (and
      (ready-road ?v ?r)
      (road-next ?r ?next)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
    )
    :effect (and
      (not (ready-road ?v ?r))
      (ready-road ?v ?next)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-road_0035-road_0136-tw_00600
    :parameters (?v - vehicle ?r - road_type_road_0035 ?next - road_type_road_0136 ?w - window_type_tw_00600)
    :precondition (and
      (ready-road ?v ?r)
      (road-next ?r ?next)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
    )
    :effect (and
      (not (ready-road ?v ?r))
      (ready-road ?v ?next)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-static-road_0036-road_0544
    :parameters (?v - vehicle ?r - road_type_road_0036 ?next - road_type_road_0544)
    :precondition (and
      (ready-road ?v ?r)
      (road-next ?r ?next)
      (road-open ?r)
      (static-road ?r)
      (not (road-blocked ?r))
    )
    :effect (and
      (not (ready-road ?v ?r))
      (ready-road ?v ?next)
      (increase (travel-time ?v) (travel-duration ?r))
      (increase (sim-time) (travel-duration ?r))
    )
  )

  (:action traverse-road-static-road_0036-road_0545
    :parameters (?v - vehicle ?r - road_type_road_0036 ?next - road_type_road_0545)
    :precondition (and
      (ready-road ?v ?r)
      (road-next ?r ?next)
      (road-open ?r)
      (static-road ?r)
      (not (road-blocked ?r))
    )
    :effect (and
      (not (ready-road ?v ?r))
      (ready-road ?v ?next)
      (increase (travel-time ?v) (travel-duration ?r))
      (increase (sim-time) (travel-duration ?r))
    )
  )

  (:action traverse-road-static-road_0037-road_0730
    :parameters (?v - vehicle ?r - road_type_road_0037 ?next - road_type_road_0730)
    :precondition (and
      (ready-road ?v ?r)
      (road-next ?r ?next)
      (road-open ?r)
      (static-road ?r)
      (not (road-blocked ?r))
    )
    :effect (and
      (not (ready-road ?v ?r))
      (ready-road ?v ?next)
      (increase (travel-time ?v) (travel-duration ?r))
      (increase (sim-time) (travel-duration ?r))
    )
  )

  (:action traverse-road-static-road_0038-road_0375
    :parameters (?v - vehicle ?r - road_type_road_0038 ?next - road_type_road_0375)
    :precondition (and
      (ready-road ?v ?r)
      (road-next ?r ?next)
      (road-open ?r)
      (static-road ?r)
      (not (road-blocked ?r))
    )
    :effect (and
      (not (ready-road ?v ?r))
      (ready-road ?v ?next)
      (increase (travel-time ?v) (travel-duration ?r))
      (increase (sim-time) (travel-duration ?r))
    )
  )

  (:action traverse-road-static-road_0038-road_0376
    :parameters (?v - vehicle ?r - road_type_road_0038 ?next - road_type_road_0376)
    :precondition (and
      (ready-road ?v ?r)
      (road-next ?r ?next)
      (road-open ?r)
      (static-road ?r)
      (not (road-blocked ?r))
    )
    :effect (and
      (not (ready-road ?v ?r))
      (ready-road ?v ?next)
      (increase (travel-time ?v) (travel-duration ?r))
      (increase (sim-time) (travel-duration ?r))
    )
  )

  (:action traverse-road-static-road_0038-road_0377
    :parameters (?v - vehicle ?r - road_type_road_0038 ?next - road_type_road_0377)
    :precondition (and
      (ready-road ?v ?r)
      (road-next ?r ?next)
      (road-open ?r)
      (static-road ?r)
      (not (road-blocked ?r))
    )
    :effect (and
      (not (ready-road ?v ?r))
      (ready-road ?v ?next)
      (increase (travel-time ?v) (travel-duration ?r))
      (increase (sim-time) (travel-duration ?r))
    )
  )

  (:action traverse-road-static-road_0039-road_0766
    :parameters (?v - vehicle ?r - road_type_road_0039 ?next - road_type_road_0766)
    :precondition (and
      (ready-road ?v ?r)
      (road-next ?r ?next)
      (road-open ?r)
      (static-road ?r)
      (not (road-blocked ?r))
    )
    :effect (and
      (not (ready-road ?v ?r))
      (ready-road ?v ?next)
      (increase (travel-time ?v) (travel-duration ?r))
      (increase (sim-time) (travel-duration ?r))
    )
  )

  (:action traverse-road-static-road_0039-road_0767
    :parameters (?v - vehicle ?r - road_type_road_0039 ?next - road_type_road_0767)
    :precondition (and
      (ready-road ?v ?r)
      (road-next ?r ?next)
      (road-open ?r)
      (static-road ?r)
      (not (road-blocked ?r))
    )
    :effect (and
      (not (ready-road ?v ?r))
      (ready-road ?v ?next)
      (increase (travel-time ?v) (travel-duration ?r))
      (increase (sim-time) (travel-duration ?r))
    )
  )

  (:action traverse-road-static-road_0042-road_0732
    :parameters (?v - vehicle ?r - road_type_road_0042 ?next - road_type_road_0732)
    :precondition (and
      (ready-road ?v ?r)
      (road-next ?r ?next)
      (road-open ?r)
      (static-road ?r)
      (not (road-blocked ?r))
    )
    :effect (and
      (not (ready-road ?v ?r))
      (ready-road ?v ?next)
      (increase (travel-time ?v) (travel-duration ?r))
      (increase (sim-time) (travel-duration ?r))
    )
  )

  (:action traverse-road-static-road_0042-road_0733
    :parameters (?v - vehicle ?r - road_type_road_0042 ?next - road_type_road_0733)
    :precondition (and
      (ready-road ?v ?r)
      (road-next ?r ?next)
      (road-open ?r)
      (static-road ?r)
      (not (road-blocked ?r))
    )
    :effect (and
      (not (ready-road ?v ?r))
      (ready-road ?v ?next)
      (increase (travel-time ?v) (travel-duration ?r))
      (increase (sim-time) (travel-duration ?r))
    )
  )

  (:action traverse-road-static-road_0043-macro_0048
    :parameters (?v - vehicle ?r - road_type_road_0043 ?next - road_type_macro_0048)
    :precondition (and
      (ready-road ?v ?r)
      (road-next ?r ?next)
      (road-open ?r)
      (static-road ?r)
      (not (road-blocked ?r))
    )
    :effect (and
      (not (ready-road ?v ?r))
      (ready-road ?v ?next)
      (increase (travel-time ?v) (travel-duration ?r))
      (increase (sim-time) (travel-duration ?r))
    )
  )

  (:action traverse-road-static-road_0044-road_0677
    :parameters (?v - vehicle ?r - road_type_road_0044 ?next - road_type_road_0677)
    :precondition (and
      (ready-road ?v ?r)
      (road-next ?r ?next)
      (road-open ?r)
      (static-road ?r)
      (not (road-blocked ?r))
    )
    :effect (and
      (not (ready-road ?v ?r))
      (ready-road ?v ?next)
      (increase (travel-time ?v) (travel-duration ?r))
      (increase (sim-time) (travel-duration ?r))
    )
  )

  (:action traverse-road-static-road_0044-road_0678
    :parameters (?v - vehicle ?r - road_type_road_0044 ?next - road_type_road_0678)
    :precondition (and
      (ready-road ?v ?r)
      (road-next ?r ?next)
      (road-open ?r)
      (static-road ?r)
      (not (road-blocked ?r))
    )
    :effect (and
      (not (ready-road ?v ?r))
      (ready-road ?v ?next)
      (increase (travel-time ?v) (travel-duration ?r))
      (increase (sim-time) (travel-duration ?r))
    )
  )

  (:action traverse-road-static-road_0044-road_0679
    :parameters (?v - vehicle ?r - road_type_road_0044 ?next - road_type_road_0679)
    :precondition (and
      (ready-road ?v ?r)
      (road-next ?r ?next)
      (road-open ?r)
      (static-road ?r)
      (not (road-blocked ?r))
    )
    :effect (and
      (not (ready-road ?v ?r))
      (ready-road ?v ?next)
      (increase (travel-time ?v) (travel-duration ?r))
      (increase (sim-time) (travel-duration ?r))
    )
  )

  (:action traverse-road-static-road_0045-macro_0006
    :parameters (?v - vehicle ?r - road_type_road_0045 ?next - road_type_macro_0006)
    :precondition (and
      (ready-road ?v ?r)
      (road-next ?r ?next)
      (road-open ?r)
      (static-road ?r)
      (not (road-blocked ?r))
    )
    :effect (and
      (not (ready-road ?v ?r))
      (ready-road ?v ?next)
      (increase (travel-time ?v) (travel-duration ?r))
      (increase (sim-time) (travel-duration ?r))
    )
  )

  (:action traverse-road-static-road_0045-road_0066
    :parameters (?v - vehicle ?r - road_type_road_0045 ?next - road_type_road_0066)
    :precondition (and
      (ready-road ?v ?r)
      (road-next ?r ?next)
      (road-open ?r)
      (static-road ?r)
      (not (road-blocked ?r))
    )
    :effect (and
      (not (ready-road ?v ?r))
      (ready-road ?v ?next)
      (increase (travel-time ?v) (travel-duration ?r))
      (increase (sim-time) (travel-duration ?r))
    )
  )

  (:action traverse-road-static-road_0045-road_0067
    :parameters (?v - vehicle ?r - road_type_road_0045 ?next - road_type_road_0067)
    :precondition (and
      (ready-road ?v ?r)
      (road-next ?r ?next)
      (road-open ?r)
      (static-road ?r)
      (not (road-blocked ?r))
    )
    :effect (and
      (not (ready-road ?v ?r))
      (ready-road ?v ?next)
      (increase (travel-time ?v) (travel-duration ?r))
      (increase (sim-time) (travel-duration ?r))
    )
  )

  (:action traverse-road-static-road_0046-road_0286
    :parameters (?v - vehicle ?r - road_type_road_0046 ?next - road_type_road_0286)
    :precondition (and
      (ready-road ?v ?r)
      (road-next ?r ?next)
      (road-open ?r)
      (static-road ?r)
      (not (road-blocked ?r))
    )
    :effect (and
      (not (ready-road ?v ?r))
      (ready-road ?v ?next)
      (increase (travel-time ?v) (travel-duration ?r))
      (increase (sim-time) (travel-duration ?r))
    )
  )

  (:action traverse-road-static-road_0046-road_0287
    :parameters (?v - vehicle ?r - road_type_road_0046 ?next - road_type_road_0287)
    :precondition (and
      (ready-road ?v ?r)
      (road-next ?r ?next)
      (road-open ?r)
      (static-road ?r)
      (not (road-blocked ?r))
    )
    :effect (and
      (not (ready-road ?v ?r))
      (ready-road ?v ?next)
      (increase (travel-time ?v) (travel-duration ?r))
      (increase (sim-time) (travel-duration ?r))
    )
  )

  (:action traverse-road-static-road_0046-road_0288
    :parameters (?v - vehicle ?r - road_type_road_0046 ?next - road_type_road_0288)
    :precondition (and
      (ready-road ?v ?r)
      (road-next ?r ?next)
      (road-open ?r)
      (static-road ?r)
      (not (road-blocked ?r))
    )
    :effect (and
      (not (ready-road ?v ?r))
      (ready-road ?v ?next)
      (increase (travel-time ?v) (travel-duration ?r))
      (increase (sim-time) (travel-duration ?r))
    )
  )

  (:action traverse-road-static-road_0047-road_0106
    :parameters (?v - vehicle ?r - road_type_road_0047 ?next - road_type_road_0106)
    :precondition (and
      (ready-road ?v ?r)
      (road-next ?r ?next)
      (road-open ?r)
      (static-road ?r)
      (not (road-blocked ?r))
    )
    :effect (and
      (not (ready-road ?v ?r))
      (ready-road ?v ?next)
      (increase (travel-time ?v) (travel-duration ?r))
      (increase (sim-time) (travel-duration ?r))
    )
  )

  (:action traverse-road-static-road_0047-road_0107
    :parameters (?v - vehicle ?r - road_type_road_0047 ?next - road_type_road_0107)
    :precondition (and
      (ready-road ?v ?r)
      (road-next ?r ?next)
      (road-open ?r)
      (static-road ?r)
      (not (road-blocked ?r))
    )
    :effect (and
      (not (ready-road ?v ?r))
      (ready-road ?v ?next)
      (increase (travel-time ?v) (travel-duration ?r))
      (increase (sim-time) (travel-duration ?r))
    )
  )

  (:action traverse-road-static-road_0050-macro_0007
    :parameters (?v - vehicle ?r - road_type_road_0050 ?next - road_type_macro_0007)
    :precondition (and
      (ready-road ?v ?r)
      (road-next ?r ?next)
      (road-open ?r)
      (static-road ?r)
      (not (road-blocked ?r))
    )
    :effect (and
      (not (ready-road ?v ?r))
      (ready-road ?v ?next)
      (increase (travel-time ?v) (travel-duration ?r))
      (increase (sim-time) (travel-duration ?r))
    )
  )

  (:action traverse-road-static-road_0050-road_0072
    :parameters (?v - vehicle ?r - road_type_road_0050 ?next - road_type_road_0072)
    :precondition (and
      (ready-road ?v ?r)
      (road-next ?r ?next)
      (road-open ?r)
      (static-road ?r)
      (not (road-blocked ?r))
    )
    :effect (and
      (not (ready-road ?v ?r))
      (ready-road ?v ?next)
      (increase (travel-time ?v) (travel-duration ?r))
      (increase (sim-time) (travel-duration ?r))
    )
  )

  (:action traverse-road-static-road_0050-road_0074
    :parameters (?v - vehicle ?r - road_type_road_0050 ?next - road_type_road_0074)
    :precondition (and
      (ready-road ?v ?r)
      (road-next ?r ?next)
      (road-open ?r)
      (static-road ?r)
      (not (road-blocked ?r))
    )
    :effect (and
      (not (ready-road ?v ?r))
      (ready-road ?v ?next)
      (increase (travel-time ?v) (travel-duration ?r))
      (increase (sim-time) (travel-duration ?r))
    )
  )

  (:action traverse-road-static-road_0051-macro_0006
    :parameters (?v - vehicle ?r - road_type_road_0051 ?next - road_type_macro_0006)
    :precondition (and
      (ready-road ?v ?r)
      (road-next ?r ?next)
      (road-open ?r)
      (static-road ?r)
      (not (road-blocked ?r))
    )
    :effect (and
      (not (ready-road ?v ?r))
      (ready-road ?v ?next)
      (increase (travel-time ?v) (travel-duration ?r))
      (increase (sim-time) (travel-duration ?r))
    )
  )

  (:action traverse-road-static-road_0051-road_0066
    :parameters (?v - vehicle ?r - road_type_road_0051 ?next - road_type_road_0066)
    :precondition (and
      (ready-road ?v ?r)
      (road-next ?r ?next)
      (road-open ?r)
      (static-road ?r)
      (not (road-blocked ?r))
    )
    :effect (and
      (not (ready-road ?v ?r))
      (ready-road ?v ?next)
      (increase (travel-time ?v) (travel-duration ?r))
      (increase (sim-time) (travel-duration ?r))
    )
  )

  (:action traverse-road-static-road_0051-road_0067
    :parameters (?v - vehicle ?r - road_type_road_0051 ?next - road_type_road_0067)
    :precondition (and
      (ready-road ?v ?r)
      (road-next ?r ?next)
      (road-open ?r)
      (static-road ?r)
      (not (road-blocked ?r))
    )
    :effect (and
      (not (ready-road ?v ?r))
      (ready-road ?v ?next)
      (increase (travel-time ?v) (travel-duration ?r))
      (increase (sim-time) (travel-duration ?r))
    )
  )

  (:action traverse-road-static-road_0052-road_0856
    :parameters (?v - vehicle ?r - road_type_road_0052 ?next - road_type_road_0856)
    :precondition (and
      (ready-road ?v ?r)
      (road-next ?r ?next)
      (road-open ?r)
      (static-road ?r)
      (not (road-blocked ?r))
    )
    :effect (and
      (not (ready-road ?v ?r))
      (ready-road ?v ?next)
      (increase (travel-time ?v) (travel-duration ?r))
      (increase (sim-time) (travel-duration ?r))
    )
  )

  (:action traverse-road-static-road_0052-road_0857
    :parameters (?v - vehicle ?r - road_type_road_0052 ?next - road_type_road_0857)
    :precondition (and
      (ready-road ?v ?r)
      (road-next ?r ?next)
      (road-open ?r)
      (static-road ?r)
      (not (road-blocked ?r))
    )
    :effect (and
      (not (ready-road ?v ?r))
      (ready-road ?v ?next)
      (increase (travel-time ?v) (travel-duration ?r))
      (increase (sim-time) (travel-duration ?r))
    )
  )

  (:action traverse-road-static-road_0053-road_0075
    :parameters (?v - vehicle ?r - road_type_road_0053 ?next - road_type_road_0075)
    :precondition (and
      (ready-road ?v ?r)
      (road-next ?r ?next)
      (road-open ?r)
      (static-road ?r)
      (not (road-blocked ?r))
    )
    :effect (and
      (not (ready-road ?v ?r))
      (ready-road ?v ?next)
      (increase (travel-time ?v) (travel-duration ?r))
      (increase (sim-time) (travel-duration ?r))
    )
  )

  (:action traverse-road-static-road_0053-road_0076
    :parameters (?v - vehicle ?r - road_type_road_0053 ?next - road_type_road_0076)
    :precondition (and
      (ready-road ?v ?r)
      (road-next ?r ?next)
      (road-open ?r)
      (static-road ?r)
      (not (road-blocked ?r))
    )
    :effect (and
      (not (ready-road ?v ?r))
      (ready-road ?v ?next)
      (increase (travel-time ?v) (travel-duration ?r))
      (increase (sim-time) (travel-duration ?r))
    )
  )

  (:action traverse-road-static-road_0056-macro_0034
    :parameters (?v - vehicle ?r - road_type_road_0056 ?next - road_type_macro_0034)
    :precondition (and
      (ready-road ?v ?r)
      (road-next ?r ?next)
      (road-open ?r)
      (static-road ?r)
      (not (road-blocked ?r))
    )
    :effect (and
      (not (ready-road ?v ?r))
      (ready-road ?v ?next)
      (increase (travel-time ?v) (travel-duration ?r))
      (increase (sim-time) (travel-duration ?r))
    )
  )

  (:action traverse-road-static-road_0056-road_0434
    :parameters (?v - vehicle ?r - road_type_road_0056 ?next - road_type_road_0434)
    :precondition (and
      (ready-road ?v ?r)
      (road-next ?r ?next)
      (road-open ?r)
      (static-road ?r)
      (not (road-blocked ?r))
    )
    :effect (and
      (not (ready-road ?v ?r))
      (ready-road ?v ?next)
      (increase (travel-time ?v) (travel-duration ?r))
      (increase (sim-time) (travel-duration ?r))
    )
  )

  (:action traverse-road-static-road_0056-road_0436
    :parameters (?v - vehicle ?r - road_type_road_0056 ?next - road_type_road_0436)
    :precondition (and
      (ready-road ?v ?r)
      (road-next ?r ?next)
      (road-open ?r)
      (static-road ?r)
      (not (road-blocked ?r))
    )
    :effect (and
      (not (ready-road ?v ?r))
      (ready-road ?v ?next)
      (increase (travel-time ?v) (travel-duration ?r))
      (increase (sim-time) (travel-duration ?r))
    )
  )

  (:action traverse-road-dynamic-road_0057-road_0529-tw_00000
    :parameters (?v - vehicle ?r - road_type_road_0057 ?next - road_type_road_0529 ?w - window_type_tw_00000)
    :precondition (and
      (ready-road ?v ?r)
      (road-next ?r ?next)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
    )
    :effect (and
      (not (ready-road ?v ?r))
      (ready-road ?v ?next)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-road_0057-road_0529-tw_00030
    :parameters (?v - vehicle ?r - road_type_road_0057 ?next - road_type_road_0529 ?w - window_type_tw_00030)
    :precondition (and
      (ready-road ?v ?r)
      (road-next ?r ?next)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
    )
    :effect (and
      (not (ready-road ?v ?r))
      (ready-road ?v ?next)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-road_0057-road_0529-tw_00060
    :parameters (?v - vehicle ?r - road_type_road_0057 ?next - road_type_road_0529 ?w - window_type_tw_00060)
    :precondition (and
      (ready-road ?v ?r)
      (road-next ?r ?next)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
    )
    :effect (and
      (not (ready-road ?v ?r))
      (ready-road ?v ?next)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-road_0057-road_0529-tw_00090
    :parameters (?v - vehicle ?r - road_type_road_0057 ?next - road_type_road_0529 ?w - window_type_tw_00090)
    :precondition (and
      (ready-road ?v ?r)
      (road-next ?r ?next)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
    )
    :effect (and
      (not (ready-road ?v ?r))
      (ready-road ?v ?next)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-road_0057-road_0529-tw_00120
    :parameters (?v - vehicle ?r - road_type_road_0057 ?next - road_type_road_0529 ?w - window_type_tw_00120)
    :precondition (and
      (ready-road ?v ?r)
      (road-next ?r ?next)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
    )
    :effect (and
      (not (ready-road ?v ?r))
      (ready-road ?v ?next)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-road_0057-road_0529-tw_00150
    :parameters (?v - vehicle ?r - road_type_road_0057 ?next - road_type_road_0529 ?w - window_type_tw_00150)
    :precondition (and
      (ready-road ?v ?r)
      (road-next ?r ?next)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
    )
    :effect (and
      (not (ready-road ?v ?r))
      (ready-road ?v ?next)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-road_0057-road_0529-tw_00180
    :parameters (?v - vehicle ?r - road_type_road_0057 ?next - road_type_road_0529 ?w - window_type_tw_00180)
    :precondition (and
      (ready-road ?v ?r)
      (road-next ?r ?next)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
    )
    :effect (and
      (not (ready-road ?v ?r))
      (ready-road ?v ?next)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-road_0057-road_0529-tw_00210
    :parameters (?v - vehicle ?r - road_type_road_0057 ?next - road_type_road_0529 ?w - window_type_tw_00210)
    :precondition (and
      (ready-road ?v ?r)
      (road-next ?r ?next)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
    )
    :effect (and
      (not (ready-road ?v ?r))
      (ready-road ?v ?next)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-road_0057-road_0529-tw_00240
    :parameters (?v - vehicle ?r - road_type_road_0057 ?next - road_type_road_0529 ?w - window_type_tw_00240)
    :precondition (and
      (ready-road ?v ?r)
      (road-next ?r ?next)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
    )
    :effect (and
      (not (ready-road ?v ?r))
      (ready-road ?v ?next)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-road_0057-road_0529-tw_00270
    :parameters (?v - vehicle ?r - road_type_road_0057 ?next - road_type_road_0529 ?w - window_type_tw_00270)
    :precondition (and
      (ready-road ?v ?r)
      (road-next ?r ?next)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
    )
    :effect (and
      (not (ready-road ?v ?r))
      (ready-road ?v ?next)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-road_0057-road_0529-tw_00300
    :parameters (?v - vehicle ?r - road_type_road_0057 ?next - road_type_road_0529 ?w - window_type_tw_00300)
    :precondition (and
      (ready-road ?v ?r)
      (road-next ?r ?next)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
    )
    :effect (and
      (not (ready-road ?v ?r))
      (ready-road ?v ?next)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-road_0057-road_0529-tw_00330
    :parameters (?v - vehicle ?r - road_type_road_0057 ?next - road_type_road_0529 ?w - window_type_tw_00330)
    :precondition (and
      (ready-road ?v ?r)
      (road-next ?r ?next)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
    )
    :effect (and
      (not (ready-road ?v ?r))
      (ready-road ?v ?next)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-road_0057-road_0529-tw_00360
    :parameters (?v - vehicle ?r - road_type_road_0057 ?next - road_type_road_0529 ?w - window_type_tw_00360)
    :precondition (and
      (ready-road ?v ?r)
      (road-next ?r ?next)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
    )
    :effect (and
      (not (ready-road ?v ?r))
      (ready-road ?v ?next)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-road_0057-road_0529-tw_00390
    :parameters (?v - vehicle ?r - road_type_road_0057 ?next - road_type_road_0529 ?w - window_type_tw_00390)
    :precondition (and
      (ready-road ?v ?r)
      (road-next ?r ?next)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
    )
    :effect (and
      (not (ready-road ?v ?r))
      (ready-road ?v ?next)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-road_0057-road_0529-tw_00420
    :parameters (?v - vehicle ?r - road_type_road_0057 ?next - road_type_road_0529 ?w - window_type_tw_00420)
    :precondition (and
      (ready-road ?v ?r)
      (road-next ?r ?next)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
    )
    :effect (and
      (not (ready-road ?v ?r))
      (ready-road ?v ?next)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-road_0057-road_0529-tw_00450
    :parameters (?v - vehicle ?r - road_type_road_0057 ?next - road_type_road_0529 ?w - window_type_tw_00450)
    :precondition (and
      (ready-road ?v ?r)
      (road-next ?r ?next)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
    )
    :effect (and
      (not (ready-road ?v ?r))
      (ready-road ?v ?next)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-road_0057-road_0529-tw_00480
    :parameters (?v - vehicle ?r - road_type_road_0057 ?next - road_type_road_0529 ?w - window_type_tw_00480)
    :precondition (and
      (ready-road ?v ?r)
      (road-next ?r ?next)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
    )
    :effect (and
      (not (ready-road ?v ?r))
      (ready-road ?v ?next)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-road_0057-road_0529-tw_00510
    :parameters (?v - vehicle ?r - road_type_road_0057 ?next - road_type_road_0529 ?w - window_type_tw_00510)
    :precondition (and
      (ready-road ?v ?r)
      (road-next ?r ?next)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
    )
    :effect (and
      (not (ready-road ?v ?r))
      (ready-road ?v ?next)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-road_0057-road_0529-tw_00540
    :parameters (?v - vehicle ?r - road_type_road_0057 ?next - road_type_road_0529 ?w - window_type_tw_00540)
    :precondition (and
      (ready-road ?v ?r)
      (road-next ?r ?next)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
    )
    :effect (and
      (not (ready-road ?v ?r))
      (ready-road ?v ?next)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-road_0057-road_0529-tw_00570
    :parameters (?v - vehicle ?r - road_type_road_0057 ?next - road_type_road_0529 ?w - window_type_tw_00570)
    :precondition (and
      (ready-road ?v ?r)
      (road-next ?r ?next)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
    )
    :effect (and
      (not (ready-road ?v ?r))
      (ready-road ?v ?next)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-road_0057-road_0529-tw_00600
    :parameters (?v - vehicle ?r - road_type_road_0057 ?next - road_type_road_0529 ?w - window_type_tw_00600)
    :precondition (and
      (ready-road ?v ?r)
      (road-next ?r ?next)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
    )
    :effect (and
      (not (ready-road ?v ?r))
      (ready-road ?v ?next)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-static-road_0058-road_0082
    :parameters (?v - vehicle ?r - road_type_road_0058 ?next - road_type_road_0082)
    :precondition (and
      (ready-road ?v ?r)
      (road-next ?r ?next)
      (road-open ?r)
      (static-road ?r)
      (not (road-blocked ?r))
    )
    :effect (and
      (not (ready-road ?v ?r))
      (ready-road ?v ?next)
      (increase (travel-time ?v) (travel-duration ?r))
      (increase (sim-time) (travel-duration ?r))
    )
  )

  (:action traverse-road-static-road_0061-road_0203
    :parameters (?v - vehicle ?r - road_type_road_0061 ?next - road_type_road_0203)
    :precondition (and
      (ready-road ?v ?r)
      (road-next ?r ?next)
      (road-open ?r)
      (static-road ?r)
      (not (road-blocked ?r))
    )
    :effect (and
      (not (ready-road ?v ?r))
      (ready-road ?v ?next)
      (increase (travel-time ?v) (travel-duration ?r))
      (increase (sim-time) (travel-duration ?r))
    )
  )

  (:action traverse-road-static-road_0062-road_0244
    :parameters (?v - vehicle ?r - road_type_road_0062 ?next - road_type_road_0244)
    :precondition (and
      (ready-road ?v ?r)
      (road-next ?r ?next)
      (road-open ?r)
      (static-road ?r)
      (not (road-blocked ?r))
    )
    :effect (and
      (not (ready-road ?v ?r))
      (ready-road ?v ?next)
      (increase (travel-time ?v) (travel-duration ?r))
      (increase (sim-time) (travel-duration ?r))
    )
  )

  (:action traverse-road-static-road_0062-road_0245
    :parameters (?v - vehicle ?r - road_type_road_0062 ?next - road_type_road_0245)
    :precondition (and
      (ready-road ?v ?r)
      (road-next ?r ?next)
      (road-open ?r)
      (static-road ?r)
      (not (road-blocked ?r))
    )
    :effect (and
      (not (ready-road ?v ?r))
      (ready-road ?v ?next)
      (increase (travel-time ?v) (travel-duration ?r))
      (increase (sim-time) (travel-duration ?r))
    )
  )

  (:action traverse-road-static-road_0063-road_0228
    :parameters (?v - vehicle ?r - road_type_road_0063 ?next - road_type_road_0228)
    :precondition (and
      (ready-road ?v ?r)
      (road-next ?r ?next)
      (road-open ?r)
      (static-road ?r)
      (not (road-blocked ?r))
    )
    :effect (and
      (not (ready-road ?v ?r))
      (ready-road ?v ?next)
      (increase (travel-time ?v) (travel-duration ?r))
      (increase (sim-time) (travel-duration ?r))
    )
  )

  (:action traverse-road-static-road_0064-road_0471
    :parameters (?v - vehicle ?r - road_type_road_0064 ?next - road_type_road_0471)
    :precondition (and
      (ready-road ?v ?r)
      (road-next ?r ?next)
      (road-open ?r)
      (static-road ?r)
      (not (road-blocked ?r))
    )
    :effect (and
      (not (ready-road ?v ?r))
      (ready-road ?v ?next)
      (increase (travel-time ?v) (travel-duration ?r))
      (increase (sim-time) (travel-duration ?r))
    )
  )

  (:action traverse-road-static-road_0064-road_0472
    :parameters (?v - vehicle ?r - road_type_road_0064 ?next - road_type_road_0472)
    :precondition (and
      (ready-road ?v ?r)
      (road-next ?r ?next)
      (road-open ?r)
      (static-road ?r)
      (not (road-blocked ?r))
    )
    :effect (and
      (not (ready-road ?v ?r))
      (ready-road ?v ?next)
      (increase (travel-time ?v) (travel-duration ?r))
      (increase (sim-time) (travel-duration ?r))
    )
  )

  (:action traverse-road-static-road_0064-road_0473
    :parameters (?v - vehicle ?r - road_type_road_0064 ?next - road_type_road_0473)
    :precondition (and
      (ready-road ?v ?r)
      (road-next ?r ?next)
      (road-open ?r)
      (static-road ?r)
      (not (road-blocked ?r))
    )
    :effect (and
      (not (ready-road ?v ?r))
      (ready-road ?v ?next)
      (increase (travel-time ?v) (travel-duration ?r))
      (increase (sim-time) (travel-duration ?r))
    )
  )

  (:action traverse-road-static-road_0065-macro_0024
    :parameters (?v - vehicle ?r - road_type_road_0065 ?next - road_type_macro_0024)
    :precondition (and
      (ready-road ?v ?r)
      (road-next ?r ?next)
      (road-open ?r)
      (static-road ?r)
      (not (road-blocked ?r))
    )
    :effect (and
      (not (ready-road ?v ?r))
      (ready-road ?v ?next)
      (increase (travel-time ?v) (travel-duration ?r))
      (increase (sim-time) (travel-duration ?r))
    )
  )

  (:action traverse-road-static-road_0065-road_0294
    :parameters (?v - vehicle ?r - road_type_road_0065 ?next - road_type_road_0294)
    :precondition (and
      (ready-road ?v ?r)
      (road-next ?r ?next)
      (road-open ?r)
      (static-road ?r)
      (not (road-blocked ?r))
    )
    :effect (and
      (not (ready-road ?v ?r))
      (ready-road ?v ?next)
      (increase (travel-time ?v) (travel-duration ?r))
      (increase (sim-time) (travel-duration ?r))
    )
  )

  (:action traverse-road-static-road_0066-road_0774
    :parameters (?v - vehicle ?r - road_type_road_0066 ?next - road_type_road_0774)
    :precondition (and
      (ready-road ?v ?r)
      (road-next ?r ?next)
      (road-open ?r)
      (static-road ?r)
      (not (road-blocked ?r))
    )
    :effect (and
      (not (ready-road ?v ?r))
      (ready-road ?v ?next)
      (increase (travel-time ?v) (travel-duration ?r))
      (increase (sim-time) (travel-duration ?r))
    )
  )

  (:action traverse-road-static-road_0066-road_0775
    :parameters (?v - vehicle ?r - road_type_road_0066 ?next - road_type_road_0775)
    :precondition (and
      (ready-road ?v ?r)
      (road-next ?r ?next)
      (road-open ?r)
      (static-road ?r)
      (not (road-blocked ?r))
    )
    :effect (and
      (not (ready-road ?v ?r))
      (ready-road ?v ?next)
      (increase (travel-time ?v) (travel-duration ?r))
      (increase (sim-time) (travel-duration ?r))
    )
  )

  (:action traverse-road-static-road_0067-road_0045
    :parameters (?v - vehicle ?r - road_type_road_0067 ?next - road_type_road_0045)
    :precondition (and
      (ready-road ?v ?r)
      (road-next ?r ?next)
      (road-open ?r)
      (static-road ?r)
      (not (road-blocked ?r))
    )
    :effect (and
      (not (ready-road ?v ?r))
      (ready-road ?v ?next)
      (increase (travel-time ?v) (travel-duration ?r))
      (increase (sim-time) (travel-duration ?r))
    )
  )

  (:action traverse-road-static-road_0067-road_0046
    :parameters (?v - vehicle ?r - road_type_road_0067 ?next - road_type_road_0046)
    :precondition (and
      (ready-road ?v ?r)
      (road-next ?r ?next)
      (road-open ?r)
      (static-road ?r)
      (not (road-blocked ?r))
    )
    :effect (and
      (not (ready-road ?v ?r))
      (ready-road ?v ?next)
      (increase (travel-time ?v) (travel-duration ?r))
      (increase (sim-time) (travel-duration ?r))
    )
  )

  (:action traverse-road-dynamic-road_0069-macro_0013-tw_00000
    :parameters (?v - vehicle ?r - road_type_road_0069 ?next - road_type_macro_0013 ?w - window_type_tw_00000)
    :precondition (and
      (ready-road ?v ?r)
      (road-next ?r ?next)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
    )
    :effect (and
      (not (ready-road ?v ?r))
      (ready-road ?v ?next)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-road_0069-macro_0013-tw_00030
    :parameters (?v - vehicle ?r - road_type_road_0069 ?next - road_type_macro_0013 ?w - window_type_tw_00030)
    :precondition (and
      (ready-road ?v ?r)
      (road-next ?r ?next)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
    )
    :effect (and
      (not (ready-road ?v ?r))
      (ready-road ?v ?next)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-road_0069-macro_0013-tw_00060
    :parameters (?v - vehicle ?r - road_type_road_0069 ?next - road_type_macro_0013 ?w - window_type_tw_00060)
    :precondition (and
      (ready-road ?v ?r)
      (road-next ?r ?next)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
    )
    :effect (and
      (not (ready-road ?v ?r))
      (ready-road ?v ?next)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-road_0069-macro_0013-tw_00090
    :parameters (?v - vehicle ?r - road_type_road_0069 ?next - road_type_macro_0013 ?w - window_type_tw_00090)
    :precondition (and
      (ready-road ?v ?r)
      (road-next ?r ?next)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
    )
    :effect (and
      (not (ready-road ?v ?r))
      (ready-road ?v ?next)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-road_0069-macro_0013-tw_00120
    :parameters (?v - vehicle ?r - road_type_road_0069 ?next - road_type_macro_0013 ?w - window_type_tw_00120)
    :precondition (and
      (ready-road ?v ?r)
      (road-next ?r ?next)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
    )
    :effect (and
      (not (ready-road ?v ?r))
      (ready-road ?v ?next)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-road_0069-macro_0013-tw_00150
    :parameters (?v - vehicle ?r - road_type_road_0069 ?next - road_type_macro_0013 ?w - window_type_tw_00150)
    :precondition (and
      (ready-road ?v ?r)
      (road-next ?r ?next)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
    )
    :effect (and
      (not (ready-road ?v ?r))
      (ready-road ?v ?next)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-road_0069-macro_0013-tw_00180
    :parameters (?v - vehicle ?r - road_type_road_0069 ?next - road_type_macro_0013 ?w - window_type_tw_00180)
    :precondition (and
      (ready-road ?v ?r)
      (road-next ?r ?next)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
    )
    :effect (and
      (not (ready-road ?v ?r))
      (ready-road ?v ?next)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-road_0069-macro_0013-tw_00210
    :parameters (?v - vehicle ?r - road_type_road_0069 ?next - road_type_macro_0013 ?w - window_type_tw_00210)
    :precondition (and
      (ready-road ?v ?r)
      (road-next ?r ?next)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
    )
    :effect (and
      (not (ready-road ?v ?r))
      (ready-road ?v ?next)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-road_0069-macro_0013-tw_00240
    :parameters (?v - vehicle ?r - road_type_road_0069 ?next - road_type_macro_0013 ?w - window_type_tw_00240)
    :precondition (and
      (ready-road ?v ?r)
      (road-next ?r ?next)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
    )
    :effect (and
      (not (ready-road ?v ?r))
      (ready-road ?v ?next)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-road_0069-macro_0013-tw_00270
    :parameters (?v - vehicle ?r - road_type_road_0069 ?next - road_type_macro_0013 ?w - window_type_tw_00270)
    :precondition (and
      (ready-road ?v ?r)
      (road-next ?r ?next)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
    )
    :effect (and
      (not (ready-road ?v ?r))
      (ready-road ?v ?next)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-road_0069-macro_0013-tw_00300
    :parameters (?v - vehicle ?r - road_type_road_0069 ?next - road_type_macro_0013 ?w - window_type_tw_00300)
    :precondition (and
      (ready-road ?v ?r)
      (road-next ?r ?next)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
    )
    :effect (and
      (not (ready-road ?v ?r))
      (ready-road ?v ?next)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-road_0069-macro_0013-tw_00330
    :parameters (?v - vehicle ?r - road_type_road_0069 ?next - road_type_macro_0013 ?w - window_type_tw_00330)
    :precondition (and
      (ready-road ?v ?r)
      (road-next ?r ?next)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
    )
    :effect (and
      (not (ready-road ?v ?r))
      (ready-road ?v ?next)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-road_0069-macro_0013-tw_00360
    :parameters (?v - vehicle ?r - road_type_road_0069 ?next - road_type_macro_0013 ?w - window_type_tw_00360)
    :precondition (and
      (ready-road ?v ?r)
      (road-next ?r ?next)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
    )
    :effect (and
      (not (ready-road ?v ?r))
      (ready-road ?v ?next)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-road_0069-macro_0013-tw_00390
    :parameters (?v - vehicle ?r - road_type_road_0069 ?next - road_type_macro_0013 ?w - window_type_tw_00390)
    :precondition (and
      (ready-road ?v ?r)
      (road-next ?r ?next)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
    )
    :effect (and
      (not (ready-road ?v ?r))
      (ready-road ?v ?next)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-road_0069-macro_0013-tw_00420
    :parameters (?v - vehicle ?r - road_type_road_0069 ?next - road_type_macro_0013 ?w - window_type_tw_00420)
    :precondition (and
      (ready-road ?v ?r)
      (road-next ?r ?next)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
    )
    :effect (and
      (not (ready-road ?v ?r))
      (ready-road ?v ?next)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-road_0069-macro_0013-tw_00450
    :parameters (?v - vehicle ?r - road_type_road_0069 ?next - road_type_macro_0013 ?w - window_type_tw_00450)
    :precondition (and
      (ready-road ?v ?r)
      (road-next ?r ?next)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
    )
    :effect (and
      (not (ready-road ?v ?r))
      (ready-road ?v ?next)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-road_0069-macro_0013-tw_00480
    :parameters (?v - vehicle ?r - road_type_road_0069 ?next - road_type_macro_0013 ?w - window_type_tw_00480)
    :precondition (and
      (ready-road ?v ?r)
      (road-next ?r ?next)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
    )
    :effect (and
      (not (ready-road ?v ?r))
      (ready-road ?v ?next)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-road_0069-macro_0013-tw_00510
    :parameters (?v - vehicle ?r - road_type_road_0069 ?next - road_type_macro_0013 ?w - window_type_tw_00510)
    :precondition (and
      (ready-road ?v ?r)
      (road-next ?r ?next)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
    )
    :effect (and
      (not (ready-road ?v ?r))
      (ready-road ?v ?next)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-road_0069-macro_0013-tw_00540
    :parameters (?v - vehicle ?r - road_type_road_0069 ?next - road_type_macro_0013 ?w - window_type_tw_00540)
    :precondition (and
      (ready-road ?v ?r)
      (road-next ?r ?next)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
    )
    :effect (and
      (not (ready-road ?v ?r))
      (ready-road ?v ?next)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-road_0069-macro_0013-tw_00570
    :parameters (?v - vehicle ?r - road_type_road_0069 ?next - road_type_macro_0013 ?w - window_type_tw_00570)
    :precondition (and
      (ready-road ?v ?r)
      (road-next ?r ?next)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
    )
    :effect (and
      (not (ready-road ?v ?r))
      (ready-road ?v ?next)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-road_0069-macro_0013-tw_00600
    :parameters (?v - vehicle ?r - road_type_road_0069 ?next - road_type_macro_0013 ?w - window_type_tw_00600)
    :precondition (and
      (ready-road ?v ?r)
      (road-next ?r ?next)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
    )
    :effect (and
      (not (ready-road ?v ?r))
      (ready-road ?v ?next)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-road_0069-road_0169-tw_00000
    :parameters (?v - vehicle ?r - road_type_road_0069 ?next - road_type_road_0169 ?w - window_type_tw_00000)
    :precondition (and
      (ready-road ?v ?r)
      (road-next ?r ?next)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
    )
    :effect (and
      (not (ready-road ?v ?r))
      (ready-road ?v ?next)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-road_0069-road_0169-tw_00030
    :parameters (?v - vehicle ?r - road_type_road_0069 ?next - road_type_road_0169 ?w - window_type_tw_00030)
    :precondition (and
      (ready-road ?v ?r)
      (road-next ?r ?next)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
    )
    :effect (and
      (not (ready-road ?v ?r))
      (ready-road ?v ?next)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-road_0069-road_0169-tw_00060
    :parameters (?v - vehicle ?r - road_type_road_0069 ?next - road_type_road_0169 ?w - window_type_tw_00060)
    :precondition (and
      (ready-road ?v ?r)
      (road-next ?r ?next)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
    )
    :effect (and
      (not (ready-road ?v ?r))
      (ready-road ?v ?next)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-road_0069-road_0169-tw_00090
    :parameters (?v - vehicle ?r - road_type_road_0069 ?next - road_type_road_0169 ?w - window_type_tw_00090)
    :precondition (and
      (ready-road ?v ?r)
      (road-next ?r ?next)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
    )
    :effect (and
      (not (ready-road ?v ?r))
      (ready-road ?v ?next)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-road_0069-road_0169-tw_00120
    :parameters (?v - vehicle ?r - road_type_road_0069 ?next - road_type_road_0169 ?w - window_type_tw_00120)
    :precondition (and
      (ready-road ?v ?r)
      (road-next ?r ?next)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
    )
    :effect (and
      (not (ready-road ?v ?r))
      (ready-road ?v ?next)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-road_0069-road_0169-tw_00150
    :parameters (?v - vehicle ?r - road_type_road_0069 ?next - road_type_road_0169 ?w - window_type_tw_00150)
    :precondition (and
      (ready-road ?v ?r)
      (road-next ?r ?next)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
    )
    :effect (and
      (not (ready-road ?v ?r))
      (ready-road ?v ?next)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-road_0069-road_0169-tw_00180
    :parameters (?v - vehicle ?r - road_type_road_0069 ?next - road_type_road_0169 ?w - window_type_tw_00180)
    :precondition (and
      (ready-road ?v ?r)
      (road-next ?r ?next)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
    )
    :effect (and
      (not (ready-road ?v ?r))
      (ready-road ?v ?next)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-road_0069-road_0169-tw_00210
    :parameters (?v - vehicle ?r - road_type_road_0069 ?next - road_type_road_0169 ?w - window_type_tw_00210)
    :precondition (and
      (ready-road ?v ?r)
      (road-next ?r ?next)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
    )
    :effect (and
      (not (ready-road ?v ?r))
      (ready-road ?v ?next)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-road_0069-road_0169-tw_00240
    :parameters (?v - vehicle ?r - road_type_road_0069 ?next - road_type_road_0169 ?w - window_type_tw_00240)
    :precondition (and
      (ready-road ?v ?r)
      (road-next ?r ?next)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
    )
    :effect (and
      (not (ready-road ?v ?r))
      (ready-road ?v ?next)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-road_0069-road_0169-tw_00270
    :parameters (?v - vehicle ?r - road_type_road_0069 ?next - road_type_road_0169 ?w - window_type_tw_00270)
    :precondition (and
      (ready-road ?v ?r)
      (road-next ?r ?next)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
    )
    :effect (and
      (not (ready-road ?v ?r))
      (ready-road ?v ?next)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-road_0069-road_0169-tw_00300
    :parameters (?v - vehicle ?r - road_type_road_0069 ?next - road_type_road_0169 ?w - window_type_tw_00300)
    :precondition (and
      (ready-road ?v ?r)
      (road-next ?r ?next)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
    )
    :effect (and
      (not (ready-road ?v ?r))
      (ready-road ?v ?next)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-road_0069-road_0169-tw_00330
    :parameters (?v - vehicle ?r - road_type_road_0069 ?next - road_type_road_0169 ?w - window_type_tw_00330)
    :precondition (and
      (ready-road ?v ?r)
      (road-next ?r ?next)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
    )
    :effect (and
      (not (ready-road ?v ?r))
      (ready-road ?v ?next)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-road_0069-road_0169-tw_00360
    :parameters (?v - vehicle ?r - road_type_road_0069 ?next - road_type_road_0169 ?w - window_type_tw_00360)
    :precondition (and
      (ready-road ?v ?r)
      (road-next ?r ?next)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
    )
    :effect (and
      (not (ready-road ?v ?r))
      (ready-road ?v ?next)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-road_0069-road_0169-tw_00390
    :parameters (?v - vehicle ?r - road_type_road_0069 ?next - road_type_road_0169 ?w - window_type_tw_00390)
    :precondition (and
      (ready-road ?v ?r)
      (road-next ?r ?next)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
    )
    :effect (and
      (not (ready-road ?v ?r))
      (ready-road ?v ?next)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-road_0069-road_0169-tw_00420
    :parameters (?v - vehicle ?r - road_type_road_0069 ?next - road_type_road_0169 ?w - window_type_tw_00420)
    :precondition (and
      (ready-road ?v ?r)
      (road-next ?r ?next)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
    )
    :effect (and
      (not (ready-road ?v ?r))
      (ready-road ?v ?next)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-road_0069-road_0169-tw_00450
    :parameters (?v - vehicle ?r - road_type_road_0069 ?next - road_type_road_0169 ?w - window_type_tw_00450)
    :precondition (and
      (ready-road ?v ?r)
      (road-next ?r ?next)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
    )
    :effect (and
      (not (ready-road ?v ?r))
      (ready-road ?v ?next)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-road_0069-road_0169-tw_00480
    :parameters (?v - vehicle ?r - road_type_road_0069 ?next - road_type_road_0169 ?w - window_type_tw_00480)
    :precondition (and
      (ready-road ?v ?r)
      (road-next ?r ?next)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
    )
    :effect (and
      (not (ready-road ?v ?r))
      (ready-road ?v ?next)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-road_0069-road_0169-tw_00510
    :parameters (?v - vehicle ?r - road_type_road_0069 ?next - road_type_road_0169 ?w - window_type_tw_00510)
    :precondition (and
      (ready-road ?v ?r)
      (road-next ?r ?next)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
    )
    :effect (and
      (not (ready-road ?v ?r))
      (ready-road ?v ?next)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-road_0069-road_0169-tw_00540
    :parameters (?v - vehicle ?r - road_type_road_0069 ?next - road_type_road_0169 ?w - window_type_tw_00540)
    :precondition (and
      (ready-road ?v ?r)
      (road-next ?r ?next)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
    )
    :effect (and
      (not (ready-road ?v ?r))
      (ready-road ?v ?next)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-road_0069-road_0169-tw_00570
    :parameters (?v - vehicle ?r - road_type_road_0069 ?next - road_type_road_0169 ?w - window_type_tw_00570)
    :precondition (and
      (ready-road ?v ?r)
      (road-next ?r ?next)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
    )
    :effect (and
      (not (ready-road ?v ?r))
      (ready-road ?v ?next)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-road_0069-road_0169-tw_00600
    :parameters (?v - vehicle ?r - road_type_road_0069 ?next - road_type_road_0169 ?w - window_type_tw_00600)
    :precondition (and
      (ready-road ?v ?r)
      (road-next ?r ?next)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
    )
    :effect (and
      (not (ready-road ?v ?r))
      (ready-road ?v ?next)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-static-road_0070-macro_0007
    :parameters (?v - vehicle ?r - road_type_road_0070 ?next - road_type_macro_0007)
    :precondition (and
      (ready-road ?v ?r)
      (road-next ?r ?next)
      (road-open ?r)
      (static-road ?r)
      (not (road-blocked ?r))
    )
    :effect (and
      (not (ready-road ?v ?r))
      (ready-road ?v ?next)
      (increase (travel-time ?v) (travel-duration ?r))
      (increase (sim-time) (travel-duration ?r))
    )
  )

  (:action traverse-road-static-road_0070-road_0072
    :parameters (?v - vehicle ?r - road_type_road_0070 ?next - road_type_road_0072)
    :precondition (and
      (ready-road ?v ?r)
      (road-next ?r ?next)
      (road-open ?r)
      (static-road ?r)
      (not (road-blocked ?r))
    )
    :effect (and
      (not (ready-road ?v ?r))
      (ready-road ?v ?next)
      (increase (travel-time ?v) (travel-duration ?r))
      (increase (sim-time) (travel-duration ?r))
    )
  )

  (:action traverse-road-static-road_0070-road_0074
    :parameters (?v - vehicle ?r - road_type_road_0070 ?next - road_type_road_0074)
    :precondition (and
      (ready-road ?v ?r)
      (road-next ?r ?next)
      (road-open ?r)
      (static-road ?r)
      (not (road-blocked ?r))
    )
    :effect (and
      (not (ready-road ?v ?r))
      (ready-road ?v ?next)
      (increase (travel-time ?v) (travel-duration ?r))
      (increase (sim-time) (travel-duration ?r))
    )
  )

  (:action traverse-road-static-road_0071-road_0286
    :parameters (?v - vehicle ?r - road_type_road_0071 ?next - road_type_road_0286)
    :precondition (and
      (ready-road ?v ?r)
      (road-next ?r ?next)
      (road-open ?r)
      (static-road ?r)
      (not (road-blocked ?r))
    )
    :effect (and
      (not (ready-road ?v ?r))
      (ready-road ?v ?next)
      (increase (travel-time ?v) (travel-duration ?r))
      (increase (sim-time) (travel-duration ?r))
    )
  )

  (:action traverse-road-static-road_0071-road_0287
    :parameters (?v - vehicle ?r - road_type_road_0071 ?next - road_type_road_0287)
    :precondition (and
      (ready-road ?v ?r)
      (road-next ?r ?next)
      (road-open ?r)
      (static-road ?r)
      (not (road-blocked ?r))
    )
    :effect (and
      (not (ready-road ?v ?r))
      (ready-road ?v ?next)
      (increase (travel-time ?v) (travel-duration ?r))
      (increase (sim-time) (travel-duration ?r))
    )
  )

  (:action traverse-road-static-road_0071-road_0288
    :parameters (?v - vehicle ?r - road_type_road_0071 ?next - road_type_road_0288)
    :precondition (and
      (ready-road ?v ?r)
      (road-next ?r ?next)
      (road-open ?r)
      (static-road ?r)
      (not (road-blocked ?r))
    )
    :effect (and
      (not (ready-road ?v ?r))
      (ready-road ?v ?next)
      (increase (travel-time ?v) (travel-duration ?r))
      (increase (sim-time) (travel-duration ?r))
    )
  )

  (:action traverse-road-static-road_0074-road_0069
    :parameters (?v - vehicle ?r - road_type_road_0074 ?next - road_type_road_0069)
    :precondition (and
      (ready-road ?v ?r)
      (road-next ?r ?next)
      (road-open ?r)
      (static-road ?r)
      (not (road-blocked ?r))
    )
    :effect (and
      (not (ready-road ?v ?r))
      (ready-road ?v ?next)
      (increase (travel-time ?v) (travel-duration ?r))
      (increase (sim-time) (travel-duration ?r))
    )
  )

  (:action traverse-road-static-road_0074-road_0070
    :parameters (?v - vehicle ?r - road_type_road_0074 ?next - road_type_road_0070)
    :precondition (and
      (ready-road ?v ?r)
      (road-next ?r ?next)
      (road-open ?r)
      (static-road ?r)
      (not (road-blocked ?r))
    )
    :effect (and
      (not (ready-road ?v ?r))
      (ready-road ?v ?next)
      (increase (travel-time ?v) (travel-duration ?r))
      (increase (sim-time) (travel-duration ?r))
    )
  )

  (:action traverse-road-static-road_0074-road_0071
    :parameters (?v - vehicle ?r - road_type_road_0074 ?next - road_type_road_0071)
    :precondition (and
      (ready-road ?v ?r)
      (road-next ?r ?next)
      (road-open ?r)
      (static-road ?r)
      (not (road-blocked ?r))
    )
    :effect (and
      (not (ready-road ?v ?r))
      (ready-road ?v ?next)
      (increase (travel-time ?v) (travel-duration ?r))
      (increase (sim-time) (travel-duration ?r))
    )
  )

  (:action traverse-road-static-road_0075-road_0042
    :parameters (?v - vehicle ?r - road_type_road_0075 ?next - road_type_road_0042)
    :precondition (and
      (ready-road ?v ?r)
      (road-next ?r ?next)
      (road-open ?r)
      (static-road ?r)
      (not (road-blocked ?r))
    )
    :effect (and
      (not (ready-road ?v ?r))
      (ready-road ?v ?next)
      (increase (travel-time ?v) (travel-duration ?r))
      (increase (sim-time) (travel-duration ?r))
    )
  )

  (:action traverse-road-static-road_0076-road_0053
    :parameters (?v - vehicle ?r - road_type_road_0076 ?next - road_type_road_0053)
    :precondition (and
      (ready-road ?v ?r)
      (road-next ?r ?next)
      (road-open ?r)
      (static-road ?r)
      (not (road-blocked ?r))
    )
    :effect (and
      (not (ready-road ?v ?r))
      (ready-road ?v ?next)
      (increase (travel-time ?v) (travel-duration ?r))
      (increase (sim-time) (travel-duration ?r))
    )
  )

  (:action finish-road-static-road_0076
    :parameters (?v - vehicle ?r - road_type_road_0076)
    :precondition (and
      (ready-road ?v ?r)
      (goal-road ?r)
      (road-open ?r)
      (static-road ?r)
      (not (road-blocked ?r))
    )
    :effect (and
      (not (ready-road ?v ?r))
      (reached-goal ?v)
      (increase (travel-time ?v) (travel-duration ?r))
      (increase (sim-time) (travel-duration ?r))
    )
  )

  (:action traverse-road-dynamic-road_0077-road_0281-tw_00000
    :parameters (?v - vehicle ?r - road_type_road_0077 ?next - road_type_road_0281 ?w - window_type_tw_00000)
    :precondition (and
      (ready-road ?v ?r)
      (road-next ?r ?next)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
    )
    :effect (and
      (not (ready-road ?v ?r))
      (ready-road ?v ?next)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-road_0077-road_0281-tw_00030
    :parameters (?v - vehicle ?r - road_type_road_0077 ?next - road_type_road_0281 ?w - window_type_tw_00030)
    :precondition (and
      (ready-road ?v ?r)
      (road-next ?r ?next)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
    )
    :effect (and
      (not (ready-road ?v ?r))
      (ready-road ?v ?next)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-road_0077-road_0281-tw_00060
    :parameters (?v - vehicle ?r - road_type_road_0077 ?next - road_type_road_0281 ?w - window_type_tw_00060)
    :precondition (and
      (ready-road ?v ?r)
      (road-next ?r ?next)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
    )
    :effect (and
      (not (ready-road ?v ?r))
      (ready-road ?v ?next)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-road_0077-road_0281-tw_00090
    :parameters (?v - vehicle ?r - road_type_road_0077 ?next - road_type_road_0281 ?w - window_type_tw_00090)
    :precondition (and
      (ready-road ?v ?r)
      (road-next ?r ?next)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
    )
    :effect (and
      (not (ready-road ?v ?r))
      (ready-road ?v ?next)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-road_0077-road_0281-tw_00120
    :parameters (?v - vehicle ?r - road_type_road_0077 ?next - road_type_road_0281 ?w - window_type_tw_00120)
    :precondition (and
      (ready-road ?v ?r)
      (road-next ?r ?next)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
    )
    :effect (and
      (not (ready-road ?v ?r))
      (ready-road ?v ?next)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-road_0077-road_0281-tw_00150
    :parameters (?v - vehicle ?r - road_type_road_0077 ?next - road_type_road_0281 ?w - window_type_tw_00150)
    :precondition (and
      (ready-road ?v ?r)
      (road-next ?r ?next)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
    )
    :effect (and
      (not (ready-road ?v ?r))
      (ready-road ?v ?next)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-road_0077-road_0281-tw_00180
    :parameters (?v - vehicle ?r - road_type_road_0077 ?next - road_type_road_0281 ?w - window_type_tw_00180)
    :precondition (and
      (ready-road ?v ?r)
      (road-next ?r ?next)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
    )
    :effect (and
      (not (ready-road ?v ?r))
      (ready-road ?v ?next)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-road_0077-road_0281-tw_00210
    :parameters (?v - vehicle ?r - road_type_road_0077 ?next - road_type_road_0281 ?w - window_type_tw_00210)
    :precondition (and
      (ready-road ?v ?r)
      (road-next ?r ?next)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
    )
    :effect (and
      (not (ready-road ?v ?r))
      (ready-road ?v ?next)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-road_0077-road_0281-tw_00240
    :parameters (?v - vehicle ?r - road_type_road_0077 ?next - road_type_road_0281 ?w - window_type_tw_00240)
    :precondition (and
      (ready-road ?v ?r)
      (road-next ?r ?next)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
    )
    :effect (and
      (not (ready-road ?v ?r))
      (ready-road ?v ?next)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-road_0077-road_0281-tw_00270
    :parameters (?v - vehicle ?r - road_type_road_0077 ?next - road_type_road_0281 ?w - window_type_tw_00270)
    :precondition (and
      (ready-road ?v ?r)
      (road-next ?r ?next)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
    )
    :effect (and
      (not (ready-road ?v ?r))
      (ready-road ?v ?next)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-road_0077-road_0281-tw_00300
    :parameters (?v - vehicle ?r - road_type_road_0077 ?next - road_type_road_0281 ?w - window_type_tw_00300)
    :precondition (and
      (ready-road ?v ?r)
      (road-next ?r ?next)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
    )
    :effect (and
      (not (ready-road ?v ?r))
      (ready-road ?v ?next)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-road_0077-road_0281-tw_00330
    :parameters (?v - vehicle ?r - road_type_road_0077 ?next - road_type_road_0281 ?w - window_type_tw_00330)
    :precondition (and
      (ready-road ?v ?r)
      (road-next ?r ?next)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
    )
    :effect (and
      (not (ready-road ?v ?r))
      (ready-road ?v ?next)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-road_0077-road_0281-tw_00360
    :parameters (?v - vehicle ?r - road_type_road_0077 ?next - road_type_road_0281 ?w - window_type_tw_00360)
    :precondition (and
      (ready-road ?v ?r)
      (road-next ?r ?next)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
    )
    :effect (and
      (not (ready-road ?v ?r))
      (ready-road ?v ?next)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-road_0077-road_0281-tw_00390
    :parameters (?v - vehicle ?r - road_type_road_0077 ?next - road_type_road_0281 ?w - window_type_tw_00390)
    :precondition (and
      (ready-road ?v ?r)
      (road-next ?r ?next)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
    )
    :effect (and
      (not (ready-road ?v ?r))
      (ready-road ?v ?next)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-road_0077-road_0281-tw_00420
    :parameters (?v - vehicle ?r - road_type_road_0077 ?next - road_type_road_0281 ?w - window_type_tw_00420)
    :precondition (and
      (ready-road ?v ?r)
      (road-next ?r ?next)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
    )
    :effect (and
      (not (ready-road ?v ?r))
      (ready-road ?v ?next)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-road_0077-road_0281-tw_00450
    :parameters (?v - vehicle ?r - road_type_road_0077 ?next - road_type_road_0281 ?w - window_type_tw_00450)
    :precondition (and
      (ready-road ?v ?r)
      (road-next ?r ?next)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
    )
    :effect (and
      (not (ready-road ?v ?r))
      (ready-road ?v ?next)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-road_0077-road_0281-tw_00480
    :parameters (?v - vehicle ?r - road_type_road_0077 ?next - road_type_road_0281 ?w - window_type_tw_00480)
    :precondition (and
      (ready-road ?v ?r)
      (road-next ?r ?next)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
    )
    :effect (and
      (not (ready-road ?v ?r))
      (ready-road ?v ?next)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-road_0077-road_0281-tw_00510
    :parameters (?v - vehicle ?r - road_type_road_0077 ?next - road_type_road_0281 ?w - window_type_tw_00510)
    :precondition (and
      (ready-road ?v ?r)
      (road-next ?r ?next)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
    )
    :effect (and
      (not (ready-road ?v ?r))
      (ready-road ?v ?next)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-road_0077-road_0281-tw_00540
    :parameters (?v - vehicle ?r - road_type_road_0077 ?next - road_type_road_0281 ?w - window_type_tw_00540)
    :precondition (and
      (ready-road ?v ?r)
      (road-next ?r ?next)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
    )
    :effect (and
      (not (ready-road ?v ?r))
      (ready-road ?v ?next)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-road_0077-road_0281-tw_00570
    :parameters (?v - vehicle ?r - road_type_road_0077 ?next - road_type_road_0281 ?w - window_type_tw_00570)
    :precondition (and
      (ready-road ?v ?r)
      (road-next ?r ?next)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
    )
    :effect (and
      (not (ready-road ?v ?r))
      (ready-road ?v ?next)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-road_0077-road_0281-tw_00600
    :parameters (?v - vehicle ?r - road_type_road_0077 ?next - road_type_road_0281 ?w - window_type_tw_00600)
    :precondition (and
      (ready-road ?v ?r)
      (road-next ?r ?next)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
    )
    :effect (and
      (not (ready-road ?v ?r))
      (ready-road ?v ?next)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-road_0077-road_0282-tw_00000
    :parameters (?v - vehicle ?r - road_type_road_0077 ?next - road_type_road_0282 ?w - window_type_tw_00000)
    :precondition (and
      (ready-road ?v ?r)
      (road-next ?r ?next)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
    )
    :effect (and
      (not (ready-road ?v ?r))
      (ready-road ?v ?next)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-road_0077-road_0282-tw_00030
    :parameters (?v - vehicle ?r - road_type_road_0077 ?next - road_type_road_0282 ?w - window_type_tw_00030)
    :precondition (and
      (ready-road ?v ?r)
      (road-next ?r ?next)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
    )
    :effect (and
      (not (ready-road ?v ?r))
      (ready-road ?v ?next)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-road_0077-road_0282-tw_00060
    :parameters (?v - vehicle ?r - road_type_road_0077 ?next - road_type_road_0282 ?w - window_type_tw_00060)
    :precondition (and
      (ready-road ?v ?r)
      (road-next ?r ?next)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
    )
    :effect (and
      (not (ready-road ?v ?r))
      (ready-road ?v ?next)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-road_0077-road_0282-tw_00090
    :parameters (?v - vehicle ?r - road_type_road_0077 ?next - road_type_road_0282 ?w - window_type_tw_00090)
    :precondition (and
      (ready-road ?v ?r)
      (road-next ?r ?next)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
    )
    :effect (and
      (not (ready-road ?v ?r))
      (ready-road ?v ?next)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-road_0077-road_0282-tw_00120
    :parameters (?v - vehicle ?r - road_type_road_0077 ?next - road_type_road_0282 ?w - window_type_tw_00120)
    :precondition (and
      (ready-road ?v ?r)
      (road-next ?r ?next)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
    )
    :effect (and
      (not (ready-road ?v ?r))
      (ready-road ?v ?next)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-road_0077-road_0282-tw_00150
    :parameters (?v - vehicle ?r - road_type_road_0077 ?next - road_type_road_0282 ?w - window_type_tw_00150)
    :precondition (and
      (ready-road ?v ?r)
      (road-next ?r ?next)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
    )
    :effect (and
      (not (ready-road ?v ?r))
      (ready-road ?v ?next)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-road_0077-road_0282-tw_00180
    :parameters (?v - vehicle ?r - road_type_road_0077 ?next - road_type_road_0282 ?w - window_type_tw_00180)
    :precondition (and
      (ready-road ?v ?r)
      (road-next ?r ?next)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
    )
    :effect (and
      (not (ready-road ?v ?r))
      (ready-road ?v ?next)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-road_0077-road_0282-tw_00210
    :parameters (?v - vehicle ?r - road_type_road_0077 ?next - road_type_road_0282 ?w - window_type_tw_00210)
    :precondition (and
      (ready-road ?v ?r)
      (road-next ?r ?next)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
    )
    :effect (and
      (not (ready-road ?v ?r))
      (ready-road ?v ?next)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-road_0077-road_0282-tw_00240
    :parameters (?v - vehicle ?r - road_type_road_0077 ?next - road_type_road_0282 ?w - window_type_tw_00240)
    :precondition (and
      (ready-road ?v ?r)
      (road-next ?r ?next)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
    )
    :effect (and
      (not (ready-road ?v ?r))
      (ready-road ?v ?next)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-road_0077-road_0282-tw_00270
    :parameters (?v - vehicle ?r - road_type_road_0077 ?next - road_type_road_0282 ?w - window_type_tw_00270)
    :precondition (and
      (ready-road ?v ?r)
      (road-next ?r ?next)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
    )
    :effect (and
      (not (ready-road ?v ?r))
      (ready-road ?v ?next)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-road_0077-road_0282-tw_00300
    :parameters (?v - vehicle ?r - road_type_road_0077 ?next - road_type_road_0282 ?w - window_type_tw_00300)
    :precondition (and
      (ready-road ?v ?r)
      (road-next ?r ?next)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
    )
    :effect (and
      (not (ready-road ?v ?r))
      (ready-road ?v ?next)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-road_0077-road_0282-tw_00330
    :parameters (?v - vehicle ?r - road_type_road_0077 ?next - road_type_road_0282 ?w - window_type_tw_00330)
    :precondition (and
      (ready-road ?v ?r)
      (road-next ?r ?next)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
    )
    :effect (and
      (not (ready-road ?v ?r))
      (ready-road ?v ?next)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-road_0077-road_0282-tw_00360
    :parameters (?v - vehicle ?r - road_type_road_0077 ?next - road_type_road_0282 ?w - window_type_tw_00360)
    :precondition (and
      (ready-road ?v ?r)
      (road-next ?r ?next)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
    )
    :effect (and
      (not (ready-road ?v ?r))
      (ready-road ?v ?next)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-road_0077-road_0282-tw_00390
    :parameters (?v - vehicle ?r - road_type_road_0077 ?next - road_type_road_0282 ?w - window_type_tw_00390)
    :precondition (and
      (ready-road ?v ?r)
      (road-next ?r ?next)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
    )
    :effect (and
      (not (ready-road ?v ?r))
      (ready-road ?v ?next)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-road_0077-road_0282-tw_00420
    :parameters (?v - vehicle ?r - road_type_road_0077 ?next - road_type_road_0282 ?w - window_type_tw_00420)
    :precondition (and
      (ready-road ?v ?r)
      (road-next ?r ?next)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
    )
    :effect (and
      (not (ready-road ?v ?r))
      (ready-road ?v ?next)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-road_0077-road_0282-tw_00450
    :parameters (?v - vehicle ?r - road_type_road_0077 ?next - road_type_road_0282 ?w - window_type_tw_00450)
    :precondition (and
      (ready-road ?v ?r)
      (road-next ?r ?next)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
    )
    :effect (and
      (not (ready-road ?v ?r))
      (ready-road ?v ?next)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-road_0077-road_0282-tw_00480
    :parameters (?v - vehicle ?r - road_type_road_0077 ?next - road_type_road_0282 ?w - window_type_tw_00480)
    :precondition (and
      (ready-road ?v ?r)
      (road-next ?r ?next)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
    )
    :effect (and
      (not (ready-road ?v ?r))
      (ready-road ?v ?next)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-road_0077-road_0282-tw_00510
    :parameters (?v - vehicle ?r - road_type_road_0077 ?next - road_type_road_0282 ?w - window_type_tw_00510)
    :precondition (and
      (ready-road ?v ?r)
      (road-next ?r ?next)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
    )
    :effect (and
      (not (ready-road ?v ?r))
      (ready-road ?v ?next)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-road_0077-road_0282-tw_00540
    :parameters (?v - vehicle ?r - road_type_road_0077 ?next - road_type_road_0282 ?w - window_type_tw_00540)
    :precondition (and
      (ready-road ?v ?r)
      (road-next ?r ?next)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
    )
    :effect (and
      (not (ready-road ?v ?r))
      (ready-road ?v ?next)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-road_0077-road_0282-tw_00570
    :parameters (?v - vehicle ?r - road_type_road_0077 ?next - road_type_road_0282 ?w - window_type_tw_00570)
    :precondition (and
      (ready-road ?v ?r)
      (road-next ?r ?next)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
    )
    :effect (and
      (not (ready-road ?v ?r))
      (ready-road ?v ?next)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-road_0077-road_0282-tw_00600
    :parameters (?v - vehicle ?r - road_type_road_0077 ?next - road_type_road_0282 ?w - window_type_tw_00600)
    :precondition (and
      (ready-road ?v ?r)
      (road-next ?r ?next)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
    )
    :effect (and
      (not (ready-road ?v ?r))
      (ready-road ?v ?next)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-static-road_0078-road_0271
    :parameters (?v - vehicle ?r - road_type_road_0078 ?next - road_type_road_0271)
    :precondition (and
      (ready-road ?v ?r)
      (road-next ?r ?next)
      (road-open ?r)
      (static-road ?r)
      (not (road-blocked ?r))
    )
    :effect (and
      (not (ready-road ?v ?r))
      (ready-road ?v ?next)
      (increase (travel-time ?v) (travel-duration ?r))
      (increase (sim-time) (travel-duration ?r))
    )
  )

  (:action traverse-road-static-road_0078-road_0272
    :parameters (?v - vehicle ?r - road_type_road_0078 ?next - road_type_road_0272)
    :precondition (and
      (ready-road ?v ?r)
      (road-next ?r ?next)
      (road-open ?r)
      (static-road ?r)
      (not (road-blocked ?r))
    )
    :effect (and
      (not (ready-road ?v ?r))
      (ready-road ?v ?next)
      (increase (travel-time ?v) (travel-duration ?r))
      (increase (sim-time) (travel-duration ?r))
    )
  )

  (:action traverse-road-static-road_0079-road_0615
    :parameters (?v - vehicle ?r - road_type_road_0079 ?next - road_type_road_0615)
    :precondition (and
      (ready-road ?v ?r)
      (road-next ?r ?next)
      (road-open ?r)
      (static-road ?r)
      (not (road-blocked ?r))
    )
    :effect (and
      (not (ready-road ?v ?r))
      (ready-road ?v ?next)
      (increase (travel-time ?v) (travel-duration ?r))
      (increase (sim-time) (travel-duration ?r))
    )
  )

  (:action traverse-road-static-road_0079-road_0616
    :parameters (?v - vehicle ?r - road_type_road_0079 ?next - road_type_road_0616)
    :precondition (and
      (ready-road ?v ?r)
      (road-next ?r ?next)
      (road-open ?r)
      (static-road ?r)
      (not (road-blocked ?r))
    )
    :effect (and
      (not (ready-road ?v ?r))
      (ready-road ?v ?next)
      (increase (travel-time ?v) (travel-duration ?r))
      (increase (sim-time) (travel-duration ?r))
    )
  )

  (:action traverse-road-static-road_0080-macro_0008
    :parameters (?v - vehicle ?r - road_type_road_0080 ?next - road_type_macro_0008)
    :precondition (and
      (ready-road ?v ?r)
      (road-next ?r ?next)
      (road-open ?r)
      (static-road ?r)
      (not (road-blocked ?r))
    )
    :effect (and
      (not (ready-road ?v ?r))
      (ready-road ?v ?next)
      (increase (travel-time ?v) (travel-duration ?r))
      (increase (sim-time) (travel-duration ?r))
    )
  )

  (:action traverse-road-static-road_0080-road_0086
    :parameters (?v - vehicle ?r - road_type_road_0080 ?next - road_type_road_0086)
    :precondition (and
      (ready-road ?v ?r)
      (road-next ?r ?next)
      (road-open ?r)
      (static-road ?r)
      (not (road-blocked ?r))
    )
    :effect (and
      (not (ready-road ?v ?r))
      (ready-road ?v ?next)
      (increase (travel-time ?v) (travel-duration ?r))
      (increase (sim-time) (travel-duration ?r))
    )
  )

  (:action traverse-road-static-road_0080-road_0087
    :parameters (?v - vehicle ?r - road_type_road_0080 ?next - road_type_road_0087)
    :precondition (and
      (ready-road ?v ?r)
      (road-next ?r ?next)
      (road-open ?r)
      (static-road ?r)
      (not (road-blocked ?r))
    )
    :effect (and
      (not (ready-road ?v ?r))
      (ready-road ?v ?next)
      (increase (travel-time ?v) (travel-duration ?r))
      (increase (sim-time) (travel-duration ?r))
    )
  )

  (:action traverse-road-static-road_0080-road_0088
    :parameters (?v - vehicle ?r - road_type_road_0080 ?next - road_type_road_0088)
    :precondition (and
      (ready-road ?v ?r)
      (road-next ?r ?next)
      (road-open ?r)
      (static-road ?r)
      (not (road-blocked ?r))
    )
    :effect (and
      (not (ready-road ?v ?r))
      (ready-road ?v ?next)
      (increase (travel-time ?v) (travel-duration ?r))
      (increase (sim-time) (travel-duration ?r))
    )
  )

  (:action traverse-road-static-road_0081-road_0856
    :parameters (?v - vehicle ?r - road_type_road_0081 ?next - road_type_road_0856)
    :precondition (and
      (ready-road ?v ?r)
      (road-next ?r ?next)
      (road-open ?r)
      (static-road ?r)
      (not (road-blocked ?r))
    )
    :effect (and
      (not (ready-road ?v ?r))
      (ready-road ?v ?next)
      (increase (travel-time ?v) (travel-duration ?r))
      (increase (sim-time) (travel-duration ?r))
    )
  )

  (:action traverse-road-static-road_0081-road_0857
    :parameters (?v - vehicle ?r - road_type_road_0081 ?next - road_type_road_0857)
    :precondition (and
      (ready-road ?v ?r)
      (road-next ?r ?next)
      (road-open ?r)
      (static-road ?r)
      (not (road-blocked ?r))
    )
    :effect (and
      (not (ready-road ?v ?r))
      (ready-road ?v ?next)
      (increase (travel-time ?v) (travel-duration ?r))
      (increase (sim-time) (travel-duration ?r))
    )
  )

  (:action traverse-road-static-road_0082-road_0080
    :parameters (?v - vehicle ?r - road_type_road_0082 ?next - road_type_road_0080)
    :precondition (and
      (ready-road ?v ?r)
      (road-next ?r ?next)
      (road-open ?r)
      (static-road ?r)
      (not (road-blocked ?r))
    )
    :effect (and
      (not (ready-road ?v ?r))
      (ready-road ?v ?next)
      (increase (travel-time ?v) (travel-duration ?r))
      (increase (sim-time) (travel-duration ?r))
    )
  )

  (:action traverse-road-static-road_0082-road_0081
    :parameters (?v - vehicle ?r - road_type_road_0082 ?next - road_type_road_0081)
    :precondition (and
      (ready-road ?v ?r)
      (road-next ?r ?next)
      (road-open ?r)
      (static-road ?r)
      (not (road-blocked ?r))
    )
    :effect (and
      (not (ready-road ?v ?r))
      (ready-road ?v ?next)
      (increase (travel-time ?v) (travel-duration ?r))
      (increase (sim-time) (travel-duration ?r))
    )
  )

  (:action traverse-road-static-road_0083-road_0093
    :parameters (?v - vehicle ?r - road_type_road_0083 ?next - road_type_road_0093)
    :precondition (and
      (ready-road ?v ?r)
      (road-next ?r ?next)
      (road-open ?r)
      (static-road ?r)
      (not (road-blocked ?r))
    )
    :effect (and
      (not (ready-road ?v ?r))
      (ready-road ?v ?next)
      (increase (travel-time ?v) (travel-duration ?r))
      (increase (sim-time) (travel-duration ?r))
    )
  )

  (:action traverse-road-static-road_0083-road_0094
    :parameters (?v - vehicle ?r - road_type_road_0083 ?next - road_type_road_0094)
    :precondition (and
      (ready-road ?v ?r)
      (road-next ?r ?next)
      (road-open ?r)
      (static-road ?r)
      (not (road-blocked ?r))
    )
    :effect (and
      (not (ready-road ?v ?r))
      (ready-road ?v ?next)
      (increase (travel-time ?v) (travel-duration ?r))
      (increase (sim-time) (travel-duration ?r))
    )
  )

  (:action traverse-road-static-road_0084-road_0052
    :parameters (?v - vehicle ?r - road_type_road_0084 ?next - road_type_road_0052)
    :precondition (and
      (ready-road ?v ?r)
      (road-next ?r ?next)
      (road-open ?r)
      (static-road ?r)
      (not (road-blocked ?r))
    )
    :effect (and
      (not (ready-road ?v ?r))
      (ready-road ?v ?next)
      (increase (travel-time ?v) (travel-duration ?r))
      (increase (sim-time) (travel-duration ?r))
    )
  )

  (:action traverse-road-dynamic-road_0086-road_0120-tw_00000
    :parameters (?v - vehicle ?r - road_type_road_0086 ?next - road_type_road_0120 ?w - window_type_tw_00000)
    :precondition (and
      (ready-road ?v ?r)
      (road-next ?r ?next)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
    )
    :effect (and
      (not (ready-road ?v ?r))
      (ready-road ?v ?next)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-road_0086-road_0120-tw_00030
    :parameters (?v - vehicle ?r - road_type_road_0086 ?next - road_type_road_0120 ?w - window_type_tw_00030)
    :precondition (and
      (ready-road ?v ?r)
      (road-next ?r ?next)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
    )
    :effect (and
      (not (ready-road ?v ?r))
      (ready-road ?v ?next)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-road_0086-road_0120-tw_00060
    :parameters (?v - vehicle ?r - road_type_road_0086 ?next - road_type_road_0120 ?w - window_type_tw_00060)
    :precondition (and
      (ready-road ?v ?r)
      (road-next ?r ?next)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
    )
    :effect (and
      (not (ready-road ?v ?r))
      (ready-road ?v ?next)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-road_0086-road_0120-tw_00090
    :parameters (?v - vehicle ?r - road_type_road_0086 ?next - road_type_road_0120 ?w - window_type_tw_00090)
    :precondition (and
      (ready-road ?v ?r)
      (road-next ?r ?next)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
    )
    :effect (and
      (not (ready-road ?v ?r))
      (ready-road ?v ?next)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-road_0086-road_0120-tw_00120
    :parameters (?v - vehicle ?r - road_type_road_0086 ?next - road_type_road_0120 ?w - window_type_tw_00120)
    :precondition (and
      (ready-road ?v ?r)
      (road-next ?r ?next)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
    )
    :effect (and
      (not (ready-road ?v ?r))
      (ready-road ?v ?next)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-road_0086-road_0120-tw_00150
    :parameters (?v - vehicle ?r - road_type_road_0086 ?next - road_type_road_0120 ?w - window_type_tw_00150)
    :precondition (and
      (ready-road ?v ?r)
      (road-next ?r ?next)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
    )
    :effect (and
      (not (ready-road ?v ?r))
      (ready-road ?v ?next)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-road_0086-road_0120-tw_00180
    :parameters (?v - vehicle ?r - road_type_road_0086 ?next - road_type_road_0120 ?w - window_type_tw_00180)
    :precondition (and
      (ready-road ?v ?r)
      (road-next ?r ?next)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
    )
    :effect (and
      (not (ready-road ?v ?r))
      (ready-road ?v ?next)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-road_0086-road_0120-tw_00210
    :parameters (?v - vehicle ?r - road_type_road_0086 ?next - road_type_road_0120 ?w - window_type_tw_00210)
    :precondition (and
      (ready-road ?v ?r)
      (road-next ?r ?next)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
    )
    :effect (and
      (not (ready-road ?v ?r))
      (ready-road ?v ?next)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-road_0086-road_0120-tw_00240
    :parameters (?v - vehicle ?r - road_type_road_0086 ?next - road_type_road_0120 ?w - window_type_tw_00240)
    :precondition (and
      (ready-road ?v ?r)
      (road-next ?r ?next)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
    )
    :effect (and
      (not (ready-road ?v ?r))
      (ready-road ?v ?next)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-road_0086-road_0120-tw_00270
    :parameters (?v - vehicle ?r - road_type_road_0086 ?next - road_type_road_0120 ?w - window_type_tw_00270)
    :precondition (and
      (ready-road ?v ?r)
      (road-next ?r ?next)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
    )
    :effect (and
      (not (ready-road ?v ?r))
      (ready-road ?v ?next)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-road_0086-road_0120-tw_00300
    :parameters (?v - vehicle ?r - road_type_road_0086 ?next - road_type_road_0120 ?w - window_type_tw_00300)
    :precondition (and
      (ready-road ?v ?r)
      (road-next ?r ?next)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
    )
    :effect (and
      (not (ready-road ?v ?r))
      (ready-road ?v ?next)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-road_0086-road_0120-tw_00330
    :parameters (?v - vehicle ?r - road_type_road_0086 ?next - road_type_road_0120 ?w - window_type_tw_00330)
    :precondition (and
      (ready-road ?v ?r)
      (road-next ?r ?next)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
    )
    :effect (and
      (not (ready-road ?v ?r))
      (ready-road ?v ?next)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-road_0086-road_0120-tw_00360
    :parameters (?v - vehicle ?r - road_type_road_0086 ?next - road_type_road_0120 ?w - window_type_tw_00360)
    :precondition (and
      (ready-road ?v ?r)
      (road-next ?r ?next)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
    )
    :effect (and
      (not (ready-road ?v ?r))
      (ready-road ?v ?next)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-road_0086-road_0120-tw_00390
    :parameters (?v - vehicle ?r - road_type_road_0086 ?next - road_type_road_0120 ?w - window_type_tw_00390)
    :precondition (and
      (ready-road ?v ?r)
      (road-next ?r ?next)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
    )
    :effect (and
      (not (ready-road ?v ?r))
      (ready-road ?v ?next)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-road_0086-road_0120-tw_00420
    :parameters (?v - vehicle ?r - road_type_road_0086 ?next - road_type_road_0120 ?w - window_type_tw_00420)
    :precondition (and
      (ready-road ?v ?r)
      (road-next ?r ?next)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
    )
    :effect (and
      (not (ready-road ?v ?r))
      (ready-road ?v ?next)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-road_0086-road_0120-tw_00450
    :parameters (?v - vehicle ?r - road_type_road_0086 ?next - road_type_road_0120 ?w - window_type_tw_00450)
    :precondition (and
      (ready-road ?v ?r)
      (road-next ?r ?next)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
    )
    :effect (and
      (not (ready-road ?v ?r))
      (ready-road ?v ?next)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-road_0086-road_0120-tw_00480
    :parameters (?v - vehicle ?r - road_type_road_0086 ?next - road_type_road_0120 ?w - window_type_tw_00480)
    :precondition (and
      (ready-road ?v ?r)
      (road-next ?r ?next)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
    )
    :effect (and
      (not (ready-road ?v ?r))
      (ready-road ?v ?next)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-road_0086-road_0120-tw_00510
    :parameters (?v - vehicle ?r - road_type_road_0086 ?next - road_type_road_0120 ?w - window_type_tw_00510)
    :precondition (and
      (ready-road ?v ?r)
      (road-next ?r ?next)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
    )
    :effect (and
      (not (ready-road ?v ?r))
      (ready-road ?v ?next)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-road_0086-road_0120-tw_00540
    :parameters (?v - vehicle ?r - road_type_road_0086 ?next - road_type_road_0120 ?w - window_type_tw_00540)
    :precondition (and
      (ready-road ?v ?r)
      (road-next ?r ?next)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
    )
    :effect (and
      (not (ready-road ?v ?r))
      (ready-road ?v ?next)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-road_0086-road_0120-tw_00570
    :parameters (?v - vehicle ?r - road_type_road_0086 ?next - road_type_road_0120 ?w - window_type_tw_00570)
    :precondition (and
      (ready-road ?v ?r)
      (road-next ?r ?next)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
    )
    :effect (and
      (not (ready-road ?v ?r))
      (ready-road ?v ?next)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-road_0086-road_0120-tw_00600
    :parameters (?v - vehicle ?r - road_type_road_0086 ?next - road_type_road_0120 ?w - window_type_tw_00600)
    :precondition (and
      (ready-road ?v ?r)
      (road-next ?r ?next)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
    )
    :effect (and
      (not (ready-road ?v ?r))
      (ready-road ?v ?next)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-road_0086-road_0121-tw_00000
    :parameters (?v - vehicle ?r - road_type_road_0086 ?next - road_type_road_0121 ?w - window_type_tw_00000)
    :precondition (and
      (ready-road ?v ?r)
      (road-next ?r ?next)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
    )
    :effect (and
      (not (ready-road ?v ?r))
      (ready-road ?v ?next)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-road_0086-road_0121-tw_00030
    :parameters (?v - vehicle ?r - road_type_road_0086 ?next - road_type_road_0121 ?w - window_type_tw_00030)
    :precondition (and
      (ready-road ?v ?r)
      (road-next ?r ?next)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
    )
    :effect (and
      (not (ready-road ?v ?r))
      (ready-road ?v ?next)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-road_0086-road_0121-tw_00060
    :parameters (?v - vehicle ?r - road_type_road_0086 ?next - road_type_road_0121 ?w - window_type_tw_00060)
    :precondition (and
      (ready-road ?v ?r)
      (road-next ?r ?next)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
    )
    :effect (and
      (not (ready-road ?v ?r))
      (ready-road ?v ?next)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-road_0086-road_0121-tw_00090
    :parameters (?v - vehicle ?r - road_type_road_0086 ?next - road_type_road_0121 ?w - window_type_tw_00090)
    :precondition (and
      (ready-road ?v ?r)
      (road-next ?r ?next)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
    )
    :effect (and
      (not (ready-road ?v ?r))
      (ready-road ?v ?next)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-road_0086-road_0121-tw_00120
    :parameters (?v - vehicle ?r - road_type_road_0086 ?next - road_type_road_0121 ?w - window_type_tw_00120)
    :precondition (and
      (ready-road ?v ?r)
      (road-next ?r ?next)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
    )
    :effect (and
      (not (ready-road ?v ?r))
      (ready-road ?v ?next)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-road_0086-road_0121-tw_00150
    :parameters (?v - vehicle ?r - road_type_road_0086 ?next - road_type_road_0121 ?w - window_type_tw_00150)
    :precondition (and
      (ready-road ?v ?r)
      (road-next ?r ?next)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
    )
    :effect (and
      (not (ready-road ?v ?r))
      (ready-road ?v ?next)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-road_0086-road_0121-tw_00180
    :parameters (?v - vehicle ?r - road_type_road_0086 ?next - road_type_road_0121 ?w - window_type_tw_00180)
    :precondition (and
      (ready-road ?v ?r)
      (road-next ?r ?next)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
    )
    :effect (and
      (not (ready-road ?v ?r))
      (ready-road ?v ?next)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-road_0086-road_0121-tw_00210
    :parameters (?v - vehicle ?r - road_type_road_0086 ?next - road_type_road_0121 ?w - window_type_tw_00210)
    :precondition (and
      (ready-road ?v ?r)
      (road-next ?r ?next)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
    )
    :effect (and
      (not (ready-road ?v ?r))
      (ready-road ?v ?next)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-road_0086-road_0121-tw_00240
    :parameters (?v - vehicle ?r - road_type_road_0086 ?next - road_type_road_0121 ?w - window_type_tw_00240)
    :precondition (and
      (ready-road ?v ?r)
      (road-next ?r ?next)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
    )
    :effect (and
      (not (ready-road ?v ?r))
      (ready-road ?v ?next)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-road_0086-road_0121-tw_00270
    :parameters (?v - vehicle ?r - road_type_road_0086 ?next - road_type_road_0121 ?w - window_type_tw_00270)
    :precondition (and
      (ready-road ?v ?r)
      (road-next ?r ?next)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
    )
    :effect (and
      (not (ready-road ?v ?r))
      (ready-road ?v ?next)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-road_0086-road_0121-tw_00300
    :parameters (?v - vehicle ?r - road_type_road_0086 ?next - road_type_road_0121 ?w - window_type_tw_00300)
    :precondition (and
      (ready-road ?v ?r)
      (road-next ?r ?next)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
    )
    :effect (and
      (not (ready-road ?v ?r))
      (ready-road ?v ?next)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-road_0086-road_0121-tw_00330
    :parameters (?v - vehicle ?r - road_type_road_0086 ?next - road_type_road_0121 ?w - window_type_tw_00330)
    :precondition (and
      (ready-road ?v ?r)
      (road-next ?r ?next)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
    )
    :effect (and
      (not (ready-road ?v ?r))
      (ready-road ?v ?next)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-road_0086-road_0121-tw_00360
    :parameters (?v - vehicle ?r - road_type_road_0086 ?next - road_type_road_0121 ?w - window_type_tw_00360)
    :precondition (and
      (ready-road ?v ?r)
      (road-next ?r ?next)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
    )
    :effect (and
      (not (ready-road ?v ?r))
      (ready-road ?v ?next)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-road_0086-road_0121-tw_00390
    :parameters (?v - vehicle ?r - road_type_road_0086 ?next - road_type_road_0121 ?w - window_type_tw_00390)
    :precondition (and
      (ready-road ?v ?r)
      (road-next ?r ?next)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
    )
    :effect (and
      (not (ready-road ?v ?r))
      (ready-road ?v ?next)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-road_0086-road_0121-tw_00420
    :parameters (?v - vehicle ?r - road_type_road_0086 ?next - road_type_road_0121 ?w - window_type_tw_00420)
    :precondition (and
      (ready-road ?v ?r)
      (road-next ?r ?next)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
    )
    :effect (and
      (not (ready-road ?v ?r))
      (ready-road ?v ?next)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-road_0086-road_0121-tw_00450
    :parameters (?v - vehicle ?r - road_type_road_0086 ?next - road_type_road_0121 ?w - window_type_tw_00450)
    :precondition (and
      (ready-road ?v ?r)
      (road-next ?r ?next)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
    )
    :effect (and
      (not (ready-road ?v ?r))
      (ready-road ?v ?next)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-road_0086-road_0121-tw_00480
    :parameters (?v - vehicle ?r - road_type_road_0086 ?next - road_type_road_0121 ?w - window_type_tw_00480)
    :precondition (and
      (ready-road ?v ?r)
      (road-next ?r ?next)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
    )
    :effect (and
      (not (ready-road ?v ?r))
      (ready-road ?v ?next)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-road_0086-road_0121-tw_00510
    :parameters (?v - vehicle ?r - road_type_road_0086 ?next - road_type_road_0121 ?w - window_type_tw_00510)
    :precondition (and
      (ready-road ?v ?r)
      (road-next ?r ?next)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
    )
    :effect (and
      (not (ready-road ?v ?r))
      (ready-road ?v ?next)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-road_0086-road_0121-tw_00540
    :parameters (?v - vehicle ?r - road_type_road_0086 ?next - road_type_road_0121 ?w - window_type_tw_00540)
    :precondition (and
      (ready-road ?v ?r)
      (road-next ?r ?next)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
    )
    :effect (and
      (not (ready-road ?v ?r))
      (ready-road ?v ?next)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-road_0086-road_0121-tw_00570
    :parameters (?v - vehicle ?r - road_type_road_0086 ?next - road_type_road_0121 ?w - window_type_tw_00570)
    :precondition (and
      (ready-road ?v ?r)
      (road-next ?r ?next)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
    )
    :effect (and
      (not (ready-road ?v ?r))
      (ready-road ?v ?next)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-road_0086-road_0121-tw_00600
    :parameters (?v - vehicle ?r - road_type_road_0086 ?next - road_type_road_0121 ?w - window_type_tw_00600)
    :precondition (and
      (ready-road ?v ?r)
      (road-next ?r ?next)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
    )
    :effect (and
      (not (ready-road ?v ?r))
      (ready-road ?v ?next)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-static-road_0087-road_0846
    :parameters (?v - vehicle ?r - road_type_road_0087 ?next - road_type_road_0846)
    :precondition (and
      (ready-road ?v ?r)
      (road-next ?r ?next)
      (road-open ?r)
      (static-road ?r)
      (not (road-blocked ?r))
    )
    :effect (and
      (not (ready-road ?v ?r))
      (ready-road ?v ?next)
      (increase (travel-time ?v) (travel-duration ?r))
      (increase (sim-time) (travel-duration ?r))
    )
  )

  (:action traverse-road-static-road_0087-road_0847
    :parameters (?v - vehicle ?r - road_type_road_0087 ?next - road_type_road_0847)
    :precondition (and
      (ready-road ?v ?r)
      (road-next ?r ?next)
      (road-open ?r)
      (static-road ?r)
      (not (road-blocked ?r))
    )
    :effect (and
      (not (ready-road ?v ?r))
      (ready-road ?v ?next)
      (increase (travel-time ?v) (travel-duration ?r))
      (increase (sim-time) (travel-duration ?r))
    )
  )

  (:action traverse-road-static-road_0088-road_0080
    :parameters (?v - vehicle ?r - road_type_road_0088 ?next - road_type_road_0080)
    :precondition (and
      (ready-road ?v ?r)
      (road-next ?r ?next)
      (road-open ?r)
      (static-road ?r)
      (not (road-blocked ?r))
    )
    :effect (and
      (not (ready-road ?v ?r))
      (ready-road ?v ?next)
      (increase (travel-time ?v) (travel-duration ?r))
      (increase (sim-time) (travel-duration ?r))
    )
  )

  (:action traverse-road-static-road_0088-road_0081
    :parameters (?v - vehicle ?r - road_type_road_0088 ?next - road_type_road_0081)
    :precondition (and
      (ready-road ?v ?r)
      (road-next ?r ?next)
      (road-open ?r)
      (static-road ?r)
      (not (road-blocked ?r))
    )
    :effect (and
      (not (ready-road ?v ?r))
      (ready-road ?v ?next)
      (increase (travel-time ?v) (travel-duration ?r))
      (increase (sim-time) (travel-duration ?r))
    )
  )

  (:action traverse-road-static-road_0089-road_0388
    :parameters (?v - vehicle ?r - road_type_road_0089 ?next - road_type_road_0388)
    :precondition (and
      (ready-road ?v ?r)
      (road-next ?r ?next)
      (road-open ?r)
      (static-road ?r)
      (not (road-blocked ?r))
    )
    :effect (and
      (not (ready-road ?v ?r))
      (ready-road ?v ?next)
      (increase (travel-time ?v) (travel-duration ?r))
      (increase (sim-time) (travel-duration ?r))
    )
  )

  (:action traverse-road-static-road_0089-road_0389
    :parameters (?v - vehicle ?r - road_type_road_0089 ?next - road_type_road_0389)
    :precondition (and
      (ready-road ?v ?r)
      (road-next ?r ?next)
      (road-open ?r)
      (static-road ?r)
      (not (road-blocked ?r))
    )
    :effect (and
      (not (ready-road ?v ?r))
      (ready-road ?v ?next)
      (increase (travel-time ?v) (travel-duration ?r))
      (increase (sim-time) (travel-duration ?r))
    )
  )

  (:action traverse-road-static-road_0089-road_0390
    :parameters (?v - vehicle ?r - road_type_road_0089 ?next - road_type_road_0390)
    :precondition (and
      (ready-road ?v ?r)
      (road-next ?r ?next)
      (road-open ?r)
      (static-road ?r)
      (not (road-blocked ?r))
    )
    :effect (and
      (not (ready-road ?v ?r))
      (ready-road ?v ?next)
      (increase (travel-time ?v) (travel-duration ?r))
      (increase (sim-time) (travel-duration ?r))
    )
  )

  (:action traverse-road-static-road_0090-road_0453
    :parameters (?v - vehicle ?r - road_type_road_0090 ?next - road_type_road_0453)
    :precondition (and
      (ready-road ?v ?r)
      (road-next ?r ?next)
      (road-open ?r)
      (static-road ?r)
      (not (road-blocked ?r))
    )
    :effect (and
      (not (ready-road ?v ?r))
      (ready-road ?v ?next)
      (increase (travel-time ?v) (travel-duration ?r))
      (increase (sim-time) (travel-duration ?r))
    )
  )

  (:action traverse-road-static-road_0090-road_0454
    :parameters (?v - vehicle ?r - road_type_road_0090 ?next - road_type_road_0454)
    :precondition (and
      (ready-road ?v ?r)
      (road-next ?r ?next)
      (road-open ?r)
      (static-road ?r)
      (not (road-blocked ?r))
    )
    :effect (and
      (not (ready-road ?v ?r))
      (ready-road ?v ?next)
      (increase (travel-time ?v) (travel-duration ?r))
      (increase (sim-time) (travel-duration ?r))
    )
  )

  (:action traverse-road-static-road_0090-road_0455
    :parameters (?v - vehicle ?r - road_type_road_0090 ?next - road_type_road_0455)
    :precondition (and
      (ready-road ?v ?r)
      (road-next ?r ?next)
      (road-open ?r)
      (static-road ?r)
      (not (road-blocked ?r))
    )
    :effect (and
      (not (ready-road ?v ?r))
      (ready-road ?v ?next)
      (increase (travel-time ?v) (travel-duration ?r))
      (increase (sim-time) (travel-duration ?r))
    )
  )

  (:action traverse-road-static-road_0091-road_0354
    :parameters (?v - vehicle ?r - road_type_road_0091 ?next - road_type_road_0354)
    :precondition (and
      (ready-road ?v ?r)
      (road-next ?r ?next)
      (road-open ?r)
      (static-road ?r)
      (not (road-blocked ?r))
    )
    :effect (and
      (not (ready-road ?v ?r))
      (ready-road ?v ?next)
      (increase (travel-time ?v) (travel-duration ?r))
      (increase (sim-time) (travel-duration ?r))
    )
  )

  (:action traverse-road-static-road_0091-road_0355
    :parameters (?v - vehicle ?r - road_type_road_0091 ?next - road_type_road_0355)
    :precondition (and
      (ready-road ?v ?r)
      (road-next ?r ?next)
      (road-open ?r)
      (static-road ?r)
      (not (road-blocked ?r))
    )
    :effect (and
      (not (ready-road ?v ?r))
      (ready-road ?v ?next)
      (increase (travel-time ?v) (travel-duration ?r))
      (increase (sim-time) (travel-duration ?r))
    )
  )

  (:action traverse-road-static-road_0091-road_0356
    :parameters (?v - vehicle ?r - road_type_road_0091 ?next - road_type_road_0356)
    :precondition (and
      (ready-road ?v ?r)
      (road-next ?r ?next)
      (road-open ?r)
      (static-road ?r)
      (not (road-blocked ?r))
    )
    :effect (and
      (not (ready-road ?v ?r))
      (ready-road ?v ?next)
      (increase (travel-time ?v) (travel-duration ?r))
      (increase (sim-time) (travel-duration ?r))
    )
  )

  (:action traverse-road-static-road_0092-road_0172
    :parameters (?v - vehicle ?r - road_type_road_0092 ?next - road_type_road_0172)
    :precondition (and
      (ready-road ?v ?r)
      (road-next ?r ?next)
      (road-open ?r)
      (static-road ?r)
      (not (road-blocked ?r))
    )
    :effect (and
      (not (ready-road ?v ?r))
      (ready-road ?v ?next)
      (increase (travel-time ?v) (travel-duration ?r))
      (increase (sim-time) (travel-duration ?r))
    )
  )

  (:action traverse-road-static-road_0092-road_0173
    :parameters (?v - vehicle ?r - road_type_road_0092 ?next - road_type_road_0173)
    :precondition (and
      (ready-road ?v ?r)
      (road-next ?r ?next)
      (road-open ?r)
      (static-road ?r)
      (not (road-blocked ?r))
    )
    :effect (and
      (not (ready-road ?v ?r))
      (ready-road ?v ?next)
      (increase (travel-time ?v) (travel-duration ?r))
      (increase (sim-time) (travel-duration ?r))
    )
  )

  (:action traverse-road-dynamic-road_0093-road_0098-tw_00000
    :parameters (?v - vehicle ?r - road_type_road_0093 ?next - road_type_road_0098 ?w - window_type_tw_00000)
    :precondition (and
      (ready-road ?v ?r)
      (road-next ?r ?next)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
    )
    :effect (and
      (not (ready-road ?v ?r))
      (ready-road ?v ?next)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-road_0093-road_0098-tw_00030
    :parameters (?v - vehicle ?r - road_type_road_0093 ?next - road_type_road_0098 ?w - window_type_tw_00030)
    :precondition (and
      (ready-road ?v ?r)
      (road-next ?r ?next)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
    )
    :effect (and
      (not (ready-road ?v ?r))
      (ready-road ?v ?next)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-road_0093-road_0098-tw_00060
    :parameters (?v - vehicle ?r - road_type_road_0093 ?next - road_type_road_0098 ?w - window_type_tw_00060)
    :precondition (and
      (ready-road ?v ?r)
      (road-next ?r ?next)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
    )
    :effect (and
      (not (ready-road ?v ?r))
      (ready-road ?v ?next)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-road_0093-road_0098-tw_00090
    :parameters (?v - vehicle ?r - road_type_road_0093 ?next - road_type_road_0098 ?w - window_type_tw_00090)
    :precondition (and
      (ready-road ?v ?r)
      (road-next ?r ?next)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
    )
    :effect (and
      (not (ready-road ?v ?r))
      (ready-road ?v ?next)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-road_0093-road_0098-tw_00120
    :parameters (?v - vehicle ?r - road_type_road_0093 ?next - road_type_road_0098 ?w - window_type_tw_00120)
    :precondition (and
      (ready-road ?v ?r)
      (road-next ?r ?next)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
    )
    :effect (and
      (not (ready-road ?v ?r))
      (ready-road ?v ?next)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-road_0093-road_0098-tw_00150
    :parameters (?v - vehicle ?r - road_type_road_0093 ?next - road_type_road_0098 ?w - window_type_tw_00150)
    :precondition (and
      (ready-road ?v ?r)
      (road-next ?r ?next)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
    )
    :effect (and
      (not (ready-road ?v ?r))
      (ready-road ?v ?next)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-road_0093-road_0098-tw_00180
    :parameters (?v - vehicle ?r - road_type_road_0093 ?next - road_type_road_0098 ?w - window_type_tw_00180)
    :precondition (and
      (ready-road ?v ?r)
      (road-next ?r ?next)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
    )
    :effect (and
      (not (ready-road ?v ?r))
      (ready-road ?v ?next)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-road_0093-road_0098-tw_00210
    :parameters (?v - vehicle ?r - road_type_road_0093 ?next - road_type_road_0098 ?w - window_type_tw_00210)
    :precondition (and
      (ready-road ?v ?r)
      (road-next ?r ?next)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
    )
    :effect (and
      (not (ready-road ?v ?r))
      (ready-road ?v ?next)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-road_0093-road_0098-tw_00240
    :parameters (?v - vehicle ?r - road_type_road_0093 ?next - road_type_road_0098 ?w - window_type_tw_00240)
    :precondition (and
      (ready-road ?v ?r)
      (road-next ?r ?next)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
    )
    :effect (and
      (not (ready-road ?v ?r))
      (ready-road ?v ?next)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-road_0093-road_0098-tw_00270
    :parameters (?v - vehicle ?r - road_type_road_0093 ?next - road_type_road_0098 ?w - window_type_tw_00270)
    :precondition (and
      (ready-road ?v ?r)
      (road-next ?r ?next)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
    )
    :effect (and
      (not (ready-road ?v ?r))
      (ready-road ?v ?next)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-road_0093-road_0098-tw_00300
    :parameters (?v - vehicle ?r - road_type_road_0093 ?next - road_type_road_0098 ?w - window_type_tw_00300)
    :precondition (and
      (ready-road ?v ?r)
      (road-next ?r ?next)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
    )
    :effect (and
      (not (ready-road ?v ?r))
      (ready-road ?v ?next)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-road_0093-road_0098-tw_00330
    :parameters (?v - vehicle ?r - road_type_road_0093 ?next - road_type_road_0098 ?w - window_type_tw_00330)
    :precondition (and
      (ready-road ?v ?r)
      (road-next ?r ?next)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
    )
    :effect (and
      (not (ready-road ?v ?r))
      (ready-road ?v ?next)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-road_0093-road_0098-tw_00360
    :parameters (?v - vehicle ?r - road_type_road_0093 ?next - road_type_road_0098 ?w - window_type_tw_00360)
    :precondition (and
      (ready-road ?v ?r)
      (road-next ?r ?next)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
    )
    :effect (and
      (not (ready-road ?v ?r))
      (ready-road ?v ?next)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-road_0093-road_0098-tw_00390
    :parameters (?v - vehicle ?r - road_type_road_0093 ?next - road_type_road_0098 ?w - window_type_tw_00390)
    :precondition (and
      (ready-road ?v ?r)
      (road-next ?r ?next)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
    )
    :effect (and
      (not (ready-road ?v ?r))
      (ready-road ?v ?next)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-road_0093-road_0098-tw_00420
    :parameters (?v - vehicle ?r - road_type_road_0093 ?next - road_type_road_0098 ?w - window_type_tw_00420)
    :precondition (and
      (ready-road ?v ?r)
      (road-next ?r ?next)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
    )
    :effect (and
      (not (ready-road ?v ?r))
      (ready-road ?v ?next)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-road_0093-road_0098-tw_00450
    :parameters (?v - vehicle ?r - road_type_road_0093 ?next - road_type_road_0098 ?w - window_type_tw_00450)
    :precondition (and
      (ready-road ?v ?r)
      (road-next ?r ?next)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
    )
    :effect (and
      (not (ready-road ?v ?r))
      (ready-road ?v ?next)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-road_0093-road_0098-tw_00480
    :parameters (?v - vehicle ?r - road_type_road_0093 ?next - road_type_road_0098 ?w - window_type_tw_00480)
    :precondition (and
      (ready-road ?v ?r)
      (road-next ?r ?next)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
    )
    :effect (and
      (not (ready-road ?v ?r))
      (ready-road ?v ?next)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-road_0093-road_0098-tw_00510
    :parameters (?v - vehicle ?r - road_type_road_0093 ?next - road_type_road_0098 ?w - window_type_tw_00510)
    :precondition (and
      (ready-road ?v ?r)
      (road-next ?r ?next)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
    )
    :effect (and
      (not (ready-road ?v ?r))
      (ready-road ?v ?next)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-road_0093-road_0098-tw_00540
    :parameters (?v - vehicle ?r - road_type_road_0093 ?next - road_type_road_0098 ?w - window_type_tw_00540)
    :precondition (and
      (ready-road ?v ?r)
      (road-next ?r ?next)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
    )
    :effect (and
      (not (ready-road ?v ?r))
      (ready-road ?v ?next)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-road_0093-road_0098-tw_00570
    :parameters (?v - vehicle ?r - road_type_road_0093 ?next - road_type_road_0098 ?w - window_type_tw_00570)
    :precondition (and
      (ready-road ?v ?r)
      (road-next ?r ?next)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
    )
    :effect (and
      (not (ready-road ?v ?r))
      (ready-road ?v ?next)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-road_0093-road_0098-tw_00600
    :parameters (?v - vehicle ?r - road_type_road_0093 ?next - road_type_road_0098 ?w - window_type_tw_00600)
    :precondition (and
      (ready-road ?v ?r)
      (road-next ?r ?next)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
    )
    :effect (and
      (not (ready-road ?v ?r))
      (ready-road ?v ?next)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-road_0093-road_0099-tw_00000
    :parameters (?v - vehicle ?r - road_type_road_0093 ?next - road_type_road_0099 ?w - window_type_tw_00000)
    :precondition (and
      (ready-road ?v ?r)
      (road-next ?r ?next)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
    )
    :effect (and
      (not (ready-road ?v ?r))
      (ready-road ?v ?next)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-road_0093-road_0099-tw_00030
    :parameters (?v - vehicle ?r - road_type_road_0093 ?next - road_type_road_0099 ?w - window_type_tw_00030)
    :precondition (and
      (ready-road ?v ?r)
      (road-next ?r ?next)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
    )
    :effect (and
      (not (ready-road ?v ?r))
      (ready-road ?v ?next)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-road_0093-road_0099-tw_00060
    :parameters (?v - vehicle ?r - road_type_road_0093 ?next - road_type_road_0099 ?w - window_type_tw_00060)
    :precondition (and
      (ready-road ?v ?r)
      (road-next ?r ?next)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
    )
    :effect (and
      (not (ready-road ?v ?r))
      (ready-road ?v ?next)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-road_0093-road_0099-tw_00090
    :parameters (?v - vehicle ?r - road_type_road_0093 ?next - road_type_road_0099 ?w - window_type_tw_00090)
    :precondition (and
      (ready-road ?v ?r)
      (road-next ?r ?next)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
    )
    :effect (and
      (not (ready-road ?v ?r))
      (ready-road ?v ?next)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-road_0093-road_0099-tw_00120
    :parameters (?v - vehicle ?r - road_type_road_0093 ?next - road_type_road_0099 ?w - window_type_tw_00120)
    :precondition (and
      (ready-road ?v ?r)
      (road-next ?r ?next)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
    )
    :effect (and
      (not (ready-road ?v ?r))
      (ready-road ?v ?next)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-road_0093-road_0099-tw_00150
    :parameters (?v - vehicle ?r - road_type_road_0093 ?next - road_type_road_0099 ?w - window_type_tw_00150)
    :precondition (and
      (ready-road ?v ?r)
      (road-next ?r ?next)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
    )
    :effect (and
      (not (ready-road ?v ?r))
      (ready-road ?v ?next)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-road_0093-road_0099-tw_00180
    :parameters (?v - vehicle ?r - road_type_road_0093 ?next - road_type_road_0099 ?w - window_type_tw_00180)
    :precondition (and
      (ready-road ?v ?r)
      (road-next ?r ?next)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
    )
    :effect (and
      (not (ready-road ?v ?r))
      (ready-road ?v ?next)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-road_0093-road_0099-tw_00210
    :parameters (?v - vehicle ?r - road_type_road_0093 ?next - road_type_road_0099 ?w - window_type_tw_00210)
    :precondition (and
      (ready-road ?v ?r)
      (road-next ?r ?next)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
    )
    :effect (and
      (not (ready-road ?v ?r))
      (ready-road ?v ?next)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-road_0093-road_0099-tw_00240
    :parameters (?v - vehicle ?r - road_type_road_0093 ?next - road_type_road_0099 ?w - window_type_tw_00240)
    :precondition (and
      (ready-road ?v ?r)
      (road-next ?r ?next)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
    )
    :effect (and
      (not (ready-road ?v ?r))
      (ready-road ?v ?next)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-road_0093-road_0099-tw_00270
    :parameters (?v - vehicle ?r - road_type_road_0093 ?next - road_type_road_0099 ?w - window_type_tw_00270)
    :precondition (and
      (ready-road ?v ?r)
      (road-next ?r ?next)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
    )
    :effect (and
      (not (ready-road ?v ?r))
      (ready-road ?v ?next)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-road_0093-road_0099-tw_00300
    :parameters (?v - vehicle ?r - road_type_road_0093 ?next - road_type_road_0099 ?w - window_type_tw_00300)
    :precondition (and
      (ready-road ?v ?r)
      (road-next ?r ?next)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
    )
    :effect (and
      (not (ready-road ?v ?r))
      (ready-road ?v ?next)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-road_0093-road_0099-tw_00330
    :parameters (?v - vehicle ?r - road_type_road_0093 ?next - road_type_road_0099 ?w - window_type_tw_00330)
    :precondition (and
      (ready-road ?v ?r)
      (road-next ?r ?next)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
    )
    :effect (and
      (not (ready-road ?v ?r))
      (ready-road ?v ?next)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-road_0093-road_0099-tw_00360
    :parameters (?v - vehicle ?r - road_type_road_0093 ?next - road_type_road_0099 ?w - window_type_tw_00360)
    :precondition (and
      (ready-road ?v ?r)
      (road-next ?r ?next)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
    )
    :effect (and
      (not (ready-road ?v ?r))
      (ready-road ?v ?next)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-road_0093-road_0099-tw_00390
    :parameters (?v - vehicle ?r - road_type_road_0093 ?next - road_type_road_0099 ?w - window_type_tw_00390)
    :precondition (and
      (ready-road ?v ?r)
      (road-next ?r ?next)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
    )
    :effect (and
      (not (ready-road ?v ?r))
      (ready-road ?v ?next)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-road_0093-road_0099-tw_00420
    :parameters (?v - vehicle ?r - road_type_road_0093 ?next - road_type_road_0099 ?w - window_type_tw_00420)
    :precondition (and
      (ready-road ?v ?r)
      (road-next ?r ?next)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
    )
    :effect (and
      (not (ready-road ?v ?r))
      (ready-road ?v ?next)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-road_0093-road_0099-tw_00450
    :parameters (?v - vehicle ?r - road_type_road_0093 ?next - road_type_road_0099 ?w - window_type_tw_00450)
    :precondition (and
      (ready-road ?v ?r)
      (road-next ?r ?next)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
    )
    :effect (and
      (not (ready-road ?v ?r))
      (ready-road ?v ?next)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-road_0093-road_0099-tw_00480
    :parameters (?v - vehicle ?r - road_type_road_0093 ?next - road_type_road_0099 ?w - window_type_tw_00480)
    :precondition (and
      (ready-road ?v ?r)
      (road-next ?r ?next)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
    )
    :effect (and
      (not (ready-road ?v ?r))
      (ready-road ?v ?next)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-road_0093-road_0099-tw_00510
    :parameters (?v - vehicle ?r - road_type_road_0093 ?next - road_type_road_0099 ?w - window_type_tw_00510)
    :precondition (and
      (ready-road ?v ?r)
      (road-next ?r ?next)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
    )
    :effect (and
      (not (ready-road ?v ?r))
      (ready-road ?v ?next)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-road_0093-road_0099-tw_00540
    :parameters (?v - vehicle ?r - road_type_road_0093 ?next - road_type_road_0099 ?w - window_type_tw_00540)
    :precondition (and
      (ready-road ?v ?r)
      (road-next ?r ?next)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
    )
    :effect (and
      (not (ready-road ?v ?r))
      (ready-road ?v ?next)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-road_0093-road_0099-tw_00570
    :parameters (?v - vehicle ?r - road_type_road_0093 ?next - road_type_road_0099 ?w - window_type_tw_00570)
    :precondition (and
      (ready-road ?v ?r)
      (road-next ?r ?next)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
    )
    :effect (and
      (not (ready-road ?v ?r))
      (ready-road ?v ?next)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-road_0093-road_0099-tw_00600
    :parameters (?v - vehicle ?r - road_type_road_0093 ?next - road_type_road_0099 ?w - window_type_tw_00600)
    :precondition (and
      (ready-road ?v ?r)
      (road-next ?r ?next)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
    )
    :effect (and
      (not (ready-road ?v ?r))
      (ready-road ?v ?next)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-static-road_0095-macro_0046
    :parameters (?v - vehicle ?r - road_type_road_0095 ?next - road_type_macro_0046)
    :precondition (and
      (ready-road ?v ?r)
      (road-next ?r ?next)
      (road-open ?r)
      (static-road ?r)
      (not (road-blocked ?r))
    )
    :effect (and
      (not (ready-road ?v ?r))
      (ready-road ?v ?next)
      (increase (travel-time ?v) (travel-duration ?r))
      (increase (sim-time) (travel-duration ?r))
    )
  )

  (:action traverse-road-static-road_0095-road_0647
    :parameters (?v - vehicle ?r - road_type_road_0095 ?next - road_type_road_0647)
    :precondition (and
      (ready-road ?v ?r)
      (road-next ?r ?next)
      (road-open ?r)
      (static-road ?r)
      (not (road-blocked ?r))
    )
    :effect (and
      (not (ready-road ?v ?r))
      (ready-road ?v ?next)
      (increase (travel-time ?v) (travel-duration ?r))
      (increase (sim-time) (travel-duration ?r))
    )
  )

  (:action traverse-road-static-road_0097-road_0311
    :parameters (?v - vehicle ?r - road_type_road_0097 ?next - road_type_road_0311)
    :precondition (and
      (ready-road ?v ?r)
      (road-next ?r ?next)
      (road-open ?r)
      (static-road ?r)
      (not (road-blocked ?r))
    )
    :effect (and
      (not (ready-road ?v ?r))
      (ready-road ?v ?next)
      (increase (travel-time ?v) (travel-duration ?r))
      (increase (sim-time) (travel-duration ?r))
    )
  )

  (:action traverse-road-static-road_0097-road_0312
    :parameters (?v - vehicle ?r - road_type_road_0097 ?next - road_type_road_0312)
    :precondition (and
      (ready-road ?v ?r)
      (road-next ?r ?next)
      (road-open ?r)
      (static-road ?r)
      (not (road-blocked ?r))
    )
    :effect (and
      (not (ready-road ?v ?r))
      (ready-road ?v ?next)
      (increase (travel-time ?v) (travel-duration ?r))
      (increase (sim-time) (travel-duration ?r))
    )
  )

  (:action traverse-road-static-road_0098-road_0838
    :parameters (?v - vehicle ?r - road_type_road_0098 ?next - road_type_road_0838)
    :precondition (and
      (ready-road ?v ?r)
      (road-next ?r ?next)
      (road-open ?r)
      (static-road ?r)
      (not (road-blocked ?r))
    )
    :effect (and
      (not (ready-road ?v ?r))
      (ready-road ?v ?next)
      (increase (travel-time ?v) (travel-duration ?r))
      (increase (sim-time) (travel-duration ?r))
    )
  )

  (:action traverse-road-static-road_0098-road_0839
    :parameters (?v - vehicle ?r - road_type_road_0098 ?next - road_type_road_0839)
    :precondition (and
      (ready-road ?v ?r)
      (road-next ?r ?next)
      (road-open ?r)
      (static-road ?r)
      (not (road-blocked ?r))
    )
    :effect (and
      (not (ready-road ?v ?r))
      (ready-road ?v ?next)
      (increase (travel-time ?v) (travel-duration ?r))
      (increase (sim-time) (travel-duration ?r))
    )
  )

  (:action traverse-road-static-road_0099-road_0667
    :parameters (?v - vehicle ?r - road_type_road_0099 ?next - road_type_road_0667)
    :precondition (and
      (ready-road ?v ?r)
      (road-next ?r ?next)
      (road-open ?r)
      (static-road ?r)
      (not (road-blocked ?r))
    )
    :effect (and
      (not (ready-road ?v ?r))
      (ready-road ?v ?next)
      (increase (travel-time ?v) (travel-duration ?r))
      (increase (sim-time) (travel-duration ?r))
    )
  )

  (:action traverse-road-static-road_0099-road_0668
    :parameters (?v - vehicle ?r - road_type_road_0099 ?next - road_type_road_0668)
    :precondition (and
      (ready-road ?v ?r)
      (road-next ?r ?next)
      (road-open ?r)
      (static-road ?r)
      (not (road-blocked ?r))
    )
    :effect (and
      (not (ready-road ?v ?r))
      (ready-road ?v ?next)
      (increase (travel-time ?v) (travel-duration ?r))
      (increase (sim-time) (travel-duration ?r))
    )
  )

  (:action traverse-road-static-road_0099-road_0669
    :parameters (?v - vehicle ?r - road_type_road_0099 ?next - road_type_road_0669)
    :precondition (and
      (ready-road ?v ?r)
      (road-next ?r ?next)
      (road-open ?r)
      (static-road ?r)
      (not (road-blocked ?r))
    )
    :effect (and
      (not (ready-road ?v ?r))
      (ready-road ?v ?next)
      (increase (travel-time ?v) (travel-duration ?r))
      (increase (sim-time) (travel-duration ?r))
    )
  )

  (:action traverse-road-static-road_0100-road_0018
    :parameters (?v - vehicle ?r - road_type_road_0100 ?next - road_type_road_0018)
    :precondition (and
      (ready-road ?v ?r)
      (road-next ?r ?next)
      (road-open ?r)
      (static-road ?r)
      (not (road-blocked ?r))
    )
    :effect (and
      (not (ready-road ?v ?r))
      (ready-road ?v ?next)
      (increase (travel-time ?v) (travel-duration ?r))
      (increase (sim-time) (travel-duration ?r))
    )
  )

  (:action traverse-road-static-road_0102-road_0821
    :parameters (?v - vehicle ?r - road_type_road_0102 ?next - road_type_road_0821)
    :precondition (and
      (ready-road ?v ?r)
      (road-next ?r ?next)
      (road-open ?r)
      (static-road ?r)
      (not (road-blocked ?r))
    )
    :effect (and
      (not (ready-road ?v ?r))
      (ready-road ?v ?next)
      (increase (travel-time ?v) (travel-duration ?r))
      (increase (sim-time) (travel-duration ?r))
    )
  )

  (:action traverse-road-static-road_0102-road_0822
    :parameters (?v - vehicle ?r - road_type_road_0102 ?next - road_type_road_0822)
    :precondition (and
      (ready-road ?v ?r)
      (road-next ?r ?next)
      (road-open ?r)
      (static-road ?r)
      (not (road-blocked ?r))
    )
    :effect (and
      (not (ready-road ?v ?r))
      (ready-road ?v ?next)
      (increase (travel-time ?v) (travel-duration ?r))
      (increase (sim-time) (travel-duration ?r))
    )
  )

  (:action traverse-road-static-road_0104-road_0098
    :parameters (?v - vehicle ?r - road_type_road_0104 ?next - road_type_road_0098)
    :precondition (and
      (ready-road ?v ?r)
      (road-next ?r ?next)
      (road-open ?r)
      (static-road ?r)
      (not (road-blocked ?r))
    )
    :effect (and
      (not (ready-road ?v ?r))
      (ready-road ?v ?next)
      (increase (travel-time ?v) (travel-duration ?r))
      (increase (sim-time) (travel-duration ?r))
    )
  )

  (:action traverse-road-static-road_0104-road_0099
    :parameters (?v - vehicle ?r - road_type_road_0104 ?next - road_type_road_0099)
    :precondition (and
      (ready-road ?v ?r)
      (road-next ?r ?next)
      (road-open ?r)
      (static-road ?r)
      (not (road-blocked ?r))
    )
    :effect (and
      (not (ready-road ?v ?r))
      (ready-road ?v ?next)
      (increase (travel-time ?v) (travel-duration ?r))
      (increase (sim-time) (travel-duration ?r))
    )
  )

  (:action traverse-road-static-road_0105-road_0379
    :parameters (?v - vehicle ?r - road_type_road_0105 ?next - road_type_road_0379)
    :precondition (and
      (ready-road ?v ?r)
      (road-next ?r ?next)
      (road-open ?r)
      (static-road ?r)
      (not (road-blocked ?r))
    )
    :effect (and
      (not (ready-road ?v ?r))
      (ready-road ?v ?next)
      (increase (travel-time ?v) (travel-duration ?r))
      (increase (sim-time) (travel-duration ?r))
    )
  )

  (:action traverse-road-static-road_0105-road_0380
    :parameters (?v - vehicle ?r - road_type_road_0105 ?next - road_type_road_0380)
    :precondition (and
      (ready-road ?v ?r)
      (road-next ?r ?next)
      (road-open ?r)
      (static-road ?r)
      (not (road-blocked ?r))
    )
    :effect (and
      (not (ready-road ?v ?r))
      (ready-road ?v ?next)
      (increase (travel-time ?v) (travel-duration ?r))
      (increase (sim-time) (travel-duration ?r))
    )
  )

  (:action traverse-road-static-road_0106-road_0825
    :parameters (?v - vehicle ?r - road_type_road_0106 ?next - road_type_road_0825)
    :precondition (and
      (ready-road ?v ?r)
      (road-next ?r ?next)
      (road-open ?r)
      (static-road ?r)
      (not (road-blocked ?r))
    )
    :effect (and
      (not (ready-road ?v ?r))
      (ready-road ?v ?next)
      (increase (travel-time ?v) (travel-duration ?r))
      (increase (sim-time) (travel-duration ?r))
    )
  )

  (:action traverse-road-static-road_0106-road_0826
    :parameters (?v - vehicle ?r - road_type_road_0106 ?next - road_type_road_0826)
    :precondition (and
      (ready-road ?v ?r)
      (road-next ?r ?next)
      (road-open ?r)
      (static-road ?r)
      (not (road-blocked ?r))
    )
    :effect (and
      (not (ready-road ?v ?r))
      (ready-road ?v ?next)
      (increase (travel-time ?v) (travel-duration ?r))
      (increase (sim-time) (travel-duration ?r))
    )
  )

  (:action traverse-road-dynamic-road_0107-road_0047-tw_00000
    :parameters (?v - vehicle ?r - road_type_road_0107 ?next - road_type_road_0047 ?w - window_type_tw_00000)
    :precondition (and
      (ready-road ?v ?r)
      (road-next ?r ?next)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
    )
    :effect (and
      (not (ready-road ?v ?r))
      (ready-road ?v ?next)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-road_0107-road_0047-tw_00030
    :parameters (?v - vehicle ?r - road_type_road_0107 ?next - road_type_road_0047 ?w - window_type_tw_00030)
    :precondition (and
      (ready-road ?v ?r)
      (road-next ?r ?next)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
    )
    :effect (and
      (not (ready-road ?v ?r))
      (ready-road ?v ?next)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-road_0107-road_0047-tw_00060
    :parameters (?v - vehicle ?r - road_type_road_0107 ?next - road_type_road_0047 ?w - window_type_tw_00060)
    :precondition (and
      (ready-road ?v ?r)
      (road-next ?r ?next)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
    )
    :effect (and
      (not (ready-road ?v ?r))
      (ready-road ?v ?next)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-road_0107-road_0047-tw_00090
    :parameters (?v - vehicle ?r - road_type_road_0107 ?next - road_type_road_0047 ?w - window_type_tw_00090)
    :precondition (and
      (ready-road ?v ?r)
      (road-next ?r ?next)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
    )
    :effect (and
      (not (ready-road ?v ?r))
      (ready-road ?v ?next)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-road_0107-road_0047-tw_00120
    :parameters (?v - vehicle ?r - road_type_road_0107 ?next - road_type_road_0047 ?w - window_type_tw_00120)
    :precondition (and
      (ready-road ?v ?r)
      (road-next ?r ?next)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
    )
    :effect (and
      (not (ready-road ?v ?r))
      (ready-road ?v ?next)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-road_0107-road_0047-tw_00150
    :parameters (?v - vehicle ?r - road_type_road_0107 ?next - road_type_road_0047 ?w - window_type_tw_00150)
    :precondition (and
      (ready-road ?v ?r)
      (road-next ?r ?next)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
    )
    :effect (and
      (not (ready-road ?v ?r))
      (ready-road ?v ?next)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-road_0107-road_0047-tw_00180
    :parameters (?v - vehicle ?r - road_type_road_0107 ?next - road_type_road_0047 ?w - window_type_tw_00180)
    :precondition (and
      (ready-road ?v ?r)
      (road-next ?r ?next)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
    )
    :effect (and
      (not (ready-road ?v ?r))
      (ready-road ?v ?next)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-road_0107-road_0047-tw_00210
    :parameters (?v - vehicle ?r - road_type_road_0107 ?next - road_type_road_0047 ?w - window_type_tw_00210)
    :precondition (and
      (ready-road ?v ?r)
      (road-next ?r ?next)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
    )
    :effect (and
      (not (ready-road ?v ?r))
      (ready-road ?v ?next)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-road_0107-road_0047-tw_00240
    :parameters (?v - vehicle ?r - road_type_road_0107 ?next - road_type_road_0047 ?w - window_type_tw_00240)
    :precondition (and
      (ready-road ?v ?r)
      (road-next ?r ?next)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
    )
    :effect (and
      (not (ready-road ?v ?r))
      (ready-road ?v ?next)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-road_0107-road_0047-tw_00270
    :parameters (?v - vehicle ?r - road_type_road_0107 ?next - road_type_road_0047 ?w - window_type_tw_00270)
    :precondition (and
      (ready-road ?v ?r)
      (road-next ?r ?next)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
    )
    :effect (and
      (not (ready-road ?v ?r))
      (ready-road ?v ?next)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-road_0107-road_0047-tw_00300
    :parameters (?v - vehicle ?r - road_type_road_0107 ?next - road_type_road_0047 ?w - window_type_tw_00300)
    :precondition (and
      (ready-road ?v ?r)
      (road-next ?r ?next)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
    )
    :effect (and
      (not (ready-road ?v ?r))
      (ready-road ?v ?next)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-road_0107-road_0047-tw_00330
    :parameters (?v - vehicle ?r - road_type_road_0107 ?next - road_type_road_0047 ?w - window_type_tw_00330)
    :precondition (and
      (ready-road ?v ?r)
      (road-next ?r ?next)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
    )
    :effect (and
      (not (ready-road ?v ?r))
      (ready-road ?v ?next)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-road_0107-road_0047-tw_00360
    :parameters (?v - vehicle ?r - road_type_road_0107 ?next - road_type_road_0047 ?w - window_type_tw_00360)
    :precondition (and
      (ready-road ?v ?r)
      (road-next ?r ?next)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
    )
    :effect (and
      (not (ready-road ?v ?r))
      (ready-road ?v ?next)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-road_0107-road_0047-tw_00390
    :parameters (?v - vehicle ?r - road_type_road_0107 ?next - road_type_road_0047 ?w - window_type_tw_00390)
    :precondition (and
      (ready-road ?v ?r)
      (road-next ?r ?next)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
    )
    :effect (and
      (not (ready-road ?v ?r))
      (ready-road ?v ?next)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-road_0107-road_0047-tw_00420
    :parameters (?v - vehicle ?r - road_type_road_0107 ?next - road_type_road_0047 ?w - window_type_tw_00420)
    :precondition (and
      (ready-road ?v ?r)
      (road-next ?r ?next)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
    )
    :effect (and
      (not (ready-road ?v ?r))
      (ready-road ?v ?next)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-road_0107-road_0047-tw_00450
    :parameters (?v - vehicle ?r - road_type_road_0107 ?next - road_type_road_0047 ?w - window_type_tw_00450)
    :precondition (and
      (ready-road ?v ?r)
      (road-next ?r ?next)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
    )
    :effect (and
      (not (ready-road ?v ?r))
      (ready-road ?v ?next)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-road_0107-road_0047-tw_00480
    :parameters (?v - vehicle ?r - road_type_road_0107 ?next - road_type_road_0047 ?w - window_type_tw_00480)
    :precondition (and
      (ready-road ?v ?r)
      (road-next ?r ?next)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
    )
    :effect (and
      (not (ready-road ?v ?r))
      (ready-road ?v ?next)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-road_0107-road_0047-tw_00510
    :parameters (?v - vehicle ?r - road_type_road_0107 ?next - road_type_road_0047 ?w - window_type_tw_00510)
    :precondition (and
      (ready-road ?v ?r)
      (road-next ?r ?next)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
    )
    :effect (and
      (not (ready-road ?v ?r))
      (ready-road ?v ?next)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-road_0107-road_0047-tw_00540
    :parameters (?v - vehicle ?r - road_type_road_0107 ?next - road_type_road_0047 ?w - window_type_tw_00540)
    :precondition (and
      (ready-road ?v ?r)
      (road-next ?r ?next)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
    )
    :effect (and
      (not (ready-road ?v ?r))
      (ready-road ?v ?next)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-road_0107-road_0047-tw_00570
    :parameters (?v - vehicle ?r - road_type_road_0107 ?next - road_type_road_0047 ?w - window_type_tw_00570)
    :precondition (and
      (ready-road ?v ?r)
      (road-next ?r ?next)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
    )
    :effect (and
      (not (ready-road ?v ?r))
      (ready-road ?v ?next)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-road_0107-road_0047-tw_00600
    :parameters (?v - vehicle ?r - road_type_road_0107 ?next - road_type_road_0047 ?w - window_type_tw_00600)
    :precondition (and
      (ready-road ?v ?r)
      (road-next ?r ?next)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
    )
    :effect (and
      (not (ready-road ?v ?r))
      (ready-road ?v ?next)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-static-road_0108-road_0723
    :parameters (?v - vehicle ?r - road_type_road_0108 ?next - road_type_road_0723)
    :precondition (and
      (ready-road ?v ?r)
      (road-next ?r ?next)
      (road-open ?r)
      (static-road ?r)
      (not (road-blocked ?r))
    )
    :effect (and
      (not (ready-road ?v ?r))
      (ready-road ?v ?next)
      (increase (travel-time ?v) (travel-duration ?r))
      (increase (sim-time) (travel-duration ?r))
    )
  )

  (:action traverse-road-static-road_0108-road_0724
    :parameters (?v - vehicle ?r - road_type_road_0108 ?next - road_type_road_0724)
    :precondition (and
      (ready-road ?v ?r)
      (road-next ?r ?next)
      (road-open ?r)
      (static-road ?r)
      (not (road-blocked ?r))
    )
    :effect (and
      (not (ready-road ?v ?r))
      (ready-road ?v ?next)
      (increase (travel-time ?v) (travel-duration ?r))
      (increase (sim-time) (travel-duration ?r))
    )
  )

  (:action traverse-road-static-road_0109-road_0610
    :parameters (?v - vehicle ?r - road_type_road_0109 ?next - road_type_road_0610)
    :precondition (and
      (ready-road ?v ?r)
      (road-next ?r ?next)
      (road-open ?r)
      (static-road ?r)
      (not (road-blocked ?r))
    )
    :effect (and
      (not (ready-road ?v ?r))
      (ready-road ?v ?next)
      (increase (travel-time ?v) (travel-duration ?r))
      (increase (sim-time) (travel-duration ?r))
    )
  )

  (:action traverse-road-static-road_0110-road_0725
    :parameters (?v - vehicle ?r - road_type_road_0110 ?next - road_type_road_0725)
    :precondition (and
      (ready-road ?v ?r)
      (road-next ?r ?next)
      (road-open ?r)
      (static-road ?r)
      (not (road-blocked ?r))
    )
    :effect (and
      (not (ready-road ?v ?r))
      (ready-road ?v ?next)
      (increase (travel-time ?v) (travel-duration ?r))
      (increase (sim-time) (travel-duration ?r))
    )
  )

  (:action traverse-road-static-road_0110-road_0726
    :parameters (?v - vehicle ?r - road_type_road_0110 ?next - road_type_road_0726)
    :precondition (and
      (ready-road ?v ?r)
      (road-next ?r ?next)
      (road-open ?r)
      (static-road ?r)
      (not (road-blocked ?r))
    )
    :effect (and
      (not (ready-road ?v ?r))
      (ready-road ?v ?next)
      (increase (travel-time ?v) (travel-duration ?r))
      (increase (sim-time) (travel-duration ?r))
    )
  )

  (:action traverse-road-static-road_0110-road_0727
    :parameters (?v - vehicle ?r - road_type_road_0110 ?next - road_type_road_0727)
    :precondition (and
      (ready-road ?v ?r)
      (road-next ?r ?next)
      (road-open ?r)
      (static-road ?r)
      (not (road-blocked ?r))
    )
    :effect (and
      (not (ready-road ?v ?r))
      (ready-road ?v ?next)
      (increase (travel-time ?v) (travel-duration ?r))
      (increase (sim-time) (travel-duration ?r))
    )
  )

  (:action traverse-road-static-road_0111-macro_0036
    :parameters (?v - vehicle ?r - road_type_road_0111 ?next - road_type_macro_0036)
    :precondition (and
      (ready-road ?v ?r)
      (road-next ?r ?next)
      (road-open ?r)
      (static-road ?r)
      (not (road-blocked ?r))
    )
    :effect (and
      (not (ready-road ?v ?r))
      (ready-road ?v ?next)
      (increase (travel-time ?v) (travel-duration ?r))
      (increase (sim-time) (travel-duration ?r))
    )
  )

  (:action traverse-road-static-road_0111-road_0507
    :parameters (?v - vehicle ?r - road_type_road_0111 ?next - road_type_road_0507)
    :precondition (and
      (ready-road ?v ?r)
      (road-next ?r ?next)
      (road-open ?r)
      (static-road ?r)
      (not (road-blocked ?r))
    )
    :effect (and
      (not (ready-road ?v ?r))
      (ready-road ?v ?next)
      (increase (travel-time ?v) (travel-duration ?r))
      (increase (sim-time) (travel-duration ?r))
    )
  )

  (:action traverse-road-static-road_0111-road_0508
    :parameters (?v - vehicle ?r - road_type_road_0111 ?next - road_type_road_0508)
    :precondition (and
      (ready-road ?v ?r)
      (road-next ?r ?next)
      (road-open ?r)
      (static-road ?r)
      (not (road-blocked ?r))
    )
    :effect (and
      (not (ready-road ?v ?r))
      (ready-road ?v ?next)
      (increase (travel-time ?v) (travel-duration ?r))
      (increase (sim-time) (travel-duration ?r))
    )
  )

  (:action traverse-road-static-road_0114-road_0144
    :parameters (?v - vehicle ?r - road_type_road_0114 ?next - road_type_road_0144)
    :precondition (and
      (ready-road ?v ?r)
      (road-next ?r ?next)
      (road-open ?r)
      (static-road ?r)
      (not (road-blocked ?r))
    )
    :effect (and
      (not (ready-road ?v ?r))
      (ready-road ?v ?next)
      (increase (travel-time ?v) (travel-duration ?r))
      (increase (sim-time) (travel-duration ?r))
    )
  )

  (:action traverse-road-static-road_0115-road_0836
    :parameters (?v - vehicle ?r - road_type_road_0115 ?next - road_type_road_0836)
    :precondition (and
      (ready-road ?v ?r)
      (road-next ?r ?next)
      (road-open ?r)
      (static-road ?r)
      (not (road-blocked ?r))
    )
    :effect (and
      (not (ready-road ?v ?r))
      (ready-road ?v ?next)
      (increase (travel-time ?v) (travel-duration ?r))
      (increase (sim-time) (travel-duration ?r))
    )
  )

  (:action traverse-road-static-road_0116-macro_0010
    :parameters (?v - vehicle ?r - road_type_road_0116 ?next - road_type_macro_0010)
    :precondition (and
      (ready-road ?v ?r)
      (road-next ?r ?next)
      (road-open ?r)
      (static-road ?r)
      (not (road-blocked ?r))
    )
    :effect (and
      (not (ready-road ?v ?r))
      (ready-road ?v ?next)
      (increase (travel-time ?v) (travel-duration ?r))
      (increase (sim-time) (travel-duration ?r))
    )
  )

  (:action traverse-road-static-road_0116-road_0102
    :parameters (?v - vehicle ?r - road_type_road_0116 ?next - road_type_road_0102)
    :precondition (and
      (ready-road ?v ?r)
      (road-next ?r ?next)
      (road-open ?r)
      (static-road ?r)
      (not (road-blocked ?r))
    )
    :effect (and
      (not (ready-road ?v ?r))
      (ready-road ?v ?next)
      (increase (travel-time ?v) (travel-duration ?r))
      (increase (sim-time) (travel-duration ?r))
    )
  )

  (:action traverse-road-static-road_0116-road_0104
    :parameters (?v - vehicle ?r - road_type_road_0116 ?next - road_type_road_0104)
    :precondition (and
      (ready-road ?v ?r)
      (road-next ?r ?next)
      (road-open ?r)
      (static-road ?r)
      (not (road-blocked ?r))
    )
    :effect (and
      (not (ready-road ?v ?r))
      (ready-road ?v ?next)
      (increase (travel-time ?v) (travel-duration ?r))
      (increase (sim-time) (travel-duration ?r))
    )
  )

  (:action traverse-road-static-road_0117-road_0172
    :parameters (?v - vehicle ?r - road_type_road_0117 ?next - road_type_road_0172)
    :precondition (and
      (ready-road ?v ?r)
      (road-next ?r ?next)
      (road-open ?r)
      (static-road ?r)
      (not (road-blocked ?r))
    )
    :effect (and
      (not (ready-road ?v ?r))
      (ready-road ?v ?next)
      (increase (travel-time ?v) (travel-duration ?r))
      (increase (sim-time) (travel-duration ?r))
    )
  )

  (:action traverse-road-static-road_0117-road_0173
    :parameters (?v - vehicle ?r - road_type_road_0117 ?next - road_type_road_0173)
    :precondition (and
      (ready-road ?v ?r)
      (road-next ?r ?next)
      (road-open ?r)
      (static-road ?r)
      (not (road-blocked ?r))
    )
    :effect (and
      (not (ready-road ?v ?r))
      (ready-road ?v ?next)
      (increase (travel-time ?v) (travel-duration ?r))
      (increase (sim-time) (travel-duration ?r))
    )
  )

  (:action traverse-road-static-road_0118-road_0295
    :parameters (?v - vehicle ?r - road_type_road_0118 ?next - road_type_road_0295)
    :precondition (and
      (ready-road ?v ?r)
      (road-next ?r ?next)
      (road-open ?r)
      (static-road ?r)
      (not (road-blocked ?r))
    )
    :effect (and
      (not (ready-road ?v ?r))
      (ready-road ?v ?next)
      (increase (travel-time ?v) (travel-duration ?r))
      (increase (sim-time) (travel-duration ?r))
    )
  )

  (:action traverse-road-static-road_0118-road_0296
    :parameters (?v - vehicle ?r - road_type_road_0118 ?next - road_type_road_0296)
    :precondition (and
      (ready-road ?v ?r)
      (road-next ?r ?next)
      (road-open ?r)
      (static-road ?r)
      (not (road-blocked ?r))
    )
    :effect (and
      (not (ready-road ?v ?r))
      (ready-road ?v ?next)
      (increase (travel-time ?v) (travel-duration ?r))
      (increase (sim-time) (travel-duration ?r))
    )
  )

  (:action traverse-road-static-road_0119-road_0211
    :parameters (?v - vehicle ?r - road_type_road_0119 ?next - road_type_road_0211)
    :precondition (and
      (ready-road ?v ?r)
      (road-next ?r ?next)
      (road-open ?r)
      (static-road ?r)
      (not (road-blocked ?r))
    )
    :effect (and
      (not (ready-road ?v ?r))
      (ready-road ?v ?next)
      (increase (travel-time ?v) (travel-duration ?r))
      (increase (sim-time) (travel-duration ?r))
    )
  )

  (:action traverse-road-static-road_0119-road_0212
    :parameters (?v - vehicle ?r - road_type_road_0119 ?next - road_type_road_0212)
    :precondition (and
      (ready-road ?v ?r)
      (road-next ?r ?next)
      (road-open ?r)
      (static-road ?r)
      (not (road-blocked ?r))
    )
    :effect (and
      (not (ready-road ?v ?r))
      (ready-road ?v ?next)
      (increase (travel-time ?v) (travel-duration ?r))
      (increase (sim-time) (travel-duration ?r))
    )
  )

  (:action traverse-road-static-road_0120-road_0740
    :parameters (?v - vehicle ?r - road_type_road_0120 ?next - road_type_road_0740)
    :precondition (and
      (ready-road ?v ?r)
      (road-next ?r ?next)
      (road-open ?r)
      (static-road ?r)
      (not (road-blocked ?r))
    )
    :effect (and
      (not (ready-road ?v ?r))
      (ready-road ?v ?next)
      (increase (travel-time ?v) (travel-duration ?r))
      (increase (sim-time) (travel-duration ?r))
    )
  )

  (:action traverse-road-static-road_0120-road_0741
    :parameters (?v - vehicle ?r - road_type_road_0120 ?next - road_type_road_0741)
    :precondition (and
      (ready-road ?v ?r)
      (road-next ?r ?next)
      (road-open ?r)
      (static-road ?r)
      (not (road-blocked ?r))
    )
    :effect (and
      (not (ready-road ?v ?r))
      (ready-road ?v ?next)
      (increase (travel-time ?v) (travel-duration ?r))
      (increase (sim-time) (travel-duration ?r))
    )
  )

  (:action traverse-road-static-road_0121-road_0740
    :parameters (?v - vehicle ?r - road_type_road_0121 ?next - road_type_road_0740)
    :precondition (and
      (ready-road ?v ?r)
      (road-next ?r ?next)
      (road-open ?r)
      (static-road ?r)
      (not (road-blocked ?r))
    )
    :effect (and
      (not (ready-road ?v ?r))
      (ready-road ?v ?next)
      (increase (travel-time ?v) (travel-duration ?r))
      (increase (sim-time) (travel-duration ?r))
    )
  )

  (:action traverse-road-static-road_0121-road_0741
    :parameters (?v - vehicle ?r - road_type_road_0121 ?next - road_type_road_0741)
    :precondition (and
      (ready-road ?v ?r)
      (road-next ?r ?next)
      (road-open ?r)
      (static-road ?r)
      (not (road-blocked ?r))
    )
    :effect (and
      (not (ready-road ?v ?r))
      (ready-road ?v ?next)
      (increase (travel-time ?v) (travel-duration ?r))
      (increase (sim-time) (travel-duration ?r))
    )
  )

  (:action traverse-road-static-road_0122-road_0776
    :parameters (?v - vehicle ?r - road_type_road_0122 ?next - road_type_road_0776)
    :precondition (and
      (ready-road ?v ?r)
      (road-next ?r ?next)
      (road-open ?r)
      (static-road ?r)
      (not (road-blocked ?r))
    )
    :effect (and
      (not (ready-road ?v ?r))
      (ready-road ?v ?next)
      (increase (travel-time ?v) (travel-duration ?r))
      (increase (sim-time) (travel-duration ?r))
    )
  )

  (:action traverse-road-static-road_0122-road_0777
    :parameters (?v - vehicle ?r - road_type_road_0122 ?next - road_type_road_0777)
    :precondition (and
      (ready-road ?v ?r)
      (road-next ?r ?next)
      (road-open ?r)
      (static-road ?r)
      (not (road-blocked ?r))
    )
    :effect (and
      (not (ready-road ?v ?r))
      (ready-road ?v ?next)
      (increase (travel-time ?v) (travel-duration ?r))
      (increase (sim-time) (travel-duration ?r))
    )
  )

  (:action traverse-road-static-road_0122-road_0778
    :parameters (?v - vehicle ?r - road_type_road_0122 ?next - road_type_road_0778)
    :precondition (and
      (ready-road ?v ?r)
      (road-next ?r ?next)
      (road-open ?r)
      (static-road ?r)
      (not (road-blocked ?r))
    )
    :effect (and
      (not (ready-road ?v ?r))
      (ready-road ?v ?next)
      (increase (travel-time ?v) (travel-duration ?r))
      (increase (sim-time) (travel-duration ?r))
    )
  )

  (:action traverse-road-static-road_0123-road_0131
    :parameters (?v - vehicle ?r - road_type_road_0123 ?next - road_type_road_0131)
    :precondition (and
      (ready-road ?v ?r)
      (road-next ?r ?next)
      (road-open ?r)
      (static-road ?r)
      (not (road-blocked ?r))
    )
    :effect (and
      (not (ready-road ?v ?r))
      (ready-road ?v ?next)
      (increase (travel-time ?v) (travel-duration ?r))
      (increase (sim-time) (travel-duration ?r))
    )
  )

  (:action traverse-road-static-road_0123-road_0132
    :parameters (?v - vehicle ?r - road_type_road_0123 ?next - road_type_road_0132)
    :precondition (and
      (ready-road ?v ?r)
      (road-next ?r ?next)
      (road-open ?r)
      (static-road ?r)
      (not (road-blocked ?r))
    )
    :effect (and
      (not (ready-road ?v ?r))
      (ready-road ?v ?next)
      (increase (travel-time ?v) (travel-duration ?r))
      (increase (sim-time) (travel-duration ?r))
    )
  )

  (:action traverse-road-static-road_0123-road_0133
    :parameters (?v - vehicle ?r - road_type_road_0123 ?next - road_type_road_0133)
    :precondition (and
      (ready-road ?v ?r)
      (road-next ?r ?next)
      (road-open ?r)
      (static-road ?r)
      (not (road-blocked ?r))
    )
    :effect (and
      (not (ready-road ?v ?r))
      (ready-road ?v ?next)
      (increase (travel-time ?v) (travel-duration ?r))
      (increase (sim-time) (travel-duration ?r))
    )
  )

  (:action traverse-road-static-road_0123-road_0134
    :parameters (?v - vehicle ?r - road_type_road_0123 ?next - road_type_road_0134)
    :precondition (and
      (ready-road ?v ?r)
      (road-next ?r ?next)
      (road-open ?r)
      (static-road ?r)
      (not (road-blocked ?r))
    )
    :effect (and
      (not (ready-road ?v ?r))
      (ready-road ?v ?next)
      (increase (travel-time ?v) (travel-duration ?r))
      (increase (sim-time) (travel-duration ?r))
    )
  )

  (:action traverse-road-static-road_0124-road_0407
    :parameters (?v - vehicle ?r - road_type_road_0124 ?next - road_type_road_0407)
    :precondition (and
      (ready-road ?v ?r)
      (road-next ?r ?next)
      (road-open ?r)
      (static-road ?r)
      (not (road-blocked ?r))
    )
    :effect (and
      (not (ready-road ?v ?r))
      (ready-road ?v ?next)
      (increase (travel-time ?v) (travel-duration ?r))
      (increase (sim-time) (travel-duration ?r))
    )
  )

  (:action traverse-road-static-road_0124-road_0408
    :parameters (?v - vehicle ?r - road_type_road_0124 ?next - road_type_road_0408)
    :precondition (and
      (ready-road ?v ?r)
      (road-next ?r ?next)
      (road-open ?r)
      (static-road ?r)
      (not (road-blocked ?r))
    )
    :effect (and
      (not (ready-road ?v ?r))
      (ready-road ?v ?next)
      (increase (travel-time ?v) (travel-duration ?r))
      (increase (sim-time) (travel-duration ?r))
    )
  )

  (:action traverse-road-static-road_0125-road_0658
    :parameters (?v - vehicle ?r - road_type_road_0125 ?next - road_type_road_0658)
    :precondition (and
      (ready-road ?v ?r)
      (road-next ?r ?next)
      (road-open ?r)
      (static-road ?r)
      (not (road-blocked ?r))
    )
    :effect (and
      (not (ready-road ?v ?r))
      (ready-road ?v ?next)
      (increase (travel-time ?v) (travel-duration ?r))
      (increase (sim-time) (travel-duration ?r))
    )
  )

  (:action traverse-road-static-road_0125-road_0659
    :parameters (?v - vehicle ?r - road_type_road_0125 ?next - road_type_road_0659)
    :precondition (and
      (ready-road ?v ?r)
      (road-next ?r ?next)
      (road-open ?r)
      (static-road ?r)
      (not (road-blocked ?r))
    )
    :effect (and
      (not (ready-road ?v ?r))
      (ready-road ?v ?next)
      (increase (travel-time ?v) (travel-duration ?r))
      (increase (sim-time) (travel-duration ?r))
    )
  )

  (:action traverse-road-static-road_0126-road_0433
    :parameters (?v - vehicle ?r - road_type_road_0126 ?next - road_type_road_0433)
    :precondition (and
      (ready-road ?v ?r)
      (road-next ?r ?next)
      (road-open ?r)
      (static-road ?r)
      (not (road-blocked ?r))
    )
    :effect (and
      (not (ready-road ?v ?r))
      (ready-road ?v ?next)
      (increase (travel-time ?v) (travel-duration ?r))
      (increase (sim-time) (travel-duration ?r))
    )
  )

  (:action traverse-road-static-road_0127-road_0291
    :parameters (?v - vehicle ?r - road_type_road_0127 ?next - road_type_road_0291)
    :precondition (and
      (ready-road ?v ?r)
      (road-next ?r ?next)
      (road-open ?r)
      (static-road ?r)
      (not (road-blocked ?r))
    )
    :effect (and
      (not (ready-road ?v ?r))
      (ready-road ?v ?next)
      (increase (travel-time ?v) (travel-duration ?r))
      (increase (sim-time) (travel-duration ?r))
    )
  )

  (:action traverse-road-static-road_0127-road_0292
    :parameters (?v - vehicle ?r - road_type_road_0127 ?next - road_type_road_0292)
    :precondition (and
      (ready-road ?v ?r)
      (road-next ?r ?next)
      (road-open ?r)
      (static-road ?r)
      (not (road-blocked ?r))
    )
    :effect (and
      (not (ready-road ?v ?r))
      (ready-road ?v ?next)
      (increase (travel-time ?v) (travel-duration ?r))
      (increase (sim-time) (travel-duration ?r))
    )
  )

  (:action traverse-road-static-road_0128-macro_0012
    :parameters (?v - vehicle ?r - road_type_road_0128 ?next - road_type_macro_0012)
    :precondition (and
      (ready-road ?v ?r)
      (road-next ?r ?next)
      (road-open ?r)
      (static-road ?r)
      (not (road-blocked ?r))
    )
    :effect (and
      (not (ready-road ?v ?r))
      (ready-road ?v ?next)
      (increase (travel-time ?v) (travel-duration ?r))
      (increase (sim-time) (travel-duration ?r))
    )
  )

  (:action traverse-road-static-road_0128-road_0129
    :parameters (?v - vehicle ?r - road_type_road_0128 ?next - road_type_road_0129)
    :precondition (and
      (ready-road ?v ?r)
      (road-next ?r ?next)
      (road-open ?r)
      (static-road ?r)
      (not (road-blocked ?r))
    )
    :effect (and
      (not (ready-road ?v ?r))
      (ready-road ?v ?next)
      (increase (travel-time ?v) (travel-duration ?r))
      (increase (sim-time) (travel-duration ?r))
    )
  )

  (:action traverse-road-static-road_0129-road_0127
    :parameters (?v - vehicle ?r - road_type_road_0129 ?next - road_type_road_0127)
    :precondition (and
      (ready-road ?v ?r)
      (road-next ?r ?next)
      (road-open ?r)
      (static-road ?r)
      (not (road-blocked ?r))
    )
    :effect (and
      (not (ready-road ?v ?r))
      (ready-road ?v ?next)
      (increase (travel-time ?v) (travel-duration ?r))
      (increase (sim-time) (travel-duration ?r))
    )
  )

  (:action traverse-road-static-road_0129-road_0128
    :parameters (?v - vehicle ?r - road_type_road_0129 ?next - road_type_road_0128)
    :precondition (and
      (ready-road ?v ?r)
      (road-next ?r ?next)
      (road-open ?r)
      (static-road ?r)
      (not (road-blocked ?r))
    )
    :effect (and
      (not (ready-road ?v ?r))
      (ready-road ?v ?next)
      (increase (travel-time ?v) (travel-duration ?r))
      (increase (sim-time) (travel-duration ?r))
    )
  )

  (:action traverse-road-static-road_0131-road_0405
    :parameters (?v - vehicle ?r - road_type_road_0131 ?next - road_type_road_0405)
    :precondition (and
      (ready-road ?v ?r)
      (road-next ?r ?next)
      (road-open ?r)
      (static-road ?r)
      (not (road-blocked ?r))
    )
    :effect (and
      (not (ready-road ?v ?r))
      (ready-road ?v ?next)
      (increase (travel-time ?v) (travel-duration ?r))
      (increase (sim-time) (travel-duration ?r))
    )
  )

  (:action traverse-road-static-road_0131-road_0406
    :parameters (?v - vehicle ?r - road_type_road_0131 ?next - road_type_road_0406)
    :precondition (and
      (ready-road ?v ?r)
      (road-next ?r ?next)
      (road-open ?r)
      (static-road ?r)
      (not (road-blocked ?r))
    )
    :effect (and
      (not (ready-road ?v ?r))
      (ready-road ?v ?next)
      (increase (travel-time ?v) (travel-duration ?r))
      (increase (sim-time) (travel-duration ?r))
    )
  )

  (:action traverse-road-static-road_0132-road_0626
    :parameters (?v - vehicle ?r - road_type_road_0132 ?next - road_type_road_0626)
    :precondition (and
      (ready-road ?v ?r)
      (road-next ?r ?next)
      (road-open ?r)
      (static-road ?r)
      (not (road-blocked ?r))
    )
    :effect (and
      (not (ready-road ?v ?r))
      (ready-road ?v ?next)
      (increase (travel-time ?v) (travel-duration ?r))
      (increase (sim-time) (travel-duration ?r))
    )
  )

  (:action traverse-road-dynamic-road_0133-road_0145-tw_00000
    :parameters (?v - vehicle ?r - road_type_road_0133 ?next - road_type_road_0145 ?w - window_type_tw_00000)
    :precondition (and
      (ready-road ?v ?r)
      (road-next ?r ?next)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
    )
    :effect (and
      (not (ready-road ?v ?r))
      (ready-road ?v ?next)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-road_0133-road_0145-tw_00030
    :parameters (?v - vehicle ?r - road_type_road_0133 ?next - road_type_road_0145 ?w - window_type_tw_00030)
    :precondition (and
      (ready-road ?v ?r)
      (road-next ?r ?next)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
    )
    :effect (and
      (not (ready-road ?v ?r))
      (ready-road ?v ?next)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-road_0133-road_0145-tw_00060
    :parameters (?v - vehicle ?r - road_type_road_0133 ?next - road_type_road_0145 ?w - window_type_tw_00060)
    :precondition (and
      (ready-road ?v ?r)
      (road-next ?r ?next)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
    )
    :effect (and
      (not (ready-road ?v ?r))
      (ready-road ?v ?next)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-road_0133-road_0145-tw_00090
    :parameters (?v - vehicle ?r - road_type_road_0133 ?next - road_type_road_0145 ?w - window_type_tw_00090)
    :precondition (and
      (ready-road ?v ?r)
      (road-next ?r ?next)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
    )
    :effect (and
      (not (ready-road ?v ?r))
      (ready-road ?v ?next)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-road_0133-road_0145-tw_00120
    :parameters (?v - vehicle ?r - road_type_road_0133 ?next - road_type_road_0145 ?w - window_type_tw_00120)
    :precondition (and
      (ready-road ?v ?r)
      (road-next ?r ?next)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
    )
    :effect (and
      (not (ready-road ?v ?r))
      (ready-road ?v ?next)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-road_0133-road_0145-tw_00150
    :parameters (?v - vehicle ?r - road_type_road_0133 ?next - road_type_road_0145 ?w - window_type_tw_00150)
    :precondition (and
      (ready-road ?v ?r)
      (road-next ?r ?next)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
    )
    :effect (and
      (not (ready-road ?v ?r))
      (ready-road ?v ?next)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-road_0133-road_0145-tw_00180
    :parameters (?v - vehicle ?r - road_type_road_0133 ?next - road_type_road_0145 ?w - window_type_tw_00180)
    :precondition (and
      (ready-road ?v ?r)
      (road-next ?r ?next)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
    )
    :effect (and
      (not (ready-road ?v ?r))
      (ready-road ?v ?next)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-road_0133-road_0145-tw_00210
    :parameters (?v - vehicle ?r - road_type_road_0133 ?next - road_type_road_0145 ?w - window_type_tw_00210)
    :precondition (and
      (ready-road ?v ?r)
      (road-next ?r ?next)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
    )
    :effect (and
      (not (ready-road ?v ?r))
      (ready-road ?v ?next)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-road_0133-road_0145-tw_00240
    :parameters (?v - vehicle ?r - road_type_road_0133 ?next - road_type_road_0145 ?w - window_type_tw_00240)
    :precondition (and
      (ready-road ?v ?r)
      (road-next ?r ?next)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
    )
    :effect (and
      (not (ready-road ?v ?r))
      (ready-road ?v ?next)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-road_0133-road_0145-tw_00270
    :parameters (?v - vehicle ?r - road_type_road_0133 ?next - road_type_road_0145 ?w - window_type_tw_00270)
    :precondition (and
      (ready-road ?v ?r)
      (road-next ?r ?next)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
    )
    :effect (and
      (not (ready-road ?v ?r))
      (ready-road ?v ?next)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-road_0133-road_0145-tw_00300
    :parameters (?v - vehicle ?r - road_type_road_0133 ?next - road_type_road_0145 ?w - window_type_tw_00300)
    :precondition (and
      (ready-road ?v ?r)
      (road-next ?r ?next)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
    )
    :effect (and
      (not (ready-road ?v ?r))
      (ready-road ?v ?next)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-road_0133-road_0145-tw_00330
    :parameters (?v - vehicle ?r - road_type_road_0133 ?next - road_type_road_0145 ?w - window_type_tw_00330)
    :precondition (and
      (ready-road ?v ?r)
      (road-next ?r ?next)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
    )
    :effect (and
      (not (ready-road ?v ?r))
      (ready-road ?v ?next)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-road_0133-road_0145-tw_00360
    :parameters (?v - vehicle ?r - road_type_road_0133 ?next - road_type_road_0145 ?w - window_type_tw_00360)
    :precondition (and
      (ready-road ?v ?r)
      (road-next ?r ?next)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
    )
    :effect (and
      (not (ready-road ?v ?r))
      (ready-road ?v ?next)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-road_0133-road_0145-tw_00390
    :parameters (?v - vehicle ?r - road_type_road_0133 ?next - road_type_road_0145 ?w - window_type_tw_00390)
    :precondition (and
      (ready-road ?v ?r)
      (road-next ?r ?next)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
    )
    :effect (and
      (not (ready-road ?v ?r))
      (ready-road ?v ?next)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-road_0133-road_0145-tw_00420
    :parameters (?v - vehicle ?r - road_type_road_0133 ?next - road_type_road_0145 ?w - window_type_tw_00420)
    :precondition (and
      (ready-road ?v ?r)
      (road-next ?r ?next)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
    )
    :effect (and
      (not (ready-road ?v ?r))
      (ready-road ?v ?next)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-road_0133-road_0145-tw_00450
    :parameters (?v - vehicle ?r - road_type_road_0133 ?next - road_type_road_0145 ?w - window_type_tw_00450)
    :precondition (and
      (ready-road ?v ?r)
      (road-next ?r ?next)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
    )
    :effect (and
      (not (ready-road ?v ?r))
      (ready-road ?v ?next)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-road_0133-road_0145-tw_00480
    :parameters (?v - vehicle ?r - road_type_road_0133 ?next - road_type_road_0145 ?w - window_type_tw_00480)
    :precondition (and
      (ready-road ?v ?r)
      (road-next ?r ?next)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
    )
    :effect (and
      (not (ready-road ?v ?r))
      (ready-road ?v ?next)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-road_0133-road_0145-tw_00510
    :parameters (?v - vehicle ?r - road_type_road_0133 ?next - road_type_road_0145 ?w - window_type_tw_00510)
    :precondition (and
      (ready-road ?v ?r)
      (road-next ?r ?next)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
    )
    :effect (and
      (not (ready-road ?v ?r))
      (ready-road ?v ?next)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-road_0133-road_0145-tw_00540
    :parameters (?v - vehicle ?r - road_type_road_0133 ?next - road_type_road_0145 ?w - window_type_tw_00540)
    :precondition (and
      (ready-road ?v ?r)
      (road-next ?r ?next)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
    )
    :effect (and
      (not (ready-road ?v ?r))
      (ready-road ?v ?next)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-road_0133-road_0145-tw_00570
    :parameters (?v - vehicle ?r - road_type_road_0133 ?next - road_type_road_0145 ?w - window_type_tw_00570)
    :precondition (and
      (ready-road ?v ?r)
      (road-next ?r ?next)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
    )
    :effect (and
      (not (ready-road ?v ?r))
      (ready-road ?v ?next)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-road_0133-road_0145-tw_00600
    :parameters (?v - vehicle ?r - road_type_road_0133 ?next - road_type_road_0145 ?w - window_type_tw_00600)
    :precondition (and
      (ready-road ?v ?r)
      (road-next ?r ?next)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
    )
    :effect (and
      (not (ready-road ?v ?r))
      (ready-road ?v ?next)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-road_0133-road_0146-tw_00000
    :parameters (?v - vehicle ?r - road_type_road_0133 ?next - road_type_road_0146 ?w - window_type_tw_00000)
    :precondition (and
      (ready-road ?v ?r)
      (road-next ?r ?next)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
    )
    :effect (and
      (not (ready-road ?v ?r))
      (ready-road ?v ?next)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-road_0133-road_0146-tw_00030
    :parameters (?v - vehicle ?r - road_type_road_0133 ?next - road_type_road_0146 ?w - window_type_tw_00030)
    :precondition (and
      (ready-road ?v ?r)
      (road-next ?r ?next)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
    )
    :effect (and
      (not (ready-road ?v ?r))
      (ready-road ?v ?next)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-road_0133-road_0146-tw_00060
    :parameters (?v - vehicle ?r - road_type_road_0133 ?next - road_type_road_0146 ?w - window_type_tw_00060)
    :precondition (and
      (ready-road ?v ?r)
      (road-next ?r ?next)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
    )
    :effect (and
      (not (ready-road ?v ?r))
      (ready-road ?v ?next)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-road_0133-road_0146-tw_00090
    :parameters (?v - vehicle ?r - road_type_road_0133 ?next - road_type_road_0146 ?w - window_type_tw_00090)
    :precondition (and
      (ready-road ?v ?r)
      (road-next ?r ?next)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
    )
    :effect (and
      (not (ready-road ?v ?r))
      (ready-road ?v ?next)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-road_0133-road_0146-tw_00120
    :parameters (?v - vehicle ?r - road_type_road_0133 ?next - road_type_road_0146 ?w - window_type_tw_00120)
    :precondition (and
      (ready-road ?v ?r)
      (road-next ?r ?next)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
    )
    :effect (and
      (not (ready-road ?v ?r))
      (ready-road ?v ?next)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-road_0133-road_0146-tw_00150
    :parameters (?v - vehicle ?r - road_type_road_0133 ?next - road_type_road_0146 ?w - window_type_tw_00150)
    :precondition (and
      (ready-road ?v ?r)
      (road-next ?r ?next)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
    )
    :effect (and
      (not (ready-road ?v ?r))
      (ready-road ?v ?next)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-road_0133-road_0146-tw_00180
    :parameters (?v - vehicle ?r - road_type_road_0133 ?next - road_type_road_0146 ?w - window_type_tw_00180)
    :precondition (and
      (ready-road ?v ?r)
      (road-next ?r ?next)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
    )
    :effect (and
      (not (ready-road ?v ?r))
      (ready-road ?v ?next)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-road_0133-road_0146-tw_00210
    :parameters (?v - vehicle ?r - road_type_road_0133 ?next - road_type_road_0146 ?w - window_type_tw_00210)
    :precondition (and
      (ready-road ?v ?r)
      (road-next ?r ?next)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
    )
    :effect (and
      (not (ready-road ?v ?r))
      (ready-road ?v ?next)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-road_0133-road_0146-tw_00240
    :parameters (?v - vehicle ?r - road_type_road_0133 ?next - road_type_road_0146 ?w - window_type_tw_00240)
    :precondition (and
      (ready-road ?v ?r)
      (road-next ?r ?next)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
    )
    :effect (and
      (not (ready-road ?v ?r))
      (ready-road ?v ?next)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-road_0133-road_0146-tw_00270
    :parameters (?v - vehicle ?r - road_type_road_0133 ?next - road_type_road_0146 ?w - window_type_tw_00270)
    :precondition (and
      (ready-road ?v ?r)
      (road-next ?r ?next)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
    )
    :effect (and
      (not (ready-road ?v ?r))
      (ready-road ?v ?next)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-road_0133-road_0146-tw_00300
    :parameters (?v - vehicle ?r - road_type_road_0133 ?next - road_type_road_0146 ?w - window_type_tw_00300)
    :precondition (and
      (ready-road ?v ?r)
      (road-next ?r ?next)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
    )
    :effect (and
      (not (ready-road ?v ?r))
      (ready-road ?v ?next)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-road_0133-road_0146-tw_00330
    :parameters (?v - vehicle ?r - road_type_road_0133 ?next - road_type_road_0146 ?w - window_type_tw_00330)
    :precondition (and
      (ready-road ?v ?r)
      (road-next ?r ?next)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
    )
    :effect (and
      (not (ready-road ?v ?r))
      (ready-road ?v ?next)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-road_0133-road_0146-tw_00360
    :parameters (?v - vehicle ?r - road_type_road_0133 ?next - road_type_road_0146 ?w - window_type_tw_00360)
    :precondition (and
      (ready-road ?v ?r)
      (road-next ?r ?next)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
    )
    :effect (and
      (not (ready-road ?v ?r))
      (ready-road ?v ?next)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-road_0133-road_0146-tw_00390
    :parameters (?v - vehicle ?r - road_type_road_0133 ?next - road_type_road_0146 ?w - window_type_tw_00390)
    :precondition (and
      (ready-road ?v ?r)
      (road-next ?r ?next)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
    )
    :effect (and
      (not (ready-road ?v ?r))
      (ready-road ?v ?next)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-road_0133-road_0146-tw_00420
    :parameters (?v - vehicle ?r - road_type_road_0133 ?next - road_type_road_0146 ?w - window_type_tw_00420)
    :precondition (and
      (ready-road ?v ?r)
      (road-next ?r ?next)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
    )
    :effect (and
      (not (ready-road ?v ?r))
      (ready-road ?v ?next)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-road_0133-road_0146-tw_00450
    :parameters (?v - vehicle ?r - road_type_road_0133 ?next - road_type_road_0146 ?w - window_type_tw_00450)
    :precondition (and
      (ready-road ?v ?r)
      (road-next ?r ?next)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
    )
    :effect (and
      (not (ready-road ?v ?r))
      (ready-road ?v ?next)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-road_0133-road_0146-tw_00480
    :parameters (?v - vehicle ?r - road_type_road_0133 ?next - road_type_road_0146 ?w - window_type_tw_00480)
    :precondition (and
      (ready-road ?v ?r)
      (road-next ?r ?next)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
    )
    :effect (and
      (not (ready-road ?v ?r))
      (ready-road ?v ?next)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-road_0133-road_0146-tw_00510
    :parameters (?v - vehicle ?r - road_type_road_0133 ?next - road_type_road_0146 ?w - window_type_tw_00510)
    :precondition (and
      (ready-road ?v ?r)
      (road-next ?r ?next)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
    )
    :effect (and
      (not (ready-road ?v ?r))
      (ready-road ?v ?next)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-road_0133-road_0146-tw_00540
    :parameters (?v - vehicle ?r - road_type_road_0133 ?next - road_type_road_0146 ?w - window_type_tw_00540)
    :precondition (and
      (ready-road ?v ?r)
      (road-next ?r ?next)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
    )
    :effect (and
      (not (ready-road ?v ?r))
      (ready-road ?v ?next)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-road_0133-road_0146-tw_00570
    :parameters (?v - vehicle ?r - road_type_road_0133 ?next - road_type_road_0146 ?w - window_type_tw_00570)
    :precondition (and
      (ready-road ?v ?r)
      (road-next ?r ?next)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
    )
    :effect (and
      (not (ready-road ?v ?r))
      (ready-road ?v ?next)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-road_0133-road_0146-tw_00600
    :parameters (?v - vehicle ?r - road_type_road_0133 ?next - road_type_road_0146 ?w - window_type_tw_00600)
    :precondition (and
      (ready-road ?v ?r)
      (road-next ?r ?next)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
    )
    :effect (and
      (not (ready-road ?v ?r))
      (ready-road ?v ?next)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-road_0133-road_0147-tw_00000
    :parameters (?v - vehicle ?r - road_type_road_0133 ?next - road_type_road_0147 ?w - window_type_tw_00000)
    :precondition (and
      (ready-road ?v ?r)
      (road-next ?r ?next)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
    )
    :effect (and
      (not (ready-road ?v ?r))
      (ready-road ?v ?next)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-road_0133-road_0147-tw_00030
    :parameters (?v - vehicle ?r - road_type_road_0133 ?next - road_type_road_0147 ?w - window_type_tw_00030)
    :precondition (and
      (ready-road ?v ?r)
      (road-next ?r ?next)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
    )
    :effect (and
      (not (ready-road ?v ?r))
      (ready-road ?v ?next)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-road_0133-road_0147-tw_00060
    :parameters (?v - vehicle ?r - road_type_road_0133 ?next - road_type_road_0147 ?w - window_type_tw_00060)
    :precondition (and
      (ready-road ?v ?r)
      (road-next ?r ?next)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
    )
    :effect (and
      (not (ready-road ?v ?r))
      (ready-road ?v ?next)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-road_0133-road_0147-tw_00090
    :parameters (?v - vehicle ?r - road_type_road_0133 ?next - road_type_road_0147 ?w - window_type_tw_00090)
    :precondition (and
      (ready-road ?v ?r)
      (road-next ?r ?next)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
    )
    :effect (and
      (not (ready-road ?v ?r))
      (ready-road ?v ?next)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-road_0133-road_0147-tw_00120
    :parameters (?v - vehicle ?r - road_type_road_0133 ?next - road_type_road_0147 ?w - window_type_tw_00120)
    :precondition (and
      (ready-road ?v ?r)
      (road-next ?r ?next)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
    )
    :effect (and
      (not (ready-road ?v ?r))
      (ready-road ?v ?next)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-road_0133-road_0147-tw_00150
    :parameters (?v - vehicle ?r - road_type_road_0133 ?next - road_type_road_0147 ?w - window_type_tw_00150)
    :precondition (and
      (ready-road ?v ?r)
      (road-next ?r ?next)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
    )
    :effect (and
      (not (ready-road ?v ?r))
      (ready-road ?v ?next)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-road_0133-road_0147-tw_00180
    :parameters (?v - vehicle ?r - road_type_road_0133 ?next - road_type_road_0147 ?w - window_type_tw_00180)
    :precondition (and
      (ready-road ?v ?r)
      (road-next ?r ?next)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
    )
    :effect (and
      (not (ready-road ?v ?r))
      (ready-road ?v ?next)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-road_0133-road_0147-tw_00210
    :parameters (?v - vehicle ?r - road_type_road_0133 ?next - road_type_road_0147 ?w - window_type_tw_00210)
    :precondition (and
      (ready-road ?v ?r)
      (road-next ?r ?next)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
    )
    :effect (and
      (not (ready-road ?v ?r))
      (ready-road ?v ?next)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-road_0133-road_0147-tw_00240
    :parameters (?v - vehicle ?r - road_type_road_0133 ?next - road_type_road_0147 ?w - window_type_tw_00240)
    :precondition (and
      (ready-road ?v ?r)
      (road-next ?r ?next)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
    )
    :effect (and
      (not (ready-road ?v ?r))
      (ready-road ?v ?next)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-road_0133-road_0147-tw_00270
    :parameters (?v - vehicle ?r - road_type_road_0133 ?next - road_type_road_0147 ?w - window_type_tw_00270)
    :precondition (and
      (ready-road ?v ?r)
      (road-next ?r ?next)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
    )
    :effect (and
      (not (ready-road ?v ?r))
      (ready-road ?v ?next)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-road_0133-road_0147-tw_00300
    :parameters (?v - vehicle ?r - road_type_road_0133 ?next - road_type_road_0147 ?w - window_type_tw_00300)
    :precondition (and
      (ready-road ?v ?r)
      (road-next ?r ?next)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
    )
    :effect (and
      (not (ready-road ?v ?r))
      (ready-road ?v ?next)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-road_0133-road_0147-tw_00330
    :parameters (?v - vehicle ?r - road_type_road_0133 ?next - road_type_road_0147 ?w - window_type_tw_00330)
    :precondition (and
      (ready-road ?v ?r)
      (road-next ?r ?next)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
    )
    :effect (and
      (not (ready-road ?v ?r))
      (ready-road ?v ?next)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-road_0133-road_0147-tw_00360
    :parameters (?v - vehicle ?r - road_type_road_0133 ?next - road_type_road_0147 ?w - window_type_tw_00360)
    :precondition (and
      (ready-road ?v ?r)
      (road-next ?r ?next)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
    )
    :effect (and
      (not (ready-road ?v ?r))
      (ready-road ?v ?next)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-road_0133-road_0147-tw_00390
    :parameters (?v - vehicle ?r - road_type_road_0133 ?next - road_type_road_0147 ?w - window_type_tw_00390)
    :precondition (and
      (ready-road ?v ?r)
      (road-next ?r ?next)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
    )
    :effect (and
      (not (ready-road ?v ?r))
      (ready-road ?v ?next)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-road_0133-road_0147-tw_00420
    :parameters (?v - vehicle ?r - road_type_road_0133 ?next - road_type_road_0147 ?w - window_type_tw_00420)
    :precondition (and
      (ready-road ?v ?r)
      (road-next ?r ?next)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
    )
    :effect (and
      (not (ready-road ?v ?r))
      (ready-road ?v ?next)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-road_0133-road_0147-tw_00450
    :parameters (?v - vehicle ?r - road_type_road_0133 ?next - road_type_road_0147 ?w - window_type_tw_00450)
    :precondition (and
      (ready-road ?v ?r)
      (road-next ?r ?next)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
    )
    :effect (and
      (not (ready-road ?v ?r))
      (ready-road ?v ?next)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-road_0133-road_0147-tw_00480
    :parameters (?v - vehicle ?r - road_type_road_0133 ?next - road_type_road_0147 ?w - window_type_tw_00480)
    :precondition (and
      (ready-road ?v ?r)
      (road-next ?r ?next)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
    )
    :effect (and
      (not (ready-road ?v ?r))
      (ready-road ?v ?next)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-road_0133-road_0147-tw_00510
    :parameters (?v - vehicle ?r - road_type_road_0133 ?next - road_type_road_0147 ?w - window_type_tw_00510)
    :precondition (and
      (ready-road ?v ?r)
      (road-next ?r ?next)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
    )
    :effect (and
      (not (ready-road ?v ?r))
      (ready-road ?v ?next)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-road_0133-road_0147-tw_00540
    :parameters (?v - vehicle ?r - road_type_road_0133 ?next - road_type_road_0147 ?w - window_type_tw_00540)
    :precondition (and
      (ready-road ?v ?r)
      (road-next ?r ?next)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
    )
    :effect (and
      (not (ready-road ?v ?r))
      (ready-road ?v ?next)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-road_0133-road_0147-tw_00570
    :parameters (?v - vehicle ?r - road_type_road_0133 ?next - road_type_road_0147 ?w - window_type_tw_00570)
    :precondition (and
      (ready-road ?v ?r)
      (road-next ?r ?next)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
    )
    :effect (and
      (not (ready-road ?v ?r))
      (ready-road ?v ?next)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-road_0133-road_0147-tw_00600
    :parameters (?v - vehicle ?r - road_type_road_0133 ?next - road_type_road_0147 ?w - window_type_tw_00600)
    :precondition (and
      (ready-road ?v ?r)
      (road-next ?r ?next)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
    )
    :effect (and
      (not (ready-road ?v ?r))
      (ready-road ?v ?next)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-static-road_0134-road_0123
    :parameters (?v - vehicle ?r - road_type_road_0134 ?next - road_type_road_0123)
    :precondition (and
      (ready-road ?v ?r)
      (road-next ?r ?next)
      (road-open ?r)
      (static-road ?r)
      (not (road-blocked ?r))
    )
    :effect (and
      (not (ready-road ?v ?r))
      (ready-road ?v ?next)
      (increase (travel-time ?v) (travel-duration ?r))
      (increase (sim-time) (travel-duration ?r))
    )
  )

  (:action traverse-road-static-road_0134-road_0124
    :parameters (?v - vehicle ?r - road_type_road_0134 ?next - road_type_road_0124)
    :precondition (and
      (ready-road ?v ?r)
      (road-next ?r ?next)
      (road-open ?r)
      (static-road ?r)
      (not (road-blocked ?r))
    )
    :effect (and
      (not (ready-road ?v ?r))
      (ready-road ?v ?next)
      (increase (travel-time ?v) (travel-duration ?r))
      (increase (sim-time) (travel-duration ?r))
    )
  )

  (:action traverse-road-static-road_0134-road_0125
    :parameters (?v - vehicle ?r - road_type_road_0134 ?next - road_type_road_0125)
    :precondition (and
      (ready-road ?v ?r)
      (road-next ?r ?next)
      (road-open ?r)
      (static-road ?r)
      (not (road-blocked ?r))
    )
    :effect (and
      (not (ready-road ?v ?r))
      (ready-road ?v ?next)
      (increase (travel-time ?v) (travel-duration ?r))
      (increase (sim-time) (travel-duration ?r))
    )
  )

  (:action traverse-road-static-road_0134-road_0126
    :parameters (?v - vehicle ?r - road_type_road_0134 ?next - road_type_road_0126)
    :precondition (and
      (ready-road ?v ?r)
      (road-next ?r ?next)
      (road-open ?r)
      (static-road ?r)
      (not (road-blocked ?r))
    )
    :effect (and
      (not (ready-road ?v ?r))
      (ready-road ?v ?next)
      (increase (travel-time ?v) (travel-duration ?r))
      (increase (sim-time) (travel-duration ?r))
    )
  )

  (:action traverse-road-static-road_0135-road_0034
    :parameters (?v - vehicle ?r - road_type_road_0135 ?next - road_type_road_0034)
    :precondition (and
      (ready-road ?v ?r)
      (road-next ?r ?next)
      (road-open ?r)
      (static-road ?r)
      (not (road-blocked ?r))
    )
    :effect (and
      (not (ready-road ?v ?r))
      (ready-road ?v ?next)
      (increase (travel-time ?v) (travel-duration ?r))
      (increase (sim-time) (travel-duration ?r))
    )
  )

  (:action traverse-road-static-road_0135-road_0035
    :parameters (?v - vehicle ?r - road_type_road_0135 ?next - road_type_road_0035)
    :precondition (and
      (ready-road ?v ?r)
      (road-next ?r ?next)
      (road-open ?r)
      (static-road ?r)
      (not (road-blocked ?r))
    )
    :effect (and
      (not (ready-road ?v ?r))
      (ready-road ?v ?next)
      (increase (travel-time ?v) (travel-duration ?r))
      (increase (sim-time) (travel-duration ?r))
    )
  )

  (:action traverse-road-static-road_0135-road_0036
    :parameters (?v - vehicle ?r - road_type_road_0135 ?next - road_type_road_0036)
    :precondition (and
      (ready-road ?v ?r)
      (road-next ?r ?next)
      (road-open ?r)
      (static-road ?r)
      (not (road-blocked ?r))
    )
    :effect (and
      (not (ready-road ?v ?r))
      (ready-road ?v ?next)
      (increase (travel-time ?v) (travel-duration ?r))
      (increase (sim-time) (travel-duration ?r))
    )
  )

  (:action traverse-road-static-road_0136-road_0137
    :parameters (?v - vehicle ?r - road_type_road_0136 ?next - road_type_road_0137)
    :precondition (and
      (ready-road ?v ?r)
      (road-next ?r ?next)
      (road-open ?r)
      (static-road ?r)
      (not (road-blocked ?r))
    )
    :effect (and
      (not (ready-road ?v ?r))
      (ready-road ?v ?next)
      (increase (travel-time ?v) (travel-duration ?r))
      (increase (sim-time) (travel-duration ?r))
    )
  )

  (:action traverse-road-static-road_0136-road_0138
    :parameters (?v - vehicle ?r - road_type_road_0136 ?next - road_type_road_0138)
    :precondition (and
      (ready-road ?v ?r)
      (road-next ?r ?next)
      (road-open ?r)
      (static-road ?r)
      (not (road-blocked ?r))
    )
    :effect (and
      (not (ready-road ?v ?r))
      (ready-road ?v ?next)
      (increase (travel-time ?v) (travel-duration ?r))
      (increase (sim-time) (travel-duration ?r))
    )
  )

  (:action traverse-road-static-road_0137-road_0262
    :parameters (?v - vehicle ?r - road_type_road_0137 ?next - road_type_road_0262)
    :precondition (and
      (ready-road ?v ?r)
      (road-next ?r ?next)
      (road-open ?r)
      (static-road ?r)
      (not (road-blocked ?r))
    )
    :effect (and
      (not (ready-road ?v ?r))
      (ready-road ?v ?next)
      (increase (travel-time ?v) (travel-duration ?r))
      (increase (sim-time) (travel-duration ?r))
    )
  )

  (:action traverse-road-static-road_0137-road_0263
    :parameters (?v - vehicle ?r - road_type_road_0137 ?next - road_type_road_0263)
    :precondition (and
      (ready-road ?v ?r)
      (road-next ?r ?next)
      (road-open ?r)
      (static-road ?r)
      (not (road-blocked ?r))
    )
    :effect (and
      (not (ready-road ?v ?r))
      (ready-road ?v ?next)
      (increase (travel-time ?v) (travel-duration ?r))
      (increase (sim-time) (travel-duration ?r))
    )
  )

  (:action traverse-road-static-road_0138-road_0024
    :parameters (?v - vehicle ?r - road_type_road_0138 ?next - road_type_road_0024)
    :precondition (and
      (ready-road ?v ?r)
      (road-next ?r ?next)
      (road-open ?r)
      (static-road ?r)
      (not (road-blocked ?r))
    )
    :effect (and
      (not (ready-road ?v ?r))
      (ready-road ?v ?next)
      (increase (travel-time ?v) (travel-duration ?r))
      (increase (sim-time) (travel-duration ?r))
    )
  )

  (:action traverse-road-static-road_0138-road_0025
    :parameters (?v - vehicle ?r - road_type_road_0138 ?next - road_type_road_0025)
    :precondition (and
      (ready-road ?v ?r)
      (road-next ?r ?next)
      (road-open ?r)
      (static-road ?r)
      (not (road-blocked ?r))
    )
    :effect (and
      (not (ready-road ?v ?r))
      (ready-road ?v ?next)
      (increase (travel-time ?v) (travel-duration ?r))
      (increase (sim-time) (travel-duration ?r))
    )
  )

  (:action traverse-road-static-road_0138-road_0026
    :parameters (?v - vehicle ?r - road_type_road_0138 ?next - road_type_road_0026)
    :precondition (and
      (ready-road ?v ?r)
      (road-next ?r ?next)
      (road-open ?r)
      (static-road ?r)
      (not (road-blocked ?r))
    )
    :effect (and
      (not (ready-road ?v ?r))
      (ready-road ?v ?next)
      (increase (travel-time ?v) (travel-duration ?r))
      (increase (sim-time) (travel-duration ?r))
    )
  )

  (:action traverse-road-static-road_0139-road_0651
    :parameters (?v - vehicle ?r - road_type_road_0139 ?next - road_type_road_0651)
    :precondition (and
      (ready-road ?v ?r)
      (road-next ?r ?next)
      (road-open ?r)
      (static-road ?r)
      (not (road-blocked ?r))
    )
    :effect (and
      (not (ready-road ?v ?r))
      (ready-road ?v ?next)
      (increase (travel-time ?v) (travel-duration ?r))
      (increase (sim-time) (travel-duration ?r))
    )
  )

  (:action traverse-road-static-road_0139-road_0652
    :parameters (?v - vehicle ?r - road_type_road_0139 ?next - road_type_road_0652)
    :precondition (and
      (ready-road ?v ?r)
      (road-next ?r ?next)
      (road-open ?r)
      (static-road ?r)
      (not (road-blocked ?r))
    )
    :effect (and
      (not (ready-road ?v ?r))
      (ready-road ?v ?next)
      (increase (travel-time ?v) (travel-duration ?r))
      (increase (sim-time) (travel-duration ?r))
    )
  )

  (:action traverse-road-static-road_0139-road_0653
    :parameters (?v - vehicle ?r - road_type_road_0139 ?next - road_type_road_0653)
    :precondition (and
      (ready-road ?v ?r)
      (road-next ?r ?next)
      (road-open ?r)
      (static-road ?r)
      (not (road-blocked ?r))
    )
    :effect (and
      (not (ready-road ?v ?r))
      (ready-road ?v ?next)
      (increase (travel-time ?v) (travel-duration ?r))
      (increase (sim-time) (travel-duration ?r))
    )
  )

  (:action traverse-road-static-road_0140-road_0682
    :parameters (?v - vehicle ?r - road_type_road_0140 ?next - road_type_road_0682)
    :precondition (and
      (ready-road ?v ?r)
      (road-next ?r ?next)
      (road-open ?r)
      (static-road ?r)
      (not (road-blocked ?r))
    )
    :effect (and
      (not (ready-road ?v ?r))
      (ready-road ?v ?next)
      (increase (travel-time ?v) (travel-duration ?r))
      (increase (sim-time) (travel-duration ?r))
    )
  )

  (:action traverse-road-static-road_0141-road_0667
    :parameters (?v - vehicle ?r - road_type_road_0141 ?next - road_type_road_0667)
    :precondition (and
      (ready-road ?v ?r)
      (road-next ?r ?next)
      (road-open ?r)
      (static-road ?r)
      (not (road-blocked ?r))
    )
    :effect (and
      (not (ready-road ?v ?r))
      (ready-road ?v ?next)
      (increase (travel-time ?v) (travel-duration ?r))
      (increase (sim-time) (travel-duration ?r))
    )
  )

  (:action traverse-road-static-road_0141-road_0668
    :parameters (?v - vehicle ?r - road_type_road_0141 ?next - road_type_road_0668)
    :precondition (and
      (ready-road ?v ?r)
      (road-next ?r ?next)
      (road-open ?r)
      (static-road ?r)
      (not (road-blocked ?r))
    )
    :effect (and
      (not (ready-road ?v ?r))
      (ready-road ?v ?next)
      (increase (travel-time ?v) (travel-duration ?r))
      (increase (sim-time) (travel-duration ?r))
    )
  )

  (:action traverse-road-static-road_0141-road_0669
    :parameters (?v - vehicle ?r - road_type_road_0141 ?next - road_type_road_0669)
    :precondition (and
      (ready-road ?v ?r)
      (road-next ?r ?next)
      (road-open ?r)
      (static-road ?r)
      (not (road-blocked ?r))
    )
    :effect (and
      (not (ready-road ?v ?r))
      (ready-road ?v ?next)
      (increase (travel-time ?v) (travel-duration ?r))
      (increase (sim-time) (travel-duration ?r))
    )
  )

  (:action traverse-road-static-road_0142-macro_0004
    :parameters (?v - vehicle ?r - road_type_road_0142 ?next - road_type_macro_0004)
    :precondition (and
      (ready-road ?v ?r)
      (road-next ?r ?next)
      (road-open ?r)
      (static-road ?r)
      (not (road-blocked ?r))
    )
    :effect (and
      (not (ready-road ?v ?r))
      (ready-road ?v ?next)
      (increase (travel-time ?v) (travel-duration ?r))
      (increase (sim-time) (travel-duration ?r))
    )
  )

  (:action traverse-road-static-road_0142-road_0058
    :parameters (?v - vehicle ?r - road_type_road_0142 ?next - road_type_road_0058)
    :precondition (and
      (ready-road ?v ?r)
      (road-next ?r ?next)
      (road-open ?r)
      (static-road ?r)
      (not (road-blocked ?r))
    )
    :effect (and
      (not (ready-road ?v ?r))
      (ready-road ?v ?next)
      (increase (travel-time ?v) (travel-duration ?r))
      (increase (sim-time) (travel-duration ?r))
    )
  )

  (:action traverse-road-static-road_0143-road_0080
    :parameters (?v - vehicle ?r - road_type_road_0143 ?next - road_type_road_0080)
    :precondition (and
      (ready-road ?v ?r)
      (road-next ?r ?next)
      (road-open ?r)
      (static-road ?r)
      (not (road-blocked ?r))
    )
    :effect (and
      (not (ready-road ?v ?r))
      (ready-road ?v ?next)
      (increase (travel-time ?v) (travel-duration ?r))
      (increase (sim-time) (travel-duration ?r))
    )
  )

  (:action traverse-road-static-road_0143-road_0081
    :parameters (?v - vehicle ?r - road_type_road_0143 ?next - road_type_road_0081)
    :precondition (and
      (ready-road ?v ?r)
      (road-next ?r ?next)
      (road-open ?r)
      (static-road ?r)
      (not (road-blocked ?r))
    )
    :effect (and
      (not (ready-road ?v ?r))
      (ready-road ?v ?next)
      (increase (travel-time ?v) (travel-duration ?r))
      (increase (sim-time) (travel-duration ?r))
    )
  )

  (:action traverse-road-static-road_0144-road_0116
    :parameters (?v - vehicle ?r - road_type_road_0144 ?next - road_type_road_0116)
    :precondition (and
      (ready-road ?v ?r)
      (road-next ?r ?next)
      (road-open ?r)
      (static-road ?r)
      (not (road-blocked ?r))
    )
    :effect (and
      (not (ready-road ?v ?r))
      (ready-road ?v ?next)
      (increase (travel-time ?v) (travel-duration ?r))
      (increase (sim-time) (travel-duration ?r))
    )
  )

  (:action traverse-road-static-road_0145-macro_0052
    :parameters (?v - vehicle ?r - road_type_road_0145 ?next - road_type_macro_0052)
    :precondition (and
      (ready-road ?v ?r)
      (road-next ?r ?next)
      (road-open ?r)
      (static-road ?r)
      (not (road-blocked ?r))
    )
    :effect (and
      (not (ready-road ?v ?r))
      (ready-road ?v ?next)
      (increase (travel-time ?v) (travel-duration ?r))
      (increase (sim-time) (travel-duration ?r))
    )
  )

  (:action traverse-road-static-road_0145-road_0828
    :parameters (?v - vehicle ?r - road_type_road_0145 ?next - road_type_road_0828)
    :precondition (and
      (ready-road ?v ?r)
      (road-next ?r ?next)
      (road-open ?r)
      (static-road ?r)
      (not (road-blocked ?r))
    )
    :effect (and
      (not (ready-road ?v ?r))
      (ready-road ?v ?next)
      (increase (travel-time ?v) (travel-duration ?r))
      (increase (sim-time) (travel-duration ?r))
    )
  )

  (:action traverse-road-static-road_0146-road_0144
    :parameters (?v - vehicle ?r - road_type_road_0146 ?next - road_type_road_0144)
    :precondition (and
      (ready-road ?v ?r)
      (road-next ?r ?next)
      (road-open ?r)
      (static-road ?r)
      (not (road-blocked ?r))
    )
    :effect (and
      (not (ready-road ?v ?r))
      (ready-road ?v ?next)
      (increase (travel-time ?v) (travel-duration ?r))
      (increase (sim-time) (travel-duration ?r))
    )
  )

  (:action traverse-road-static-road_0147-road_0131
    :parameters (?v - vehicle ?r - road_type_road_0147 ?next - road_type_road_0131)
    :precondition (and
      (ready-road ?v ?r)
      (road-next ?r ?next)
      (road-open ?r)
      (static-road ?r)
      (not (road-blocked ?r))
    )
    :effect (and
      (not (ready-road ?v ?r))
      (ready-road ?v ?next)
      (increase (travel-time ?v) (travel-duration ?r))
      (increase (sim-time) (travel-duration ?r))
    )
  )

  (:action traverse-road-static-road_0147-road_0132
    :parameters (?v - vehicle ?r - road_type_road_0147 ?next - road_type_road_0132)
    :precondition (and
      (ready-road ?v ?r)
      (road-next ?r ?next)
      (road-open ?r)
      (static-road ?r)
      (not (road-blocked ?r))
    )
    :effect (and
      (not (ready-road ?v ?r))
      (ready-road ?v ?next)
      (increase (travel-time ?v) (travel-duration ?r))
      (increase (sim-time) (travel-duration ?r))
    )
  )

  (:action traverse-road-static-road_0147-road_0133
    :parameters (?v - vehicle ?r - road_type_road_0147 ?next - road_type_road_0133)
    :precondition (and
      (ready-road ?v ?r)
      (road-next ?r ?next)
      (road-open ?r)
      (static-road ?r)
      (not (road-blocked ?r))
    )
    :effect (and
      (not (ready-road ?v ?r))
      (ready-road ?v ?next)
      (increase (travel-time ?v) (travel-duration ?r))
      (increase (sim-time) (travel-duration ?r))
    )
  )

  (:action traverse-road-static-road_0147-road_0134
    :parameters (?v - vehicle ?r - road_type_road_0147 ?next - road_type_road_0134)
    :precondition (and
      (ready-road ?v ?r)
      (road-next ?r ?next)
      (road-open ?r)
      (static-road ?r)
      (not (road-blocked ?r))
    )
    :effect (and
      (not (ready-road ?v ?r))
      (ready-road ?v ?next)
      (increase (travel-time ?v) (travel-duration ?r))
      (increase (sim-time) (travel-duration ?r))
    )
  )

  (:action traverse-road-static-road_0148-road_0159
    :parameters (?v - vehicle ?r - road_type_road_0148 ?next - road_type_road_0159)
    :precondition (and
      (ready-road ?v ?r)
      (road-next ?r ?next)
      (road-open ?r)
      (static-road ?r)
      (not (road-blocked ?r))
    )
    :effect (and
      (not (ready-road ?v ?r))
      (ready-road ?v ?next)
      (increase (travel-time ?v) (travel-duration ?r))
      (increase (sim-time) (travel-duration ?r))
    )
  )

  (:action traverse-road-static-road_0148-road_0160
    :parameters (?v - vehicle ?r - road_type_road_0148 ?next - road_type_road_0160)
    :precondition (and
      (ready-road ?v ?r)
      (road-next ?r ?next)
      (road-open ?r)
      (static-road ?r)
      (not (road-blocked ?r))
    )
    :effect (and
      (not (ready-road ?v ?r))
      (ready-road ?v ?next)
      (increase (travel-time ?v) (travel-duration ?r))
      (increase (sim-time) (travel-duration ?r))
    )
  )

  (:action traverse-road-static-road_0149-road_0683
    :parameters (?v - vehicle ?r - road_type_road_0149 ?next - road_type_road_0683)
    :precondition (and
      (ready-road ?v ?r)
      (road-next ?r ?next)
      (road-open ?r)
      (static-road ?r)
      (not (road-blocked ?r))
    )
    :effect (and
      (not (ready-road ?v ?r))
      (ready-road ?v ?next)
      (increase (travel-time ?v) (travel-duration ?r))
      (increase (sim-time) (travel-duration ?r))
    )
  )

  (:action traverse-road-static-road_0149-road_0684
    :parameters (?v - vehicle ?r - road_type_road_0149 ?next - road_type_road_0684)
    :precondition (and
      (ready-road ?v ?r)
      (road-next ?r ?next)
      (road-open ?r)
      (static-road ?r)
      (not (road-blocked ?r))
    )
    :effect (and
      (not (ready-road ?v ?r))
      (ready-road ?v ?next)
      (increase (travel-time ?v) (travel-duration ?r))
      (increase (sim-time) (travel-duration ?r))
    )
  )

  (:action traverse-road-static-road_0150-road_0375
    :parameters (?v - vehicle ?r - road_type_road_0150 ?next - road_type_road_0375)
    :precondition (and
      (ready-road ?v ?r)
      (road-next ?r ?next)
      (road-open ?r)
      (static-road ?r)
      (not (road-blocked ?r))
    )
    :effect (and
      (not (ready-road ?v ?r))
      (ready-road ?v ?next)
      (increase (travel-time ?v) (travel-duration ?r))
      (increase (sim-time) (travel-duration ?r))
    )
  )

  (:action traverse-road-static-road_0150-road_0376
    :parameters (?v - vehicle ?r - road_type_road_0150 ?next - road_type_road_0376)
    :precondition (and
      (ready-road ?v ?r)
      (road-next ?r ?next)
      (road-open ?r)
      (static-road ?r)
      (not (road-blocked ?r))
    )
    :effect (and
      (not (ready-road ?v ?r))
      (ready-road ?v ?next)
      (increase (travel-time ?v) (travel-duration ?r))
      (increase (sim-time) (travel-duration ?r))
    )
  )

  (:action traverse-road-static-road_0150-road_0377
    :parameters (?v - vehicle ?r - road_type_road_0150 ?next - road_type_road_0377)
    :precondition (and
      (ready-road ?v ?r)
      (road-next ?r ?next)
      (road-open ?r)
      (static-road ?r)
      (not (road-blocked ?r))
    )
    :effect (and
      (not (ready-road ?v ?r))
      (ready-road ?v ?next)
      (increase (travel-time ?v) (travel-duration ?r))
      (increase (sim-time) (travel-duration ?r))
    )
  )

  (:action traverse-road-static-road_0151-road_0180
    :parameters (?v - vehicle ?r - road_type_road_0151 ?next - road_type_road_0180)
    :precondition (and
      (ready-road ?v ?r)
      (road-next ?r ?next)
      (road-open ?r)
      (static-road ?r)
      (not (road-blocked ?r))
    )
    :effect (and
      (not (ready-road ?v ?r))
      (ready-road ?v ?next)
      (increase (travel-time ?v) (travel-duration ?r))
      (increase (sim-time) (travel-duration ?r))
    )
  )

  (:action traverse-road-static-road_0152-road_0523
    :parameters (?v - vehicle ?r - road_type_road_0152 ?next - road_type_road_0523)
    :precondition (and
      (ready-road ?v ?r)
      (road-next ?r ?next)
      (road-open ?r)
      (static-road ?r)
      (not (road-blocked ?r))
    )
    :effect (and
      (not (ready-road ?v ?r))
      (ready-road ?v ?next)
      (increase (travel-time ?v) (travel-duration ?r))
      (increase (sim-time) (travel-duration ?r))
    )
  )

  (:action traverse-road-static-road_0152-road_0524
    :parameters (?v - vehicle ?r - road_type_road_0152 ?next - road_type_road_0524)
    :precondition (and
      (ready-road ?v ?r)
      (road-next ?r ?next)
      (road-open ?r)
      (static-road ?r)
      (not (road-blocked ?r))
    )
    :effect (and
      (not (ready-road ?v ?r))
      (ready-road ?v ?next)
      (increase (travel-time ?v) (travel-duration ?r))
      (increase (sim-time) (travel-duration ?r))
    )
  )

  (:action traverse-road-static-road_0152-road_0525
    :parameters (?v - vehicle ?r - road_type_road_0152 ?next - road_type_road_0525)
    :precondition (and
      (ready-road ?v ?r)
      (road-next ?r ?next)
      (road-open ?r)
      (static-road ?r)
      (not (road-blocked ?r))
    )
    :effect (and
      (not (ready-road ?v ?r))
      (ready-road ?v ?next)
      (increase (travel-time ?v) (travel-duration ?r))
      (increase (sim-time) (travel-duration ?r))
    )
  )

  (:action traverse-road-static-road_0152-road_0526
    :parameters (?v - vehicle ?r - road_type_road_0152 ?next - road_type_road_0526)
    :precondition (and
      (ready-road ?v ?r)
      (road-next ?r ?next)
      (road-open ?r)
      (static-road ?r)
      (not (road-blocked ?r))
    )
    :effect (and
      (not (ready-road ?v ?r))
      (ready-road ?v ?next)
      (increase (travel-time ?v) (travel-duration ?r))
      (increase (sim-time) (travel-duration ?r))
    )
  )

  (:action traverse-road-static-road_0153-road_0532
    :parameters (?v - vehicle ?r - road_type_road_0153 ?next - road_type_road_0532)
    :precondition (and
      (ready-road ?v ?r)
      (road-next ?r ?next)
      (road-open ?r)
      (static-road ?r)
      (not (road-blocked ?r))
    )
    :effect (and
      (not (ready-road ?v ?r))
      (ready-road ?v ?next)
      (increase (travel-time ?v) (travel-duration ?r))
      (increase (sim-time) (travel-duration ?r))
    )
  )

  (:action traverse-road-static-road_0153-road_0533
    :parameters (?v - vehicle ?r - road_type_road_0153 ?next - road_type_road_0533)
    :precondition (and
      (ready-road ?v ?r)
      (road-next ?r ?next)
      (road-open ?r)
      (static-road ?r)
      (not (road-blocked ?r))
    )
    :effect (and
      (not (ready-road ?v ?r))
      (ready-road ?v ?next)
      (increase (travel-time ?v) (travel-duration ?r))
      (increase (sim-time) (travel-duration ?r))
    )
  )

  (:action traverse-road-static-road_0153-road_0534
    :parameters (?v - vehicle ?r - road_type_road_0153 ?next - road_type_road_0534)
    :precondition (and
      (ready-road ?v ?r)
      (road-next ?r ?next)
      (road-open ?r)
      (static-road ?r)
      (not (road-blocked ?r))
    )
    :effect (and
      (not (ready-road ?v ?r))
      (ready-road ?v ?next)
      (increase (travel-time ?v) (travel-duration ?r))
      (increase (sim-time) (travel-duration ?r))
    )
  )

  (:action traverse-road-static-road_0154-road_0651
    :parameters (?v - vehicle ?r - road_type_road_0154 ?next - road_type_road_0651)
    :precondition (and
      (ready-road ?v ?r)
      (road-next ?r ?next)
      (road-open ?r)
      (static-road ?r)
      (not (road-blocked ?r))
    )
    :effect (and
      (not (ready-road ?v ?r))
      (ready-road ?v ?next)
      (increase (travel-time ?v) (travel-duration ?r))
      (increase (sim-time) (travel-duration ?r))
    )
  )

  (:action traverse-road-static-road_0154-road_0652
    :parameters (?v - vehicle ?r - road_type_road_0154 ?next - road_type_road_0652)
    :precondition (and
      (ready-road ?v ?r)
      (road-next ?r ?next)
      (road-open ?r)
      (static-road ?r)
      (not (road-blocked ?r))
    )
    :effect (and
      (not (ready-road ?v ?r))
      (ready-road ?v ?next)
      (increase (travel-time ?v) (travel-duration ?r))
      (increase (sim-time) (travel-duration ?r))
    )
  )

  (:action traverse-road-static-road_0154-road_0653
    :parameters (?v - vehicle ?r - road_type_road_0154 ?next - road_type_road_0653)
    :precondition (and
      (ready-road ?v ?r)
      (road-next ?r ?next)
      (road-open ?r)
      (static-road ?r)
      (not (road-blocked ?r))
    )
    :effect (and
      (not (ready-road ?v ?r))
      (ready-road ?v ?next)
      (increase (travel-time ?v) (travel-duration ?r))
      (increase (sim-time) (travel-duration ?r))
    )
  )

  (:action traverse-road-static-road_0155-road_0673
    :parameters (?v - vehicle ?r - road_type_road_0155 ?next - road_type_road_0673)
    :precondition (and
      (ready-road ?v ?r)
      (road-next ?r ?next)
      (road-open ?r)
      (static-road ?r)
      (not (road-blocked ?r))
    )
    :effect (and
      (not (ready-road ?v ?r))
      (ready-road ?v ?next)
      (increase (travel-time ?v) (travel-duration ?r))
      (increase (sim-time) (travel-duration ?r))
    )
  )

  (:action traverse-road-static-road_0155-road_0674
    :parameters (?v - vehicle ?r - road_type_road_0155 ?next - road_type_road_0674)
    :precondition (and
      (ready-road ?v ?r)
      (road-next ?r ?next)
      (road-open ?r)
      (static-road ?r)
      (not (road-blocked ?r))
    )
    :effect (and
      (not (ready-road ?v ?r))
      (ready-road ?v ?next)
      (increase (travel-time ?v) (travel-duration ?r))
      (increase (sim-time) (travel-duration ?r))
    )
  )

  (:action traverse-road-static-road_0156-road_0419
    :parameters (?v - vehicle ?r - road_type_road_0156 ?next - road_type_road_0419)
    :precondition (and
      (ready-road ?v ?r)
      (road-next ?r ?next)
      (road-open ?r)
      (static-road ?r)
      (not (road-blocked ?r))
    )
    :effect (and
      (not (ready-road ?v ?r))
      (ready-road ?v ?next)
      (increase (travel-time ?v) (travel-duration ?r))
      (increase (sim-time) (travel-duration ?r))
    )
  )

  (:action traverse-road-static-road_0157-road_0344
    :parameters (?v - vehicle ?r - road_type_road_0157 ?next - road_type_road_0344)
    :precondition (and
      (ready-road ?v ?r)
      (road-next ?r ?next)
      (road-open ?r)
      (static-road ?r)
      (not (road-blocked ?r))
    )
    :effect (and
      (not (ready-road ?v ?r))
      (ready-road ?v ?next)
      (increase (travel-time ?v) (travel-duration ?r))
      (increase (sim-time) (travel-duration ?r))
    )
  )

  (:action traverse-road-static-road_0157-road_0345
    :parameters (?v - vehicle ?r - road_type_road_0157 ?next - road_type_road_0345)
    :precondition (and
      (ready-road ?v ?r)
      (road-next ?r ?next)
      (road-open ?r)
      (static-road ?r)
      (not (road-blocked ?r))
    )
    :effect (and
      (not (ready-road ?v ?r))
      (ready-road ?v ?next)
      (increase (travel-time ?v) (travel-duration ?r))
      (increase (sim-time) (travel-duration ?r))
    )
  )

  (:action traverse-road-static-road_0159-road_0148
    :parameters (?v - vehicle ?r - road_type_road_0159 ?next - road_type_road_0148)
    :precondition (and
      (ready-road ?v ?r)
      (road-next ?r ?next)
      (road-open ?r)
      (static-road ?r)
      (not (road-blocked ?r))
    )
    :effect (and
      (not (ready-road ?v ?r))
      (ready-road ?v ?next)
      (increase (travel-time ?v) (travel-duration ?r))
      (increase (sim-time) (travel-duration ?r))
    )
  )

  (:action traverse-road-static-road_0161-road_0823
    :parameters (?v - vehicle ?r - road_type_road_0161 ?next - road_type_road_0823)
    :precondition (and
      (ready-road ?v ?r)
      (road-next ?r ?next)
      (road-open ?r)
      (static-road ?r)
      (not (road-blocked ?r))
    )
    :effect (and
      (not (ready-road ?v ?r))
      (ready-road ?v ?next)
      (increase (travel-time ?v) (travel-duration ?r))
      (increase (sim-time) (travel-duration ?r))
    )
  )

  (:action traverse-road-static-road_0161-road_0824
    :parameters (?v - vehicle ?r - road_type_road_0161 ?next - road_type_road_0824)
    :precondition (and
      (ready-road ?v ?r)
      (road-next ?r ?next)
      (road-open ?r)
      (static-road ?r)
      (not (road-blocked ?r))
    )
    :effect (and
      (not (ready-road ?v ?r))
      (ready-road ?v ?next)
      (increase (travel-time ?v) (travel-duration ?r))
      (increase (sim-time) (travel-duration ?r))
    )
  )

  (:action traverse-road-static-road_0162-road_0836
    :parameters (?v - vehicle ?r - road_type_road_0162 ?next - road_type_road_0836)
    :precondition (and
      (ready-road ?v ?r)
      (road-next ?r ?next)
      (road-open ?r)
      (static-road ?r)
      (not (road-blocked ?r))
    )
    :effect (and
      (not (ready-road ?v ?r))
      (ready-road ?v ?next)
      (increase (travel-time ?v) (travel-duration ?r))
      (increase (sim-time) (travel-duration ?r))
    )
  )

  (:action traverse-road-static-road_0163-road_0201
    :parameters (?v - vehicle ?r - road_type_road_0163 ?next - road_type_road_0201)
    :precondition (and
      (ready-road ?v ?r)
      (road-next ?r ?next)
      (road-open ?r)
      (static-road ?r)
      (not (road-blocked ?r))
    )
    :effect (and
      (not (ready-road ?v ?r))
      (ready-road ?v ?next)
      (increase (travel-time ?v) (travel-duration ?r))
      (increase (sim-time) (travel-duration ?r))
    )
  )

  (:action traverse-road-static-road_0163-road_0202
    :parameters (?v - vehicle ?r - road_type_road_0163 ?next - road_type_road_0202)
    :precondition (and
      (ready-road ?v ?r)
      (road-next ?r ?next)
      (road-open ?r)
      (static-road ?r)
      (not (road-blocked ?r))
    )
    :effect (and
      (not (ready-road ?v ?r))
      (ready-road ?v ?next)
      (increase (travel-time ?v) (travel-duration ?r))
      (increase (sim-time) (travel-duration ?r))
    )
  )

  (:action traverse-road-static-road_0164-road_0654
    :parameters (?v - vehicle ?r - road_type_road_0164 ?next - road_type_road_0654)
    :precondition (and
      (ready-road ?v ?r)
      (road-next ?r ?next)
      (road-open ?r)
      (static-road ?r)
      (not (road-blocked ?r))
    )
    :effect (and
      (not (ready-road ?v ?r))
      (ready-road ?v ?next)
      (increase (travel-time ?v) (travel-duration ?r))
      (increase (sim-time) (travel-duration ?r))
    )
  )

  (:action traverse-road-static-road_0165-road_0281
    :parameters (?v - vehicle ?r - road_type_road_0165 ?next - road_type_road_0281)
    :precondition (and
      (ready-road ?v ?r)
      (road-next ?r ?next)
      (road-open ?r)
      (static-road ?r)
      (not (road-blocked ?r))
    )
    :effect (and
      (not (ready-road ?v ?r))
      (ready-road ?v ?next)
      (increase (travel-time ?v) (travel-duration ?r))
      (increase (sim-time) (travel-duration ?r))
    )
  )

  (:action traverse-road-static-road_0165-road_0282
    :parameters (?v - vehicle ?r - road_type_road_0165 ?next - road_type_road_0282)
    :precondition (and
      (ready-road ?v ?r)
      (road-next ?r ?next)
      (road-open ?r)
      (static-road ?r)
      (not (road-blocked ?r))
    )
    :effect (and
      (not (ready-road ?v ?r))
      (ready-road ?v ?next)
      (increase (travel-time ?v) (travel-duration ?r))
      (increase (sim-time) (travel-duration ?r))
    )
  )

  (:action traverse-road-static-road_0166-road_0846
    :parameters (?v - vehicle ?r - road_type_road_0166 ?next - road_type_road_0846)
    :precondition (and
      (ready-road ?v ?r)
      (road-next ?r ?next)
      (road-open ?r)
      (static-road ?r)
      (not (road-blocked ?r))
    )
    :effect (and
      (not (ready-road ?v ?r))
      (ready-road ?v ?next)
      (increase (travel-time ?v) (travel-duration ?r))
      (increase (sim-time) (travel-duration ?r))
    )
  )

  (:action traverse-road-static-road_0166-road_0847
    :parameters (?v - vehicle ?r - road_type_road_0166 ?next - road_type_road_0847)
    :precondition (and
      (ready-road ?v ?r)
      (road-next ?r ?next)
      (road-open ?r)
      (static-road ?r)
      (not (road-blocked ?r))
    )
    :effect (and
      (not (ready-road ?v ?r))
      (ready-road ?v ?next)
      (increase (travel-time ?v) (travel-duration ?r))
      (increase (sim-time) (travel-duration ?r))
    )
  )

  (:action traverse-road-static-road_0167-road_0170
    :parameters (?v - vehicle ?r - road_type_road_0167 ?next - road_type_road_0170)
    :precondition (and
      (ready-road ?v ?r)
      (road-next ?r ?next)
      (road-open ?r)
      (static-road ?r)
      (not (road-blocked ?r))
    )
    :effect (and
      (not (ready-road ?v ?r))
      (ready-road ?v ?next)
      (increase (travel-time ?v) (travel-duration ?r))
      (increase (sim-time) (travel-duration ?r))
    )
  )

  (:action traverse-road-static-road_0169-road_0341
    :parameters (?v - vehicle ?r - road_type_road_0169 ?next - road_type_road_0341)
    :precondition (and
      (ready-road ?v ?r)
      (road-next ?r ?next)
      (road-open ?r)
      (static-road ?r)
      (not (road-blocked ?r))
    )
    :effect (and
      (not (ready-road ?v ?r))
      (ready-road ?v ?next)
      (increase (travel-time ?v) (travel-duration ?r))
      (increase (sim-time) (travel-duration ?r))
    )
  )

  (:action traverse-road-static-road_0170-road_0843
    :parameters (?v - vehicle ?r - road_type_road_0170 ?next - road_type_road_0843)
    :precondition (and
      (ready-road ?v ?r)
      (road-next ?r ?next)
      (road-open ?r)
      (static-road ?r)
      (not (road-blocked ?r))
    )
    :effect (and
      (not (ready-road ?v ?r))
      (ready-road ?v ?next)
      (increase (travel-time ?v) (travel-duration ?r))
      (increase (sim-time) (travel-duration ?r))
    )
  )

  (:action traverse-road-static-road_0170-road_0844
    :parameters (?v - vehicle ?r - road_type_road_0170 ?next - road_type_road_0844)
    :precondition (and
      (ready-road ?v ?r)
      (road-next ?r ?next)
      (road-open ?r)
      (static-road ?r)
      (not (road-blocked ?r))
    )
    :effect (and
      (not (ready-road ?v ?r))
      (ready-road ?v ?next)
      (increase (travel-time ?v) (travel-duration ?r))
      (increase (sim-time) (travel-duration ?r))
    )
  )

  (:action traverse-road-static-road_0171-road_0451
    :parameters (?v - vehicle ?r - road_type_road_0171 ?next - road_type_road_0451)
    :precondition (and
      (ready-road ?v ?r)
      (road-next ?r ?next)
      (road-open ?r)
      (static-road ?r)
      (not (road-blocked ?r))
    )
    :effect (and
      (not (ready-road ?v ?r))
      (ready-road ?v ?next)
      (increase (travel-time ?v) (travel-duration ?r))
      (increase (sim-time) (travel-duration ?r))
    )
  )

  (:action traverse-road-static-road_0171-road_0452
    :parameters (?v - vehicle ?r - road_type_road_0171 ?next - road_type_road_0452)
    :precondition (and
      (ready-road ?v ?r)
      (road-next ?r ?next)
      (road-open ?r)
      (static-road ?r)
      (not (road-blocked ?r))
    )
    :effect (and
      (not (ready-road ?v ?r))
      (ready-road ?v ?next)
      (increase (travel-time ?v) (travel-duration ?r))
      (increase (sim-time) (travel-duration ?r))
    )
  )

  (:action traverse-road-static-road_0172-road_0092
    :parameters (?v - vehicle ?r - road_type_road_0172 ?next - road_type_road_0092)
    :precondition (and
      (ready-road ?v ?r)
      (road-next ?r ?next)
      (road-open ?r)
      (static-road ?r)
      (not (road-blocked ?r))
    )
    :effect (and
      (not (ready-road ?v ?r))
      (ready-road ?v ?next)
      (increase (travel-time ?v) (travel-duration ?r))
      (increase (sim-time) (travel-duration ?r))
    )
  )

  (:action traverse-road-static-road_0173-road_0304
    :parameters (?v - vehicle ?r - road_type_road_0173 ?next - road_type_road_0304)
    :precondition (and
      (ready-road ?v ?r)
      (road-next ?r ?next)
      (road-open ?r)
      (static-road ?r)
      (not (road-blocked ?r))
    )
    :effect (and
      (not (ready-road ?v ?r))
      (ready-road ?v ?next)
      (increase (travel-time ?v) (travel-duration ?r))
      (increase (sim-time) (travel-duration ?r))
    )
  )

  (:action traverse-road-static-road_0175-road_0301
    :parameters (?v - vehicle ?r - road_type_road_0175 ?next - road_type_road_0301)
    :precondition (and
      (ready-road ?v ?r)
      (road-next ?r ?next)
      (road-open ?r)
      (static-road ?r)
      (not (road-blocked ?r))
    )
    :effect (and
      (not (ready-road ?v ?r))
      (ready-road ?v ?next)
      (increase (travel-time ?v) (travel-duration ?r))
      (increase (sim-time) (travel-duration ?r))
    )
  )

  (:action traverse-road-static-road_0175-road_0302
    :parameters (?v - vehicle ?r - road_type_road_0175 ?next - road_type_road_0302)
    :precondition (and
      (ready-road ?v ?r)
      (road-next ?r ?next)
      (road-open ?r)
      (static-road ?r)
      (not (road-blocked ?r))
    )
    :effect (and
      (not (ready-road ?v ?r))
      (ready-road ?v ?next)
      (increase (travel-time ?v) (travel-duration ?r))
      (increase (sim-time) (travel-duration ?r))
    )
  )

  (:action traverse-road-dynamic-road_0176-road_0858-tw_00000
    :parameters (?v - vehicle ?r - road_type_road_0176 ?next - road_type_road_0858 ?w - window_type_tw_00000)
    :precondition (and
      (ready-road ?v ?r)
      (road-next ?r ?next)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
    )
    :effect (and
      (not (ready-road ?v ?r))
      (ready-road ?v ?next)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-road_0176-road_0858-tw_00030
    :parameters (?v - vehicle ?r - road_type_road_0176 ?next - road_type_road_0858 ?w - window_type_tw_00030)
    :precondition (and
      (ready-road ?v ?r)
      (road-next ?r ?next)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
    )
    :effect (and
      (not (ready-road ?v ?r))
      (ready-road ?v ?next)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-road_0176-road_0858-tw_00060
    :parameters (?v - vehicle ?r - road_type_road_0176 ?next - road_type_road_0858 ?w - window_type_tw_00060)
    :precondition (and
      (ready-road ?v ?r)
      (road-next ?r ?next)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
    )
    :effect (and
      (not (ready-road ?v ?r))
      (ready-road ?v ?next)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-road_0176-road_0858-tw_00090
    :parameters (?v - vehicle ?r - road_type_road_0176 ?next - road_type_road_0858 ?w - window_type_tw_00090)
    :precondition (and
      (ready-road ?v ?r)
      (road-next ?r ?next)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
    )
    :effect (and
      (not (ready-road ?v ?r))
      (ready-road ?v ?next)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-road_0176-road_0858-tw_00120
    :parameters (?v - vehicle ?r - road_type_road_0176 ?next - road_type_road_0858 ?w - window_type_tw_00120)
    :precondition (and
      (ready-road ?v ?r)
      (road-next ?r ?next)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
    )
    :effect (and
      (not (ready-road ?v ?r))
      (ready-road ?v ?next)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-road_0176-road_0858-tw_00150
    :parameters (?v - vehicle ?r - road_type_road_0176 ?next - road_type_road_0858 ?w - window_type_tw_00150)
    :precondition (and
      (ready-road ?v ?r)
      (road-next ?r ?next)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
    )
    :effect (and
      (not (ready-road ?v ?r))
      (ready-road ?v ?next)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-road_0176-road_0858-tw_00180
    :parameters (?v - vehicle ?r - road_type_road_0176 ?next - road_type_road_0858 ?w - window_type_tw_00180)
    :precondition (and
      (ready-road ?v ?r)
      (road-next ?r ?next)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
    )
    :effect (and
      (not (ready-road ?v ?r))
      (ready-road ?v ?next)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-road_0176-road_0858-tw_00210
    :parameters (?v - vehicle ?r - road_type_road_0176 ?next - road_type_road_0858 ?w - window_type_tw_00210)
    :precondition (and
      (ready-road ?v ?r)
      (road-next ?r ?next)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
    )
    :effect (and
      (not (ready-road ?v ?r))
      (ready-road ?v ?next)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-road_0176-road_0858-tw_00240
    :parameters (?v - vehicle ?r - road_type_road_0176 ?next - road_type_road_0858 ?w - window_type_tw_00240)
    :precondition (and
      (ready-road ?v ?r)
      (road-next ?r ?next)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
    )
    :effect (and
      (not (ready-road ?v ?r))
      (ready-road ?v ?next)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-road_0176-road_0858-tw_00270
    :parameters (?v - vehicle ?r - road_type_road_0176 ?next - road_type_road_0858 ?w - window_type_tw_00270)
    :precondition (and
      (ready-road ?v ?r)
      (road-next ?r ?next)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
    )
    :effect (and
      (not (ready-road ?v ?r))
      (ready-road ?v ?next)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-road_0176-road_0858-tw_00300
    :parameters (?v - vehicle ?r - road_type_road_0176 ?next - road_type_road_0858 ?w - window_type_tw_00300)
    :precondition (and
      (ready-road ?v ?r)
      (road-next ?r ?next)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
    )
    :effect (and
      (not (ready-road ?v ?r))
      (ready-road ?v ?next)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-road_0176-road_0858-tw_00330
    :parameters (?v - vehicle ?r - road_type_road_0176 ?next - road_type_road_0858 ?w - window_type_tw_00330)
    :precondition (and
      (ready-road ?v ?r)
      (road-next ?r ?next)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
    )
    :effect (and
      (not (ready-road ?v ?r))
      (ready-road ?v ?next)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-road_0176-road_0858-tw_00360
    :parameters (?v - vehicle ?r - road_type_road_0176 ?next - road_type_road_0858 ?w - window_type_tw_00360)
    :precondition (and
      (ready-road ?v ?r)
      (road-next ?r ?next)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
    )
    :effect (and
      (not (ready-road ?v ?r))
      (ready-road ?v ?next)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-road_0176-road_0858-tw_00390
    :parameters (?v - vehicle ?r - road_type_road_0176 ?next - road_type_road_0858 ?w - window_type_tw_00390)
    :precondition (and
      (ready-road ?v ?r)
      (road-next ?r ?next)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
    )
    :effect (and
      (not (ready-road ?v ?r))
      (ready-road ?v ?next)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-road_0176-road_0858-tw_00420
    :parameters (?v - vehicle ?r - road_type_road_0176 ?next - road_type_road_0858 ?w - window_type_tw_00420)
    :precondition (and
      (ready-road ?v ?r)
      (road-next ?r ?next)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
    )
    :effect (and
      (not (ready-road ?v ?r))
      (ready-road ?v ?next)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-road_0176-road_0858-tw_00450
    :parameters (?v - vehicle ?r - road_type_road_0176 ?next - road_type_road_0858 ?w - window_type_tw_00450)
    :precondition (and
      (ready-road ?v ?r)
      (road-next ?r ?next)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
    )
    :effect (and
      (not (ready-road ?v ?r))
      (ready-road ?v ?next)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-road_0176-road_0858-tw_00480
    :parameters (?v - vehicle ?r - road_type_road_0176 ?next - road_type_road_0858 ?w - window_type_tw_00480)
    :precondition (and
      (ready-road ?v ?r)
      (road-next ?r ?next)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
    )
    :effect (and
      (not (ready-road ?v ?r))
      (ready-road ?v ?next)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-road_0176-road_0858-tw_00510
    :parameters (?v - vehicle ?r - road_type_road_0176 ?next - road_type_road_0858 ?w - window_type_tw_00510)
    :precondition (and
      (ready-road ?v ?r)
      (road-next ?r ?next)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
    )
    :effect (and
      (not (ready-road ?v ?r))
      (ready-road ?v ?next)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-road_0176-road_0858-tw_00540
    :parameters (?v - vehicle ?r - road_type_road_0176 ?next - road_type_road_0858 ?w - window_type_tw_00540)
    :precondition (and
      (ready-road ?v ?r)
      (road-next ?r ?next)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
    )
    :effect (and
      (not (ready-road ?v ?r))
      (ready-road ?v ?next)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-road_0176-road_0858-tw_00570
    :parameters (?v - vehicle ?r - road_type_road_0176 ?next - road_type_road_0858 ?w - window_type_tw_00570)
    :precondition (and
      (ready-road ?v ?r)
      (road-next ?r ?next)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
    )
    :effect (and
      (not (ready-road ?v ?r))
      (ready-road ?v ?next)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-road_0176-road_0858-tw_00600
    :parameters (?v - vehicle ?r - road_type_road_0176 ?next - road_type_road_0858 ?w - window_type_tw_00600)
    :precondition (and
      (ready-road ?v ?r)
      (road-next ?r ?next)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
    )
    :effect (and
      (not (ready-road ?v ?r))
      (ready-road ?v ?next)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-road_0176-road_0859-tw_00000
    :parameters (?v - vehicle ?r - road_type_road_0176 ?next - road_type_road_0859 ?w - window_type_tw_00000)
    :precondition (and
      (ready-road ?v ?r)
      (road-next ?r ?next)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
    )
    :effect (and
      (not (ready-road ?v ?r))
      (ready-road ?v ?next)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-road_0176-road_0859-tw_00030
    :parameters (?v - vehicle ?r - road_type_road_0176 ?next - road_type_road_0859 ?w - window_type_tw_00030)
    :precondition (and
      (ready-road ?v ?r)
      (road-next ?r ?next)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
    )
    :effect (and
      (not (ready-road ?v ?r))
      (ready-road ?v ?next)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-road_0176-road_0859-tw_00060
    :parameters (?v - vehicle ?r - road_type_road_0176 ?next - road_type_road_0859 ?w - window_type_tw_00060)
    :precondition (and
      (ready-road ?v ?r)
      (road-next ?r ?next)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
    )
    :effect (and
      (not (ready-road ?v ?r))
      (ready-road ?v ?next)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-road_0176-road_0859-tw_00090
    :parameters (?v - vehicle ?r - road_type_road_0176 ?next - road_type_road_0859 ?w - window_type_tw_00090)
    :precondition (and
      (ready-road ?v ?r)
      (road-next ?r ?next)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
    )
    :effect (and
      (not (ready-road ?v ?r))
      (ready-road ?v ?next)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-road_0176-road_0859-tw_00120
    :parameters (?v - vehicle ?r - road_type_road_0176 ?next - road_type_road_0859 ?w - window_type_tw_00120)
    :precondition (and
      (ready-road ?v ?r)
      (road-next ?r ?next)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
    )
    :effect (and
      (not (ready-road ?v ?r))
      (ready-road ?v ?next)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-road_0176-road_0859-tw_00150
    :parameters (?v - vehicle ?r - road_type_road_0176 ?next - road_type_road_0859 ?w - window_type_tw_00150)
    :precondition (and
      (ready-road ?v ?r)
      (road-next ?r ?next)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
    )
    :effect (and
      (not (ready-road ?v ?r))
      (ready-road ?v ?next)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-road_0176-road_0859-tw_00180
    :parameters (?v - vehicle ?r - road_type_road_0176 ?next - road_type_road_0859 ?w - window_type_tw_00180)
    :precondition (and
      (ready-road ?v ?r)
      (road-next ?r ?next)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
    )
    :effect (and
      (not (ready-road ?v ?r))
      (ready-road ?v ?next)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-road_0176-road_0859-tw_00210
    :parameters (?v - vehicle ?r - road_type_road_0176 ?next - road_type_road_0859 ?w - window_type_tw_00210)
    :precondition (and
      (ready-road ?v ?r)
      (road-next ?r ?next)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
    )
    :effect (and
      (not (ready-road ?v ?r))
      (ready-road ?v ?next)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-road_0176-road_0859-tw_00240
    :parameters (?v - vehicle ?r - road_type_road_0176 ?next - road_type_road_0859 ?w - window_type_tw_00240)
    :precondition (and
      (ready-road ?v ?r)
      (road-next ?r ?next)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
    )
    :effect (and
      (not (ready-road ?v ?r))
      (ready-road ?v ?next)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-road_0176-road_0859-tw_00270
    :parameters (?v - vehicle ?r - road_type_road_0176 ?next - road_type_road_0859 ?w - window_type_tw_00270)
    :precondition (and
      (ready-road ?v ?r)
      (road-next ?r ?next)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
    )
    :effect (and
      (not (ready-road ?v ?r))
      (ready-road ?v ?next)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-road_0176-road_0859-tw_00300
    :parameters (?v - vehicle ?r - road_type_road_0176 ?next - road_type_road_0859 ?w - window_type_tw_00300)
    :precondition (and
      (ready-road ?v ?r)
      (road-next ?r ?next)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
    )
    :effect (and
      (not (ready-road ?v ?r))
      (ready-road ?v ?next)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-road_0176-road_0859-tw_00330
    :parameters (?v - vehicle ?r - road_type_road_0176 ?next - road_type_road_0859 ?w - window_type_tw_00330)
    :precondition (and
      (ready-road ?v ?r)
      (road-next ?r ?next)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
    )
    :effect (and
      (not (ready-road ?v ?r))
      (ready-road ?v ?next)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-road_0176-road_0859-tw_00360
    :parameters (?v - vehicle ?r - road_type_road_0176 ?next - road_type_road_0859 ?w - window_type_tw_00360)
    :precondition (and
      (ready-road ?v ?r)
      (road-next ?r ?next)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
    )
    :effect (and
      (not (ready-road ?v ?r))
      (ready-road ?v ?next)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-road_0176-road_0859-tw_00390
    :parameters (?v - vehicle ?r - road_type_road_0176 ?next - road_type_road_0859 ?w - window_type_tw_00390)
    :precondition (and
      (ready-road ?v ?r)
      (road-next ?r ?next)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
    )
    :effect (and
      (not (ready-road ?v ?r))
      (ready-road ?v ?next)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-road_0176-road_0859-tw_00420
    :parameters (?v - vehicle ?r - road_type_road_0176 ?next - road_type_road_0859 ?w - window_type_tw_00420)
    :precondition (and
      (ready-road ?v ?r)
      (road-next ?r ?next)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
    )
    :effect (and
      (not (ready-road ?v ?r))
      (ready-road ?v ?next)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-road_0176-road_0859-tw_00450
    :parameters (?v - vehicle ?r - road_type_road_0176 ?next - road_type_road_0859 ?w - window_type_tw_00450)
    :precondition (and
      (ready-road ?v ?r)
      (road-next ?r ?next)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
    )
    :effect (and
      (not (ready-road ?v ?r))
      (ready-road ?v ?next)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-road_0176-road_0859-tw_00480
    :parameters (?v - vehicle ?r - road_type_road_0176 ?next - road_type_road_0859 ?w - window_type_tw_00480)
    :precondition (and
      (ready-road ?v ?r)
      (road-next ?r ?next)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
    )
    :effect (and
      (not (ready-road ?v ?r))
      (ready-road ?v ?next)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-road_0176-road_0859-tw_00510
    :parameters (?v - vehicle ?r - road_type_road_0176 ?next - road_type_road_0859 ?w - window_type_tw_00510)
    :precondition (and
      (ready-road ?v ?r)
      (road-next ?r ?next)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
    )
    :effect (and
      (not (ready-road ?v ?r))
      (ready-road ?v ?next)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-road_0176-road_0859-tw_00540
    :parameters (?v - vehicle ?r - road_type_road_0176 ?next - road_type_road_0859 ?w - window_type_tw_00540)
    :precondition (and
      (ready-road ?v ?r)
      (road-next ?r ?next)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
    )
    :effect (and
      (not (ready-road ?v ?r))
      (ready-road ?v ?next)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-road_0176-road_0859-tw_00570
    :parameters (?v - vehicle ?r - road_type_road_0176 ?next - road_type_road_0859 ?w - window_type_tw_00570)
    :precondition (and
      (ready-road ?v ?r)
      (road-next ?r ?next)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
    )
    :effect (and
      (not (ready-road ?v ?r))
      (ready-road ?v ?next)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-road_0176-road_0859-tw_00600
    :parameters (?v - vehicle ?r - road_type_road_0176 ?next - road_type_road_0859 ?w - window_type_tw_00600)
    :precondition (and
      (ready-road ?v ?r)
      (road-next ?r ?next)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
    )
    :effect (and
      (not (ready-road ?v ?r))
      (ready-road ?v ?next)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-static-road_0177-road_0736
    :parameters (?v - vehicle ?r - road_type_road_0177 ?next - road_type_road_0736)
    :precondition (and
      (ready-road ?v ?r)
      (road-next ?r ?next)
      (road-open ?r)
      (static-road ?r)
      (not (road-blocked ?r))
    )
    :effect (and
      (not (ready-road ?v ?r))
      (ready-road ?v ?next)
      (increase (travel-time ?v) (travel-duration ?r))
      (increase (sim-time) (travel-duration ?r))
    )
  )

  (:action traverse-road-static-road_0178-road_0176
    :parameters (?v - vehicle ?r - road_type_road_0178 ?next - road_type_road_0176)
    :precondition (and
      (ready-road ?v ?r)
      (road-next ?r ?next)
      (road-open ?r)
      (static-road ?r)
      (not (road-blocked ?r))
    )
    :effect (and
      (not (ready-road ?v ?r))
      (ready-road ?v ?next)
      (increase (travel-time ?v) (travel-duration ?r))
      (increase (sim-time) (travel-duration ?r))
    )
  )

  (:action traverse-road-static-road_0179-road_0303
    :parameters (?v - vehicle ?r - road_type_road_0179 ?next - road_type_road_0303)
    :precondition (and
      (ready-road ?v ?r)
      (road-next ?r ?next)
      (road-open ?r)
      (static-road ?r)
      (not (road-blocked ?r))
    )
    :effect (and
      (not (ready-road ?v ?r))
      (ready-road ?v ?next)
      (increase (travel-time ?v) (travel-duration ?r))
      (increase (sim-time) (travel-duration ?r))
    )
  )

  (:action traverse-road-static-road_0180-macro_0045
    :parameters (?v - vehicle ?r - road_type_road_0180 ?next - road_type_macro_0045)
    :precondition (and
      (ready-road ?v ?r)
      (road-next ?r ?next)
      (road-open ?r)
      (static-road ?r)
      (not (road-blocked ?r))
    )
    :effect (and
      (not (ready-road ?v ?r))
      (ready-road ?v ?next)
      (increase (travel-time ?v) (travel-duration ?r))
      (increase (sim-time) (travel-duration ?r))
    )
  )

  (:action traverse-road-static-road_0180-road_0639
    :parameters (?v - vehicle ?r - road_type_road_0180 ?next - road_type_road_0639)
    :precondition (and
      (ready-road ?v ?r)
      (road-next ?r ?next)
      (road-open ?r)
      (static-road ?r)
      (not (road-blocked ?r))
    )
    :effect (and
      (not (ready-road ?v ?r))
      (ready-road ?v ?next)
      (increase (travel-time ?v) (travel-duration ?r))
      (increase (sim-time) (travel-duration ?r))
    )
  )

  (:action traverse-road-static-road_0181-road_0606
    :parameters (?v - vehicle ?r - road_type_road_0181 ?next - road_type_road_0606)
    :precondition (and
      (ready-road ?v ?r)
      (road-next ?r ?next)
      (road-open ?r)
      (static-road ?r)
      (not (road-blocked ?r))
    )
    :effect (and
      (not (ready-road ?v ?r))
      (ready-road ?v ?next)
      (increase (travel-time ?v) (travel-duration ?r))
      (increase (sim-time) (travel-duration ?r))
    )
  )

  (:action traverse-road-static-road_0181-road_0607
    :parameters (?v - vehicle ?r - road_type_road_0181 ?next - road_type_road_0607)
    :precondition (and
      (ready-road ?v ?r)
      (road-next ?r ?next)
      (road-open ?r)
      (static-road ?r)
      (not (road-blocked ?r))
    )
    :effect (and
      (not (ready-road ?v ?r))
      (ready-road ?v ?next)
      (increase (travel-time ?v) (travel-duration ?r))
      (increase (sim-time) (travel-duration ?r))
    )
  )

  (:action traverse-road-static-road_0182-road_0034
    :parameters (?v - vehicle ?r - road_type_road_0182 ?next - road_type_road_0034)
    :precondition (and
      (ready-road ?v ?r)
      (road-next ?r ?next)
      (road-open ?r)
      (static-road ?r)
      (not (road-blocked ?r))
    )
    :effect (and
      (not (ready-road ?v ?r))
      (ready-road ?v ?next)
      (increase (travel-time ?v) (travel-duration ?r))
      (increase (sim-time) (travel-duration ?r))
    )
  )

  (:action traverse-road-static-road_0182-road_0035
    :parameters (?v - vehicle ?r - road_type_road_0182 ?next - road_type_road_0035)
    :precondition (and
      (ready-road ?v ?r)
      (road-next ?r ?next)
      (road-open ?r)
      (static-road ?r)
      (not (road-blocked ?r))
    )
    :effect (and
      (not (ready-road ?v ?r))
      (ready-road ?v ?next)
      (increase (travel-time ?v) (travel-duration ?r))
      (increase (sim-time) (travel-duration ?r))
    )
  )

  (:action traverse-road-static-road_0182-road_0036
    :parameters (?v - vehicle ?r - road_type_road_0182 ?next - road_type_road_0036)
    :precondition (and
      (ready-road ?v ?r)
      (road-next ?r ?next)
      (road-open ?r)
      (static-road ?r)
      (not (road-blocked ?r))
    )
    :effect (and
      (not (ready-road ?v ?r))
      (ready-road ?v ?next)
      (increase (travel-time ?v) (travel-duration ?r))
      (increase (sim-time) (travel-duration ?r))
    )
  )

  (:action traverse-road-static-road_0183-road_0184
    :parameters (?v - vehicle ?r - road_type_road_0183 ?next - road_type_road_0184)
    :precondition (and
      (ready-road ?v ?r)
      (road-next ?r ?next)
      (road-open ?r)
      (static-road ?r)
      (not (road-blocked ?r))
    )
    :effect (and
      (not (ready-road ?v ?r))
      (ready-road ?v ?next)
      (increase (travel-time ?v) (travel-duration ?r))
      (increase (sim-time) (travel-duration ?r))
    )
  )

  (:action traverse-road-static-road_0184-road_0182
    :parameters (?v - vehicle ?r - road_type_road_0184 ?next - road_type_road_0182)
    :precondition (and
      (ready-road ?v ?r)
      (road-next ?r ?next)
      (road-open ?r)
      (static-road ?r)
      (not (road-blocked ?r))
    )
    :effect (and
      (not (ready-road ?v ?r))
      (ready-road ?v ?next)
      (increase (travel-time ?v) (travel-duration ?r))
      (increase (sim-time) (travel-duration ?r))
    )
  )

  (:action traverse-road-static-road_0184-road_0183
    :parameters (?v - vehicle ?r - road_type_road_0184 ?next - road_type_road_0183)
    :precondition (and
      (ready-road ?v ?r)
      (road-next ?r ?next)
      (road-open ?r)
      (static-road ?r)
      (not (road-blocked ?r))
    )
    :effect (and
      (not (ready-road ?v ?r))
      (ready-road ?v ?next)
      (increase (travel-time ?v) (travel-duration ?r))
      (increase (sim-time) (travel-duration ?r))
    )
  )

  (:action traverse-road-static-road_0187-road_0012
    :parameters (?v - vehicle ?r - road_type_road_0187 ?next - road_type_road_0012)
    :precondition (and
      (ready-road ?v ?r)
      (road-next ?r ?next)
      (road-open ?r)
      (static-road ?r)
      (not (road-blocked ?r))
    )
    :effect (and
      (not (ready-road ?v ?r))
      (ready-road ?v ?next)
      (increase (travel-time ?v) (travel-duration ?r))
      (increase (sim-time) (travel-duration ?r))
    )
  )

  (:action traverse-road-static-road_0187-road_0013
    :parameters (?v - vehicle ?r - road_type_road_0187 ?next - road_type_road_0013)
    :precondition (and
      (ready-road ?v ?r)
      (road-next ?r ?next)
      (road-open ?r)
      (static-road ?r)
      (not (road-blocked ?r))
    )
    :effect (and
      (not (ready-road ?v ?r))
      (ready-road ?v ?next)
      (increase (travel-time ?v) (travel-duration ?r))
      (increase (sim-time) (travel-duration ?r))
    )
  )

  (:action traverse-road-static-road_0188-road_0189
    :parameters (?v - vehicle ?r - road_type_road_0188 ?next - road_type_road_0189)
    :precondition (and
      (ready-road ?v ?r)
      (road-next ?r ?next)
      (road-open ?r)
      (static-road ?r)
      (not (road-blocked ?r))
    )
    :effect (and
      (not (ready-road ?v ?r))
      (ready-road ?v ?next)
      (increase (travel-time ?v) (travel-duration ?r))
      (increase (sim-time) (travel-duration ?r))
    )
  )

  (:action traverse-road-static-road_0189-road_0187
    :parameters (?v - vehicle ?r - road_type_road_0189 ?next - road_type_road_0187)
    :precondition (and
      (ready-road ?v ?r)
      (road-next ?r ?next)
      (road-open ?r)
      (static-road ?r)
      (not (road-blocked ?r))
    )
    :effect (and
      (not (ready-road ?v ?r))
      (ready-road ?v ?next)
      (increase (travel-time ?v) (travel-duration ?r))
      (increase (sim-time) (travel-duration ?r))
    )
  )

  (:action traverse-road-static-road_0189-road_0188
    :parameters (?v - vehicle ?r - road_type_road_0189 ?next - road_type_road_0188)
    :precondition (and
      (ready-road ?v ?r)
      (road-next ?r ?next)
      (road-open ?r)
      (static-road ?r)
      (not (road-blocked ?r))
    )
    :effect (and
      (not (ready-road ?v ?r))
      (ready-road ?v ?next)
      (increase (travel-time ?v) (travel-duration ?r))
      (increase (sim-time) (travel-duration ?r))
    )
  )

  (:action traverse-road-static-road_0191-road_0762
    :parameters (?v - vehicle ?r - road_type_road_0191 ?next - road_type_road_0762)
    :precondition (and
      (ready-road ?v ?r)
      (road-next ?r ?next)
      (road-open ?r)
      (static-road ?r)
      (not (road-blocked ?r))
    )
    :effect (and
      (not (ready-road ?v ?r))
      (ready-road ?v ?next)
      (increase (travel-time ?v) (travel-duration ?r))
      (increase (sim-time) (travel-duration ?r))
    )
  )

  (:action traverse-road-static-road_0191-road_0763
    :parameters (?v - vehicle ?r - road_type_road_0191 ?next - road_type_road_0763)
    :precondition (and
      (ready-road ?v ?r)
      (road-next ?r ?next)
      (road-open ?r)
      (static-road ?r)
      (not (road-blocked ?r))
    )
    :effect (and
      (not (ready-road ?v ?r))
      (ready-road ?v ?next)
      (increase (travel-time ?v) (travel-duration ?r))
      (increase (sim-time) (travel-duration ?r))
    )
  )

  (:action traverse-road-static-road_0192-road_0012
    :parameters (?v - vehicle ?r - road_type_road_0192 ?next - road_type_road_0012)
    :precondition (and
      (ready-road ?v ?r)
      (road-next ?r ?next)
      (road-open ?r)
      (static-road ?r)
      (not (road-blocked ?r))
    )
    :effect (and
      (not (ready-road ?v ?r))
      (ready-road ?v ?next)
      (increase (travel-time ?v) (travel-duration ?r))
      (increase (sim-time) (travel-duration ?r))
    )
  )

  (:action traverse-road-static-road_0192-road_0013
    :parameters (?v - vehicle ?r - road_type_road_0192 ?next - road_type_road_0013)
    :precondition (and
      (ready-road ?v ?r)
      (road-next ?r ?next)
      (road-open ?r)
      (static-road ?r)
      (not (road-blocked ?r))
    )
    :effect (and
      (not (ready-road ?v ?r))
      (ready-road ?v ?next)
      (increase (travel-time ?v) (travel-duration ?r))
      (increase (sim-time) (travel-duration ?r))
    )
  )

  (:action traverse-road-static-road_0193-macro_0005
    :parameters (?v - vehicle ?r - road_type_road_0193 ?next - road_type_macro_0005)
    :precondition (and
      (ready-road ?v ?r)
      (road-next ?r ?next)
      (road-open ?r)
      (static-road ?r)
      (not (road-blocked ?r))
    )
    :effect (and
      (not (ready-road ?v ?r))
      (ready-road ?v ?next)
      (increase (travel-time ?v) (travel-duration ?r))
      (increase (sim-time) (travel-duration ?r))
    )
  )

  (:action traverse-road-static-road_0193-road_0061
    :parameters (?v - vehicle ?r - road_type_road_0193 ?next - road_type_road_0061)
    :precondition (and
      (ready-road ?v ?r)
      (road-next ?r ?next)
      (road-open ?r)
      (static-road ?r)
      (not (road-blocked ?r))
    )
    :effect (and
      (not (ready-road ?v ?r))
      (ready-road ?v ?next)
      (increase (travel-time ?v) (travel-duration ?r))
      (increase (sim-time) (travel-duration ?r))
    )
  )

  (:action traverse-road-static-road_0194-road_0848
    :parameters (?v - vehicle ?r - road_type_road_0194 ?next - road_type_road_0848)
    :precondition (and
      (ready-road ?v ?r)
      (road-next ?r ?next)
      (road-open ?r)
      (static-road ?r)
      (not (road-blocked ?r))
    )
    :effect (and
      (not (ready-road ?v ?r))
      (ready-road ?v ?next)
      (increase (travel-time ?v) (travel-duration ?r))
      (increase (sim-time) (travel-duration ?r))
    )
  )

  (:action traverse-road-static-road_0194-road_0849
    :parameters (?v - vehicle ?r - road_type_road_0194 ?next - road_type_road_0849)
    :precondition (and
      (ready-road ?v ?r)
      (road-next ?r ?next)
      (road-open ?r)
      (static-road ?r)
      (not (road-blocked ?r))
    )
    :effect (and
      (not (ready-road ?v ?r))
      (ready-road ?v ?next)
      (increase (travel-time ?v) (travel-duration ?r))
      (increase (sim-time) (travel-duration ?r))
    )
  )

  (:action traverse-road-static-road_0195-road_0323
    :parameters (?v - vehicle ?r - road_type_road_0195 ?next - road_type_road_0323)
    :precondition (and
      (ready-road ?v ?r)
      (road-next ?r ?next)
      (road-open ?r)
      (static-road ?r)
      (not (road-blocked ?r))
    )
    :effect (and
      (not (ready-road ?v ?r))
      (ready-road ?v ?next)
      (increase (travel-time ?v) (travel-duration ?r))
      (increase (sim-time) (travel-duration ?r))
    )
  )

  (:action traverse-road-static-road_0195-road_0324
    :parameters (?v - vehicle ?r - road_type_road_0195 ?next - road_type_road_0324)
    :precondition (and
      (ready-road ?v ?r)
      (road-next ?r ?next)
      (road-open ?r)
      (static-road ?r)
      (not (road-blocked ?r))
    )
    :effect (and
      (not (ready-road ?v ?r))
      (ready-road ?v ?next)
      (increase (travel-time ?v) (travel-duration ?r))
      (increase (sim-time) (travel-duration ?r))
    )
  )

  (:action traverse-road-static-road_0196-road_0359
    :parameters (?v - vehicle ?r - road_type_road_0196 ?next - road_type_road_0359)
    :precondition (and
      (ready-road ?v ?r)
      (road-next ?r ?next)
      (road-open ?r)
      (static-road ?r)
      (not (road-blocked ?r))
    )
    :effect (and
      (not (ready-road ?v ?r))
      (ready-road ?v ?next)
      (increase (travel-time ?v) (travel-duration ?r))
      (increase (sim-time) (travel-duration ?r))
    )
  )

  (:action traverse-road-static-road_0196-road_0360
    :parameters (?v - vehicle ?r - road_type_road_0196 ?next - road_type_road_0360)
    :precondition (and
      (ready-road ?v ?r)
      (road-next ?r ?next)
      (road-open ?r)
      (static-road ?r)
      (not (road-blocked ?r))
    )
    :effect (and
      (not (ready-road ?v ?r))
      (ready-road ?v ?next)
      (increase (travel-time ?v) (travel-duration ?r))
      (increase (sim-time) (travel-duration ?r))
    )
  )

  (:action traverse-road-static-road_0197-road_0246
    :parameters (?v - vehicle ?r - road_type_road_0197 ?next - road_type_road_0246)
    :precondition (and
      (ready-road ?v ?r)
      (road-next ?r ?next)
      (road-open ?r)
      (static-road ?r)
      (not (road-blocked ?r))
    )
    :effect (and
      (not (ready-road ?v ?r))
      (ready-road ?v ?next)
      (increase (travel-time ?v) (travel-duration ?r))
      (increase (sim-time) (travel-duration ?r))
    )
  )

  (:action traverse-road-static-road_0198-road_0397
    :parameters (?v - vehicle ?r - road_type_road_0198 ?next - road_type_road_0397)
    :precondition (and
      (ready-road ?v ?r)
      (road-next ?r ?next)
      (road-open ?r)
      (static-road ?r)
      (not (road-blocked ?r))
    )
    :effect (and
      (not (ready-road ?v ?r))
      (ready-road ?v ?next)
      (increase (travel-time ?v) (travel-duration ?r))
      (increase (sim-time) (travel-duration ?r))
    )
  )

  (:action traverse-road-static-road_0198-road_0398
    :parameters (?v - vehicle ?r - road_type_road_0198 ?next - road_type_road_0398)
    :precondition (and
      (ready-road ?v ?r)
      (road-next ?r ?next)
      (road-open ?r)
      (static-road ?r)
      (not (road-blocked ?r))
    )
    :effect (and
      (not (ready-road ?v ?r))
      (ready-road ?v ?next)
      (increase (travel-time ?v) (travel-duration ?r))
      (increase (sim-time) (travel-duration ?r))
    )
  )

  (:action traverse-road-static-road_0199-road_0447
    :parameters (?v - vehicle ?r - road_type_road_0199 ?next - road_type_road_0447)
    :precondition (and
      (ready-road ?v ?r)
      (road-next ?r ?next)
      (road-open ?r)
      (static-road ?r)
      (not (road-blocked ?r))
    )
    :effect (and
      (not (ready-road ?v ?r))
      (ready-road ?v ?next)
      (increase (travel-time ?v) (travel-duration ?r))
      (increase (sim-time) (travel-duration ?r))
    )
  )

  (:action traverse-road-static-road_0199-road_0448
    :parameters (?v - vehicle ?r - road_type_road_0199 ?next - road_type_road_0448)
    :precondition (and
      (ready-road ?v ?r)
      (road-next ?r ?next)
      (road-open ?r)
      (static-road ?r)
      (not (road-blocked ?r))
    )
    :effect (and
      (not (ready-road ?v ?r))
      (ready-road ?v ?next)
      (increase (travel-time ?v) (travel-duration ?r))
      (increase (sim-time) (travel-duration ?r))
    )
  )

  (:action traverse-road-static-road_0200-road_0680
    :parameters (?v - vehicle ?r - road_type_road_0200 ?next - road_type_road_0680)
    :precondition (and
      (ready-road ?v ?r)
      (road-next ?r ?next)
      (road-open ?r)
      (static-road ?r)
      (not (road-blocked ?r))
    )
    :effect (and
      (not (ready-road ?v ?r))
      (ready-road ?v ?next)
      (increase (travel-time ?v) (travel-duration ?r))
      (increase (sim-time) (travel-duration ?r))
    )
  )

  (:action traverse-road-static-road_0200-road_0681
    :parameters (?v - vehicle ?r - road_type_road_0200 ?next - road_type_road_0681)
    :precondition (and
      (ready-road ?v ?r)
      (road-next ?r ?next)
      (road-open ?r)
      (static-road ?r)
      (not (road-blocked ?r))
    )
    :effect (and
      (not (ready-road ?v ?r))
      (ready-road ?v ?next)
      (increase (travel-time ?v) (travel-duration ?r))
      (increase (sim-time) (travel-duration ?r))
    )
  )

  (:action traverse-road-static-road_0201-road_0203
    :parameters (?v - vehicle ?r - road_type_road_0201 ?next - road_type_road_0203)
    :precondition (and
      (ready-road ?v ?r)
      (road-next ?r ?next)
      (road-open ?r)
      (static-road ?r)
      (not (road-blocked ?r))
    )
    :effect (and
      (not (ready-road ?v ?r))
      (ready-road ?v ?next)
      (increase (travel-time ?v) (travel-duration ?r))
      (increase (sim-time) (travel-duration ?r))
    )
  )

  (:action traverse-road-static-road_0202-road_0266
    :parameters (?v - vehicle ?r - road_type_road_0202 ?next - road_type_road_0266)
    :precondition (and
      (ready-road ?v ?r)
      (road-next ?r ?next)
      (road-open ?r)
      (static-road ?r)
      (not (road-blocked ?r))
    )
    :effect (and
      (not (ready-road ?v ?r))
      (ready-road ?v ?next)
      (increase (travel-time ?v) (travel-duration ?r))
      (increase (sim-time) (travel-duration ?r))
    )
  )

  (:action traverse-road-static-road_0203-road_0321
    :parameters (?v - vehicle ?r - road_type_road_0203 ?next - road_type_road_0321)
    :precondition (and
      (ready-road ?v ?r)
      (road-next ?r ?next)
      (road-open ?r)
      (static-road ?r)
      (not (road-blocked ?r))
    )
    :effect (and
      (not (ready-road ?v ?r))
      (ready-road ?v ?next)
      (increase (travel-time ?v) (travel-duration ?r))
      (increase (sim-time) (travel-duration ?r))
    )
  )

  (:action traverse-road-static-road_0203-road_0322
    :parameters (?v - vehicle ?r - road_type_road_0203 ?next - road_type_road_0322)
    :precondition (and
      (ready-road ?v ?r)
      (road-next ?r ?next)
      (road-open ?r)
      (static-road ?r)
      (not (road-blocked ?r))
    )
    :effect (and
      (not (ready-road ?v ?r))
      (ready-road ?v ?next)
      (increase (travel-time ?v) (travel-duration ?r))
      (increase (sim-time) (travel-duration ?r))
    )
  )

  (:action traverse-road-static-road_0204-road_0298
    :parameters (?v - vehicle ?r - road_type_road_0204 ?next - road_type_road_0298)
    :precondition (and
      (ready-road ?v ?r)
      (road-next ?r ?next)
      (road-open ?r)
      (static-road ?r)
      (not (road-blocked ?r))
    )
    :effect (and
      (not (ready-road ?v ?r))
      (ready-road ?v ?next)
      (increase (travel-time ?v) (travel-duration ?r))
      (increase (sim-time) (travel-duration ?r))
    )
  )

  (:action traverse-road-static-road_0204-road_0299
    :parameters (?v - vehicle ?r - road_type_road_0204 ?next - road_type_road_0299)
    :precondition (and
      (ready-road ?v ?r)
      (road-next ?r ?next)
      (road-open ?r)
      (static-road ?r)
      (not (road-blocked ?r))
    )
    :effect (and
      (not (ready-road ?v ?r))
      (ready-road ?v ?next)
      (increase (travel-time ?v) (travel-duration ?r))
      (increase (sim-time) (travel-duration ?r))
    )
  )

  (:action traverse-road-static-road_0204-road_0300
    :parameters (?v - vehicle ?r - road_type_road_0204 ?next - road_type_road_0300)
    :precondition (and
      (ready-road ?v ?r)
      (road-next ?r ?next)
      (road-open ?r)
      (static-road ?r)
      (not (road-blocked ?r))
    )
    :effect (and
      (not (ready-road ?v ?r))
      (ready-road ?v ?next)
      (increase (travel-time ?v) (travel-duration ?r))
      (increase (sim-time) (travel-duration ?r))
    )
  )

  (:action traverse-road-static-road_0206-road_0776
    :parameters (?v - vehicle ?r - road_type_road_0206 ?next - road_type_road_0776)
    :precondition (and
      (ready-road ?v ?r)
      (road-next ?r ?next)
      (road-open ?r)
      (static-road ?r)
      (not (road-blocked ?r))
    )
    :effect (and
      (not (ready-road ?v ?r))
      (ready-road ?v ?next)
      (increase (travel-time ?v) (travel-duration ?r))
      (increase (sim-time) (travel-duration ?r))
    )
  )

  (:action traverse-road-static-road_0206-road_0777
    :parameters (?v - vehicle ?r - road_type_road_0206 ?next - road_type_road_0777)
    :precondition (and
      (ready-road ?v ?r)
      (road-next ?r ?next)
      (road-open ?r)
      (static-road ?r)
      (not (road-blocked ?r))
    )
    :effect (and
      (not (ready-road ?v ?r))
      (ready-road ?v ?next)
      (increase (travel-time ?v) (travel-duration ?r))
      (increase (sim-time) (travel-duration ?r))
    )
  )

  (:action traverse-road-static-road_0206-road_0778
    :parameters (?v - vehicle ?r - road_type_road_0206 ?next - road_type_road_0778)
    :precondition (and
      (ready-road ?v ?r)
      (road-next ?r ?next)
      (road-open ?r)
      (static-road ?r)
      (not (road-blocked ?r))
    )
    :effect (and
      (not (ready-road ?v ?r))
      (ready-road ?v ?next)
      (increase (travel-time ?v) (travel-duration ?r))
      (increase (sim-time) (travel-duration ?r))
    )
  )

  (:action traverse-road-static-road_0207-road_0135
    :parameters (?v - vehicle ?r - road_type_road_0207 ?next - road_type_road_0135)
    :precondition (and
      (ready-road ?v ?r)
      (road-next ?r ?next)
      (road-open ?r)
      (static-road ?r)
      (not (road-blocked ?r))
    )
    :effect (and
      (not (ready-road ?v ?r))
      (ready-road ?v ?next)
      (increase (travel-time ?v) (travel-duration ?r))
      (increase (sim-time) (travel-duration ?r))
    )
  )

  (:action traverse-road-static-road_0207-road_0136
    :parameters (?v - vehicle ?r - road_type_road_0207 ?next - road_type_road_0136)
    :precondition (and
      (ready-road ?v ?r)
      (road-next ?r ?next)
      (road-open ?r)
      (static-road ?r)
      (not (road-blocked ?r))
    )
    :effect (and
      (not (ready-road ?v ?r))
      (ready-road ?v ?next)
      (increase (travel-time ?v) (travel-duration ?r))
      (increase (sim-time) (travel-duration ?r))
    )
  )

  (:action traverse-road-static-road_0208-road_0691
    :parameters (?v - vehicle ?r - road_type_road_0208 ?next - road_type_road_0691)
    :precondition (and
      (ready-road ?v ?r)
      (road-next ?r ?next)
      (road-open ?r)
      (static-road ?r)
      (not (road-blocked ?r))
    )
    :effect (and
      (not (ready-road ?v ?r))
      (ready-road ?v ?next)
      (increase (travel-time ?v) (travel-duration ?r))
      (increase (sim-time) (travel-duration ?r))
    )
  )

  (:action traverse-road-static-road_0208-road_0692
    :parameters (?v - vehicle ?r - road_type_road_0208 ?next - road_type_road_0692)
    :precondition (and
      (ready-road ?v ?r)
      (road-next ?r ?next)
      (road-open ?r)
      (static-road ?r)
      (not (road-blocked ?r))
    )
    :effect (and
      (not (ready-road ?v ?r))
      (ready-road ?v ?next)
      (increase (travel-time ?v) (travel-duration ?r))
      (increase (sim-time) (travel-duration ?r))
    )
  )

  (:action traverse-road-static-road_0208-road_0693
    :parameters (?v - vehicle ?r - road_type_road_0208 ?next - road_type_road_0693)
    :precondition (and
      (ready-road ?v ?r)
      (road-next ?r ?next)
      (road-open ?r)
      (static-road ?r)
      (not (road-blocked ?r))
    )
    :effect (and
      (not (ready-road ?v ?r))
      (ready-road ?v ?next)
      (increase (travel-time ?v) (travel-duration ?r))
      (increase (sim-time) (travel-duration ?r))
    )
  )

  (:action traverse-road-static-road_0209-road_0295
    :parameters (?v - vehicle ?r - road_type_road_0209 ?next - road_type_road_0295)
    :precondition (and
      (ready-road ?v ?r)
      (road-next ?r ?next)
      (road-open ?r)
      (static-road ?r)
      (not (road-blocked ?r))
    )
    :effect (and
      (not (ready-road ?v ?r))
      (ready-road ?v ?next)
      (increase (travel-time ?v) (travel-duration ?r))
      (increase (sim-time) (travel-duration ?r))
    )
  )

  (:action traverse-road-static-road_0209-road_0296
    :parameters (?v - vehicle ?r - road_type_road_0209 ?next - road_type_road_0296)
    :precondition (and
      (ready-road ?v ?r)
      (road-next ?r ?next)
      (road-open ?r)
      (static-road ?r)
      (not (road-blocked ?r))
    )
    :effect (and
      (not (ready-road ?v ?r))
      (ready-road ?v ?next)
      (increase (travel-time ?v) (travel-duration ?r))
      (increase (sim-time) (travel-duration ?r))
    )
  )

  (:action traverse-road-static-road_0210-road_0479
    :parameters (?v - vehicle ?r - road_type_road_0210 ?next - road_type_road_0479)
    :precondition (and
      (ready-road ?v ?r)
      (road-next ?r ?next)
      (road-open ?r)
      (static-road ?r)
      (not (road-blocked ?r))
    )
    :effect (and
      (not (ready-road ?v ?r))
      (ready-road ?v ?next)
      (increase (travel-time ?v) (travel-duration ?r))
      (increase (sim-time) (travel-duration ?r))
    )
  )

  (:action traverse-road-static-road_0211-road_0117
    :parameters (?v - vehicle ?r - road_type_road_0211 ?next - road_type_road_0117)
    :precondition (and
      (ready-road ?v ?r)
      (road-next ?r ?next)
      (road-open ?r)
      (static-road ?r)
      (not (road-blocked ?r))
    )
    :effect (and
      (not (ready-road ?v ?r))
      (ready-road ?v ?next)
      (increase (travel-time ?v) (travel-duration ?r))
      (increase (sim-time) (travel-duration ?r))
    )
  )

  (:action traverse-road-static-road_0211-road_0118
    :parameters (?v - vehicle ?r - road_type_road_0211 ?next - road_type_road_0118)
    :precondition (and
      (ready-road ?v ?r)
      (road-next ?r ?next)
      (road-open ?r)
      (static-road ?r)
      (not (road-blocked ?r))
    )
    :effect (and
      (not (ready-road ?v ?r))
      (ready-road ?v ?next)
      (increase (travel-time ?v) (travel-duration ?r))
      (increase (sim-time) (travel-duration ?r))
    )
  )

  (:action traverse-road-static-road_0211-road_0119
    :parameters (?v - vehicle ?r - road_type_road_0211 ?next - road_type_road_0119)
    :precondition (and
      (ready-road ?v ?r)
      (road-next ?r ?next)
      (road-open ?r)
      (static-road ?r)
      (not (road-blocked ?r))
    )
    :effect (and
      (not (ready-road ?v ?r))
      (ready-road ?v ?next)
      (increase (travel-time ?v) (travel-duration ?r))
      (increase (sim-time) (travel-duration ?r))
    )
  )

  (:action traverse-road-static-road_0212-road_0213
    :parameters (?v - vehicle ?r - road_type_road_0212 ?next - road_type_road_0213)
    :precondition (and
      (ready-road ?v ?r)
      (road-next ?r ?next)
      (road-open ?r)
      (static-road ?r)
      (not (road-blocked ?r))
    )
    :effect (and
      (not (ready-road ?v ?r))
      (ready-road ?v ?next)
      (increase (travel-time ?v) (travel-duration ?r))
      (increase (sim-time) (travel-duration ?r))
    )
  )

  (:action traverse-road-static-road_0212-road_0214
    :parameters (?v - vehicle ?r - road_type_road_0212 ?next - road_type_road_0214)
    :precondition (and
      (ready-road ?v ?r)
      (road-next ?r ?next)
      (road-open ?r)
      (static-road ?r)
      (not (road-blocked ?r))
    )
    :effect (and
      (not (ready-road ?v ?r))
      (ready-road ?v ?next)
      (increase (travel-time ?v) (travel-duration ?r))
      (increase (sim-time) (travel-duration ?r))
    )
  )

  (:action traverse-road-static-road_0212-road_0215
    :parameters (?v - vehicle ?r - road_type_road_0212 ?next - road_type_road_0215)
    :precondition (and
      (ready-road ?v ?r)
      (road-next ?r ?next)
      (road-open ?r)
      (static-road ?r)
      (not (road-blocked ?r))
    )
    :effect (and
      (not (ready-road ?v ?r))
      (ready-road ?v ?next)
      (increase (travel-time ?v) (travel-duration ?r))
      (increase (sim-time) (travel-duration ?r))
    )
  )

  (:action traverse-road-static-road_0213-road_0010
    :parameters (?v - vehicle ?r - road_type_road_0213 ?next - road_type_road_0010)
    :precondition (and
      (ready-road ?v ?r)
      (road-next ?r ?next)
      (road-open ?r)
      (static-road ?r)
      (not (road-blocked ?r))
    )
    :effect (and
      (not (ready-road ?v ?r))
      (ready-road ?v ?next)
      (increase (travel-time ?v) (travel-duration ?r))
      (increase (sim-time) (travel-duration ?r))
    )
  )

  (:action traverse-road-static-road_0214-road_0211
    :parameters (?v - vehicle ?r - road_type_road_0214 ?next - road_type_road_0211)
    :precondition (and
      (ready-road ?v ?r)
      (road-next ?r ?next)
      (road-open ?r)
      (static-road ?r)
      (not (road-blocked ?r))
    )
    :effect (and
      (not (ready-road ?v ?r))
      (ready-road ?v ?next)
      (increase (travel-time ?v) (travel-duration ?r))
      (increase (sim-time) (travel-duration ?r))
    )
  )

  (:action traverse-road-static-road_0214-road_0212
    :parameters (?v - vehicle ?r - road_type_road_0214 ?next - road_type_road_0212)
    :precondition (and
      (ready-road ?v ?r)
      (road-next ?r ?next)
      (road-open ?r)
      (static-road ?r)
      (not (road-blocked ?r))
    )
    :effect (and
      (not (ready-road ?v ?r))
      (ready-road ?v ?next)
      (increase (travel-time ?v) (travel-duration ?r))
      (increase (sim-time) (travel-duration ?r))
    )
  )

  (:action traverse-road-static-road_0215-road_0716
    :parameters (?v - vehicle ?r - road_type_road_0215 ?next - road_type_road_0716)
    :precondition (and
      (ready-road ?v ?r)
      (road-next ?r ?next)
      (road-open ?r)
      (static-road ?r)
      (not (road-blocked ?r))
    )
    :effect (and
      (not (ready-road ?v ?r))
      (ready-road ?v ?next)
      (increase (travel-time ?v) (travel-duration ?r))
      (increase (sim-time) (travel-duration ?r))
    )
  )

  (:action traverse-road-static-road_0215-road_0717
    :parameters (?v - vehicle ?r - road_type_road_0215 ?next - road_type_road_0717)
    :precondition (and
      (ready-road ?v ?r)
      (road-next ?r ?next)
      (road-open ?r)
      (static-road ?r)
      (not (road-blocked ?r))
    )
    :effect (and
      (not (ready-road ?v ?r))
      (ready-road ?v ?next)
      (increase (travel-time ?v) (travel-duration ?r))
      (increase (sim-time) (travel-duration ?r))
    )
  )

  (:action traverse-road-dynamic-road_0216-road_0657-tw_00000
    :parameters (?v - vehicle ?r - road_type_road_0216 ?next - road_type_road_0657 ?w - window_type_tw_00000)
    :precondition (and
      (ready-road ?v ?r)
      (road-next ?r ?next)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
    )
    :effect (and
      (not (ready-road ?v ?r))
      (ready-road ?v ?next)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-road_0216-road_0657-tw_00030
    :parameters (?v - vehicle ?r - road_type_road_0216 ?next - road_type_road_0657 ?w - window_type_tw_00030)
    :precondition (and
      (ready-road ?v ?r)
      (road-next ?r ?next)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
    )
    :effect (and
      (not (ready-road ?v ?r))
      (ready-road ?v ?next)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-road_0216-road_0657-tw_00060
    :parameters (?v - vehicle ?r - road_type_road_0216 ?next - road_type_road_0657 ?w - window_type_tw_00060)
    :precondition (and
      (ready-road ?v ?r)
      (road-next ?r ?next)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
    )
    :effect (and
      (not (ready-road ?v ?r))
      (ready-road ?v ?next)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-road_0216-road_0657-tw_00090
    :parameters (?v - vehicle ?r - road_type_road_0216 ?next - road_type_road_0657 ?w - window_type_tw_00090)
    :precondition (and
      (ready-road ?v ?r)
      (road-next ?r ?next)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
    )
    :effect (and
      (not (ready-road ?v ?r))
      (ready-road ?v ?next)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-road_0216-road_0657-tw_00120
    :parameters (?v - vehicle ?r - road_type_road_0216 ?next - road_type_road_0657 ?w - window_type_tw_00120)
    :precondition (and
      (ready-road ?v ?r)
      (road-next ?r ?next)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
    )
    :effect (and
      (not (ready-road ?v ?r))
      (ready-road ?v ?next)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-road_0216-road_0657-tw_00150
    :parameters (?v - vehicle ?r - road_type_road_0216 ?next - road_type_road_0657 ?w - window_type_tw_00150)
    :precondition (and
      (ready-road ?v ?r)
      (road-next ?r ?next)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
    )
    :effect (and
      (not (ready-road ?v ?r))
      (ready-road ?v ?next)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-road_0216-road_0657-tw_00180
    :parameters (?v - vehicle ?r - road_type_road_0216 ?next - road_type_road_0657 ?w - window_type_tw_00180)
    :precondition (and
      (ready-road ?v ?r)
      (road-next ?r ?next)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
    )
    :effect (and
      (not (ready-road ?v ?r))
      (ready-road ?v ?next)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-road_0216-road_0657-tw_00210
    :parameters (?v - vehicle ?r - road_type_road_0216 ?next - road_type_road_0657 ?w - window_type_tw_00210)
    :precondition (and
      (ready-road ?v ?r)
      (road-next ?r ?next)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
    )
    :effect (and
      (not (ready-road ?v ?r))
      (ready-road ?v ?next)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-road_0216-road_0657-tw_00240
    :parameters (?v - vehicle ?r - road_type_road_0216 ?next - road_type_road_0657 ?w - window_type_tw_00240)
    :precondition (and
      (ready-road ?v ?r)
      (road-next ?r ?next)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
    )
    :effect (and
      (not (ready-road ?v ?r))
      (ready-road ?v ?next)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-road_0216-road_0657-tw_00270
    :parameters (?v - vehicle ?r - road_type_road_0216 ?next - road_type_road_0657 ?w - window_type_tw_00270)
    :precondition (and
      (ready-road ?v ?r)
      (road-next ?r ?next)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
    )
    :effect (and
      (not (ready-road ?v ?r))
      (ready-road ?v ?next)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-road_0216-road_0657-tw_00300
    :parameters (?v - vehicle ?r - road_type_road_0216 ?next - road_type_road_0657 ?w - window_type_tw_00300)
    :precondition (and
      (ready-road ?v ?r)
      (road-next ?r ?next)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
    )
    :effect (and
      (not (ready-road ?v ?r))
      (ready-road ?v ?next)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-road_0216-road_0657-tw_00330
    :parameters (?v - vehicle ?r - road_type_road_0216 ?next - road_type_road_0657 ?w - window_type_tw_00330)
    :precondition (and
      (ready-road ?v ?r)
      (road-next ?r ?next)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
    )
    :effect (and
      (not (ready-road ?v ?r))
      (ready-road ?v ?next)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-road_0216-road_0657-tw_00360
    :parameters (?v - vehicle ?r - road_type_road_0216 ?next - road_type_road_0657 ?w - window_type_tw_00360)
    :precondition (and
      (ready-road ?v ?r)
      (road-next ?r ?next)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
    )
    :effect (and
      (not (ready-road ?v ?r))
      (ready-road ?v ?next)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-road_0216-road_0657-tw_00390
    :parameters (?v - vehicle ?r - road_type_road_0216 ?next - road_type_road_0657 ?w - window_type_tw_00390)
    :precondition (and
      (ready-road ?v ?r)
      (road-next ?r ?next)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
    )
    :effect (and
      (not (ready-road ?v ?r))
      (ready-road ?v ?next)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-road_0216-road_0657-tw_00420
    :parameters (?v - vehicle ?r - road_type_road_0216 ?next - road_type_road_0657 ?w - window_type_tw_00420)
    :precondition (and
      (ready-road ?v ?r)
      (road-next ?r ?next)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
    )
    :effect (and
      (not (ready-road ?v ?r))
      (ready-road ?v ?next)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-road_0216-road_0657-tw_00450
    :parameters (?v - vehicle ?r - road_type_road_0216 ?next - road_type_road_0657 ?w - window_type_tw_00450)
    :precondition (and
      (ready-road ?v ?r)
      (road-next ?r ?next)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
    )
    :effect (and
      (not (ready-road ?v ?r))
      (ready-road ?v ?next)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-road_0216-road_0657-tw_00480
    :parameters (?v - vehicle ?r - road_type_road_0216 ?next - road_type_road_0657 ?w - window_type_tw_00480)
    :precondition (and
      (ready-road ?v ?r)
      (road-next ?r ?next)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
    )
    :effect (and
      (not (ready-road ?v ?r))
      (ready-road ?v ?next)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-road_0216-road_0657-tw_00510
    :parameters (?v - vehicle ?r - road_type_road_0216 ?next - road_type_road_0657 ?w - window_type_tw_00510)
    :precondition (and
      (ready-road ?v ?r)
      (road-next ?r ?next)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
    )
    :effect (and
      (not (ready-road ?v ?r))
      (ready-road ?v ?next)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-road_0216-road_0657-tw_00540
    :parameters (?v - vehicle ?r - road_type_road_0216 ?next - road_type_road_0657 ?w - window_type_tw_00540)
    :precondition (and
      (ready-road ?v ?r)
      (road-next ?r ?next)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
    )
    :effect (and
      (not (ready-road ?v ?r))
      (ready-road ?v ?next)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-road_0216-road_0657-tw_00570
    :parameters (?v - vehicle ?r - road_type_road_0216 ?next - road_type_road_0657 ?w - window_type_tw_00570)
    :precondition (and
      (ready-road ?v ?r)
      (road-next ?r ?next)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
    )
    :effect (and
      (not (ready-road ?v ?r))
      (ready-road ?v ?next)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-road_0216-road_0657-tw_00600
    :parameters (?v - vehicle ?r - road_type_road_0216 ?next - road_type_road_0657 ?w - window_type_tw_00600)
    :precondition (and
      (ready-road ?v ?r)
      (road-next ?r ?next)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
    )
    :effect (and
      (not (ready-road ?v ?r))
      (ready-road ?v ?next)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-static-road_0217-road_0261
    :parameters (?v - vehicle ?r - road_type_road_0217 ?next - road_type_road_0261)
    :precondition (and
      (ready-road ?v ?r)
      (road-next ?r ?next)
      (road-open ?r)
      (static-road ?r)
      (not (road-blocked ?r))
    )
    :effect (and
      (not (ready-road ?v ?r))
      (ready-road ?v ?next)
      (increase (travel-time ?v) (travel-duration ?r))
      (increase (sim-time) (travel-duration ?r))
    )
  )

  (:action traverse-road-static-road_0219-road_0388
    :parameters (?v - vehicle ?r - road_type_road_0219 ?next - road_type_road_0388)
    :precondition (and
      (ready-road ?v ?r)
      (road-next ?r ?next)
      (road-open ?r)
      (static-road ?r)
      (not (road-blocked ?r))
    )
    :effect (and
      (not (ready-road ?v ?r))
      (ready-road ?v ?next)
      (increase (travel-time ?v) (travel-duration ?r))
      (increase (sim-time) (travel-duration ?r))
    )
  )

  (:action traverse-road-static-road_0219-road_0389
    :parameters (?v - vehicle ?r - road_type_road_0219 ?next - road_type_road_0389)
    :precondition (and
      (ready-road ?v ?r)
      (road-next ?r ?next)
      (road-open ?r)
      (static-road ?r)
      (not (road-blocked ?r))
    )
    :effect (and
      (not (ready-road ?v ?r))
      (ready-road ?v ?next)
      (increase (travel-time ?v) (travel-duration ?r))
      (increase (sim-time) (travel-duration ?r))
    )
  )

  (:action traverse-road-static-road_0219-road_0390
    :parameters (?v - vehicle ?r - road_type_road_0219 ?next - road_type_road_0390)
    :precondition (and
      (ready-road ?v ?r)
      (road-next ?r ?next)
      (road-open ?r)
      (static-road ?r)
      (not (road-blocked ?r))
    )
    :effect (and
      (not (ready-road ?v ?r))
      (ready-road ?v ?next)
      (increase (travel-time ?v) (travel-duration ?r))
      (increase (sim-time) (travel-duration ?r))
    )
  )

  (:action traverse-road-static-road_0220-macro_0052
    :parameters (?v - vehicle ?r - road_type_road_0220 ?next - road_type_macro_0052)
    :precondition (and
      (ready-road ?v ?r)
      (road-next ?r ?next)
      (road-open ?r)
      (static-road ?r)
      (not (road-blocked ?r))
    )
    :effect (and
      (not (ready-road ?v ?r))
      (ready-road ?v ?next)
      (increase (travel-time ?v) (travel-duration ?r))
      (increase (sim-time) (travel-duration ?r))
    )
  )

  (:action traverse-road-static-road_0220-road_0828
    :parameters (?v - vehicle ?r - road_type_road_0220 ?next - road_type_road_0828)
    :precondition (and
      (ready-road ?v ?r)
      (road-next ?r ?next)
      (road-open ?r)
      (static-road ?r)
      (not (road-blocked ?r))
    )
    :effect (and
      (not (ready-road ?v ?r))
      (ready-road ?v ?next)
      (increase (travel-time ?v) (travel-duration ?r))
      (increase (sim-time) (travel-duration ?r))
    )
  )

  (:action traverse-road-static-road_0221-macro_0053
    :parameters (?v - vehicle ?r - road_type_road_0221 ?next - road_type_macro_0053)
    :precondition (and
      (ready-road ?v ?r)
      (road-next ?r ?next)
      (road-open ?r)
      (static-road ?r)
      (not (road-blocked ?r))
    )
    :effect (and
      (not (ready-road ?v ?r))
      (ready-road ?v ?next)
      (increase (travel-time ?v) (travel-duration ?r))
      (increase (sim-time) (travel-duration ?r))
    )
  )

  (:action traverse-road-static-road_0221-macro_0054
    :parameters (?v - vehicle ?r - road_type_road_0221 ?next - road_type_macro_0054)
    :precondition (and
      (ready-road ?v ?r)
      (road-next ?r ?next)
      (road-open ?r)
      (static-road ?r)
      (not (road-blocked ?r))
    )
    :effect (and
      (not (ready-road ?v ?r))
      (ready-road ?v ?next)
      (increase (travel-time ?v) (travel-duration ?r))
      (increase (sim-time) (travel-duration ?r))
    )
  )

  (:action traverse-road-static-road_0222-road_0349
    :parameters (?v - vehicle ?r - road_type_road_0222 ?next - road_type_road_0349)
    :precondition (and
      (ready-road ?v ?r)
      (road-next ?r ?next)
      (road-open ?r)
      (static-road ?r)
      (not (road-blocked ?r))
    )
    :effect (and
      (not (ready-road ?v ?r))
      (ready-road ?v ?next)
      (increase (travel-time ?v) (travel-duration ?r))
      (increase (sim-time) (travel-duration ?r))
    )
  )

  (:action traverse-road-static-road_0222-road_0350
    :parameters (?v - vehicle ?r - road_type_road_0222 ?next - road_type_road_0350)
    :precondition (and
      (ready-road ?v ?r)
      (road-next ?r ?next)
      (road-open ?r)
      (static-road ?r)
      (not (road-blocked ?r))
    )
    :effect (and
      (not (ready-road ?v ?r))
      (ready-road ?v ?next)
      (increase (travel-time ?v) (travel-duration ?r))
      (increase (sim-time) (travel-duration ?r))
    )
  )

  (:action traverse-road-static-road_0223-road_0325
    :parameters (?v - vehicle ?r - road_type_road_0223 ?next - road_type_road_0325)
    :precondition (and
      (ready-road ?v ?r)
      (road-next ?r ?next)
      (road-open ?r)
      (static-road ?r)
      (not (road-blocked ?r))
    )
    :effect (and
      (not (ready-road ?v ?r))
      (ready-road ?v ?next)
      (increase (travel-time ?v) (travel-duration ?r))
      (increase (sim-time) (travel-duration ?r))
    )
  )

  (:action traverse-road-static-road_0223-road_0326
    :parameters (?v - vehicle ?r - road_type_road_0223 ?next - road_type_road_0326)
    :precondition (and
      (ready-road ?v ?r)
      (road-next ?r ?next)
      (road-open ?r)
      (static-road ?r)
      (not (road-blocked ?r))
    )
    :effect (and
      (not (ready-road ?v ?r))
      (ready-road ?v ?next)
      (increase (travel-time ?v) (travel-duration ?r))
      (increase (sim-time) (travel-duration ?r))
    )
  )

  (:action traverse-road-static-road_0227-road_0289
    :parameters (?v - vehicle ?r - road_type_road_0227 ?next - road_type_road_0289)
    :precondition (and
      (ready-road ?v ?r)
      (road-next ?r ?next)
      (road-open ?r)
      (static-road ?r)
      (not (road-blocked ?r))
    )
    :effect (and
      (not (ready-road ?v ?r))
      (ready-road ?v ?next)
      (increase (travel-time ?v) (travel-duration ?r))
      (increase (sim-time) (travel-duration ?r))
    )
  )

  (:action traverse-road-dynamic-road_0228-road_0423-tw_00000
    :parameters (?v - vehicle ?r - road_type_road_0228 ?next - road_type_road_0423 ?w - window_type_tw_00000)
    :precondition (and
      (ready-road ?v ?r)
      (road-next ?r ?next)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
    )
    :effect (and
      (not (ready-road ?v ?r))
      (ready-road ?v ?next)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-road_0228-road_0423-tw_00030
    :parameters (?v - vehicle ?r - road_type_road_0228 ?next - road_type_road_0423 ?w - window_type_tw_00030)
    :precondition (and
      (ready-road ?v ?r)
      (road-next ?r ?next)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
    )
    :effect (and
      (not (ready-road ?v ?r))
      (ready-road ?v ?next)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-road_0228-road_0423-tw_00060
    :parameters (?v - vehicle ?r - road_type_road_0228 ?next - road_type_road_0423 ?w - window_type_tw_00060)
    :precondition (and
      (ready-road ?v ?r)
      (road-next ?r ?next)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
    )
    :effect (and
      (not (ready-road ?v ?r))
      (ready-road ?v ?next)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-road_0228-road_0423-tw_00090
    :parameters (?v - vehicle ?r - road_type_road_0228 ?next - road_type_road_0423 ?w - window_type_tw_00090)
    :precondition (and
      (ready-road ?v ?r)
      (road-next ?r ?next)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
    )
    :effect (and
      (not (ready-road ?v ?r))
      (ready-road ?v ?next)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-road_0228-road_0423-tw_00120
    :parameters (?v - vehicle ?r - road_type_road_0228 ?next - road_type_road_0423 ?w - window_type_tw_00120)
    :precondition (and
      (ready-road ?v ?r)
      (road-next ?r ?next)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
    )
    :effect (and
      (not (ready-road ?v ?r))
      (ready-road ?v ?next)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-road_0228-road_0423-tw_00150
    :parameters (?v - vehicle ?r - road_type_road_0228 ?next - road_type_road_0423 ?w - window_type_tw_00150)
    :precondition (and
      (ready-road ?v ?r)
      (road-next ?r ?next)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
    )
    :effect (and
      (not (ready-road ?v ?r))
      (ready-road ?v ?next)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-road_0228-road_0423-tw_00180
    :parameters (?v - vehicle ?r - road_type_road_0228 ?next - road_type_road_0423 ?w - window_type_tw_00180)
    :precondition (and
      (ready-road ?v ?r)
      (road-next ?r ?next)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
    )
    :effect (and
      (not (ready-road ?v ?r))
      (ready-road ?v ?next)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-road_0228-road_0423-tw_00210
    :parameters (?v - vehicle ?r - road_type_road_0228 ?next - road_type_road_0423 ?w - window_type_tw_00210)
    :precondition (and
      (ready-road ?v ?r)
      (road-next ?r ?next)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
    )
    :effect (and
      (not (ready-road ?v ?r))
      (ready-road ?v ?next)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-road_0228-road_0423-tw_00240
    :parameters (?v - vehicle ?r - road_type_road_0228 ?next - road_type_road_0423 ?w - window_type_tw_00240)
    :precondition (and
      (ready-road ?v ?r)
      (road-next ?r ?next)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
    )
    :effect (and
      (not (ready-road ?v ?r))
      (ready-road ?v ?next)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-road_0228-road_0423-tw_00270
    :parameters (?v - vehicle ?r - road_type_road_0228 ?next - road_type_road_0423 ?w - window_type_tw_00270)
    :precondition (and
      (ready-road ?v ?r)
      (road-next ?r ?next)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
    )
    :effect (and
      (not (ready-road ?v ?r))
      (ready-road ?v ?next)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-road_0228-road_0423-tw_00300
    :parameters (?v - vehicle ?r - road_type_road_0228 ?next - road_type_road_0423 ?w - window_type_tw_00300)
    :precondition (and
      (ready-road ?v ?r)
      (road-next ?r ?next)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
    )
    :effect (and
      (not (ready-road ?v ?r))
      (ready-road ?v ?next)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-road_0228-road_0423-tw_00330
    :parameters (?v - vehicle ?r - road_type_road_0228 ?next - road_type_road_0423 ?w - window_type_tw_00330)
    :precondition (and
      (ready-road ?v ?r)
      (road-next ?r ?next)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
    )
    :effect (and
      (not (ready-road ?v ?r))
      (ready-road ?v ?next)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-road_0228-road_0423-tw_00360
    :parameters (?v - vehicle ?r - road_type_road_0228 ?next - road_type_road_0423 ?w - window_type_tw_00360)
    :precondition (and
      (ready-road ?v ?r)
      (road-next ?r ?next)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
    )
    :effect (and
      (not (ready-road ?v ?r))
      (ready-road ?v ?next)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-road_0228-road_0423-tw_00390
    :parameters (?v - vehicle ?r - road_type_road_0228 ?next - road_type_road_0423 ?w - window_type_tw_00390)
    :precondition (and
      (ready-road ?v ?r)
      (road-next ?r ?next)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
    )
    :effect (and
      (not (ready-road ?v ?r))
      (ready-road ?v ?next)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-road_0228-road_0423-tw_00420
    :parameters (?v - vehicle ?r - road_type_road_0228 ?next - road_type_road_0423 ?w - window_type_tw_00420)
    :precondition (and
      (ready-road ?v ?r)
      (road-next ?r ?next)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
    )
    :effect (and
      (not (ready-road ?v ?r))
      (ready-road ?v ?next)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-road_0228-road_0423-tw_00450
    :parameters (?v - vehicle ?r - road_type_road_0228 ?next - road_type_road_0423 ?w - window_type_tw_00450)
    :precondition (and
      (ready-road ?v ?r)
      (road-next ?r ?next)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
    )
    :effect (and
      (not (ready-road ?v ?r))
      (ready-road ?v ?next)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-road_0228-road_0423-tw_00480
    :parameters (?v - vehicle ?r - road_type_road_0228 ?next - road_type_road_0423 ?w - window_type_tw_00480)
    :precondition (and
      (ready-road ?v ?r)
      (road-next ?r ?next)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
    )
    :effect (and
      (not (ready-road ?v ?r))
      (ready-road ?v ?next)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-road_0228-road_0423-tw_00510
    :parameters (?v - vehicle ?r - road_type_road_0228 ?next - road_type_road_0423 ?w - window_type_tw_00510)
    :precondition (and
      (ready-road ?v ?r)
      (road-next ?r ?next)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
    )
    :effect (and
      (not (ready-road ?v ?r))
      (ready-road ?v ?next)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-road_0228-road_0423-tw_00540
    :parameters (?v - vehicle ?r - road_type_road_0228 ?next - road_type_road_0423 ?w - window_type_tw_00540)
    :precondition (and
      (ready-road ?v ?r)
      (road-next ?r ?next)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
    )
    :effect (and
      (not (ready-road ?v ?r))
      (ready-road ?v ?next)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-road_0228-road_0423-tw_00570
    :parameters (?v - vehicle ?r - road_type_road_0228 ?next - road_type_road_0423 ?w - window_type_tw_00570)
    :precondition (and
      (ready-road ?v ?r)
      (road-next ?r ?next)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
    )
    :effect (and
      (not (ready-road ?v ?r))
      (ready-road ?v ?next)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-road_0228-road_0423-tw_00600
    :parameters (?v - vehicle ?r - road_type_road_0228 ?next - road_type_road_0423 ?w - window_type_tw_00600)
    :precondition (and
      (ready-road ?v ?r)
      (road-next ?r ?next)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
    )
    :effect (and
      (not (ready-road ?v ?r))
      (ready-road ?v ?next)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-static-road_0230-macro_0010
    :parameters (?v - vehicle ?r - road_type_road_0230 ?next - road_type_macro_0010)
    :precondition (and
      (ready-road ?v ?r)
      (road-next ?r ?next)
      (road-open ?r)
      (static-road ?r)
      (not (road-blocked ?r))
    )
    :effect (and
      (not (ready-road ?v ?r))
      (ready-road ?v ?next)
      (increase (travel-time ?v) (travel-duration ?r))
      (increase (sim-time) (travel-duration ?r))
    )
  )

  (:action traverse-road-static-road_0230-road_0102
    :parameters (?v - vehicle ?r - road_type_road_0230 ?next - road_type_road_0102)
    :precondition (and
      (ready-road ?v ?r)
      (road-next ?r ?next)
      (road-open ?r)
      (static-road ?r)
      (not (road-blocked ?r))
    )
    :effect (and
      (not (ready-road ?v ?r))
      (ready-road ?v ?next)
      (increase (travel-time ?v) (travel-duration ?r))
      (increase (sim-time) (travel-duration ?r))
    )
  )

  (:action traverse-road-static-road_0230-road_0104
    :parameters (?v - vehicle ?r - road_type_road_0230 ?next - road_type_road_0104)
    :precondition (and
      (ready-road ?v ?r)
      (road-next ?r ?next)
      (road-open ?r)
      (static-road ?r)
      (not (road-blocked ?r))
    )
    :effect (and
      (not (ready-road ?v ?r))
      (ready-road ?v ?next)
      (increase (travel-time ?v) (travel-duration ?r))
      (increase (sim-time) (travel-duration ?r))
    )
  )

  (:action traverse-road-static-road_0232-road_0463
    :parameters (?v - vehicle ?r - road_type_road_0232 ?next - road_type_road_0463)
    :precondition (and
      (ready-road ?v ?r)
      (road-next ?r ?next)
      (road-open ?r)
      (static-road ?r)
      (not (road-blocked ?r))
    )
    :effect (and
      (not (ready-road ?v ?r))
      (ready-road ?v ?next)
      (increase (travel-time ?v) (travel-duration ?r))
      (increase (sim-time) (travel-duration ?r))
    )
  )

  (:action traverse-road-static-road_0232-road_0464
    :parameters (?v - vehicle ?r - road_type_road_0232 ?next - road_type_road_0464)
    :precondition (and
      (ready-road ?v ?r)
      (road-next ?r ?next)
      (road-open ?r)
      (static-road ?r)
      (not (road-blocked ?r))
    )
    :effect (and
      (not (ready-road ?v ?r))
      (ready-road ?v ?next)
      (increase (travel-time ?v) (travel-duration ?r))
      (increase (sim-time) (travel-duration ?r))
    )
  )

  (:action traverse-road-static-road_0233-road_0244
    :parameters (?v - vehicle ?r - road_type_road_0233 ?next - road_type_road_0244)
    :precondition (and
      (ready-road ?v ?r)
      (road-next ?r ?next)
      (road-open ?r)
      (static-road ?r)
      (not (road-blocked ?r))
    )
    :effect (and
      (not (ready-road ?v ?r))
      (ready-road ?v ?next)
      (increase (travel-time ?v) (travel-duration ?r))
      (increase (sim-time) (travel-duration ?r))
    )
  )

  (:action traverse-road-static-road_0233-road_0245
    :parameters (?v - vehicle ?r - road_type_road_0233 ?next - road_type_road_0245)
    :precondition (and
      (ready-road ?v ?r)
      (road-next ?r ?next)
      (road-open ?r)
      (static-road ?r)
      (not (road-blocked ?r))
    )
    :effect (and
      (not (ready-road ?v ?r))
      (ready-road ?v ?next)
      (increase (travel-time ?v) (travel-duration ?r))
      (increase (sim-time) (travel-duration ?r))
    )
  )

  (:action traverse-road-static-road_0234-road_0225
    :parameters (?v - vehicle ?r - road_type_road_0234 ?next - road_type_road_0225)
    :precondition (and
      (ready-road ?v ?r)
      (road-next ?r ?next)
      (road-open ?r)
      (static-road ?r)
      (not (road-blocked ?r))
    )
    :effect (and
      (not (ready-road ?v ?r))
      (ready-road ?v ?next)
      (increase (travel-time ?v) (travel-duration ?r))
      (increase (sim-time) (travel-duration ?r))
    )
  )

  (:action traverse-road-static-road_0236-road_0749
    :parameters (?v - vehicle ?r - road_type_road_0236 ?next - road_type_road_0749)
    :precondition (and
      (ready-road ?v ?r)
      (road-next ?r ?next)
      (road-open ?r)
      (static-road ?r)
      (not (road-blocked ?r))
    )
    :effect (and
      (not (ready-road ?v ?r))
      (ready-road ?v ?next)
      (increase (travel-time ?v) (travel-duration ?r))
      (increase (sim-time) (travel-duration ?r))
    )
  )

  (:action traverse-road-static-road_0236-road_0750
    :parameters (?v - vehicle ?r - road_type_road_0236 ?next - road_type_road_0750)
    :precondition (and
      (ready-road ?v ?r)
      (road-next ?r ?next)
      (road-open ?r)
      (static-road ?r)
      (not (road-blocked ?r))
    )
    :effect (and
      (not (ready-road ?v ?r))
      (ready-road ?v ?next)
      (increase (travel-time ?v) (travel-duration ?r))
      (increase (sim-time) (travel-duration ?r))
    )
  )

  (:action traverse-road-static-road_0236-road_0751
    :parameters (?v - vehicle ?r - road_type_road_0236 ?next - road_type_road_0751)
    :precondition (and
      (ready-road ?v ?r)
      (road-next ?r ?next)
      (road-open ?r)
      (static-road ?r)
      (not (road-blocked ?r))
    )
    :effect (and
      (not (ready-road ?v ?r))
      (ready-road ?v ?next)
      (increase (travel-time ?v) (travel-duration ?r))
      (increase (sim-time) (travel-duration ?r))
    )
  )

  (:action traverse-road-static-road_0237-macro_0016
    :parameters (?v - vehicle ?r - road_type_road_0237 ?next - road_type_macro_0016)
    :precondition (and
      (ready-road ?v ?r)
      (road-next ?r ?next)
      (road-open ?r)
      (static-road ?r)
      (not (road-blocked ?r))
    )
    :effect (and
      (not (ready-road ?v ?r))
      (ready-road ?v ?next)
      (increase (travel-time ?v) (travel-duration ?r))
      (increase (sim-time) (travel-duration ?r))
    )
  )

  (:action traverse-road-static-road_0237-road_0191
    :parameters (?v - vehicle ?r - road_type_road_0237 ?next - road_type_road_0191)
    :precondition (and
      (ready-road ?v ?r)
      (road-next ?r ?next)
      (road-open ?r)
      (static-road ?r)
      (not (road-blocked ?r))
    )
    :effect (and
      (not (ready-road ?v ?r))
      (ready-road ?v ?next)
      (increase (travel-time ?v) (travel-duration ?r))
      (increase (sim-time) (travel-duration ?r))
    )
  )

  (:action traverse-road-static-road_0238-road_0451
    :parameters (?v - vehicle ?r - road_type_road_0238 ?next - road_type_road_0451)
    :precondition (and
      (ready-road ?v ?r)
      (road-next ?r ?next)
      (road-open ?r)
      (static-road ?r)
      (not (road-blocked ?r))
    )
    :effect (and
      (not (ready-road ?v ?r))
      (ready-road ?v ?next)
      (increase (travel-time ?v) (travel-duration ?r))
      (increase (sim-time) (travel-duration ?r))
    )
  )

  (:action traverse-road-static-road_0238-road_0452
    :parameters (?v - vehicle ?r - road_type_road_0238 ?next - road_type_road_0452)
    :precondition (and
      (ready-road ?v ?r)
      (road-next ?r ?next)
      (road-open ?r)
      (static-road ?r)
      (not (road-blocked ?r))
    )
    :effect (and
      (not (ready-road ?v ?r))
      (ready-road ?v ?next)
      (increase (travel-time ?v) (travel-duration ?r))
      (increase (sim-time) (travel-duration ?r))
    )
  )

  (:action traverse-road-static-road_0239-road_0714
    :parameters (?v - vehicle ?r - road_type_road_0239 ?next - road_type_road_0714)
    :precondition (and
      (ready-road ?v ?r)
      (road-next ?r ?next)
      (road-open ?r)
      (static-road ?r)
      (not (road-blocked ?r))
    )
    :effect (and
      (not (ready-road ?v ?r))
      (ready-road ?v ?next)
      (increase (travel-time ?v) (travel-duration ?r))
      (increase (sim-time) (travel-duration ?r))
    )
  )

  (:action traverse-road-static-road_0239-road_0715
    :parameters (?v - vehicle ?r - road_type_road_0239 ?next - road_type_road_0715)
    :precondition (and
      (ready-road ?v ?r)
      (road-next ?r ?next)
      (road-open ?r)
      (static-road ?r)
      (not (road-blocked ?r))
    )
    :effect (and
      (not (ready-road ?v ?r))
      (ready-road ?v ?next)
      (increase (travel-time ?v) (travel-duration ?r))
      (increase (sim-time) (travel-duration ?r))
    )
  )

  (:action traverse-road-static-road_0240-road_0762
    :parameters (?v - vehicle ?r - road_type_road_0240 ?next - road_type_road_0762)
    :precondition (and
      (ready-road ?v ?r)
      (road-next ?r ?next)
      (road-open ?r)
      (static-road ?r)
      (not (road-blocked ?r))
    )
    :effect (and
      (not (ready-road ?v ?r))
      (ready-road ?v ?next)
      (increase (travel-time ?v) (travel-duration ?r))
      (increase (sim-time) (travel-duration ?r))
    )
  )

  (:action traverse-road-static-road_0240-road_0763
    :parameters (?v - vehicle ?r - road_type_road_0240 ?next - road_type_road_0763)
    :precondition (and
      (ready-road ?v ?r)
      (road-next ?r ?next)
      (road-open ?r)
      (static-road ?r)
      (not (road-blocked ?r))
    )
    :effect (and
      (not (ready-road ?v ?r))
      (ready-road ?v ?next)
      (increase (travel-time ?v) (travel-duration ?r))
      (increase (sim-time) (travel-duration ?r))
    )
  )

  (:action traverse-road-static-road_0241-road_0239
    :parameters (?v - vehicle ?r - road_type_road_0241 ?next - road_type_road_0239)
    :precondition (and
      (ready-road ?v ?r)
      (road-next ?r ?next)
      (road-open ?r)
      (static-road ?r)
      (not (road-blocked ?r))
    )
    :effect (and
      (not (ready-road ?v ?r))
      (ready-road ?v ?next)
      (increase (travel-time ?v) (travel-duration ?r))
      (increase (sim-time) (travel-duration ?r))
    )
  )

  (:action traverse-road-static-road_0244-road_0062
    :parameters (?v - vehicle ?r - road_type_road_0244 ?next - road_type_road_0062)
    :precondition (and
      (ready-road ?v ?r)
      (road-next ?r ?next)
      (road-open ?r)
      (static-road ?r)
      (not (road-blocked ?r))
    )
    :effect (and
      (not (ready-road ?v ?r))
      (ready-road ?v ?next)
      (increase (travel-time ?v) (travel-duration ?r))
      (increase (sim-time) (travel-duration ?r))
    )
  )

  (:action traverse-road-static-road_0244-road_0063
    :parameters (?v - vehicle ?r - road_type_road_0244 ?next - road_type_road_0063)
    :precondition (and
      (ready-road ?v ?r)
      (road-next ?r ?next)
      (road-open ?r)
      (static-road ?r)
      (not (road-blocked ?r))
    )
    :effect (and
      (not (ready-road ?v ?r))
      (ready-road ?v ?next)
      (increase (travel-time ?v) (travel-duration ?r))
      (increase (sim-time) (travel-duration ?r))
    )
  )

  (:action traverse-road-static-road_0244-road_0064
    :parameters (?v - vehicle ?r - road_type_road_0244 ?next - road_type_road_0064)
    :precondition (and
      (ready-road ?v ?r)
      (road-next ?r ?next)
      (road-open ?r)
      (static-road ?r)
      (not (road-blocked ?r))
    )
    :effect (and
      (not (ready-road ?v ?r))
      (ready-road ?v ?next)
      (increase (travel-time ?v) (travel-duration ?r))
      (increase (sim-time) (travel-duration ?r))
    )
  )

  (:action traverse-road-static-road_0244-road_0065
    :parameters (?v - vehicle ?r - road_type_road_0244 ?next - road_type_road_0065)
    :precondition (and
      (ready-road ?v ?r)
      (road-next ?r ?next)
      (road-open ?r)
      (static-road ?r)
      (not (road-blocked ?r))
    )
    :effect (and
      (not (ready-road ?v ?r))
      (ready-road ?v ?next)
      (increase (travel-time ?v) (travel-duration ?r))
      (increase (sim-time) (travel-duration ?r))
    )
  )

  (:action traverse-road-static-road_0245-road_0225
    :parameters (?v - vehicle ?r - road_type_road_0245 ?next - road_type_road_0225)
    :precondition (and
      (ready-road ?v ?r)
      (road-next ?r ?next)
      (road-open ?r)
      (static-road ?r)
      (not (road-blocked ?r))
    )
    :effect (and
      (not (ready-road ?v ?r))
      (ready-road ?v ?next)
      (increase (travel-time ?v) (travel-duration ?r))
      (increase (sim-time) (travel-duration ?r))
    )
  )

  (:action traverse-road-static-road_0246-road_0414
    :parameters (?v - vehicle ?r - road_type_road_0246 ?next - road_type_road_0414)
    :precondition (and
      (ready-road ?v ?r)
      (road-next ?r ?next)
      (road-open ?r)
      (static-road ?r)
      (not (road-blocked ?r))
    )
    :effect (and
      (not (ready-road ?v ?r))
      (ready-road ?v ?next)
      (increase (travel-time ?v) (travel-duration ?r))
      (increase (sim-time) (travel-duration ?r))
    )
  )

  (:action traverse-road-static-road_0247-road_0589
    :parameters (?v - vehicle ?r - road_type_road_0247 ?next - road_type_road_0589)
    :precondition (and
      (ready-road ?v ?r)
      (road-next ?r ?next)
      (road-open ?r)
      (static-road ?r)
      (not (road-blocked ?r))
    )
    :effect (and
      (not (ready-road ?v ?r))
      (ready-road ?v ?next)
      (increase (travel-time ?v) (travel-duration ?r))
      (increase (sim-time) (travel-duration ?r))
    )
  )

  (:action traverse-road-static-road_0248-road_0249
    :parameters (?v - vehicle ?r - road_type_road_0248 ?next - road_type_road_0249)
    :precondition (and
      (ready-road ?v ?r)
      (road-next ?r ?next)
      (road-open ?r)
      (static-road ?r)
      (not (road-blocked ?r))
    )
    :effect (and
      (not (ready-road ?v ?r))
      (ready-road ?v ?next)
      (increase (travel-time ?v) (travel-duration ?r))
      (increase (sim-time) (travel-duration ?r))
    )
  )

  (:action traverse-road-static-road_0248-road_0250
    :parameters (?v - vehicle ?r - road_type_road_0248 ?next - road_type_road_0250)
    :precondition (and
      (ready-road ?v ?r)
      (road-next ?r ?next)
      (road-open ?r)
      (static-road ?r)
      (not (road-blocked ?r))
    )
    :effect (and
      (not (ready-road ?v ?r))
      (ready-road ?v ?next)
      (increase (travel-time ?v) (travel-duration ?r))
      (increase (sim-time) (travel-duration ?r))
    )
  )

  (:action traverse-road-static-road_0248-road_0251
    :parameters (?v - vehicle ?r - road_type_road_0248 ?next - road_type_road_0251)
    :precondition (and
      (ready-road ?v ?r)
      (road-next ?r ?next)
      (road-open ?r)
      (static-road ?r)
      (not (road-blocked ?r))
    )
    :effect (and
      (not (ready-road ?v ?r))
      (ready-road ?v ?next)
      (increase (travel-time ?v) (travel-duration ?r))
      (increase (sim-time) (travel-duration ?r))
    )
  )

  (:action traverse-road-static-road_0249-road_0438
    :parameters (?v - vehicle ?r - road_type_road_0249 ?next - road_type_road_0438)
    :precondition (and
      (ready-road ?v ?r)
      (road-next ?r ?next)
      (road-open ?r)
      (static-road ?r)
      (not (road-blocked ?r))
    )
    :effect (and
      (not (ready-road ?v ?r))
      (ready-road ?v ?next)
      (increase (travel-time ?v) (travel-duration ?r))
      (increase (sim-time) (travel-duration ?r))
    )
  )

  (:action traverse-road-static-road_0249-road_0439
    :parameters (?v - vehicle ?r - road_type_road_0249 ?next - road_type_road_0439)
    :precondition (and
      (ready-road ?v ?r)
      (road-next ?r ?next)
      (road-open ?r)
      (static-road ?r)
      (not (road-blocked ?r))
    )
    :effect (and
      (not (ready-road ?v ?r))
      (ready-road ?v ?next)
      (increase (travel-time ?v) (travel-duration ?r))
      (increase (sim-time) (travel-duration ?r))
    )
  )

  (:action traverse-road-static-road_0249-road_0440
    :parameters (?v - vehicle ?r - road_type_road_0249 ?next - road_type_road_0440)
    :precondition (and
      (ready-road ?v ?r)
      (road-next ?r ?next)
      (road-open ?r)
      (static-road ?r)
      (not (road-blocked ?r))
    )
    :effect (and
      (not (ready-road ?v ?r))
      (ready-road ?v ?next)
      (increase (travel-time ?v) (travel-duration ?r))
      (increase (sim-time) (travel-duration ?r))
    )
  )

  (:action traverse-road-static-road_0250-road_0247
    :parameters (?v - vehicle ?r - road_type_road_0250 ?next - road_type_road_0247)
    :precondition (and
      (ready-road ?v ?r)
      (road-next ?r ?next)
      (road-open ?r)
      (static-road ?r)
      (not (road-blocked ?r))
    )
    :effect (and
      (not (ready-road ?v ?r))
      (ready-road ?v ?next)
      (increase (travel-time ?v) (travel-duration ?r))
      (increase (sim-time) (travel-duration ?r))
    )
  )

  (:action traverse-road-static-road_0250-road_0248
    :parameters (?v - vehicle ?r - road_type_road_0250 ?next - road_type_road_0248)
    :precondition (and
      (ready-road ?v ?r)
      (road-next ?r ?next)
      (road-open ?r)
      (static-road ?r)
      (not (road-blocked ?r))
    )
    :effect (and
      (not (ready-road ?v ?r))
      (ready-road ?v ?next)
      (increase (travel-time ?v) (travel-duration ?r))
      (increase (sim-time) (travel-duration ?r))
    )
  )

  (:action traverse-road-static-road_0251-macro_0056
    :parameters (?v - vehicle ?r - road_type_road_0251 ?next - road_type_macro_0056)
    :precondition (and
      (ready-road ?v ?r)
      (road-next ?r ?next)
      (road-open ?r)
      (static-road ?r)
      (not (road-blocked ?r))
    )
    :effect (and
      (not (ready-road ?v ?r))
      (ready-road ?v ?next)
      (increase (travel-time ?v) (travel-duration ?r))
      (increase (sim-time) (travel-duration ?r))
    )
  )

  (:action traverse-road-static-road_0251-macro_0057
    :parameters (?v - vehicle ?r - road_type_road_0251 ?next - road_type_macro_0057)
    :precondition (and
      (ready-road ?v ?r)
      (road-next ?r ?next)
      (road-open ?r)
      (static-road ?r)
      (not (road-blocked ?r))
    )
    :effect (and
      (not (ready-road ?v ?r))
      (ready-road ?v ?next)
      (increase (travel-time ?v) (travel-duration ?r))
      (increase (sim-time) (travel-duration ?r))
    )
  )

  (:action traverse-road-static-road_0252-road_0260
    :parameters (?v - vehicle ?r - road_type_road_0252 ?next - road_type_road_0260)
    :precondition (and
      (ready-road ?v ?r)
      (road-next ?r ?next)
      (road-open ?r)
      (static-road ?r)
      (not (road-blocked ?r))
    )
    :effect (and
      (not (ready-road ?v ?r))
      (ready-road ?v ?next)
      (increase (travel-time ?v) (travel-duration ?r))
      (increase (sim-time) (travel-duration ?r))
    )
  )

  (:action traverse-road-static-road_0253-macro_0017
    :parameters (?v - vehicle ?r - road_type_road_0253 ?next - road_type_macro_0017)
    :precondition (and
      (ready-road ?v ?r)
      (road-next ?r ?next)
      (road-open ?r)
      (static-road ?r)
      (not (road-blocked ?r))
    )
    :effect (and
      (not (ready-road ?v ?r))
      (ready-road ?v ?next)
      (increase (travel-time ?v) (travel-duration ?r))
      (increase (sim-time) (travel-duration ?r))
    )
  )

  (:action traverse-road-static-road_0253-road_0227
    :parameters (?v - vehicle ?r - road_type_road_0253 ?next - road_type_road_0227)
    :precondition (and
      (ready-road ?v ?r)
      (road-next ?r ?next)
      (road-open ?r)
      (static-road ?r)
      (not (road-blocked ?r))
    )
    :effect (and
      (not (ready-road ?v ?r))
      (ready-road ?v ?next)
      (increase (travel-time ?v) (travel-duration ?r))
      (increase (sim-time) (travel-duration ?r))
    )
  )

  (:action traverse-road-static-road_0254-road_0127
    :parameters (?v - vehicle ?r - road_type_road_0254 ?next - road_type_road_0127)
    :precondition (and
      (ready-road ?v ?r)
      (road-next ?r ?next)
      (road-open ?r)
      (static-road ?r)
      (not (road-blocked ?r))
    )
    :effect (and
      (not (ready-road ?v ?r))
      (ready-road ?v ?next)
      (increase (travel-time ?v) (travel-duration ?r))
      (increase (sim-time) (travel-duration ?r))
    )
  )

  (:action traverse-road-static-road_0254-road_0128
    :parameters (?v - vehicle ?r - road_type_road_0254 ?next - road_type_road_0128)
    :precondition (and
      (ready-road ?v ?r)
      (road-next ?r ?next)
      (road-open ?r)
      (static-road ?r)
      (not (road-blocked ?r))
    )
    :effect (and
      (not (ready-road ?v ?r))
      (ready-road ?v ?next)
      (increase (travel-time ?v) (travel-duration ?r))
      (increase (sim-time) (travel-duration ?r))
    )
  )

  (:action traverse-road-static-road_0257-road_0258
    :parameters (?v - vehicle ?r - road_type_road_0257 ?next - road_type_road_0258)
    :precondition (and
      (ready-road ?v ?r)
      (road-next ?r ?next)
      (road-open ?r)
      (static-road ?r)
      (not (road-blocked ?r))
    )
    :effect (and
      (not (ready-road ?v ?r))
      (ready-road ?v ?next)
      (increase (travel-time ?v) (travel-duration ?r))
      (increase (sim-time) (travel-duration ?r))
    )
  )

  (:action traverse-road-static-road_0257-road_0259
    :parameters (?v - vehicle ?r - road_type_road_0257 ?next - road_type_road_0259)
    :precondition (and
      (ready-road ?v ?r)
      (road-next ?r ?next)
      (road-open ?r)
      (static-road ?r)
      (not (road-blocked ?r))
    )
    :effect (and
      (not (ready-road ?v ?r))
      (ready-road ?v ?next)
      (increase (travel-time ?v) (travel-duration ?r))
      (increase (sim-time) (travel-duration ?r))
    )
  )

  (:action traverse-road-static-road_0258-macro_0019
    :parameters (?v - vehicle ?r - road_type_road_0258 ?next - road_type_macro_0019)
    :precondition (and
      (ready-road ?v ?r)
      (road-next ?r ?next)
      (road-open ?r)
      (static-road ?r)
      (not (road-blocked ?r))
    )
    :effect (and
      (not (ready-road ?v ?r))
      (ready-road ?v ?next)
      (increase (travel-time ?v) (travel-duration ?r))
      (increase (sim-time) (travel-duration ?r))
    )
  )

  (:action traverse-road-static-road_0258-macro_0020
    :parameters (?v - vehicle ?r - road_type_road_0258 ?next - road_type_macro_0020)
    :precondition (and
      (ready-road ?v ?r)
      (road-next ?r ?next)
      (road-open ?r)
      (static-road ?r)
      (not (road-blocked ?r))
    )
    :effect (and
      (not (ready-road ?v ?r))
      (ready-road ?v ?next)
      (increase (travel-time ?v) (travel-duration ?r))
      (increase (sim-time) (travel-duration ?r))
    )
  )

  (:action traverse-road-static-road_0258-road_0257
    :parameters (?v - vehicle ?r - road_type_road_0258 ?next - road_type_road_0257)
    :precondition (and
      (ready-road ?v ?r)
      (road-next ?r ?next)
      (road-open ?r)
      (static-road ?r)
      (not (road-blocked ?r))
    )
    :effect (and
      (not (ready-road ?v ?r))
      (ready-road ?v ?next)
      (increase (travel-time ?v) (travel-duration ?r))
      (increase (sim-time) (travel-duration ?r))
    )
  )

  (:action traverse-road-static-road_0259-road_0483
    :parameters (?v - vehicle ?r - road_type_road_0259 ?next - road_type_road_0483)
    :precondition (and
      (ready-road ?v ?r)
      (road-next ?r ?next)
      (road-open ?r)
      (static-road ?r)
      (not (road-blocked ?r))
    )
    :effect (and
      (not (ready-road ?v ?r))
      (ready-road ?v ?next)
      (increase (travel-time ?v) (travel-duration ?r))
      (increase (sim-time) (travel-duration ?r))
    )
  )

  (:action traverse-road-static-road_0259-road_0484
    :parameters (?v - vehicle ?r - road_type_road_0259 ?next - road_type_road_0484)
    :precondition (and
      (ready-road ?v ?r)
      (road-next ?r ?next)
      (road-open ?r)
      (static-road ?r)
      (not (road-blocked ?r))
    )
    :effect (and
      (not (ready-road ?v ?r))
      (ready-road ?v ?next)
      (increase (travel-time ?v) (travel-duration ?r))
      (increase (sim-time) (travel-duration ?r))
    )
  )

  (:action traverse-road-static-road_0260-road_0264
    :parameters (?v - vehicle ?r - road_type_road_0260 ?next - road_type_road_0264)
    :precondition (and
      (ready-road ?v ?r)
      (road-next ?r ?next)
      (road-open ?r)
      (static-road ?r)
      (not (road-blocked ?r))
    )
    :effect (and
      (not (ready-road ?v ?r))
      (ready-road ?v ?next)
      (increase (travel-time ?v) (travel-duration ?r))
      (increase (sim-time) (travel-duration ?r))
    )
  )

  (:action traverse-road-static-road_0261-road_0716
    :parameters (?v - vehicle ?r - road_type_road_0261 ?next - road_type_road_0716)
    :precondition (and
      (ready-road ?v ?r)
      (road-next ?r ?next)
      (road-open ?r)
      (static-road ?r)
      (not (road-blocked ?r))
    )
    :effect (and
      (not (ready-road ?v ?r))
      (ready-road ?v ?next)
      (increase (travel-time ?v) (travel-duration ?r))
      (increase (sim-time) (travel-duration ?r))
    )
  )

  (:action traverse-road-static-road_0261-road_0717
    :parameters (?v - vehicle ?r - road_type_road_0261 ?next - road_type_road_0717)
    :precondition (and
      (ready-road ?v ?r)
      (road-next ?r ?next)
      (road-open ?r)
      (static-road ?r)
      (not (road-blocked ?r))
    )
    :effect (and
      (not (ready-road ?v ?r))
      (ready-road ?v ?next)
      (increase (travel-time ?v) (travel-duration ?r))
      (increase (sim-time) (travel-duration ?r))
    )
  )

  (:action traverse-road-static-road_0262-road_0816
    :parameters (?v - vehicle ?r - road_type_road_0262 ?next - road_type_road_0816)
    :precondition (and
      (ready-road ?v ?r)
      (road-next ?r ?next)
      (road-open ?r)
      (static-road ?r)
      (not (road-blocked ?r))
    )
    :effect (and
      (not (ready-road ?v ?r))
      (ready-road ?v ?next)
      (increase (travel-time ?v) (travel-duration ?r))
      (increase (sim-time) (travel-duration ?r))
    )
  )

  (:action traverse-road-static-road_0262-road_0817
    :parameters (?v - vehicle ?r - road_type_road_0262 ?next - road_type_road_0817)
    :precondition (and
      (ready-road ?v ?r)
      (road-next ?r ?next)
      (road-open ?r)
      (static-road ?r)
      (not (road-blocked ?r))
    )
    :effect (and
      (not (ready-road ?v ?r))
      (ready-road ?v ?next)
      (increase (travel-time ?v) (travel-duration ?r))
      (increase (sim-time) (travel-duration ?r))
    )
  )

  (:action traverse-road-static-road_0262-road_0818
    :parameters (?v - vehicle ?r - road_type_road_0262 ?next - road_type_road_0818)
    :precondition (and
      (ready-road ?v ?r)
      (road-next ?r ?next)
      (road-open ?r)
      (static-road ?r)
      (not (road-blocked ?r))
    )
    :effect (and
      (not (ready-road ?v ?r))
      (ready-road ?v ?next)
      (increase (travel-time ?v) (travel-duration ?r))
      (increase (sim-time) (travel-duration ?r))
    )
  )

  (:action traverse-road-static-road_0263-road_0787
    :parameters (?v - vehicle ?r - road_type_road_0263 ?next - road_type_road_0787)
    :precondition (and
      (ready-road ?v ?r)
      (road-next ?r ?next)
      (road-open ?r)
      (static-road ?r)
      (not (road-blocked ?r))
    )
    :effect (and
      (not (ready-road ?v ?r))
      (ready-road ?v ?next)
      (increase (travel-time ?v) (travel-duration ?r))
      (increase (sim-time) (travel-duration ?r))
    )
  )

  (:action traverse-road-static-road_0264-road_0273
    :parameters (?v - vehicle ?r - road_type_road_0264 ?next - road_type_road_0273)
    :precondition (and
      (ready-road ?v ?r)
      (road-next ?r ?next)
      (road-open ?r)
      (static-road ?r)
      (not (road-blocked ?r))
    )
    :effect (and
      (not (ready-road ?v ?r))
      (ready-road ?v ?next)
      (increase (travel-time ?v) (travel-duration ?r))
      (increase (sim-time) (travel-duration ?r))
    )
  )

  (:action traverse-road-static-road_0264-road_0274
    :parameters (?v - vehicle ?r - road_type_road_0264 ?next - road_type_road_0274)
    :precondition (and
      (ready-road ?v ?r)
      (road-next ?r ?next)
      (road-open ?r)
      (static-road ?r)
      (not (road-blocked ?r))
    )
    :effect (and
      (not (ready-road ?v ?r))
      (ready-road ?v ?next)
      (increase (travel-time ?v) (travel-duration ?r))
      (increase (sim-time) (travel-duration ?r))
    )
  )

  (:action traverse-road-static-road_0264-road_0275
    :parameters (?v - vehicle ?r - road_type_road_0264 ?next - road_type_road_0275)
    :precondition (and
      (ready-road ?v ?r)
      (road-next ?r ?next)
      (road-open ?r)
      (static-road ?r)
      (not (road-blocked ?r))
    )
    :effect (and
      (not (ready-road ?v ?r))
      (ready-road ?v ?next)
      (increase (travel-time ?v) (travel-duration ?r))
      (increase (sim-time) (travel-duration ?r))
    )
  )

  (:action traverse-road-static-road_0265-road_0262
    :parameters (?v - vehicle ?r - road_type_road_0265 ?next - road_type_road_0262)
    :precondition (and
      (ready-road ?v ?r)
      (road-next ?r ?next)
      (road-open ?r)
      (static-road ?r)
      (not (road-blocked ?r))
    )
    :effect (and
      (not (ready-road ?v ?r))
      (ready-road ?v ?next)
      (increase (travel-time ?v) (travel-duration ?r))
      (increase (sim-time) (travel-duration ?r))
    )
  )

  (:action traverse-road-static-road_0265-road_0263
    :parameters (?v - vehicle ?r - road_type_road_0265 ?next - road_type_road_0263)
    :precondition (and
      (ready-road ?v ?r)
      (road-next ?r ?next)
      (road-open ?r)
      (static-road ?r)
      (not (road-blocked ?r))
    )
    :effect (and
      (not (ready-road ?v ?r))
      (ready-road ?v ?next)
      (increase (travel-time ?v) (travel-duration ?r))
      (increase (sim-time) (travel-duration ?r))
    )
  )

  (:action traverse-road-dynamic-road_0266-road_0654-tw_00000
    :parameters (?v - vehicle ?r - road_type_road_0266 ?next - road_type_road_0654 ?w - window_type_tw_00000)
    :precondition (and
      (ready-road ?v ?r)
      (road-next ?r ?next)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
    )
    :effect (and
      (not (ready-road ?v ?r))
      (ready-road ?v ?next)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-road_0266-road_0654-tw_00030
    :parameters (?v - vehicle ?r - road_type_road_0266 ?next - road_type_road_0654 ?w - window_type_tw_00030)
    :precondition (and
      (ready-road ?v ?r)
      (road-next ?r ?next)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
    )
    :effect (and
      (not (ready-road ?v ?r))
      (ready-road ?v ?next)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-road_0266-road_0654-tw_00060
    :parameters (?v - vehicle ?r - road_type_road_0266 ?next - road_type_road_0654 ?w - window_type_tw_00060)
    :precondition (and
      (ready-road ?v ?r)
      (road-next ?r ?next)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
    )
    :effect (and
      (not (ready-road ?v ?r))
      (ready-road ?v ?next)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-road_0266-road_0654-tw_00090
    :parameters (?v - vehicle ?r - road_type_road_0266 ?next - road_type_road_0654 ?w - window_type_tw_00090)
    :precondition (and
      (ready-road ?v ?r)
      (road-next ?r ?next)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
    )
    :effect (and
      (not (ready-road ?v ?r))
      (ready-road ?v ?next)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-road_0266-road_0654-tw_00120
    :parameters (?v - vehicle ?r - road_type_road_0266 ?next - road_type_road_0654 ?w - window_type_tw_00120)
    :precondition (and
      (ready-road ?v ?r)
      (road-next ?r ?next)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
    )
    :effect (and
      (not (ready-road ?v ?r))
      (ready-road ?v ?next)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-road_0266-road_0654-tw_00150
    :parameters (?v - vehicle ?r - road_type_road_0266 ?next - road_type_road_0654 ?w - window_type_tw_00150)
    :precondition (and
      (ready-road ?v ?r)
      (road-next ?r ?next)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
    )
    :effect (and
      (not (ready-road ?v ?r))
      (ready-road ?v ?next)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-road_0266-road_0654-tw_00180
    :parameters (?v - vehicle ?r - road_type_road_0266 ?next - road_type_road_0654 ?w - window_type_tw_00180)
    :precondition (and
      (ready-road ?v ?r)
      (road-next ?r ?next)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
    )
    :effect (and
      (not (ready-road ?v ?r))
      (ready-road ?v ?next)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-road_0266-road_0654-tw_00210
    :parameters (?v - vehicle ?r - road_type_road_0266 ?next - road_type_road_0654 ?w - window_type_tw_00210)
    :precondition (and
      (ready-road ?v ?r)
      (road-next ?r ?next)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
    )
    :effect (and
      (not (ready-road ?v ?r))
      (ready-road ?v ?next)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-road_0266-road_0654-tw_00240
    :parameters (?v - vehicle ?r - road_type_road_0266 ?next - road_type_road_0654 ?w - window_type_tw_00240)
    :precondition (and
      (ready-road ?v ?r)
      (road-next ?r ?next)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
    )
    :effect (and
      (not (ready-road ?v ?r))
      (ready-road ?v ?next)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-road_0266-road_0654-tw_00270
    :parameters (?v - vehicle ?r - road_type_road_0266 ?next - road_type_road_0654 ?w - window_type_tw_00270)
    :precondition (and
      (ready-road ?v ?r)
      (road-next ?r ?next)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
    )
    :effect (and
      (not (ready-road ?v ?r))
      (ready-road ?v ?next)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-road_0266-road_0654-tw_00300
    :parameters (?v - vehicle ?r - road_type_road_0266 ?next - road_type_road_0654 ?w - window_type_tw_00300)
    :precondition (and
      (ready-road ?v ?r)
      (road-next ?r ?next)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
    )
    :effect (and
      (not (ready-road ?v ?r))
      (ready-road ?v ?next)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-road_0266-road_0654-tw_00330
    :parameters (?v - vehicle ?r - road_type_road_0266 ?next - road_type_road_0654 ?w - window_type_tw_00330)
    :precondition (and
      (ready-road ?v ?r)
      (road-next ?r ?next)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
    )
    :effect (and
      (not (ready-road ?v ?r))
      (ready-road ?v ?next)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-road_0266-road_0654-tw_00360
    :parameters (?v - vehicle ?r - road_type_road_0266 ?next - road_type_road_0654 ?w - window_type_tw_00360)
    :precondition (and
      (ready-road ?v ?r)
      (road-next ?r ?next)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
    )
    :effect (and
      (not (ready-road ?v ?r))
      (ready-road ?v ?next)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-road_0266-road_0654-tw_00390
    :parameters (?v - vehicle ?r - road_type_road_0266 ?next - road_type_road_0654 ?w - window_type_tw_00390)
    :precondition (and
      (ready-road ?v ?r)
      (road-next ?r ?next)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
    )
    :effect (and
      (not (ready-road ?v ?r))
      (ready-road ?v ?next)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-road_0266-road_0654-tw_00420
    :parameters (?v - vehicle ?r - road_type_road_0266 ?next - road_type_road_0654 ?w - window_type_tw_00420)
    :precondition (and
      (ready-road ?v ?r)
      (road-next ?r ?next)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
    )
    :effect (and
      (not (ready-road ?v ?r))
      (ready-road ?v ?next)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-road_0266-road_0654-tw_00450
    :parameters (?v - vehicle ?r - road_type_road_0266 ?next - road_type_road_0654 ?w - window_type_tw_00450)
    :precondition (and
      (ready-road ?v ?r)
      (road-next ?r ?next)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
    )
    :effect (and
      (not (ready-road ?v ?r))
      (ready-road ?v ?next)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-road_0266-road_0654-tw_00480
    :parameters (?v - vehicle ?r - road_type_road_0266 ?next - road_type_road_0654 ?w - window_type_tw_00480)
    :precondition (and
      (ready-road ?v ?r)
      (road-next ?r ?next)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
    )
    :effect (and
      (not (ready-road ?v ?r))
      (ready-road ?v ?next)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-road_0266-road_0654-tw_00510
    :parameters (?v - vehicle ?r - road_type_road_0266 ?next - road_type_road_0654 ?w - window_type_tw_00510)
    :precondition (and
      (ready-road ?v ?r)
      (road-next ?r ?next)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
    )
    :effect (and
      (not (ready-road ?v ?r))
      (ready-road ?v ?next)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-road_0266-road_0654-tw_00540
    :parameters (?v - vehicle ?r - road_type_road_0266 ?next - road_type_road_0654 ?w - window_type_tw_00540)
    :precondition (and
      (ready-road ?v ?r)
      (road-next ?r ?next)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
    )
    :effect (and
      (not (ready-road ?v ?r))
      (ready-road ?v ?next)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-road_0266-road_0654-tw_00570
    :parameters (?v - vehicle ?r - road_type_road_0266 ?next - road_type_road_0654 ?w - window_type_tw_00570)
    :precondition (and
      (ready-road ?v ?r)
      (road-next ?r ?next)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
    )
    :effect (and
      (not (ready-road ?v ?r))
      (ready-road ?v ?next)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-road_0266-road_0654-tw_00600
    :parameters (?v - vehicle ?r - road_type_road_0266 ?next - road_type_road_0654 ?w - window_type_tw_00600)
    :precondition (and
      (ready-road ?v ?r)
      (road-next ?r ?next)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
    )
    :effect (and
      (not (ready-road ?v ?r))
      (ready-road ?v ?next)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-static-road_0267-road_0412
    :parameters (?v - vehicle ?r - road_type_road_0267 ?next - road_type_road_0412)
    :precondition (and
      (ready-road ?v ?r)
      (road-next ?r ?next)
      (road-open ?r)
      (static-road ?r)
      (not (road-blocked ?r))
    )
    :effect (and
      (not (ready-road ?v ?r))
      (ready-road ?v ?next)
      (increase (travel-time ?v) (travel-duration ?r))
      (increase (sim-time) (travel-duration ?r))
    )
  )

  (:action traverse-road-static-road_0267-road_0413
    :parameters (?v - vehicle ?r - road_type_road_0267 ?next - road_type_road_0413)
    :precondition (and
      (ready-road ?v ?r)
      (road-next ?r ?next)
      (road-open ?r)
      (static-road ?r)
      (not (road-blocked ?r))
    )
    :effect (and
      (not (ready-road ?v ?r))
      (ready-road ?v ?next)
      (increase (travel-time ?v) (travel-duration ?r))
      (increase (sim-time) (travel-duration ?r))
    )
  )

  (:action traverse-road-static-road_0268-road_0265
    :parameters (?v - vehicle ?r - road_type_road_0268 ?next - road_type_road_0265)
    :precondition (and
      (ready-road ?v ?r)
      (road-next ?r ?next)
      (road-open ?r)
      (static-road ?r)
      (not (road-blocked ?r))
    )
    :effect (and
      (not (ready-road ?v ?r))
      (ready-road ?v ?next)
      (increase (travel-time ?v) (travel-duration ?r))
      (increase (sim-time) (travel-duration ?r))
    )
  )

  (:action traverse-road-static-road_0269-road_0156
    :parameters (?v - vehicle ?r - road_type_road_0269 ?next - road_type_road_0156)
    :precondition (and
      (ready-road ?v ?r)
      (road-next ?r ?next)
      (road-open ?r)
      (static-road ?r)
      (not (road-blocked ?r))
    )
    :effect (and
      (not (ready-road ?v ?r))
      (ready-road ?v ?next)
      (increase (travel-time ?v) (travel-duration ?r))
      (increase (sim-time) (travel-duration ?r))
    )
  )

  (:action traverse-road-static-road_0269-road_0157
    :parameters (?v - vehicle ?r - road_type_road_0269 ?next - road_type_road_0157)
    :precondition (and
      (ready-road ?v ?r)
      (road-next ?r ?next)
      (road-open ?r)
      (static-road ?r)
      (not (road-blocked ?r))
    )
    :effect (and
      (not (ready-road ?v ?r))
      (ready-road ?v ?next)
      (increase (travel-time ?v) (travel-duration ?r))
      (increase (sim-time) (travel-duration ?r))
    )
  )

  (:action traverse-road-static-road_0271-macro_0042
    :parameters (?v - vehicle ?r - road_type_road_0271 ?next - road_type_macro_0042)
    :precondition (and
      (ready-road ?v ?r)
      (road-next ?r ?next)
      (road-open ?r)
      (static-road ?r)
      (not (road-blocked ?r))
    )
    :effect (and
      (not (ready-road ?v ?r))
      (ready-road ?v ?next)
      (increase (travel-time ?v) (travel-duration ?r))
      (increase (sim-time) (travel-duration ?r))
    )
  )

  (:action traverse-road-static-road_0271-road_0583
    :parameters (?v - vehicle ?r - road_type_road_0271 ?next - road_type_road_0583)
    :precondition (and
      (ready-road ?v ?r)
      (road-next ?r ?next)
      (road-open ?r)
      (static-road ?r)
      (not (road-blocked ?r))
    )
    :effect (and
      (not (ready-road ?v ?r))
      (ready-road ?v ?next)
      (increase (travel-time ?v) (travel-duration ?r))
      (increase (sim-time) (travel-duration ?r))
    )
  )

  (:action traverse-road-static-road_0271-road_0584
    :parameters (?v - vehicle ?r - road_type_road_0271 ?next - road_type_road_0584)
    :precondition (and
      (ready-road ?v ?r)
      (road-next ?r ?next)
      (road-open ?r)
      (static-road ?r)
      (not (road-blocked ?r))
    )
    :effect (and
      (not (ready-road ?v ?r))
      (ready-road ?v ?next)
      (increase (travel-time ?v) (travel-duration ?r))
      (increase (sim-time) (travel-duration ?r))
    )
  )

  (:action traverse-road-static-road_0272-road_0283
    :parameters (?v - vehicle ?r - road_type_road_0272 ?next - road_type_road_0283)
    :precondition (and
      (ready-road ?v ?r)
      (road-next ?r ?next)
      (road-open ?r)
      (static-road ?r)
      (not (road-blocked ?r))
    )
    :effect (and
      (not (ready-road ?v ?r))
      (ready-road ?v ?next)
      (increase (travel-time ?v) (travel-duration ?r))
      (increase (sim-time) (travel-duration ?r))
    )
  )

  (:action traverse-road-static-road_0273-road_0139
    :parameters (?v - vehicle ?r - road_type_road_0273 ?next - road_type_road_0139)
    :precondition (and
      (ready-road ?v ?r)
      (road-next ?r ?next)
      (road-open ?r)
      (static-road ?r)
      (not (road-blocked ?r))
    )
    :effect (and
      (not (ready-road ?v ?r))
      (ready-road ?v ?next)
      (increase (travel-time ?v) (travel-duration ?r))
      (increase (sim-time) (travel-duration ?r))
    )
  )

  (:action traverse-road-static-road_0273-road_0140
    :parameters (?v - vehicle ?r - road_type_road_0273 ?next - road_type_road_0140)
    :precondition (and
      (ready-road ?v ?r)
      (road-next ?r ?next)
      (road-open ?r)
      (static-road ?r)
      (not (road-blocked ?r))
    )
    :effect (and
      (not (ready-road ?v ?r))
      (ready-road ?v ?next)
      (increase (travel-time ?v) (travel-duration ?r))
      (increase (sim-time) (travel-duration ?r))
    )
  )

  (:action traverse-road-static-road_0273-road_0141
    :parameters (?v - vehicle ?r - road_type_road_0273 ?next - road_type_road_0141)
    :precondition (and
      (ready-road ?v ?r)
      (road-next ?r ?next)
      (road-open ?r)
      (static-road ?r)
      (not (road-blocked ?r))
    )
    :effect (and
      (not (ready-road ?v ?r))
      (ready-road ?v ?next)
      (increase (travel-time ?v) (travel-duration ?r))
      (increase (sim-time) (travel-duration ?r))
    )
  )

  (:action traverse-road-static-road_0274-road_0342
    :parameters (?v - vehicle ?r - road_type_road_0274 ?next - road_type_road_0342)
    :precondition (and
      (ready-road ?v ?r)
      (road-next ?r ?next)
      (road-open ?r)
      (static-road ?r)
      (not (road-blocked ?r))
    )
    :effect (and
      (not (ready-road ?v ?r))
      (ready-road ?v ?next)
      (increase (travel-time ?v) (travel-duration ?r))
      (increase (sim-time) (travel-duration ?r))
    )
  )

  (:action traverse-road-static-road_0275-road_0673
    :parameters (?v - vehicle ?r - road_type_road_0275 ?next - road_type_road_0673)
    :precondition (and
      (ready-road ?v ?r)
      (road-next ?r ?next)
      (road-open ?r)
      (static-road ?r)
      (not (road-blocked ?r))
    )
    :effect (and
      (not (ready-road ?v ?r))
      (ready-road ?v ?next)
      (increase (travel-time ?v) (travel-duration ?r))
      (increase (sim-time) (travel-duration ?r))
    )
  )

  (:action traverse-road-static-road_0275-road_0674
    :parameters (?v - vehicle ?r - road_type_road_0275 ?next - road_type_road_0674)
    :precondition (and
      (ready-road ?v ?r)
      (road-next ?r ?next)
      (road-open ?r)
      (static-road ?r)
      (not (road-blocked ?r))
    )
    :effect (and
      (not (ready-road ?v ?r))
      (ready-road ?v ?next)
      (increase (travel-time ?v) (travel-duration ?r))
      (increase (sim-time) (travel-duration ?r))
    )
  )

  (:action traverse-road-static-road_0276-road_0077
    :parameters (?v - vehicle ?r - road_type_road_0276 ?next - road_type_road_0077)
    :precondition (and
      (ready-road ?v ?r)
      (road-next ?r ?next)
      (road-open ?r)
      (static-road ?r)
      (not (road-blocked ?r))
    )
    :effect (and
      (not (ready-road ?v ?r))
      (ready-road ?v ?next)
      (increase (travel-time ?v) (travel-duration ?r))
      (increase (sim-time) (travel-duration ?r))
    )
  )

  (:action traverse-road-static-road_0276-road_0078
    :parameters (?v - vehicle ?r - road_type_road_0276 ?next - road_type_road_0078)
    :precondition (and
      (ready-road ?v ?r)
      (road-next ?r ?next)
      (road-open ?r)
      (static-road ?r)
      (not (road-blocked ?r))
    )
    :effect (and
      (not (ready-road ?v ?r))
      (ready-road ?v ?next)
      (increase (travel-time ?v) (travel-duration ?r))
      (increase (sim-time) (travel-duration ?r))
    )
  )

  (:action traverse-road-static-road_0277-macro_0017
    :parameters (?v - vehicle ?r - road_type_road_0277 ?next - road_type_macro_0017)
    :precondition (and
      (ready-road ?v ?r)
      (road-next ?r ?next)
      (road-open ?r)
      (static-road ?r)
      (not (road-blocked ?r))
    )
    :effect (and
      (not (ready-road ?v ?r))
      (ready-road ?v ?next)
      (increase (travel-time ?v) (travel-duration ?r))
      (increase (sim-time) (travel-duration ?r))
    )
  )

  (:action traverse-road-static-road_0277-road_0227
    :parameters (?v - vehicle ?r - road_type_road_0277 ?next - road_type_road_0227)
    :precondition (and
      (ready-road ?v ?r)
      (road-next ?r ?next)
      (road-open ?r)
      (static-road ?r)
      (not (road-blocked ?r))
    )
    :effect (and
      (not (ready-road ?v ?r))
      (ready-road ?v ?next)
      (increase (travel-time ?v) (travel-duration ?r))
      (increase (sim-time) (travel-duration ?r))
    )
  )

  (:action traverse-road-dynamic-road_0281-road_0077-tw_00000
    :parameters (?v - vehicle ?r - road_type_road_0281 ?next - road_type_road_0077 ?w - window_type_tw_00000)
    :precondition (and
      (ready-road ?v ?r)
      (road-next ?r ?next)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
    )
    :effect (and
      (not (ready-road ?v ?r))
      (ready-road ?v ?next)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-road_0281-road_0077-tw_00030
    :parameters (?v - vehicle ?r - road_type_road_0281 ?next - road_type_road_0077 ?w - window_type_tw_00030)
    :precondition (and
      (ready-road ?v ?r)
      (road-next ?r ?next)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
    )
    :effect (and
      (not (ready-road ?v ?r))
      (ready-road ?v ?next)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-road_0281-road_0077-tw_00060
    :parameters (?v - vehicle ?r - road_type_road_0281 ?next - road_type_road_0077 ?w - window_type_tw_00060)
    :precondition (and
      (ready-road ?v ?r)
      (road-next ?r ?next)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
    )
    :effect (and
      (not (ready-road ?v ?r))
      (ready-road ?v ?next)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-road_0281-road_0077-tw_00090
    :parameters (?v - vehicle ?r - road_type_road_0281 ?next - road_type_road_0077 ?w - window_type_tw_00090)
    :precondition (and
      (ready-road ?v ?r)
      (road-next ?r ?next)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
    )
    :effect (and
      (not (ready-road ?v ?r))
      (ready-road ?v ?next)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-road_0281-road_0077-tw_00120
    :parameters (?v - vehicle ?r - road_type_road_0281 ?next - road_type_road_0077 ?w - window_type_tw_00120)
    :precondition (and
      (ready-road ?v ?r)
      (road-next ?r ?next)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
    )
    :effect (and
      (not (ready-road ?v ?r))
      (ready-road ?v ?next)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-road_0281-road_0077-tw_00150
    :parameters (?v - vehicle ?r - road_type_road_0281 ?next - road_type_road_0077 ?w - window_type_tw_00150)
    :precondition (and
      (ready-road ?v ?r)
      (road-next ?r ?next)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
    )
    :effect (and
      (not (ready-road ?v ?r))
      (ready-road ?v ?next)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-road_0281-road_0077-tw_00180
    :parameters (?v - vehicle ?r - road_type_road_0281 ?next - road_type_road_0077 ?w - window_type_tw_00180)
    :precondition (and
      (ready-road ?v ?r)
      (road-next ?r ?next)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
    )
    :effect (and
      (not (ready-road ?v ?r))
      (ready-road ?v ?next)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-road_0281-road_0077-tw_00210
    :parameters (?v - vehicle ?r - road_type_road_0281 ?next - road_type_road_0077 ?w - window_type_tw_00210)
    :precondition (and
      (ready-road ?v ?r)
      (road-next ?r ?next)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
    )
    :effect (and
      (not (ready-road ?v ?r))
      (ready-road ?v ?next)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-road_0281-road_0077-tw_00240
    :parameters (?v - vehicle ?r - road_type_road_0281 ?next - road_type_road_0077 ?w - window_type_tw_00240)
    :precondition (and
      (ready-road ?v ?r)
      (road-next ?r ?next)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
    )
    :effect (and
      (not (ready-road ?v ?r))
      (ready-road ?v ?next)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-road_0281-road_0077-tw_00270
    :parameters (?v - vehicle ?r - road_type_road_0281 ?next - road_type_road_0077 ?w - window_type_tw_00270)
    :precondition (and
      (ready-road ?v ?r)
      (road-next ?r ?next)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
    )
    :effect (and
      (not (ready-road ?v ?r))
      (ready-road ?v ?next)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-road_0281-road_0077-tw_00300
    :parameters (?v - vehicle ?r - road_type_road_0281 ?next - road_type_road_0077 ?w - window_type_tw_00300)
    :precondition (and
      (ready-road ?v ?r)
      (road-next ?r ?next)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
    )
    :effect (and
      (not (ready-road ?v ?r))
      (ready-road ?v ?next)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-road_0281-road_0077-tw_00330
    :parameters (?v - vehicle ?r - road_type_road_0281 ?next - road_type_road_0077 ?w - window_type_tw_00330)
    :precondition (and
      (ready-road ?v ?r)
      (road-next ?r ?next)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
    )
    :effect (and
      (not (ready-road ?v ?r))
      (ready-road ?v ?next)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-road_0281-road_0077-tw_00360
    :parameters (?v - vehicle ?r - road_type_road_0281 ?next - road_type_road_0077 ?w - window_type_tw_00360)
    :precondition (and
      (ready-road ?v ?r)
      (road-next ?r ?next)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
    )
    :effect (and
      (not (ready-road ?v ?r))
      (ready-road ?v ?next)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-road_0281-road_0077-tw_00390
    :parameters (?v - vehicle ?r - road_type_road_0281 ?next - road_type_road_0077 ?w - window_type_tw_00390)
    :precondition (and
      (ready-road ?v ?r)
      (road-next ?r ?next)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
    )
    :effect (and
      (not (ready-road ?v ?r))
      (ready-road ?v ?next)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-road_0281-road_0077-tw_00420
    :parameters (?v - vehicle ?r - road_type_road_0281 ?next - road_type_road_0077 ?w - window_type_tw_00420)
    :precondition (and
      (ready-road ?v ?r)
      (road-next ?r ?next)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
    )
    :effect (and
      (not (ready-road ?v ?r))
      (ready-road ?v ?next)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-road_0281-road_0077-tw_00450
    :parameters (?v - vehicle ?r - road_type_road_0281 ?next - road_type_road_0077 ?w - window_type_tw_00450)
    :precondition (and
      (ready-road ?v ?r)
      (road-next ?r ?next)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
    )
    :effect (and
      (not (ready-road ?v ?r))
      (ready-road ?v ?next)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-road_0281-road_0077-tw_00480
    :parameters (?v - vehicle ?r - road_type_road_0281 ?next - road_type_road_0077 ?w - window_type_tw_00480)
    :precondition (and
      (ready-road ?v ?r)
      (road-next ?r ?next)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
    )
    :effect (and
      (not (ready-road ?v ?r))
      (ready-road ?v ?next)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-road_0281-road_0077-tw_00510
    :parameters (?v - vehicle ?r - road_type_road_0281 ?next - road_type_road_0077 ?w - window_type_tw_00510)
    :precondition (and
      (ready-road ?v ?r)
      (road-next ?r ?next)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
    )
    :effect (and
      (not (ready-road ?v ?r))
      (ready-road ?v ?next)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-road_0281-road_0077-tw_00540
    :parameters (?v - vehicle ?r - road_type_road_0281 ?next - road_type_road_0077 ?w - window_type_tw_00540)
    :precondition (and
      (ready-road ?v ?r)
      (road-next ?r ?next)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
    )
    :effect (and
      (not (ready-road ?v ?r))
      (ready-road ?v ?next)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-road_0281-road_0077-tw_00570
    :parameters (?v - vehicle ?r - road_type_road_0281 ?next - road_type_road_0077 ?w - window_type_tw_00570)
    :precondition (and
      (ready-road ?v ?r)
      (road-next ?r ?next)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
    )
    :effect (and
      (not (ready-road ?v ?r))
      (ready-road ?v ?next)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-road_0281-road_0077-tw_00600
    :parameters (?v - vehicle ?r - road_type_road_0281 ?next - road_type_road_0077 ?w - window_type_tw_00600)
    :precondition (and
      (ready-road ?v ?r)
      (road-next ?r ?next)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
    )
    :effect (and
      (not (ready-road ?v ?r))
      (ready-road ?v ?next)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-road_0281-road_0078-tw_00000
    :parameters (?v - vehicle ?r - road_type_road_0281 ?next - road_type_road_0078 ?w - window_type_tw_00000)
    :precondition (and
      (ready-road ?v ?r)
      (road-next ?r ?next)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
    )
    :effect (and
      (not (ready-road ?v ?r))
      (ready-road ?v ?next)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-road_0281-road_0078-tw_00030
    :parameters (?v - vehicle ?r - road_type_road_0281 ?next - road_type_road_0078 ?w - window_type_tw_00030)
    :precondition (and
      (ready-road ?v ?r)
      (road-next ?r ?next)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
    )
    :effect (and
      (not (ready-road ?v ?r))
      (ready-road ?v ?next)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-road_0281-road_0078-tw_00060
    :parameters (?v - vehicle ?r - road_type_road_0281 ?next - road_type_road_0078 ?w - window_type_tw_00060)
    :precondition (and
      (ready-road ?v ?r)
      (road-next ?r ?next)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
    )
    :effect (and
      (not (ready-road ?v ?r))
      (ready-road ?v ?next)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-road_0281-road_0078-tw_00090
    :parameters (?v - vehicle ?r - road_type_road_0281 ?next - road_type_road_0078 ?w - window_type_tw_00090)
    :precondition (and
      (ready-road ?v ?r)
      (road-next ?r ?next)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
    )
    :effect (and
      (not (ready-road ?v ?r))
      (ready-road ?v ?next)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-road_0281-road_0078-tw_00120
    :parameters (?v - vehicle ?r - road_type_road_0281 ?next - road_type_road_0078 ?w - window_type_tw_00120)
    :precondition (and
      (ready-road ?v ?r)
      (road-next ?r ?next)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
    )
    :effect (and
      (not (ready-road ?v ?r))
      (ready-road ?v ?next)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-road_0281-road_0078-tw_00150
    :parameters (?v - vehicle ?r - road_type_road_0281 ?next - road_type_road_0078 ?w - window_type_tw_00150)
    :precondition (and
      (ready-road ?v ?r)
      (road-next ?r ?next)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
    )
    :effect (and
      (not (ready-road ?v ?r))
      (ready-road ?v ?next)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-road_0281-road_0078-tw_00180
    :parameters (?v - vehicle ?r - road_type_road_0281 ?next - road_type_road_0078 ?w - window_type_tw_00180)
    :precondition (and
      (ready-road ?v ?r)
      (road-next ?r ?next)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
    )
    :effect (and
      (not (ready-road ?v ?r))
      (ready-road ?v ?next)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-road_0281-road_0078-tw_00210
    :parameters (?v - vehicle ?r - road_type_road_0281 ?next - road_type_road_0078 ?w - window_type_tw_00210)
    :precondition (and
      (ready-road ?v ?r)
      (road-next ?r ?next)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
    )
    :effect (and
      (not (ready-road ?v ?r))
      (ready-road ?v ?next)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-road_0281-road_0078-tw_00240
    :parameters (?v - vehicle ?r - road_type_road_0281 ?next - road_type_road_0078 ?w - window_type_tw_00240)
    :precondition (and
      (ready-road ?v ?r)
      (road-next ?r ?next)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
    )
    :effect (and
      (not (ready-road ?v ?r))
      (ready-road ?v ?next)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-road_0281-road_0078-tw_00270
    :parameters (?v - vehicle ?r - road_type_road_0281 ?next - road_type_road_0078 ?w - window_type_tw_00270)
    :precondition (and
      (ready-road ?v ?r)
      (road-next ?r ?next)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
    )
    :effect (and
      (not (ready-road ?v ?r))
      (ready-road ?v ?next)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-road_0281-road_0078-tw_00300
    :parameters (?v - vehicle ?r - road_type_road_0281 ?next - road_type_road_0078 ?w - window_type_tw_00300)
    :precondition (and
      (ready-road ?v ?r)
      (road-next ?r ?next)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
    )
    :effect (and
      (not (ready-road ?v ?r))
      (ready-road ?v ?next)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-road_0281-road_0078-tw_00330
    :parameters (?v - vehicle ?r - road_type_road_0281 ?next - road_type_road_0078 ?w - window_type_tw_00330)
    :precondition (and
      (ready-road ?v ?r)
      (road-next ?r ?next)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
    )
    :effect (and
      (not (ready-road ?v ?r))
      (ready-road ?v ?next)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-road_0281-road_0078-tw_00360
    :parameters (?v - vehicle ?r - road_type_road_0281 ?next - road_type_road_0078 ?w - window_type_tw_00360)
    :precondition (and
      (ready-road ?v ?r)
      (road-next ?r ?next)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
    )
    :effect (and
      (not (ready-road ?v ?r))
      (ready-road ?v ?next)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-road_0281-road_0078-tw_00390
    :parameters (?v - vehicle ?r - road_type_road_0281 ?next - road_type_road_0078 ?w - window_type_tw_00390)
    :precondition (and
      (ready-road ?v ?r)
      (road-next ?r ?next)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
    )
    :effect (and
      (not (ready-road ?v ?r))
      (ready-road ?v ?next)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-road_0281-road_0078-tw_00420
    :parameters (?v - vehicle ?r - road_type_road_0281 ?next - road_type_road_0078 ?w - window_type_tw_00420)
    :precondition (and
      (ready-road ?v ?r)
      (road-next ?r ?next)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
    )
    :effect (and
      (not (ready-road ?v ?r))
      (ready-road ?v ?next)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-road_0281-road_0078-tw_00450
    :parameters (?v - vehicle ?r - road_type_road_0281 ?next - road_type_road_0078 ?w - window_type_tw_00450)
    :precondition (and
      (ready-road ?v ?r)
      (road-next ?r ?next)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
    )
    :effect (and
      (not (ready-road ?v ?r))
      (ready-road ?v ?next)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-road_0281-road_0078-tw_00480
    :parameters (?v - vehicle ?r - road_type_road_0281 ?next - road_type_road_0078 ?w - window_type_tw_00480)
    :precondition (and
      (ready-road ?v ?r)
      (road-next ?r ?next)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
    )
    :effect (and
      (not (ready-road ?v ?r))
      (ready-road ?v ?next)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-road_0281-road_0078-tw_00510
    :parameters (?v - vehicle ?r - road_type_road_0281 ?next - road_type_road_0078 ?w - window_type_tw_00510)
    :precondition (and
      (ready-road ?v ?r)
      (road-next ?r ?next)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
    )
    :effect (and
      (not (ready-road ?v ?r))
      (ready-road ?v ?next)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-road_0281-road_0078-tw_00540
    :parameters (?v - vehicle ?r - road_type_road_0281 ?next - road_type_road_0078 ?w - window_type_tw_00540)
    :precondition (and
      (ready-road ?v ?r)
      (road-next ?r ?next)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
    )
    :effect (and
      (not (ready-road ?v ?r))
      (ready-road ?v ?next)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-road_0281-road_0078-tw_00570
    :parameters (?v - vehicle ?r - road_type_road_0281 ?next - road_type_road_0078 ?w - window_type_tw_00570)
    :precondition (and
      (ready-road ?v ?r)
      (road-next ?r ?next)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
    )
    :effect (and
      (not (ready-road ?v ?r))
      (ready-road ?v ?next)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-road_0281-road_0078-tw_00600
    :parameters (?v - vehicle ?r - road_type_road_0281 ?next - road_type_road_0078 ?w - window_type_tw_00600)
    :precondition (and
      (ready-road ?v ?r)
      (road-next ?r ?next)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
    )
    :effect (and
      (not (ready-road ?v ?r))
      (ready-road ?v ?next)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-static-road_0282-road_0286
    :parameters (?v - vehicle ?r - road_type_road_0282 ?next - road_type_road_0286)
    :precondition (and
      (ready-road ?v ?r)
      (road-next ?r ?next)
      (road-open ?r)
      (static-road ?r)
      (not (road-blocked ?r))
    )
    :effect (and
      (not (ready-road ?v ?r))
      (ready-road ?v ?next)
      (increase (travel-time ?v) (travel-duration ?r))
      (increase (sim-time) (travel-duration ?r))
    )
  )

  (:action traverse-road-static-road_0282-road_0287
    :parameters (?v - vehicle ?r - road_type_road_0282 ?next - road_type_road_0287)
    :precondition (and
      (ready-road ?v ?r)
      (road-next ?r ?next)
      (road-open ?r)
      (static-road ?r)
      (not (road-blocked ?r))
    )
    :effect (and
      (not (ready-road ?v ?r))
      (ready-road ?v ?next)
      (increase (travel-time ?v) (travel-duration ?r))
      (increase (sim-time) (travel-duration ?r))
    )
  )

  (:action traverse-road-static-road_0282-road_0288
    :parameters (?v - vehicle ?r - road_type_road_0282 ?next - road_type_road_0288)
    :precondition (and
      (ready-road ?v ?r)
      (road-next ?r ?next)
      (road-open ?r)
      (static-road ?r)
      (not (road-blocked ?r))
    )
    :effect (and
      (not (ready-road ?v ?r))
      (ready-road ?v ?next)
      (increase (travel-time ?v) (travel-duration ?r))
      (increase (sim-time) (travel-duration ?r))
    )
  )

  (:action traverse-road-dynamic-road_0283-road_0289-tw_00000
    :parameters (?v - vehicle ?r - road_type_road_0283 ?next - road_type_road_0289 ?w - window_type_tw_00000)
    :precondition (and
      (ready-road ?v ?r)
      (road-next ?r ?next)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
    )
    :effect (and
      (not (ready-road ?v ?r))
      (ready-road ?v ?next)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-road_0283-road_0289-tw_00030
    :parameters (?v - vehicle ?r - road_type_road_0283 ?next - road_type_road_0289 ?w - window_type_tw_00030)
    :precondition (and
      (ready-road ?v ?r)
      (road-next ?r ?next)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
    )
    :effect (and
      (not (ready-road ?v ?r))
      (ready-road ?v ?next)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-road_0283-road_0289-tw_00060
    :parameters (?v - vehicle ?r - road_type_road_0283 ?next - road_type_road_0289 ?w - window_type_tw_00060)
    :precondition (and
      (ready-road ?v ?r)
      (road-next ?r ?next)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
    )
    :effect (and
      (not (ready-road ?v ?r))
      (ready-road ?v ?next)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-road_0283-road_0289-tw_00090
    :parameters (?v - vehicle ?r - road_type_road_0283 ?next - road_type_road_0289 ?w - window_type_tw_00090)
    :precondition (and
      (ready-road ?v ?r)
      (road-next ?r ?next)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
    )
    :effect (and
      (not (ready-road ?v ?r))
      (ready-road ?v ?next)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-road_0283-road_0289-tw_00120
    :parameters (?v - vehicle ?r - road_type_road_0283 ?next - road_type_road_0289 ?w - window_type_tw_00120)
    :precondition (and
      (ready-road ?v ?r)
      (road-next ?r ?next)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
    )
    :effect (and
      (not (ready-road ?v ?r))
      (ready-road ?v ?next)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-road_0283-road_0289-tw_00150
    :parameters (?v - vehicle ?r - road_type_road_0283 ?next - road_type_road_0289 ?w - window_type_tw_00150)
    :precondition (and
      (ready-road ?v ?r)
      (road-next ?r ?next)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
    )
    :effect (and
      (not (ready-road ?v ?r))
      (ready-road ?v ?next)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-road_0283-road_0289-tw_00180
    :parameters (?v - vehicle ?r - road_type_road_0283 ?next - road_type_road_0289 ?w - window_type_tw_00180)
    :precondition (and
      (ready-road ?v ?r)
      (road-next ?r ?next)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
    )
    :effect (and
      (not (ready-road ?v ?r))
      (ready-road ?v ?next)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-road_0283-road_0289-tw_00210
    :parameters (?v - vehicle ?r - road_type_road_0283 ?next - road_type_road_0289 ?w - window_type_tw_00210)
    :precondition (and
      (ready-road ?v ?r)
      (road-next ?r ?next)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
    )
    :effect (and
      (not (ready-road ?v ?r))
      (ready-road ?v ?next)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-road_0283-road_0289-tw_00240
    :parameters (?v - vehicle ?r - road_type_road_0283 ?next - road_type_road_0289 ?w - window_type_tw_00240)
    :precondition (and
      (ready-road ?v ?r)
      (road-next ?r ?next)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
    )
    :effect (and
      (not (ready-road ?v ?r))
      (ready-road ?v ?next)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-road_0283-road_0289-tw_00270
    :parameters (?v - vehicle ?r - road_type_road_0283 ?next - road_type_road_0289 ?w - window_type_tw_00270)
    :precondition (and
      (ready-road ?v ?r)
      (road-next ?r ?next)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
    )
    :effect (and
      (not (ready-road ?v ?r))
      (ready-road ?v ?next)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-road_0283-road_0289-tw_00300
    :parameters (?v - vehicle ?r - road_type_road_0283 ?next - road_type_road_0289 ?w - window_type_tw_00300)
    :precondition (and
      (ready-road ?v ?r)
      (road-next ?r ?next)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
    )
    :effect (and
      (not (ready-road ?v ?r))
      (ready-road ?v ?next)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-road_0283-road_0289-tw_00330
    :parameters (?v - vehicle ?r - road_type_road_0283 ?next - road_type_road_0289 ?w - window_type_tw_00330)
    :precondition (and
      (ready-road ?v ?r)
      (road-next ?r ?next)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
    )
    :effect (and
      (not (ready-road ?v ?r))
      (ready-road ?v ?next)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-road_0283-road_0289-tw_00360
    :parameters (?v - vehicle ?r - road_type_road_0283 ?next - road_type_road_0289 ?w - window_type_tw_00360)
    :precondition (and
      (ready-road ?v ?r)
      (road-next ?r ?next)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
    )
    :effect (and
      (not (ready-road ?v ?r))
      (ready-road ?v ?next)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-road_0283-road_0289-tw_00390
    :parameters (?v - vehicle ?r - road_type_road_0283 ?next - road_type_road_0289 ?w - window_type_tw_00390)
    :precondition (and
      (ready-road ?v ?r)
      (road-next ?r ?next)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
    )
    :effect (and
      (not (ready-road ?v ?r))
      (ready-road ?v ?next)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-road_0283-road_0289-tw_00420
    :parameters (?v - vehicle ?r - road_type_road_0283 ?next - road_type_road_0289 ?w - window_type_tw_00420)
    :precondition (and
      (ready-road ?v ?r)
      (road-next ?r ?next)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
    )
    :effect (and
      (not (ready-road ?v ?r))
      (ready-road ?v ?next)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-road_0283-road_0289-tw_00450
    :parameters (?v - vehicle ?r - road_type_road_0283 ?next - road_type_road_0289 ?w - window_type_tw_00450)
    :precondition (and
      (ready-road ?v ?r)
      (road-next ?r ?next)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
    )
    :effect (and
      (not (ready-road ?v ?r))
      (ready-road ?v ?next)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-road_0283-road_0289-tw_00480
    :parameters (?v - vehicle ?r - road_type_road_0283 ?next - road_type_road_0289 ?w - window_type_tw_00480)
    :precondition (and
      (ready-road ?v ?r)
      (road-next ?r ?next)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
    )
    :effect (and
      (not (ready-road ?v ?r))
      (ready-road ?v ?next)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-road_0283-road_0289-tw_00510
    :parameters (?v - vehicle ?r - road_type_road_0283 ?next - road_type_road_0289 ?w - window_type_tw_00510)
    :precondition (and
      (ready-road ?v ?r)
      (road-next ?r ?next)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
    )
    :effect (and
      (not (ready-road ?v ?r))
      (ready-road ?v ?next)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-road_0283-road_0289-tw_00540
    :parameters (?v - vehicle ?r - road_type_road_0283 ?next - road_type_road_0289 ?w - window_type_tw_00540)
    :precondition (and
      (ready-road ?v ?r)
      (road-next ?r ?next)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
    )
    :effect (and
      (not (ready-road ?v ?r))
      (ready-road ?v ?next)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-road_0283-road_0289-tw_00570
    :parameters (?v - vehicle ?r - road_type_road_0283 ?next - road_type_road_0289 ?w - window_type_tw_00570)
    :precondition (and
      (ready-road ?v ?r)
      (road-next ?r ?next)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
    )
    :effect (and
      (not (ready-road ?v ?r))
      (ready-road ?v ?next)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-road_0283-road_0289-tw_00600
    :parameters (?v - vehicle ?r - road_type_road_0283 ?next - road_type_road_0289 ?w - window_type_tw_00600)
    :precondition (and
      (ready-road ?v ?r)
      (road-next ?r ?next)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
    )
    :effect (and
      (not (ready-road ?v ?r))
      (ready-road ?v ?next)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-static-road_0286-road_0281
    :parameters (?v - vehicle ?r - road_type_road_0286 ?next - road_type_road_0281)
    :precondition (and
      (ready-road ?v ?r)
      (road-next ?r ?next)
      (road-open ?r)
      (static-road ?r)
      (not (road-blocked ?r))
    )
    :effect (and
      (not (ready-road ?v ?r))
      (ready-road ?v ?next)
      (increase (travel-time ?v) (travel-duration ?r))
      (increase (sim-time) (travel-duration ?r))
    )
  )

  (:action traverse-road-static-road_0286-road_0282
    :parameters (?v - vehicle ?r - road_type_road_0286 ?next - road_type_road_0282)
    :precondition (and
      (ready-road ?v ?r)
      (road-next ?r ?next)
      (road-open ?r)
      (static-road ?r)
      (not (road-blocked ?r))
    )
    :effect (and
      (not (ready-road ?v ?r))
      (ready-road ?v ?next)
      (increase (travel-time ?v) (travel-duration ?r))
      (increase (sim-time) (travel-duration ?r))
    )
  )

  (:action traverse-road-static-road_0287-road_0069
    :parameters (?v - vehicle ?r - road_type_road_0287 ?next - road_type_road_0069)
    :precondition (and
      (ready-road ?v ?r)
      (road-next ?r ?next)
      (road-open ?r)
      (static-road ?r)
      (not (road-blocked ?r))
    )
    :effect (and
      (not (ready-road ?v ?r))
      (ready-road ?v ?next)
      (increase (travel-time ?v) (travel-duration ?r))
      (increase (sim-time) (travel-duration ?r))
    )
  )

  (:action traverse-road-static-road_0287-road_0070
    :parameters (?v - vehicle ?r - road_type_road_0287 ?next - road_type_road_0070)
    :precondition (and
      (ready-road ?v ?r)
      (road-next ?r ?next)
      (road-open ?r)
      (static-road ?r)
      (not (road-blocked ?r))
    )
    :effect (and
      (not (ready-road ?v ?r))
      (ready-road ?v ?next)
      (increase (travel-time ?v) (travel-duration ?r))
      (increase (sim-time) (travel-duration ?r))
    )
  )

  (:action traverse-road-static-road_0287-road_0071
    :parameters (?v - vehicle ?r - road_type_road_0287 ?next - road_type_road_0071)
    :precondition (and
      (ready-road ?v ?r)
      (road-next ?r ?next)
      (road-open ?r)
      (static-road ?r)
      (not (road-blocked ?r))
    )
    :effect (and
      (not (ready-road ?v ?r))
      (ready-road ?v ?next)
      (increase (travel-time ?v) (travel-duration ?r))
      (increase (sim-time) (travel-duration ?r))
    )
  )

  (:action traverse-road-static-road_0288-road_0045
    :parameters (?v - vehicle ?r - road_type_road_0288 ?next - road_type_road_0045)
    :precondition (and
      (ready-road ?v ?r)
      (road-next ?r ?next)
      (road-open ?r)
      (static-road ?r)
      (not (road-blocked ?r))
    )
    :effect (and
      (not (ready-road ?v ?r))
      (ready-road ?v ?next)
      (increase (travel-time ?v) (travel-duration ?r))
      (increase (sim-time) (travel-duration ?r))
    )
  )

  (:action traverse-road-static-road_0288-road_0046
    :parameters (?v - vehicle ?r - road_type_road_0288 ?next - road_type_road_0046)
    :precondition (and
      (ready-road ?v ?r)
      (road-next ?r ?next)
      (road-open ?r)
      (static-road ?r)
      (not (road-blocked ?r))
    )
    :effect (and
      (not (ready-road ?v ?r))
      (ready-road ?v ?next)
      (increase (travel-time ?v) (travel-duration ?r))
      (increase (sim-time) (travel-duration ?r))
    )
  )

  (:action traverse-road-dynamic-road_0289-macro_0022-tw_00000
    :parameters (?v - vehicle ?r - road_type_road_0289 ?next - road_type_macro_0022 ?w - window_type_tw_00000)
    :precondition (and
      (ready-road ?v ?r)
      (road-next ?r ?next)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
    )
    :effect (and
      (not (ready-road ?v ?r))
      (ready-road ?v ?next)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-road_0289-macro_0022-tw_00030
    :parameters (?v - vehicle ?r - road_type_road_0289 ?next - road_type_macro_0022 ?w - window_type_tw_00030)
    :precondition (and
      (ready-road ?v ?r)
      (road-next ?r ?next)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
    )
    :effect (and
      (not (ready-road ?v ?r))
      (ready-road ?v ?next)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-road_0289-macro_0022-tw_00060
    :parameters (?v - vehicle ?r - road_type_road_0289 ?next - road_type_macro_0022 ?w - window_type_tw_00060)
    :precondition (and
      (ready-road ?v ?r)
      (road-next ?r ?next)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
    )
    :effect (and
      (not (ready-road ?v ?r))
      (ready-road ?v ?next)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-road_0289-macro_0022-tw_00090
    :parameters (?v - vehicle ?r - road_type_road_0289 ?next - road_type_macro_0022 ?w - window_type_tw_00090)
    :precondition (and
      (ready-road ?v ?r)
      (road-next ?r ?next)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
    )
    :effect (and
      (not (ready-road ?v ?r))
      (ready-road ?v ?next)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-road_0289-macro_0022-tw_00120
    :parameters (?v - vehicle ?r - road_type_road_0289 ?next - road_type_macro_0022 ?w - window_type_tw_00120)
    :precondition (and
      (ready-road ?v ?r)
      (road-next ?r ?next)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
    )
    :effect (and
      (not (ready-road ?v ?r))
      (ready-road ?v ?next)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-road_0289-macro_0022-tw_00150
    :parameters (?v - vehicle ?r - road_type_road_0289 ?next - road_type_macro_0022 ?w - window_type_tw_00150)
    :precondition (and
      (ready-road ?v ?r)
      (road-next ?r ?next)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
    )
    :effect (and
      (not (ready-road ?v ?r))
      (ready-road ?v ?next)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-road_0289-macro_0022-tw_00180
    :parameters (?v - vehicle ?r - road_type_road_0289 ?next - road_type_macro_0022 ?w - window_type_tw_00180)
    :precondition (and
      (ready-road ?v ?r)
      (road-next ?r ?next)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
    )
    :effect (and
      (not (ready-road ?v ?r))
      (ready-road ?v ?next)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-road_0289-macro_0022-tw_00210
    :parameters (?v - vehicle ?r - road_type_road_0289 ?next - road_type_macro_0022 ?w - window_type_tw_00210)
    :precondition (and
      (ready-road ?v ?r)
      (road-next ?r ?next)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
    )
    :effect (and
      (not (ready-road ?v ?r))
      (ready-road ?v ?next)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-road_0289-macro_0022-tw_00240
    :parameters (?v - vehicle ?r - road_type_road_0289 ?next - road_type_macro_0022 ?w - window_type_tw_00240)
    :precondition (and
      (ready-road ?v ?r)
      (road-next ?r ?next)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
    )
    :effect (and
      (not (ready-road ?v ?r))
      (ready-road ?v ?next)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-road_0289-macro_0022-tw_00270
    :parameters (?v - vehicle ?r - road_type_road_0289 ?next - road_type_macro_0022 ?w - window_type_tw_00270)
    :precondition (and
      (ready-road ?v ?r)
      (road-next ?r ?next)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
    )
    :effect (and
      (not (ready-road ?v ?r))
      (ready-road ?v ?next)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-road_0289-macro_0022-tw_00300
    :parameters (?v - vehicle ?r - road_type_road_0289 ?next - road_type_macro_0022 ?w - window_type_tw_00300)
    :precondition (and
      (ready-road ?v ?r)
      (road-next ?r ?next)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
    )
    :effect (and
      (not (ready-road ?v ?r))
      (ready-road ?v ?next)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-road_0289-macro_0022-tw_00330
    :parameters (?v - vehicle ?r - road_type_road_0289 ?next - road_type_macro_0022 ?w - window_type_tw_00330)
    :precondition (and
      (ready-road ?v ?r)
      (road-next ?r ?next)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
    )
    :effect (and
      (not (ready-road ?v ?r))
      (ready-road ?v ?next)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-road_0289-macro_0022-tw_00360
    :parameters (?v - vehicle ?r - road_type_road_0289 ?next - road_type_macro_0022 ?w - window_type_tw_00360)
    :precondition (and
      (ready-road ?v ?r)
      (road-next ?r ?next)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
    )
    :effect (and
      (not (ready-road ?v ?r))
      (ready-road ?v ?next)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-road_0289-macro_0022-tw_00390
    :parameters (?v - vehicle ?r - road_type_road_0289 ?next - road_type_macro_0022 ?w - window_type_tw_00390)
    :precondition (and
      (ready-road ?v ?r)
      (road-next ?r ?next)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
    )
    :effect (and
      (not (ready-road ?v ?r))
      (ready-road ?v ?next)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-road_0289-macro_0022-tw_00420
    :parameters (?v - vehicle ?r - road_type_road_0289 ?next - road_type_macro_0022 ?w - window_type_tw_00420)
    :precondition (and
      (ready-road ?v ?r)
      (road-next ?r ?next)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
    )
    :effect (and
      (not (ready-road ?v ?r))
      (ready-road ?v ?next)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-road_0289-macro_0022-tw_00450
    :parameters (?v - vehicle ?r - road_type_road_0289 ?next - road_type_macro_0022 ?w - window_type_tw_00450)
    :precondition (and
      (ready-road ?v ?r)
      (road-next ?r ?next)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
    )
    :effect (and
      (not (ready-road ?v ?r))
      (ready-road ?v ?next)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-road_0289-macro_0022-tw_00480
    :parameters (?v - vehicle ?r - road_type_road_0289 ?next - road_type_macro_0022 ?w - window_type_tw_00480)
    :precondition (and
      (ready-road ?v ?r)
      (road-next ?r ?next)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
    )
    :effect (and
      (not (ready-road ?v ?r))
      (ready-road ?v ?next)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-road_0289-macro_0022-tw_00510
    :parameters (?v - vehicle ?r - road_type_road_0289 ?next - road_type_macro_0022 ?w - window_type_tw_00510)
    :precondition (and
      (ready-road ?v ?r)
      (road-next ?r ?next)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
    )
    :effect (and
      (not (ready-road ?v ?r))
      (ready-road ?v ?next)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-road_0289-macro_0022-tw_00540
    :parameters (?v - vehicle ?r - road_type_road_0289 ?next - road_type_macro_0022 ?w - window_type_tw_00540)
    :precondition (and
      (ready-road ?v ?r)
      (road-next ?r ?next)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
    )
    :effect (and
      (not (ready-road ?v ?r))
      (ready-road ?v ?next)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-road_0289-macro_0022-tw_00570
    :parameters (?v - vehicle ?r - road_type_road_0289 ?next - road_type_macro_0022 ?w - window_type_tw_00570)
    :precondition (and
      (ready-road ?v ?r)
      (road-next ?r ?next)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
    )
    :effect (and
      (not (ready-road ?v ?r))
      (ready-road ?v ?next)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-road_0289-macro_0022-tw_00600
    :parameters (?v - vehicle ?r - road_type_road_0289 ?next - road_type_macro_0022 ?w - window_type_tw_00600)
    :precondition (and
      (ready-road ?v ?r)
      (road-next ?r ?next)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
    )
    :effect (and
      (not (ready-road ?v ?r))
      (ready-road ?v ?next)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-road_0289-macro_0023-tw_00000
    :parameters (?v - vehicle ?r - road_type_road_0289 ?next - road_type_macro_0023 ?w - window_type_tw_00000)
    :precondition (and
      (ready-road ?v ?r)
      (road-next ?r ?next)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
    )
    :effect (and
      (not (ready-road ?v ?r))
      (ready-road ?v ?next)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-road_0289-macro_0023-tw_00030
    :parameters (?v - vehicle ?r - road_type_road_0289 ?next - road_type_macro_0023 ?w - window_type_tw_00030)
    :precondition (and
      (ready-road ?v ?r)
      (road-next ?r ?next)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
    )
    :effect (and
      (not (ready-road ?v ?r))
      (ready-road ?v ?next)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-road_0289-macro_0023-tw_00060
    :parameters (?v - vehicle ?r - road_type_road_0289 ?next - road_type_macro_0023 ?w - window_type_tw_00060)
    :precondition (and
      (ready-road ?v ?r)
      (road-next ?r ?next)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
    )
    :effect (and
      (not (ready-road ?v ?r))
      (ready-road ?v ?next)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-road_0289-macro_0023-tw_00090
    :parameters (?v - vehicle ?r - road_type_road_0289 ?next - road_type_macro_0023 ?w - window_type_tw_00090)
    :precondition (and
      (ready-road ?v ?r)
      (road-next ?r ?next)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
    )
    :effect (and
      (not (ready-road ?v ?r))
      (ready-road ?v ?next)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-road_0289-macro_0023-tw_00120
    :parameters (?v - vehicle ?r - road_type_road_0289 ?next - road_type_macro_0023 ?w - window_type_tw_00120)
    :precondition (and
      (ready-road ?v ?r)
      (road-next ?r ?next)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
    )
    :effect (and
      (not (ready-road ?v ?r))
      (ready-road ?v ?next)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-road_0289-macro_0023-tw_00150
    :parameters (?v - vehicle ?r - road_type_road_0289 ?next - road_type_macro_0023 ?w - window_type_tw_00150)
    :precondition (and
      (ready-road ?v ?r)
      (road-next ?r ?next)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
    )
    :effect (and
      (not (ready-road ?v ?r))
      (ready-road ?v ?next)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-road_0289-macro_0023-tw_00180
    :parameters (?v - vehicle ?r - road_type_road_0289 ?next - road_type_macro_0023 ?w - window_type_tw_00180)
    :precondition (and
      (ready-road ?v ?r)
      (road-next ?r ?next)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
    )
    :effect (and
      (not (ready-road ?v ?r))
      (ready-road ?v ?next)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-road_0289-macro_0023-tw_00210
    :parameters (?v - vehicle ?r - road_type_road_0289 ?next - road_type_macro_0023 ?w - window_type_tw_00210)
    :precondition (and
      (ready-road ?v ?r)
      (road-next ?r ?next)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
    )
    :effect (and
      (not (ready-road ?v ?r))
      (ready-road ?v ?next)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-road_0289-macro_0023-tw_00240
    :parameters (?v - vehicle ?r - road_type_road_0289 ?next - road_type_macro_0023 ?w - window_type_tw_00240)
    :precondition (and
      (ready-road ?v ?r)
      (road-next ?r ?next)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
    )
    :effect (and
      (not (ready-road ?v ?r))
      (ready-road ?v ?next)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-road_0289-macro_0023-tw_00270
    :parameters (?v - vehicle ?r - road_type_road_0289 ?next - road_type_macro_0023 ?w - window_type_tw_00270)
    :precondition (and
      (ready-road ?v ?r)
      (road-next ?r ?next)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
    )
    :effect (and
      (not (ready-road ?v ?r))
      (ready-road ?v ?next)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-road_0289-macro_0023-tw_00300
    :parameters (?v - vehicle ?r - road_type_road_0289 ?next - road_type_macro_0023 ?w - window_type_tw_00300)
    :precondition (and
      (ready-road ?v ?r)
      (road-next ?r ?next)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
    )
    :effect (and
      (not (ready-road ?v ?r))
      (ready-road ?v ?next)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-road_0289-macro_0023-tw_00330
    :parameters (?v - vehicle ?r - road_type_road_0289 ?next - road_type_macro_0023 ?w - window_type_tw_00330)
    :precondition (and
      (ready-road ?v ?r)
      (road-next ?r ?next)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
    )
    :effect (and
      (not (ready-road ?v ?r))
      (ready-road ?v ?next)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-road_0289-macro_0023-tw_00360
    :parameters (?v - vehicle ?r - road_type_road_0289 ?next - road_type_macro_0023 ?w - window_type_tw_00360)
    :precondition (and
      (ready-road ?v ?r)
      (road-next ?r ?next)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
    )
    :effect (and
      (not (ready-road ?v ?r))
      (ready-road ?v ?next)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-road_0289-macro_0023-tw_00390
    :parameters (?v - vehicle ?r - road_type_road_0289 ?next - road_type_macro_0023 ?w - window_type_tw_00390)
    :precondition (and
      (ready-road ?v ?r)
      (road-next ?r ?next)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
    )
    :effect (and
      (not (ready-road ?v ?r))
      (ready-road ?v ?next)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-road_0289-macro_0023-tw_00420
    :parameters (?v - vehicle ?r - road_type_road_0289 ?next - road_type_macro_0023 ?w - window_type_tw_00420)
    :precondition (and
      (ready-road ?v ?r)
      (road-next ?r ?next)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
    )
    :effect (and
      (not (ready-road ?v ?r))
      (ready-road ?v ?next)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-road_0289-macro_0023-tw_00450
    :parameters (?v - vehicle ?r - road_type_road_0289 ?next - road_type_macro_0023 ?w - window_type_tw_00450)
    :precondition (and
      (ready-road ?v ?r)
      (road-next ?r ?next)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
    )
    :effect (and
      (not (ready-road ?v ?r))
      (ready-road ?v ?next)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-road_0289-macro_0023-tw_00480
    :parameters (?v - vehicle ?r - road_type_road_0289 ?next - road_type_macro_0023 ?w - window_type_tw_00480)
    :precondition (and
      (ready-road ?v ?r)
      (road-next ?r ?next)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
    )
    :effect (and
      (not (ready-road ?v ?r))
      (ready-road ?v ?next)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-road_0289-macro_0023-tw_00510
    :parameters (?v - vehicle ?r - road_type_road_0289 ?next - road_type_macro_0023 ?w - window_type_tw_00510)
    :precondition (and
      (ready-road ?v ?r)
      (road-next ?r ?next)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
    )
    :effect (and
      (not (ready-road ?v ?r))
      (ready-road ?v ?next)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-road_0289-macro_0023-tw_00540
    :parameters (?v - vehicle ?r - road_type_road_0289 ?next - road_type_macro_0023 ?w - window_type_tw_00540)
    :precondition (and
      (ready-road ?v ?r)
      (road-next ?r ?next)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
    )
    :effect (and
      (not (ready-road ?v ?r))
      (ready-road ?v ?next)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-road_0289-macro_0023-tw_00570
    :parameters (?v - vehicle ?r - road_type_road_0289 ?next - road_type_macro_0023 ?w - window_type_tw_00570)
    :precondition (and
      (ready-road ?v ?r)
      (road-next ?r ?next)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
    )
    :effect (and
      (not (ready-road ?v ?r))
      (ready-road ?v ?next)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-road_0289-macro_0023-tw_00600
    :parameters (?v - vehicle ?r - road_type_road_0289 ?next - road_type_macro_0023 ?w - window_type_tw_00600)
    :precondition (and
      (ready-road ?v ?r)
      (road-next ?r ?next)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
    )
    :effect (and
      (not (ready-road ?v ?r))
      (ready-road ?v ?next)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-static-road_0290-road_0807
    :parameters (?v - vehicle ?r - road_type_road_0290 ?next - road_type_road_0807)
    :precondition (and
      (ready-road ?v ?r)
      (road-next ?r ?next)
      (road-open ?r)
      (static-road ?r)
      (not (road-blocked ?r))
    )
    :effect (and
      (not (ready-road ?v ?r))
      (ready-road ?v ?next)
      (increase (travel-time ?v) (travel-duration ?r))
      (increase (sim-time) (travel-duration ?r))
    )
  )

  (:action traverse-road-static-road_0290-road_0808
    :parameters (?v - vehicle ?r - road_type_road_0290 ?next - road_type_road_0808)
    :precondition (and
      (ready-road ?v ?r)
      (road-next ?r ?next)
      (road-open ?r)
      (static-road ?r)
      (not (road-blocked ?r))
    )
    :effect (and
      (not (ready-road ?v ?r))
      (ready-road ?v ?next)
      (increase (travel-time ?v) (travel-duration ?r))
      (increase (sim-time) (travel-duration ?r))
    )
  )

  (:action traverse-road-static-road_0290-road_0809
    :parameters (?v - vehicle ?r - road_type_road_0290 ?next - road_type_road_0809)
    :precondition (and
      (ready-road ?v ?r)
      (road-next ?r ?next)
      (road-open ?r)
      (static-road ?r)
      (not (road-blocked ?r))
    )
    :effect (and
      (not (ready-road ?v ?r))
      (ready-road ?v ?next)
      (increase (travel-time ?v) (travel-duration ?r))
      (increase (sim-time) (travel-duration ?r))
    )
  )

  (:action traverse-road-static-road_0291-road_0295
    :parameters (?v - vehicle ?r - road_type_road_0291 ?next - road_type_road_0295)
    :precondition (and
      (ready-road ?v ?r)
      (road-next ?r ?next)
      (road-open ?r)
      (static-road ?r)
      (not (road-blocked ?r))
    )
    :effect (and
      (not (ready-road ?v ?r))
      (ready-road ?v ?next)
      (increase (travel-time ?v) (travel-duration ?r))
      (increase (sim-time) (travel-duration ?r))
    )
  )

  (:action traverse-road-static-road_0291-road_0296
    :parameters (?v - vehicle ?r - road_type_road_0291 ?next - road_type_road_0296)
    :precondition (and
      (ready-road ?v ?r)
      (road-next ?r ?next)
      (road-open ?r)
      (static-road ?r)
      (not (road-blocked ?r))
    )
    :effect (and
      (not (ready-road ?v ?r))
      (ready-road ?v ?next)
      (increase (travel-time ?v) (travel-duration ?r))
      (increase (sim-time) (travel-duration ?r))
    )
  )

  (:action traverse-road-static-road_0292-road_0304
    :parameters (?v - vehicle ?r - road_type_road_0292 ?next - road_type_road_0304)
    :precondition (and
      (ready-road ?v ?r)
      (road-next ?r ?next)
      (road-open ?r)
      (static-road ?r)
      (not (road-blocked ?r))
    )
    :effect (and
      (not (ready-road ?v ?r))
      (ready-road ?v ?next)
      (increase (travel-time ?v) (travel-duration ?r))
      (increase (sim-time) (travel-duration ?r))
    )
  )

  (:action traverse-road-static-road_0294-road_0786
    :parameters (?v - vehicle ?r - road_type_road_0294 ?next - road_type_road_0786)
    :precondition (and
      (ready-road ?v ?r)
      (road-next ?r ?next)
      (road-open ?r)
      (static-road ?r)
      (not (road-blocked ?r))
    )
    :effect (and
      (not (ready-road ?v ?r))
      (ready-road ?v ?next)
      (increase (travel-time ?v) (travel-duration ?r))
      (increase (sim-time) (travel-duration ?r))
    )
  )

  (:action traverse-road-static-road_0295-road_0207
    :parameters (?v - vehicle ?r - road_type_road_0295 ?next - road_type_road_0207)
    :precondition (and
      (ready-road ?v ?r)
      (road-next ?r ?next)
      (road-open ?r)
      (static-road ?r)
      (not (road-blocked ?r))
    )
    :effect (and
      (not (ready-road ?v ?r))
      (ready-road ?v ?next)
      (increase (travel-time ?v) (travel-duration ?r))
      (increase (sim-time) (travel-duration ?r))
    )
  )

  (:action traverse-road-static-road_0295-road_0208
    :parameters (?v - vehicle ?r - road_type_road_0295 ?next - road_type_road_0208)
    :precondition (and
      (ready-road ?v ?r)
      (road-next ?r ?next)
      (road-open ?r)
      (static-road ?r)
      (not (road-blocked ?r))
    )
    :effect (and
      (not (ready-road ?v ?r))
      (ready-road ?v ?next)
      (increase (travel-time ?v) (travel-duration ?r))
      (increase (sim-time) (travel-duration ?r))
    )
  )

  (:action traverse-road-static-road_0295-road_0209
    :parameters (?v - vehicle ?r - road_type_road_0295 ?next - road_type_road_0209)
    :precondition (and
      (ready-road ?v ?r)
      (road-next ?r ?next)
      (road-open ?r)
      (static-road ?r)
      (not (road-blocked ?r))
    )
    :effect (and
      (not (ready-road ?v ?r))
      (ready-road ?v ?next)
      (increase (travel-time ?v) (travel-duration ?r))
      (increase (sim-time) (travel-duration ?r))
    )
  )

  (:action traverse-road-static-road_0296-road_0117
    :parameters (?v - vehicle ?r - road_type_road_0296 ?next - road_type_road_0117)
    :precondition (and
      (ready-road ?v ?r)
      (road-next ?r ?next)
      (road-open ?r)
      (static-road ?r)
      (not (road-blocked ?r))
    )
    :effect (and
      (not (ready-road ?v ?r))
      (ready-road ?v ?next)
      (increase (travel-time ?v) (travel-duration ?r))
      (increase (sim-time) (travel-duration ?r))
    )
  )

  (:action traverse-road-static-road_0296-road_0118
    :parameters (?v - vehicle ?r - road_type_road_0296 ?next - road_type_road_0118)
    :precondition (and
      (ready-road ?v ?r)
      (road-next ?r ?next)
      (road-open ?r)
      (static-road ?r)
      (not (road-blocked ?r))
    )
    :effect (and
      (not (ready-road ?v ?r))
      (ready-road ?v ?next)
      (increase (travel-time ?v) (travel-duration ?r))
      (increase (sim-time) (travel-duration ?r))
    )
  )

  (:action traverse-road-static-road_0296-road_0119
    :parameters (?v - vehicle ?r - road_type_road_0296 ?next - road_type_road_0119)
    :precondition (and
      (ready-road ?v ?r)
      (road-next ?r ?next)
      (road-open ?r)
      (static-road ?r)
      (not (road-blocked ?r))
    )
    :effect (and
      (not (ready-road ?v ?r))
      (ready-road ?v ?next)
      (increase (travel-time ?v) (travel-duration ?r))
      (increase (sim-time) (travel-duration ?r))
    )
  )

  (:action traverse-road-static-road_0297-road_0651
    :parameters (?v - vehicle ?r - road_type_road_0297 ?next - road_type_road_0651)
    :precondition (and
      (ready-road ?v ?r)
      (road-next ?r ?next)
      (road-open ?r)
      (static-road ?r)
      (not (road-blocked ?r))
    )
    :effect (and
      (not (ready-road ?v ?r))
      (ready-road ?v ?next)
      (increase (travel-time ?v) (travel-duration ?r))
      (increase (sim-time) (travel-duration ?r))
    )
  )

  (:action traverse-road-static-road_0297-road_0652
    :parameters (?v - vehicle ?r - road_type_road_0297 ?next - road_type_road_0652)
    :precondition (and
      (ready-road ?v ?r)
      (road-next ?r ?next)
      (road-open ?r)
      (static-road ?r)
      (not (road-blocked ?r))
    )
    :effect (and
      (not (ready-road ?v ?r))
      (ready-road ?v ?next)
      (increase (travel-time ?v) (travel-duration ?r))
      (increase (sim-time) (travel-duration ?r))
    )
  )

  (:action traverse-road-static-road_0297-road_0653
    :parameters (?v - vehicle ?r - road_type_road_0297 ?next - road_type_road_0653)
    :precondition (and
      (ready-road ?v ?r)
      (road-next ?r ?next)
      (road-open ?r)
      (static-road ?r)
      (not (road-blocked ?r))
    )
    :effect (and
      (not (ready-road ?v ?r))
      (ready-road ?v ?next)
      (increase (travel-time ?v) (travel-duration ?r))
      (increase (sim-time) (travel-duration ?r))
    )
  )

  (:action traverse-road-static-road_0298-road_0303
    :parameters (?v - vehicle ?r - road_type_road_0298 ?next - road_type_road_0303)
    :precondition (and
      (ready-road ?v ?r)
      (road-next ?r ?next)
      (road-open ?r)
      (static-road ?r)
      (not (road-blocked ?r))
    )
    :effect (and
      (not (ready-road ?v ?r))
      (ready-road ?v ?next)
      (increase (travel-time ?v) (travel-duration ?r))
      (increase (sim-time) (travel-duration ?r))
    )
  )

  (:action traverse-road-static-road_0299-road_0301
    :parameters (?v - vehicle ?r - road_type_road_0299 ?next - road_type_road_0301)
    :precondition (and
      (ready-road ?v ?r)
      (road-next ?r ?next)
      (road-open ?r)
      (static-road ?r)
      (not (road-blocked ?r))
    )
    :effect (and
      (not (ready-road ?v ?r))
      (ready-road ?v ?next)
      (increase (travel-time ?v) (travel-duration ?r))
      (increase (sim-time) (travel-duration ?r))
    )
  )

  (:action traverse-road-static-road_0299-road_0302
    :parameters (?v - vehicle ?r - road_type_road_0299 ?next - road_type_road_0302)
    :precondition (and
      (ready-road ?v ?r)
      (road-next ?r ?next)
      (road-open ?r)
      (static-road ?r)
      (not (road-blocked ?r))
    )
    :effect (and
      (not (ready-road ?v ?r))
      (ready-road ?v ?next)
      (increase (travel-time ?v) (travel-duration ?r))
      (increase (sim-time) (travel-duration ?r))
    )
  )

  (:action traverse-road-static-road_0300-road_0204
    :parameters (?v - vehicle ?r - road_type_road_0300 ?next - road_type_road_0204)
    :precondition (and
      (ready-road ?v ?r)
      (road-next ?r ?next)
      (road-open ?r)
      (static-road ?r)
      (not (road-blocked ?r))
    )
    :effect (and
      (not (ready-road ?v ?r))
      (ready-road ?v ?next)
      (increase (travel-time ?v) (travel-duration ?r))
      (increase (sim-time) (travel-duration ?r))
    )
  )

  (:action traverse-road-dynamic-road_0301-road_0176-tw_00000
    :parameters (?v - vehicle ?r - road_type_road_0301 ?next - road_type_road_0176 ?w - window_type_tw_00000)
    :precondition (and
      (ready-road ?v ?r)
      (road-next ?r ?next)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
    )
    :effect (and
      (not (ready-road ?v ?r))
      (ready-road ?v ?next)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-road_0301-road_0176-tw_00030
    :parameters (?v - vehicle ?r - road_type_road_0301 ?next - road_type_road_0176 ?w - window_type_tw_00030)
    :precondition (and
      (ready-road ?v ?r)
      (road-next ?r ?next)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
    )
    :effect (and
      (not (ready-road ?v ?r))
      (ready-road ?v ?next)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-road_0301-road_0176-tw_00060
    :parameters (?v - vehicle ?r - road_type_road_0301 ?next - road_type_road_0176 ?w - window_type_tw_00060)
    :precondition (and
      (ready-road ?v ?r)
      (road-next ?r ?next)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
    )
    :effect (and
      (not (ready-road ?v ?r))
      (ready-road ?v ?next)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-road_0301-road_0176-tw_00090
    :parameters (?v - vehicle ?r - road_type_road_0301 ?next - road_type_road_0176 ?w - window_type_tw_00090)
    :precondition (and
      (ready-road ?v ?r)
      (road-next ?r ?next)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
    )
    :effect (and
      (not (ready-road ?v ?r))
      (ready-road ?v ?next)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-road_0301-road_0176-tw_00120
    :parameters (?v - vehicle ?r - road_type_road_0301 ?next - road_type_road_0176 ?w - window_type_tw_00120)
    :precondition (and
      (ready-road ?v ?r)
      (road-next ?r ?next)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
    )
    :effect (and
      (not (ready-road ?v ?r))
      (ready-road ?v ?next)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-road_0301-road_0176-tw_00150
    :parameters (?v - vehicle ?r - road_type_road_0301 ?next - road_type_road_0176 ?w - window_type_tw_00150)
    :precondition (and
      (ready-road ?v ?r)
      (road-next ?r ?next)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
    )
    :effect (and
      (not (ready-road ?v ?r))
      (ready-road ?v ?next)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-road_0301-road_0176-tw_00180
    :parameters (?v - vehicle ?r - road_type_road_0301 ?next - road_type_road_0176 ?w - window_type_tw_00180)
    :precondition (and
      (ready-road ?v ?r)
      (road-next ?r ?next)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
    )
    :effect (and
      (not (ready-road ?v ?r))
      (ready-road ?v ?next)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-road_0301-road_0176-tw_00210
    :parameters (?v - vehicle ?r - road_type_road_0301 ?next - road_type_road_0176 ?w - window_type_tw_00210)
    :precondition (and
      (ready-road ?v ?r)
      (road-next ?r ?next)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
    )
    :effect (and
      (not (ready-road ?v ?r))
      (ready-road ?v ?next)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-road_0301-road_0176-tw_00240
    :parameters (?v - vehicle ?r - road_type_road_0301 ?next - road_type_road_0176 ?w - window_type_tw_00240)
    :precondition (and
      (ready-road ?v ?r)
      (road-next ?r ?next)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
    )
    :effect (and
      (not (ready-road ?v ?r))
      (ready-road ?v ?next)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-road_0301-road_0176-tw_00270
    :parameters (?v - vehicle ?r - road_type_road_0301 ?next - road_type_road_0176 ?w - window_type_tw_00270)
    :precondition (and
      (ready-road ?v ?r)
      (road-next ?r ?next)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
    )
    :effect (and
      (not (ready-road ?v ?r))
      (ready-road ?v ?next)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-road_0301-road_0176-tw_00300
    :parameters (?v - vehicle ?r - road_type_road_0301 ?next - road_type_road_0176 ?w - window_type_tw_00300)
    :precondition (and
      (ready-road ?v ?r)
      (road-next ?r ?next)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
    )
    :effect (and
      (not (ready-road ?v ?r))
      (ready-road ?v ?next)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-road_0301-road_0176-tw_00330
    :parameters (?v - vehicle ?r - road_type_road_0301 ?next - road_type_road_0176 ?w - window_type_tw_00330)
    :precondition (and
      (ready-road ?v ?r)
      (road-next ?r ?next)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
    )
    :effect (and
      (not (ready-road ?v ?r))
      (ready-road ?v ?next)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-road_0301-road_0176-tw_00360
    :parameters (?v - vehicle ?r - road_type_road_0301 ?next - road_type_road_0176 ?w - window_type_tw_00360)
    :precondition (and
      (ready-road ?v ?r)
      (road-next ?r ?next)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
    )
    :effect (and
      (not (ready-road ?v ?r))
      (ready-road ?v ?next)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-road_0301-road_0176-tw_00390
    :parameters (?v - vehicle ?r - road_type_road_0301 ?next - road_type_road_0176 ?w - window_type_tw_00390)
    :precondition (and
      (ready-road ?v ?r)
      (road-next ?r ?next)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
    )
    :effect (and
      (not (ready-road ?v ?r))
      (ready-road ?v ?next)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-road_0301-road_0176-tw_00420
    :parameters (?v - vehicle ?r - road_type_road_0301 ?next - road_type_road_0176 ?w - window_type_tw_00420)
    :precondition (and
      (ready-road ?v ?r)
      (road-next ?r ?next)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
    )
    :effect (and
      (not (ready-road ?v ?r))
      (ready-road ?v ?next)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-road_0301-road_0176-tw_00450
    :parameters (?v - vehicle ?r - road_type_road_0301 ?next - road_type_road_0176 ?w - window_type_tw_00450)
    :precondition (and
      (ready-road ?v ?r)
      (road-next ?r ?next)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
    )
    :effect (and
      (not (ready-road ?v ?r))
      (ready-road ?v ?next)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-road_0301-road_0176-tw_00480
    :parameters (?v - vehicle ?r - road_type_road_0301 ?next - road_type_road_0176 ?w - window_type_tw_00480)
    :precondition (and
      (ready-road ?v ?r)
      (road-next ?r ?next)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
    )
    :effect (and
      (not (ready-road ?v ?r))
      (ready-road ?v ?next)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-road_0301-road_0176-tw_00510
    :parameters (?v - vehicle ?r - road_type_road_0301 ?next - road_type_road_0176 ?w - window_type_tw_00510)
    :precondition (and
      (ready-road ?v ?r)
      (road-next ?r ?next)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
    )
    :effect (and
      (not (ready-road ?v ?r))
      (ready-road ?v ?next)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-road_0301-road_0176-tw_00540
    :parameters (?v - vehicle ?r - road_type_road_0301 ?next - road_type_road_0176 ?w - window_type_tw_00540)
    :precondition (and
      (ready-road ?v ?r)
      (road-next ?r ?next)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
    )
    :effect (and
      (not (ready-road ?v ?r))
      (ready-road ?v ?next)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-road_0301-road_0176-tw_00570
    :parameters (?v - vehicle ?r - road_type_road_0301 ?next - road_type_road_0176 ?w - window_type_tw_00570)
    :precondition (and
      (ready-road ?v ?r)
      (road-next ?r ?next)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
    )
    :effect (and
      (not (ready-road ?v ?r))
      (ready-road ?v ?next)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-road_0301-road_0176-tw_00600
    :parameters (?v - vehicle ?r - road_type_road_0301 ?next - road_type_road_0176 ?w - window_type_tw_00600)
    :precondition (and
      (ready-road ?v ?r)
      (road-next ?r ?next)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
    )
    :effect (and
      (not (ready-road ?v ?r))
      (ready-road ?v ?next)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-static-road_0302-road_0298
    :parameters (?v - vehicle ?r - road_type_road_0302 ?next - road_type_road_0298)
    :precondition (and
      (ready-road ?v ?r)
      (road-next ?r ?next)
      (road-open ?r)
      (static-road ?r)
      (not (road-blocked ?r))
    )
    :effect (and
      (not (ready-road ?v ?r))
      (ready-road ?v ?next)
      (increase (travel-time ?v) (travel-duration ?r))
      (increase (sim-time) (travel-duration ?r))
    )
  )

  (:action traverse-road-static-road_0302-road_0299
    :parameters (?v - vehicle ?r - road_type_road_0302 ?next - road_type_road_0299)
    :precondition (and
      (ready-road ?v ?r)
      (road-next ?r ?next)
      (road-open ?r)
      (static-road ?r)
      (not (road-blocked ?r))
    )
    :effect (and
      (not (ready-road ?v ?r))
      (ready-road ?v ?next)
      (increase (travel-time ?v) (travel-duration ?r))
      (increase (sim-time) (travel-duration ?r))
    )
  )

  (:action traverse-road-static-road_0302-road_0300
    :parameters (?v - vehicle ?r - road_type_road_0302 ?next - road_type_road_0300)
    :precondition (and
      (ready-road ?v ?r)
      (road-next ?r ?next)
      (road-open ?r)
      (static-road ?r)
      (not (road-blocked ?r))
    )
    :effect (and
      (not (ready-road ?v ?r))
      (ready-road ?v ?next)
      (increase (travel-time ?v) (travel-duration ?r))
      (increase (sim-time) (travel-duration ?r))
    )
  )

  (:action traverse-road-static-road_0303-road_0180
    :parameters (?v - vehicle ?r - road_type_road_0303 ?next - road_type_road_0180)
    :precondition (and
      (ready-road ?v ?r)
      (road-next ?r ?next)
      (road-open ?r)
      (static-road ?r)
      (not (road-blocked ?r))
    )
    :effect (and
      (not (ready-road ?v ?r))
      (ready-road ?v ?next)
      (increase (travel-time ?v) (travel-duration ?r))
      (increase (sim-time) (travel-duration ?r))
    )
  )

  (:action traverse-road-static-road_0304-road_0211
    :parameters (?v - vehicle ?r - road_type_road_0304 ?next - road_type_road_0211)
    :precondition (and
      (ready-road ?v ?r)
      (road-next ?r ?next)
      (road-open ?r)
      (static-road ?r)
      (not (road-blocked ?r))
    )
    :effect (and
      (not (ready-road ?v ?r))
      (ready-road ?v ?next)
      (increase (travel-time ?v) (travel-duration ?r))
      (increase (sim-time) (travel-duration ?r))
    )
  )

  (:action traverse-road-static-road_0304-road_0212
    :parameters (?v - vehicle ?r - road_type_road_0304 ?next - road_type_road_0212)
    :precondition (and
      (ready-road ?v ?r)
      (road-next ?r ?next)
      (road-open ?r)
      (static-road ?r)
      (not (road-blocked ?r))
    )
    :effect (and
      (not (ready-road ?v ?r))
      (ready-road ?v ?next)
      (increase (travel-time ?v) (travel-duration ?r))
      (increase (sim-time) (travel-duration ?r))
    )
  )

  (:action traverse-road-static-road_0305-macro_0038
    :parameters (?v - vehicle ?r - road_type_road_0305 ?next - road_type_macro_0038)
    :precondition (and
      (ready-road ?v ?r)
      (road-next ?r ?next)
      (road-open ?r)
      (static-road ?r)
      (not (road-blocked ?r))
    )
    :effect (and
      (not (ready-road ?v ?r))
      (ready-road ?v ?next)
      (increase (travel-time ?v) (travel-duration ?r))
      (increase (sim-time) (travel-duration ?r))
    )
  )

  (:action traverse-road-static-road_0305-road_0537
    :parameters (?v - vehicle ?r - road_type_road_0305 ?next - road_type_road_0537)
    :precondition (and
      (ready-road ?v ?r)
      (road-next ?r ?next)
      (road-open ?r)
      (static-road ?r)
      (not (road-blocked ?r))
    )
    :effect (and
      (not (ready-road ?v ?r))
      (ready-road ?v ?next)
      (increase (travel-time ?v) (travel-duration ?r))
      (increase (sim-time) (travel-duration ?r))
    )
  )

  (:action traverse-road-static-road_0309-road_0424
    :parameters (?v - vehicle ?r - road_type_road_0309 ?next - road_type_road_0424)
    :precondition (and
      (ready-road ?v ?r)
      (road-next ?r ?next)
      (road-open ?r)
      (static-road ?r)
      (not (road-blocked ?r))
    )
    :effect (and
      (not (ready-road ?v ?r))
      (ready-road ?v ?next)
      (increase (travel-time ?v) (travel-duration ?r))
      (increase (sim-time) (travel-duration ?r))
    )
  )

  (:action traverse-road-static-road_0309-road_0425
    :parameters (?v - vehicle ?r - road_type_road_0309 ?next - road_type_road_0425)
    :precondition (and
      (ready-road ?v ?r)
      (road-next ?r ?next)
      (road-open ?r)
      (static-road ?r)
      (not (road-blocked ?r))
    )
    :effect (and
      (not (ready-road ?v ?r))
      (ready-road ?v ?next)
      (increase (travel-time ?v) (travel-duration ?r))
      (increase (sim-time) (travel-duration ?r))
    )
  )

  (:action traverse-road-static-road_0310-road_0364
    :parameters (?v - vehicle ?r - road_type_road_0310 ?next - road_type_road_0364)
    :precondition (and
      (ready-road ?v ?r)
      (road-next ?r ?next)
      (road-open ?r)
      (static-road ?r)
      (not (road-blocked ?r))
    )
    :effect (and
      (not (ready-road ?v ?r))
      (ready-road ?v ?next)
      (increase (travel-time ?v) (travel-duration ?r))
      (increase (sim-time) (travel-duration ?r))
    )
  )

  (:action traverse-road-static-road_0310-road_0365
    :parameters (?v - vehicle ?r - road_type_road_0310 ?next - road_type_road_0365)
    :precondition (and
      (ready-road ?v ?r)
      (road-next ?r ?next)
      (road-open ?r)
      (static-road ?r)
      (not (road-blocked ?r))
    )
    :effect (and
      (not (ready-road ?v ?r))
      (ready-road ?v ?next)
      (increase (travel-time ?v) (travel-duration ?r))
      (increase (sim-time) (travel-duration ?r))
    )
  )

  (:action traverse-road-static-road_0311-road_0661
    :parameters (?v - vehicle ?r - road_type_road_0311 ?next - road_type_road_0661)
    :precondition (and
      (ready-road ?v ?r)
      (road-next ?r ?next)
      (road-open ?r)
      (static-road ?r)
      (not (road-blocked ?r))
    )
    :effect (and
      (not (ready-road ?v ?r))
      (ready-road ?v ?next)
      (increase (travel-time ?v) (travel-duration ?r))
      (increase (sim-time) (travel-duration ?r))
    )
  )

  (:action traverse-road-static-road_0311-road_0662
    :parameters (?v - vehicle ?r - road_type_road_0311 ?next - road_type_road_0662)
    :precondition (and
      (ready-road ?v ?r)
      (road-next ?r ?next)
      (road-open ?r)
      (static-road ?r)
      (not (road-blocked ?r))
    )
    :effect (and
      (not (ready-road ?v ?r))
      (ready-road ?v ?next)
      (increase (travel-time ?v) (travel-duration ?r))
      (increase (sim-time) (travel-duration ?r))
    )
  )

  (:action traverse-road-static-road_0312-road_0309
    :parameters (?v - vehicle ?r - road_type_road_0312 ?next - road_type_road_0309)
    :precondition (and
      (ready-road ?v ?r)
      (road-next ?r ?next)
      (road-open ?r)
      (static-road ?r)
      (not (road-blocked ?r))
    )
    :effect (and
      (not (ready-road ?v ?r))
      (ready-road ?v ?next)
      (increase (travel-time ?v) (travel-duration ?r))
      (increase (sim-time) (travel-duration ?r))
    )
  )

  (:action traverse-road-static-road_0312-road_0310
    :parameters (?v - vehicle ?r - road_type_road_0312 ?next - road_type_road_0310)
    :precondition (and
      (ready-road ?v ?r)
      (road-next ?r ?next)
      (road-open ?r)
      (static-road ?r)
      (not (road-blocked ?r))
    )
    :effect (and
      (not (ready-road ?v ?r))
      (ready-road ?v ?next)
      (increase (travel-time ?v) (travel-duration ?r))
      (increase (sim-time) (travel-duration ?r))
    )
  )

  (:action traverse-road-static-road_0313-road_0769
    :parameters (?v - vehicle ?r - road_type_road_0313 ?next - road_type_road_0769)
    :precondition (and
      (ready-road ?v ?r)
      (road-next ?r ?next)
      (road-open ?r)
      (static-road ?r)
      (not (road-blocked ?r))
    )
    :effect (and
      (not (ready-road ?v ?r))
      (ready-road ?v ?next)
      (increase (travel-time ?v) (travel-duration ?r))
      (increase (sim-time) (travel-duration ?r))
    )
  )

  (:action traverse-road-static-road_0313-road_0770
    :parameters (?v - vehicle ?r - road_type_road_0313 ?next - road_type_road_0770)
    :precondition (and
      (ready-road ?v ?r)
      (road-next ?r ?next)
      (road-open ?r)
      (static-road ?r)
      (not (road-blocked ?r))
    )
    :effect (and
      (not (ready-road ?v ?r))
      (ready-road ?v ?next)
      (increase (travel-time ?v) (travel-duration ?r))
      (increase (sim-time) (travel-duration ?r))
    )
  )

  (:action traverse-road-static-road_0313-road_0771
    :parameters (?v - vehicle ?r - road_type_road_0313 ?next - road_type_road_0771)
    :precondition (and
      (ready-road ?v ?r)
      (road-next ?r ?next)
      (road-open ?r)
      (static-road ?r)
      (not (road-blocked ?r))
    )
    :effect (and
      (not (ready-road ?v ?r))
      (ready-road ?v ?next)
      (increase (travel-time ?v) (travel-duration ?r))
      (increase (sim-time) (travel-duration ?r))
    )
  )

  (:action traverse-road-static-road_0314-road_0453
    :parameters (?v - vehicle ?r - road_type_road_0314 ?next - road_type_road_0453)
    :precondition (and
      (ready-road ?v ?r)
      (road-next ?r ?next)
      (road-open ?r)
      (static-road ?r)
      (not (road-blocked ?r))
    )
    :effect (and
      (not (ready-road ?v ?r))
      (ready-road ?v ?next)
      (increase (travel-time ?v) (travel-duration ?r))
      (increase (sim-time) (travel-duration ?r))
    )
  )

  (:action traverse-road-static-road_0314-road_0454
    :parameters (?v - vehicle ?r - road_type_road_0314 ?next - road_type_road_0454)
    :precondition (and
      (ready-road ?v ?r)
      (road-next ?r ?next)
      (road-open ?r)
      (static-road ?r)
      (not (road-blocked ?r))
    )
    :effect (and
      (not (ready-road ?v ?r))
      (ready-road ?v ?next)
      (increase (travel-time ?v) (travel-duration ?r))
      (increase (sim-time) (travel-duration ?r))
    )
  )

  (:action traverse-road-static-road_0314-road_0455
    :parameters (?v - vehicle ?r - road_type_road_0314 ?next - road_type_road_0455)
    :precondition (and
      (ready-road ?v ?r)
      (road-next ?r ?next)
      (road-open ?r)
      (static-road ?r)
      (not (road-blocked ?r))
    )
    :effect (and
      (not (ready-road ?v ?r))
      (ready-road ?v ?next)
      (increase (travel-time ?v) (travel-duration ?r))
      (increase (sim-time) (travel-duration ?r))
    )
  )

  (:action traverse-road-static-road_0315-road_0135
    :parameters (?v - vehicle ?r - road_type_road_0315 ?next - road_type_road_0135)
    :precondition (and
      (ready-road ?v ?r)
      (road-next ?r ?next)
      (road-open ?r)
      (static-road ?r)
      (not (road-blocked ?r))
    )
    :effect (and
      (not (ready-road ?v ?r))
      (ready-road ?v ?next)
      (increase (travel-time ?v) (travel-duration ?r))
      (increase (sim-time) (travel-duration ?r))
    )
  )

  (:action traverse-road-static-road_0315-road_0136
    :parameters (?v - vehicle ?r - road_type_road_0315 ?next - road_type_road_0136)
    :precondition (and
      (ready-road ?v ?r)
      (road-next ?r ?next)
      (road-open ?r)
      (static-road ?r)
      (not (road-blocked ?r))
    )
    :effect (and
      (not (ready-road ?v ?r))
      (ready-road ?v ?next)
      (increase (travel-time ?v) (travel-duration ?r))
      (increase (sim-time) (travel-duration ?r))
    )
  )

  (:action traverse-road-static-road_0316-road_0544
    :parameters (?v - vehicle ?r - road_type_road_0316 ?next - road_type_road_0544)
    :precondition (and
      (ready-road ?v ?r)
      (road-next ?r ?next)
      (road-open ?r)
      (static-road ?r)
      (not (road-blocked ?r))
    )
    :effect (and
      (not (ready-road ?v ?r))
      (ready-road ?v ?next)
      (increase (travel-time ?v) (travel-duration ?r))
      (increase (sim-time) (travel-duration ?r))
    )
  )

  (:action traverse-road-static-road_0316-road_0545
    :parameters (?v - vehicle ?r - road_type_road_0316 ?next - road_type_road_0545)
    :precondition (and
      (ready-road ?v ?r)
      (road-next ?r ?next)
      (road-open ?r)
      (static-road ?r)
      (not (road-blocked ?r))
    )
    :effect (and
      (not (ready-road ?v ?r))
      (ready-road ?v ?next)
      (increase (travel-time ?v) (travel-duration ?r))
      (increase (sim-time) (travel-duration ?r))
    )
  )

  (:action traverse-road-static-road_0317-road_0786
    :parameters (?v - vehicle ?r - road_type_road_0317 ?next - road_type_road_0786)
    :precondition (and
      (ready-road ?v ?r)
      (road-next ?r ?next)
      (road-open ?r)
      (static-road ?r)
      (not (road-blocked ?r))
    )
    :effect (and
      (not (ready-road ?v ?r))
      (ready-road ?v ?next)
      (increase (travel-time ?v) (travel-duration ?r))
      (increase (sim-time) (travel-duration ?r))
    )
  )

  (:action traverse-road-static-road_0318-road_0542
    :parameters (?v - vehicle ?r - road_type_road_0318 ?next - road_type_road_0542)
    :precondition (and
      (ready-road ?v ?r)
      (road-next ?r ?next)
      (road-open ?r)
      (static-road ?r)
      (not (road-blocked ?r))
    )
    :effect (and
      (not (ready-road ?v ?r))
      (ready-road ?v ?next)
      (increase (travel-time ?v) (travel-duration ?r))
      (increase (sim-time) (travel-duration ?r))
    )
  )

  (:action traverse-road-static-road_0318-road_0543
    :parameters (?v - vehicle ?r - road_type_road_0318 ?next - road_type_road_0543)
    :precondition (and
      (ready-road ?v ?r)
      (road-next ?r ?next)
      (road-open ?r)
      (static-road ?r)
      (not (road-blocked ?r))
    )
    :effect (and
      (not (ready-road ?v ?r))
      (ready-road ?v ?next)
      (increase (travel-time ?v) (travel-duration ?r))
      (increase (sim-time) (travel-duration ?r))
    )
  )

  (:action traverse-road-static-road_0319-road_0675
    :parameters (?v - vehicle ?r - road_type_road_0319 ?next - road_type_road_0675)
    :precondition (and
      (ready-road ?v ?r)
      (road-next ?r ?next)
      (road-open ?r)
      (static-road ?r)
      (not (road-blocked ?r))
    )
    :effect (and
      (not (ready-road ?v ?r))
      (ready-road ?v ?next)
      (increase (travel-time ?v) (travel-duration ?r))
      (increase (sim-time) (travel-duration ?r))
    )
  )

  (:action traverse-road-static-road_0319-road_0676
    :parameters (?v - vehicle ?r - road_type_road_0319 ?next - road_type_road_0676)
    :precondition (and
      (ready-road ?v ?r)
      (road-next ?r ?next)
      (road-open ?r)
      (static-road ?r)
      (not (road-blocked ?r))
    )
    :effect (and
      (not (ready-road ?v ?r))
      (ready-road ?v ?next)
      (increase (travel-time ?v) (travel-duration ?r))
      (increase (sim-time) (travel-duration ?r))
    )
  )

  (:action traverse-road-static-road_0320-road_0807
    :parameters (?v - vehicle ?r - road_type_road_0320 ?next - road_type_road_0807)
    :precondition (and
      (ready-road ?v ?r)
      (road-next ?r ?next)
      (road-open ?r)
      (static-road ?r)
      (not (road-blocked ?r))
    )
    :effect (and
      (not (ready-road ?v ?r))
      (ready-road ?v ?next)
      (increase (travel-time ?v) (travel-duration ?r))
      (increase (sim-time) (travel-duration ?r))
    )
  )

  (:action traverse-road-static-road_0320-road_0808
    :parameters (?v - vehicle ?r - road_type_road_0320 ?next - road_type_road_0808)
    :precondition (and
      (ready-road ?v ?r)
      (road-next ?r ?next)
      (road-open ?r)
      (static-road ?r)
      (not (road-blocked ?r))
    )
    :effect (and
      (not (ready-road ?v ?r))
      (ready-road ?v ?next)
      (increase (travel-time ?v) (travel-duration ?r))
      (increase (sim-time) (travel-duration ?r))
    )
  )

  (:action traverse-road-static-road_0320-road_0809
    :parameters (?v - vehicle ?r - road_type_road_0320 ?next - road_type_road_0809)
    :precondition (and
      (ready-road ?v ?r)
      (road-next ?r ?next)
      (road-open ?r)
      (static-road ?r)
      (not (road-blocked ?r))
    )
    :effect (and
      (not (ready-road ?v ?r))
      (ready-road ?v ?next)
      (increase (travel-time ?v) (travel-duration ?r))
      (increase (sim-time) (travel-duration ?r))
    )
  )

  (:action traverse-road-static-road_0321-road_0768
    :parameters (?v - vehicle ?r - road_type_road_0321 ?next - road_type_road_0768)
    :precondition (and
      (ready-road ?v ?r)
      (road-next ?r ?next)
      (road-open ?r)
      (static-road ?r)
      (not (road-blocked ?r))
    )
    :effect (and
      (not (ready-road ?v ?r))
      (ready-road ?v ?next)
      (increase (travel-time ?v) (travel-duration ?r))
      (increase (sim-time) (travel-duration ?r))
    )
  )

  (:action traverse-road-static-road_0322-road_0194
    :parameters (?v - vehicle ?r - road_type_road_0322 ?next - road_type_road_0194)
    :precondition (and
      (ready-road ?v ?r)
      (road-next ?r ?next)
      (road-open ?r)
      (static-road ?r)
      (not (road-blocked ?r))
    )
    :effect (and
      (not (ready-road ?v ?r))
      (ready-road ?v ?next)
      (increase (travel-time ?v) (travel-duration ?r))
      (increase (sim-time) (travel-duration ?r))
    )
  )

  (:action traverse-road-static-road_0322-road_0195
    :parameters (?v - vehicle ?r - road_type_road_0322 ?next - road_type_road_0195)
    :precondition (and
      (ready-road ?v ?r)
      (road-next ?r ?next)
      (road-open ?r)
      (static-road ?r)
      (not (road-blocked ?r))
    )
    :effect (and
      (not (ready-road ?v ?r))
      (ready-road ?v ?next)
      (increase (travel-time ?v) (travel-duration ?r))
      (increase (sim-time) (travel-duration ?r))
    )
  )

  (:action traverse-road-static-road_0323-road_0100
    :parameters (?v - vehicle ?r - road_type_road_0323 ?next - road_type_road_0100)
    :precondition (and
      (ready-road ?v ?r)
      (road-next ?r ?next)
      (road-open ?r)
      (static-road ?r)
      (not (road-blocked ?r))
    )
    :effect (and
      (not (ready-road ?v ?r))
      (ready-road ?v ?next)
      (increase (travel-time ?v) (travel-duration ?r))
      (increase (sim-time) (travel-duration ?r))
    )
  )

  (:action traverse-road-static-road_0324-road_0785
    :parameters (?v - vehicle ?r - road_type_road_0324 ?next - road_type_road_0785)
    :precondition (and
      (ready-road ?v ?r)
      (road-next ?r ?next)
      (road-open ?r)
      (static-road ?r)
      (not (road-blocked ?r))
    )
    :effect (and
      (not (ready-road ?v ?r))
      (ready-road ?v ?next)
      (increase (travel-time ?v) (travel-duration ?r))
      (increase (sim-time) (travel-duration ?r))
    )
  )

  (:action traverse-road-static-road_0326-road_0404
    :parameters (?v - vehicle ?r - road_type_road_0326 ?next - road_type_road_0404)
    :precondition (and
      (ready-road ?v ?r)
      (road-next ?r ?next)
      (road-open ?r)
      (static-road ?r)
      (not (road-blocked ?r))
    )
    :effect (and
      (not (ready-road ?v ?r))
      (ready-road ?v ?next)
      (increase (travel-time ?v) (travel-duration ?r))
      (increase (sim-time) (travel-duration ?r))
    )
  )

  (:action traverse-road-static-road_0327-road_0197
    :parameters (?v - vehicle ?r - road_type_road_0327 ?next - road_type_road_0197)
    :precondition (and
      (ready-road ?v ?r)
      (road-next ?r ?next)
      (road-open ?r)
      (static-road ?r)
      (not (road-blocked ?r))
    )
    :effect (and
      (not (ready-road ?v ?r))
      (ready-road ?v ?next)
      (increase (travel-time ?v) (travel-duration ?r))
      (increase (sim-time) (travel-duration ?r))
    )
  )

  (:action traverse-road-static-road_0327-road_0198
    :parameters (?v - vehicle ?r - road_type_road_0327 ?next - road_type_road_0198)
    :precondition (and
      (ready-road ?v ?r)
      (road-next ?r ?next)
      (road-open ?r)
      (static-road ?r)
      (not (road-blocked ?r))
    )
    :effect (and
      (not (ready-road ?v ?r))
      (ready-road ?v ?next)
      (increase (travel-time ?v) (travel-duration ?r))
      (increase (sim-time) (travel-duration ?r))
    )
  )

  (:action traverse-road-static-road_0327-road_0199
    :parameters (?v - vehicle ?r - road_type_road_0327 ?next - road_type_road_0199)
    :precondition (and
      (ready-road ?v ?r)
      (road-next ?r ?next)
      (road-open ?r)
      (static-road ?r)
      (not (road-blocked ?r))
    )
    :effect (and
      (not (ready-road ?v ?r))
      (ready-road ?v ?next)
      (increase (travel-time ?v) (travel-duration ?r))
      (increase (sim-time) (travel-duration ?r))
    )
  )

  (:action traverse-road-static-road_0328-road_0267
    :parameters (?v - vehicle ?r - road_type_road_0328 ?next - road_type_road_0267)
    :precondition (and
      (ready-road ?v ?r)
      (road-next ?r ?next)
      (road-open ?r)
      (static-road ?r)
      (not (road-blocked ?r))
    )
    :effect (and
      (not (ready-road ?v ?r))
      (ready-road ?v ?next)
      (increase (travel-time ?v) (travel-duration ?r))
      (increase (sim-time) (travel-duration ?r))
    )
  )

  (:action traverse-road-dynamic-road_0329-road_0581-tw_00000
    :parameters (?v - vehicle ?r - road_type_road_0329 ?next - road_type_road_0581 ?w - window_type_tw_00000)
    :precondition (and
      (ready-road ?v ?r)
      (road-next ?r ?next)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
    )
    :effect (and
      (not (ready-road ?v ?r))
      (ready-road ?v ?next)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-road_0329-road_0581-tw_00030
    :parameters (?v - vehicle ?r - road_type_road_0329 ?next - road_type_road_0581 ?w - window_type_tw_00030)
    :precondition (and
      (ready-road ?v ?r)
      (road-next ?r ?next)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
    )
    :effect (and
      (not (ready-road ?v ?r))
      (ready-road ?v ?next)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-road_0329-road_0581-tw_00060
    :parameters (?v - vehicle ?r - road_type_road_0329 ?next - road_type_road_0581 ?w - window_type_tw_00060)
    :precondition (and
      (ready-road ?v ?r)
      (road-next ?r ?next)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
    )
    :effect (and
      (not (ready-road ?v ?r))
      (ready-road ?v ?next)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-road_0329-road_0581-tw_00090
    :parameters (?v - vehicle ?r - road_type_road_0329 ?next - road_type_road_0581 ?w - window_type_tw_00090)
    :precondition (and
      (ready-road ?v ?r)
      (road-next ?r ?next)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
    )
    :effect (and
      (not (ready-road ?v ?r))
      (ready-road ?v ?next)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-road_0329-road_0581-tw_00120
    :parameters (?v - vehicle ?r - road_type_road_0329 ?next - road_type_road_0581 ?w - window_type_tw_00120)
    :precondition (and
      (ready-road ?v ?r)
      (road-next ?r ?next)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
    )
    :effect (and
      (not (ready-road ?v ?r))
      (ready-road ?v ?next)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-road_0329-road_0581-tw_00150
    :parameters (?v - vehicle ?r - road_type_road_0329 ?next - road_type_road_0581 ?w - window_type_tw_00150)
    :precondition (and
      (ready-road ?v ?r)
      (road-next ?r ?next)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
    )
    :effect (and
      (not (ready-road ?v ?r))
      (ready-road ?v ?next)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-road_0329-road_0581-tw_00180
    :parameters (?v - vehicle ?r - road_type_road_0329 ?next - road_type_road_0581 ?w - window_type_tw_00180)
    :precondition (and
      (ready-road ?v ?r)
      (road-next ?r ?next)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
    )
    :effect (and
      (not (ready-road ?v ?r))
      (ready-road ?v ?next)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-road_0329-road_0581-tw_00210
    :parameters (?v - vehicle ?r - road_type_road_0329 ?next - road_type_road_0581 ?w - window_type_tw_00210)
    :precondition (and
      (ready-road ?v ?r)
      (road-next ?r ?next)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
    )
    :effect (and
      (not (ready-road ?v ?r))
      (ready-road ?v ?next)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-road_0329-road_0581-tw_00240
    :parameters (?v - vehicle ?r - road_type_road_0329 ?next - road_type_road_0581 ?w - window_type_tw_00240)
    :precondition (and
      (ready-road ?v ?r)
      (road-next ?r ?next)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
    )
    :effect (and
      (not (ready-road ?v ?r))
      (ready-road ?v ?next)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-road_0329-road_0581-tw_00270
    :parameters (?v - vehicle ?r - road_type_road_0329 ?next - road_type_road_0581 ?w - window_type_tw_00270)
    :precondition (and
      (ready-road ?v ?r)
      (road-next ?r ?next)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
    )
    :effect (and
      (not (ready-road ?v ?r))
      (ready-road ?v ?next)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-road_0329-road_0581-tw_00300
    :parameters (?v - vehicle ?r - road_type_road_0329 ?next - road_type_road_0581 ?w - window_type_tw_00300)
    :precondition (and
      (ready-road ?v ?r)
      (road-next ?r ?next)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
    )
    :effect (and
      (not (ready-road ?v ?r))
      (ready-road ?v ?next)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-road_0329-road_0581-tw_00330
    :parameters (?v - vehicle ?r - road_type_road_0329 ?next - road_type_road_0581 ?w - window_type_tw_00330)
    :precondition (and
      (ready-road ?v ?r)
      (road-next ?r ?next)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
    )
    :effect (and
      (not (ready-road ?v ?r))
      (ready-road ?v ?next)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-road_0329-road_0581-tw_00360
    :parameters (?v - vehicle ?r - road_type_road_0329 ?next - road_type_road_0581 ?w - window_type_tw_00360)
    :precondition (and
      (ready-road ?v ?r)
      (road-next ?r ?next)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
    )
    :effect (and
      (not (ready-road ?v ?r))
      (ready-road ?v ?next)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-road_0329-road_0581-tw_00390
    :parameters (?v - vehicle ?r - road_type_road_0329 ?next - road_type_road_0581 ?w - window_type_tw_00390)
    :precondition (and
      (ready-road ?v ?r)
      (road-next ?r ?next)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
    )
    :effect (and
      (not (ready-road ?v ?r))
      (ready-road ?v ?next)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-road_0329-road_0581-tw_00420
    :parameters (?v - vehicle ?r - road_type_road_0329 ?next - road_type_road_0581 ?w - window_type_tw_00420)
    :precondition (and
      (ready-road ?v ?r)
      (road-next ?r ?next)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
    )
    :effect (and
      (not (ready-road ?v ?r))
      (ready-road ?v ?next)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-road_0329-road_0581-tw_00450
    :parameters (?v - vehicle ?r - road_type_road_0329 ?next - road_type_road_0581 ?w - window_type_tw_00450)
    :precondition (and
      (ready-road ?v ?r)
      (road-next ?r ?next)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
    )
    :effect (and
      (not (ready-road ?v ?r))
      (ready-road ?v ?next)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-road_0329-road_0581-tw_00480
    :parameters (?v - vehicle ?r - road_type_road_0329 ?next - road_type_road_0581 ?w - window_type_tw_00480)
    :precondition (and
      (ready-road ?v ?r)
      (road-next ?r ?next)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
    )
    :effect (and
      (not (ready-road ?v ?r))
      (ready-road ?v ?next)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-road_0329-road_0581-tw_00510
    :parameters (?v - vehicle ?r - road_type_road_0329 ?next - road_type_road_0581 ?w - window_type_tw_00510)
    :precondition (and
      (ready-road ?v ?r)
      (road-next ?r ?next)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
    )
    :effect (and
      (not (ready-road ?v ?r))
      (ready-road ?v ?next)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-road_0329-road_0581-tw_00540
    :parameters (?v - vehicle ?r - road_type_road_0329 ?next - road_type_road_0581 ?w - window_type_tw_00540)
    :precondition (and
      (ready-road ?v ?r)
      (road-next ?r ?next)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
    )
    :effect (and
      (not (ready-road ?v ?r))
      (ready-road ?v ?next)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-road_0329-road_0581-tw_00570
    :parameters (?v - vehicle ?r - road_type_road_0329 ?next - road_type_road_0581 ?w - window_type_tw_00570)
    :precondition (and
      (ready-road ?v ?r)
      (road-next ?r ?next)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
    )
    :effect (and
      (not (ready-road ?v ?r))
      (ready-road ?v ?next)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-road_0329-road_0581-tw_00600
    :parameters (?v - vehicle ?r - road_type_road_0329 ?next - road_type_road_0581 ?w - window_type_tw_00600)
    :precondition (and
      (ready-road ?v ?r)
      (road-next ?r ?next)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
    )
    :effect (and
      (not (ready-road ?v ?r))
      (ready-road ?v ?next)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-road_0329-road_0582-tw_00000
    :parameters (?v - vehicle ?r - road_type_road_0329 ?next - road_type_road_0582 ?w - window_type_tw_00000)
    :precondition (and
      (ready-road ?v ?r)
      (road-next ?r ?next)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
    )
    :effect (and
      (not (ready-road ?v ?r))
      (ready-road ?v ?next)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-road_0329-road_0582-tw_00030
    :parameters (?v - vehicle ?r - road_type_road_0329 ?next - road_type_road_0582 ?w - window_type_tw_00030)
    :precondition (and
      (ready-road ?v ?r)
      (road-next ?r ?next)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
    )
    :effect (and
      (not (ready-road ?v ?r))
      (ready-road ?v ?next)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-road_0329-road_0582-tw_00060
    :parameters (?v - vehicle ?r - road_type_road_0329 ?next - road_type_road_0582 ?w - window_type_tw_00060)
    :precondition (and
      (ready-road ?v ?r)
      (road-next ?r ?next)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
    )
    :effect (and
      (not (ready-road ?v ?r))
      (ready-road ?v ?next)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-road_0329-road_0582-tw_00090
    :parameters (?v - vehicle ?r - road_type_road_0329 ?next - road_type_road_0582 ?w - window_type_tw_00090)
    :precondition (and
      (ready-road ?v ?r)
      (road-next ?r ?next)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
    )
    :effect (and
      (not (ready-road ?v ?r))
      (ready-road ?v ?next)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-road_0329-road_0582-tw_00120
    :parameters (?v - vehicle ?r - road_type_road_0329 ?next - road_type_road_0582 ?w - window_type_tw_00120)
    :precondition (and
      (ready-road ?v ?r)
      (road-next ?r ?next)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
    )
    :effect (and
      (not (ready-road ?v ?r))
      (ready-road ?v ?next)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-road_0329-road_0582-tw_00150
    :parameters (?v - vehicle ?r - road_type_road_0329 ?next - road_type_road_0582 ?w - window_type_tw_00150)
    :precondition (and
      (ready-road ?v ?r)
      (road-next ?r ?next)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
    )
    :effect (and
      (not (ready-road ?v ?r))
      (ready-road ?v ?next)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-road_0329-road_0582-tw_00180
    :parameters (?v - vehicle ?r - road_type_road_0329 ?next - road_type_road_0582 ?w - window_type_tw_00180)
    :precondition (and
      (ready-road ?v ?r)
      (road-next ?r ?next)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
    )
    :effect (and
      (not (ready-road ?v ?r))
      (ready-road ?v ?next)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-road_0329-road_0582-tw_00210
    :parameters (?v - vehicle ?r - road_type_road_0329 ?next - road_type_road_0582 ?w - window_type_tw_00210)
    :precondition (and
      (ready-road ?v ?r)
      (road-next ?r ?next)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
    )
    :effect (and
      (not (ready-road ?v ?r))
      (ready-road ?v ?next)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-road_0329-road_0582-tw_00240
    :parameters (?v - vehicle ?r - road_type_road_0329 ?next - road_type_road_0582 ?w - window_type_tw_00240)
    :precondition (and
      (ready-road ?v ?r)
      (road-next ?r ?next)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
    )
    :effect (and
      (not (ready-road ?v ?r))
      (ready-road ?v ?next)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-road_0329-road_0582-tw_00270
    :parameters (?v - vehicle ?r - road_type_road_0329 ?next - road_type_road_0582 ?w - window_type_tw_00270)
    :precondition (and
      (ready-road ?v ?r)
      (road-next ?r ?next)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
    )
    :effect (and
      (not (ready-road ?v ?r))
      (ready-road ?v ?next)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-road_0329-road_0582-tw_00300
    :parameters (?v - vehicle ?r - road_type_road_0329 ?next - road_type_road_0582 ?w - window_type_tw_00300)
    :precondition (and
      (ready-road ?v ?r)
      (road-next ?r ?next)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
    )
    :effect (and
      (not (ready-road ?v ?r))
      (ready-road ?v ?next)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-road_0329-road_0582-tw_00330
    :parameters (?v - vehicle ?r - road_type_road_0329 ?next - road_type_road_0582 ?w - window_type_tw_00330)
    :precondition (and
      (ready-road ?v ?r)
      (road-next ?r ?next)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
    )
    :effect (and
      (not (ready-road ?v ?r))
      (ready-road ?v ?next)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-road_0329-road_0582-tw_00360
    :parameters (?v - vehicle ?r - road_type_road_0329 ?next - road_type_road_0582 ?w - window_type_tw_00360)
    :precondition (and
      (ready-road ?v ?r)
      (road-next ?r ?next)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
    )
    :effect (and
      (not (ready-road ?v ?r))
      (ready-road ?v ?next)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-road_0329-road_0582-tw_00390
    :parameters (?v - vehicle ?r - road_type_road_0329 ?next - road_type_road_0582 ?w - window_type_tw_00390)
    :precondition (and
      (ready-road ?v ?r)
      (road-next ?r ?next)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
    )
    :effect (and
      (not (ready-road ?v ?r))
      (ready-road ?v ?next)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-road_0329-road_0582-tw_00420
    :parameters (?v - vehicle ?r - road_type_road_0329 ?next - road_type_road_0582 ?w - window_type_tw_00420)
    :precondition (and
      (ready-road ?v ?r)
      (road-next ?r ?next)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
    )
    :effect (and
      (not (ready-road ?v ?r))
      (ready-road ?v ?next)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-road_0329-road_0582-tw_00450
    :parameters (?v - vehicle ?r - road_type_road_0329 ?next - road_type_road_0582 ?w - window_type_tw_00450)
    :precondition (and
      (ready-road ?v ?r)
      (road-next ?r ?next)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
    )
    :effect (and
      (not (ready-road ?v ?r))
      (ready-road ?v ?next)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-road_0329-road_0582-tw_00480
    :parameters (?v - vehicle ?r - road_type_road_0329 ?next - road_type_road_0582 ?w - window_type_tw_00480)
    :precondition (and
      (ready-road ?v ?r)
      (road-next ?r ?next)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
    )
    :effect (and
      (not (ready-road ?v ?r))
      (ready-road ?v ?next)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-road_0329-road_0582-tw_00510
    :parameters (?v - vehicle ?r - road_type_road_0329 ?next - road_type_road_0582 ?w - window_type_tw_00510)
    :precondition (and
      (ready-road ?v ?r)
      (road-next ?r ?next)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
    )
    :effect (and
      (not (ready-road ?v ?r))
      (ready-road ?v ?next)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-road_0329-road_0582-tw_00540
    :parameters (?v - vehicle ?r - road_type_road_0329 ?next - road_type_road_0582 ?w - window_type_tw_00540)
    :precondition (and
      (ready-road ?v ?r)
      (road-next ?r ?next)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
    )
    :effect (and
      (not (ready-road ?v ?r))
      (ready-road ?v ?next)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-road_0329-road_0582-tw_00570
    :parameters (?v - vehicle ?r - road_type_road_0329 ?next - road_type_road_0582 ?w - window_type_tw_00570)
    :precondition (and
      (ready-road ?v ?r)
      (road-next ?r ?next)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
    )
    :effect (and
      (not (ready-road ?v ?r))
      (ready-road ?v ?next)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-road_0329-road_0582-tw_00600
    :parameters (?v - vehicle ?r - road_type_road_0329 ?next - road_type_road_0582 ?w - window_type_tw_00600)
    :precondition (and
      (ready-road ?v ?r)
      (road-next ?r ?next)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
    )
    :effect (and
      (not (ready-road ?v ?r))
      (ready-road ?v ?next)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-static-road_0330-road_0694
    :parameters (?v - vehicle ?r - road_type_road_0330 ?next - road_type_road_0694)
    :precondition (and
      (ready-road ?v ?r)
      (road-next ?r ?next)
      (road-open ?r)
      (static-road ?r)
      (not (road-blocked ?r))
    )
    :effect (and
      (not (ready-road ?v ?r))
      (ready-road ?v ?next)
      (increase (travel-time ?v) (travel-duration ?r))
      (increase (sim-time) (travel-duration ?r))
    )
  )

  (:action traverse-road-static-road_0332-road_0333
    :parameters (?v - vehicle ?r - road_type_road_0332 ?next - road_type_road_0333)
    :precondition (and
      (ready-road ?v ?r)
      (road-next ?r ?next)
      (road-open ?r)
      (static-road ?r)
      (not (road-blocked ?r))
    )
    :effect (and
      (not (ready-road ?v ?r))
      (ready-road ?v ?next)
      (increase (travel-time ?v) (travel-duration ?r))
      (increase (sim-time) (travel-duration ?r))
    )
  )

  (:action traverse-road-static-road_0332-road_0334
    :parameters (?v - vehicle ?r - road_type_road_0332 ?next - road_type_road_0334)
    :precondition (and
      (ready-road ?v ?r)
      (road-next ?r ?next)
      (road-open ?r)
      (static-road ?r)
      (not (road-blocked ?r))
    )
    :effect (and
      (not (ready-road ?v ?r))
      (ready-road ?v ?next)
      (increase (travel-time ?v) (travel-duration ?r))
      (increase (sim-time) (travel-duration ?r))
    )
  )

  (:action traverse-road-static-road_0332-road_0335
    :parameters (?v - vehicle ?r - road_type_road_0332 ?next - road_type_road_0335)
    :precondition (and
      (ready-road ?v ?r)
      (road-next ?r ?next)
      (road-open ?r)
      (static-road ?r)
      (not (road-blocked ?r))
    )
    :effect (and
      (not (ready-road ?v ?r))
      (ready-road ?v ?next)
      (increase (travel-time ?v) (travel-duration ?r))
      (increase (sim-time) (travel-duration ?r))
    )
  )

  (:action traverse-road-static-road_0333-road_0530
    :parameters (?v - vehicle ?r - road_type_road_0333 ?next - road_type_road_0530)
    :precondition (and
      (ready-road ?v ?r)
      (road-next ?r ?next)
      (road-open ?r)
      (static-road ?r)
      (not (road-blocked ?r))
    )
    :effect (and
      (not (ready-road ?v ?r))
      (ready-road ?v ?next)
      (increase (travel-time ?v) (travel-duration ?r))
      (increase (sim-time) (travel-duration ?r))
    )
  )

  (:action traverse-road-static-road_0334-road_0736
    :parameters (?v - vehicle ?r - road_type_road_0334 ?next - road_type_road_0736)
    :precondition (and
      (ready-road ?v ?r)
      (road-next ?r ?next)
      (road-open ?r)
      (static-road ?r)
      (not (road-blocked ?r))
    )
    :effect (and
      (not (ready-road ?v ?r))
      (ready-road ?v ?next)
      (increase (travel-time ?v) (travel-duration ?r))
      (increase (sim-time) (travel-duration ?r))
    )
  )

  (:action traverse-road-static-road_0335-road_0627
    :parameters (?v - vehicle ?r - road_type_road_0335 ?next - road_type_road_0627)
    :precondition (and
      (ready-road ?v ?r)
      (road-next ?r ?next)
      (road-open ?r)
      (static-road ?r)
      (not (road-blocked ?r))
    )
    :effect (and
      (not (ready-road ?v ?r))
      (ready-road ?v ?next)
      (increase (travel-time ?v) (travel-duration ?r))
      (increase (sim-time) (travel-duration ?r))
    )
  )

  (:action traverse-road-static-road_0336-road_0600
    :parameters (?v - vehicle ?r - road_type_road_0336 ?next - road_type_road_0600)
    :precondition (and
      (ready-road ?v ?r)
      (road-next ?r ?next)
      (road-open ?r)
      (static-road ?r)
      (not (road-blocked ?r))
    )
    :effect (and
      (not (ready-road ?v ?r))
      (ready-road ?v ?next)
      (increase (travel-time ?v) (travel-duration ?r))
      (increase (sim-time) (travel-duration ?r))
    )
  )

  (:action traverse-road-static-road_0336-road_0601
    :parameters (?v - vehicle ?r - road_type_road_0336 ?next - road_type_road_0601)
    :precondition (and
      (ready-road ?v ?r)
      (road-next ?r ?next)
      (road-open ?r)
      (static-road ?r)
      (not (road-blocked ?r))
    )
    :effect (and
      (not (ready-road ?v ?r))
      (ready-road ?v ?next)
      (increase (travel-time ?v) (travel-duration ?r))
      (increase (sim-time) (travel-duration ?r))
    )
  )

  (:action traverse-road-static-road_0337-road_0127
    :parameters (?v - vehicle ?r - road_type_road_0337 ?next - road_type_road_0127)
    :precondition (and
      (ready-road ?v ?r)
      (road-next ?r ?next)
      (road-open ?r)
      (static-road ?r)
      (not (road-blocked ?r))
    )
    :effect (and
      (not (ready-road ?v ?r))
      (ready-road ?v ?next)
      (increase (travel-time ?v) (travel-duration ?r))
      (increase (sim-time) (travel-duration ?r))
    )
  )

  (:action traverse-road-static-road_0337-road_0128
    :parameters (?v - vehicle ?r - road_type_road_0337 ?next - road_type_road_0128)
    :precondition (and
      (ready-road ?v ?r)
      (road-next ?r ?next)
      (road-open ?r)
      (static-road ?r)
      (not (road-blocked ?r))
    )
    :effect (and
      (not (ready-road ?v ?r))
      (ready-road ?v ?next)
      (increase (travel-time ?v) (travel-duration ?r))
      (increase (sim-time) (travel-duration ?r))
    )
  )

  (:action traverse-road-static-road_0338-macro_0043
    :parameters (?v - vehicle ?r - road_type_road_0338 ?next - road_type_macro_0043)
    :precondition (and
      (ready-road ?v ?r)
      (road-next ?r ?next)
      (road-open ?r)
      (static-road ?r)
      (not (road-blocked ?r))
    )
    :effect (and
      (not (ready-road ?v ?r))
      (ready-road ?v ?next)
      (increase (travel-time ?v) (travel-duration ?r))
      (increase (sim-time) (travel-duration ?r))
    )
  )

  (:action traverse-road-static-road_0339-road_0337
    :parameters (?v - vehicle ?r - road_type_road_0339 ?next - road_type_road_0337)
    :precondition (and
      (ready-road ?v ?r)
      (road-next ?r ?next)
      (road-open ?r)
      (static-road ?r)
      (not (road-blocked ?r))
    )
    :effect (and
      (not (ready-road ?v ?r))
      (ready-road ?v ?next)
      (increase (travel-time ?v) (travel-duration ?r))
      (increase (sim-time) (travel-duration ?r))
    )
  )

  (:action traverse-road-static-road_0340-road_0516
    :parameters (?v - vehicle ?r - road_type_road_0340 ?next - road_type_road_0516)
    :precondition (and
      (ready-road ?v ?r)
      (road-next ?r ?next)
      (road-open ?r)
      (static-road ?r)
      (not (road-blocked ?r))
    )
    :effect (and
      (not (ready-road ?v ?r))
      (ready-road ?v ?next)
      (increase (travel-time ?v) (travel-duration ?r))
      (increase (sim-time) (travel-duration ?r))
    )
  )

  (:action traverse-road-static-road_0340-road_0517
    :parameters (?v - vehicle ?r - road_type_road_0340 ?next - road_type_road_0517)
    :precondition (and
      (ready-road ?v ?r)
      (road-next ?r ?next)
      (road-open ?r)
      (static-road ?r)
      (not (road-blocked ?r))
    )
    :effect (and
      (not (ready-road ?v ?r))
      (ready-road ?v ?next)
      (increase (travel-time ?v) (travel-duration ?r))
      (increase (sim-time) (travel-duration ?r))
    )
  )

  (:action traverse-road-static-road_0341-macro_0013
    :parameters (?v - vehicle ?r - road_type_road_0341 ?next - road_type_macro_0013)
    :precondition (and
      (ready-road ?v ?r)
      (road-next ?r ?next)
      (road-open ?r)
      (static-road ?r)
      (not (road-blocked ?r))
    )
    :effect (and
      (not (ready-road ?v ?r))
      (ready-road ?v ?next)
      (increase (travel-time ?v) (travel-duration ?r))
      (increase (sim-time) (travel-duration ?r))
    )
  )

  (:action traverse-road-static-road_0341-road_0169
    :parameters (?v - vehicle ?r - road_type_road_0341 ?next - road_type_road_0169)
    :precondition (and
      (ready-road ?v ?r)
      (road-next ?r ?next)
      (road-open ?r)
      (static-road ?r)
      (not (road-blocked ?r))
    )
    :effect (and
      (not (ready-road ?v ?r))
      (ready-road ?v ?next)
      (increase (travel-time ?v) (travel-duration ?r))
      (increase (sim-time) (travel-duration ?r))
    )
  )

  (:action traverse-road-static-road_0342-road_0264
    :parameters (?v - vehicle ?r - road_type_road_0342 ?next - road_type_road_0264)
    :precondition (and
      (ready-road ?v ?r)
      (road-next ?r ?next)
      (road-open ?r)
      (static-road ?r)
      (not (road-blocked ?r))
    )
    :effect (and
      (not (ready-road ?v ?r))
      (ready-road ?v ?next)
      (increase (travel-time ?v) (travel-duration ?r))
      (increase (sim-time) (travel-duration ?r))
    )
  )

  (:action traverse-road-static-road_0343-road_0410
    :parameters (?v - vehicle ?r - road_type_road_0343 ?next - road_type_road_0410)
    :precondition (and
      (ready-road ?v ?r)
      (road-next ?r ?next)
      (road-open ?r)
      (static-road ?r)
      (not (road-blocked ?r))
    )
    :effect (and
      (not (ready-road ?v ?r))
      (ready-road ?v ?next)
      (increase (travel-time ?v) (travel-duration ?r))
      (increase (sim-time) (travel-duration ?r))
    )
  )

  (:action traverse-road-static-road_0343-road_0411
    :parameters (?v - vehicle ?r - road_type_road_0343 ?next - road_type_road_0411)
    :precondition (and
      (ready-road ?v ?r)
      (road-next ?r ?next)
      (road-open ?r)
      (static-road ?r)
      (not (road-blocked ?r))
    )
    :effect (and
      (not (ready-road ?v ?r))
      (ready-road ?v ?next)
      (increase (travel-time ?v) (travel-duration ?r))
      (increase (sim-time) (travel-duration ?r))
    )
  )

  (:action traverse-road-static-road_0344-road_0265
    :parameters (?v - vehicle ?r - road_type_road_0344 ?next - road_type_road_0265)
    :precondition (and
      (ready-road ?v ?r)
      (road-next ?r ?next)
      (road-open ?r)
      (static-road ?r)
      (not (road-blocked ?r))
    )
    :effect (and
      (not (ready-road ?v ?r))
      (ready-road ?v ?next)
      (increase (travel-time ?v) (travel-duration ?r))
      (increase (sim-time) (travel-duration ?r))
    )
  )

  (:action traverse-road-static-road_0345-road_0691
    :parameters (?v - vehicle ?r - road_type_road_0345 ?next - road_type_road_0691)
    :precondition (and
      (ready-road ?v ?r)
      (road-next ?r ?next)
      (road-open ?r)
      (static-road ?r)
      (not (road-blocked ?r))
    )
    :effect (and
      (not (ready-road ?v ?r))
      (ready-road ?v ?next)
      (increase (travel-time ?v) (travel-duration ?r))
      (increase (sim-time) (travel-duration ?r))
    )
  )

  (:action traverse-road-static-road_0345-road_0692
    :parameters (?v - vehicle ?r - road_type_road_0345 ?next - road_type_road_0692)
    :precondition (and
      (ready-road ?v ?r)
      (road-next ?r ?next)
      (road-open ?r)
      (static-road ?r)
      (not (road-blocked ?r))
    )
    :effect (and
      (not (ready-road ?v ?r))
      (ready-road ?v ?next)
      (increase (travel-time ?v) (travel-duration ?r))
      (increase (sim-time) (travel-duration ?r))
    )
  )

  (:action traverse-road-static-road_0345-road_0693
    :parameters (?v - vehicle ?r - road_type_road_0345 ?next - road_type_road_0693)
    :precondition (and
      (ready-road ?v ?r)
      (road-next ?r ?next)
      (road-open ?r)
      (static-road ?r)
      (not (road-blocked ?r))
    )
    :effect (and
      (not (ready-road ?v ?r))
      (ready-road ?v ?next)
      (increase (travel-time ?v) (travel-duration ?r))
      (increase (sim-time) (travel-duration ?r))
    )
  )

  (:action traverse-road-dynamic-road_0346-road_0002-tw_00000
    :parameters (?v - vehicle ?r - road_type_road_0346 ?next - road_type_road_0002 ?w - window_type_tw_00000)
    :precondition (and
      (ready-road ?v ?r)
      (road-next ?r ?next)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
    )
    :effect (and
      (not (ready-road ?v ?r))
      (ready-road ?v ?next)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-road_0346-road_0002-tw_00030
    :parameters (?v - vehicle ?r - road_type_road_0346 ?next - road_type_road_0002 ?w - window_type_tw_00030)
    :precondition (and
      (ready-road ?v ?r)
      (road-next ?r ?next)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
    )
    :effect (and
      (not (ready-road ?v ?r))
      (ready-road ?v ?next)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-road_0346-road_0002-tw_00060
    :parameters (?v - vehicle ?r - road_type_road_0346 ?next - road_type_road_0002 ?w - window_type_tw_00060)
    :precondition (and
      (ready-road ?v ?r)
      (road-next ?r ?next)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
    )
    :effect (and
      (not (ready-road ?v ?r))
      (ready-road ?v ?next)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-road_0346-road_0002-tw_00090
    :parameters (?v - vehicle ?r - road_type_road_0346 ?next - road_type_road_0002 ?w - window_type_tw_00090)
    :precondition (and
      (ready-road ?v ?r)
      (road-next ?r ?next)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
    )
    :effect (and
      (not (ready-road ?v ?r))
      (ready-road ?v ?next)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-road_0346-road_0002-tw_00120
    :parameters (?v - vehicle ?r - road_type_road_0346 ?next - road_type_road_0002 ?w - window_type_tw_00120)
    :precondition (and
      (ready-road ?v ?r)
      (road-next ?r ?next)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
    )
    :effect (and
      (not (ready-road ?v ?r))
      (ready-road ?v ?next)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-road_0346-road_0002-tw_00150
    :parameters (?v - vehicle ?r - road_type_road_0346 ?next - road_type_road_0002 ?w - window_type_tw_00150)
    :precondition (and
      (ready-road ?v ?r)
      (road-next ?r ?next)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
    )
    :effect (and
      (not (ready-road ?v ?r))
      (ready-road ?v ?next)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-road_0346-road_0002-tw_00180
    :parameters (?v - vehicle ?r - road_type_road_0346 ?next - road_type_road_0002 ?w - window_type_tw_00180)
    :precondition (and
      (ready-road ?v ?r)
      (road-next ?r ?next)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
    )
    :effect (and
      (not (ready-road ?v ?r))
      (ready-road ?v ?next)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-road_0346-road_0002-tw_00210
    :parameters (?v - vehicle ?r - road_type_road_0346 ?next - road_type_road_0002 ?w - window_type_tw_00210)
    :precondition (and
      (ready-road ?v ?r)
      (road-next ?r ?next)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
    )
    :effect (and
      (not (ready-road ?v ?r))
      (ready-road ?v ?next)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-road_0346-road_0002-tw_00240
    :parameters (?v - vehicle ?r - road_type_road_0346 ?next - road_type_road_0002 ?w - window_type_tw_00240)
    :precondition (and
      (ready-road ?v ?r)
      (road-next ?r ?next)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
    )
    :effect (and
      (not (ready-road ?v ?r))
      (ready-road ?v ?next)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-road_0346-road_0002-tw_00270
    :parameters (?v - vehicle ?r - road_type_road_0346 ?next - road_type_road_0002 ?w - window_type_tw_00270)
    :precondition (and
      (ready-road ?v ?r)
      (road-next ?r ?next)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
    )
    :effect (and
      (not (ready-road ?v ?r))
      (ready-road ?v ?next)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-road_0346-road_0002-tw_00300
    :parameters (?v - vehicle ?r - road_type_road_0346 ?next - road_type_road_0002 ?w - window_type_tw_00300)
    :precondition (and
      (ready-road ?v ?r)
      (road-next ?r ?next)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
    )
    :effect (and
      (not (ready-road ?v ?r))
      (ready-road ?v ?next)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-road_0346-road_0002-tw_00330
    :parameters (?v - vehicle ?r - road_type_road_0346 ?next - road_type_road_0002 ?w - window_type_tw_00330)
    :precondition (and
      (ready-road ?v ?r)
      (road-next ?r ?next)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
    )
    :effect (and
      (not (ready-road ?v ?r))
      (ready-road ?v ?next)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-road_0346-road_0002-tw_00360
    :parameters (?v - vehicle ?r - road_type_road_0346 ?next - road_type_road_0002 ?w - window_type_tw_00360)
    :precondition (and
      (ready-road ?v ?r)
      (road-next ?r ?next)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
    )
    :effect (and
      (not (ready-road ?v ?r))
      (ready-road ?v ?next)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-road_0346-road_0002-tw_00390
    :parameters (?v - vehicle ?r - road_type_road_0346 ?next - road_type_road_0002 ?w - window_type_tw_00390)
    :precondition (and
      (ready-road ?v ?r)
      (road-next ?r ?next)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
    )
    :effect (and
      (not (ready-road ?v ?r))
      (ready-road ?v ?next)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-road_0346-road_0002-tw_00420
    :parameters (?v - vehicle ?r - road_type_road_0346 ?next - road_type_road_0002 ?w - window_type_tw_00420)
    :precondition (and
      (ready-road ?v ?r)
      (road-next ?r ?next)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
    )
    :effect (and
      (not (ready-road ?v ?r))
      (ready-road ?v ?next)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-road_0346-road_0002-tw_00450
    :parameters (?v - vehicle ?r - road_type_road_0346 ?next - road_type_road_0002 ?w - window_type_tw_00450)
    :precondition (and
      (ready-road ?v ?r)
      (road-next ?r ?next)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
    )
    :effect (and
      (not (ready-road ?v ?r))
      (ready-road ?v ?next)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-road_0346-road_0002-tw_00480
    :parameters (?v - vehicle ?r - road_type_road_0346 ?next - road_type_road_0002 ?w - window_type_tw_00480)
    :precondition (and
      (ready-road ?v ?r)
      (road-next ?r ?next)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
    )
    :effect (and
      (not (ready-road ?v ?r))
      (ready-road ?v ?next)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-road_0346-road_0002-tw_00510
    :parameters (?v - vehicle ?r - road_type_road_0346 ?next - road_type_road_0002 ?w - window_type_tw_00510)
    :precondition (and
      (ready-road ?v ?r)
      (road-next ?r ?next)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
    )
    :effect (and
      (not (ready-road ?v ?r))
      (ready-road ?v ?next)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-road_0346-road_0002-tw_00540
    :parameters (?v - vehicle ?r - road_type_road_0346 ?next - road_type_road_0002 ?w - window_type_tw_00540)
    :precondition (and
      (ready-road ?v ?r)
      (road-next ?r ?next)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
    )
    :effect (and
      (not (ready-road ?v ?r))
      (ready-road ?v ?next)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-road_0346-road_0002-tw_00570
    :parameters (?v - vehicle ?r - road_type_road_0346 ?next - road_type_road_0002 ?w - window_type_tw_00570)
    :precondition (and
      (ready-road ?v ?r)
      (road-next ?r ?next)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
    )
    :effect (and
      (not (ready-road ?v ?r))
      (ready-road ?v ?next)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-road_0346-road_0002-tw_00600
    :parameters (?v - vehicle ?r - road_type_road_0346 ?next - road_type_road_0002 ?w - window_type_tw_00600)
    :precondition (and
      (ready-road ?v ?r)
      (road-next ?r ?next)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
    )
    :effect (and
      (not (ready-road ?v ?r))
      (ready-road ?v ?next)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-static-road_0347-road_0747
    :parameters (?v - vehicle ?r - road_type_road_0347 ?next - road_type_road_0747)
    :precondition (and
      (ready-road ?v ?r)
      (road-next ?r ?next)
      (road-open ?r)
      (static-road ?r)
      (not (road-blocked ?r))
    )
    :effect (and
      (not (ready-road ?v ?r))
      (ready-road ?v ?next)
      (increase (travel-time ?v) (travel-duration ?r))
      (increase (sim-time) (travel-duration ?r))
    )
  )

  (:action traverse-road-static-road_0348-road_0783
    :parameters (?v - vehicle ?r - road_type_road_0348 ?next - road_type_road_0783)
    :precondition (and
      (ready-road ?v ?r)
      (road-next ?r ?next)
      (road-open ?r)
      (static-road ?r)
      (not (road-blocked ?r))
    )
    :effect (and
      (not (ready-road ?v ?r))
      (ready-road ?v ?next)
      (increase (travel-time ?v) (travel-duration ?r))
      (increase (sim-time) (travel-duration ?r))
    )
  )

  (:action traverse-road-static-road_0348-road_0784
    :parameters (?v - vehicle ?r - road_type_road_0348 ?next - road_type_road_0784)
    :precondition (and
      (ready-road ?v ?r)
      (road-next ?r ?next)
      (road-open ?r)
      (static-road ?r)
      (not (road-blocked ?r))
    )
    :effect (and
      (not (ready-road ?v ?r))
      (ready-road ?v ?next)
      (increase (travel-time ?v) (travel-duration ?r))
      (increase (sim-time) (travel-duration ?r))
    )
  )

  (:action traverse-road-static-road_0349-road_0634
    :parameters (?v - vehicle ?r - road_type_road_0349 ?next - road_type_road_0634)
    :precondition (and
      (ready-road ?v ?r)
      (road-next ?r ?next)
      (road-open ?r)
      (static-road ?r)
      (not (road-blocked ?r))
    )
    :effect (and
      (not (ready-road ?v ?r))
      (ready-road ?v ?next)
      (increase (travel-time ?v) (travel-duration ?r))
      (increase (sim-time) (travel-duration ?r))
    )
  )

  (:action traverse-road-static-road_0349-road_0635
    :parameters (?v - vehicle ?r - road_type_road_0349 ?next - road_type_road_0635)
    :precondition (and
      (ready-road ?v ?r)
      (road-next ?r ?next)
      (road-open ?r)
      (static-road ?r)
      (not (road-blocked ?r))
    )
    :effect (and
      (not (ready-road ?v ?r))
      (ready-road ?v ?next)
      (increase (travel-time ?v) (travel-duration ?r))
      (increase (sim-time) (travel-duration ?r))
    )
  )

  (:action traverse-road-static-road_0350-road_0493
    :parameters (?v - vehicle ?r - road_type_road_0350 ?next - road_type_road_0493)
    :precondition (and
      (ready-road ?v ?r)
      (road-next ?r ?next)
      (road-open ?r)
      (static-road ?r)
      (not (road-blocked ?r))
    )
    :effect (and
      (not (ready-road ?v ?r))
      (ready-road ?v ?next)
      (increase (travel-time ?v) (travel-duration ?r))
      (increase (sim-time) (travel-duration ?r))
    )
  )

  (:action traverse-road-static-road_0350-road_0494
    :parameters (?v - vehicle ?r - road_type_road_0350 ?next - road_type_road_0494)
    :precondition (and
      (ready-road ?v ?r)
      (road-next ?r ?next)
      (road-open ?r)
      (static-road ?r)
      (not (road-blocked ?r))
    )
    :effect (and
      (not (ready-road ?v ?r))
      (ready-road ?v ?next)
      (increase (travel-time ?v) (travel-duration ?r))
      (increase (sim-time) (travel-duration ?r))
    )
  )

  (:action traverse-road-static-road_0351-road_0535
    :parameters (?v - vehicle ?r - road_type_road_0351 ?next - road_type_road_0535)
    :precondition (and
      (ready-road ?v ?r)
      (road-next ?r ?next)
      (road-open ?r)
      (static-road ?r)
      (not (road-blocked ?r))
    )
    :effect (and
      (not (ready-road ?v ?r))
      (ready-road ?v ?next)
      (increase (travel-time ?v) (travel-duration ?r))
      (increase (sim-time) (travel-duration ?r))
    )
  )

  (:action traverse-road-static-road_0351-road_0536
    :parameters (?v - vehicle ?r - road_type_road_0351 ?next - road_type_road_0536)
    :precondition (and
      (ready-road ?v ?r)
      (road-next ?r ?next)
      (road-open ?r)
      (static-road ?r)
      (not (road-blocked ?r))
    )
    :effect (and
      (not (ready-road ?v ?r))
      (ready-road ?v ?next)
      (increase (travel-time ?v) (travel-duration ?r))
      (increase (sim-time) (travel-duration ?r))
    )
  )

  (:action traverse-road-static-road_0352-macro_0014
    :parameters (?v - vehicle ?r - road_type_road_0352 ?next - road_type_macro_0014)
    :precondition (and
      (ready-road ?v ?r)
      (road-next ?r ?next)
      (road-open ?r)
      (static-road ?r)
      (not (road-blocked ?r))
    )
    :effect (and
      (not (ready-road ?v ?r))
      (ready-road ?v ?next)
      (increase (travel-time ?v) (travel-duration ?r))
      (increase (sim-time) (travel-duration ?r))
    )
  )

  (:action traverse-road-static-road_0352-macro_0015
    :parameters (?v - vehicle ?r - road_type_road_0352 ?next - road_type_macro_0015)
    :precondition (and
      (ready-road ?v ?r)
      (road-next ?r ?next)
      (road-open ?r)
      (static-road ?r)
      (not (road-blocked ?r))
    )
    :effect (and
      (not (ready-road ?v ?r))
      (ready-road ?v ?next)
      (increase (travel-time ?v) (travel-duration ?r))
      (increase (sim-time) (travel-duration ?r))
    )
  )

  (:action traverse-road-static-road_0354-macro_0030
    :parameters (?v - vehicle ?r - road_type_road_0354 ?next - road_type_macro_0030)
    :precondition (and
      (ready-road ?v ?r)
      (road-next ?r ?next)
      (road-open ?r)
      (static-road ?r)
      (not (road-blocked ?r))
    )
    :effect (and
      (not (ready-road ?v ?r))
      (ready-road ?v ?next)
      (increase (travel-time ?v) (travel-duration ?r))
      (increase (sim-time) (travel-duration ?r))
    )
  )

  (:action traverse-road-static-road_0354-road_0401
    :parameters (?v - vehicle ?r - road_type_road_0354 ?next - road_type_road_0401)
    :precondition (and
      (ready-road ?v ?r)
      (road-next ?r ?next)
      (road-open ?r)
      (static-road ?r)
      (not (road-blocked ?r))
    )
    :effect (and
      (not (ready-road ?v ?r))
      (ready-road ?v ?next)
      (increase (travel-time ?v) (travel-duration ?r))
      (increase (sim-time) (travel-duration ?r))
    )
  )

  (:action traverse-road-static-road_0354-road_0403
    :parameters (?v - vehicle ?r - road_type_road_0354 ?next - road_type_road_0403)
    :precondition (and
      (ready-road ?v ?r)
      (road-next ?r ?next)
      (road-open ?r)
      (static-road ?r)
      (not (road-blocked ?r))
    )
    :effect (and
      (not (ready-road ?v ?r))
      (ready-road ?v ?next)
      (increase (travel-time ?v) (travel-duration ?r))
      (increase (sim-time) (travel-duration ?r))
    )
  )

  (:action traverse-road-static-road_0355-road_0807
    :parameters (?v - vehicle ?r - road_type_road_0355 ?next - road_type_road_0807)
    :precondition (and
      (ready-road ?v ?r)
      (road-next ?r ?next)
      (road-open ?r)
      (static-road ?r)
      (not (road-blocked ?r))
    )
    :effect (and
      (not (ready-road ?v ?r))
      (ready-road ?v ?next)
      (increase (travel-time ?v) (travel-duration ?r))
      (increase (sim-time) (travel-duration ?r))
    )
  )

  (:action traverse-road-static-road_0355-road_0808
    :parameters (?v - vehicle ?r - road_type_road_0355 ?next - road_type_road_0808)
    :precondition (and
      (ready-road ?v ?r)
      (road-next ?r ?next)
      (road-open ?r)
      (static-road ?r)
      (not (road-blocked ?r))
    )
    :effect (and
      (not (ready-road ?v ?r))
      (ready-road ?v ?next)
      (increase (travel-time ?v) (travel-duration ?r))
      (increase (sim-time) (travel-duration ?r))
    )
  )

  (:action traverse-road-static-road_0355-road_0809
    :parameters (?v - vehicle ?r - road_type_road_0355 ?next - road_type_road_0809)
    :precondition (and
      (ready-road ?v ?r)
      (road-next ?r ?next)
      (road-open ?r)
      (static-road ?r)
      (not (road-blocked ?r))
    )
    :effect (and
      (not (ready-road ?v ?r))
      (ready-road ?v ?next)
      (increase (travel-time ?v) (travel-duration ?r))
      (increase (sim-time) (travel-duration ?r))
    )
  )

  (:action traverse-road-static-road_0356-road_0089
    :parameters (?v - vehicle ?r - road_type_road_0356 ?next - road_type_road_0089)
    :precondition (and
      (ready-road ?v ?r)
      (road-next ?r ?next)
      (road-open ?r)
      (static-road ?r)
      (not (road-blocked ?r))
    )
    :effect (and
      (not (ready-road ?v ?r))
      (ready-road ?v ?next)
      (increase (travel-time ?v) (travel-duration ?r))
      (increase (sim-time) (travel-duration ?r))
    )
  )

  (:action traverse-road-static-road_0356-road_0090
    :parameters (?v - vehicle ?r - road_type_road_0356 ?next - road_type_road_0090)
    :precondition (and
      (ready-road ?v ?r)
      (road-next ?r ?next)
      (road-open ?r)
      (static-road ?r)
      (not (road-blocked ?r))
    )
    :effect (and
      (not (ready-road ?v ?r))
      (ready-road ?v ?next)
      (increase (travel-time ?v) (travel-duration ?r))
      (increase (sim-time) (travel-duration ?r))
    )
  )

  (:action traverse-road-static-road_0356-road_0091
    :parameters (?v - vehicle ?r - road_type_road_0356 ?next - road_type_road_0091)
    :precondition (and
      (ready-road ?v ?r)
      (road-next ?r ?next)
      (road-open ?r)
      (static-road ?r)
      (not (road-blocked ?r))
    )
    :effect (and
      (not (ready-road ?v ?r))
      (ready-road ?v ?next)
      (increase (travel-time ?v) (travel-duration ?r))
      (increase (sim-time) (travel-duration ?r))
    )
  )

  (:action traverse-road-static-road_0357-road_0664
    :parameters (?v - vehicle ?r - road_type_road_0357 ?next - road_type_road_0664)
    :precondition (and
      (ready-road ?v ?r)
      (road-next ?r ?next)
      (road-open ?r)
      (static-road ?r)
      (not (road-blocked ?r))
    )
    :effect (and
      (not (ready-road ?v ?r))
      (ready-road ?v ?next)
      (increase (travel-time ?v) (travel-duration ?r))
      (increase (sim-time) (travel-duration ?r))
    )
  )

  (:action traverse-road-static-road_0358-road_0054
    :parameters (?v - vehicle ?r - road_type_road_0358 ?next - road_type_road_0054)
    :precondition (and
      (ready-road ?v ?r)
      (road-next ?r ?next)
      (road-open ?r)
      (static-road ?r)
      (not (road-blocked ?r))
    )
    :effect (and
      (not (ready-road ?v ?r))
      (ready-road ?v ?next)
      (increase (travel-time ?v) (travel-duration ?r))
      (increase (sim-time) (travel-duration ?r))
    )
  )

  (:action traverse-road-static-road_0359-road_0706
    :parameters (?v - vehicle ?r - road_type_road_0359 ?next - road_type_road_0706)
    :precondition (and
      (ready-road ?v ?r)
      (road-next ?r ?next)
      (road-open ?r)
      (static-road ?r)
      (not (road-blocked ?r))
    )
    :effect (and
      (not (ready-road ?v ?r))
      (ready-road ?v ?next)
      (increase (travel-time ?v) (travel-duration ?r))
      (increase (sim-time) (travel-duration ?r))
    )
  )

  (:action traverse-road-static-road_0360-road_0768
    :parameters (?v - vehicle ?r - road_type_road_0360 ?next - road_type_road_0768)
    :precondition (and
      (ready-road ?v ?r)
      (road-next ?r ?next)
      (road-open ?r)
      (static-road ?r)
      (not (road-blocked ?r))
    )
    :effect (and
      (not (ready-road ?v ?r))
      (ready-road ?v ?next)
      (increase (travel-time ?v) (travel-duration ?r))
      (increase (sim-time) (travel-duration ?r))
    )
  )

  (:action traverse-road-static-road_0361-road_0577
    :parameters (?v - vehicle ?r - road_type_road_0361 ?next - road_type_road_0577)
    :precondition (and
      (ready-road ?v ?r)
      (road-next ?r ?next)
      (road-open ?r)
      (static-road ?r)
      (not (road-blocked ?r))
    )
    :effect (and
      (not (ready-road ?v ?r))
      (ready-road ?v ?next)
      (increase (travel-time ?v) (travel-duration ?r))
      (increase (sim-time) (travel-duration ?r))
    )
  )

  (:action traverse-road-static-road_0361-road_0578
    :parameters (?v - vehicle ?r - road_type_road_0361 ?next - road_type_road_0578)
    :precondition (and
      (ready-road ?v ?r)
      (road-next ?r ?next)
      (road-open ?r)
      (static-road ?r)
      (not (road-blocked ?r))
    )
    :effect (and
      (not (ready-road ?v ?r))
      (ready-road ?v ?next)
      (increase (travel-time ?v) (travel-duration ?r))
      (increase (sim-time) (travel-duration ?r))
    )
  )

  (:action traverse-road-static-road_0362-road_0407
    :parameters (?v - vehicle ?r - road_type_road_0362 ?next - road_type_road_0407)
    :precondition (and
      (ready-road ?v ?r)
      (road-next ?r ?next)
      (road-open ?r)
      (static-road ?r)
      (not (road-blocked ?r))
    )
    :effect (and
      (not (ready-road ?v ?r))
      (ready-road ?v ?next)
      (increase (travel-time ?v) (travel-duration ?r))
      (increase (sim-time) (travel-duration ?r))
    )
  )

  (:action traverse-road-static-road_0362-road_0408
    :parameters (?v - vehicle ?r - road_type_road_0362 ?next - road_type_road_0408)
    :precondition (and
      (ready-road ?v ?r)
      (road-next ?r ?next)
      (road-open ?r)
      (static-road ?r)
      (not (road-blocked ?r))
    )
    :effect (and
      (not (ready-road ?v ?r))
      (ready-road ?v ?next)
      (increase (travel-time ?v) (travel-duration ?r))
      (increase (sim-time) (travel-duration ?r))
    )
  )

  (:action traverse-road-static-road_0364-road_0618
    :parameters (?v - vehicle ?r - road_type_road_0364 ?next - road_type_road_0618)
    :precondition (and
      (ready-road ?v ?r)
      (road-next ?r ?next)
      (road-open ?r)
      (static-road ?r)
      (not (road-blocked ?r))
    )
    :effect (and
      (not (ready-road ?v ?r))
      (ready-road ?v ?next)
      (increase (travel-time ?v) (travel-duration ?r))
      (increase (sim-time) (travel-duration ?r))
    )
  )

  (:action traverse-road-static-road_0364-road_0619
    :parameters (?v - vehicle ?r - road_type_road_0364 ?next - road_type_road_0619)
    :precondition (and
      (ready-road ?v ?r)
      (road-next ?r ?next)
      (road-open ?r)
      (static-road ?r)
      (not (road-blocked ?r))
    )
    :effect (and
      (not (ready-road ?v ?r))
      (ready-road ?v ?next)
      (increase (travel-time ?v) (travel-duration ?r))
      (increase (sim-time) (travel-duration ?r))
    )
  )

  (:action traverse-road-static-road_0364-road_0620
    :parameters (?v - vehicle ?r - road_type_road_0364 ?next - road_type_road_0620)
    :precondition (and
      (ready-road ?v ?r)
      (road-next ?r ?next)
      (road-open ?r)
      (static-road ?r)
      (not (road-blocked ?r))
    )
    :effect (and
      (not (ready-road ?v ?r))
      (ready-road ?v ?next)
      (increase (travel-time ?v) (travel-duration ?r))
      (increase (sim-time) (travel-duration ?r))
    )
  )

  (:action traverse-road-static-road_0365-road_0097
    :parameters (?v - vehicle ?r - road_type_road_0365 ?next - road_type_road_0097)
    :precondition (and
      (ready-road ?v ?r)
      (road-next ?r ?next)
      (road-open ?r)
      (static-road ?r)
      (not (road-blocked ?r))
    )
    :effect (and
      (not (ready-road ?v ?r))
      (ready-road ?v ?next)
      (increase (travel-time ?v) (travel-duration ?r))
      (increase (sim-time) (travel-duration ?r))
    )
  )

  (:action traverse-road-static-road_0367-road_0433
    :parameters (?v - vehicle ?r - road_type_road_0367 ?next - road_type_road_0433)
    :precondition (and
      (ready-road ?v ?r)
      (road-next ?r ?next)
      (road-open ?r)
      (static-road ?r)
      (not (road-blocked ?r))
    )
    :effect (and
      (not (ready-road ?v ?r))
      (ready-road ?v ?next)
      (increase (travel-time ?v) (travel-duration ?r))
      (increase (sim-time) (travel-duration ?r))
    )
  )

  (:action traverse-road-dynamic-road_0370-road_0648-tw_00000
    :parameters (?v - vehicle ?r - road_type_road_0370 ?next - road_type_road_0648 ?w - window_type_tw_00000)
    :precondition (and
      (ready-road ?v ?r)
      (road-next ?r ?next)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
    )
    :effect (and
      (not (ready-road ?v ?r))
      (ready-road ?v ?next)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-road_0370-road_0648-tw_00030
    :parameters (?v - vehicle ?r - road_type_road_0370 ?next - road_type_road_0648 ?w - window_type_tw_00030)
    :precondition (and
      (ready-road ?v ?r)
      (road-next ?r ?next)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
    )
    :effect (and
      (not (ready-road ?v ?r))
      (ready-road ?v ?next)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-road_0370-road_0648-tw_00060
    :parameters (?v - vehicle ?r - road_type_road_0370 ?next - road_type_road_0648 ?w - window_type_tw_00060)
    :precondition (and
      (ready-road ?v ?r)
      (road-next ?r ?next)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
    )
    :effect (and
      (not (ready-road ?v ?r))
      (ready-road ?v ?next)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-road_0370-road_0648-tw_00090
    :parameters (?v - vehicle ?r - road_type_road_0370 ?next - road_type_road_0648 ?w - window_type_tw_00090)
    :precondition (and
      (ready-road ?v ?r)
      (road-next ?r ?next)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
    )
    :effect (and
      (not (ready-road ?v ?r))
      (ready-road ?v ?next)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-road_0370-road_0648-tw_00120
    :parameters (?v - vehicle ?r - road_type_road_0370 ?next - road_type_road_0648 ?w - window_type_tw_00120)
    :precondition (and
      (ready-road ?v ?r)
      (road-next ?r ?next)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
    )
    :effect (and
      (not (ready-road ?v ?r))
      (ready-road ?v ?next)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-road_0370-road_0648-tw_00150
    :parameters (?v - vehicle ?r - road_type_road_0370 ?next - road_type_road_0648 ?w - window_type_tw_00150)
    :precondition (and
      (ready-road ?v ?r)
      (road-next ?r ?next)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
    )
    :effect (and
      (not (ready-road ?v ?r))
      (ready-road ?v ?next)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-road_0370-road_0648-tw_00180
    :parameters (?v - vehicle ?r - road_type_road_0370 ?next - road_type_road_0648 ?w - window_type_tw_00180)
    :precondition (and
      (ready-road ?v ?r)
      (road-next ?r ?next)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
    )
    :effect (and
      (not (ready-road ?v ?r))
      (ready-road ?v ?next)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-road_0370-road_0648-tw_00210
    :parameters (?v - vehicle ?r - road_type_road_0370 ?next - road_type_road_0648 ?w - window_type_tw_00210)
    :precondition (and
      (ready-road ?v ?r)
      (road-next ?r ?next)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
    )
    :effect (and
      (not (ready-road ?v ?r))
      (ready-road ?v ?next)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-road_0370-road_0648-tw_00240
    :parameters (?v - vehicle ?r - road_type_road_0370 ?next - road_type_road_0648 ?w - window_type_tw_00240)
    :precondition (and
      (ready-road ?v ?r)
      (road-next ?r ?next)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
    )
    :effect (and
      (not (ready-road ?v ?r))
      (ready-road ?v ?next)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-road_0370-road_0648-tw_00270
    :parameters (?v - vehicle ?r - road_type_road_0370 ?next - road_type_road_0648 ?w - window_type_tw_00270)
    :precondition (and
      (ready-road ?v ?r)
      (road-next ?r ?next)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
    )
    :effect (and
      (not (ready-road ?v ?r))
      (ready-road ?v ?next)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-road_0370-road_0648-tw_00300
    :parameters (?v - vehicle ?r - road_type_road_0370 ?next - road_type_road_0648 ?w - window_type_tw_00300)
    :precondition (and
      (ready-road ?v ?r)
      (road-next ?r ?next)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
    )
    :effect (and
      (not (ready-road ?v ?r))
      (ready-road ?v ?next)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-road_0370-road_0648-tw_00330
    :parameters (?v - vehicle ?r - road_type_road_0370 ?next - road_type_road_0648 ?w - window_type_tw_00330)
    :precondition (and
      (ready-road ?v ?r)
      (road-next ?r ?next)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
    )
    :effect (and
      (not (ready-road ?v ?r))
      (ready-road ?v ?next)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-road_0370-road_0648-tw_00360
    :parameters (?v - vehicle ?r - road_type_road_0370 ?next - road_type_road_0648 ?w - window_type_tw_00360)
    :precondition (and
      (ready-road ?v ?r)
      (road-next ?r ?next)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
    )
    :effect (and
      (not (ready-road ?v ?r))
      (ready-road ?v ?next)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-road_0370-road_0648-tw_00390
    :parameters (?v - vehicle ?r - road_type_road_0370 ?next - road_type_road_0648 ?w - window_type_tw_00390)
    :precondition (and
      (ready-road ?v ?r)
      (road-next ?r ?next)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
    )
    :effect (and
      (not (ready-road ?v ?r))
      (ready-road ?v ?next)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-road_0370-road_0648-tw_00420
    :parameters (?v - vehicle ?r - road_type_road_0370 ?next - road_type_road_0648 ?w - window_type_tw_00420)
    :precondition (and
      (ready-road ?v ?r)
      (road-next ?r ?next)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
    )
    :effect (and
      (not (ready-road ?v ?r))
      (ready-road ?v ?next)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-road_0370-road_0648-tw_00450
    :parameters (?v - vehicle ?r - road_type_road_0370 ?next - road_type_road_0648 ?w - window_type_tw_00450)
    :precondition (and
      (ready-road ?v ?r)
      (road-next ?r ?next)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
    )
    :effect (and
      (not (ready-road ?v ?r))
      (ready-road ?v ?next)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-road_0370-road_0648-tw_00480
    :parameters (?v - vehicle ?r - road_type_road_0370 ?next - road_type_road_0648 ?w - window_type_tw_00480)
    :precondition (and
      (ready-road ?v ?r)
      (road-next ?r ?next)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
    )
    :effect (and
      (not (ready-road ?v ?r))
      (ready-road ?v ?next)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-road_0370-road_0648-tw_00510
    :parameters (?v - vehicle ?r - road_type_road_0370 ?next - road_type_road_0648 ?w - window_type_tw_00510)
    :precondition (and
      (ready-road ?v ?r)
      (road-next ?r ?next)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
    )
    :effect (and
      (not (ready-road ?v ?r))
      (ready-road ?v ?next)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-road_0370-road_0648-tw_00540
    :parameters (?v - vehicle ?r - road_type_road_0370 ?next - road_type_road_0648 ?w - window_type_tw_00540)
    :precondition (and
      (ready-road ?v ?r)
      (road-next ?r ?next)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
    )
    :effect (and
      (not (ready-road ?v ?r))
      (ready-road ?v ?next)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-road_0370-road_0648-tw_00570
    :parameters (?v - vehicle ?r - road_type_road_0370 ?next - road_type_road_0648 ?w - window_type_tw_00570)
    :precondition (and
      (ready-road ?v ?r)
      (road-next ?r ?next)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
    )
    :effect (and
      (not (ready-road ?v ?r))
      (ready-road ?v ?next)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-road_0370-road_0648-tw_00600
    :parameters (?v - vehicle ?r - road_type_road_0370 ?next - road_type_road_0648 ?w - window_type_tw_00600)
    :precondition (and
      (ready-road ?v ?r)
      (road-next ?r ?next)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
    )
    :effect (and
      (not (ready-road ?v ?r))
      (ready-road ?v ?next)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-road_0370-road_0649-tw_00000
    :parameters (?v - vehicle ?r - road_type_road_0370 ?next - road_type_road_0649 ?w - window_type_tw_00000)
    :precondition (and
      (ready-road ?v ?r)
      (road-next ?r ?next)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
    )
    :effect (and
      (not (ready-road ?v ?r))
      (ready-road ?v ?next)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-road_0370-road_0649-tw_00030
    :parameters (?v - vehicle ?r - road_type_road_0370 ?next - road_type_road_0649 ?w - window_type_tw_00030)
    :precondition (and
      (ready-road ?v ?r)
      (road-next ?r ?next)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
    )
    :effect (and
      (not (ready-road ?v ?r))
      (ready-road ?v ?next)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-road_0370-road_0649-tw_00060
    :parameters (?v - vehicle ?r - road_type_road_0370 ?next - road_type_road_0649 ?w - window_type_tw_00060)
    :precondition (and
      (ready-road ?v ?r)
      (road-next ?r ?next)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
    )
    :effect (and
      (not (ready-road ?v ?r))
      (ready-road ?v ?next)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-road_0370-road_0649-tw_00090
    :parameters (?v - vehicle ?r - road_type_road_0370 ?next - road_type_road_0649 ?w - window_type_tw_00090)
    :precondition (and
      (ready-road ?v ?r)
      (road-next ?r ?next)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
    )
    :effect (and
      (not (ready-road ?v ?r))
      (ready-road ?v ?next)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-road_0370-road_0649-tw_00120
    :parameters (?v - vehicle ?r - road_type_road_0370 ?next - road_type_road_0649 ?w - window_type_tw_00120)
    :precondition (and
      (ready-road ?v ?r)
      (road-next ?r ?next)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
    )
    :effect (and
      (not (ready-road ?v ?r))
      (ready-road ?v ?next)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-road_0370-road_0649-tw_00150
    :parameters (?v - vehicle ?r - road_type_road_0370 ?next - road_type_road_0649 ?w - window_type_tw_00150)
    :precondition (and
      (ready-road ?v ?r)
      (road-next ?r ?next)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
    )
    :effect (and
      (not (ready-road ?v ?r))
      (ready-road ?v ?next)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-road_0370-road_0649-tw_00180
    :parameters (?v - vehicle ?r - road_type_road_0370 ?next - road_type_road_0649 ?w - window_type_tw_00180)
    :precondition (and
      (ready-road ?v ?r)
      (road-next ?r ?next)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
    )
    :effect (and
      (not (ready-road ?v ?r))
      (ready-road ?v ?next)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-road_0370-road_0649-tw_00210
    :parameters (?v - vehicle ?r - road_type_road_0370 ?next - road_type_road_0649 ?w - window_type_tw_00210)
    :precondition (and
      (ready-road ?v ?r)
      (road-next ?r ?next)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
    )
    :effect (and
      (not (ready-road ?v ?r))
      (ready-road ?v ?next)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-road_0370-road_0649-tw_00240
    :parameters (?v - vehicle ?r - road_type_road_0370 ?next - road_type_road_0649 ?w - window_type_tw_00240)
    :precondition (and
      (ready-road ?v ?r)
      (road-next ?r ?next)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
    )
    :effect (and
      (not (ready-road ?v ?r))
      (ready-road ?v ?next)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-road_0370-road_0649-tw_00270
    :parameters (?v - vehicle ?r - road_type_road_0370 ?next - road_type_road_0649 ?w - window_type_tw_00270)
    :precondition (and
      (ready-road ?v ?r)
      (road-next ?r ?next)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
    )
    :effect (and
      (not (ready-road ?v ?r))
      (ready-road ?v ?next)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-road_0370-road_0649-tw_00300
    :parameters (?v - vehicle ?r - road_type_road_0370 ?next - road_type_road_0649 ?w - window_type_tw_00300)
    :precondition (and
      (ready-road ?v ?r)
      (road-next ?r ?next)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
    )
    :effect (and
      (not (ready-road ?v ?r))
      (ready-road ?v ?next)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-road_0370-road_0649-tw_00330
    :parameters (?v - vehicle ?r - road_type_road_0370 ?next - road_type_road_0649 ?w - window_type_tw_00330)
    :precondition (and
      (ready-road ?v ?r)
      (road-next ?r ?next)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
    )
    :effect (and
      (not (ready-road ?v ?r))
      (ready-road ?v ?next)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-road_0370-road_0649-tw_00360
    :parameters (?v - vehicle ?r - road_type_road_0370 ?next - road_type_road_0649 ?w - window_type_tw_00360)
    :precondition (and
      (ready-road ?v ?r)
      (road-next ?r ?next)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
    )
    :effect (and
      (not (ready-road ?v ?r))
      (ready-road ?v ?next)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-road_0370-road_0649-tw_00390
    :parameters (?v - vehicle ?r - road_type_road_0370 ?next - road_type_road_0649 ?w - window_type_tw_00390)
    :precondition (and
      (ready-road ?v ?r)
      (road-next ?r ?next)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
    )
    :effect (and
      (not (ready-road ?v ?r))
      (ready-road ?v ?next)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-road_0370-road_0649-tw_00420
    :parameters (?v - vehicle ?r - road_type_road_0370 ?next - road_type_road_0649 ?w - window_type_tw_00420)
    :precondition (and
      (ready-road ?v ?r)
      (road-next ?r ?next)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
    )
    :effect (and
      (not (ready-road ?v ?r))
      (ready-road ?v ?next)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-road_0370-road_0649-tw_00450
    :parameters (?v - vehicle ?r - road_type_road_0370 ?next - road_type_road_0649 ?w - window_type_tw_00450)
    :precondition (and
      (ready-road ?v ?r)
      (road-next ?r ?next)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
    )
    :effect (and
      (not (ready-road ?v ?r))
      (ready-road ?v ?next)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-road_0370-road_0649-tw_00480
    :parameters (?v - vehicle ?r - road_type_road_0370 ?next - road_type_road_0649 ?w - window_type_tw_00480)
    :precondition (and
      (ready-road ?v ?r)
      (road-next ?r ?next)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
    )
    :effect (and
      (not (ready-road ?v ?r))
      (ready-road ?v ?next)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-road_0370-road_0649-tw_00510
    :parameters (?v - vehicle ?r - road_type_road_0370 ?next - road_type_road_0649 ?w - window_type_tw_00510)
    :precondition (and
      (ready-road ?v ?r)
      (road-next ?r ?next)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
    )
    :effect (and
      (not (ready-road ?v ?r))
      (ready-road ?v ?next)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-road_0370-road_0649-tw_00540
    :parameters (?v - vehicle ?r - road_type_road_0370 ?next - road_type_road_0649 ?w - window_type_tw_00540)
    :precondition (and
      (ready-road ?v ?r)
      (road-next ?r ?next)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
    )
    :effect (and
      (not (ready-road ?v ?r))
      (ready-road ?v ?next)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-road_0370-road_0649-tw_00570
    :parameters (?v - vehicle ?r - road_type_road_0370 ?next - road_type_road_0649 ?w - window_type_tw_00570)
    :precondition (and
      (ready-road ?v ?r)
      (road-next ?r ?next)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
    )
    :effect (and
      (not (ready-road ?v ?r))
      (ready-road ?v ?next)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-road_0370-road_0649-tw_00600
    :parameters (?v - vehicle ?r - road_type_road_0370 ?next - road_type_road_0649 ?w - window_type_tw_00600)
    :precondition (and
      (ready-road ?v ?r)
      (road-next ?r ?next)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
    )
    :effect (and
      (not (ready-road ?v ?r))
      (ready-road ?v ?next)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-static-road_0371-road_0370
    :parameters (?v - vehicle ?r - road_type_road_0371 ?next - road_type_road_0370)
    :precondition (and
      (ready-road ?v ?r)
      (road-next ?r ?next)
      (road-open ?r)
      (static-road ?r)
      (not (road-blocked ?r))
    )
    :effect (and
      (not (ready-road ?v ?r))
      (ready-road ?v ?next)
      (increase (travel-time ?v) (travel-duration ?r))
      (increase (sim-time) (travel-duration ?r))
    )
  )

  (:action traverse-road-static-road_0372-road_0383
    :parameters (?v - vehicle ?r - road_type_road_0372 ?next - road_type_road_0383)
    :precondition (and
      (ready-road ?v ?r)
      (road-next ?r ?next)
      (road-open ?r)
      (static-road ?r)
      (not (road-blocked ?r))
    )
    :effect (and
      (not (ready-road ?v ?r))
      (ready-road ?v ?next)
      (increase (travel-time ?v) (travel-duration ?r))
      (increase (sim-time) (travel-duration ?r))
    )
  )

  (:action traverse-road-static-road_0372-road_0384
    :parameters (?v - vehicle ?r - road_type_road_0372 ?next - road_type_road_0384)
    :precondition (and
      (ready-road ?v ?r)
      (road-next ?r ?next)
      (road-open ?r)
      (static-road ?r)
      (not (road-blocked ?r))
    )
    :effect (and
      (not (ready-road ?v ?r))
      (ready-road ?v ?next)
      (increase (travel-time ?v) (travel-duration ?r))
      (increase (sim-time) (travel-duration ?r))
    )
  )

  (:action traverse-road-static-road_0373-road_0371
    :parameters (?v - vehicle ?r - road_type_road_0373 ?next - road_type_road_0371)
    :precondition (and
      (ready-road ?v ?r)
      (road-next ?r ?next)
      (road-open ?r)
      (static-road ?r)
      (not (road-blocked ?r))
    )
    :effect (and
      (not (ready-road ?v ?r))
      (ready-road ?v ?next)
      (increase (travel-time ?v) (travel-duration ?r))
      (increase (sim-time) (travel-duration ?r))
    )
  )

  (:action traverse-road-static-road_0373-road_0372
    :parameters (?v - vehicle ?r - road_type_road_0373 ?next - road_type_road_0372)
    :precondition (and
      (ready-road ?v ?r)
      (road-next ?r ?next)
      (road-open ?r)
      (static-road ?r)
      (not (road-blocked ?r))
    )
    :effect (and
      (not (ready-road ?v ?r))
      (ready-road ?v ?next)
      (increase (travel-time ?v) (travel-duration ?r))
      (increase (sim-time) (travel-duration ?r))
    )
  )

  (:action traverse-road-static-road_0374-road_0706
    :parameters (?v - vehicle ?r - road_type_road_0374 ?next - road_type_road_0706)
    :precondition (and
      (ready-road ?v ?r)
      (road-next ?r ?next)
      (road-open ?r)
      (static-road ?r)
      (not (road-blocked ?r))
    )
    :effect (and
      (not (ready-road ?v ?r))
      (ready-road ?v ?next)
      (increase (travel-time ?v) (travel-duration ?r))
      (increase (sim-time) (travel-duration ?r))
    )
  )

  (:action traverse-road-static-road_0375-road_0193
    :parameters (?v - vehicle ?r - road_type_road_0375 ?next - road_type_road_0193)
    :precondition (and
      (ready-road ?v ?r)
      (road-next ?r ?next)
      (road-open ?r)
      (static-road ?r)
      (not (road-blocked ?r))
    )
    :effect (and
      (not (ready-road ?v ?r))
      (ready-road ?v ?next)
      (increase (travel-time ?v) (travel-duration ?r))
      (increase (sim-time) (travel-duration ?r))
    )
  )

  (:action traverse-road-static-road_0376-road_0038
    :parameters (?v - vehicle ?r - road_type_road_0376 ?next - road_type_road_0038)
    :precondition (and
      (ready-road ?v ?r)
      (road-next ?r ?next)
      (road-open ?r)
      (static-road ?r)
      (not (road-blocked ?r))
    )
    :effect (and
      (not (ready-road ?v ?r))
      (ready-road ?v ?next)
      (increase (travel-time ?v) (travel-duration ?r))
      (increase (sim-time) (travel-duration ?r))
    )
  )

  (:action traverse-road-static-road_0377-road_0150
    :parameters (?v - vehicle ?r - road_type_road_0377 ?next - road_type_road_0150)
    :precondition (and
      (ready-road ?v ?r)
      (road-next ?r ?next)
      (road-open ?r)
      (static-road ?r)
      (not (road-blocked ?r))
    )
    :effect (and
      (not (ready-road ?v ?r))
      (ready-road ?v ?next)
      (increase (travel-time ?v) (travel-duration ?r))
      (increase (sim-time) (travel-duration ?r))
    )
  )

  (:action traverse-road-static-road_0377-road_0151
    :parameters (?v - vehicle ?r - road_type_road_0377 ?next - road_type_road_0151)
    :precondition (and
      (ready-road ?v ?r)
      (road-next ?r ?next)
      (road-open ?r)
      (static-road ?r)
      (not (road-blocked ?r))
    )
    :effect (and
      (not (ready-road ?v ?r))
      (ready-road ?v ?next)
      (increase (travel-time ?v) (travel-duration ?r))
      (increase (sim-time) (travel-duration ?r))
    )
  )

  (:action traverse-road-static-road_0378-road_0194
    :parameters (?v - vehicle ?r - road_type_road_0378 ?next - road_type_road_0194)
    :precondition (and
      (ready-road ?v ?r)
      (road-next ?r ?next)
      (road-open ?r)
      (static-road ?r)
      (not (road-blocked ?r))
    )
    :effect (and
      (not (ready-road ?v ?r))
      (ready-road ?v ?next)
      (increase (travel-time ?v) (travel-duration ?r))
      (increase (sim-time) (travel-duration ?r))
    )
  )

  (:action traverse-road-static-road_0378-road_0195
    :parameters (?v - vehicle ?r - road_type_road_0378 ?next - road_type_road_0195)
    :precondition (and
      (ready-road ?v ?r)
      (road-next ?r ?next)
      (road-open ?r)
      (static-road ?r)
      (not (road-blocked ?r))
    )
    :effect (and
      (not (ready-road ?v ?r))
      (ready-road ?v ?next)
      (increase (travel-time ?v) (travel-duration ?r))
      (increase (sim-time) (travel-duration ?r))
    )
  )

  (:action traverse-road-static-road_0379-road_0608
    :parameters (?v - vehicle ?r - road_type_road_0379 ?next - road_type_road_0608)
    :precondition (and
      (ready-road ?v ?r)
      (road-next ?r ?next)
      (road-open ?r)
      (static-road ?r)
      (not (road-blocked ?r))
    )
    :effect (and
      (not (ready-road ?v ?r))
      (ready-road ?v ?next)
      (increase (travel-time ?v) (travel-duration ?r))
      (increase (sim-time) (travel-duration ?r))
    )
  )

  (:action traverse-road-static-road_0379-road_0609
    :parameters (?v - vehicle ?r - road_type_road_0379 ?next - road_type_road_0609)
    :precondition (and
      (ready-road ?v ?r)
      (road-next ?r ?next)
      (road-open ?r)
      (static-road ?r)
      (not (road-blocked ?r))
    )
    :effect (and
      (not (ready-road ?v ?r))
      (ready-road ?v ?next)
      (increase (travel-time ?v) (travel-duration ?r))
      (increase (sim-time) (travel-duration ?r))
    )
  )

  (:action traverse-road-static-road_0380-road_0459
    :parameters (?v - vehicle ?r - road_type_road_0380 ?next - road_type_road_0459)
    :precondition (and
      (ready-road ?v ?r)
      (road-next ?r ?next)
      (road-open ?r)
      (static-road ?r)
      (not (road-blocked ?r))
    )
    :effect (and
      (not (ready-road ?v ?r))
      (ready-road ?v ?next)
      (increase (travel-time ?v) (travel-duration ?r))
      (increase (sim-time) (travel-duration ?r))
    )
  )

  (:action traverse-road-static-road_0380-road_0460
    :parameters (?v - vehicle ?r - road_type_road_0380 ?next - road_type_road_0460)
    :precondition (and
      (ready-road ?v ?r)
      (road-next ?r ?next)
      (road-open ?r)
      (static-road ?r)
      (not (road-blocked ?r))
    )
    :effect (and
      (not (ready-road ?v ?r))
      (ready-road ?v ?next)
      (increase (travel-time ?v) (travel-duration ?r))
      (increase (sim-time) (travel-duration ?r))
    )
  )

  (:action traverse-road-static-road_0381-macro_0034
    :parameters (?v - vehicle ?r - road_type_road_0381 ?next - road_type_macro_0034)
    :precondition (and
      (ready-road ?v ?r)
      (road-next ?r ?next)
      (road-open ?r)
      (static-road ?r)
      (not (road-blocked ?r))
    )
    :effect (and
      (not (ready-road ?v ?r))
      (ready-road ?v ?next)
      (increase (travel-time ?v) (travel-duration ?r))
      (increase (sim-time) (travel-duration ?r))
    )
  )

  (:action traverse-road-static-road_0381-road_0434
    :parameters (?v - vehicle ?r - road_type_road_0381 ?next - road_type_road_0434)
    :precondition (and
      (ready-road ?v ?r)
      (road-next ?r ?next)
      (road-open ?r)
      (static-road ?r)
      (not (road-blocked ?r))
    )
    :effect (and
      (not (ready-road ?v ?r))
      (ready-road ?v ?next)
      (increase (travel-time ?v) (travel-duration ?r))
      (increase (sim-time) (travel-duration ?r))
    )
  )

  (:action traverse-road-static-road_0381-road_0436
    :parameters (?v - vehicle ?r - road_type_road_0381 ?next - road_type_road_0436)
    :precondition (and
      (ready-road ?v ?r)
      (road-next ?r ?next)
      (road-open ?r)
      (static-road ?r)
      (not (road-blocked ?r))
    )
    :effect (and
      (not (ready-road ?v ?r))
      (ready-road ?v ?next)
      (increase (travel-time ?v) (travel-duration ?r))
      (increase (sim-time) (travel-duration ?r))
    )
  )

  (:action traverse-road-static-road_0382-road_0497
    :parameters (?v - vehicle ?r - road_type_road_0382 ?next - road_type_road_0497)
    :precondition (and
      (ready-road ?v ?r)
      (road-next ?r ?next)
      (road-open ?r)
      (static-road ?r)
      (not (road-blocked ?r))
    )
    :effect (and
      (not (ready-road ?v ?r))
      (ready-road ?v ?next)
      (increase (travel-time ?v) (travel-duration ?r))
      (increase (sim-time) (travel-duration ?r))
    )
  )

  (:action traverse-road-static-road_0382-road_0498
    :parameters (?v - vehicle ?r - road_type_road_0382 ?next - road_type_road_0498)
    :precondition (and
      (ready-road ?v ?r)
      (road-next ?r ?next)
      (road-open ?r)
      (static-road ?r)
      (not (road-blocked ?r))
    )
    :effect (and
      (not (ready-road ?v ?r))
      (ready-road ?v ?next)
      (increase (travel-time ?v) (travel-duration ?r))
      (increase (sim-time) (travel-duration ?r))
    )
  )

  (:action traverse-road-static-road_0383-road_0798
    :parameters (?v - vehicle ?r - road_type_road_0383 ?next - road_type_road_0798)
    :precondition (and
      (ready-road ?v ?r)
      (road-next ?r ?next)
      (road-open ?r)
      (static-road ?r)
      (not (road-blocked ?r))
    )
    :effect (and
      (not (ready-road ?v ?r))
      (ready-road ?v ?next)
      (increase (travel-time ?v) (travel-duration ?r))
      (increase (sim-time) (travel-duration ?r))
    )
  )

  (:action traverse-road-static-road_0383-road_0799
    :parameters (?v - vehicle ?r - road_type_road_0383 ?next - road_type_road_0799)
    :precondition (and
      (ready-road ?v ?r)
      (road-next ?r ?next)
      (road-open ?r)
      (static-road ?r)
      (not (road-blocked ?r))
    )
    :effect (and
      (not (ready-road ?v ?r))
      (ready-road ?v ?next)
      (increase (travel-time ?v) (travel-duration ?r))
      (increase (sim-time) (travel-duration ?r))
    )
  )

  (:action traverse-road-static-road_0384-road_0371
    :parameters (?v - vehicle ?r - road_type_road_0384 ?next - road_type_road_0371)
    :precondition (and
      (ready-road ?v ?r)
      (road-next ?r ?next)
      (road-open ?r)
      (static-road ?r)
      (not (road-blocked ?r))
    )
    :effect (and
      (not (ready-road ?v ?r))
      (ready-road ?v ?next)
      (increase (travel-time ?v) (travel-duration ?r))
      (increase (sim-time) (travel-duration ?r))
    )
  )

  (:action traverse-road-static-road_0384-road_0372
    :parameters (?v - vehicle ?r - road_type_road_0384 ?next - road_type_road_0372)
    :precondition (and
      (ready-road ?v ?r)
      (road-next ?r ?next)
      (road-open ?r)
      (static-road ?r)
      (not (road-blocked ?r))
    )
    :effect (and
      (not (ready-road ?v ?r))
      (ready-road ?v ?next)
      (increase (travel-time ?v) (travel-duration ?r))
      (increase (sim-time) (travel-duration ?r))
    )
  )

  (:action traverse-road-static-road_0385-road_0351
    :parameters (?v - vehicle ?r - road_type_road_0385 ?next - road_type_road_0351)
    :precondition (and
      (ready-road ?v ?r)
      (road-next ?r ?next)
      (road-open ?r)
      (static-road ?r)
      (not (road-blocked ?r))
    )
    :effect (and
      (not (ready-road ?v ?r))
      (ready-road ?v ?next)
      (increase (travel-time ?v) (travel-duration ?r))
      (increase (sim-time) (travel-duration ?r))
    )
  )

  (:action traverse-road-static-road_0386-road_0387
    :parameters (?v - vehicle ?r - road_type_road_0386 ?next - road_type_road_0387)
    :precondition (and
      (ready-road ?v ?r)
      (road-next ?r ?next)
      (road-open ?r)
      (static-road ?r)
      (not (road-blocked ?r))
    )
    :effect (and
      (not (ready-road ?v ?r))
      (ready-road ?v ?next)
      (increase (travel-time ?v) (travel-duration ?r))
      (increase (sim-time) (travel-duration ?r))
    )
  )

  (:action traverse-road-static-road_0387-road_0385
    :parameters (?v - vehicle ?r - road_type_road_0387 ?next - road_type_road_0385)
    :precondition (and
      (ready-road ?v ?r)
      (road-next ?r ?next)
      (road-open ?r)
      (static-road ?r)
      (not (road-blocked ?r))
    )
    :effect (and
      (not (ready-road ?v ?r))
      (ready-road ?v ?next)
      (increase (travel-time ?v) (travel-duration ?r))
      (increase (sim-time) (travel-duration ?r))
    )
  )

  (:action traverse-road-static-road_0387-road_0386
    :parameters (?v - vehicle ?r - road_type_road_0387 ?next - road_type_road_0386)
    :precondition (and
      (ready-road ?v ?r)
      (road-next ?r ?next)
      (road-open ?r)
      (static-road ?r)
      (not (road-blocked ?r))
    )
    :effect (and
      (not (ready-road ?v ?r))
      (ready-road ?v ?next)
      (increase (travel-time ?v) (travel-duration ?r))
      (increase (sim-time) (travel-duration ?r))
    )
  )

  (:action traverse-road-static-road_0388-road_0400
    :parameters (?v - vehicle ?r - road_type_road_0388 ?next - road_type_road_0400)
    :precondition (and
      (ready-road ?v ?r)
      (road-next ?r ?next)
      (road-open ?r)
      (static-road ?r)
      (not (road-blocked ?r))
    )
    :effect (and
      (not (ready-road ?v ?r))
      (ready-road ?v ?next)
      (increase (travel-time ?v) (travel-duration ?r))
      (increase (sim-time) (travel-duration ?r))
    )
  )

  (:action traverse-road-static-road_0389-road_0219
    :parameters (?v - vehicle ?r - road_type_road_0389 ?next - road_type_road_0219)
    :precondition (and
      (ready-road ?v ?r)
      (road-next ?r ?next)
      (road-open ?r)
      (static-road ?r)
      (not (road-blocked ?r))
    )
    :effect (and
      (not (ready-road ?v ?r))
      (ready-road ?v ?next)
      (increase (travel-time ?v) (travel-duration ?r))
      (increase (sim-time) (travel-duration ?r))
    )
  )

  (:action traverse-road-static-road_0390-road_0089
    :parameters (?v - vehicle ?r - road_type_road_0390 ?next - road_type_road_0089)
    :precondition (and
      (ready-road ?v ?r)
      (road-next ?r ?next)
      (road-open ?r)
      (static-road ?r)
      (not (road-blocked ?r))
    )
    :effect (and
      (not (ready-road ?v ?r))
      (ready-road ?v ?next)
      (increase (travel-time ?v) (travel-duration ?r))
      (increase (sim-time) (travel-duration ?r))
    )
  )

  (:action traverse-road-static-road_0390-road_0090
    :parameters (?v - vehicle ?r - road_type_road_0390 ?next - road_type_road_0090)
    :precondition (and
      (ready-road ?v ?r)
      (road-next ?r ?next)
      (road-open ?r)
      (static-road ?r)
      (not (road-blocked ?r))
    )
    :effect (and
      (not (ready-road ?v ?r))
      (ready-road ?v ?next)
      (increase (travel-time ?v) (travel-duration ?r))
      (increase (sim-time) (travel-duration ?r))
    )
  )

  (:action traverse-road-static-road_0390-road_0091
    :parameters (?v - vehicle ?r - road_type_road_0390 ?next - road_type_road_0091)
    :precondition (and
      (ready-road ?v ?r)
      (road-next ?r ?next)
      (road-open ?r)
      (static-road ?r)
      (not (road-blocked ?r))
    )
    :effect (and
      (not (ready-road ?v ?r))
      (ready-road ?v ?next)
      (increase (travel-time ?v) (travel-duration ?r))
      (increase (sim-time) (travel-duration ?r))
    )
  )

  (:action traverse-road-static-road_0391-road_0634
    :parameters (?v - vehicle ?r - road_type_road_0391 ?next - road_type_road_0634)
    :precondition (and
      (ready-road ?v ?r)
      (road-next ?r ?next)
      (road-open ?r)
      (static-road ?r)
      (not (road-blocked ?r))
    )
    :effect (and
      (not (ready-road ?v ?r))
      (ready-road ?v ?next)
      (increase (travel-time ?v) (travel-duration ?r))
      (increase (sim-time) (travel-duration ?r))
    )
  )

  (:action traverse-road-static-road_0391-road_0635
    :parameters (?v - vehicle ?r - road_type_road_0391 ?next - road_type_road_0635)
    :precondition (and
      (ready-road ?v ?r)
      (road-next ?r ?next)
      (road-open ?r)
      (static-road ?r)
      (not (road-blocked ?r))
    )
    :effect (and
      (not (ready-road ?v ?r))
      (ready-road ?v ?next)
      (increase (travel-time ?v) (travel-duration ?r))
      (increase (sim-time) (travel-duration ?r))
    )
  )

  (:action traverse-road-static-road_0392-macro_0031
    :parameters (?v - vehicle ?r - road_type_road_0392 ?next - road_type_macro_0031)
    :precondition (and
      (ready-road ?v ?r)
      (road-next ?r ?next)
      (road-open ?r)
      (static-road ?r)
      (not (road-blocked ?r))
    )
    :effect (and
      (not (ready-road ?v ?r))
      (ready-road ?v ?next)
      (increase (travel-time ?v) (travel-duration ?r))
      (increase (sim-time) (travel-duration ?r))
    )
  )

  (:action traverse-road-static-road_0393-road_0200
    :parameters (?v - vehicle ?r - road_type_road_0393 ?next - road_type_road_0200)
    :precondition (and
      (ready-road ?v ?r)
      (road-next ?r ?next)
      (road-open ?r)
      (static-road ?r)
      (not (road-blocked ?r))
    )
    :effect (and
      (not (ready-road ?v ?r))
      (ready-road ?v ?next)
      (increase (travel-time ?v) (travel-duration ?r))
      (increase (sim-time) (travel-duration ?r))
    )
  )

  (:action traverse-road-static-road_0394-road_0520
    :parameters (?v - vehicle ?r - road_type_road_0394 ?next - road_type_road_0520)
    :precondition (and
      (ready-road ?v ?r)
      (road-next ?r ?next)
      (road-open ?r)
      (static-road ?r)
      (not (road-blocked ?r))
    )
    :effect (and
      (not (ready-road ?v ?r))
      (ready-road ?v ?next)
      (increase (travel-time ?v) (travel-duration ?r))
      (increase (sim-time) (travel-duration ?r))
    )
  )

  (:action traverse-road-static-road_0395-macro_0000
    :parameters (?v - vehicle ?r - road_type_road_0395 ?next - road_type_macro_0000)
    :precondition (and
      (ready-road ?v ?r)
      (road-next ?r ?next)
      (road-open ?r)
      (static-road ?r)
      (not (road-blocked ?r))
    )
    :effect (and
      (not (ready-road ?v ?r))
      (ready-road ?v ?next)
      (increase (travel-time ?v) (travel-duration ?r))
      (increase (sim-time) (travel-duration ?r))
    )
  )

  (:action traverse-road-static-road_0395-road_0003
    :parameters (?v - vehicle ?r - road_type_road_0395 ?next - road_type_road_0003)
    :precondition (and
      (ready-road ?v ?r)
      (road-next ?r ?next)
      (road-open ?r)
      (static-road ?r)
      (not (road-blocked ?r))
    )
    :effect (and
      (not (ready-road ?v ?r))
      (ready-road ?v ?next)
      (increase (travel-time ?v) (travel-duration ?r))
      (increase (sim-time) (travel-duration ?r))
    )
  )

  (:action traverse-road-static-road_0395-road_0004
    :parameters (?v - vehicle ?r - road_type_road_0395 ?next - road_type_road_0004)
    :precondition (and
      (ready-road ?v ?r)
      (road-next ?r ?next)
      (road-open ?r)
      (static-road ?r)
      (not (road-blocked ?r))
    )
    :effect (and
      (not (ready-road ?v ?r))
      (ready-road ?v ?next)
      (increase (travel-time ?v) (travel-duration ?r))
      (increase (sim-time) (travel-duration ?r))
    )
  )

  (:action traverse-road-static-road_0396-road_0852
    :parameters (?v - vehicle ?r - road_type_road_0396 ?next - road_type_road_0852)
    :precondition (and
      (ready-road ?v ?r)
      (road-next ?r ?next)
      (road-open ?r)
      (static-road ?r)
      (not (road-blocked ?r))
    )
    :effect (and
      (not (ready-road ?v ?r))
      (ready-road ?v ?next)
      (increase (travel-time ?v) (travel-duration ?r))
      (increase (sim-time) (travel-duration ?r))
    )
  )

  (:action traverse-road-static-road_0396-road_0853
    :parameters (?v - vehicle ?r - road_type_road_0396 ?next - road_type_road_0853)
    :precondition (and
      (ready-road ?v ?r)
      (road-next ?r ?next)
      (road-open ?r)
      (static-road ?r)
      (not (road-blocked ?r))
    )
    :effect (and
      (not (ready-road ?v ?r))
      (ready-road ?v ?next)
      (increase (travel-time ?v) (travel-duration ?r))
      (increase (sim-time) (travel-duration ?r))
    )
  )

  (:action traverse-road-static-road_0397-road_0395
    :parameters (?v - vehicle ?r - road_type_road_0397 ?next - road_type_road_0395)
    :precondition (and
      (ready-road ?v ?r)
      (road-next ?r ?next)
      (road-open ?r)
      (static-road ?r)
      (not (road-blocked ?r))
    )
    :effect (and
      (not (ready-road ?v ?r))
      (ready-road ?v ?next)
      (increase (travel-time ?v) (travel-duration ?r))
      (increase (sim-time) (travel-duration ?r))
    )
  )

  (:action traverse-road-static-road_0397-road_0396
    :parameters (?v - vehicle ?r - road_type_road_0397 ?next - road_type_road_0396)
    :precondition (and
      (ready-road ?v ?r)
      (road-next ?r ?next)
      (road-open ?r)
      (static-road ?r)
      (not (road-blocked ?r))
    )
    :effect (and
      (not (ready-road ?v ?r))
      (ready-road ?v ?next)
      (increase (travel-time ?v) (travel-duration ?r))
      (increase (sim-time) (travel-duration ?r))
    )
  )

  (:action traverse-road-static-road_0398-road_0246
    :parameters (?v - vehicle ?r - road_type_road_0398 ?next - road_type_road_0246)
    :precondition (and
      (ready-road ?v ?r)
      (road-next ?r ?next)
      (road-open ?r)
      (static-road ?r)
      (not (road-blocked ?r))
    )
    :effect (and
      (not (ready-road ?v ?r))
      (ready-road ?v ?next)
      (increase (travel-time ?v) (travel-duration ?r))
      (increase (sim-time) (travel-duration ?r))
    )
  )

  (:action traverse-road-static-road_0399-road_0145
    :parameters (?v - vehicle ?r - road_type_road_0399 ?next - road_type_road_0145)
    :precondition (and
      (ready-road ?v ?r)
      (road-next ?r ?next)
      (road-open ?r)
      (static-road ?r)
      (not (road-blocked ?r))
    )
    :effect (and
      (not (ready-road ?v ?r))
      (ready-road ?v ?next)
      (increase (travel-time ?v) (travel-duration ?r))
      (increase (sim-time) (travel-duration ?r))
    )
  )

  (:action traverse-road-static-road_0399-road_0146
    :parameters (?v - vehicle ?r - road_type_road_0399 ?next - road_type_road_0146)
    :precondition (and
      (ready-road ?v ?r)
      (road-next ?r ?next)
      (road-open ?r)
      (static-road ?r)
      (not (road-blocked ?r))
    )
    :effect (and
      (not (ready-road ?v ?r))
      (ready-road ?v ?next)
      (increase (travel-time ?v) (travel-duration ?r))
      (increase (sim-time) (travel-duration ?r))
    )
  )

  (:action traverse-road-static-road_0399-road_0147
    :parameters (?v - vehicle ?r - road_type_road_0399 ?next - road_type_road_0147)
    :precondition (and
      (ready-road ?v ?r)
      (road-next ?r ?next)
      (road-open ?r)
      (static-road ?r)
      (not (road-blocked ?r))
    )
    :effect (and
      (not (ready-road ?v ?r))
      (ready-road ?v ?next)
      (increase (travel-time ?v) (travel-duration ?r))
      (increase (sim-time) (travel-duration ?r))
    )
  )

  (:action traverse-road-static-road_0400-road_0388
    :parameters (?v - vehicle ?r - road_type_road_0400 ?next - road_type_road_0388)
    :precondition (and
      (ready-road ?v ?r)
      (road-next ?r ?next)
      (road-open ?r)
      (static-road ?r)
      (not (road-blocked ?r))
    )
    :effect (and
      (not (ready-road ?v ?r))
      (ready-road ?v ?next)
      (increase (travel-time ?v) (travel-duration ?r))
      (increase (sim-time) (travel-duration ?r))
    )
  )

  (:action traverse-road-static-road_0400-road_0389
    :parameters (?v - vehicle ?r - road_type_road_0400 ?next - road_type_road_0389)
    :precondition (and
      (ready-road ?v ?r)
      (road-next ?r ?next)
      (road-open ?r)
      (static-road ?r)
      (not (road-blocked ?r))
    )
    :effect (and
      (not (ready-road ?v ?r))
      (ready-road ?v ?next)
      (increase (travel-time ?v) (travel-duration ?r))
      (increase (sim-time) (travel-duration ?r))
    )
  )

  (:action traverse-road-static-road_0400-road_0390
    :parameters (?v - vehicle ?r - road_type_road_0400 ?next - road_type_road_0390)
    :precondition (and
      (ready-road ?v ?r)
      (road-next ?r ?next)
      (road-open ?r)
      (static-road ?r)
      (not (road-blocked ?r))
    )
    :effect (and
      (not (ready-road ?v ?r))
      (ready-road ?v ?next)
      (increase (travel-time ?v) (travel-duration ?r))
      (increase (sim-time) (travel-duration ?r))
    )
  )

  (:action traverse-road-static-road_0401-road_0354
    :parameters (?v - vehicle ?r - road_type_road_0401 ?next - road_type_road_0354)
    :precondition (and
      (ready-road ?v ?r)
      (road-next ?r ?next)
      (road-open ?r)
      (static-road ?r)
      (not (road-blocked ?r))
    )
    :effect (and
      (not (ready-road ?v ?r))
      (ready-road ?v ?next)
      (increase (travel-time ?v) (travel-duration ?r))
      (increase (sim-time) (travel-duration ?r))
    )
  )

  (:action traverse-road-static-road_0401-road_0355
    :parameters (?v - vehicle ?r - road_type_road_0401 ?next - road_type_road_0355)
    :precondition (and
      (ready-road ?v ?r)
      (road-next ?r ?next)
      (road-open ?r)
      (static-road ?r)
      (not (road-blocked ?r))
    )
    :effect (and
      (not (ready-road ?v ?r))
      (ready-road ?v ?next)
      (increase (travel-time ?v) (travel-duration ?r))
      (increase (sim-time) (travel-duration ?r))
    )
  )

  (:action traverse-road-static-road_0401-road_0356
    :parameters (?v - vehicle ?r - road_type_road_0401 ?next - road_type_road_0356)
    :precondition (and
      (ready-road ?v ?r)
      (road-next ?r ?next)
      (road-open ?r)
      (static-road ?r)
      (not (road-blocked ?r))
    )
    :effect (and
      (not (ready-road ?v ?r))
      (ready-road ?v ?next)
      (increase (travel-time ?v) (travel-duration ?r))
      (increase (sim-time) (travel-duration ?r))
    )
  )

  (:action traverse-road-static-road_0403-road_0705
    :parameters (?v - vehicle ?r - road_type_road_0403 ?next - road_type_road_0705)
    :precondition (and
      (ready-road ?v ?r)
      (road-next ?r ?next)
      (road-open ?r)
      (static-road ?r)
      (not (road-blocked ?r))
    )
    :effect (and
      (not (ready-road ?v ?r))
      (ready-road ?v ?next)
      (increase (travel-time ?v) (travel-duration ?r))
      (increase (sim-time) (travel-duration ?r))
    )
  )

  (:action traverse-road-static-road_0404-road_0487
    :parameters (?v - vehicle ?r - road_type_road_0404 ?next - road_type_road_0487)
    :precondition (and
      (ready-road ?v ?r)
      (road-next ?r ?next)
      (road-open ?r)
      (static-road ?r)
      (not (road-blocked ?r))
    )
    :effect (and
      (not (ready-road ?v ?r))
      (ready-road ?v ?next)
      (increase (travel-time ?v) (travel-duration ?r))
      (increase (sim-time) (travel-duration ?r))
    )
  )

  (:action traverse-road-static-road_0405-road_0617
    :parameters (?v - vehicle ?r - road_type_road_0405 ?next - road_type_road_0617)
    :precondition (and
      (ready-road ?v ?r)
      (road-next ?r ?next)
      (road-open ?r)
      (static-road ?r)
      (not (road-blocked ?r))
    )
    :effect (and
      (not (ready-road ?v ?r))
      (ready-road ?v ?next)
      (increase (travel-time ?v) (travel-duration ?r))
      (increase (sim-time) (travel-duration ?r))
    )
  )

  (:action traverse-road-static-road_0406-road_0116
    :parameters (?v - vehicle ?r - road_type_road_0406 ?next - road_type_road_0116)
    :precondition (and
      (ready-road ?v ?r)
      (road-next ?r ?next)
      (road-open ?r)
      (static-road ?r)
      (not (road-blocked ?r))
    )
    :effect (and
      (not (ready-road ?v ?r))
      (ready-road ?v ?next)
      (increase (travel-time ?v) (travel-duration ?r))
      (increase (sim-time) (travel-duration ?r))
    )
  )

  (:action traverse-road-static-road_0407-road_0617
    :parameters (?v - vehicle ?r - road_type_road_0407 ?next - road_type_road_0617)
    :precondition (and
      (ready-road ?v ?r)
      (road-next ?r ?next)
      (road-open ?r)
      (static-road ?r)
      (not (road-blocked ?r))
    )
    :effect (and
      (not (ready-road ?v ?r))
      (ready-road ?v ?next)
      (increase (travel-time ?v) (travel-duration ?r))
      (increase (sim-time) (travel-duration ?r))
    )
  )

  (:action traverse-road-static-road_0408-road_0123
    :parameters (?v - vehicle ?r - road_type_road_0408 ?next - road_type_road_0123)
    :precondition (and
      (ready-road ?v ?r)
      (road-next ?r ?next)
      (road-open ?r)
      (static-road ?r)
      (not (road-blocked ?r))
    )
    :effect (and
      (not (ready-road ?v ?r))
      (ready-road ?v ?next)
      (increase (travel-time ?v) (travel-duration ?r))
      (increase (sim-time) (travel-duration ?r))
    )
  )

  (:action traverse-road-static-road_0408-road_0124
    :parameters (?v - vehicle ?r - road_type_road_0408 ?next - road_type_road_0124)
    :precondition (and
      (ready-road ?v ?r)
      (road-next ?r ?next)
      (road-open ?r)
      (static-road ?r)
      (not (road-blocked ?r))
    )
    :effect (and
      (not (ready-road ?v ?r))
      (ready-road ?v ?next)
      (increase (travel-time ?v) (travel-duration ?r))
      (increase (sim-time) (travel-duration ?r))
    )
  )

  (:action traverse-road-static-road_0408-road_0125
    :parameters (?v - vehicle ?r - road_type_road_0408 ?next - road_type_road_0125)
    :precondition (and
      (ready-road ?v ?r)
      (road-next ?r ?next)
      (road-open ?r)
      (static-road ?r)
      (not (road-blocked ?r))
    )
    :effect (and
      (not (ready-road ?v ?r))
      (ready-road ?v ?next)
      (increase (travel-time ?v) (travel-duration ?r))
      (increase (sim-time) (travel-duration ?r))
    )
  )

  (:action traverse-road-static-road_0408-road_0126
    :parameters (?v - vehicle ?r - road_type_road_0408 ?next - road_type_road_0126)
    :precondition (and
      (ready-road ?v ?r)
      (road-next ?r ?next)
      (road-open ?r)
      (static-road ?r)
      (not (road-blocked ?r))
    )
    :effect (and
      (not (ready-road ?v ?r))
      (ready-road ?v ?next)
      (increase (travel-time ?v) (travel-duration ?r))
      (increase (sim-time) (travel-duration ?r))
    )
  )

  (:action traverse-road-static-road_0410-road_0433
    :parameters (?v - vehicle ?r - road_type_road_0410 ?next - road_type_road_0433)
    :precondition (and
      (ready-road ?v ?r)
      (road-next ?r ?next)
      (road-open ?r)
      (static-road ?r)
      (not (road-blocked ?r))
    )
    :effect (and
      (not (ready-road ?v ?r))
      (ready-road ?v ?next)
      (increase (travel-time ?v) (travel-duration ?r))
      (increase (sim-time) (travel-duration ?r))
    )
  )

  (:action traverse-road-static-road_0411-road_0123
    :parameters (?v - vehicle ?r - road_type_road_0411 ?next - road_type_road_0123)
    :precondition (and
      (ready-road ?v ?r)
      (road-next ?r ?next)
      (road-open ?r)
      (static-road ?r)
      (not (road-blocked ?r))
    )
    :effect (and
      (not (ready-road ?v ?r))
      (ready-road ?v ?next)
      (increase (travel-time ?v) (travel-duration ?r))
      (increase (sim-time) (travel-duration ?r))
    )
  )

  (:action traverse-road-static-road_0411-road_0124
    :parameters (?v - vehicle ?r - road_type_road_0411 ?next - road_type_road_0124)
    :precondition (and
      (ready-road ?v ?r)
      (road-next ?r ?next)
      (road-open ?r)
      (static-road ?r)
      (not (road-blocked ?r))
    )
    :effect (and
      (not (ready-road ?v ?r))
      (ready-road ?v ?next)
      (increase (travel-time ?v) (travel-duration ?r))
      (increase (sim-time) (travel-duration ?r))
    )
  )

  (:action traverse-road-static-road_0411-road_0125
    :parameters (?v - vehicle ?r - road_type_road_0411 ?next - road_type_road_0125)
    :precondition (and
      (ready-road ?v ?r)
      (road-next ?r ?next)
      (road-open ?r)
      (static-road ?r)
      (not (road-blocked ?r))
    )
    :effect (and
      (not (ready-road ?v ?r))
      (ready-road ?v ?next)
      (increase (travel-time ?v) (travel-duration ?r))
      (increase (sim-time) (travel-duration ?r))
    )
  )

  (:action traverse-road-static-road_0411-road_0126
    :parameters (?v - vehicle ?r - road_type_road_0411 ?next - road_type_road_0126)
    :precondition (and
      (ready-road ?v ?r)
      (road-next ?r ?next)
      (road-open ?r)
      (static-road ?r)
      (not (road-blocked ?r))
    )
    :effect (and
      (not (ready-road ?v ?r))
      (ready-road ?v ?next)
      (increase (travel-time ?v) (travel-duration ?r))
      (increase (sim-time) (travel-duration ?r))
    )
  )

  (:action traverse-road-static-road_0412-road_0624
    :parameters (?v - vehicle ?r - road_type_road_0412 ?next - road_type_road_0624)
    :precondition (and
      (ready-road ?v ?r)
      (road-next ?r ?next)
      (road-open ?r)
      (static-road ?r)
      (not (road-blocked ?r))
    )
    :effect (and
      (not (ready-road ?v ?r))
      (ready-road ?v ?next)
      (increase (travel-time ?v) (travel-duration ?r))
      (increase (sim-time) (travel-duration ?r))
    )
  )

  (:action traverse-road-static-road_0412-road_0625
    :parameters (?v - vehicle ?r - road_type_road_0412 ?next - road_type_road_0625)
    :precondition (and
      (ready-road ?v ?r)
      (road-next ?r ?next)
      (road-open ?r)
      (static-road ?r)
      (not (road-blocked ?r))
    )
    :effect (and
      (not (ready-road ?v ?r))
      (ready-road ?v ?next)
      (increase (travel-time ?v) (travel-duration ?r))
      (increase (sim-time) (travel-duration ?r))
    )
  )

  (:action traverse-road-static-road_0413-road_0622
    :parameters (?v - vehicle ?r - road_type_road_0413 ?next - road_type_road_0622)
    :precondition (and
      (ready-road ?v ?r)
      (road-next ?r ?next)
      (road-open ?r)
      (static-road ?r)
      (not (road-blocked ?r))
    )
    :effect (and
      (not (ready-road ?v ?r))
      (ready-road ?v ?next)
      (increase (travel-time ?v) (travel-duration ?r))
      (increase (sim-time) (travel-duration ?r))
    )
  )

  (:action traverse-road-static-road_0413-road_0623
    :parameters (?v - vehicle ?r - road_type_road_0413 ?next - road_type_road_0623)
    :precondition (and
      (ready-road ?v ?r)
      (road-next ?r ?next)
      (road-open ?r)
      (static-road ?r)
      (not (road-blocked ?r))
    )
    :effect (and
      (not (ready-road ?v ?r))
      (ready-road ?v ?next)
      (increase (travel-time ?v) (travel-duration ?r))
      (increase (sim-time) (travel-duration ?r))
    )
  )

  (:action traverse-road-static-road_0414-road_0774
    :parameters (?v - vehicle ?r - road_type_road_0414 ?next - road_type_road_0774)
    :precondition (and
      (ready-road ?v ?r)
      (road-next ?r ?next)
      (road-open ?r)
      (static-road ?r)
      (not (road-blocked ?r))
    )
    :effect (and
      (not (ready-road ?v ?r))
      (ready-road ?v ?next)
      (increase (travel-time ?v) (travel-duration ?r))
      (increase (sim-time) (travel-duration ?r))
    )
  )

  (:action traverse-road-static-road_0414-road_0775
    :parameters (?v - vehicle ?r - road_type_road_0414 ?next - road_type_road_0775)
    :precondition (and
      (ready-road ?v ?r)
      (road-next ?r ?next)
      (road-open ?r)
      (static-road ?r)
      (not (road-blocked ?r))
    )
    :effect (and
      (not (ready-road ?v ?r))
      (ready-road ?v ?next)
      (increase (travel-time ?v) (travel-duration ?r))
      (increase (sim-time) (travel-duration ?r))
    )
  )

  (:action traverse-road-static-road_0416-road_0181
    :parameters (?v - vehicle ?r - road_type_road_0416 ?next - road_type_road_0181)
    :precondition (and
      (ready-road ?v ?r)
      (road-next ?r ?next)
      (road-open ?r)
      (static-road ?r)
      (not (road-blocked ?r))
    )
    :effect (and
      (not (ready-road ?v ?r))
      (ready-road ?v ?next)
      (increase (travel-time ?v) (travel-duration ?r))
      (increase (sim-time) (travel-duration ?r))
    )
  )

  (:action traverse-road-static-road_0417-road_0523
    :parameters (?v - vehicle ?r - road_type_road_0417 ?next - road_type_road_0523)
    :precondition (and
      (ready-road ?v ?r)
      (road-next ?r ?next)
      (road-open ?r)
      (static-road ?r)
      (not (road-blocked ?r))
    )
    :effect (and
      (not (ready-road ?v ?r))
      (ready-road ?v ?next)
      (increase (travel-time ?v) (travel-duration ?r))
      (increase (sim-time) (travel-duration ?r))
    )
  )

  (:action traverse-road-static-road_0417-road_0524
    :parameters (?v - vehicle ?r - road_type_road_0417 ?next - road_type_road_0524)
    :precondition (and
      (ready-road ?v ?r)
      (road-next ?r ?next)
      (road-open ?r)
      (static-road ?r)
      (not (road-blocked ?r))
    )
    :effect (and
      (not (ready-road ?v ?r))
      (ready-road ?v ?next)
      (increase (travel-time ?v) (travel-duration ?r))
      (increase (sim-time) (travel-duration ?r))
    )
  )

  (:action traverse-road-static-road_0417-road_0525
    :parameters (?v - vehicle ?r - road_type_road_0417 ?next - road_type_road_0525)
    :precondition (and
      (ready-road ?v ?r)
      (road-next ?r ?next)
      (road-open ?r)
      (static-road ?r)
      (not (road-blocked ?r))
    )
    :effect (and
      (not (ready-road ?v ?r))
      (ready-road ?v ?next)
      (increase (travel-time ?v) (travel-duration ?r))
      (increase (sim-time) (travel-duration ?r))
    )
  )

  (:action traverse-road-static-road_0417-road_0526
    :parameters (?v - vehicle ?r - road_type_road_0417 ?next - road_type_road_0526)
    :precondition (and
      (ready-road ?v ?r)
      (road-next ?r ?next)
      (road-open ?r)
      (static-road ?r)
      (not (road-blocked ?r))
    )
    :effect (and
      (not (ready-road ?v ?r))
      (ready-road ?v ?next)
      (increase (travel-time ?v) (travel-duration ?r))
      (increase (sim-time) (travel-duration ?r))
    )
  )

  (:action traverse-road-static-road_0418-road_0424
    :parameters (?v - vehicle ?r - road_type_road_0418 ?next - road_type_road_0424)
    :precondition (and
      (ready-road ?v ?r)
      (road-next ?r ?next)
      (road-open ?r)
      (static-road ?r)
      (not (road-blocked ?r))
    )
    :effect (and
      (not (ready-road ?v ?r))
      (ready-road ?v ?next)
      (increase (travel-time ?v) (travel-duration ?r))
      (increase (sim-time) (travel-duration ?r))
    )
  )

  (:action traverse-road-static-road_0418-road_0425
    :parameters (?v - vehicle ?r - road_type_road_0418 ?next - road_type_road_0425)
    :precondition (and
      (ready-road ?v ?r)
      (road-next ?r ?next)
      (road-open ?r)
      (static-road ?r)
      (not (road-blocked ?r))
    )
    :effect (and
      (not (ready-road ?v ?r))
      (ready-road ?v ?next)
      (increase (travel-time ?v) (travel-duration ?r))
      (increase (sim-time) (travel-duration ?r))
    )
  )

  (:action traverse-road-static-road_0419-road_0156
    :parameters (?v - vehicle ?r - road_type_road_0419 ?next - road_type_road_0156)
    :precondition (and
      (ready-road ?v ?r)
      (road-next ?r ?next)
      (road-open ?r)
      (static-road ?r)
      (not (road-blocked ?r))
    )
    :effect (and
      (not (ready-road ?v ?r))
      (ready-road ?v ?next)
      (increase (travel-time ?v) (travel-duration ?r))
      (increase (sim-time) (travel-duration ?r))
    )
  )

  (:action traverse-road-static-road_0419-road_0157
    :parameters (?v - vehicle ?r - road_type_road_0419 ?next - road_type_road_0157)
    :precondition (and
      (ready-road ?v ?r)
      (road-next ?r ?next)
      (road-open ?r)
      (static-road ?r)
      (not (road-blocked ?r))
    )
    :effect (and
      (not (ready-road ?v ?r))
      (ready-road ?v ?next)
      (increase (travel-time ?v) (travel-duration ?r))
      (increase (sim-time) (travel-duration ?r))
    )
  )

  (:action traverse-road-static-road_0420-road_0650
    :parameters (?v - vehicle ?r - road_type_road_0420 ?next - road_type_road_0650)
    :precondition (and
      (ready-road ?v ?r)
      (road-next ?r ?next)
      (road-open ?r)
      (static-road ?r)
      (not (road-blocked ?r))
    )
    :effect (and
      (not (ready-road ?v ?r))
      (ready-road ?v ?next)
      (increase (travel-time ?v) (travel-duration ?r))
      (increase (sim-time) (travel-duration ?r))
    )
  )

  (:action traverse-road-static-road_0421-road_0772
    :parameters (?v - vehicle ?r - road_type_road_0421 ?next - road_type_road_0772)
    :precondition (and
      (ready-road ?v ?r)
      (road-next ?r ?next)
      (road-open ?r)
      (static-road ?r)
      (not (road-blocked ?r))
    )
    :effect (and
      (not (ready-road ?v ?r))
      (ready-road ?v ?next)
      (increase (travel-time ?v) (travel-duration ?r))
      (increase (sim-time) (travel-duration ?r))
    )
  )

  (:action traverse-road-static-road_0421-road_0773
    :parameters (?v - vehicle ?r - road_type_road_0421 ?next - road_type_road_0773)
    :precondition (and
      (ready-road ?v ?r)
      (road-next ?r ?next)
      (road-open ?r)
      (static-road ?r)
      (not (road-blocked ?r))
    )
    :effect (and
      (not (ready-road ?v ?r))
      (ready-road ?v ?next)
      (increase (travel-time ?v) (travel-duration ?r))
      (increase (sim-time) (travel-duration ?r))
    )
  )

  (:action traverse-road-static-road_0422-road_0640
    :parameters (?v - vehicle ?r - road_type_road_0422 ?next - road_type_road_0640)
    :precondition (and
      (ready-road ?v ?r)
      (road-next ?r ?next)
      (road-open ?r)
      (static-road ?r)
      (not (road-blocked ?r))
    )
    :effect (and
      (not (ready-road ?v ?r))
      (ready-road ?v ?next)
      (increase (travel-time ?v) (travel-duration ?r))
      (increase (sim-time) (travel-duration ?r))
    )
  )

  (:action traverse-road-static-road_0422-road_0641
    :parameters (?v - vehicle ?r - road_type_road_0422 ?next - road_type_road_0641)
    :precondition (and
      (ready-road ?v ?r)
      (road-next ?r ?next)
      (road-open ?r)
      (static-road ?r)
      (not (road-blocked ?r))
    )
    :effect (and
      (not (ready-road ?v ?r))
      (ready-road ?v ?next)
      (increase (travel-time ?v) (travel-duration ?r))
      (increase (sim-time) (travel-duration ?r))
    )
  )

  (:action traverse-road-static-road_0423-road_0769
    :parameters (?v - vehicle ?r - road_type_road_0423 ?next - road_type_road_0769)
    :precondition (and
      (ready-road ?v ?r)
      (road-next ?r ?next)
      (road-open ?r)
      (static-road ?r)
      (not (road-blocked ?r))
    )
    :effect (and
      (not (ready-road ?v ?r))
      (ready-road ?v ?next)
      (increase (travel-time ?v) (travel-duration ?r))
      (increase (sim-time) (travel-duration ?r))
    )
  )

  (:action traverse-road-static-road_0423-road_0770
    :parameters (?v - vehicle ?r - road_type_road_0423 ?next - road_type_road_0770)
    :precondition (and
      (ready-road ?v ?r)
      (road-next ?r ?next)
      (road-open ?r)
      (static-road ?r)
      (not (road-blocked ?r))
    )
    :effect (and
      (not (ready-road ?v ?r))
      (ready-road ?v ?next)
      (increase (travel-time ?v) (travel-duration ?r))
      (increase (sim-time) (travel-duration ?r))
    )
  )

  (:action traverse-road-static-road_0423-road_0771
    :parameters (?v - vehicle ?r - road_type_road_0423 ?next - road_type_road_0771)
    :precondition (and
      (ready-road ?v ?r)
      (road-next ?r ?next)
      (road-open ?r)
      (static-road ?r)
      (not (road-blocked ?r))
    )
    :effect (and
      (not (ready-road ?v ?r))
      (ready-road ?v ?next)
      (increase (travel-time ?v) (travel-duration ?r))
      (increase (sim-time) (travel-duration ?r))
    )
  )

  (:action traverse-road-static-road_0424-road_0309
    :parameters (?v - vehicle ?r - road_type_road_0424 ?next - road_type_road_0309)
    :precondition (and
      (ready-road ?v ?r)
      (road-next ?r ?next)
      (road-open ?r)
      (static-road ?r)
      (not (road-blocked ?r))
    )
    :effect (and
      (not (ready-road ?v ?r))
      (ready-road ?v ?next)
      (increase (travel-time ?v) (travel-duration ?r))
      (increase (sim-time) (travel-duration ?r))
    )
  )

  (:action traverse-road-static-road_0424-road_0310
    :parameters (?v - vehicle ?r - road_type_road_0424 ?next - road_type_road_0310)
    :precondition (and
      (ready-road ?v ?r)
      (road-next ?r ?next)
      (road-open ?r)
      (static-road ?r)
      (not (road-blocked ?r))
    )
    :effect (and
      (not (ready-road ?v ?r))
      (ready-road ?v ?next)
      (increase (travel-time ?v) (travel-duration ?r))
      (increase (sim-time) (travel-duration ?r))
    )
  )

  (:action traverse-road-static-road_0425-road_0031
    :parameters (?v - vehicle ?r - road_type_road_0425 ?next - road_type_road_0031)
    :precondition (and
      (ready-road ?v ?r)
      (road-next ?r ?next)
      (road-open ?r)
      (static-road ?r)
      (not (road-blocked ?r))
    )
    :effect (and
      (not (ready-road ?v ?r))
      (ready-road ?v ?next)
      (increase (travel-time ?v) (travel-duration ?r))
      (increase (sim-time) (travel-duration ?r))
    )
  )

  (:action traverse-road-static-road_0425-road_0032
    :parameters (?v - vehicle ?r - road_type_road_0425 ?next - road_type_road_0032)
    :precondition (and
      (ready-road ?v ?r)
      (road-next ?r ?next)
      (road-open ?r)
      (static-road ?r)
      (not (road-blocked ?r))
    )
    :effect (and
      (not (ready-road ?v ?r))
      (ready-road ?v ?next)
      (increase (travel-time ?v) (travel-duration ?r))
      (increase (sim-time) (travel-duration ?r))
    )
  )

  (:action traverse-road-static-road_0425-road_0033
    :parameters (?v - vehicle ?r - road_type_road_0425 ?next - road_type_road_0033)
    :precondition (and
      (ready-road ?v ?r)
      (road-next ?r ?next)
      (road-open ?r)
      (static-road ?r)
      (not (road-blocked ?r))
    )
    :effect (and
      (not (ready-road ?v ?r))
      (ready-road ?v ?next)
      (increase (travel-time ?v) (travel-duration ?r))
      (increase (sim-time) (travel-duration ?r))
    )
  )

  (:action traverse-road-static-road_0426-road_0108
    :parameters (?v - vehicle ?r - road_type_road_0426 ?next - road_type_road_0108)
    :precondition (and
      (ready-road ?v ?r)
      (road-next ?r ?next)
      (road-open ?r)
      (static-road ?r)
      (not (road-blocked ?r))
    )
    :effect (and
      (not (ready-road ?v ?r))
      (ready-road ?v ?next)
      (increase (travel-time ?v) (travel-duration ?r))
      (increase (sim-time) (travel-duration ?r))
    )
  )

  (:action traverse-road-static-road_0426-road_0109
    :parameters (?v - vehicle ?r - road_type_road_0426 ?next - road_type_road_0109)
    :precondition (and
      (ready-road ?v ?r)
      (road-next ?r ?next)
      (road-open ?r)
      (static-road ?r)
      (not (road-blocked ?r))
    )
    :effect (and
      (not (ready-road ?v ?r))
      (ready-road ?v ?next)
      (increase (travel-time ?v) (travel-duration ?r))
      (increase (sim-time) (travel-duration ?r))
    )
  )

  (:action traverse-road-static-road_0427-macro_0021
    :parameters (?v - vehicle ?r - road_type_road_0427 ?next - road_type_macro_0021)
    :precondition (and
      (ready-road ?v ?r)
      (road-next ?r ?next)
      (road-open ?r)
      (static-road ?r)
      (not (road-blocked ?r))
    )
    :effect (and
      (not (ready-road ?v ?r))
      (ready-road ?v ?next)
      (increase (travel-time ?v) (travel-duration ?r))
      (increase (sim-time) (travel-duration ?r))
    )
  )

  (:action traverse-road-static-road_0427-road_0280
    :parameters (?v - vehicle ?r - road_type_road_0427 ?next - road_type_road_0280)
    :precondition (and
      (ready-road ?v ?r)
      (road-next ?r ?next)
      (road-open ?r)
      (static-road ?r)
      (not (road-blocked ?r))
    )
    :effect (and
      (not (ready-road ?v ?r))
      (ready-road ?v ?next)
      (increase (travel-time ?v) (travel-duration ?r))
      (increase (sim-time) (travel-duration ?r))
    )
  )

  (:action traverse-road-static-road_0428-road_0696
    :parameters (?v - vehicle ?r - road_type_road_0428 ?next - road_type_road_0696)
    :precondition (and
      (ready-road ?v ?r)
      (road-next ?r ?next)
      (road-open ?r)
      (static-road ?r)
      (not (road-blocked ?r))
    )
    :effect (and
      (not (ready-road ?v ?r))
      (ready-road ?v ?next)
      (increase (travel-time ?v) (travel-duration ?r))
      (increase (sim-time) (travel-duration ?r))
    )
  )

  (:action traverse-road-static-road_0428-road_0697
    :parameters (?v - vehicle ?r - road_type_road_0428 ?next - road_type_road_0697)
    :precondition (and
      (ready-road ?v ?r)
      (road-next ?r ?next)
      (road-open ?r)
      (static-road ?r)
      (not (road-blocked ?r))
    )
    :effect (and
      (not (ready-road ?v ?r))
      (ready-road ?v ?next)
      (increase (travel-time ?v) (travel-duration ?r))
      (increase (sim-time) (travel-duration ?r))
    )
  )

  (:action traverse-road-static-road_0428-road_0698
    :parameters (?v - vehicle ?r - road_type_road_0428 ?next - road_type_road_0698)
    :precondition (and
      (ready-road ?v ?r)
      (road-next ?r ?next)
      (road-open ?r)
      (static-road ?r)
      (not (road-blocked ?r))
    )
    :effect (and
      (not (ready-road ?v ?r))
      (ready-road ?v ?next)
      (increase (travel-time ?v) (travel-duration ?r))
      (increase (sim-time) (travel-duration ?r))
    )
  )

  (:action traverse-road-static-road_0430-road_0640
    :parameters (?v - vehicle ?r - road_type_road_0430 ?next - road_type_road_0640)
    :precondition (and
      (ready-road ?v ?r)
      (road-next ?r ?next)
      (road-open ?r)
      (static-road ?r)
      (not (road-blocked ?r))
    )
    :effect (and
      (not (ready-road ?v ?r))
      (ready-road ?v ?next)
      (increase (travel-time ?v) (travel-duration ?r))
      (increase (sim-time) (travel-duration ?r))
    )
  )

  (:action traverse-road-static-road_0430-road_0641
    :parameters (?v - vehicle ?r - road_type_road_0430 ?next - road_type_road_0641)
    :precondition (and
      (ready-road ?v ?r)
      (road-next ?r ?next)
      (road-open ?r)
      (static-road ?r)
      (not (road-blocked ?r))
    )
    :effect (and
      (not (ready-road ?v ?r))
      (ready-road ?v ?next)
      (increase (travel-time ?v) (travel-duration ?r))
      (increase (sim-time) (travel-duration ?r))
    )
  )

  (:action traverse-road-static-road_0433-road_0742
    :parameters (?v - vehicle ?r - road_type_road_0433 ?next - road_type_road_0742)
    :precondition (and
      (ready-road ?v ?r)
      (road-next ?r ?next)
      (road-open ?r)
      (static-road ?r)
      (not (road-blocked ?r))
    )
    :effect (and
      (not (ready-road ?v ?r))
      (ready-road ?v ?next)
      (increase (travel-time ?v) (travel-duration ?r))
      (increase (sim-time) (travel-duration ?r))
    )
  )

  (:action traverse-road-static-road_0433-road_0743
    :parameters (?v - vehicle ?r - road_type_road_0433 ?next - road_type_road_0743)
    :precondition (and
      (ready-road ?v ?r)
      (road-next ?r ?next)
      (road-open ?r)
      (static-road ?r)
      (not (road-blocked ?r))
    )
    :effect (and
      (not (ready-road ?v ?r))
      (ready-road ?v ?next)
      (increase (travel-time ?v) (travel-duration ?r))
      (increase (sim-time) (travel-duration ?r))
    )
  )

  (:action traverse-road-static-road_0434-road_0529
    :parameters (?v - vehicle ?r - road_type_road_0434 ?next - road_type_road_0529)
    :precondition (and
      (ready-road ?v ?r)
      (road-next ?r ?next)
      (road-open ?r)
      (static-road ?r)
      (not (road-blocked ?r))
    )
    :effect (and
      (not (ready-road ?v ?r))
      (ready-road ?v ?next)
      (increase (travel-time ?v) (travel-duration ?r))
      (increase (sim-time) (travel-duration ?r))
    )
  )

  (:action traverse-road-static-road_0436-road_0024
    :parameters (?v - vehicle ?r - road_type_road_0436 ?next - road_type_road_0024)
    :precondition (and
      (ready-road ?v ?r)
      (road-next ?r ?next)
      (road-open ?r)
      (static-road ?r)
      (not (road-blocked ?r))
    )
    :effect (and
      (not (ready-road ?v ?r))
      (ready-road ?v ?next)
      (increase (travel-time ?v) (travel-duration ?r))
      (increase (sim-time) (travel-duration ?r))
    )
  )

  (:action traverse-road-static-road_0436-road_0025
    :parameters (?v - vehicle ?r - road_type_road_0436 ?next - road_type_road_0025)
    :precondition (and
      (ready-road ?v ?r)
      (road-next ?r ?next)
      (road-open ?r)
      (static-road ?r)
      (not (road-blocked ?r))
    )
    :effect (and
      (not (ready-road ?v ?r))
      (ready-road ?v ?next)
      (increase (travel-time ?v) (travel-duration ?r))
      (increase (sim-time) (travel-duration ?r))
    )
  )

  (:action traverse-road-static-road_0436-road_0026
    :parameters (?v - vehicle ?r - road_type_road_0436 ?next - road_type_road_0026)
    :precondition (and
      (ready-road ?v ?r)
      (road-next ?r ?next)
      (road-open ?r)
      (static-road ?r)
      (not (road-blocked ?r))
    )
    :effect (and
      (not (ready-road ?v ?r))
      (ready-road ?v ?next)
      (increase (travel-time ?v) (travel-duration ?r))
      (increase (sim-time) (travel-duration ?r))
    )
  )

  (:action traverse-road-static-road_0437-road_0516
    :parameters (?v - vehicle ?r - road_type_road_0437 ?next - road_type_road_0516)
    :precondition (and
      (ready-road ?v ?r)
      (road-next ?r ?next)
      (road-open ?r)
      (static-road ?r)
      (not (road-blocked ?r))
    )
    :effect (and
      (not (ready-road ?v ?r))
      (ready-road ?v ?next)
      (increase (travel-time ?v) (travel-duration ?r))
      (increase (sim-time) (travel-duration ?r))
    )
  )

  (:action traverse-road-static-road_0437-road_0517
    :parameters (?v - vehicle ?r - road_type_road_0437 ?next - road_type_road_0517)
    :precondition (and
      (ready-road ?v ?r)
      (road-next ?r ?next)
      (road-open ?r)
      (static-road ?r)
      (not (road-blocked ?r))
    )
    :effect (and
      (not (ready-road ?v ?r))
      (ready-road ?v ?next)
      (increase (travel-time ?v) (travel-duration ?r))
      (increase (sim-time) (travel-duration ?r))
    )
  )

  (:action traverse-road-static-road_0438-road_0444
    :parameters (?v - vehicle ?r - road_type_road_0438 ?next - road_type_road_0444)
    :precondition (and
      (ready-road ?v ?r)
      (road-next ?r ?next)
      (road-open ?r)
      (static-road ?r)
      (not (road-blocked ?r))
    )
    :effect (and
      (not (ready-road ?v ?r))
      (ready-road ?v ?next)
      (increase (travel-time ?v) (travel-duration ?r))
      (increase (sim-time) (travel-duration ?r))
    )
  )

  (:action traverse-road-static-road_0438-road_0445
    :parameters (?v - vehicle ?r - road_type_road_0438 ?next - road_type_road_0445)
    :precondition (and
      (ready-road ?v ?r)
      (road-next ?r ?next)
      (road-open ?r)
      (static-road ?r)
      (not (road-blocked ?r))
    )
    :effect (and
      (not (ready-road ?v ?r))
      (ready-road ?v ?next)
      (increase (travel-time ?v) (travel-duration ?r))
      (increase (sim-time) (travel-duration ?r))
    )
  )

  (:action traverse-road-static-road_0438-road_0446
    :parameters (?v - vehicle ?r - road_type_road_0438 ?next - road_type_road_0446)
    :precondition (and
      (ready-road ?v ?r)
      (road-next ?r ?next)
      (road-open ?r)
      (static-road ?r)
      (not (road-blocked ?r))
    )
    :effect (and
      (not (ready-road ?v ?r))
      (ready-road ?v ?next)
      (increase (travel-time ?v) (travel-duration ?r))
      (increase (sim-time) (travel-duration ?r))
    )
  )

  (:action traverse-road-static-road_0439-road_0465
    :parameters (?v - vehicle ?r - road_type_road_0439 ?next - road_type_road_0465)
    :precondition (and
      (ready-road ?v ?r)
      (road-next ?r ?next)
      (road-open ?r)
      (static-road ?r)
      (not (road-blocked ?r))
    )
    :effect (and
      (not (ready-road ?v ?r))
      (ready-road ?v ?next)
      (increase (travel-time ?v) (travel-duration ?r))
      (increase (sim-time) (travel-duration ?r))
    )
  )

  (:action traverse-road-static-road_0440-road_0249
    :parameters (?v - vehicle ?r - road_type_road_0440 ?next - road_type_road_0249)
    :precondition (and
      (ready-road ?v ?r)
      (road-next ?r ?next)
      (road-open ?r)
      (static-road ?r)
      (not (road-blocked ?r))
    )
    :effect (and
      (not (ready-road ?v ?r))
      (ready-road ?v ?next)
      (increase (travel-time ?v) (travel-duration ?r))
      (increase (sim-time) (travel-duration ?r))
    )
  )

  (:action traverse-road-static-road_0440-road_0250
    :parameters (?v - vehicle ?r - road_type_road_0440 ?next - road_type_road_0250)
    :precondition (and
      (ready-road ?v ?r)
      (road-next ?r ?next)
      (road-open ?r)
      (static-road ?r)
      (not (road-blocked ?r))
    )
    :effect (and
      (not (ready-road ?v ?r))
      (ready-road ?v ?next)
      (increase (travel-time ?v) (travel-duration ?r))
      (increase (sim-time) (travel-duration ?r))
    )
  )

  (:action traverse-road-static-road_0440-road_0251
    :parameters (?v - vehicle ?r - road_type_road_0440 ?next - road_type_road_0251)
    :precondition (and
      (ready-road ?v ?r)
      (road-next ?r ?next)
      (road-open ?r)
      (static-road ?r)
      (not (road-blocked ?r))
    )
    :effect (and
      (not (ready-road ?v ?r))
      (ready-road ?v ?next)
      (increase (travel-time ?v) (travel-duration ?r))
      (increase (sim-time) (travel-duration ?r))
    )
  )

  (:action traverse-road-static-road_0441-road_0258
    :parameters (?v - vehicle ?r - road_type_road_0441 ?next - road_type_road_0258)
    :precondition (and
      (ready-road ?v ?r)
      (road-next ?r ?next)
      (road-open ?r)
      (static-road ?r)
      (not (road-blocked ?r))
    )
    :effect (and
      (not (ready-road ?v ?r))
      (ready-road ?v ?next)
      (increase (travel-time ?v) (travel-duration ?r))
      (increase (sim-time) (travel-duration ?r))
    )
  )

  (:action traverse-road-static-road_0441-road_0259
    :parameters (?v - vehicle ?r - road_type_road_0441 ?next - road_type_road_0259)
    :precondition (and
      (ready-road ?v ?r)
      (road-next ?r ?next)
      (road-open ?r)
      (static-road ?r)
      (not (road-blocked ?r))
    )
    :effect (and
      (not (ready-road ?v ?r))
      (ready-road ?v ?next)
      (increase (travel-time ?v) (travel-duration ?r))
      (increase (sim-time) (travel-duration ?r))
    )
  )

  (:action traverse-road-static-road_0442-road_0443
    :parameters (?v - vehicle ?r - road_type_road_0442 ?next - road_type_road_0443)
    :precondition (and
      (ready-road ?v ?r)
      (road-next ?r ?next)
      (road-open ?r)
      (static-road ?r)
      (not (road-blocked ?r))
    )
    :effect (and
      (not (ready-road ?v ?r))
      (ready-road ?v ?next)
      (increase (travel-time ?v) (travel-duration ?r))
      (increase (sim-time) (travel-duration ?r))
    )
  )

  (:action traverse-road-static-road_0443-road_0315
    :parameters (?v - vehicle ?r - road_type_road_0443 ?next - road_type_road_0315)
    :precondition (and
      (ready-road ?v ?r)
      (road-next ?r ?next)
      (road-open ?r)
      (static-road ?r)
      (not (road-blocked ?r))
    )
    :effect (and
      (not (ready-road ?v ?r))
      (ready-road ?v ?next)
      (increase (travel-time ?v) (travel-duration ?r))
      (increase (sim-time) (travel-duration ?r))
    )
  )

  (:action traverse-road-static-road_0443-road_0316
    :parameters (?v - vehicle ?r - road_type_road_0443 ?next - road_type_road_0316)
    :precondition (and
      (ready-road ?v ?r)
      (road-next ?r ?next)
      (road-open ?r)
      (static-road ?r)
      (not (road-blocked ?r))
    )
    :effect (and
      (not (ready-road ?v ?r))
      (ready-road ?v ?next)
      (increase (travel-time ?v) (travel-duration ?r))
      (increase (sim-time) (travel-duration ?r))
    )
  )

  (:action traverse-road-static-road_0444-road_0034
    :parameters (?v - vehicle ?r - road_type_road_0444 ?next - road_type_road_0034)
    :precondition (and
      (ready-road ?v ?r)
      (road-next ?r ?next)
      (road-open ?r)
      (static-road ?r)
      (not (road-blocked ?r))
    )
    :effect (and
      (not (ready-road ?v ?r))
      (ready-road ?v ?next)
      (increase (travel-time ?v) (travel-duration ?r))
      (increase (sim-time) (travel-duration ?r))
    )
  )

  (:action traverse-road-static-road_0444-road_0035
    :parameters (?v - vehicle ?r - road_type_road_0444 ?next - road_type_road_0035)
    :precondition (and
      (ready-road ?v ?r)
      (road-next ?r ?next)
      (road-open ?r)
      (static-road ?r)
      (not (road-blocked ?r))
    )
    :effect (and
      (not (ready-road ?v ?r))
      (ready-road ?v ?next)
      (increase (travel-time ?v) (travel-duration ?r))
      (increase (sim-time) (travel-duration ?r))
    )
  )

  (:action traverse-road-static-road_0444-road_0036
    :parameters (?v - vehicle ?r - road_type_road_0444 ?next - road_type_road_0036)
    :precondition (and
      (ready-road ?v ?r)
      (road-next ?r ?next)
      (road-open ?r)
      (static-road ?r)
      (not (road-blocked ?r))
    )
    :effect (and
      (not (ready-road ?v ?r))
      (ready-road ?v ?next)
      (increase (travel-time ?v) (travel-duration ?r))
      (increase (sim-time) (travel-duration ?r))
    )
  )

  (:action traverse-road-static-road_0445-road_0438
    :parameters (?v - vehicle ?r - road_type_road_0445 ?next - road_type_road_0438)
    :precondition (and
      (ready-road ?v ?r)
      (road-next ?r ?next)
      (road-open ?r)
      (static-road ?r)
      (not (road-blocked ?r))
    )
    :effect (and
      (not (ready-road ?v ?r))
      (ready-road ?v ?next)
      (increase (travel-time ?v) (travel-duration ?r))
      (increase (sim-time) (travel-duration ?r))
    )
  )

  (:action traverse-road-static-road_0445-road_0439
    :parameters (?v - vehicle ?r - road_type_road_0445 ?next - road_type_road_0439)
    :precondition (and
      (ready-road ?v ?r)
      (road-next ?r ?next)
      (road-open ?r)
      (static-road ?r)
      (not (road-blocked ?r))
    )
    :effect (and
      (not (ready-road ?v ?r))
      (ready-road ?v ?next)
      (increase (travel-time ?v) (travel-duration ?r))
      (increase (sim-time) (travel-duration ?r))
    )
  )

  (:action traverse-road-static-road_0445-road_0440
    :parameters (?v - vehicle ?r - road_type_road_0445 ?next - road_type_road_0440)
    :precondition (and
      (ready-road ?v ?r)
      (road-next ?r ?next)
      (road-open ?r)
      (static-road ?r)
      (not (road-blocked ?r))
    )
    :effect (and
      (not (ready-road ?v ?r))
      (ready-road ?v ?next)
      (increase (travel-time ?v) (travel-duration ?r))
      (increase (sim-time) (travel-duration ?r))
    )
  )

  (:action traverse-road-static-road_0446-road_0739
    :parameters (?v - vehicle ?r - road_type_road_0446 ?next - road_type_road_0739)
    :precondition (and
      (ready-road ?v ?r)
      (road-next ?r ?next)
      (road-open ?r)
      (static-road ?r)
      (not (road-blocked ?r))
    )
    :effect (and
      (not (ready-road ?v ?r))
      (ready-road ?v ?next)
      (increase (travel-time ?v) (travel-duration ?r))
      (increase (sim-time) (travel-duration ?r))
    )
  )

  (:action traverse-road-static-road_0447-road_0267
    :parameters (?v - vehicle ?r - road_type_road_0447 ?next - road_type_road_0267)
    :precondition (and
      (ready-road ?v ?r)
      (road-next ?r ?next)
      (road-open ?r)
      (static-road ?r)
      (not (road-blocked ?r))
    )
    :effect (and
      (not (ready-road ?v ?r))
      (ready-road ?v ?next)
      (increase (travel-time ?v) (travel-duration ?r))
      (increase (sim-time) (travel-duration ?r))
    )
  )

  (:action traverse-road-static-road_0448-road_0197
    :parameters (?v - vehicle ?r - road_type_road_0448 ?next - road_type_road_0197)
    :precondition (and
      (ready-road ?v ?r)
      (road-next ?r ?next)
      (road-open ?r)
      (static-road ?r)
      (not (road-blocked ?r))
    )
    :effect (and
      (not (ready-road ?v ?r))
      (ready-road ?v ?next)
      (increase (travel-time ?v) (travel-duration ?r))
      (increase (sim-time) (travel-duration ?r))
    )
  )

  (:action traverse-road-static-road_0448-road_0198
    :parameters (?v - vehicle ?r - road_type_road_0448 ?next - road_type_road_0198)
    :precondition (and
      (ready-road ?v ?r)
      (road-next ?r ?next)
      (road-open ?r)
      (static-road ?r)
      (not (road-blocked ?r))
    )
    :effect (and
      (not (ready-road ?v ?r))
      (ready-road ?v ?next)
      (increase (travel-time ?v) (travel-duration ?r))
      (increase (sim-time) (travel-duration ?r))
    )
  )

  (:action traverse-road-static-road_0448-road_0199
    :parameters (?v - vehicle ?r - road_type_road_0448 ?next - road_type_road_0199)
    :precondition (and
      (ready-road ?v ?r)
      (road-next ?r ?next)
      (road-open ?r)
      (static-road ?r)
      (not (road-blocked ?r))
    )
    :effect (and
      (not (ready-road ?v ?r))
      (ready-road ?v ?next)
      (increase (travel-time ?v) (travel-duration ?r))
      (increase (sim-time) (travel-duration ?r))
    )
  )

  (:action traverse-road-static-road_0449-macro_0035
    :parameters (?v - vehicle ?r - road_type_road_0449 ?next - road_type_macro_0035)
    :precondition (and
      (ready-road ?v ?r)
      (road-next ?r ?next)
      (road-open ?r)
      (static-road ?r)
      (not (road-blocked ?r))
    )
    :effect (and
      (not (ready-road ?v ?r))
      (ready-road ?v ?next)
      (increase (travel-time ?v) (travel-duration ?r))
      (increase (sim-time) (travel-duration ?r))
    )
  )

  (:action traverse-road-static-road_0449-road_0475
    :parameters (?v - vehicle ?r - road_type_road_0449 ?next - road_type_road_0475)
    :precondition (and
      (ready-road ?v ?r)
      (road-next ?r ?next)
      (road-open ?r)
      (static-road ?r)
      (not (road-blocked ?r))
    )
    :effect (and
      (not (ready-road ?v ?r))
      (ready-road ?v ?next)
      (increase (travel-time ?v) (travel-duration ?r))
      (increase (sim-time) (travel-duration ?r))
    )
  )

  (:action traverse-road-dynamic-road_0450-road_0801-tw_00000
    :parameters (?v - vehicle ?r - road_type_road_0450 ?next - road_type_road_0801 ?w - window_type_tw_00000)
    :precondition (and
      (ready-road ?v ?r)
      (road-next ?r ?next)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
    )
    :effect (and
      (not (ready-road ?v ?r))
      (ready-road ?v ?next)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-road_0450-road_0801-tw_00030
    :parameters (?v - vehicle ?r - road_type_road_0450 ?next - road_type_road_0801 ?w - window_type_tw_00030)
    :precondition (and
      (ready-road ?v ?r)
      (road-next ?r ?next)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
    )
    :effect (and
      (not (ready-road ?v ?r))
      (ready-road ?v ?next)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-road_0450-road_0801-tw_00060
    :parameters (?v - vehicle ?r - road_type_road_0450 ?next - road_type_road_0801 ?w - window_type_tw_00060)
    :precondition (and
      (ready-road ?v ?r)
      (road-next ?r ?next)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
    )
    :effect (and
      (not (ready-road ?v ?r))
      (ready-road ?v ?next)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-road_0450-road_0801-tw_00090
    :parameters (?v - vehicle ?r - road_type_road_0450 ?next - road_type_road_0801 ?w - window_type_tw_00090)
    :precondition (and
      (ready-road ?v ?r)
      (road-next ?r ?next)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
    )
    :effect (and
      (not (ready-road ?v ?r))
      (ready-road ?v ?next)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-road_0450-road_0801-tw_00120
    :parameters (?v - vehicle ?r - road_type_road_0450 ?next - road_type_road_0801 ?w - window_type_tw_00120)
    :precondition (and
      (ready-road ?v ?r)
      (road-next ?r ?next)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
    )
    :effect (and
      (not (ready-road ?v ?r))
      (ready-road ?v ?next)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-road_0450-road_0801-tw_00150
    :parameters (?v - vehicle ?r - road_type_road_0450 ?next - road_type_road_0801 ?w - window_type_tw_00150)
    :precondition (and
      (ready-road ?v ?r)
      (road-next ?r ?next)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
    )
    :effect (and
      (not (ready-road ?v ?r))
      (ready-road ?v ?next)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-road_0450-road_0801-tw_00180
    :parameters (?v - vehicle ?r - road_type_road_0450 ?next - road_type_road_0801 ?w - window_type_tw_00180)
    :precondition (and
      (ready-road ?v ?r)
      (road-next ?r ?next)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
    )
    :effect (and
      (not (ready-road ?v ?r))
      (ready-road ?v ?next)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-road_0450-road_0801-tw_00210
    :parameters (?v - vehicle ?r - road_type_road_0450 ?next - road_type_road_0801 ?w - window_type_tw_00210)
    :precondition (and
      (ready-road ?v ?r)
      (road-next ?r ?next)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
    )
    :effect (and
      (not (ready-road ?v ?r))
      (ready-road ?v ?next)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-road_0450-road_0801-tw_00240
    :parameters (?v - vehicle ?r - road_type_road_0450 ?next - road_type_road_0801 ?w - window_type_tw_00240)
    :precondition (and
      (ready-road ?v ?r)
      (road-next ?r ?next)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
    )
    :effect (and
      (not (ready-road ?v ?r))
      (ready-road ?v ?next)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-road_0450-road_0801-tw_00270
    :parameters (?v - vehicle ?r - road_type_road_0450 ?next - road_type_road_0801 ?w - window_type_tw_00270)
    :precondition (and
      (ready-road ?v ?r)
      (road-next ?r ?next)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
    )
    :effect (and
      (not (ready-road ?v ?r))
      (ready-road ?v ?next)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-road_0450-road_0801-tw_00300
    :parameters (?v - vehicle ?r - road_type_road_0450 ?next - road_type_road_0801 ?w - window_type_tw_00300)
    :precondition (and
      (ready-road ?v ?r)
      (road-next ?r ?next)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
    )
    :effect (and
      (not (ready-road ?v ?r))
      (ready-road ?v ?next)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-road_0450-road_0801-tw_00330
    :parameters (?v - vehicle ?r - road_type_road_0450 ?next - road_type_road_0801 ?w - window_type_tw_00330)
    :precondition (and
      (ready-road ?v ?r)
      (road-next ?r ?next)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
    )
    :effect (and
      (not (ready-road ?v ?r))
      (ready-road ?v ?next)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-road_0450-road_0801-tw_00360
    :parameters (?v - vehicle ?r - road_type_road_0450 ?next - road_type_road_0801 ?w - window_type_tw_00360)
    :precondition (and
      (ready-road ?v ?r)
      (road-next ?r ?next)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
    )
    :effect (and
      (not (ready-road ?v ?r))
      (ready-road ?v ?next)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-road_0450-road_0801-tw_00390
    :parameters (?v - vehicle ?r - road_type_road_0450 ?next - road_type_road_0801 ?w - window_type_tw_00390)
    :precondition (and
      (ready-road ?v ?r)
      (road-next ?r ?next)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
    )
    :effect (and
      (not (ready-road ?v ?r))
      (ready-road ?v ?next)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-road_0450-road_0801-tw_00420
    :parameters (?v - vehicle ?r - road_type_road_0450 ?next - road_type_road_0801 ?w - window_type_tw_00420)
    :precondition (and
      (ready-road ?v ?r)
      (road-next ?r ?next)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
    )
    :effect (and
      (not (ready-road ?v ?r))
      (ready-road ?v ?next)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-road_0450-road_0801-tw_00450
    :parameters (?v - vehicle ?r - road_type_road_0450 ?next - road_type_road_0801 ?w - window_type_tw_00450)
    :precondition (and
      (ready-road ?v ?r)
      (road-next ?r ?next)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
    )
    :effect (and
      (not (ready-road ?v ?r))
      (ready-road ?v ?next)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-road_0450-road_0801-tw_00480
    :parameters (?v - vehicle ?r - road_type_road_0450 ?next - road_type_road_0801 ?w - window_type_tw_00480)
    :precondition (and
      (ready-road ?v ?r)
      (road-next ?r ?next)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
    )
    :effect (and
      (not (ready-road ?v ?r))
      (ready-road ?v ?next)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-road_0450-road_0801-tw_00510
    :parameters (?v - vehicle ?r - road_type_road_0450 ?next - road_type_road_0801 ?w - window_type_tw_00510)
    :precondition (and
      (ready-road ?v ?r)
      (road-next ?r ?next)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
    )
    :effect (and
      (not (ready-road ?v ?r))
      (ready-road ?v ?next)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-road_0450-road_0801-tw_00540
    :parameters (?v - vehicle ?r - road_type_road_0450 ?next - road_type_road_0801 ?w - window_type_tw_00540)
    :precondition (and
      (ready-road ?v ?r)
      (road-next ?r ?next)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
    )
    :effect (and
      (not (ready-road ?v ?r))
      (ready-road ?v ?next)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-road_0450-road_0801-tw_00570
    :parameters (?v - vehicle ?r - road_type_road_0450 ?next - road_type_road_0801 ?w - window_type_tw_00570)
    :precondition (and
      (ready-road ?v ?r)
      (road-next ?r ?next)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
    )
    :effect (and
      (not (ready-road ?v ?r))
      (ready-road ?v ?next)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-road_0450-road_0801-tw_00600
    :parameters (?v - vehicle ?r - road_type_road_0450 ?next - road_type_road_0801 ?w - window_type_tw_00600)
    :precondition (and
      (ready-road ?v ?r)
      (road-next ?r ?next)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
    )
    :effect (and
      (not (ready-road ?v ?r))
      (ready-road ?v ?next)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-road_0450-road_0802-tw_00000
    :parameters (?v - vehicle ?r - road_type_road_0450 ?next - road_type_road_0802 ?w - window_type_tw_00000)
    :precondition (and
      (ready-road ?v ?r)
      (road-next ?r ?next)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
    )
    :effect (and
      (not (ready-road ?v ?r))
      (ready-road ?v ?next)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-road_0450-road_0802-tw_00030
    :parameters (?v - vehicle ?r - road_type_road_0450 ?next - road_type_road_0802 ?w - window_type_tw_00030)
    :precondition (and
      (ready-road ?v ?r)
      (road-next ?r ?next)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
    )
    :effect (and
      (not (ready-road ?v ?r))
      (ready-road ?v ?next)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-road_0450-road_0802-tw_00060
    :parameters (?v - vehicle ?r - road_type_road_0450 ?next - road_type_road_0802 ?w - window_type_tw_00060)
    :precondition (and
      (ready-road ?v ?r)
      (road-next ?r ?next)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
    )
    :effect (and
      (not (ready-road ?v ?r))
      (ready-road ?v ?next)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-road_0450-road_0802-tw_00090
    :parameters (?v - vehicle ?r - road_type_road_0450 ?next - road_type_road_0802 ?w - window_type_tw_00090)
    :precondition (and
      (ready-road ?v ?r)
      (road-next ?r ?next)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
    )
    :effect (and
      (not (ready-road ?v ?r))
      (ready-road ?v ?next)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-road_0450-road_0802-tw_00120
    :parameters (?v - vehicle ?r - road_type_road_0450 ?next - road_type_road_0802 ?w - window_type_tw_00120)
    :precondition (and
      (ready-road ?v ?r)
      (road-next ?r ?next)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
    )
    :effect (and
      (not (ready-road ?v ?r))
      (ready-road ?v ?next)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-road_0450-road_0802-tw_00150
    :parameters (?v - vehicle ?r - road_type_road_0450 ?next - road_type_road_0802 ?w - window_type_tw_00150)
    :precondition (and
      (ready-road ?v ?r)
      (road-next ?r ?next)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
    )
    :effect (and
      (not (ready-road ?v ?r))
      (ready-road ?v ?next)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-road_0450-road_0802-tw_00180
    :parameters (?v - vehicle ?r - road_type_road_0450 ?next - road_type_road_0802 ?w - window_type_tw_00180)
    :precondition (and
      (ready-road ?v ?r)
      (road-next ?r ?next)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
    )
    :effect (and
      (not (ready-road ?v ?r))
      (ready-road ?v ?next)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-road_0450-road_0802-tw_00210
    :parameters (?v - vehicle ?r - road_type_road_0450 ?next - road_type_road_0802 ?w - window_type_tw_00210)
    :precondition (and
      (ready-road ?v ?r)
      (road-next ?r ?next)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
    )
    :effect (and
      (not (ready-road ?v ?r))
      (ready-road ?v ?next)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-road_0450-road_0802-tw_00240
    :parameters (?v - vehicle ?r - road_type_road_0450 ?next - road_type_road_0802 ?w - window_type_tw_00240)
    :precondition (and
      (ready-road ?v ?r)
      (road-next ?r ?next)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
    )
    :effect (and
      (not (ready-road ?v ?r))
      (ready-road ?v ?next)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-road_0450-road_0802-tw_00270
    :parameters (?v - vehicle ?r - road_type_road_0450 ?next - road_type_road_0802 ?w - window_type_tw_00270)
    :precondition (and
      (ready-road ?v ?r)
      (road-next ?r ?next)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
    )
    :effect (and
      (not (ready-road ?v ?r))
      (ready-road ?v ?next)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-road_0450-road_0802-tw_00300
    :parameters (?v - vehicle ?r - road_type_road_0450 ?next - road_type_road_0802 ?w - window_type_tw_00300)
    :precondition (and
      (ready-road ?v ?r)
      (road-next ?r ?next)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
    )
    :effect (and
      (not (ready-road ?v ?r))
      (ready-road ?v ?next)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-road_0450-road_0802-tw_00330
    :parameters (?v - vehicle ?r - road_type_road_0450 ?next - road_type_road_0802 ?w - window_type_tw_00330)
    :precondition (and
      (ready-road ?v ?r)
      (road-next ?r ?next)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
    )
    :effect (and
      (not (ready-road ?v ?r))
      (ready-road ?v ?next)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-road_0450-road_0802-tw_00360
    :parameters (?v - vehicle ?r - road_type_road_0450 ?next - road_type_road_0802 ?w - window_type_tw_00360)
    :precondition (and
      (ready-road ?v ?r)
      (road-next ?r ?next)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
    )
    :effect (and
      (not (ready-road ?v ?r))
      (ready-road ?v ?next)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-road_0450-road_0802-tw_00390
    :parameters (?v - vehicle ?r - road_type_road_0450 ?next - road_type_road_0802 ?w - window_type_tw_00390)
    :precondition (and
      (ready-road ?v ?r)
      (road-next ?r ?next)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
    )
    :effect (and
      (not (ready-road ?v ?r))
      (ready-road ?v ?next)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-road_0450-road_0802-tw_00420
    :parameters (?v - vehicle ?r - road_type_road_0450 ?next - road_type_road_0802 ?w - window_type_tw_00420)
    :precondition (and
      (ready-road ?v ?r)
      (road-next ?r ?next)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
    )
    :effect (and
      (not (ready-road ?v ?r))
      (ready-road ?v ?next)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-road_0450-road_0802-tw_00450
    :parameters (?v - vehicle ?r - road_type_road_0450 ?next - road_type_road_0802 ?w - window_type_tw_00450)
    :precondition (and
      (ready-road ?v ?r)
      (road-next ?r ?next)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
    )
    :effect (and
      (not (ready-road ?v ?r))
      (ready-road ?v ?next)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-road_0450-road_0802-tw_00480
    :parameters (?v - vehicle ?r - road_type_road_0450 ?next - road_type_road_0802 ?w - window_type_tw_00480)
    :precondition (and
      (ready-road ?v ?r)
      (road-next ?r ?next)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
    )
    :effect (and
      (not (ready-road ?v ?r))
      (ready-road ?v ?next)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-road_0450-road_0802-tw_00510
    :parameters (?v - vehicle ?r - road_type_road_0450 ?next - road_type_road_0802 ?w - window_type_tw_00510)
    :precondition (and
      (ready-road ?v ?r)
      (road-next ?r ?next)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
    )
    :effect (and
      (not (ready-road ?v ?r))
      (ready-road ?v ?next)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-road_0450-road_0802-tw_00540
    :parameters (?v - vehicle ?r - road_type_road_0450 ?next - road_type_road_0802 ?w - window_type_tw_00540)
    :precondition (and
      (ready-road ?v ?r)
      (road-next ?r ?next)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
    )
    :effect (and
      (not (ready-road ?v ?r))
      (ready-road ?v ?next)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-road_0450-road_0802-tw_00570
    :parameters (?v - vehicle ?r - road_type_road_0450 ?next - road_type_road_0802 ?w - window_type_tw_00570)
    :precondition (and
      (ready-road ?v ?r)
      (road-next ?r ?next)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
    )
    :effect (and
      (not (ready-road ?v ?r))
      (ready-road ?v ?next)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-road_0450-road_0802-tw_00600
    :parameters (?v - vehicle ?r - road_type_road_0450 ?next - road_type_road_0802 ?w - window_type_tw_00600)
    :precondition (and
      (ready-road ?v ?r)
      (road-next ?r ?next)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
    )
    :effect (and
      (not (ready-road ?v ?r))
      (ready-road ?v ?next)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-static-road_0451-road_0479
    :parameters (?v - vehicle ?r - road_type_road_0451 ?next - road_type_road_0479)
    :precondition (and
      (ready-road ?v ?r)
      (road-next ?r ?next)
      (road-open ?r)
      (static-road ?r)
      (not (road-blocked ?r))
    )
    :effect (and
      (not (ready-road ?v ?r))
      (ready-road ?v ?next)
      (increase (travel-time ?v) (travel-duration ?r))
      (increase (sim-time) (travel-duration ?r))
    )
  )

  (:action traverse-road-static-road_0452-road_0171
    :parameters (?v - vehicle ?r - road_type_road_0452 ?next - road_type_road_0171)
    :precondition (and
      (ready-road ?v ?r)
      (road-next ?r ?next)
      (road-open ?r)
      (static-road ?r)
      (not (road-blocked ?r))
    )
    :effect (and
      (not (ready-road ?v ?r))
      (ready-road ?v ?next)
      (increase (travel-time ?v) (travel-duration ?r))
      (increase (sim-time) (travel-duration ?r))
    )
  )

  (:action traverse-road-static-road_0453-road_0089
    :parameters (?v - vehicle ?r - road_type_road_0453 ?next - road_type_road_0089)
    :precondition (and
      (ready-road ?v ?r)
      (road-next ?r ?next)
      (road-open ?r)
      (static-road ?r)
      (not (road-blocked ?r))
    )
    :effect (and
      (not (ready-road ?v ?r))
      (ready-road ?v ?next)
      (increase (travel-time ?v) (travel-duration ?r))
      (increase (sim-time) (travel-duration ?r))
    )
  )

  (:action traverse-road-static-road_0453-road_0090
    :parameters (?v - vehicle ?r - road_type_road_0453 ?next - road_type_road_0090)
    :precondition (and
      (ready-road ?v ?r)
      (road-next ?r ?next)
      (road-open ?r)
      (static-road ?r)
      (not (road-blocked ?r))
    )
    :effect (and
      (not (ready-road ?v ?r))
      (ready-road ?v ?next)
      (increase (travel-time ?v) (travel-duration ?r))
      (increase (sim-time) (travel-duration ?r))
    )
  )

  (:action traverse-road-static-road_0453-road_0091
    :parameters (?v - vehicle ?r - road_type_road_0453 ?next - road_type_road_0091)
    :precondition (and
      (ready-road ?v ?r)
      (road-next ?r ?next)
      (road-open ?r)
      (static-road ?r)
      (not (road-blocked ?r))
    )
    :effect (and
      (not (ready-road ?v ?r))
      (ready-road ?v ?next)
      (increase (travel-time ?v) (travel-duration ?r))
      (increase (sim-time) (travel-duration ?r))
    )
  )

  (:action traverse-road-static-road_0454-road_0471
    :parameters (?v - vehicle ?r - road_type_road_0454 ?next - road_type_road_0471)
    :precondition (and
      (ready-road ?v ?r)
      (road-next ?r ?next)
      (road-open ?r)
      (static-road ?r)
      (not (road-blocked ?r))
    )
    :effect (and
      (not (ready-road ?v ?r))
      (ready-road ?v ?next)
      (increase (travel-time ?v) (travel-duration ?r))
      (increase (sim-time) (travel-duration ?r))
    )
  )

  (:action traverse-road-static-road_0454-road_0472
    :parameters (?v - vehicle ?r - road_type_road_0454 ?next - road_type_road_0472)
    :precondition (and
      (ready-road ?v ?r)
      (road-next ?r ?next)
      (road-open ?r)
      (static-road ?r)
      (not (road-blocked ?r))
    )
    :effect (and
      (not (ready-road ?v ?r))
      (ready-road ?v ?next)
      (increase (travel-time ?v) (travel-duration ?r))
      (increase (sim-time) (travel-duration ?r))
    )
  )

  (:action traverse-road-static-road_0454-road_0473
    :parameters (?v - vehicle ?r - road_type_road_0454 ?next - road_type_road_0473)
    :precondition (and
      (ready-road ?v ?r)
      (road-next ?r ?next)
      (road-open ?r)
      (static-road ?r)
      (not (road-blocked ?r))
    )
    :effect (and
      (not (ready-road ?v ?r))
      (ready-road ?v ?next)
      (increase (travel-time ?v) (travel-duration ?r))
      (increase (sim-time) (travel-duration ?r))
    )
  )

  (:action traverse-road-static-road_0455-macro_0001
    :parameters (?v - vehicle ?r - road_type_road_0455 ?next - road_type_macro_0001)
    :precondition (and
      (ready-road ?v ?r)
      (road-next ?r ?next)
      (road-open ?r)
      (static-road ?r)
      (not (road-blocked ?r))
    )
    :effect (and
      (not (ready-road ?v ?r))
      (ready-road ?v ?next)
      (increase (travel-time ?v) (travel-duration ?r))
      (increase (sim-time) (travel-duration ?r))
    )
  )

  (:action traverse-road-static-road_0455-road_0039
    :parameters (?v - vehicle ?r - road_type_road_0455 ?next - road_type_road_0039)
    :precondition (and
      (ready-road ?v ?r)
      (road-next ?r ?next)
      (road-open ?r)
      (static-road ?r)
      (not (road-blocked ?r))
    )
    :effect (and
      (not (ready-road ?v ?r))
      (ready-road ?v ?next)
      (increase (travel-time ?v) (travel-duration ?r))
      (increase (sim-time) (travel-duration ?r))
    )
  )

  (:action traverse-road-static-road_0456-road_0614
    :parameters (?v - vehicle ?r - road_type_road_0456 ?next - road_type_road_0614)
    :precondition (and
      (ready-road ?v ?r)
      (road-next ?r ?next)
      (road-open ?r)
      (static-road ?r)
      (not (road-blocked ?r))
    )
    :effect (and
      (not (ready-road ?v ?r))
      (ready-road ?v ?next)
      (increase (travel-time ?v) (travel-duration ?r))
      (increase (sim-time) (travel-duration ?r))
    )
  )

  (:action traverse-road-static-road_0457-road_0722
    :parameters (?v - vehicle ?r - road_type_road_0457 ?next - road_type_road_0722)
    :precondition (and
      (ready-road ?v ?r)
      (road-next ?r ?next)
      (road-open ?r)
      (static-road ?r)
      (not (road-blocked ?r))
    )
    :effect (and
      (not (ready-road ?v ?r))
      (ready-road ?v ?next)
      (increase (travel-time ?v) (travel-duration ?r))
      (increase (sim-time) (travel-duration ?r))
    )
  )

  (:action traverse-road-static-road_0458-road_0568
    :parameters (?v - vehicle ?r - road_type_road_0458 ?next - road_type_road_0568)
    :precondition (and
      (ready-road ?v ?r)
      (road-next ?r ?next)
      (road-open ?r)
      (static-road ?r)
      (not (road-blocked ?r))
    )
    :effect (and
      (not (ready-road ?v ?r))
      (ready-road ?v ?next)
      (increase (travel-time ?v) (travel-duration ?r))
      (increase (sim-time) (travel-duration ?r))
    )
  )

  (:action traverse-road-static-road_0458-road_0569
    :parameters (?v - vehicle ?r - road_type_road_0458 ?next - road_type_road_0569)
    :precondition (and
      (ready-road ?v ?r)
      (road-next ?r ?next)
      (road-open ?r)
      (static-road ?r)
      (not (road-blocked ?r))
    )
    :effect (and
      (not (ready-road ?v ?r))
      (ready-road ?v ?next)
      (increase (travel-time ?v) (travel-duration ?r))
      (increase (sim-time) (travel-duration ?r))
    )
  )

  (:action traverse-road-static-road_0459-road_0083
    :parameters (?v - vehicle ?r - road_type_road_0459 ?next - road_type_road_0083)
    :precondition (and
      (ready-road ?v ?r)
      (road-next ?r ?next)
      (road-open ?r)
      (static-road ?r)
      (not (road-blocked ?r))
    )
    :effect (and
      (not (ready-road ?v ?r))
      (ready-road ?v ?next)
      (increase (travel-time ?v) (travel-duration ?r))
      (increase (sim-time) (travel-duration ?r))
    )
  )

  (:action traverse-road-static-road_0459-road_0084
    :parameters (?v - vehicle ?r - road_type_road_0459 ?next - road_type_road_0084)
    :precondition (and
      (ready-road ?v ?r)
      (road-next ?r ?next)
      (road-open ?r)
      (static-road ?r)
      (not (road-blocked ?r))
    )
    :effect (and
      (not (ready-road ?v ?r))
      (ready-road ?v ?next)
      (increase (travel-time ?v) (travel-duration ?r))
      (increase (sim-time) (travel-duration ?r))
    )
  )

  (:action traverse-road-static-road_0460-road_0379
    :parameters (?v - vehicle ?r - road_type_road_0460 ?next - road_type_road_0379)
    :precondition (and
      (ready-road ?v ?r)
      (road-next ?r ?next)
      (road-open ?r)
      (static-road ?r)
      (not (road-blocked ?r))
    )
    :effect (and
      (not (ready-road ?v ?r))
      (ready-road ?v ?next)
      (increase (travel-time ?v) (travel-duration ?r))
      (increase (sim-time) (travel-duration ?r))
    )
  )

  (:action traverse-road-static-road_0460-road_0380
    :parameters (?v - vehicle ?r - road_type_road_0460 ?next - road_type_road_0380)
    :precondition (and
      (ready-road ?v ?r)
      (road-next ?r ?next)
      (road-open ?r)
      (static-road ?r)
      (not (road-blocked ?r))
    )
    :effect (and
      (not (ready-road ?v ?r))
      (ready-road ?v ?next)
      (increase (travel-time ?v) (travel-duration ?r))
      (increase (sim-time) (travel-duration ?r))
    )
  )

  (:action traverse-road-static-road_0461-road_0626
    :parameters (?v - vehicle ?r - road_type_road_0461 ?next - road_type_road_0626)
    :precondition (and
      (ready-road ?v ?r)
      (road-next ?r ?next)
      (road-open ?r)
      (static-road ?r)
      (not (road-blocked ?r))
    )
    :effect (and
      (not (ready-road ?v ?r))
      (ready-road ?v ?next)
      (increase (travel-time ?v) (travel-duration ?r))
      (increase (sim-time) (travel-duration ?r))
    )
  )

  (:action traverse-road-static-road_0462-road_0658
    :parameters (?v - vehicle ?r - road_type_road_0462 ?next - road_type_road_0658)
    :precondition (and
      (ready-road ?v ?r)
      (road-next ?r ?next)
      (road-open ?r)
      (static-road ?r)
      (not (road-blocked ?r))
    )
    :effect (and
      (not (ready-road ?v ?r))
      (ready-road ?v ?next)
      (increase (travel-time ?v) (travel-duration ?r))
      (increase (sim-time) (travel-duration ?r))
    )
  )

  (:action traverse-road-static-road_0462-road_0659
    :parameters (?v - vehicle ?r - road_type_road_0462 ?next - road_type_road_0659)
    :precondition (and
      (ready-road ?v ?r)
      (road-next ?r ?next)
      (road-open ?r)
      (static-road ?r)
      (not (road-blocked ?r))
    )
    :effect (and
      (not (ready-road ?v ?r))
      (ready-road ?v ?next)
      (increase (travel-time ?v) (travel-duration ?r))
      (increase (sim-time) (travel-duration ?r))
    )
  )

  (:action traverse-road-static-road_0463-road_0466
    :parameters (?v - vehicle ?r - road_type_road_0463 ?next - road_type_road_0466)
    :precondition (and
      (ready-road ?v ?r)
      (road-next ?r ?next)
      (road-open ?r)
      (static-road ?r)
      (not (road-blocked ?r))
    )
    :effect (and
      (not (ready-road ?v ?r))
      (ready-road ?v ?next)
      (increase (travel-time ?v) (travel-duration ?r))
      (increase (sim-time) (travel-duration ?r))
    )
  )

  (:action traverse-road-static-road_0463-road_0467
    :parameters (?v - vehicle ?r - road_type_road_0463 ?next - road_type_road_0467)
    :precondition (and
      (ready-road ?v ?r)
      (road-next ?r ?next)
      (road-open ?r)
      (static-road ?r)
      (not (road-blocked ?r))
    )
    :effect (and
      (not (ready-road ?v ?r))
      (ready-road ?v ?next)
      (increase (travel-time ?v) (travel-duration ?r))
      (increase (sim-time) (travel-duration ?r))
    )
  )

  (:action traverse-road-static-road_0464-road_0362
    :parameters (?v - vehicle ?r - road_type_road_0464 ?next - road_type_road_0362)
    :precondition (and
      (ready-road ?v ?r)
      (road-next ?r ?next)
      (road-open ?r)
      (static-road ?r)
      (not (road-blocked ?r))
    )
    :effect (and
      (not (ready-road ?v ?r))
      (ready-road ?v ?next)
      (increase (travel-time ?v) (travel-duration ?r))
      (increase (sim-time) (travel-duration ?r))
    )
  )

  (:action traverse-road-static-road_0465-road_0438
    :parameters (?v - vehicle ?r - road_type_road_0465 ?next - road_type_road_0438)
    :precondition (and
      (ready-road ?v ?r)
      (road-next ?r ?next)
      (road-open ?r)
      (static-road ?r)
      (not (road-blocked ?r))
    )
    :effect (and
      (not (ready-road ?v ?r))
      (ready-road ?v ?next)
      (increase (travel-time ?v) (travel-duration ?r))
      (increase (sim-time) (travel-duration ?r))
    )
  )

  (:action traverse-road-static-road_0465-road_0439
    :parameters (?v - vehicle ?r - road_type_road_0465 ?next - road_type_road_0439)
    :precondition (and
      (ready-road ?v ?r)
      (road-next ?r ?next)
      (road-open ?r)
      (static-road ?r)
      (not (road-blocked ?r))
    )
    :effect (and
      (not (ready-road ?v ?r))
      (ready-road ?v ?next)
      (increase (travel-time ?v) (travel-duration ?r))
      (increase (sim-time) (travel-duration ?r))
    )
  )

  (:action traverse-road-static-road_0465-road_0440
    :parameters (?v - vehicle ?r - road_type_road_0465 ?next - road_type_road_0440)
    :precondition (and
      (ready-road ?v ?r)
      (road-next ?r ?next)
      (road-open ?r)
      (static-road ?r)
      (not (road-blocked ?r))
    )
    :effect (and
      (not (ready-road ?v ?r))
      (ready-road ?v ?next)
      (increase (travel-time ?v) (travel-duration ?r))
      (increase (sim-time) (travel-duration ?r))
    )
  )

  (:action traverse-road-static-road_0466-road_0362
    :parameters (?v - vehicle ?r - road_type_road_0466 ?next - road_type_road_0362)
    :precondition (and
      (ready-road ?v ?r)
      (road-next ?r ?next)
      (road-open ?r)
      (static-road ?r)
      (not (road-blocked ?r))
    )
    :effect (and
      (not (ready-road ?v ?r))
      (ready-road ?v ?next)
      (increase (travel-time ?v) (travel-duration ?r))
      (increase (sim-time) (travel-duration ?r))
    )
  )

  (:action traverse-road-static-road_0467-macro_0028
    :parameters (?v - vehicle ?r - road_type_road_0467 ?next - road_type_macro_0028)
    :precondition (and
      (ready-road ?v ?r)
      (road-next ?r ?next)
      (road-open ?r)
      (static-road ?r)
      (not (road-blocked ?r))
    )
    :effect (and
      (not (ready-road ?v ?r))
      (ready-road ?v ?next)
      (increase (travel-time ?v) (travel-duration ?r))
      (increase (sim-time) (travel-duration ?r))
    )
  )

  (:action traverse-road-static-road_0467-road_0367
    :parameters (?v - vehicle ?r - road_type_road_0467 ?next - road_type_road_0367)
    :precondition (and
      (ready-road ?v ?r)
      (road-next ?r ?next)
      (road-open ?r)
      (static-road ?r)
      (not (road-blocked ?r))
    )
    :effect (and
      (not (ready-road ?v ?r))
      (ready-road ?v ?next)
      (increase (travel-time ?v) (travel-duration ?r))
      (increase (sim-time) (travel-duration ?r))
    )
  )

  (:action traverse-road-static-road_0468-macro_0047
    :parameters (?v - vehicle ?r - road_type_road_0468 ?next - road_type_macro_0047)
    :precondition (and
      (ready-road ?v ?r)
      (road-next ?r ?next)
      (road-open ?r)
      (static-road ?r)
      (not (road-blocked ?r))
    )
    :effect (and
      (not (ready-road ?v ?r))
      (ready-road ?v ?next)
      (increase (travel-time ?v) (travel-duration ?r))
      (increase (sim-time) (travel-duration ?r))
    )
  )

  (:action traverse-road-static-road_0469-road_0627
    :parameters (?v - vehicle ?r - road_type_road_0469 ?next - road_type_road_0627)
    :precondition (and
      (ready-road ?v ?r)
      (road-next ?r ?next)
      (road-open ?r)
      (static-road ?r)
      (not (road-blocked ?r))
    )
    :effect (and
      (not (ready-road ?v ?r))
      (ready-road ?v ?next)
      (increase (travel-time ?v) (travel-duration ?r))
      (increase (sim-time) (travel-duration ?r))
    )
  )

  (:action traverse-road-dynamic-road_0470-road_0664-tw_00000
    :parameters (?v - vehicle ?r - road_type_road_0470 ?next - road_type_road_0664 ?w - window_type_tw_00000)
    :precondition (and
      (ready-road ?v ?r)
      (road-next ?r ?next)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
    )
    :effect (and
      (not (ready-road ?v ?r))
      (ready-road ?v ?next)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-road_0470-road_0664-tw_00030
    :parameters (?v - vehicle ?r - road_type_road_0470 ?next - road_type_road_0664 ?w - window_type_tw_00030)
    :precondition (and
      (ready-road ?v ?r)
      (road-next ?r ?next)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
    )
    :effect (and
      (not (ready-road ?v ?r))
      (ready-road ?v ?next)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-road_0470-road_0664-tw_00060
    :parameters (?v - vehicle ?r - road_type_road_0470 ?next - road_type_road_0664 ?w - window_type_tw_00060)
    :precondition (and
      (ready-road ?v ?r)
      (road-next ?r ?next)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
    )
    :effect (and
      (not (ready-road ?v ?r))
      (ready-road ?v ?next)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-road_0470-road_0664-tw_00090
    :parameters (?v - vehicle ?r - road_type_road_0470 ?next - road_type_road_0664 ?w - window_type_tw_00090)
    :precondition (and
      (ready-road ?v ?r)
      (road-next ?r ?next)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
    )
    :effect (and
      (not (ready-road ?v ?r))
      (ready-road ?v ?next)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-road_0470-road_0664-tw_00120
    :parameters (?v - vehicle ?r - road_type_road_0470 ?next - road_type_road_0664 ?w - window_type_tw_00120)
    :precondition (and
      (ready-road ?v ?r)
      (road-next ?r ?next)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
    )
    :effect (and
      (not (ready-road ?v ?r))
      (ready-road ?v ?next)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-road_0470-road_0664-tw_00150
    :parameters (?v - vehicle ?r - road_type_road_0470 ?next - road_type_road_0664 ?w - window_type_tw_00150)
    :precondition (and
      (ready-road ?v ?r)
      (road-next ?r ?next)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
    )
    :effect (and
      (not (ready-road ?v ?r))
      (ready-road ?v ?next)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-road_0470-road_0664-tw_00180
    :parameters (?v - vehicle ?r - road_type_road_0470 ?next - road_type_road_0664 ?w - window_type_tw_00180)
    :precondition (and
      (ready-road ?v ?r)
      (road-next ?r ?next)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
    )
    :effect (and
      (not (ready-road ?v ?r))
      (ready-road ?v ?next)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-road_0470-road_0664-tw_00210
    :parameters (?v - vehicle ?r - road_type_road_0470 ?next - road_type_road_0664 ?w - window_type_tw_00210)
    :precondition (and
      (ready-road ?v ?r)
      (road-next ?r ?next)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
    )
    :effect (and
      (not (ready-road ?v ?r))
      (ready-road ?v ?next)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-road_0470-road_0664-tw_00240
    :parameters (?v - vehicle ?r - road_type_road_0470 ?next - road_type_road_0664 ?w - window_type_tw_00240)
    :precondition (and
      (ready-road ?v ?r)
      (road-next ?r ?next)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
    )
    :effect (and
      (not (ready-road ?v ?r))
      (ready-road ?v ?next)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-road_0470-road_0664-tw_00270
    :parameters (?v - vehicle ?r - road_type_road_0470 ?next - road_type_road_0664 ?w - window_type_tw_00270)
    :precondition (and
      (ready-road ?v ?r)
      (road-next ?r ?next)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
    )
    :effect (and
      (not (ready-road ?v ?r))
      (ready-road ?v ?next)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-road_0470-road_0664-tw_00300
    :parameters (?v - vehicle ?r - road_type_road_0470 ?next - road_type_road_0664 ?w - window_type_tw_00300)
    :precondition (and
      (ready-road ?v ?r)
      (road-next ?r ?next)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
    )
    :effect (and
      (not (ready-road ?v ?r))
      (ready-road ?v ?next)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-road_0470-road_0664-tw_00330
    :parameters (?v - vehicle ?r - road_type_road_0470 ?next - road_type_road_0664 ?w - window_type_tw_00330)
    :precondition (and
      (ready-road ?v ?r)
      (road-next ?r ?next)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
    )
    :effect (and
      (not (ready-road ?v ?r))
      (ready-road ?v ?next)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-road_0470-road_0664-tw_00360
    :parameters (?v - vehicle ?r - road_type_road_0470 ?next - road_type_road_0664 ?w - window_type_tw_00360)
    :precondition (and
      (ready-road ?v ?r)
      (road-next ?r ?next)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
    )
    :effect (and
      (not (ready-road ?v ?r))
      (ready-road ?v ?next)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-road_0470-road_0664-tw_00390
    :parameters (?v - vehicle ?r - road_type_road_0470 ?next - road_type_road_0664 ?w - window_type_tw_00390)
    :precondition (and
      (ready-road ?v ?r)
      (road-next ?r ?next)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
    )
    :effect (and
      (not (ready-road ?v ?r))
      (ready-road ?v ?next)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-road_0470-road_0664-tw_00420
    :parameters (?v - vehicle ?r - road_type_road_0470 ?next - road_type_road_0664 ?w - window_type_tw_00420)
    :precondition (and
      (ready-road ?v ?r)
      (road-next ?r ?next)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
    )
    :effect (and
      (not (ready-road ?v ?r))
      (ready-road ?v ?next)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-road_0470-road_0664-tw_00450
    :parameters (?v - vehicle ?r - road_type_road_0470 ?next - road_type_road_0664 ?w - window_type_tw_00450)
    :precondition (and
      (ready-road ?v ?r)
      (road-next ?r ?next)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
    )
    :effect (and
      (not (ready-road ?v ?r))
      (ready-road ?v ?next)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-road_0470-road_0664-tw_00480
    :parameters (?v - vehicle ?r - road_type_road_0470 ?next - road_type_road_0664 ?w - window_type_tw_00480)
    :precondition (and
      (ready-road ?v ?r)
      (road-next ?r ?next)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
    )
    :effect (and
      (not (ready-road ?v ?r))
      (ready-road ?v ?next)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-road_0470-road_0664-tw_00510
    :parameters (?v - vehicle ?r - road_type_road_0470 ?next - road_type_road_0664 ?w - window_type_tw_00510)
    :precondition (and
      (ready-road ?v ?r)
      (road-next ?r ?next)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
    )
    :effect (and
      (not (ready-road ?v ?r))
      (ready-road ?v ?next)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-road_0470-road_0664-tw_00540
    :parameters (?v - vehicle ?r - road_type_road_0470 ?next - road_type_road_0664 ?w - window_type_tw_00540)
    :precondition (and
      (ready-road ?v ?r)
      (road-next ?r ?next)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
    )
    :effect (and
      (not (ready-road ?v ?r))
      (ready-road ?v ?next)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-road_0470-road_0664-tw_00570
    :parameters (?v - vehicle ?r - road_type_road_0470 ?next - road_type_road_0664 ?w - window_type_tw_00570)
    :precondition (and
      (ready-road ?v ?r)
      (road-next ?r ?next)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
    )
    :effect (and
      (not (ready-road ?v ?r))
      (ready-road ?v ?next)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-road_0470-road_0664-tw_00600
    :parameters (?v - vehicle ?r - road_type_road_0470 ?next - road_type_road_0664 ?w - window_type_tw_00600)
    :precondition (and
      (ready-road ?v ?r)
      (road-next ?r ?next)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
    )
    :effect (and
      (not (ready-road ?v ?r))
      (ready-road ?v ?next)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-static-road_0471-road_0062
    :parameters (?v - vehicle ?r - road_type_road_0471 ?next - road_type_road_0062)
    :precondition (and
      (ready-road ?v ?r)
      (road-next ?r ?next)
      (road-open ?r)
      (static-road ?r)
      (not (road-blocked ?r))
    )
    :effect (and
      (not (ready-road ?v ?r))
      (ready-road ?v ?next)
      (increase (travel-time ?v) (travel-duration ?r))
      (increase (sim-time) (travel-duration ?r))
    )
  )

  (:action traverse-road-static-road_0471-road_0063
    :parameters (?v - vehicle ?r - road_type_road_0471 ?next - road_type_road_0063)
    :precondition (and
      (ready-road ?v ?r)
      (road-next ?r ?next)
      (road-open ?r)
      (static-road ?r)
      (not (road-blocked ?r))
    )
    :effect (and
      (not (ready-road ?v ?r))
      (ready-road ?v ?next)
      (increase (travel-time ?v) (travel-duration ?r))
      (increase (sim-time) (travel-duration ?r))
    )
  )

  (:action traverse-road-static-road_0471-road_0064
    :parameters (?v - vehicle ?r - road_type_road_0471 ?next - road_type_road_0064)
    :precondition (and
      (ready-road ?v ?r)
      (road-next ?r ?next)
      (road-open ?r)
      (static-road ?r)
      (not (road-blocked ?r))
    )
    :effect (and
      (not (ready-road ?v ?r))
      (ready-road ?v ?next)
      (increase (travel-time ?v) (travel-duration ?r))
      (increase (sim-time) (travel-duration ?r))
    )
  )

  (:action traverse-road-static-road_0471-road_0065
    :parameters (?v - vehicle ?r - road_type_road_0471 ?next - road_type_road_0065)
    :precondition (and
      (ready-road ?v ?r)
      (road-next ?r ?next)
      (road-open ?r)
      (static-road ?r)
      (not (road-blocked ?r))
    )
    :effect (and
      (not (ready-road ?v ?r))
      (ready-road ?v ?next)
      (increase (travel-time ?v) (travel-duration ?r))
      (increase (sim-time) (travel-duration ?r))
    )
  )

  (:action traverse-road-static-road_0472-road_0453
    :parameters (?v - vehicle ?r - road_type_road_0472 ?next - road_type_road_0453)
    :precondition (and
      (ready-road ?v ?r)
      (road-next ?r ?next)
      (road-open ?r)
      (static-road ?r)
      (not (road-blocked ?r))
    )
    :effect (and
      (not (ready-road ?v ?r))
      (ready-road ?v ?next)
      (increase (travel-time ?v) (travel-duration ?r))
      (increase (sim-time) (travel-duration ?r))
    )
  )

  (:action traverse-road-static-road_0472-road_0454
    :parameters (?v - vehicle ?r - road_type_road_0472 ?next - road_type_road_0454)
    :precondition (and
      (ready-road ?v ?r)
      (road-next ?r ?next)
      (road-open ?r)
      (static-road ?r)
      (not (road-blocked ?r))
    )
    :effect (and
      (not (ready-road ?v ?r))
      (ready-road ?v ?next)
      (increase (travel-time ?v) (travel-duration ?r))
      (increase (sim-time) (travel-duration ?r))
    )
  )

  (:action traverse-road-static-road_0472-road_0455
    :parameters (?v - vehicle ?r - road_type_road_0472 ?next - road_type_road_0455)
    :precondition (and
      (ready-road ?v ?r)
      (road-next ?r ?next)
      (road-open ?r)
      (static-road ?r)
      (not (road-blocked ?r))
    )
    :effect (and
      (not (ready-road ?v ?r))
      (ready-road ?v ?next)
      (increase (travel-time ?v) (travel-duration ?r))
      (increase (sim-time) (travel-duration ?r))
    )
  )

  (:action traverse-road-static-road_0473-road_0423
    :parameters (?v - vehicle ?r - road_type_road_0473 ?next - road_type_road_0423)
    :precondition (and
      (ready-road ?v ?r)
      (road-next ?r ?next)
      (road-open ?r)
      (static-road ?r)
      (not (road-blocked ?r))
    )
    :effect (and
      (not (ready-road ?v ?r))
      (ready-road ?v ?next)
      (increase (travel-time ?v) (travel-duration ?r))
      (increase (sim-time) (travel-duration ?r))
    )
  )

  (:action traverse-road-static-road_0476-road_0700
    :parameters (?v - vehicle ?r - road_type_road_0476 ?next - road_type_road_0700)
    :precondition (and
      (ready-road ?v ?r)
      (road-next ?r ?next)
      (road-open ?r)
      (static-road ?r)
      (not (road-blocked ?r))
    )
    :effect (and
      (not (ready-road ?v ?r))
      (ready-road ?v ?next)
      (increase (travel-time ?v) (travel-duration ?r))
      (increase (sim-time) (travel-duration ?r))
    )
  )

  (:action traverse-road-static-road_0476-road_0701
    :parameters (?v - vehicle ?r - road_type_road_0476 ?next - road_type_road_0701)
    :precondition (and
      (ready-road ?v ?r)
      (road-next ?r ?next)
      (road-open ?r)
      (static-road ?r)
      (not (road-blocked ?r))
    )
    :effect (and
      (not (ready-road ?v ?r))
      (ready-road ?v ?next)
      (increase (travel-time ?v) (travel-duration ?r))
      (increase (sim-time) (travel-duration ?r))
    )
  )

  (:action traverse-road-static-road_0476-road_0702
    :parameters (?v - vehicle ?r - road_type_road_0476 ?next - road_type_road_0702)
    :precondition (and
      (ready-road ?v ?r)
      (road-next ?r ?next)
      (road-open ?r)
      (static-road ?r)
      (not (road-blocked ?r))
    )
    :effect (and
      (not (ready-road ?v ?r))
      (ready-road ?v ?next)
      (increase (travel-time ?v) (travel-duration ?r))
      (increase (sim-time) (travel-duration ?r))
    )
  )

  (:action traverse-road-static-road_0479-road_0493
    :parameters (?v - vehicle ?r - road_type_road_0479 ?next - road_type_road_0493)
    :precondition (and
      (ready-road ?v ?r)
      (road-next ?r ?next)
      (road-open ?r)
      (static-road ?r)
      (not (road-blocked ?r))
    )
    :effect (and
      (not (ready-road ?v ?r))
      (ready-road ?v ?next)
      (increase (travel-time ?v) (travel-duration ?r))
      (increase (sim-time) (travel-duration ?r))
    )
  )

  (:action traverse-road-static-road_0479-road_0494
    :parameters (?v - vehicle ?r - road_type_road_0479 ?next - road_type_road_0494)
    :precondition (and
      (ready-road ?v ?r)
      (road-next ?r ?next)
      (road-open ?r)
      (static-road ?r)
      (not (road-blocked ?r))
    )
    :effect (and
      (not (ready-road ?v ?r))
      (ready-road ?v ?next)
      (increase (travel-time ?v) (travel-duration ?r))
      (increase (sim-time) (travel-duration ?r))
    )
  )

  (:action traverse-road-static-road_0480-road_0490
    :parameters (?v - vehicle ?r - road_type_road_0480 ?next - road_type_road_0490)
    :precondition (and
      (ready-road ?v ?r)
      (road-next ?r ?next)
      (road-open ?r)
      (static-road ?r)
      (not (road-blocked ?r))
    )
    :effect (and
      (not (ready-road ?v ?r))
      (ready-road ?v ?next)
      (increase (travel-time ?v) (travel-duration ?r))
      (increase (sim-time) (travel-duration ?r))
    )
  )

  (:action traverse-road-static-road_0480-road_0491
    :parameters (?v - vehicle ?r - road_type_road_0480 ?next - road_type_road_0491)
    :precondition (and
      (ready-road ?v ?r)
      (road-next ?r ?next)
      (road-open ?r)
      (static-road ?r)
      (not (road-blocked ?r))
    )
    :effect (and
      (not (ready-road ?v ?r))
      (ready-road ?v ?next)
      (increase (travel-time ?v) (travel-duration ?r))
      (increase (sim-time) (travel-duration ?r))
    )
  )

  (:action traverse-road-static-road_0480-road_0492
    :parameters (?v - vehicle ?r - road_type_road_0480 ?next - road_type_road_0492)
    :precondition (and
      (ready-road ?v ?r)
      (road-next ?r ?next)
      (road-open ?r)
      (static-road ?r)
      (not (road-blocked ?r))
    )
    :effect (and
      (not (ready-road ?v ?r))
      (ready-road ?v ?next)
      (increase (travel-time ?v) (travel-duration ?r))
      (increase (sim-time) (travel-duration ?r))
    )
  )

  (:action traverse-road-static-road_0481-road_0590
    :parameters (?v - vehicle ?r - road_type_road_0481 ?next - road_type_road_0590)
    :precondition (and
      (ready-road ?v ?r)
      (road-next ?r ?next)
      (road-open ?r)
      (static-road ?r)
      (not (road-blocked ?r))
    )
    :effect (and
      (not (ready-road ?v ?r))
      (ready-road ?v ?next)
      (increase (travel-time ?v) (travel-duration ?r))
      (increase (sim-time) (travel-duration ?r))
    )
  )

  (:action traverse-road-static-road_0482-road_0483
    :parameters (?v - vehicle ?r - road_type_road_0482 ?next - road_type_road_0483)
    :precondition (and
      (ready-road ?v ?r)
      (road-next ?r ?next)
      (road-open ?r)
      (static-road ?r)
      (not (road-blocked ?r))
    )
    :effect (and
      (not (ready-road ?v ?r))
      (ready-road ?v ?next)
      (increase (travel-time ?v) (travel-duration ?r))
      (increase (sim-time) (travel-duration ?r))
    )
  )

  (:action traverse-road-static-road_0482-road_0484
    :parameters (?v - vehicle ?r - road_type_road_0482 ?next - road_type_road_0484)
    :precondition (and
      (ready-road ?v ?r)
      (road-next ?r ?next)
      (road-open ?r)
      (static-road ?r)
      (not (road-blocked ?r))
    )
    :effect (and
      (not (ready-road ?v ?r))
      (ready-road ?v ?next)
      (increase (travel-time ?v) (travel-duration ?r))
      (increase (sim-time) (travel-duration ?r))
    )
  )

  (:action traverse-road-static-road_0483-road_0482
    :parameters (?v - vehicle ?r - road_type_road_0483 ?next - road_type_road_0482)
    :precondition (and
      (ready-road ?v ?r)
      (road-next ?r ?next)
      (road-open ?r)
      (static-road ?r)
      (not (road-blocked ?r))
    )
    :effect (and
      (not (ready-road ?v ?r))
      (ready-road ?v ?next)
      (increase (travel-time ?v) (travel-duration ?r))
      (increase (sim-time) (travel-duration ?r))
    )
  )

  (:action traverse-road-static-road_0484-road_0313
    :parameters (?v - vehicle ?r - road_type_road_0484 ?next - road_type_road_0313)
    :precondition (and
      (ready-road ?v ?r)
      (road-next ?r ?next)
      (road-open ?r)
      (static-road ?r)
      (not (road-blocked ?r))
    )
    :effect (and
      (not (ready-road ?v ?r))
      (ready-road ?v ?next)
      (increase (travel-time ?v) (travel-duration ?r))
      (increase (sim-time) (travel-duration ?r))
    )
  )

  (:action traverse-road-static-road_0484-road_0314
    :parameters (?v - vehicle ?r - road_type_road_0484 ?next - road_type_road_0314)
    :precondition (and
      (ready-road ?v ?r)
      (road-next ?r ?next)
      (road-open ?r)
      (static-road ?r)
      (not (road-blocked ?r))
    )
    :effect (and
      (not (ready-road ?v ?r))
      (ready-road ?v ?next)
      (increase (travel-time ?v) (travel-duration ?r))
      (increase (sim-time) (travel-duration ?r))
    )
  )

  (:action traverse-road-static-road_0485-road_0487
    :parameters (?v - vehicle ?r - road_type_road_0485 ?next - road_type_road_0487)
    :precondition (and
      (ready-road ?v ?r)
      (road-next ?r ?next)
      (road-open ?r)
      (static-road ?r)
      (not (road-blocked ?r))
    )
    :effect (and
      (not (ready-road ?v ?r))
      (ready-road ?v ?next)
      (increase (travel-time ?v) (travel-duration ?r))
      (increase (sim-time) (travel-duration ?r))
    )
  )

  (:action traverse-road-static-road_0486-road_0325
    :parameters (?v - vehicle ?r - road_type_road_0486 ?next - road_type_road_0325)
    :precondition (and
      (ready-road ?v ?r)
      (road-next ?r ?next)
      (road-open ?r)
      (static-road ?r)
      (not (road-blocked ?r))
    )
    :effect (and
      (not (ready-road ?v ?r))
      (ready-road ?v ?next)
      (increase (travel-time ?v) (travel-duration ?r))
      (increase (sim-time) (travel-duration ?r))
    )
  )

  (:action traverse-road-static-road_0486-road_0326
    :parameters (?v - vehicle ?r - road_type_road_0486 ?next - road_type_road_0326)
    :precondition (and
      (ready-road ?v ?r)
      (road-next ?r ?next)
      (road-open ?r)
      (static-road ?r)
      (not (road-blocked ?r))
    )
    :effect (and
      (not (ready-road ?v ?r))
      (ready-road ?v ?next)
      (increase (travel-time ?v) (travel-duration ?r))
      (increase (sim-time) (travel-duration ?r))
    )
  )

  (:action traverse-road-static-road_0487-road_0426
    :parameters (?v - vehicle ?r - road_type_road_0487 ?next - road_type_road_0426)
    :precondition (and
      (ready-road ?v ?r)
      (road-next ?r ?next)
      (road-open ?r)
      (static-road ?r)
      (not (road-blocked ?r))
    )
    :effect (and
      (not (ready-road ?v ?r))
      (ready-road ?v ?next)
      (increase (travel-time ?v) (travel-duration ?r))
      (increase (sim-time) (travel-duration ?r))
    )
  )

  (:action traverse-road-static-road_0487-road_0427
    :parameters (?v - vehicle ?r - road_type_road_0487 ?next - road_type_road_0427)
    :precondition (and
      (ready-road ?v ?r)
      (road-next ?r ?next)
      (road-open ?r)
      (static-road ?r)
      (not (road-blocked ?r))
    )
    :effect (and
      (not (ready-road ?v ?r))
      (ready-road ?v ?next)
      (increase (travel-time ?v) (travel-duration ?r))
      (increase (sim-time) (travel-duration ?r))
    )
  )

  (:action traverse-road-static-road_0488-macro_0025
    :parameters (?v - vehicle ?r - road_type_road_0488 ?next - road_type_macro_0025)
    :precondition (and
      (ready-road ?v ?r)
      (road-next ?r ?next)
      (road-open ?r)
      (static-road ?r)
      (not (road-blocked ?r))
    )
    :effect (and
      (not (ready-road ?v ?r))
      (ready-road ?v ?next)
      (increase (travel-time ?v) (travel-duration ?r))
      (increase (sim-time) (travel-duration ?r))
    )
  )

  (:action traverse-road-static-road_0488-road_0305
    :parameters (?v - vehicle ?r - road_type_road_0488 ?next - road_type_road_0305)
    :precondition (and
      (ready-road ?v ?r)
      (road-next ?r ?next)
      (road-open ?r)
      (static-road ?r)
      (not (road-blocked ?r))
    )
    :effect (and
      (not (ready-road ?v ?r))
      (ready-road ?v ?next)
      (increase (travel-time ?v) (travel-duration ?r))
      (increase (sim-time) (travel-duration ?r))
    )
  )

  (:action traverse-road-static-road_0490-road_0480
    :parameters (?v - vehicle ?r - road_type_road_0490 ?next - road_type_road_0480)
    :precondition (and
      (ready-road ?v ?r)
      (road-next ?r ?next)
      (road-open ?r)
      (static-road ?r)
      (not (road-blocked ?r))
    )
    :effect (and
      (not (ready-road ?v ?r))
      (ready-road ?v ?next)
      (increase (travel-time ?v) (travel-duration ?r))
      (increase (sim-time) (travel-duration ?r))
    )
  )

  (:action traverse-road-static-road_0490-road_0481
    :parameters (?v - vehicle ?r - road_type_road_0490 ?next - road_type_road_0481)
    :precondition (and
      (ready-road ?v ?r)
      (road-next ?r ?next)
      (road-open ?r)
      (static-road ?r)
      (not (road-blocked ?r))
    )
    :effect (and
      (not (ready-road ?v ?r))
      (ready-road ?v ?next)
      (increase (travel-time ?v) (travel-duration ?r))
      (increase (sim-time) (travel-duration ?r))
    )
  )

  (:action traverse-road-static-road_0491-road_0591
    :parameters (?v - vehicle ?r - road_type_road_0491 ?next - road_type_road_0591)
    :precondition (and
      (ready-road ?v ?r)
      (road-next ?r ?next)
      (road-open ?r)
      (static-road ?r)
      (not (road-blocked ?r))
    )
    :effect (and
      (not (ready-road ?v ?r))
      (ready-road ?v ?next)
      (increase (travel-time ?v) (travel-duration ?r))
      (increase (sim-time) (travel-duration ?r))
    )
  )

  (:action traverse-road-static-road_0491-road_0592
    :parameters (?v - vehicle ?r - road_type_road_0491 ?next - road_type_road_0592)
    :precondition (and
      (ready-road ?v ?r)
      (road-next ?r ?next)
      (road-open ?r)
      (static-road ?r)
      (not (road-blocked ?r))
    )
    :effect (and
      (not (ready-road ?v ?r))
      (ready-road ?v ?next)
      (increase (travel-time ?v) (travel-duration ?r))
      (increase (sim-time) (travel-duration ?r))
    )
  )

  (:action traverse-road-static-road_0491-road_0593
    :parameters (?v - vehicle ?r - road_type_road_0491 ?next - road_type_road_0593)
    :precondition (and
      (ready-road ?v ?r)
      (road-next ?r ?next)
      (road-open ?r)
      (static-road ?r)
      (not (road-blocked ?r))
    )
    :effect (and
      (not (ready-road ?v ?r))
      (ready-road ?v ?next)
      (increase (travel-time ?v) (travel-duration ?r))
      (increase (sim-time) (travel-duration ?r))
    )
  )

  (:action traverse-road-static-road_0492-macro_0040
    :parameters (?v - vehicle ?r - road_type_road_0492 ?next - road_type_macro_0040)
    :precondition (and
      (ready-road ?v ?r)
      (road-next ?r ?next)
      (road-open ?r)
      (static-road ?r)
      (not (road-blocked ?r))
    )
    :effect (and
      (not (ready-road ?v ?r))
      (ready-road ?v ?next)
      (increase (travel-time ?v) (travel-duration ?r))
      (increase (sim-time) (travel-duration ?r))
    )
  )

  (:action traverse-road-static-road_0492-macro_0041
    :parameters (?v - vehicle ?r - road_type_road_0492 ?next - road_type_macro_0041)
    :precondition (and
      (ready-road ?v ?r)
      (road-next ?r ?next)
      (road-open ?r)
      (static-road ?r)
      (not (road-blocked ?r))
    )
    :effect (and
      (not (ready-road ?v ?r))
      (ready-road ?v ?next)
      (increase (travel-time ?v) (travel-duration ?r))
      (increase (sim-time) (travel-duration ?r))
    )
  )

  (:action traverse-road-static-road_0492-road_0572
    :parameters (?v - vehicle ?r - road_type_road_0492 ?next - road_type_road_0572)
    :precondition (and
      (ready-road ?v ?r)
      (road-next ?r ?next)
      (road-open ?r)
      (static-road ?r)
      (not (road-blocked ?r))
    )
    :effect (and
      (not (ready-road ?v ?r))
      (ready-road ?v ?next)
      (increase (travel-time ?v) (travel-duration ?r))
      (increase (sim-time) (travel-duration ?r))
    )
  )

  (:action traverse-road-static-road_0492-road_0573
    :parameters (?v - vehicle ?r - road_type_road_0492 ?next - road_type_road_0573)
    :precondition (and
      (ready-road ?v ?r)
      (road-next ?r ?next)
      (road-open ?r)
      (static-road ?r)
      (not (road-blocked ?r))
    )
    :effect (and
      (not (ready-road ?v ?r))
      (ready-road ?v ?next)
      (increase (travel-time ?v) (travel-duration ?r))
      (increase (sim-time) (travel-duration ?r))
    )
  )

  (:action traverse-road-static-road_0493-road_0349
    :parameters (?v - vehicle ?r - road_type_road_0493 ?next - road_type_road_0349)
    :precondition (and
      (ready-road ?v ?r)
      (road-next ?r ?next)
      (road-open ?r)
      (static-road ?r)
      (not (road-blocked ?r))
    )
    :effect (and
      (not (ready-road ?v ?r))
      (ready-road ?v ?next)
      (increase (travel-time ?v) (travel-duration ?r))
      (increase (sim-time) (travel-duration ?r))
    )
  )

  (:action traverse-road-static-road_0493-road_0350
    :parameters (?v - vehicle ?r - road_type_road_0493 ?next - road_type_road_0350)
    :precondition (and
      (ready-road ?v ?r)
      (road-next ?r ?next)
      (road-open ?r)
      (static-road ?r)
      (not (road-blocked ?r))
    )
    :effect (and
      (not (ready-road ?v ?r))
      (ready-road ?v ?next)
      (increase (travel-time ?v) (travel-duration ?r))
      (increase (sim-time) (travel-duration ?r))
    )
  )

  (:action traverse-road-static-road_0494-road_0495
    :parameters (?v - vehicle ?r - road_type_road_0494 ?next - road_type_road_0495)
    :precondition (and
      (ready-road ?v ?r)
      (road-next ?r ?next)
      (road-open ?r)
      (static-road ?r)
      (not (road-blocked ?r))
    )
    :effect (and
      (not (ready-road ?v ?r))
      (ready-road ?v ?next)
      (increase (travel-time ?v) (travel-duration ?r))
      (increase (sim-time) (travel-duration ?r))
    )
  )

  (:action traverse-road-static-road_0494-road_0496
    :parameters (?v - vehicle ?r - road_type_road_0494 ?next - road_type_road_0496)
    :precondition (and
      (ready-road ?v ?r)
      (road-next ?r ?next)
      (road-open ?r)
      (static-road ?r)
      (not (road-blocked ?r))
    )
    :effect (and
      (not (ready-road ?v ?r))
      (ready-road ?v ?next)
      (increase (travel-time ?v) (travel-duration ?r))
      (increase (sim-time) (travel-duration ?r))
    )
  )

  (:action traverse-road-static-road_0495-road_0683
    :parameters (?v - vehicle ?r - road_type_road_0495 ?next - road_type_road_0683)
    :precondition (and
      (ready-road ?v ?r)
      (road-next ?r ?next)
      (road-open ?r)
      (static-road ?r)
      (not (road-blocked ?r))
    )
    :effect (and
      (not (ready-road ?v ?r))
      (ready-road ?v ?next)
      (increase (travel-time ?v) (travel-duration ?r))
      (increase (sim-time) (travel-duration ?r))
    )
  )

  (:action traverse-road-static-road_0495-road_0684
    :parameters (?v - vehicle ?r - road_type_road_0495 ?next - road_type_road_0684)
    :precondition (and
      (ready-road ?v ?r)
      (road-next ?r ?next)
      (road-open ?r)
      (static-road ?r)
      (not (road-blocked ?r))
    )
    :effect (and
      (not (ready-road ?v ?r))
      (ready-road ?v ?next)
      (increase (travel-time ?v) (travel-duration ?r))
      (increase (sim-time) (travel-duration ?r))
    )
  )

  (:action traverse-road-static-road_0496-road_0210
    :parameters (?v - vehicle ?r - road_type_road_0496 ?next - road_type_road_0210)
    :precondition (and
      (ready-road ?v ?r)
      (road-next ?r ?next)
      (road-open ?r)
      (static-road ?r)
      (not (road-blocked ?r))
    )
    :effect (and
      (not (ready-road ?v ?r))
      (ready-road ?v ?next)
      (increase (travel-time ?v) (travel-duration ?r))
      (increase (sim-time) (travel-duration ?r))
    )
  )

  (:action traverse-road-static-road_0497-road_0008
    :parameters (?v - vehicle ?r - road_type_road_0497 ?next - road_type_road_0008)
    :precondition (and
      (ready-road ?v ?r)
      (road-next ?r ?next)
      (road-open ?r)
      (static-road ?r)
      (not (road-blocked ?r))
    )
    :effect (and
      (not (ready-road ?v ?r))
      (ready-road ?v ?next)
      (increase (travel-time ?v) (travel-duration ?r))
      (increase (sim-time) (travel-duration ?r))
    )
  )

  (:action traverse-road-static-road_0497-road_0009
    :parameters (?v - vehicle ?r - road_type_road_0497 ?next - road_type_road_0009)
    :precondition (and
      (ready-road ?v ?r)
      (road-next ?r ?next)
      (road-open ?r)
      (static-road ?r)
      (not (road-blocked ?r))
    )
    :effect (and
      (not (ready-road ?v ?r))
      (ready-road ?v ?next)
      (increase (travel-time ?v) (travel-duration ?r))
      (increase (sim-time) (travel-duration ?r))
    )
  )

  (:action traverse-road-static-road_0498-road_0499
    :parameters (?v - vehicle ?r - road_type_road_0498 ?next - road_type_road_0499)
    :precondition (and
      (ready-road ?v ?r)
      (road-next ?r ?next)
      (road-open ?r)
      (static-road ?r)
      (not (road-blocked ?r))
    )
    :effect (and
      (not (ready-road ?v ?r))
      (ready-road ?v ?next)
      (increase (travel-time ?v) (travel-duration ?r))
      (increase (sim-time) (travel-duration ?r))
    )
  )

  (:action traverse-road-static-road_0498-road_0500
    :parameters (?v - vehicle ?r - road_type_road_0498 ?next - road_type_road_0500)
    :precondition (and
      (ready-road ?v ?r)
      (road-next ?r ?next)
      (road-open ?r)
      (static-road ?r)
      (not (road-blocked ?r))
    )
    :effect (and
      (not (ready-road ?v ?r))
      (ready-road ?v ?next)
      (increase (travel-time ?v) (travel-duration ?r))
      (increase (sim-time) (travel-duration ?r))
    )
  )

  (:action traverse-road-static-road_0498-road_0501
    :parameters (?v - vehicle ?r - road_type_road_0498 ?next - road_type_road_0501)
    :precondition (and
      (ready-road ?v ?r)
      (road-next ?r ?next)
      (road-open ?r)
      (static-road ?r)
      (not (road-blocked ?r))
    )
    :effect (and
      (not (ready-road ?v ?r))
      (ready-road ?v ?next)
      (increase (travel-time ?v) (travel-duration ?r))
      (increase (sim-time) (travel-duration ?r))
    )
  )

  (:action traverse-road-static-road_0499-road_0670
    :parameters (?v - vehicle ?r - road_type_road_0499 ?next - road_type_road_0670)
    :precondition (and
      (ready-road ?v ?r)
      (road-next ?r ?next)
      (road-open ?r)
      (static-road ?r)
      (not (road-blocked ?r))
    )
    :effect (and
      (not (ready-road ?v ?r))
      (ready-road ?v ?next)
      (increase (travel-time ?v) (travel-duration ?r))
      (increase (sim-time) (travel-duration ?r))
    )
  )

  (:action traverse-road-static-road_0499-road_0671
    :parameters (?v - vehicle ?r - road_type_road_0499 ?next - road_type_road_0671)
    :precondition (and
      (ready-road ?v ?r)
      (road-next ?r ?next)
      (road-open ?r)
      (static-road ?r)
      (not (road-blocked ?r))
    )
    :effect (and
      (not (ready-road ?v ?r))
      (ready-road ?v ?next)
      (increase (travel-time ?v) (travel-duration ?r))
      (increase (sim-time) (travel-duration ?r))
    )
  )

  (:action traverse-road-static-road_0499-road_0672
    :parameters (?v - vehicle ?r - road_type_road_0499 ?next - road_type_road_0672)
    :precondition (and
      (ready-road ?v ?r)
      (road-next ?r ?next)
      (road-open ?r)
      (static-road ?r)
      (not (road-blocked ?r))
    )
    :effect (and
      (not (ready-road ?v ?r))
      (ready-road ?v ?next)
      (increase (travel-time ?v) (travel-duration ?r))
      (increase (sim-time) (travel-duration ?r))
    )
  )

  (:action traverse-road-static-road_0500-road_0546
    :parameters (?v - vehicle ?r - road_type_road_0500 ?next - road_type_road_0546)
    :precondition (and
      (ready-road ?v ?r)
      (road-next ?r ?next)
      (road-open ?r)
      (static-road ?r)
      (not (road-blocked ?r))
    )
    :effect (and
      (not (ready-road ?v ?r))
      (ready-road ?v ?next)
      (increase (travel-time ?v) (travel-duration ?r))
      (increase (sim-time) (travel-duration ?r))
    )
  )

  (:action traverse-road-static-road_0500-road_0547
    :parameters (?v - vehicle ?r - road_type_road_0500 ?next - road_type_road_0547)
    :precondition (and
      (ready-road ?v ?r)
      (road-next ?r ?next)
      (road-open ?r)
      (static-road ?r)
      (not (road-blocked ?r))
    )
    :effect (and
      (not (ready-road ?v ?r))
      (ready-road ?v ?next)
      (increase (travel-time ?v) (travel-duration ?r))
      (increase (sim-time) (travel-duration ?r))
    )
  )

  (:action traverse-road-static-road_0501-road_0490
    :parameters (?v - vehicle ?r - road_type_road_0501 ?next - road_type_road_0490)
    :precondition (and
      (ready-road ?v ?r)
      (road-next ?r ?next)
      (road-open ?r)
      (static-road ?r)
      (not (road-blocked ?r))
    )
    :effect (and
      (not (ready-road ?v ?r))
      (ready-road ?v ?next)
      (increase (travel-time ?v) (travel-duration ?r))
      (increase (sim-time) (travel-duration ?r))
    )
  )

  (:action traverse-road-static-road_0501-road_0491
    :parameters (?v - vehicle ?r - road_type_road_0501 ?next - road_type_road_0491)
    :precondition (and
      (ready-road ?v ?r)
      (road-next ?r ?next)
      (road-open ?r)
      (static-road ?r)
      (not (road-blocked ?r))
    )
    :effect (and
      (not (ready-road ?v ?r))
      (ready-road ?v ?next)
      (increase (travel-time ?v) (travel-duration ?r))
      (increase (sim-time) (travel-duration ?r))
    )
  )

  (:action traverse-road-static-road_0501-road_0492
    :parameters (?v - vehicle ?r - road_type_road_0501 ?next - road_type_road_0492)
    :precondition (and
      (ready-road ?v ?r)
      (road-next ?r ?next)
      (road-open ?r)
      (static-road ?r)
      (not (road-blocked ?r))
    )
    :effect (and
      (not (ready-road ?v ?r))
      (ready-road ?v ?next)
      (increase (travel-time ?v) (travel-duration ?r))
      (increase (sim-time) (travel-duration ?r))
    )
  )

  (:action traverse-road-static-road_0502-macro_0030
    :parameters (?v - vehicle ?r - road_type_road_0502 ?next - road_type_macro_0030)
    :precondition (and
      (ready-road ?v ?r)
      (road-next ?r ?next)
      (road-open ?r)
      (static-road ?r)
      (not (road-blocked ?r))
    )
    :effect (and
      (not (ready-road ?v ?r))
      (ready-road ?v ?next)
      (increase (travel-time ?v) (travel-duration ?r))
      (increase (sim-time) (travel-duration ?r))
    )
  )

  (:action traverse-road-static-road_0502-road_0401
    :parameters (?v - vehicle ?r - road_type_road_0502 ?next - road_type_road_0401)
    :precondition (and
      (ready-road ?v ?r)
      (road-next ?r ?next)
      (road-open ?r)
      (static-road ?r)
      (not (road-blocked ?r))
    )
    :effect (and
      (not (ready-road ?v ?r))
      (ready-road ?v ?next)
      (increase (travel-time ?v) (travel-duration ?r))
      (increase (sim-time) (travel-duration ?r))
    )
  )

  (:action traverse-road-static-road_0502-road_0403
    :parameters (?v - vehicle ?r - road_type_road_0502 ?next - road_type_road_0403)
    :precondition (and
      (ready-road ?v ?r)
      (road-next ?r ?next)
      (road-open ?r)
      (static-road ?r)
      (not (road-blocked ?r))
    )
    :effect (and
      (not (ready-road ?v ?r))
      (ready-road ?v ?next)
      (increase (travel-time ?v) (travel-duration ?r))
      (increase (sim-time) (travel-duration ?r))
    )
  )

  (:action traverse-road-dynamic-road_0505-road_0516-tw_00000
    :parameters (?v - vehicle ?r - road_type_road_0505 ?next - road_type_road_0516 ?w - window_type_tw_00000)
    :precondition (and
      (ready-road ?v ?r)
      (road-next ?r ?next)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
    )
    :effect (and
      (not (ready-road ?v ?r))
      (ready-road ?v ?next)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-road_0505-road_0516-tw_00030
    :parameters (?v - vehicle ?r - road_type_road_0505 ?next - road_type_road_0516 ?w - window_type_tw_00030)
    :precondition (and
      (ready-road ?v ?r)
      (road-next ?r ?next)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
    )
    :effect (and
      (not (ready-road ?v ?r))
      (ready-road ?v ?next)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-road_0505-road_0516-tw_00060
    :parameters (?v - vehicle ?r - road_type_road_0505 ?next - road_type_road_0516 ?w - window_type_tw_00060)
    :precondition (and
      (ready-road ?v ?r)
      (road-next ?r ?next)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
    )
    :effect (and
      (not (ready-road ?v ?r))
      (ready-road ?v ?next)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-road_0505-road_0516-tw_00090
    :parameters (?v - vehicle ?r - road_type_road_0505 ?next - road_type_road_0516 ?w - window_type_tw_00090)
    :precondition (and
      (ready-road ?v ?r)
      (road-next ?r ?next)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
    )
    :effect (and
      (not (ready-road ?v ?r))
      (ready-road ?v ?next)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-road_0505-road_0516-tw_00120
    :parameters (?v - vehicle ?r - road_type_road_0505 ?next - road_type_road_0516 ?w - window_type_tw_00120)
    :precondition (and
      (ready-road ?v ?r)
      (road-next ?r ?next)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
    )
    :effect (and
      (not (ready-road ?v ?r))
      (ready-road ?v ?next)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-road_0505-road_0516-tw_00150
    :parameters (?v - vehicle ?r - road_type_road_0505 ?next - road_type_road_0516 ?w - window_type_tw_00150)
    :precondition (and
      (ready-road ?v ?r)
      (road-next ?r ?next)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
    )
    :effect (and
      (not (ready-road ?v ?r))
      (ready-road ?v ?next)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-road_0505-road_0516-tw_00180
    :parameters (?v - vehicle ?r - road_type_road_0505 ?next - road_type_road_0516 ?w - window_type_tw_00180)
    :precondition (and
      (ready-road ?v ?r)
      (road-next ?r ?next)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
    )
    :effect (and
      (not (ready-road ?v ?r))
      (ready-road ?v ?next)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-road_0505-road_0516-tw_00210
    :parameters (?v - vehicle ?r - road_type_road_0505 ?next - road_type_road_0516 ?w - window_type_tw_00210)
    :precondition (and
      (ready-road ?v ?r)
      (road-next ?r ?next)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
    )
    :effect (and
      (not (ready-road ?v ?r))
      (ready-road ?v ?next)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-road_0505-road_0516-tw_00240
    :parameters (?v - vehicle ?r - road_type_road_0505 ?next - road_type_road_0516 ?w - window_type_tw_00240)
    :precondition (and
      (ready-road ?v ?r)
      (road-next ?r ?next)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
    )
    :effect (and
      (not (ready-road ?v ?r))
      (ready-road ?v ?next)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-road_0505-road_0516-tw_00270
    :parameters (?v - vehicle ?r - road_type_road_0505 ?next - road_type_road_0516 ?w - window_type_tw_00270)
    :precondition (and
      (ready-road ?v ?r)
      (road-next ?r ?next)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
    )
    :effect (and
      (not (ready-road ?v ?r))
      (ready-road ?v ?next)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-road_0505-road_0516-tw_00300
    :parameters (?v - vehicle ?r - road_type_road_0505 ?next - road_type_road_0516 ?w - window_type_tw_00300)
    :precondition (and
      (ready-road ?v ?r)
      (road-next ?r ?next)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
    )
    :effect (and
      (not (ready-road ?v ?r))
      (ready-road ?v ?next)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-road_0505-road_0516-tw_00330
    :parameters (?v - vehicle ?r - road_type_road_0505 ?next - road_type_road_0516 ?w - window_type_tw_00330)
    :precondition (and
      (ready-road ?v ?r)
      (road-next ?r ?next)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
    )
    :effect (and
      (not (ready-road ?v ?r))
      (ready-road ?v ?next)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-road_0505-road_0516-tw_00360
    :parameters (?v - vehicle ?r - road_type_road_0505 ?next - road_type_road_0516 ?w - window_type_tw_00360)
    :precondition (and
      (ready-road ?v ?r)
      (road-next ?r ?next)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
    )
    :effect (and
      (not (ready-road ?v ?r))
      (ready-road ?v ?next)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-road_0505-road_0516-tw_00390
    :parameters (?v - vehicle ?r - road_type_road_0505 ?next - road_type_road_0516 ?w - window_type_tw_00390)
    :precondition (and
      (ready-road ?v ?r)
      (road-next ?r ?next)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
    )
    :effect (and
      (not (ready-road ?v ?r))
      (ready-road ?v ?next)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-road_0505-road_0516-tw_00420
    :parameters (?v - vehicle ?r - road_type_road_0505 ?next - road_type_road_0516 ?w - window_type_tw_00420)
    :precondition (and
      (ready-road ?v ?r)
      (road-next ?r ?next)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
    )
    :effect (and
      (not (ready-road ?v ?r))
      (ready-road ?v ?next)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-road_0505-road_0516-tw_00450
    :parameters (?v - vehicle ?r - road_type_road_0505 ?next - road_type_road_0516 ?w - window_type_tw_00450)
    :precondition (and
      (ready-road ?v ?r)
      (road-next ?r ?next)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
    )
    :effect (and
      (not (ready-road ?v ?r))
      (ready-road ?v ?next)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-road_0505-road_0516-tw_00480
    :parameters (?v - vehicle ?r - road_type_road_0505 ?next - road_type_road_0516 ?w - window_type_tw_00480)
    :precondition (and
      (ready-road ?v ?r)
      (road-next ?r ?next)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
    )
    :effect (and
      (not (ready-road ?v ?r))
      (ready-road ?v ?next)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-road_0505-road_0516-tw_00510
    :parameters (?v - vehicle ?r - road_type_road_0505 ?next - road_type_road_0516 ?w - window_type_tw_00510)
    :precondition (and
      (ready-road ?v ?r)
      (road-next ?r ?next)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
    )
    :effect (and
      (not (ready-road ?v ?r))
      (ready-road ?v ?next)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-road_0505-road_0516-tw_00540
    :parameters (?v - vehicle ?r - road_type_road_0505 ?next - road_type_road_0516 ?w - window_type_tw_00540)
    :precondition (and
      (ready-road ?v ?r)
      (road-next ?r ?next)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
    )
    :effect (and
      (not (ready-road ?v ?r))
      (ready-road ?v ?next)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-road_0505-road_0516-tw_00570
    :parameters (?v - vehicle ?r - road_type_road_0505 ?next - road_type_road_0516 ?w - window_type_tw_00570)
    :precondition (and
      (ready-road ?v ?r)
      (road-next ?r ?next)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
    )
    :effect (and
      (not (ready-road ?v ?r))
      (ready-road ?v ?next)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-road_0505-road_0516-tw_00600
    :parameters (?v - vehicle ?r - road_type_road_0505 ?next - road_type_road_0516 ?w - window_type_tw_00600)
    :precondition (and
      (ready-road ?v ?r)
      (road-next ?r ?next)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
    )
    :effect (and
      (not (ready-road ?v ?r))
      (ready-road ?v ?next)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-road_0505-road_0517-tw_00000
    :parameters (?v - vehicle ?r - road_type_road_0505 ?next - road_type_road_0517 ?w - window_type_tw_00000)
    :precondition (and
      (ready-road ?v ?r)
      (road-next ?r ?next)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
    )
    :effect (and
      (not (ready-road ?v ?r))
      (ready-road ?v ?next)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-road_0505-road_0517-tw_00030
    :parameters (?v - vehicle ?r - road_type_road_0505 ?next - road_type_road_0517 ?w - window_type_tw_00030)
    :precondition (and
      (ready-road ?v ?r)
      (road-next ?r ?next)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
    )
    :effect (and
      (not (ready-road ?v ?r))
      (ready-road ?v ?next)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-road_0505-road_0517-tw_00060
    :parameters (?v - vehicle ?r - road_type_road_0505 ?next - road_type_road_0517 ?w - window_type_tw_00060)
    :precondition (and
      (ready-road ?v ?r)
      (road-next ?r ?next)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
    )
    :effect (and
      (not (ready-road ?v ?r))
      (ready-road ?v ?next)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-road_0505-road_0517-tw_00090
    :parameters (?v - vehicle ?r - road_type_road_0505 ?next - road_type_road_0517 ?w - window_type_tw_00090)
    :precondition (and
      (ready-road ?v ?r)
      (road-next ?r ?next)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
    )
    :effect (and
      (not (ready-road ?v ?r))
      (ready-road ?v ?next)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-road_0505-road_0517-tw_00120
    :parameters (?v - vehicle ?r - road_type_road_0505 ?next - road_type_road_0517 ?w - window_type_tw_00120)
    :precondition (and
      (ready-road ?v ?r)
      (road-next ?r ?next)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
    )
    :effect (and
      (not (ready-road ?v ?r))
      (ready-road ?v ?next)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-road_0505-road_0517-tw_00150
    :parameters (?v - vehicle ?r - road_type_road_0505 ?next - road_type_road_0517 ?w - window_type_tw_00150)
    :precondition (and
      (ready-road ?v ?r)
      (road-next ?r ?next)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
    )
    :effect (and
      (not (ready-road ?v ?r))
      (ready-road ?v ?next)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-road_0505-road_0517-tw_00180
    :parameters (?v - vehicle ?r - road_type_road_0505 ?next - road_type_road_0517 ?w - window_type_tw_00180)
    :precondition (and
      (ready-road ?v ?r)
      (road-next ?r ?next)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
    )
    :effect (and
      (not (ready-road ?v ?r))
      (ready-road ?v ?next)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-road_0505-road_0517-tw_00210
    :parameters (?v - vehicle ?r - road_type_road_0505 ?next - road_type_road_0517 ?w - window_type_tw_00210)
    :precondition (and
      (ready-road ?v ?r)
      (road-next ?r ?next)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
    )
    :effect (and
      (not (ready-road ?v ?r))
      (ready-road ?v ?next)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-road_0505-road_0517-tw_00240
    :parameters (?v - vehicle ?r - road_type_road_0505 ?next - road_type_road_0517 ?w - window_type_tw_00240)
    :precondition (and
      (ready-road ?v ?r)
      (road-next ?r ?next)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
    )
    :effect (and
      (not (ready-road ?v ?r))
      (ready-road ?v ?next)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-road_0505-road_0517-tw_00270
    :parameters (?v - vehicle ?r - road_type_road_0505 ?next - road_type_road_0517 ?w - window_type_tw_00270)
    :precondition (and
      (ready-road ?v ?r)
      (road-next ?r ?next)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
    )
    :effect (and
      (not (ready-road ?v ?r))
      (ready-road ?v ?next)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-road_0505-road_0517-tw_00300
    :parameters (?v - vehicle ?r - road_type_road_0505 ?next - road_type_road_0517 ?w - window_type_tw_00300)
    :precondition (and
      (ready-road ?v ?r)
      (road-next ?r ?next)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
    )
    :effect (and
      (not (ready-road ?v ?r))
      (ready-road ?v ?next)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-road_0505-road_0517-tw_00330
    :parameters (?v - vehicle ?r - road_type_road_0505 ?next - road_type_road_0517 ?w - window_type_tw_00330)
    :precondition (and
      (ready-road ?v ?r)
      (road-next ?r ?next)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
    )
    :effect (and
      (not (ready-road ?v ?r))
      (ready-road ?v ?next)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-road_0505-road_0517-tw_00360
    :parameters (?v - vehicle ?r - road_type_road_0505 ?next - road_type_road_0517 ?w - window_type_tw_00360)
    :precondition (and
      (ready-road ?v ?r)
      (road-next ?r ?next)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
    )
    :effect (and
      (not (ready-road ?v ?r))
      (ready-road ?v ?next)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-road_0505-road_0517-tw_00390
    :parameters (?v - vehicle ?r - road_type_road_0505 ?next - road_type_road_0517 ?w - window_type_tw_00390)
    :precondition (and
      (ready-road ?v ?r)
      (road-next ?r ?next)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
    )
    :effect (and
      (not (ready-road ?v ?r))
      (ready-road ?v ?next)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-road_0505-road_0517-tw_00420
    :parameters (?v - vehicle ?r - road_type_road_0505 ?next - road_type_road_0517 ?w - window_type_tw_00420)
    :precondition (and
      (ready-road ?v ?r)
      (road-next ?r ?next)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
    )
    :effect (and
      (not (ready-road ?v ?r))
      (ready-road ?v ?next)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-road_0505-road_0517-tw_00450
    :parameters (?v - vehicle ?r - road_type_road_0505 ?next - road_type_road_0517 ?w - window_type_tw_00450)
    :precondition (and
      (ready-road ?v ?r)
      (road-next ?r ?next)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
    )
    :effect (and
      (not (ready-road ?v ?r))
      (ready-road ?v ?next)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-road_0505-road_0517-tw_00480
    :parameters (?v - vehicle ?r - road_type_road_0505 ?next - road_type_road_0517 ?w - window_type_tw_00480)
    :precondition (and
      (ready-road ?v ?r)
      (road-next ?r ?next)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
    )
    :effect (and
      (not (ready-road ?v ?r))
      (ready-road ?v ?next)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-road_0505-road_0517-tw_00510
    :parameters (?v - vehicle ?r - road_type_road_0505 ?next - road_type_road_0517 ?w - window_type_tw_00510)
    :precondition (and
      (ready-road ?v ?r)
      (road-next ?r ?next)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
    )
    :effect (and
      (not (ready-road ?v ?r))
      (ready-road ?v ?next)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-road_0505-road_0517-tw_00540
    :parameters (?v - vehicle ?r - road_type_road_0505 ?next - road_type_road_0517 ?w - window_type_tw_00540)
    :precondition (and
      (ready-road ?v ?r)
      (road-next ?r ?next)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
    )
    :effect (and
      (not (ready-road ?v ?r))
      (ready-road ?v ?next)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-road_0505-road_0517-tw_00570
    :parameters (?v - vehicle ?r - road_type_road_0505 ?next - road_type_road_0517 ?w - window_type_tw_00570)
    :precondition (and
      (ready-road ?v ?r)
      (road-next ?r ?next)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
    )
    :effect (and
      (not (ready-road ?v ?r))
      (ready-road ?v ?next)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-road_0505-road_0517-tw_00600
    :parameters (?v - vehicle ?r - road_type_road_0505 ?next - road_type_road_0517 ?w - window_type_tw_00600)
    :precondition (and
      (ready-road ?v ?r)
      (road-next ?r ?next)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
    )
    :effect (and
      (not (ready-road ?v ?r))
      (ready-road ?v ?next)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-static-road_0506-road_0513
    :parameters (?v - vehicle ?r - road_type_road_0506 ?next - road_type_road_0513)
    :precondition (and
      (ready-road ?v ?r)
      (road-next ?r ?next)
      (road-open ?r)
      (static-road ?r)
      (not (road-blocked ?r))
    )
    :effect (and
      (not (ready-road ?v ?r))
      (ready-road ?v ?next)
      (increase (travel-time ?v) (travel-duration ?r))
      (increase (sim-time) (travel-duration ?r))
    )
  )

  (:action traverse-road-static-road_0506-road_0514
    :parameters (?v - vehicle ?r - road_type_road_0506 ?next - road_type_road_0514)
    :precondition (and
      (ready-road ?v ?r)
      (road-next ?r ?next)
      (road-open ?r)
      (static-road ?r)
      (not (road-blocked ?r))
    )
    :effect (and
      (not (ready-road ?v ?r))
      (ready-road ?v ?next)
      (increase (travel-time ?v) (travel-duration ?r))
      (increase (sim-time) (travel-duration ?r))
    )
  )

  (:action traverse-road-static-road_0506-road_0515
    :parameters (?v - vehicle ?r - road_type_road_0506 ?next - road_type_road_0515)
    :precondition (and
      (ready-road ?v ?r)
      (road-next ?r ?next)
      (road-open ?r)
      (static-road ?r)
      (not (road-blocked ?r))
    )
    :effect (and
      (not (ready-road ?v ?r))
      (ready-road ?v ?next)
      (increase (travel-time ?v) (travel-duration ?r))
      (increase (sim-time) (travel-duration ?r))
    )
  )

  (:action traverse-road-static-road_0507-road_0553
    :parameters (?v - vehicle ?r - road_type_road_0507 ?next - road_type_road_0553)
    :precondition (and
      (ready-road ?v ?r)
      (road-next ?r ?next)
      (road-open ?r)
      (static-road ?r)
      (not (road-blocked ?r))
    )
    :effect (and
      (not (ready-road ?v ?r))
      (ready-road ?v ?next)
      (increase (travel-time ?v) (travel-duration ?r))
      (increase (sim-time) (travel-duration ?r))
    )
  )

  (:action traverse-road-static-road_0507-road_0554
    :parameters (?v - vehicle ?r - road_type_road_0507 ?next - road_type_road_0554)
    :precondition (and
      (ready-road ?v ?r)
      (road-next ?r ?next)
      (road-open ?r)
      (static-road ?r)
      (not (road-blocked ?r))
    )
    :effect (and
      (not (ready-road ?v ?r))
      (ready-road ?v ?next)
      (increase (travel-time ?v) (travel-duration ?r))
      (increase (sim-time) (travel-duration ?r))
    )
  )

  (:action traverse-road-static-road_0507-road_0555
    :parameters (?v - vehicle ?r - road_type_road_0507 ?next - road_type_road_0555)
    :precondition (and
      (ready-road ?v ?r)
      (road-next ?r ?next)
      (road-open ?r)
      (static-road ?r)
      (not (road-blocked ?r))
    )
    :effect (and
      (not (ready-road ?v ?r))
      (ready-road ?v ?next)
      (increase (travel-time ?v) (travel-duration ?r))
      (increase (sim-time) (travel-duration ?r))
    )
  )

  (:action traverse-road-static-road_0508-road_0505
    :parameters (?v - vehicle ?r - road_type_road_0508 ?next - road_type_road_0505)
    :precondition (and
      (ready-road ?v ?r)
      (road-next ?r ?next)
      (road-open ?r)
      (static-road ?r)
      (not (road-blocked ?r))
    )
    :effect (and
      (not (ready-road ?v ?r))
      (ready-road ?v ?next)
      (increase (travel-time ?v) (travel-duration ?r))
      (increase (sim-time) (travel-duration ?r))
    )
  )

  (:action traverse-road-static-road_0508-road_0506
    :parameters (?v - vehicle ?r - road_type_road_0508 ?next - road_type_road_0506)
    :precondition (and
      (ready-road ?v ?r)
      (road-next ?r ?next)
      (road-open ?r)
      (static-road ?r)
      (not (road-blocked ?r))
    )
    :effect (and
      (not (ready-road ?v ?r))
      (ready-road ?v ?next)
      (increase (travel-time ?v) (travel-duration ?r))
      (increase (sim-time) (travel-duration ?r))
    )
  )

  (:action traverse-road-static-road_0511-road_0553
    :parameters (?v - vehicle ?r - road_type_road_0511 ?next - road_type_road_0553)
    :precondition (and
      (ready-road ?v ?r)
      (road-next ?r ?next)
      (road-open ?r)
      (static-road ?r)
      (not (road-blocked ?r))
    )
    :effect (and
      (not (ready-road ?v ?r))
      (ready-road ?v ?next)
      (increase (travel-time ?v) (travel-duration ?r))
      (increase (sim-time) (travel-duration ?r))
    )
  )

  (:action traverse-road-static-road_0511-road_0554
    :parameters (?v - vehicle ?r - road_type_road_0511 ?next - road_type_road_0554)
    :precondition (and
      (ready-road ?v ?r)
      (road-next ?r ?next)
      (road-open ?r)
      (static-road ?r)
      (not (road-blocked ?r))
    )
    :effect (and
      (not (ready-road ?v ?r))
      (ready-road ?v ?next)
      (increase (travel-time ?v) (travel-duration ?r))
      (increase (sim-time) (travel-duration ?r))
    )
  )

  (:action traverse-road-static-road_0511-road_0555
    :parameters (?v - vehicle ?r - road_type_road_0511 ?next - road_type_road_0555)
    :precondition (and
      (ready-road ?v ?r)
      (road-next ?r ?next)
      (road-open ?r)
      (static-road ?r)
      (not (road-blocked ?r))
    )
    :effect (and
      (not (ready-road ?v ?r))
      (ready-road ?v ?next)
      (increase (travel-time ?v) (travel-duration ?r))
      (increase (sim-time) (travel-duration ?r))
    )
  )

  (:action traverse-road-static-road_0512-road_0532
    :parameters (?v - vehicle ?r - road_type_road_0512 ?next - road_type_road_0532)
    :precondition (and
      (ready-road ?v ?r)
      (road-next ?r ?next)
      (road-open ?r)
      (static-road ?r)
      (not (road-blocked ?r))
    )
    :effect (and
      (not (ready-road ?v ?r))
      (ready-road ?v ?next)
      (increase (travel-time ?v) (travel-duration ?r))
      (increase (sim-time) (travel-duration ?r))
    )
  )

  (:action traverse-road-static-road_0512-road_0533
    :parameters (?v - vehicle ?r - road_type_road_0512 ?next - road_type_road_0533)
    :precondition (and
      (ready-road ?v ?r)
      (road-next ?r ?next)
      (road-open ?r)
      (static-road ?r)
      (not (road-blocked ?r))
    )
    :effect (and
      (not (ready-road ?v ?r))
      (ready-road ?v ?next)
      (increase (travel-time ?v) (travel-duration ?r))
      (increase (sim-time) (travel-duration ?r))
    )
  )

  (:action traverse-road-static-road_0512-road_0534
    :parameters (?v - vehicle ?r - road_type_road_0512 ?next - road_type_road_0534)
    :precondition (and
      (ready-road ?v ?r)
      (road-next ?r ?next)
      (road-open ?r)
      (static-road ?r)
      (not (road-blocked ?r))
    )
    :effect (and
      (not (ready-road ?v ?r))
      (ready-road ?v ?next)
      (increase (travel-time ?v) (travel-duration ?r))
      (increase (sim-time) (travel-duration ?r))
    )
  )

  (:action traverse-road-static-road_0513-road_0521
    :parameters (?v - vehicle ?r - road_type_road_0513 ?next - road_type_road_0521)
    :precondition (and
      (ready-road ?v ?r)
      (road-next ?r ?next)
      (road-open ?r)
      (static-road ?r)
      (not (road-blocked ?r))
    )
    :effect (and
      (not (ready-road ?v ?r))
      (ready-road ?v ?next)
      (increase (travel-time ?v) (travel-duration ?r))
      (increase (sim-time) (travel-duration ?r))
    )
  )

  (:action traverse-road-static-road_0513-road_0522
    :parameters (?v - vehicle ?r - road_type_road_0513 ?next - road_type_road_0522)
    :precondition (and
      (ready-road ?v ?r)
      (road-next ?r ?next)
      (road-open ?r)
      (static-road ?r)
      (not (road-blocked ?r))
    )
    :effect (and
      (not (ready-road ?v ?r))
      (ready-road ?v ?next)
      (increase (travel-time ?v) (travel-duration ?r))
      (increase (sim-time) (travel-duration ?r))
    )
  )

  (:action traverse-road-static-road_0514-road_0511
    :parameters (?v - vehicle ?r - road_type_road_0514 ?next - road_type_road_0511)
    :precondition (and
      (ready-road ?v ?r)
      (road-next ?r ?next)
      (road-open ?r)
      (static-road ?r)
      (not (road-blocked ?r))
    )
    :effect (and
      (not (ready-road ?v ?r))
      (ready-road ?v ?next)
      (increase (travel-time ?v) (travel-duration ?r))
      (increase (sim-time) (travel-duration ?r))
    )
  )

  (:action traverse-road-static-road_0514-road_0512
    :parameters (?v - vehicle ?r - road_type_road_0514 ?next - road_type_road_0512)
    :precondition (and
      (ready-road ?v ?r)
      (road-next ?r ?next)
      (road-open ?r)
      (static-road ?r)
      (not (road-blocked ?r))
    )
    :effect (and
      (not (ready-road ?v ?r))
      (ready-road ?v ?next)
      (increase (travel-time ?v) (travel-duration ?r))
      (increase (sim-time) (travel-duration ?r))
    )
  )

  (:action traverse-road-static-road_0515-road_0505
    :parameters (?v - vehicle ?r - road_type_road_0515 ?next - road_type_road_0505)
    :precondition (and
      (ready-road ?v ?r)
      (road-next ?r ?next)
      (road-open ?r)
      (static-road ?r)
      (not (road-blocked ?r))
    )
    :effect (and
      (not (ready-road ?v ?r))
      (ready-road ?v ?next)
      (increase (travel-time ?v) (travel-duration ?r))
      (increase (sim-time) (travel-duration ?r))
    )
  )

  (:action traverse-road-static-road_0515-road_0506
    :parameters (?v - vehicle ?r - road_type_road_0515 ?next - road_type_road_0506)
    :precondition (and
      (ready-road ?v ?r)
      (road-next ?r ?next)
      (road-open ?r)
      (static-road ?r)
      (not (road-blocked ?r))
    )
    :effect (and
      (not (ready-road ?v ?r))
      (ready-road ?v ?next)
      (increase (travel-time ?v) (travel-duration ?r))
      (increase (sim-time) (travel-duration ?r))
    )
  )

  (:action traverse-road-static-road_0516-road_0437
    :parameters (?v - vehicle ?r - road_type_road_0516 ?next - road_type_road_0437)
    :precondition (and
      (ready-road ?v ?r)
      (road-next ?r ?next)
      (road-open ?r)
      (static-road ?r)
      (not (road-blocked ?r))
    )
    :effect (and
      (not (ready-road ?v ?r))
      (ready-road ?v ?next)
      (increase (travel-time ?v) (travel-duration ?r))
      (increase (sim-time) (travel-duration ?r))
    )
  )

  (:action traverse-road-static-road_0517-road_0340
    :parameters (?v - vehicle ?r - road_type_road_0517 ?next - road_type_road_0340)
    :precondition (and
      (ready-road ?v ?r)
      (road-next ?r ?next)
      (road-open ?r)
      (static-road ?r)
      (not (road-blocked ?r))
    )
    :effect (and
      (not (ready-road ?v ?r))
      (ready-road ?v ?next)
      (increase (travel-time ?v) (travel-duration ?r))
      (increase (sim-time) (travel-duration ?r))
    )
  )

  (:action traverse-road-static-road_0518-macro_0049
    :parameters (?v - vehicle ?r - road_type_road_0518 ?next - road_type_macro_0049)
    :precondition (and
      (ready-road ?v ?r)
      (road-next ?r ?next)
      (road-open ?r)
      (static-road ?r)
      (not (road-blocked ?r))
    )
    :effect (and
      (not (ready-road ?v ?r))
      (ready-road ?v ?next)
      (increase (travel-time ?v) (travel-duration ?r))
      (increase (sim-time) (travel-duration ?r))
    )
  )

  (:action traverse-road-static-road_0518-road_0738
    :parameters (?v - vehicle ?r - road_type_road_0518 ?next - road_type_road_0738)
    :precondition (and
      (ready-road ?v ?r)
      (road-next ?r ?next)
      (road-open ?r)
      (static-road ?r)
      (not (road-blocked ?r))
    )
    :effect (and
      (not (ready-road ?v ?r))
      (ready-road ?v ?next)
      (increase (travel-time ?v) (travel-duration ?r))
      (increase (sim-time) (travel-duration ?r))
    )
  )

  (:action traverse-road-static-road_0519-road_0730
    :parameters (?v - vehicle ?r - road_type_road_0519 ?next - road_type_road_0730)
    :precondition (and
      (ready-road ?v ?r)
      (road-next ?r ?next)
      (road-open ?r)
      (static-road ?r)
      (not (road-blocked ?r))
    )
    :effect (and
      (not (ready-road ?v ?r))
      (ready-road ?v ?next)
      (increase (travel-time ?v) (travel-duration ?r))
      (increase (sim-time) (travel-duration ?r))
    )
  )

  (:action traverse-road-static-road_0520-road_0393
    :parameters (?v - vehicle ?r - road_type_road_0520 ?next - road_type_road_0393)
    :precondition (and
      (ready-road ?v ?r)
      (road-next ?r ?next)
      (road-open ?r)
      (static-road ?r)
      (not (road-blocked ?r))
    )
    :effect (and
      (not (ready-road ?v ?r))
      (ready-road ?v ?next)
      (increase (travel-time ?v) (travel-duration ?r))
      (increase (sim-time) (travel-duration ?r))
    )
  )

  (:action traverse-road-static-road_0520-road_0394
    :parameters (?v - vehicle ?r - road_type_road_0520 ?next - road_type_road_0394)
    :precondition (and
      (ready-road ?v ?r)
      (road-next ?r ?next)
      (road-open ?r)
      (static-road ?r)
      (not (road-blocked ?r))
    )
    :effect (and
      (not (ready-road ?v ?r))
      (ready-road ?v ?next)
      (increase (travel-time ?v) (travel-duration ?r))
      (increase (sim-time) (travel-duration ?r))
    )
  )

  (:action traverse-road-static-road_0521-road_0513
    :parameters (?v - vehicle ?r - road_type_road_0521 ?next - road_type_road_0513)
    :precondition (and
      (ready-road ?v ?r)
      (road-next ?r ?next)
      (road-open ?r)
      (static-road ?r)
      (not (road-blocked ?r))
    )
    :effect (and
      (not (ready-road ?v ?r))
      (ready-road ?v ?next)
      (increase (travel-time ?v) (travel-duration ?r))
      (increase (sim-time) (travel-duration ?r))
    )
  )

  (:action traverse-road-static-road_0521-road_0514
    :parameters (?v - vehicle ?r - road_type_road_0521 ?next - road_type_road_0514)
    :precondition (and
      (ready-road ?v ?r)
      (road-next ?r ?next)
      (road-open ?r)
      (static-road ?r)
      (not (road-blocked ?r))
    )
    :effect (and
      (not (ready-road ?v ?r))
      (ready-road ?v ?next)
      (increase (travel-time ?v) (travel-duration ?r))
      (increase (sim-time) (travel-duration ?r))
    )
  )

  (:action traverse-road-static-road_0521-road_0515
    :parameters (?v - vehicle ?r - road_type_road_0521 ?next - road_type_road_0515)
    :precondition (and
      (ready-road ?v ?r)
      (road-next ?r ?next)
      (road-open ?r)
      (static-road ?r)
      (not (road-blocked ?r))
    )
    :effect (and
      (not (ready-road ?v ?r))
      (ready-road ?v ?next)
      (increase (travel-time ?v) (travel-duration ?r))
      (increase (sim-time) (travel-duration ?r))
    )
  )

  (:action traverse-road-static-road_0522-road_0532
    :parameters (?v - vehicle ?r - road_type_road_0522 ?next - road_type_road_0532)
    :precondition (and
      (ready-road ?v ?r)
      (road-next ?r ?next)
      (road-open ?r)
      (static-road ?r)
      (not (road-blocked ?r))
    )
    :effect (and
      (not (ready-road ?v ?r))
      (ready-road ?v ?next)
      (increase (travel-time ?v) (travel-duration ?r))
      (increase (sim-time) (travel-duration ?r))
    )
  )

  (:action traverse-road-static-road_0522-road_0533
    :parameters (?v - vehicle ?r - road_type_road_0522 ?next - road_type_road_0533)
    :precondition (and
      (ready-road ?v ?r)
      (road-next ?r ?next)
      (road-open ?r)
      (static-road ?r)
      (not (road-blocked ?r))
    )
    :effect (and
      (not (ready-road ?v ?r))
      (ready-road ?v ?next)
      (increase (travel-time ?v) (travel-duration ?r))
      (increase (sim-time) (travel-duration ?r))
    )
  )

  (:action traverse-road-static-road_0522-road_0534
    :parameters (?v - vehicle ?r - road_type_road_0522 ?next - road_type_road_0534)
    :precondition (and
      (ready-road ?v ?r)
      (road-next ?r ?next)
      (road-open ?r)
      (static-road ?r)
      (not (road-blocked ?r))
    )
    :effect (and
      (not (ready-road ?v ?r))
      (ready-road ?v ?next)
      (increase (travel-time ?v) (travel-duration ?r))
      (increase (sim-time) (travel-duration ?r))
    )
  )

  (:action traverse-road-static-road_0523-road_0152
    :parameters (?v - vehicle ?r - road_type_road_0523 ?next - road_type_road_0152)
    :precondition (and
      (ready-road ?v ?r)
      (road-next ?r ?next)
      (road-open ?r)
      (static-road ?r)
      (not (road-blocked ?r))
    )
    :effect (and
      (not (ready-road ?v ?r))
      (ready-road ?v ?next)
      (increase (travel-time ?v) (travel-duration ?r))
      (increase (sim-time) (travel-duration ?r))
    )
  )

  (:action traverse-road-static-road_0523-road_0153
    :parameters (?v - vehicle ?r - road_type_road_0523 ?next - road_type_road_0153)
    :precondition (and
      (ready-road ?v ?r)
      (road-next ?r ?next)
      (road-open ?r)
      (static-road ?r)
      (not (road-blocked ?r))
    )
    :effect (and
      (not (ready-road ?v ?r))
      (ready-road ?v ?next)
      (increase (travel-time ?v) (travel-duration ?r))
      (increase (sim-time) (travel-duration ?r))
    )
  )

  (:action traverse-road-static-road_0523-road_0154
    :parameters (?v - vehicle ?r - road_type_road_0523 ?next - road_type_road_0154)
    :precondition (and
      (ready-road ?v ?r)
      (road-next ?r ?next)
      (road-open ?r)
      (static-road ?r)
      (not (road-blocked ?r))
    )
    :effect (and
      (not (ready-road ?v ?r))
      (ready-road ?v ?next)
      (increase (travel-time ?v) (travel-duration ?r))
      (increase (sim-time) (travel-duration ?r))
    )
  )

  (:action traverse-road-static-road_0523-road_0155
    :parameters (?v - vehicle ?r - road_type_road_0523 ?next - road_type_road_0155)
    :precondition (and
      (ready-road ?v ?r)
      (road-next ?r ?next)
      (road-open ?r)
      (static-road ?r)
      (not (road-blocked ?r))
    )
    :effect (and
      (not (ready-road ?v ?r))
      (ready-road ?v ?next)
      (increase (travel-time ?v) (travel-duration ?r))
      (increase (sim-time) (travel-duration ?r))
    )
  )

  (:action traverse-road-static-road_0524-road_0521
    :parameters (?v - vehicle ?r - road_type_road_0524 ?next - road_type_road_0521)
    :precondition (and
      (ready-road ?v ?r)
      (road-next ?r ?next)
      (road-open ?r)
      (static-road ?r)
      (not (road-blocked ?r))
    )
    :effect (and
      (not (ready-road ?v ?r))
      (ready-road ?v ?next)
      (increase (travel-time ?v) (travel-duration ?r))
      (increase (sim-time) (travel-duration ?r))
    )
  )

  (:action traverse-road-static-road_0524-road_0522
    :parameters (?v - vehicle ?r - road_type_road_0524 ?next - road_type_road_0522)
    :precondition (and
      (ready-road ?v ?r)
      (road-next ?r ?next)
      (road-open ?r)
      (static-road ?r)
      (not (road-blocked ?r))
    )
    :effect (and
      (not (ready-road ?v ?r))
      (ready-road ?v ?next)
      (increase (travel-time ?v) (travel-duration ?r))
      (increase (sim-time) (travel-duration ?r))
    )
  )

  (:action traverse-road-static-road_0525-road_0686
    :parameters (?v - vehicle ?r - road_type_road_0525 ?next - road_type_road_0686)
    :precondition (and
      (ready-road ?v ?r)
      (road-next ?r ?next)
      (road-open ?r)
      (static-road ?r)
      (not (road-blocked ?r))
    )
    :effect (and
      (not (ready-road ?v ?r))
      (ready-road ?v ?next)
      (increase (travel-time ?v) (travel-duration ?r))
      (increase (sim-time) (travel-duration ?r))
    )
  )

  (:action traverse-road-static-road_0525-road_0687
    :parameters (?v - vehicle ?r - road_type_road_0525 ?next - road_type_road_0687)
    :precondition (and
      (ready-road ?v ?r)
      (road-next ?r ?next)
      (road-open ?r)
      (static-road ?r)
      (not (road-blocked ?r))
    )
    :effect (and
      (not (ready-road ?v ?r))
      (ready-road ?v ?next)
      (increase (travel-time ?v) (travel-duration ?r))
      (increase (sim-time) (travel-duration ?r))
    )
  )

  (:action traverse-road-static-road_0525-road_0688
    :parameters (?v - vehicle ?r - road_type_road_0525 ?next - road_type_road_0688)
    :precondition (and
      (ready-road ?v ?r)
      (road-next ?r ?next)
      (road-open ?r)
      (static-road ?r)
      (not (road-blocked ?r))
    )
    :effect (and
      (not (ready-road ?v ?r))
      (ready-road ?v ?next)
      (increase (travel-time ?v) (travel-duration ?r))
      (increase (sim-time) (travel-duration ?r))
    )
  )

  (:action traverse-road-static-road_0526-macro_0032
    :parameters (?v - vehicle ?r - road_type_road_0526 ?next - road_type_macro_0032)
    :precondition (and
      (ready-road ?v ?r)
      (road-next ?r ?next)
      (road-open ?r)
      (static-road ?r)
      (not (road-blocked ?r))
    )
    :effect (and
      (not (ready-road ?v ?r))
      (ready-road ?v ?next)
      (increase (travel-time ?v) (travel-duration ?r))
      (increase (sim-time) (travel-duration ?r))
    )
  )

  (:action traverse-road-static-road_0526-road_0416
    :parameters (?v - vehicle ?r - road_type_road_0526 ?next - road_type_road_0416)
    :precondition (and
      (ready-road ?v ?r)
      (road-next ?r ?next)
      (road-open ?r)
      (static-road ?r)
      (not (road-blocked ?r))
    )
    :effect (and
      (not (ready-road ?v ?r))
      (ready-road ?v ?next)
      (increase (travel-time ?v) (travel-duration ?r))
      (increase (sim-time) (travel-duration ?r))
    )
  )

  (:action traverse-road-static-road_0526-road_0417
    :parameters (?v - vehicle ?r - road_type_road_0526 ?next - road_type_road_0417)
    :precondition (and
      (ready-road ?v ?r)
      (road-next ?r ?next)
      (road-open ?r)
      (static-road ?r)
      (not (road-blocked ?r))
    )
    :effect (and
      (not (ready-road ?v ?r))
      (ready-road ?v ?next)
      (increase (travel-time ?v) (travel-duration ?r))
      (increase (sim-time) (travel-duration ?r))
    )
  )

  (:action traverse-road-static-road_0527-macro_0014
    :parameters (?v - vehicle ?r - road_type_road_0527 ?next - road_type_macro_0014)
    :precondition (and
      (ready-road ?v ?r)
      (road-next ?r ?next)
      (road-open ?r)
      (static-road ?r)
      (not (road-blocked ?r))
    )
    :effect (and
      (not (ready-road ?v ?r))
      (ready-road ?v ?next)
      (increase (travel-time ?v) (travel-duration ?r))
      (increase (sim-time) (travel-duration ?r))
    )
  )

  (:action traverse-road-static-road_0527-macro_0015
    :parameters (?v - vehicle ?r - road_type_road_0527 ?next - road_type_macro_0015)
    :precondition (and
      (ready-road ?v ?r)
      (road-next ?r ?next)
      (road-open ?r)
      (static-road ?r)
      (not (road-blocked ?r))
    )
    :effect (and
      (not (ready-road ?v ?r))
      (ready-road ?v ?next)
      (increase (travel-time ?v) (travel-duration ?r))
      (increase (sim-time) (travel-duration ?r))
    )
  )

  (:action traverse-road-dynamic-road_0529-road_0027-tw_00000
    :parameters (?v - vehicle ?r - road_type_road_0529 ?next - road_type_road_0027 ?w - window_type_tw_00000)
    :precondition (and
      (ready-road ?v ?r)
      (road-next ?r ?next)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
    )
    :effect (and
      (not (ready-road ?v ?r))
      (ready-road ?v ?next)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-road_0529-road_0027-tw_00030
    :parameters (?v - vehicle ?r - road_type_road_0529 ?next - road_type_road_0027 ?w - window_type_tw_00030)
    :precondition (and
      (ready-road ?v ?r)
      (road-next ?r ?next)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
    )
    :effect (and
      (not (ready-road ?v ?r))
      (ready-road ?v ?next)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-road_0529-road_0027-tw_00060
    :parameters (?v - vehicle ?r - road_type_road_0529 ?next - road_type_road_0027 ?w - window_type_tw_00060)
    :precondition (and
      (ready-road ?v ?r)
      (road-next ?r ?next)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
    )
    :effect (and
      (not (ready-road ?v ?r))
      (ready-road ?v ?next)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-road_0529-road_0027-tw_00090
    :parameters (?v - vehicle ?r - road_type_road_0529 ?next - road_type_road_0027 ?w - window_type_tw_00090)
    :precondition (and
      (ready-road ?v ?r)
      (road-next ?r ?next)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
    )
    :effect (and
      (not (ready-road ?v ?r))
      (ready-road ?v ?next)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-road_0529-road_0027-tw_00120
    :parameters (?v - vehicle ?r - road_type_road_0529 ?next - road_type_road_0027 ?w - window_type_tw_00120)
    :precondition (and
      (ready-road ?v ?r)
      (road-next ?r ?next)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
    )
    :effect (and
      (not (ready-road ?v ?r))
      (ready-road ?v ?next)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-road_0529-road_0027-tw_00150
    :parameters (?v - vehicle ?r - road_type_road_0529 ?next - road_type_road_0027 ?w - window_type_tw_00150)
    :precondition (and
      (ready-road ?v ?r)
      (road-next ?r ?next)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
    )
    :effect (and
      (not (ready-road ?v ?r))
      (ready-road ?v ?next)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-road_0529-road_0027-tw_00180
    :parameters (?v - vehicle ?r - road_type_road_0529 ?next - road_type_road_0027 ?w - window_type_tw_00180)
    :precondition (and
      (ready-road ?v ?r)
      (road-next ?r ?next)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
    )
    :effect (and
      (not (ready-road ?v ?r))
      (ready-road ?v ?next)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-road_0529-road_0027-tw_00210
    :parameters (?v - vehicle ?r - road_type_road_0529 ?next - road_type_road_0027 ?w - window_type_tw_00210)
    :precondition (and
      (ready-road ?v ?r)
      (road-next ?r ?next)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
    )
    :effect (and
      (not (ready-road ?v ?r))
      (ready-road ?v ?next)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-road_0529-road_0027-tw_00240
    :parameters (?v - vehicle ?r - road_type_road_0529 ?next - road_type_road_0027 ?w - window_type_tw_00240)
    :precondition (and
      (ready-road ?v ?r)
      (road-next ?r ?next)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
    )
    :effect (and
      (not (ready-road ?v ?r))
      (ready-road ?v ?next)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-road_0529-road_0027-tw_00270
    :parameters (?v - vehicle ?r - road_type_road_0529 ?next - road_type_road_0027 ?w - window_type_tw_00270)
    :precondition (and
      (ready-road ?v ?r)
      (road-next ?r ?next)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
    )
    :effect (and
      (not (ready-road ?v ?r))
      (ready-road ?v ?next)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-road_0529-road_0027-tw_00300
    :parameters (?v - vehicle ?r - road_type_road_0529 ?next - road_type_road_0027 ?w - window_type_tw_00300)
    :precondition (and
      (ready-road ?v ?r)
      (road-next ?r ?next)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
    )
    :effect (and
      (not (ready-road ?v ?r))
      (ready-road ?v ?next)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-road_0529-road_0027-tw_00330
    :parameters (?v - vehicle ?r - road_type_road_0529 ?next - road_type_road_0027 ?w - window_type_tw_00330)
    :precondition (and
      (ready-road ?v ?r)
      (road-next ?r ?next)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
    )
    :effect (and
      (not (ready-road ?v ?r))
      (ready-road ?v ?next)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-road_0529-road_0027-tw_00360
    :parameters (?v - vehicle ?r - road_type_road_0529 ?next - road_type_road_0027 ?w - window_type_tw_00360)
    :precondition (and
      (ready-road ?v ?r)
      (road-next ?r ?next)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
    )
    :effect (and
      (not (ready-road ?v ?r))
      (ready-road ?v ?next)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-road_0529-road_0027-tw_00390
    :parameters (?v - vehicle ?r - road_type_road_0529 ?next - road_type_road_0027 ?w - window_type_tw_00390)
    :precondition (and
      (ready-road ?v ?r)
      (road-next ?r ?next)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
    )
    :effect (and
      (not (ready-road ?v ?r))
      (ready-road ?v ?next)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-road_0529-road_0027-tw_00420
    :parameters (?v - vehicle ?r - road_type_road_0529 ?next - road_type_road_0027 ?w - window_type_tw_00420)
    :precondition (and
      (ready-road ?v ?r)
      (road-next ?r ?next)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
    )
    :effect (and
      (not (ready-road ?v ?r))
      (ready-road ?v ?next)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-road_0529-road_0027-tw_00450
    :parameters (?v - vehicle ?r - road_type_road_0529 ?next - road_type_road_0027 ?w - window_type_tw_00450)
    :precondition (and
      (ready-road ?v ?r)
      (road-next ?r ?next)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
    )
    :effect (and
      (not (ready-road ?v ?r))
      (ready-road ?v ?next)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-road_0529-road_0027-tw_00480
    :parameters (?v - vehicle ?r - road_type_road_0529 ?next - road_type_road_0027 ?w - window_type_tw_00480)
    :precondition (and
      (ready-road ?v ?r)
      (road-next ?r ?next)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
    )
    :effect (and
      (not (ready-road ?v ?r))
      (ready-road ?v ?next)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-road_0529-road_0027-tw_00510
    :parameters (?v - vehicle ?r - road_type_road_0529 ?next - road_type_road_0027 ?w - window_type_tw_00510)
    :precondition (and
      (ready-road ?v ?r)
      (road-next ?r ?next)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
    )
    :effect (and
      (not (ready-road ?v ?r))
      (ready-road ?v ?next)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-road_0529-road_0027-tw_00540
    :parameters (?v - vehicle ?r - road_type_road_0529 ?next - road_type_road_0027 ?w - window_type_tw_00540)
    :precondition (and
      (ready-road ?v ?r)
      (road-next ?r ?next)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
    )
    :effect (and
      (not (ready-road ?v ?r))
      (ready-road ?v ?next)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-road_0529-road_0027-tw_00570
    :parameters (?v - vehicle ?r - road_type_road_0529 ?next - road_type_road_0027 ?w - window_type_tw_00570)
    :precondition (and
      (ready-road ?v ?r)
      (road-next ?r ?next)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
    )
    :effect (and
      (not (ready-road ?v ?r))
      (ready-road ?v ?next)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-road_0529-road_0027-tw_00600
    :parameters (?v - vehicle ?r - road_type_road_0529 ?next - road_type_road_0027 ?w - window_type_tw_00600)
    :precondition (and
      (ready-road ?v ?r)
      (road-next ?r ?next)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
    )
    :effect (and
      (not (ready-road ?v ?r))
      (ready-road ?v ?next)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-static-road_0530-road_0333
    :parameters (?v - vehicle ?r - road_type_road_0530 ?next - road_type_road_0333)
    :precondition (and
      (ready-road ?v ?r)
      (road-next ?r ?next)
      (road-open ?r)
      (static-road ?r)
      (not (road-blocked ?r))
    )
    :effect (and
      (not (ready-road ?v ?r))
      (ready-road ?v ?next)
      (increase (travel-time ?v) (travel-duration ?r))
      (increase (sim-time) (travel-duration ?r))
    )
  )

  (:action traverse-road-static-road_0530-road_0334
    :parameters (?v - vehicle ?r - road_type_road_0530 ?next - road_type_road_0334)
    :precondition (and
      (ready-road ?v ?r)
      (road-next ?r ?next)
      (road-open ?r)
      (static-road ?r)
      (not (road-blocked ?r))
    )
    :effect (and
      (not (ready-road ?v ?r))
      (ready-road ?v ?next)
      (increase (travel-time ?v) (travel-duration ?r))
      (increase (sim-time) (travel-duration ?r))
    )
  )

  (:action traverse-road-static-road_0530-road_0335
    :parameters (?v - vehicle ?r - road_type_road_0530 ?next - road_type_road_0335)
    :precondition (and
      (ready-road ?v ?r)
      (road-next ?r ?next)
      (road-open ?r)
      (static-road ?r)
      (not (road-blocked ?r))
    )
    :effect (and
      (not (ready-road ?v ?r))
      (ready-road ?v ?next)
      (increase (travel-time ?v) (travel-duration ?r))
      (increase (sim-time) (travel-duration ?r))
    )
  )

  (:action traverse-road-static-road_0532-road_0152
    :parameters (?v - vehicle ?r - road_type_road_0532 ?next - road_type_road_0152)
    :precondition (and
      (ready-road ?v ?r)
      (road-next ?r ?next)
      (road-open ?r)
      (static-road ?r)
      (not (road-blocked ?r))
    )
    :effect (and
      (not (ready-road ?v ?r))
      (ready-road ?v ?next)
      (increase (travel-time ?v) (travel-duration ?r))
      (increase (sim-time) (travel-duration ?r))
    )
  )

  (:action traverse-road-static-road_0532-road_0153
    :parameters (?v - vehicle ?r - road_type_road_0532 ?next - road_type_road_0153)
    :precondition (and
      (ready-road ?v ?r)
      (road-next ?r ?next)
      (road-open ?r)
      (static-road ?r)
      (not (road-blocked ?r))
    )
    :effect (and
      (not (ready-road ?v ?r))
      (ready-road ?v ?next)
      (increase (travel-time ?v) (travel-duration ?r))
      (increase (sim-time) (travel-duration ?r))
    )
  )

  (:action traverse-road-static-road_0532-road_0154
    :parameters (?v - vehicle ?r - road_type_road_0532 ?next - road_type_road_0154)
    :precondition (and
      (ready-road ?v ?r)
      (road-next ?r ?next)
      (road-open ?r)
      (static-road ?r)
      (not (road-blocked ?r))
    )
    :effect (and
      (not (ready-road ?v ?r))
      (ready-road ?v ?next)
      (increase (travel-time ?v) (travel-duration ?r))
      (increase (sim-time) (travel-duration ?r))
    )
  )

  (:action traverse-road-static-road_0532-road_0155
    :parameters (?v - vehicle ?r - road_type_road_0532 ?next - road_type_road_0155)
    :precondition (and
      (ready-road ?v ?r)
      (road-next ?r ?next)
      (road-open ?r)
      (static-road ?r)
      (not (road-blocked ?r))
    )
    :effect (and
      (not (ready-road ?v ?r))
      (ready-road ?v ?next)
      (increase (travel-time ?v) (travel-duration ?r))
      (increase (sim-time) (travel-duration ?r))
    )
  )

  (:action traverse-road-static-road_0533-road_0511
    :parameters (?v - vehicle ?r - road_type_road_0533 ?next - road_type_road_0511)
    :precondition (and
      (ready-road ?v ?r)
      (road-next ?r ?next)
      (road-open ?r)
      (static-road ?r)
      (not (road-blocked ?r))
    )
    :effect (and
      (not (ready-road ?v ?r))
      (ready-road ?v ?next)
      (increase (travel-time ?v) (travel-duration ?r))
      (increase (sim-time) (travel-duration ?r))
    )
  )

  (:action traverse-road-static-road_0533-road_0512
    :parameters (?v - vehicle ?r - road_type_road_0533 ?next - road_type_road_0512)
    :precondition (and
      (ready-road ?v ?r)
      (road-next ?r ?next)
      (road-open ?r)
      (static-road ?r)
      (not (road-blocked ?r))
    )
    :effect (and
      (not (ready-road ?v ?r))
      (ready-road ?v ?next)
      (increase (travel-time ?v) (travel-duration ?r))
      (increase (sim-time) (travel-duration ?r))
    )
  )

  (:action traverse-road-dynamic-road_0534-road_0539-tw_00000
    :parameters (?v - vehicle ?r - road_type_road_0534 ?next - road_type_road_0539 ?w - window_type_tw_00000)
    :precondition (and
      (ready-road ?v ?r)
      (road-next ?r ?next)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
    )
    :effect (and
      (not (ready-road ?v ?r))
      (ready-road ?v ?next)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-road_0534-road_0539-tw_00030
    :parameters (?v - vehicle ?r - road_type_road_0534 ?next - road_type_road_0539 ?w - window_type_tw_00030)
    :precondition (and
      (ready-road ?v ?r)
      (road-next ?r ?next)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
    )
    :effect (and
      (not (ready-road ?v ?r))
      (ready-road ?v ?next)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-road_0534-road_0539-tw_00060
    :parameters (?v - vehicle ?r - road_type_road_0534 ?next - road_type_road_0539 ?w - window_type_tw_00060)
    :precondition (and
      (ready-road ?v ?r)
      (road-next ?r ?next)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
    )
    :effect (and
      (not (ready-road ?v ?r))
      (ready-road ?v ?next)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-road_0534-road_0539-tw_00090
    :parameters (?v - vehicle ?r - road_type_road_0534 ?next - road_type_road_0539 ?w - window_type_tw_00090)
    :precondition (and
      (ready-road ?v ?r)
      (road-next ?r ?next)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
    )
    :effect (and
      (not (ready-road ?v ?r))
      (ready-road ?v ?next)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-road_0534-road_0539-tw_00120
    :parameters (?v - vehicle ?r - road_type_road_0534 ?next - road_type_road_0539 ?w - window_type_tw_00120)
    :precondition (and
      (ready-road ?v ?r)
      (road-next ?r ?next)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
    )
    :effect (and
      (not (ready-road ?v ?r))
      (ready-road ?v ?next)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-road_0534-road_0539-tw_00150
    :parameters (?v - vehicle ?r - road_type_road_0534 ?next - road_type_road_0539 ?w - window_type_tw_00150)
    :precondition (and
      (ready-road ?v ?r)
      (road-next ?r ?next)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
    )
    :effect (and
      (not (ready-road ?v ?r))
      (ready-road ?v ?next)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-road_0534-road_0539-tw_00180
    :parameters (?v - vehicle ?r - road_type_road_0534 ?next - road_type_road_0539 ?w - window_type_tw_00180)
    :precondition (and
      (ready-road ?v ?r)
      (road-next ?r ?next)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
    )
    :effect (and
      (not (ready-road ?v ?r))
      (ready-road ?v ?next)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-road_0534-road_0539-tw_00210
    :parameters (?v - vehicle ?r - road_type_road_0534 ?next - road_type_road_0539 ?w - window_type_tw_00210)
    :precondition (and
      (ready-road ?v ?r)
      (road-next ?r ?next)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
    )
    :effect (and
      (not (ready-road ?v ?r))
      (ready-road ?v ?next)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-road_0534-road_0539-tw_00240
    :parameters (?v - vehicle ?r - road_type_road_0534 ?next - road_type_road_0539 ?w - window_type_tw_00240)
    :precondition (and
      (ready-road ?v ?r)
      (road-next ?r ?next)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
    )
    :effect (and
      (not (ready-road ?v ?r))
      (ready-road ?v ?next)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-road_0534-road_0539-tw_00270
    :parameters (?v - vehicle ?r - road_type_road_0534 ?next - road_type_road_0539 ?w - window_type_tw_00270)
    :precondition (and
      (ready-road ?v ?r)
      (road-next ?r ?next)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
    )
    :effect (and
      (not (ready-road ?v ?r))
      (ready-road ?v ?next)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-road_0534-road_0539-tw_00300
    :parameters (?v - vehicle ?r - road_type_road_0534 ?next - road_type_road_0539 ?w - window_type_tw_00300)
    :precondition (and
      (ready-road ?v ?r)
      (road-next ?r ?next)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
    )
    :effect (and
      (not (ready-road ?v ?r))
      (ready-road ?v ?next)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-road_0534-road_0539-tw_00330
    :parameters (?v - vehicle ?r - road_type_road_0534 ?next - road_type_road_0539 ?w - window_type_tw_00330)
    :precondition (and
      (ready-road ?v ?r)
      (road-next ?r ?next)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
    )
    :effect (and
      (not (ready-road ?v ?r))
      (ready-road ?v ?next)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-road_0534-road_0539-tw_00360
    :parameters (?v - vehicle ?r - road_type_road_0534 ?next - road_type_road_0539 ?w - window_type_tw_00360)
    :precondition (and
      (ready-road ?v ?r)
      (road-next ?r ?next)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
    )
    :effect (and
      (not (ready-road ?v ?r))
      (ready-road ?v ?next)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-road_0534-road_0539-tw_00390
    :parameters (?v - vehicle ?r - road_type_road_0534 ?next - road_type_road_0539 ?w - window_type_tw_00390)
    :precondition (and
      (ready-road ?v ?r)
      (road-next ?r ?next)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
    )
    :effect (and
      (not (ready-road ?v ?r))
      (ready-road ?v ?next)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-road_0534-road_0539-tw_00420
    :parameters (?v - vehicle ?r - road_type_road_0534 ?next - road_type_road_0539 ?w - window_type_tw_00420)
    :precondition (and
      (ready-road ?v ?r)
      (road-next ?r ?next)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
    )
    :effect (and
      (not (ready-road ?v ?r))
      (ready-road ?v ?next)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-road_0534-road_0539-tw_00450
    :parameters (?v - vehicle ?r - road_type_road_0534 ?next - road_type_road_0539 ?w - window_type_tw_00450)
    :precondition (and
      (ready-road ?v ?r)
      (road-next ?r ?next)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
    )
    :effect (and
      (not (ready-road ?v ?r))
      (ready-road ?v ?next)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-road_0534-road_0539-tw_00480
    :parameters (?v - vehicle ?r - road_type_road_0534 ?next - road_type_road_0539 ?w - window_type_tw_00480)
    :precondition (and
      (ready-road ?v ?r)
      (road-next ?r ?next)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
    )
    :effect (and
      (not (ready-road ?v ?r))
      (ready-road ?v ?next)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-road_0534-road_0539-tw_00510
    :parameters (?v - vehicle ?r - road_type_road_0534 ?next - road_type_road_0539 ?w - window_type_tw_00510)
    :precondition (and
      (ready-road ?v ?r)
      (road-next ?r ?next)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
    )
    :effect (and
      (not (ready-road ?v ?r))
      (ready-road ?v ?next)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-road_0534-road_0539-tw_00540
    :parameters (?v - vehicle ?r - road_type_road_0534 ?next - road_type_road_0539 ?w - window_type_tw_00540)
    :precondition (and
      (ready-road ?v ?r)
      (road-next ?r ?next)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
    )
    :effect (and
      (not (ready-road ?v ?r))
      (ready-road ?v ?next)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-road_0534-road_0539-tw_00570
    :parameters (?v - vehicle ?r - road_type_road_0534 ?next - road_type_road_0539 ?w - window_type_tw_00570)
    :precondition (and
      (ready-road ?v ?r)
      (road-next ?r ?next)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
    )
    :effect (and
      (not (ready-road ?v ?r))
      (ready-road ?v ?next)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-road_0534-road_0539-tw_00600
    :parameters (?v - vehicle ?r - road_type_road_0534 ?next - road_type_road_0539 ?w - window_type_tw_00600)
    :precondition (and
      (ready-road ?v ?r)
      (road-next ?r ?next)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
    )
    :effect (and
      (not (ready-road ?v ?r))
      (ready-road ?v ?next)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-road_0534-road_0540-tw_00000
    :parameters (?v - vehicle ?r - road_type_road_0534 ?next - road_type_road_0540 ?w - window_type_tw_00000)
    :precondition (and
      (ready-road ?v ?r)
      (road-next ?r ?next)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
    )
    :effect (and
      (not (ready-road ?v ?r))
      (ready-road ?v ?next)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-road_0534-road_0540-tw_00030
    :parameters (?v - vehicle ?r - road_type_road_0534 ?next - road_type_road_0540 ?w - window_type_tw_00030)
    :precondition (and
      (ready-road ?v ?r)
      (road-next ?r ?next)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
    )
    :effect (and
      (not (ready-road ?v ?r))
      (ready-road ?v ?next)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-road_0534-road_0540-tw_00060
    :parameters (?v - vehicle ?r - road_type_road_0534 ?next - road_type_road_0540 ?w - window_type_tw_00060)
    :precondition (and
      (ready-road ?v ?r)
      (road-next ?r ?next)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
    )
    :effect (and
      (not (ready-road ?v ?r))
      (ready-road ?v ?next)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-road_0534-road_0540-tw_00090
    :parameters (?v - vehicle ?r - road_type_road_0534 ?next - road_type_road_0540 ?w - window_type_tw_00090)
    :precondition (and
      (ready-road ?v ?r)
      (road-next ?r ?next)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
    )
    :effect (and
      (not (ready-road ?v ?r))
      (ready-road ?v ?next)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-road_0534-road_0540-tw_00120
    :parameters (?v - vehicle ?r - road_type_road_0534 ?next - road_type_road_0540 ?w - window_type_tw_00120)
    :precondition (and
      (ready-road ?v ?r)
      (road-next ?r ?next)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
    )
    :effect (and
      (not (ready-road ?v ?r))
      (ready-road ?v ?next)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-road_0534-road_0540-tw_00150
    :parameters (?v - vehicle ?r - road_type_road_0534 ?next - road_type_road_0540 ?w - window_type_tw_00150)
    :precondition (and
      (ready-road ?v ?r)
      (road-next ?r ?next)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
    )
    :effect (and
      (not (ready-road ?v ?r))
      (ready-road ?v ?next)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-road_0534-road_0540-tw_00180
    :parameters (?v - vehicle ?r - road_type_road_0534 ?next - road_type_road_0540 ?w - window_type_tw_00180)
    :precondition (and
      (ready-road ?v ?r)
      (road-next ?r ?next)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
    )
    :effect (and
      (not (ready-road ?v ?r))
      (ready-road ?v ?next)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-road_0534-road_0540-tw_00210
    :parameters (?v - vehicle ?r - road_type_road_0534 ?next - road_type_road_0540 ?w - window_type_tw_00210)
    :precondition (and
      (ready-road ?v ?r)
      (road-next ?r ?next)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
    )
    :effect (and
      (not (ready-road ?v ?r))
      (ready-road ?v ?next)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-road_0534-road_0540-tw_00240
    :parameters (?v - vehicle ?r - road_type_road_0534 ?next - road_type_road_0540 ?w - window_type_tw_00240)
    :precondition (and
      (ready-road ?v ?r)
      (road-next ?r ?next)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
    )
    :effect (and
      (not (ready-road ?v ?r))
      (ready-road ?v ?next)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-road_0534-road_0540-tw_00270
    :parameters (?v - vehicle ?r - road_type_road_0534 ?next - road_type_road_0540 ?w - window_type_tw_00270)
    :precondition (and
      (ready-road ?v ?r)
      (road-next ?r ?next)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
    )
    :effect (and
      (not (ready-road ?v ?r))
      (ready-road ?v ?next)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-road_0534-road_0540-tw_00300
    :parameters (?v - vehicle ?r - road_type_road_0534 ?next - road_type_road_0540 ?w - window_type_tw_00300)
    :precondition (and
      (ready-road ?v ?r)
      (road-next ?r ?next)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
    )
    :effect (and
      (not (ready-road ?v ?r))
      (ready-road ?v ?next)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-road_0534-road_0540-tw_00330
    :parameters (?v - vehicle ?r - road_type_road_0534 ?next - road_type_road_0540 ?w - window_type_tw_00330)
    :precondition (and
      (ready-road ?v ?r)
      (road-next ?r ?next)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
    )
    :effect (and
      (not (ready-road ?v ?r))
      (ready-road ?v ?next)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-road_0534-road_0540-tw_00360
    :parameters (?v - vehicle ?r - road_type_road_0534 ?next - road_type_road_0540 ?w - window_type_tw_00360)
    :precondition (and
      (ready-road ?v ?r)
      (road-next ?r ?next)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
    )
    :effect (and
      (not (ready-road ?v ?r))
      (ready-road ?v ?next)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-road_0534-road_0540-tw_00390
    :parameters (?v - vehicle ?r - road_type_road_0534 ?next - road_type_road_0540 ?w - window_type_tw_00390)
    :precondition (and
      (ready-road ?v ?r)
      (road-next ?r ?next)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
    )
    :effect (and
      (not (ready-road ?v ?r))
      (ready-road ?v ?next)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-road_0534-road_0540-tw_00420
    :parameters (?v - vehicle ?r - road_type_road_0534 ?next - road_type_road_0540 ?w - window_type_tw_00420)
    :precondition (and
      (ready-road ?v ?r)
      (road-next ?r ?next)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
    )
    :effect (and
      (not (ready-road ?v ?r))
      (ready-road ?v ?next)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-road_0534-road_0540-tw_00450
    :parameters (?v - vehicle ?r - road_type_road_0534 ?next - road_type_road_0540 ?w - window_type_tw_00450)
    :precondition (and
      (ready-road ?v ?r)
      (road-next ?r ?next)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
    )
    :effect (and
      (not (ready-road ?v ?r))
      (ready-road ?v ?next)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-road_0534-road_0540-tw_00480
    :parameters (?v - vehicle ?r - road_type_road_0534 ?next - road_type_road_0540 ?w - window_type_tw_00480)
    :precondition (and
      (ready-road ?v ?r)
      (road-next ?r ?next)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
    )
    :effect (and
      (not (ready-road ?v ?r))
      (ready-road ?v ?next)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-road_0534-road_0540-tw_00510
    :parameters (?v - vehicle ?r - road_type_road_0534 ?next - road_type_road_0540 ?w - window_type_tw_00510)
    :precondition (and
      (ready-road ?v ?r)
      (road-next ?r ?next)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
    )
    :effect (and
      (not (ready-road ?v ?r))
      (ready-road ?v ?next)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-road_0534-road_0540-tw_00540
    :parameters (?v - vehicle ?r - road_type_road_0534 ?next - road_type_road_0540 ?w - window_type_tw_00540)
    :precondition (and
      (ready-road ?v ?r)
      (road-next ?r ?next)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
    )
    :effect (and
      (not (ready-road ?v ?r))
      (ready-road ?v ?next)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-road_0534-road_0540-tw_00570
    :parameters (?v - vehicle ?r - road_type_road_0534 ?next - road_type_road_0540 ?w - window_type_tw_00570)
    :precondition (and
      (ready-road ?v ?r)
      (road-next ?r ?next)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
    )
    :effect (and
      (not (ready-road ?v ?r))
      (ready-road ?v ?next)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-road_0534-road_0540-tw_00600
    :parameters (?v - vehicle ?r - road_type_road_0534 ?next - road_type_road_0540 ?w - window_type_tw_00600)
    :precondition (and
      (ready-road ?v ?r)
      (road-next ?r ?next)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
    )
    :effect (and
      (not (ready-road ?v ?r))
      (ready-road ?v ?next)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-road_0534-road_0541-tw_00000
    :parameters (?v - vehicle ?r - road_type_road_0534 ?next - road_type_road_0541 ?w - window_type_tw_00000)
    :precondition (and
      (ready-road ?v ?r)
      (road-next ?r ?next)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
    )
    :effect (and
      (not (ready-road ?v ?r))
      (ready-road ?v ?next)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-road_0534-road_0541-tw_00030
    :parameters (?v - vehicle ?r - road_type_road_0534 ?next - road_type_road_0541 ?w - window_type_tw_00030)
    :precondition (and
      (ready-road ?v ?r)
      (road-next ?r ?next)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
    )
    :effect (and
      (not (ready-road ?v ?r))
      (ready-road ?v ?next)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-road_0534-road_0541-tw_00060
    :parameters (?v - vehicle ?r - road_type_road_0534 ?next - road_type_road_0541 ?w - window_type_tw_00060)
    :precondition (and
      (ready-road ?v ?r)
      (road-next ?r ?next)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
    )
    :effect (and
      (not (ready-road ?v ?r))
      (ready-road ?v ?next)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-road_0534-road_0541-tw_00090
    :parameters (?v - vehicle ?r - road_type_road_0534 ?next - road_type_road_0541 ?w - window_type_tw_00090)
    :precondition (and
      (ready-road ?v ?r)
      (road-next ?r ?next)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
    )
    :effect (and
      (not (ready-road ?v ?r))
      (ready-road ?v ?next)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-road_0534-road_0541-tw_00120
    :parameters (?v - vehicle ?r - road_type_road_0534 ?next - road_type_road_0541 ?w - window_type_tw_00120)
    :precondition (and
      (ready-road ?v ?r)
      (road-next ?r ?next)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
    )
    :effect (and
      (not (ready-road ?v ?r))
      (ready-road ?v ?next)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-road_0534-road_0541-tw_00150
    :parameters (?v - vehicle ?r - road_type_road_0534 ?next - road_type_road_0541 ?w - window_type_tw_00150)
    :precondition (and
      (ready-road ?v ?r)
      (road-next ?r ?next)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
    )
    :effect (and
      (not (ready-road ?v ?r))
      (ready-road ?v ?next)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-road_0534-road_0541-tw_00180
    :parameters (?v - vehicle ?r - road_type_road_0534 ?next - road_type_road_0541 ?w - window_type_tw_00180)
    :precondition (and
      (ready-road ?v ?r)
      (road-next ?r ?next)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
    )
    :effect (and
      (not (ready-road ?v ?r))
      (ready-road ?v ?next)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-road_0534-road_0541-tw_00210
    :parameters (?v - vehicle ?r - road_type_road_0534 ?next - road_type_road_0541 ?w - window_type_tw_00210)
    :precondition (and
      (ready-road ?v ?r)
      (road-next ?r ?next)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
    )
    :effect (and
      (not (ready-road ?v ?r))
      (ready-road ?v ?next)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-road_0534-road_0541-tw_00240
    :parameters (?v - vehicle ?r - road_type_road_0534 ?next - road_type_road_0541 ?w - window_type_tw_00240)
    :precondition (and
      (ready-road ?v ?r)
      (road-next ?r ?next)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
    )
    :effect (and
      (not (ready-road ?v ?r))
      (ready-road ?v ?next)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-road_0534-road_0541-tw_00270
    :parameters (?v - vehicle ?r - road_type_road_0534 ?next - road_type_road_0541 ?w - window_type_tw_00270)
    :precondition (and
      (ready-road ?v ?r)
      (road-next ?r ?next)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
    )
    :effect (and
      (not (ready-road ?v ?r))
      (ready-road ?v ?next)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-road_0534-road_0541-tw_00300
    :parameters (?v - vehicle ?r - road_type_road_0534 ?next - road_type_road_0541 ?w - window_type_tw_00300)
    :precondition (and
      (ready-road ?v ?r)
      (road-next ?r ?next)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
    )
    :effect (and
      (not (ready-road ?v ?r))
      (ready-road ?v ?next)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-road_0534-road_0541-tw_00330
    :parameters (?v - vehicle ?r - road_type_road_0534 ?next - road_type_road_0541 ?w - window_type_tw_00330)
    :precondition (and
      (ready-road ?v ?r)
      (road-next ?r ?next)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
    )
    :effect (and
      (not (ready-road ?v ?r))
      (ready-road ?v ?next)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-road_0534-road_0541-tw_00360
    :parameters (?v - vehicle ?r - road_type_road_0534 ?next - road_type_road_0541 ?w - window_type_tw_00360)
    :precondition (and
      (ready-road ?v ?r)
      (road-next ?r ?next)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
    )
    :effect (and
      (not (ready-road ?v ?r))
      (ready-road ?v ?next)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-road_0534-road_0541-tw_00390
    :parameters (?v - vehicle ?r - road_type_road_0534 ?next - road_type_road_0541 ?w - window_type_tw_00390)
    :precondition (and
      (ready-road ?v ?r)
      (road-next ?r ?next)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
    )
    :effect (and
      (not (ready-road ?v ?r))
      (ready-road ?v ?next)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-road_0534-road_0541-tw_00420
    :parameters (?v - vehicle ?r - road_type_road_0534 ?next - road_type_road_0541 ?w - window_type_tw_00420)
    :precondition (and
      (ready-road ?v ?r)
      (road-next ?r ?next)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
    )
    :effect (and
      (not (ready-road ?v ?r))
      (ready-road ?v ?next)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-road_0534-road_0541-tw_00450
    :parameters (?v - vehicle ?r - road_type_road_0534 ?next - road_type_road_0541 ?w - window_type_tw_00450)
    :precondition (and
      (ready-road ?v ?r)
      (road-next ?r ?next)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
    )
    :effect (and
      (not (ready-road ?v ?r))
      (ready-road ?v ?next)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-road_0534-road_0541-tw_00480
    :parameters (?v - vehicle ?r - road_type_road_0534 ?next - road_type_road_0541 ?w - window_type_tw_00480)
    :precondition (and
      (ready-road ?v ?r)
      (road-next ?r ?next)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
    )
    :effect (and
      (not (ready-road ?v ?r))
      (ready-road ?v ?next)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-road_0534-road_0541-tw_00510
    :parameters (?v - vehicle ?r - road_type_road_0534 ?next - road_type_road_0541 ?w - window_type_tw_00510)
    :precondition (and
      (ready-road ?v ?r)
      (road-next ?r ?next)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
    )
    :effect (and
      (not (ready-road ?v ?r))
      (ready-road ?v ?next)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-road_0534-road_0541-tw_00540
    :parameters (?v - vehicle ?r - road_type_road_0534 ?next - road_type_road_0541 ?w - window_type_tw_00540)
    :precondition (and
      (ready-road ?v ?r)
      (road-next ?r ?next)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
    )
    :effect (and
      (not (ready-road ?v ?r))
      (ready-road ?v ?next)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-road_0534-road_0541-tw_00570
    :parameters (?v - vehicle ?r - road_type_road_0534 ?next - road_type_road_0541 ?w - window_type_tw_00570)
    :precondition (and
      (ready-road ?v ?r)
      (road-next ?r ?next)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
    )
    :effect (and
      (not (ready-road ?v ?r))
      (ready-road ?v ?next)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-road_0534-road_0541-tw_00600
    :parameters (?v - vehicle ?r - road_type_road_0534 ?next - road_type_road_0541 ?w - window_type_tw_00600)
    :precondition (and
      (ready-road ?v ?r)
      (road-next ?r ?next)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
    )
    :effect (and
      (not (ready-road ?v ?r))
      (ready-road ?v ?next)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-static-road_0535-road_0610
    :parameters (?v - vehicle ?r - road_type_road_0535 ?next - road_type_road_0610)
    :precondition (and
      (ready-road ?v ?r)
      (road-next ?r ?next)
      (road-open ?r)
      (static-road ?r)
      (not (road-blocked ?r))
    )
    :effect (and
      (not (ready-road ?v ?r))
      (ready-road ?v ?next)
      (increase (travel-time ?v) (travel-duration ?r))
      (increase (sim-time) (travel-duration ?r))
    )
  )

  (:action traverse-road-static-road_0536-road_0108
    :parameters (?v - vehicle ?r - road_type_road_0536 ?next - road_type_road_0108)
    :precondition (and
      (ready-road ?v ?r)
      (road-next ?r ?next)
      (road-open ?r)
      (static-road ?r)
      (not (road-blocked ?r))
    )
    :effect (and
      (not (ready-road ?v ?r))
      (ready-road ?v ?next)
      (increase (travel-time ?v) (travel-duration ?r))
      (increase (sim-time) (travel-duration ?r))
    )
  )

  (:action traverse-road-static-road_0536-road_0109
    :parameters (?v - vehicle ?r - road_type_road_0536 ?next - road_type_road_0109)
    :precondition (and
      (ready-road ?v ?r)
      (road-next ?r ?next)
      (road-open ?r)
      (static-road ?r)
      (not (road-blocked ?r))
    )
    :effect (and
      (not (ready-road ?v ?r))
      (ready-road ?v ?next)
      (increase (travel-time ?v) (travel-duration ?r))
      (increase (sim-time) (travel-duration ?r))
    )
  )

  (:action traverse-road-static-road_0539-road_0558
    :parameters (?v - vehicle ?r - road_type_road_0539 ?next - road_type_road_0558)
    :precondition (and
      (ready-road ?v ?r)
      (road-next ?r ?next)
      (road-open ?r)
      (static-road ?r)
      (not (road-blocked ?r))
    )
    :effect (and
      (not (ready-road ?v ?r))
      (ready-road ?v ?next)
      (increase (travel-time ?v) (travel-duration ?r))
      (increase (sim-time) (travel-duration ?r))
    )
  )

  (:action traverse-road-static-road_0540-road_0563
    :parameters (?v - vehicle ?r - road_type_road_0540 ?next - road_type_road_0563)
    :precondition (and
      (ready-road ?v ?r)
      (road-next ?r ?next)
      (road-open ?r)
      (static-road ?r)
      (not (road-blocked ?r))
    )
    :effect (and
      (not (ready-road ?v ?r))
      (ready-road ?v ?next)
      (increase (travel-time ?v) (travel-duration ?r))
      (increase (sim-time) (travel-duration ?r))
    )
  )

  (:action traverse-road-static-road_0540-road_0564
    :parameters (?v - vehicle ?r - road_type_road_0540 ?next - road_type_road_0564)
    :precondition (and
      (ready-road ?v ?r)
      (road-next ?r ?next)
      (road-open ?r)
      (static-road ?r)
      (not (road-blocked ?r))
    )
    :effect (and
      (not (ready-road ?v ?r))
      (ready-road ?v ?next)
      (increase (travel-time ?v) (travel-duration ?r))
      (increase (sim-time) (travel-duration ?r))
    )
  )

  (:action traverse-road-static-road_0541-road_0532
    :parameters (?v - vehicle ?r - road_type_road_0541 ?next - road_type_road_0532)
    :precondition (and
      (ready-road ?v ?r)
      (road-next ?r ?next)
      (road-open ?r)
      (static-road ?r)
      (not (road-blocked ?r))
    )
    :effect (and
      (not (ready-road ?v ?r))
      (ready-road ?v ?next)
      (increase (travel-time ?v) (travel-duration ?r))
      (increase (sim-time) (travel-duration ?r))
    )
  )

  (:action traverse-road-static-road_0541-road_0533
    :parameters (?v - vehicle ?r - road_type_road_0541 ?next - road_type_road_0533)
    :precondition (and
      (ready-road ?v ?r)
      (road-next ?r ?next)
      (road-open ?r)
      (static-road ?r)
      (not (road-blocked ?r))
    )
    :effect (and
      (not (ready-road ?v ?r))
      (ready-road ?v ?next)
      (increase (travel-time ?v) (travel-duration ?r))
      (increase (sim-time) (travel-duration ?r))
    )
  )

  (:action traverse-road-static-road_0541-road_0534
    :parameters (?v - vehicle ?r - road_type_road_0541 ?next - road_type_road_0534)
    :precondition (and
      (ready-road ?v ?r)
      (road-next ?r ?next)
      (road-open ?r)
      (static-road ?r)
      (not (road-blocked ?r))
    )
    :effect (and
      (not (ready-road ?v ?r))
      (ready-road ?v ?next)
      (increase (travel-time ?v) (travel-duration ?r))
      (increase (sim-time) (travel-duration ?r))
    )
  )

  (:action traverse-road-static-road_0542-macro_0039
    :parameters (?v - vehicle ?r - road_type_road_0542 ?next - road_type_macro_0039)
    :precondition (and
      (ready-road ?v ?r)
      (road-next ?r ?next)
      (road-open ?r)
      (static-road ?r)
      (not (road-blocked ?r))
    )
    :effect (and
      (not (ready-road ?v ?r))
      (ready-road ?v ?next)
      (increase (travel-time ?v) (travel-duration ?r))
      (increase (sim-time) (travel-duration ?r))
    )
  )

  (:action traverse-road-static-road_0542-road_0566
    :parameters (?v - vehicle ?r - road_type_road_0542 ?next - road_type_road_0566)
    :precondition (and
      (ready-road ?v ?r)
      (road-next ?r ?next)
      (road-open ?r)
      (static-road ?r)
      (not (road-blocked ?r))
    )
    :effect (and
      (not (ready-road ?v ?r))
      (ready-road ?v ?next)
      (increase (travel-time ?v) (travel-duration ?r))
      (increase (sim-time) (travel-duration ?r))
    )
  )

  (:action traverse-road-dynamic-road_0543-road_0539-tw_00000
    :parameters (?v - vehicle ?r - road_type_road_0543 ?next - road_type_road_0539 ?w - window_type_tw_00000)
    :precondition (and
      (ready-road ?v ?r)
      (road-next ?r ?next)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
    )
    :effect (and
      (not (ready-road ?v ?r))
      (ready-road ?v ?next)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-road_0543-road_0539-tw_00030
    :parameters (?v - vehicle ?r - road_type_road_0543 ?next - road_type_road_0539 ?w - window_type_tw_00030)
    :precondition (and
      (ready-road ?v ?r)
      (road-next ?r ?next)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
    )
    :effect (and
      (not (ready-road ?v ?r))
      (ready-road ?v ?next)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-road_0543-road_0539-tw_00060
    :parameters (?v - vehicle ?r - road_type_road_0543 ?next - road_type_road_0539 ?w - window_type_tw_00060)
    :precondition (and
      (ready-road ?v ?r)
      (road-next ?r ?next)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
    )
    :effect (and
      (not (ready-road ?v ?r))
      (ready-road ?v ?next)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-road_0543-road_0539-tw_00090
    :parameters (?v - vehicle ?r - road_type_road_0543 ?next - road_type_road_0539 ?w - window_type_tw_00090)
    :precondition (and
      (ready-road ?v ?r)
      (road-next ?r ?next)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
    )
    :effect (and
      (not (ready-road ?v ?r))
      (ready-road ?v ?next)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-road_0543-road_0539-tw_00120
    :parameters (?v - vehicle ?r - road_type_road_0543 ?next - road_type_road_0539 ?w - window_type_tw_00120)
    :precondition (and
      (ready-road ?v ?r)
      (road-next ?r ?next)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
    )
    :effect (and
      (not (ready-road ?v ?r))
      (ready-road ?v ?next)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-road_0543-road_0539-tw_00150
    :parameters (?v - vehicle ?r - road_type_road_0543 ?next - road_type_road_0539 ?w - window_type_tw_00150)
    :precondition (and
      (ready-road ?v ?r)
      (road-next ?r ?next)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
    )
    :effect (and
      (not (ready-road ?v ?r))
      (ready-road ?v ?next)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-road_0543-road_0539-tw_00180
    :parameters (?v - vehicle ?r - road_type_road_0543 ?next - road_type_road_0539 ?w - window_type_tw_00180)
    :precondition (and
      (ready-road ?v ?r)
      (road-next ?r ?next)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
    )
    :effect (and
      (not (ready-road ?v ?r))
      (ready-road ?v ?next)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-road_0543-road_0539-tw_00210
    :parameters (?v - vehicle ?r - road_type_road_0543 ?next - road_type_road_0539 ?w - window_type_tw_00210)
    :precondition (and
      (ready-road ?v ?r)
      (road-next ?r ?next)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
    )
    :effect (and
      (not (ready-road ?v ?r))
      (ready-road ?v ?next)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-road_0543-road_0539-tw_00240
    :parameters (?v - vehicle ?r - road_type_road_0543 ?next - road_type_road_0539 ?w - window_type_tw_00240)
    :precondition (and
      (ready-road ?v ?r)
      (road-next ?r ?next)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
    )
    :effect (and
      (not (ready-road ?v ?r))
      (ready-road ?v ?next)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-road_0543-road_0539-tw_00270
    :parameters (?v - vehicle ?r - road_type_road_0543 ?next - road_type_road_0539 ?w - window_type_tw_00270)
    :precondition (and
      (ready-road ?v ?r)
      (road-next ?r ?next)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
    )
    :effect (and
      (not (ready-road ?v ?r))
      (ready-road ?v ?next)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-road_0543-road_0539-tw_00300
    :parameters (?v - vehicle ?r - road_type_road_0543 ?next - road_type_road_0539 ?w - window_type_tw_00300)
    :precondition (and
      (ready-road ?v ?r)
      (road-next ?r ?next)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
    )
    :effect (and
      (not (ready-road ?v ?r))
      (ready-road ?v ?next)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-road_0543-road_0539-tw_00330
    :parameters (?v - vehicle ?r - road_type_road_0543 ?next - road_type_road_0539 ?w - window_type_tw_00330)
    :precondition (and
      (ready-road ?v ?r)
      (road-next ?r ?next)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
    )
    :effect (and
      (not (ready-road ?v ?r))
      (ready-road ?v ?next)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-road_0543-road_0539-tw_00360
    :parameters (?v - vehicle ?r - road_type_road_0543 ?next - road_type_road_0539 ?w - window_type_tw_00360)
    :precondition (and
      (ready-road ?v ?r)
      (road-next ?r ?next)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
    )
    :effect (and
      (not (ready-road ?v ?r))
      (ready-road ?v ?next)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-road_0543-road_0539-tw_00390
    :parameters (?v - vehicle ?r - road_type_road_0543 ?next - road_type_road_0539 ?w - window_type_tw_00390)
    :precondition (and
      (ready-road ?v ?r)
      (road-next ?r ?next)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
    )
    :effect (and
      (not (ready-road ?v ?r))
      (ready-road ?v ?next)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-road_0543-road_0539-tw_00420
    :parameters (?v - vehicle ?r - road_type_road_0543 ?next - road_type_road_0539 ?w - window_type_tw_00420)
    :precondition (and
      (ready-road ?v ?r)
      (road-next ?r ?next)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
    )
    :effect (and
      (not (ready-road ?v ?r))
      (ready-road ?v ?next)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-road_0543-road_0539-tw_00450
    :parameters (?v - vehicle ?r - road_type_road_0543 ?next - road_type_road_0539 ?w - window_type_tw_00450)
    :precondition (and
      (ready-road ?v ?r)
      (road-next ?r ?next)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
    )
    :effect (and
      (not (ready-road ?v ?r))
      (ready-road ?v ?next)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-road_0543-road_0539-tw_00480
    :parameters (?v - vehicle ?r - road_type_road_0543 ?next - road_type_road_0539 ?w - window_type_tw_00480)
    :precondition (and
      (ready-road ?v ?r)
      (road-next ?r ?next)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
    )
    :effect (and
      (not (ready-road ?v ?r))
      (ready-road ?v ?next)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-road_0543-road_0539-tw_00510
    :parameters (?v - vehicle ?r - road_type_road_0543 ?next - road_type_road_0539 ?w - window_type_tw_00510)
    :precondition (and
      (ready-road ?v ?r)
      (road-next ?r ?next)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
    )
    :effect (and
      (not (ready-road ?v ?r))
      (ready-road ?v ?next)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-road_0543-road_0539-tw_00540
    :parameters (?v - vehicle ?r - road_type_road_0543 ?next - road_type_road_0539 ?w - window_type_tw_00540)
    :precondition (and
      (ready-road ?v ?r)
      (road-next ?r ?next)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
    )
    :effect (and
      (not (ready-road ?v ?r))
      (ready-road ?v ?next)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-road_0543-road_0539-tw_00570
    :parameters (?v - vehicle ?r - road_type_road_0543 ?next - road_type_road_0539 ?w - window_type_tw_00570)
    :precondition (and
      (ready-road ?v ?r)
      (road-next ?r ?next)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
    )
    :effect (and
      (not (ready-road ?v ?r))
      (ready-road ?v ?next)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-road_0543-road_0539-tw_00600
    :parameters (?v - vehicle ?r - road_type_road_0543 ?next - road_type_road_0539 ?w - window_type_tw_00600)
    :precondition (and
      (ready-road ?v ?r)
      (road-next ?r ?next)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
    )
    :effect (and
      (not (ready-road ?v ?r))
      (ready-road ?v ?next)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-road_0543-road_0540-tw_00000
    :parameters (?v - vehicle ?r - road_type_road_0543 ?next - road_type_road_0540 ?w - window_type_tw_00000)
    :precondition (and
      (ready-road ?v ?r)
      (road-next ?r ?next)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
    )
    :effect (and
      (not (ready-road ?v ?r))
      (ready-road ?v ?next)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-road_0543-road_0540-tw_00030
    :parameters (?v - vehicle ?r - road_type_road_0543 ?next - road_type_road_0540 ?w - window_type_tw_00030)
    :precondition (and
      (ready-road ?v ?r)
      (road-next ?r ?next)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
    )
    :effect (and
      (not (ready-road ?v ?r))
      (ready-road ?v ?next)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-road_0543-road_0540-tw_00060
    :parameters (?v - vehicle ?r - road_type_road_0543 ?next - road_type_road_0540 ?w - window_type_tw_00060)
    :precondition (and
      (ready-road ?v ?r)
      (road-next ?r ?next)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
    )
    :effect (and
      (not (ready-road ?v ?r))
      (ready-road ?v ?next)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-road_0543-road_0540-tw_00090
    :parameters (?v - vehicle ?r - road_type_road_0543 ?next - road_type_road_0540 ?w - window_type_tw_00090)
    :precondition (and
      (ready-road ?v ?r)
      (road-next ?r ?next)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
    )
    :effect (and
      (not (ready-road ?v ?r))
      (ready-road ?v ?next)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-road_0543-road_0540-tw_00120
    :parameters (?v - vehicle ?r - road_type_road_0543 ?next - road_type_road_0540 ?w - window_type_tw_00120)
    :precondition (and
      (ready-road ?v ?r)
      (road-next ?r ?next)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
    )
    :effect (and
      (not (ready-road ?v ?r))
      (ready-road ?v ?next)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-road_0543-road_0540-tw_00150
    :parameters (?v - vehicle ?r - road_type_road_0543 ?next - road_type_road_0540 ?w - window_type_tw_00150)
    :precondition (and
      (ready-road ?v ?r)
      (road-next ?r ?next)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
    )
    :effect (and
      (not (ready-road ?v ?r))
      (ready-road ?v ?next)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-road_0543-road_0540-tw_00180
    :parameters (?v - vehicle ?r - road_type_road_0543 ?next - road_type_road_0540 ?w - window_type_tw_00180)
    :precondition (and
      (ready-road ?v ?r)
      (road-next ?r ?next)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
    )
    :effect (and
      (not (ready-road ?v ?r))
      (ready-road ?v ?next)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-road_0543-road_0540-tw_00210
    :parameters (?v - vehicle ?r - road_type_road_0543 ?next - road_type_road_0540 ?w - window_type_tw_00210)
    :precondition (and
      (ready-road ?v ?r)
      (road-next ?r ?next)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
    )
    :effect (and
      (not (ready-road ?v ?r))
      (ready-road ?v ?next)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-road_0543-road_0540-tw_00240
    :parameters (?v - vehicle ?r - road_type_road_0543 ?next - road_type_road_0540 ?w - window_type_tw_00240)
    :precondition (and
      (ready-road ?v ?r)
      (road-next ?r ?next)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
    )
    :effect (and
      (not (ready-road ?v ?r))
      (ready-road ?v ?next)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-road_0543-road_0540-tw_00270
    :parameters (?v - vehicle ?r - road_type_road_0543 ?next - road_type_road_0540 ?w - window_type_tw_00270)
    :precondition (and
      (ready-road ?v ?r)
      (road-next ?r ?next)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
    )
    :effect (and
      (not (ready-road ?v ?r))
      (ready-road ?v ?next)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-road_0543-road_0540-tw_00300
    :parameters (?v - vehicle ?r - road_type_road_0543 ?next - road_type_road_0540 ?w - window_type_tw_00300)
    :precondition (and
      (ready-road ?v ?r)
      (road-next ?r ?next)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
    )
    :effect (and
      (not (ready-road ?v ?r))
      (ready-road ?v ?next)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-road_0543-road_0540-tw_00330
    :parameters (?v - vehicle ?r - road_type_road_0543 ?next - road_type_road_0540 ?w - window_type_tw_00330)
    :precondition (and
      (ready-road ?v ?r)
      (road-next ?r ?next)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
    )
    :effect (and
      (not (ready-road ?v ?r))
      (ready-road ?v ?next)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-road_0543-road_0540-tw_00360
    :parameters (?v - vehicle ?r - road_type_road_0543 ?next - road_type_road_0540 ?w - window_type_tw_00360)
    :precondition (and
      (ready-road ?v ?r)
      (road-next ?r ?next)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
    )
    :effect (and
      (not (ready-road ?v ?r))
      (ready-road ?v ?next)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-road_0543-road_0540-tw_00390
    :parameters (?v - vehicle ?r - road_type_road_0543 ?next - road_type_road_0540 ?w - window_type_tw_00390)
    :precondition (and
      (ready-road ?v ?r)
      (road-next ?r ?next)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
    )
    :effect (and
      (not (ready-road ?v ?r))
      (ready-road ?v ?next)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-road_0543-road_0540-tw_00420
    :parameters (?v - vehicle ?r - road_type_road_0543 ?next - road_type_road_0540 ?w - window_type_tw_00420)
    :precondition (and
      (ready-road ?v ?r)
      (road-next ?r ?next)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
    )
    :effect (and
      (not (ready-road ?v ?r))
      (ready-road ?v ?next)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-road_0543-road_0540-tw_00450
    :parameters (?v - vehicle ?r - road_type_road_0543 ?next - road_type_road_0540 ?w - window_type_tw_00450)
    :precondition (and
      (ready-road ?v ?r)
      (road-next ?r ?next)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
    )
    :effect (and
      (not (ready-road ?v ?r))
      (ready-road ?v ?next)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-road_0543-road_0540-tw_00480
    :parameters (?v - vehicle ?r - road_type_road_0543 ?next - road_type_road_0540 ?w - window_type_tw_00480)
    :precondition (and
      (ready-road ?v ?r)
      (road-next ?r ?next)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
    )
    :effect (and
      (not (ready-road ?v ?r))
      (ready-road ?v ?next)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-road_0543-road_0540-tw_00510
    :parameters (?v - vehicle ?r - road_type_road_0543 ?next - road_type_road_0540 ?w - window_type_tw_00510)
    :precondition (and
      (ready-road ?v ?r)
      (road-next ?r ?next)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
    )
    :effect (and
      (not (ready-road ?v ?r))
      (ready-road ?v ?next)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-road_0543-road_0540-tw_00540
    :parameters (?v - vehicle ?r - road_type_road_0543 ?next - road_type_road_0540 ?w - window_type_tw_00540)
    :precondition (and
      (ready-road ?v ?r)
      (road-next ?r ?next)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
    )
    :effect (and
      (not (ready-road ?v ?r))
      (ready-road ?v ?next)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-road_0543-road_0540-tw_00570
    :parameters (?v - vehicle ?r - road_type_road_0543 ?next - road_type_road_0540 ?w - window_type_tw_00570)
    :precondition (and
      (ready-road ?v ?r)
      (road-next ?r ?next)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
    )
    :effect (and
      (not (ready-road ?v ?r))
      (ready-road ?v ?next)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-road_0543-road_0540-tw_00600
    :parameters (?v - vehicle ?r - road_type_road_0543 ?next - road_type_road_0540 ?w - window_type_tw_00600)
    :precondition (and
      (ready-road ?v ?r)
      (road-next ?r ?next)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
    )
    :effect (and
      (not (ready-road ?v ?r))
      (ready-road ?v ?next)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-road_0543-road_0541-tw_00000
    :parameters (?v - vehicle ?r - road_type_road_0543 ?next - road_type_road_0541 ?w - window_type_tw_00000)
    :precondition (and
      (ready-road ?v ?r)
      (road-next ?r ?next)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
    )
    :effect (and
      (not (ready-road ?v ?r))
      (ready-road ?v ?next)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-road_0543-road_0541-tw_00030
    :parameters (?v - vehicle ?r - road_type_road_0543 ?next - road_type_road_0541 ?w - window_type_tw_00030)
    :precondition (and
      (ready-road ?v ?r)
      (road-next ?r ?next)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
    )
    :effect (and
      (not (ready-road ?v ?r))
      (ready-road ?v ?next)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-road_0543-road_0541-tw_00060
    :parameters (?v - vehicle ?r - road_type_road_0543 ?next - road_type_road_0541 ?w - window_type_tw_00060)
    :precondition (and
      (ready-road ?v ?r)
      (road-next ?r ?next)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
    )
    :effect (and
      (not (ready-road ?v ?r))
      (ready-road ?v ?next)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-road_0543-road_0541-tw_00090
    :parameters (?v - vehicle ?r - road_type_road_0543 ?next - road_type_road_0541 ?w - window_type_tw_00090)
    :precondition (and
      (ready-road ?v ?r)
      (road-next ?r ?next)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
    )
    :effect (and
      (not (ready-road ?v ?r))
      (ready-road ?v ?next)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-road_0543-road_0541-tw_00120
    :parameters (?v - vehicle ?r - road_type_road_0543 ?next - road_type_road_0541 ?w - window_type_tw_00120)
    :precondition (and
      (ready-road ?v ?r)
      (road-next ?r ?next)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
    )
    :effect (and
      (not (ready-road ?v ?r))
      (ready-road ?v ?next)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-road_0543-road_0541-tw_00150
    :parameters (?v - vehicle ?r - road_type_road_0543 ?next - road_type_road_0541 ?w - window_type_tw_00150)
    :precondition (and
      (ready-road ?v ?r)
      (road-next ?r ?next)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
    )
    :effect (and
      (not (ready-road ?v ?r))
      (ready-road ?v ?next)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-road_0543-road_0541-tw_00180
    :parameters (?v - vehicle ?r - road_type_road_0543 ?next - road_type_road_0541 ?w - window_type_tw_00180)
    :precondition (and
      (ready-road ?v ?r)
      (road-next ?r ?next)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
    )
    :effect (and
      (not (ready-road ?v ?r))
      (ready-road ?v ?next)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-road_0543-road_0541-tw_00210
    :parameters (?v - vehicle ?r - road_type_road_0543 ?next - road_type_road_0541 ?w - window_type_tw_00210)
    :precondition (and
      (ready-road ?v ?r)
      (road-next ?r ?next)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
    )
    :effect (and
      (not (ready-road ?v ?r))
      (ready-road ?v ?next)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-road_0543-road_0541-tw_00240
    :parameters (?v - vehicle ?r - road_type_road_0543 ?next - road_type_road_0541 ?w - window_type_tw_00240)
    :precondition (and
      (ready-road ?v ?r)
      (road-next ?r ?next)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
    )
    :effect (and
      (not (ready-road ?v ?r))
      (ready-road ?v ?next)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-road_0543-road_0541-tw_00270
    :parameters (?v - vehicle ?r - road_type_road_0543 ?next - road_type_road_0541 ?w - window_type_tw_00270)
    :precondition (and
      (ready-road ?v ?r)
      (road-next ?r ?next)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
    )
    :effect (and
      (not (ready-road ?v ?r))
      (ready-road ?v ?next)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-road_0543-road_0541-tw_00300
    :parameters (?v - vehicle ?r - road_type_road_0543 ?next - road_type_road_0541 ?w - window_type_tw_00300)
    :precondition (and
      (ready-road ?v ?r)
      (road-next ?r ?next)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
    )
    :effect (and
      (not (ready-road ?v ?r))
      (ready-road ?v ?next)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-road_0543-road_0541-tw_00330
    :parameters (?v - vehicle ?r - road_type_road_0543 ?next - road_type_road_0541 ?w - window_type_tw_00330)
    :precondition (and
      (ready-road ?v ?r)
      (road-next ?r ?next)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
    )
    :effect (and
      (not (ready-road ?v ?r))
      (ready-road ?v ?next)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-road_0543-road_0541-tw_00360
    :parameters (?v - vehicle ?r - road_type_road_0543 ?next - road_type_road_0541 ?w - window_type_tw_00360)
    :precondition (and
      (ready-road ?v ?r)
      (road-next ?r ?next)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
    )
    :effect (and
      (not (ready-road ?v ?r))
      (ready-road ?v ?next)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-road_0543-road_0541-tw_00390
    :parameters (?v - vehicle ?r - road_type_road_0543 ?next - road_type_road_0541 ?w - window_type_tw_00390)
    :precondition (and
      (ready-road ?v ?r)
      (road-next ?r ?next)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
    )
    :effect (and
      (not (ready-road ?v ?r))
      (ready-road ?v ?next)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-road_0543-road_0541-tw_00420
    :parameters (?v - vehicle ?r - road_type_road_0543 ?next - road_type_road_0541 ?w - window_type_tw_00420)
    :precondition (and
      (ready-road ?v ?r)
      (road-next ?r ?next)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
    )
    :effect (and
      (not (ready-road ?v ?r))
      (ready-road ?v ?next)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-road_0543-road_0541-tw_00450
    :parameters (?v - vehicle ?r - road_type_road_0543 ?next - road_type_road_0541 ?w - window_type_tw_00450)
    :precondition (and
      (ready-road ?v ?r)
      (road-next ?r ?next)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
    )
    :effect (and
      (not (ready-road ?v ?r))
      (ready-road ?v ?next)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-road_0543-road_0541-tw_00480
    :parameters (?v - vehicle ?r - road_type_road_0543 ?next - road_type_road_0541 ?w - window_type_tw_00480)
    :precondition (and
      (ready-road ?v ?r)
      (road-next ?r ?next)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
    )
    :effect (and
      (not (ready-road ?v ?r))
      (ready-road ?v ?next)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-road_0543-road_0541-tw_00510
    :parameters (?v - vehicle ?r - road_type_road_0543 ?next - road_type_road_0541 ?w - window_type_tw_00510)
    :precondition (and
      (ready-road ?v ?r)
      (road-next ?r ?next)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
    )
    :effect (and
      (not (ready-road ?v ?r))
      (ready-road ?v ?next)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-road_0543-road_0541-tw_00540
    :parameters (?v - vehicle ?r - road_type_road_0543 ?next - road_type_road_0541 ?w - window_type_tw_00540)
    :precondition (and
      (ready-road ?v ?r)
      (road-next ?r ?next)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
    )
    :effect (and
      (not (ready-road ?v ?r))
      (ready-road ?v ?next)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-road_0543-road_0541-tw_00570
    :parameters (?v - vehicle ?r - road_type_road_0543 ?next - road_type_road_0541 ?w - window_type_tw_00570)
    :precondition (and
      (ready-road ?v ?r)
      (road-next ?r ?next)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
    )
    :effect (and
      (not (ready-road ?v ?r))
      (ready-road ?v ?next)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-road_0543-road_0541-tw_00600
    :parameters (?v - vehicle ?r - road_type_road_0543 ?next - road_type_road_0541 ?w - window_type_tw_00600)
    :precondition (and
      (ready-road ?v ?r)
      (road-next ?r ?next)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
    )
    :effect (and
      (not (ready-road ?v ?r))
      (ready-road ?v ?next)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-static-road_0544-road_0720
    :parameters (?v - vehicle ?r - road_type_road_0544 ?next - road_type_road_0720)
    :precondition (and
      (ready-road ?v ?r)
      (road-next ?r ?next)
      (road-open ?r)
      (static-road ?r)
      (not (road-blocked ?r))
    )
    :effect (and
      (not (ready-road ?v ?r))
      (ready-road ?v ?next)
      (increase (travel-time ?v) (travel-duration ?r))
      (increase (sim-time) (travel-duration ?r))
    )
  )

  (:action traverse-road-static-road_0545-road_0441
    :parameters (?v - vehicle ?r - road_type_road_0545 ?next - road_type_road_0441)
    :precondition (and
      (ready-road ?v ?r)
      (road-next ?r ?next)
      (road-open ?r)
      (static-road ?r)
      (not (road-blocked ?r))
    )
    :effect (and
      (not (ready-road ?v ?r))
      (ready-road ?v ?next)
      (increase (travel-time ?v) (travel-duration ?r))
      (increase (sim-time) (travel-duration ?r))
    )
  )

  (:action traverse-road-static-road_0545-road_0442
    :parameters (?v - vehicle ?r - road_type_road_0545 ?next - road_type_road_0442)
    :precondition (and
      (ready-road ?v ?r)
      (road-next ?r ?next)
      (road-open ?r)
      (static-road ?r)
      (not (road-blocked ?r))
    )
    :effect (and
      (not (ready-road ?v ?r))
      (ready-road ?v ?next)
      (increase (travel-time ?v) (travel-duration ?r))
      (increase (sim-time) (travel-duration ?r))
    )
  )

  (:action traverse-road-static-road_0546-road_0499
    :parameters (?v - vehicle ?r - road_type_road_0546 ?next - road_type_road_0499)
    :precondition (and
      (ready-road ?v ?r)
      (road-next ?r ?next)
      (road-open ?r)
      (static-road ?r)
      (not (road-blocked ?r))
    )
    :effect (and
      (not (ready-road ?v ?r))
      (ready-road ?v ?next)
      (increase (travel-time ?v) (travel-duration ?r))
      (increase (sim-time) (travel-duration ?r))
    )
  )

  (:action traverse-road-static-road_0546-road_0500
    :parameters (?v - vehicle ?r - road_type_road_0546 ?next - road_type_road_0500)
    :precondition (and
      (ready-road ?v ?r)
      (road-next ?r ?next)
      (road-open ?r)
      (static-road ?r)
      (not (road-blocked ?r))
    )
    :effect (and
      (not (ready-road ?v ?r))
      (ready-road ?v ?next)
      (increase (travel-time ?v) (travel-duration ?r))
      (increase (sim-time) (travel-duration ?r))
    )
  )

  (:action traverse-road-static-road_0546-road_0501
    :parameters (?v - vehicle ?r - road_type_road_0546 ?next - road_type_road_0501)
    :precondition (and
      (ready-road ?v ?r)
      (road-next ?r ?next)
      (road-open ?r)
      (static-road ?r)
      (not (road-blocked ?r))
    )
    :effect (and
      (not (ready-road ?v ?r))
      (ready-road ?v ?next)
      (increase (travel-time ?v) (travel-duration ?r))
      (increase (sim-time) (travel-duration ?r))
    )
  )

  (:action traverse-road-static-road_0547-road_0548
    :parameters (?v - vehicle ?r - road_type_road_0547 ?next - road_type_road_0548)
    :precondition (and
      (ready-road ?v ?r)
      (road-next ?r ?next)
      (road-open ?r)
      (static-road ?r)
      (not (road-blocked ?r))
    )
    :effect (and
      (not (ready-road ?v ?r))
      (ready-road ?v ?next)
      (increase (travel-time ?v) (travel-duration ?r))
      (increase (sim-time) (travel-duration ?r))
    )
  )

  (:action traverse-road-static-road_0547-road_0549
    :parameters (?v - vehicle ?r - road_type_road_0547 ?next - road_type_road_0549)
    :precondition (and
      (ready-road ?v ?r)
      (road-next ?r ?next)
      (road-open ?r)
      (static-road ?r)
      (not (road-blocked ?r))
    )
    :effect (and
      (not (ready-road ?v ?r))
      (ready-road ?v ?next)
      (increase (travel-time ?v) (travel-duration ?r))
      (increase (sim-time) (travel-duration ?r))
    )
  )

  (:action traverse-road-static-road_0548-road_0346
    :parameters (?v - vehicle ?r - road_type_road_0548 ?next - road_type_road_0346)
    :precondition (and
      (ready-road ?v ?r)
      (road-next ?r ?next)
      (road-open ?r)
      (static-road ?r)
      (not (road-blocked ?r))
    )
    :effect (and
      (not (ready-road ?v ?r))
      (ready-road ?v ?next)
      (increase (travel-time ?v) (travel-duration ?r))
      (increase (sim-time) (travel-duration ?r))
    )
  )

  (:action traverse-road-static-road_0548-road_0347
    :parameters (?v - vehicle ?r - road_type_road_0548 ?next - road_type_road_0347)
    :precondition (and
      (ready-road ?v ?r)
      (road-next ?r ?next)
      (road-open ?r)
      (static-road ?r)
      (not (road-blocked ?r))
    )
    :effect (and
      (not (ready-road ?v ?r))
      (ready-road ?v ?next)
      (increase (travel-time ?v) (travel-duration ?r))
      (increase (sim-time) (travel-duration ?r))
    )
  )

  (:action traverse-road-static-road_0549-road_0159
    :parameters (?v - vehicle ?r - road_type_road_0549 ?next - road_type_road_0159)
    :precondition (and
      (ready-road ?v ?r)
      (road-next ?r ?next)
      (road-open ?r)
      (static-road ?r)
      (not (road-blocked ?r))
    )
    :effect (and
      (not (ready-road ?v ?r))
      (ready-road ?v ?next)
      (increase (travel-time ?v) (travel-duration ?r))
      (increase (sim-time) (travel-duration ?r))
    )
  )

  (:action traverse-road-static-road_0549-road_0160
    :parameters (?v - vehicle ?r - road_type_road_0549 ?next - road_type_road_0160)
    :precondition (and
      (ready-road ?v ?r)
      (road-next ?r ?next)
      (road-open ?r)
      (static-road ?r)
      (not (road-blocked ?r))
    )
    :effect (and
      (not (ready-road ?v ?r))
      (ready-road ?v ?next)
      (increase (travel-time ?v) (travel-duration ?r))
      (increase (sim-time) (travel-duration ?r))
    )
  )

  (:action traverse-road-static-road_0550-road_0827
    :parameters (?v - vehicle ?r - road_type_road_0550 ?next - road_type_road_0827)
    :precondition (and
      (ready-road ?v ?r)
      (road-next ?r ?next)
      (road-open ?r)
      (static-road ?r)
      (not (road-blocked ?r))
    )
    :effect (and
      (not (ready-road ?v ?r))
      (ready-road ?v ?next)
      (increase (travel-time ?v) (travel-duration ?r))
      (increase (sim-time) (travel-duration ?r))
    )
  )

  (:action traverse-road-static-road_0551-road_0242
    :parameters (?v - vehicle ?r - road_type_road_0551 ?next - road_type_road_0242)
    :precondition (and
      (ready-road ?v ?r)
      (road-next ?r ?next)
      (road-open ?r)
      (static-road ?r)
      (not (road-blocked ?r))
    )
    :effect (and
      (not (ready-road ?v ?r))
      (ready-road ?v ?next)
      (increase (travel-time ?v) (travel-duration ?r))
      (increase (sim-time) (travel-duration ?r))
    )
  )

  (:action traverse-road-static-road_0551-road_0243
    :parameters (?v - vehicle ?r - road_type_road_0551 ?next - road_type_road_0243)
    :precondition (and
      (ready-road ?v ?r)
      (road-next ?r ?next)
      (road-open ?r)
      (static-road ?r)
      (not (road-blocked ?r))
    )
    :effect (and
      (not (ready-road ?v ?r))
      (ready-road ?v ?next)
      (increase (travel-time ?v) (travel-duration ?r))
      (increase (sim-time) (travel-duration ?r))
    )
  )

  (:action traverse-road-static-road_0552-road_0718
    :parameters (?v - vehicle ?r - road_type_road_0552 ?next - road_type_road_0718)
    :precondition (and
      (ready-road ?v ?r)
      (road-next ?r ?next)
      (road-open ?r)
      (static-road ?r)
      (not (road-blocked ?r))
    )
    :effect (and
      (not (ready-road ?v ?r))
      (ready-road ?v ?next)
      (increase (travel-time ?v) (travel-duration ?r))
      (increase (sim-time) (travel-duration ?r))
    )
  )

  (:action traverse-road-static-road_0552-road_0719
    :parameters (?v - vehicle ?r - road_type_road_0552 ?next - road_type_road_0719)
    :precondition (and
      (ready-road ?v ?r)
      (road-next ?r ?next)
      (road-open ?r)
      (static-road ?r)
      (not (road-blocked ?r))
    )
    :effect (and
      (not (ready-road ?v ?r))
      (ready-road ?v ?next)
      (increase (travel-time ?v) (travel-duration ?r))
      (increase (sim-time) (travel-duration ?r))
    )
  )

  (:action traverse-road-static-road_0553-macro_0036
    :parameters (?v - vehicle ?r - road_type_road_0553 ?next - road_type_macro_0036)
    :precondition (and
      (ready-road ?v ?r)
      (road-next ?r ?next)
      (road-open ?r)
      (static-road ?r)
      (not (road-blocked ?r))
    )
    :effect (and
      (not (ready-road ?v ?r))
      (ready-road ?v ?next)
      (increase (travel-time ?v) (travel-duration ?r))
      (increase (sim-time) (travel-duration ?r))
    )
  )

  (:action traverse-road-static-road_0553-road_0507
    :parameters (?v - vehicle ?r - road_type_road_0553 ?next - road_type_road_0507)
    :precondition (and
      (ready-road ?v ?r)
      (road-next ?r ?next)
      (road-open ?r)
      (static-road ?r)
      (not (road-blocked ?r))
    )
    :effect (and
      (not (ready-road ?v ?r))
      (ready-road ?v ?next)
      (increase (travel-time ?v) (travel-duration ?r))
      (increase (sim-time) (travel-duration ?r))
    )
  )

  (:action traverse-road-static-road_0553-road_0508
    :parameters (?v - vehicle ?r - road_type_road_0553 ?next - road_type_road_0508)
    :precondition (and
      (ready-road ?v ?r)
      (road-next ?r ?next)
      (road-open ?r)
      (static-road ?r)
      (not (road-blocked ?r))
    )
    :effect (and
      (not (ready-road ?v ?r))
      (ready-road ?v ?next)
      (increase (travel-time ?v) (travel-duration ?r))
      (increase (sim-time) (travel-duration ?r))
    )
  )

  (:action traverse-road-static-road_0554-road_0558
    :parameters (?v - vehicle ?r - road_type_road_0554 ?next - road_type_road_0558)
    :precondition (and
      (ready-road ?v ?r)
      (road-next ?r ?next)
      (road-open ?r)
      (static-road ?r)
      (not (road-blocked ?r))
    )
    :effect (and
      (not (ready-road ?v ?r))
      (ready-road ?v ?next)
      (increase (travel-time ?v) (travel-duration ?r))
      (increase (sim-time) (travel-duration ?r))
    )
  )

  (:action traverse-road-static-road_0555-road_0511
    :parameters (?v - vehicle ?r - road_type_road_0555 ?next - road_type_road_0511)
    :precondition (and
      (ready-road ?v ?r)
      (road-next ?r ?next)
      (road-open ?r)
      (static-road ?r)
      (not (road-blocked ?r))
    )
    :effect (and
      (not (ready-road ?v ?r))
      (ready-road ?v ?next)
      (increase (travel-time ?v) (travel-duration ?r))
      (increase (sim-time) (travel-duration ?r))
    )
  )

  (:action traverse-road-static-road_0555-road_0512
    :parameters (?v - vehicle ?r - road_type_road_0555 ?next - road_type_road_0512)
    :precondition (and
      (ready-road ?v ?r)
      (road-next ?r ?next)
      (road-open ?r)
      (static-road ?r)
      (not (road-blocked ?r))
    )
    :effect (and
      (not (ready-road ?v ?r))
      (ready-road ?v ?next)
      (increase (travel-time ?v) (travel-duration ?r))
      (increase (sim-time) (travel-duration ?r))
    )
  )

  (:action traverse-road-static-road_0556-road_0670
    :parameters (?v - vehicle ?r - road_type_road_0556 ?next - road_type_road_0670)
    :precondition (and
      (ready-road ?v ?r)
      (road-next ?r ?next)
      (road-open ?r)
      (static-road ?r)
      (not (road-blocked ?r))
    )
    :effect (and
      (not (ready-road ?v ?r))
      (ready-road ?v ?next)
      (increase (travel-time ?v) (travel-duration ?r))
      (increase (sim-time) (travel-duration ?r))
    )
  )

  (:action traverse-road-static-road_0556-road_0671
    :parameters (?v - vehicle ?r - road_type_road_0556 ?next - road_type_road_0671)
    :precondition (and
      (ready-road ?v ?r)
      (road-next ?r ?next)
      (road-open ?r)
      (static-road ?r)
      (not (road-blocked ?r))
    )
    :effect (and
      (not (ready-road ?v ?r))
      (ready-road ?v ?next)
      (increase (travel-time ?v) (travel-duration ?r))
      (increase (sim-time) (travel-duration ?r))
    )
  )

  (:action traverse-road-static-road_0556-road_0672
    :parameters (?v - vehicle ?r - road_type_road_0556 ?next - road_type_road_0672)
    :precondition (and
      (ready-road ?v ?r)
      (road-next ?r ?next)
      (road-open ?r)
      (static-road ?r)
      (not (road-blocked ?r))
    )
    :effect (and
      (not (ready-road ?v ?r))
      (ready-road ?v ?next)
      (increase (travel-time ?v) (travel-duration ?r))
      (increase (sim-time) (travel-duration ?r))
    )
  )

  (:action traverse-road-static-road_0557-road_0591
    :parameters (?v - vehicle ?r - road_type_road_0557 ?next - road_type_road_0591)
    :precondition (and
      (ready-road ?v ?r)
      (road-next ?r ?next)
      (road-open ?r)
      (static-road ?r)
      (not (road-blocked ?r))
    )
    :effect (and
      (not (ready-road ?v ?r))
      (ready-road ?v ?next)
      (increase (travel-time ?v) (travel-duration ?r))
      (increase (sim-time) (travel-duration ?r))
    )
  )

  (:action traverse-road-static-road_0557-road_0592
    :parameters (?v - vehicle ?r - road_type_road_0557 ?next - road_type_road_0592)
    :precondition (and
      (ready-road ?v ?r)
      (road-next ?r ?next)
      (road-open ?r)
      (static-road ?r)
      (not (road-blocked ?r))
    )
    :effect (and
      (not (ready-road ?v ?r))
      (ready-road ?v ?next)
      (increase (travel-time ?v) (travel-duration ?r))
      (increase (sim-time) (travel-duration ?r))
    )
  )

  (:action traverse-road-static-road_0557-road_0593
    :parameters (?v - vehicle ?r - road_type_road_0557 ?next - road_type_road_0593)
    :precondition (and
      (ready-road ?v ?r)
      (road-next ?r ?next)
      (road-open ?r)
      (static-road ?r)
      (not (road-blocked ?r))
    )
    :effect (and
      (not (ready-road ?v ?r))
      (ready-road ?v ?next)
      (increase (travel-time ?v) (travel-duration ?r))
      (increase (sim-time) (travel-duration ?r))
    )
  )

  (:action traverse-road-static-road_0558-macro_0025
    :parameters (?v - vehicle ?r - road_type_road_0558 ?next - road_type_macro_0025)
    :precondition (and
      (ready-road ?v ?r)
      (road-next ?r ?next)
      (road-open ?r)
      (static-road ?r)
      (not (road-blocked ?r))
    )
    :effect (and
      (not (ready-road ?v ?r))
      (ready-road ?v ?next)
      (increase (travel-time ?v) (travel-duration ?r))
      (increase (sim-time) (travel-duration ?r))
    )
  )

  (:action traverse-road-static-road_0558-road_0305
    :parameters (?v - vehicle ?r - road_type_road_0558 ?next - road_type_road_0305)
    :precondition (and
      (ready-road ?v ?r)
      (road-next ?r ?next)
      (road-open ?r)
      (static-road ?r)
      (not (road-blocked ?r))
    )
    :effect (and
      (not (ready-road ?v ?r))
      (ready-road ?v ?next)
      (increase (travel-time ?v) (travel-duration ?r))
      (increase (sim-time) (travel-duration ?r))
    )
  )

  (:action traverse-road-static-road_0559-macro_0038
    :parameters (?v - vehicle ?r - road_type_road_0559 ?next - road_type_macro_0038)
    :precondition (and
      (ready-road ?v ?r)
      (road-next ?r ?next)
      (road-open ?r)
      (static-road ?r)
      (not (road-blocked ?r))
    )
    :effect (and
      (not (ready-road ?v ?r))
      (ready-road ?v ?next)
      (increase (travel-time ?v) (travel-duration ?r))
      (increase (sim-time) (travel-duration ?r))
    )
  )

  (:action traverse-road-static-road_0559-road_0537
    :parameters (?v - vehicle ?r - road_type_road_0559 ?next - road_type_road_0537)
    :precondition (and
      (ready-road ?v ?r)
      (road-next ?r ?next)
      (road-open ?r)
      (static-road ?r)
      (not (road-blocked ?r))
    )
    :effect (and
      (not (ready-road ?v ?r))
      (ready-road ?v ?next)
      (increase (travel-time ?v) (travel-duration ?r))
      (increase (sim-time) (travel-duration ?r))
    )
  )

  (:action traverse-road-static-road_0561-macro_0032
    :parameters (?v - vehicle ?r - road_type_road_0561 ?next - road_type_macro_0032)
    :precondition (and
      (ready-road ?v ?r)
      (road-next ?r ?next)
      (road-open ?r)
      (static-road ?r)
      (not (road-blocked ?r))
    )
    :effect (and
      (not (ready-road ?v ?r))
      (ready-road ?v ?next)
      (increase (travel-time ?v) (travel-duration ?r))
      (increase (sim-time) (travel-duration ?r))
    )
  )

  (:action traverse-road-static-road_0561-road_0416
    :parameters (?v - vehicle ?r - road_type_road_0561 ?next - road_type_road_0416)
    :precondition (and
      (ready-road ?v ?r)
      (road-next ?r ?next)
      (road-open ?r)
      (static-road ?r)
      (not (road-blocked ?r))
    )
    :effect (and
      (not (ready-road ?v ?r))
      (ready-road ?v ?next)
      (increase (travel-time ?v) (travel-duration ?r))
      (increase (sim-time) (travel-duration ?r))
    )
  )

  (:action traverse-road-static-road_0561-road_0417
    :parameters (?v - vehicle ?r - road_type_road_0561 ?next - road_type_road_0417)
    :precondition (and
      (ready-road ?v ?r)
      (road-next ?r ?next)
      (road-open ?r)
      (static-road ?r)
      (not (road-blocked ?r))
    )
    :effect (and
      (not (ready-road ?v ?r))
      (ready-road ?v ?next)
      (increase (travel-time ?v) (travel-duration ?r))
      (increase (sim-time) (travel-duration ?r))
    )
  )

  (:action traverse-road-static-road_0563-road_0539
    :parameters (?v - vehicle ?r - road_type_road_0563 ?next - road_type_road_0539)
    :precondition (and
      (ready-road ?v ?r)
      (road-next ?r ?next)
      (road-open ?r)
      (static-road ?r)
      (not (road-blocked ?r))
    )
    :effect (and
      (not (ready-road ?v ?r))
      (ready-road ?v ?next)
      (increase (travel-time ?v) (travel-duration ?r))
      (increase (sim-time) (travel-duration ?r))
    )
  )

  (:action traverse-road-static-road_0563-road_0540
    :parameters (?v - vehicle ?r - road_type_road_0563 ?next - road_type_road_0540)
    :precondition (and
      (ready-road ?v ?r)
      (road-next ?r ?next)
      (road-open ?r)
      (static-road ?r)
      (not (road-blocked ?r))
    )
    :effect (and
      (not (ready-road ?v ?r))
      (ready-road ?v ?next)
      (increase (travel-time ?v) (travel-duration ?r))
      (increase (sim-time) (travel-duration ?r))
    )
  )

  (:action traverse-road-static-road_0563-road_0541
    :parameters (?v - vehicle ?r - road_type_road_0563 ?next - road_type_road_0541)
    :precondition (and
      (ready-road ?v ?r)
      (road-next ?r ?next)
      (road-open ?r)
      (static-road ?r)
      (not (road-blocked ?r))
    )
    :effect (and
      (not (ready-road ?v ?r))
      (ready-road ?v ?next)
      (increase (travel-time ?v) (travel-duration ?r))
      (increase (sim-time) (travel-duration ?r))
    )
  )

  (:action traverse-road-static-road_0564-macro_0039
    :parameters (?v - vehicle ?r - road_type_road_0564 ?next - road_type_macro_0039)
    :precondition (and
      (ready-road ?v ?r)
      (road-next ?r ?next)
      (road-open ?r)
      (static-road ?r)
      (not (road-blocked ?r))
    )
    :effect (and
      (not (ready-road ?v ?r))
      (ready-road ?v ?next)
      (increase (travel-time ?v) (travel-duration ?r))
      (increase (sim-time) (travel-duration ?r))
    )
  )

  (:action traverse-road-static-road_0564-road_0566
    :parameters (?v - vehicle ?r - road_type_road_0564 ?next - road_type_road_0566)
    :precondition (and
      (ready-road ?v ?r)
      (road-next ?r ?next)
      (road-open ?r)
      (static-road ?r)
      (not (road-blocked ?r))
    )
    :effect (and
      (not (ready-road ?v ?r))
      (ready-road ?v ?next)
      (increase (travel-time ?v) (travel-duration ?r))
      (increase (sim-time) (travel-duration ?r))
    )
  )

  (:action traverse-road-static-road_0566-road_0563
    :parameters (?v - vehicle ?r - road_type_road_0566 ?next - road_type_road_0563)
    :precondition (and
      (ready-road ?v ?r)
      (road-next ?r ?next)
      (road-open ?r)
      (static-road ?r)
      (not (road-blocked ?r))
    )
    :effect (and
      (not (ready-road ?v ?r))
      (ready-road ?v ?next)
      (increase (travel-time ?v) (travel-duration ?r))
      (increase (sim-time) (travel-duration ?r))
    )
  )

  (:action traverse-road-static-road_0566-road_0564
    :parameters (?v - vehicle ?r - road_type_road_0566 ?next - road_type_road_0564)
    :precondition (and
      (ready-road ?v ?r)
      (road-next ?r ?next)
      (road-open ?r)
      (static-road ?r)
      (not (road-blocked ?r))
    )
    :effect (and
      (not (ready-road ?v ?r))
      (ready-road ?v ?next)
      (increase (travel-time ?v) (travel-duration ?r))
      (increase (sim-time) (travel-duration ?r))
    )
  )

  (:action traverse-road-static-road_0567-road_0337
    :parameters (?v - vehicle ?r - road_type_road_0567 ?next - road_type_road_0337)
    :precondition (and
      (ready-road ?v ?r)
      (road-next ?r ?next)
      (road-open ?r)
      (static-road ?r)
      (not (road-blocked ?r))
    )
    :effect (and
      (not (ready-road ?v ?r))
      (ready-road ?v ?next)
      (increase (travel-time ?v) (travel-duration ?r))
      (increase (sim-time) (travel-duration ?r))
    )
  )

  (:action traverse-road-static-road_0568-road_0640
    :parameters (?v - vehicle ?r - road_type_road_0568 ?next - road_type_road_0640)
    :precondition (and
      (ready-road ?v ?r)
      (road-next ?r ?next)
      (road-open ?r)
      (static-road ?r)
      (not (road-blocked ?r))
    )
    :effect (and
      (not (ready-road ?v ?r))
      (ready-road ?v ?next)
      (increase (travel-time ?v) (travel-duration ?r))
      (increase (sim-time) (travel-duration ?r))
    )
  )

  (:action traverse-road-static-road_0568-road_0641
    :parameters (?v - vehicle ?r - road_type_road_0568 ?next - road_type_road_0641)
    :precondition (and
      (ready-road ?v ?r)
      (road-next ?r ?next)
      (road-open ?r)
      (static-road ?r)
      (not (road-blocked ?r))
    )
    :effect (and
      (not (ready-road ?v ?r))
      (ready-road ?v ?next)
      (increase (travel-time ?v) (travel-duration ?r))
      (increase (sim-time) (travel-duration ?r))
    )
  )

  (:action traverse-road-dynamic-road_0569-road_0456-tw_00000
    :parameters (?v - vehicle ?r - road_type_road_0569 ?next - road_type_road_0456 ?w - window_type_tw_00000)
    :precondition (and
      (ready-road ?v ?r)
      (road-next ?r ?next)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
    )
    :effect (and
      (not (ready-road ?v ?r))
      (ready-road ?v ?next)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-road_0569-road_0456-tw_00030
    :parameters (?v - vehicle ?r - road_type_road_0569 ?next - road_type_road_0456 ?w - window_type_tw_00030)
    :precondition (and
      (ready-road ?v ?r)
      (road-next ?r ?next)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
    )
    :effect (and
      (not (ready-road ?v ?r))
      (ready-road ?v ?next)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-road_0569-road_0456-tw_00060
    :parameters (?v - vehicle ?r - road_type_road_0569 ?next - road_type_road_0456 ?w - window_type_tw_00060)
    :precondition (and
      (ready-road ?v ?r)
      (road-next ?r ?next)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
    )
    :effect (and
      (not (ready-road ?v ?r))
      (ready-road ?v ?next)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-road_0569-road_0456-tw_00090
    :parameters (?v - vehicle ?r - road_type_road_0569 ?next - road_type_road_0456 ?w - window_type_tw_00090)
    :precondition (and
      (ready-road ?v ?r)
      (road-next ?r ?next)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
    )
    :effect (and
      (not (ready-road ?v ?r))
      (ready-road ?v ?next)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-road_0569-road_0456-tw_00120
    :parameters (?v - vehicle ?r - road_type_road_0569 ?next - road_type_road_0456 ?w - window_type_tw_00120)
    :precondition (and
      (ready-road ?v ?r)
      (road-next ?r ?next)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
    )
    :effect (and
      (not (ready-road ?v ?r))
      (ready-road ?v ?next)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-road_0569-road_0456-tw_00150
    :parameters (?v - vehicle ?r - road_type_road_0569 ?next - road_type_road_0456 ?w - window_type_tw_00150)
    :precondition (and
      (ready-road ?v ?r)
      (road-next ?r ?next)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
    )
    :effect (and
      (not (ready-road ?v ?r))
      (ready-road ?v ?next)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-road_0569-road_0456-tw_00180
    :parameters (?v - vehicle ?r - road_type_road_0569 ?next - road_type_road_0456 ?w - window_type_tw_00180)
    :precondition (and
      (ready-road ?v ?r)
      (road-next ?r ?next)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
    )
    :effect (and
      (not (ready-road ?v ?r))
      (ready-road ?v ?next)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-road_0569-road_0456-tw_00210
    :parameters (?v - vehicle ?r - road_type_road_0569 ?next - road_type_road_0456 ?w - window_type_tw_00210)
    :precondition (and
      (ready-road ?v ?r)
      (road-next ?r ?next)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
    )
    :effect (and
      (not (ready-road ?v ?r))
      (ready-road ?v ?next)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-road_0569-road_0456-tw_00240
    :parameters (?v - vehicle ?r - road_type_road_0569 ?next - road_type_road_0456 ?w - window_type_tw_00240)
    :precondition (and
      (ready-road ?v ?r)
      (road-next ?r ?next)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
    )
    :effect (and
      (not (ready-road ?v ?r))
      (ready-road ?v ?next)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-road_0569-road_0456-tw_00270
    :parameters (?v - vehicle ?r - road_type_road_0569 ?next - road_type_road_0456 ?w - window_type_tw_00270)
    :precondition (and
      (ready-road ?v ?r)
      (road-next ?r ?next)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
    )
    :effect (and
      (not (ready-road ?v ?r))
      (ready-road ?v ?next)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-road_0569-road_0456-tw_00300
    :parameters (?v - vehicle ?r - road_type_road_0569 ?next - road_type_road_0456 ?w - window_type_tw_00300)
    :precondition (and
      (ready-road ?v ?r)
      (road-next ?r ?next)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
    )
    :effect (and
      (not (ready-road ?v ?r))
      (ready-road ?v ?next)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-road_0569-road_0456-tw_00330
    :parameters (?v - vehicle ?r - road_type_road_0569 ?next - road_type_road_0456 ?w - window_type_tw_00330)
    :precondition (and
      (ready-road ?v ?r)
      (road-next ?r ?next)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
    )
    :effect (and
      (not (ready-road ?v ?r))
      (ready-road ?v ?next)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-road_0569-road_0456-tw_00360
    :parameters (?v - vehicle ?r - road_type_road_0569 ?next - road_type_road_0456 ?w - window_type_tw_00360)
    :precondition (and
      (ready-road ?v ?r)
      (road-next ?r ?next)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
    )
    :effect (and
      (not (ready-road ?v ?r))
      (ready-road ?v ?next)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-road_0569-road_0456-tw_00390
    :parameters (?v - vehicle ?r - road_type_road_0569 ?next - road_type_road_0456 ?w - window_type_tw_00390)
    :precondition (and
      (ready-road ?v ?r)
      (road-next ?r ?next)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
    )
    :effect (and
      (not (ready-road ?v ?r))
      (ready-road ?v ?next)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-road_0569-road_0456-tw_00420
    :parameters (?v - vehicle ?r - road_type_road_0569 ?next - road_type_road_0456 ?w - window_type_tw_00420)
    :precondition (and
      (ready-road ?v ?r)
      (road-next ?r ?next)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
    )
    :effect (and
      (not (ready-road ?v ?r))
      (ready-road ?v ?next)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-road_0569-road_0456-tw_00450
    :parameters (?v - vehicle ?r - road_type_road_0569 ?next - road_type_road_0456 ?w - window_type_tw_00450)
    :precondition (and
      (ready-road ?v ?r)
      (road-next ?r ?next)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
    )
    :effect (and
      (not (ready-road ?v ?r))
      (ready-road ?v ?next)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-road_0569-road_0456-tw_00480
    :parameters (?v - vehicle ?r - road_type_road_0569 ?next - road_type_road_0456 ?w - window_type_tw_00480)
    :precondition (and
      (ready-road ?v ?r)
      (road-next ?r ?next)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
    )
    :effect (and
      (not (ready-road ?v ?r))
      (ready-road ?v ?next)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-road_0569-road_0456-tw_00510
    :parameters (?v - vehicle ?r - road_type_road_0569 ?next - road_type_road_0456 ?w - window_type_tw_00510)
    :precondition (and
      (ready-road ?v ?r)
      (road-next ?r ?next)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
    )
    :effect (and
      (not (ready-road ?v ?r))
      (ready-road ?v ?next)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-road_0569-road_0456-tw_00540
    :parameters (?v - vehicle ?r - road_type_road_0569 ?next - road_type_road_0456 ?w - window_type_tw_00540)
    :precondition (and
      (ready-road ?v ?r)
      (road-next ?r ?next)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
    )
    :effect (and
      (not (ready-road ?v ?r))
      (ready-road ?v ?next)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-road_0569-road_0456-tw_00570
    :parameters (?v - vehicle ?r - road_type_road_0569 ?next - road_type_road_0456 ?w - window_type_tw_00570)
    :precondition (and
      (ready-road ?v ?r)
      (road-next ?r ?next)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
    )
    :effect (and
      (not (ready-road ?v ?r))
      (ready-road ?v ?next)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-road_0569-road_0456-tw_00600
    :parameters (?v - vehicle ?r - road_type_road_0569 ?next - road_type_road_0456 ?w - window_type_tw_00600)
    :precondition (and
      (ready-road ?v ?r)
      (road-next ?r ?next)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
    )
    :effect (and
      (not (ready-road ?v ?r))
      (ready-road ?v ?next)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-road_0569-road_0457-tw_00000
    :parameters (?v - vehicle ?r - road_type_road_0569 ?next - road_type_road_0457 ?w - window_type_tw_00000)
    :precondition (and
      (ready-road ?v ?r)
      (road-next ?r ?next)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
    )
    :effect (and
      (not (ready-road ?v ?r))
      (ready-road ?v ?next)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-road_0569-road_0457-tw_00030
    :parameters (?v - vehicle ?r - road_type_road_0569 ?next - road_type_road_0457 ?w - window_type_tw_00030)
    :precondition (and
      (ready-road ?v ?r)
      (road-next ?r ?next)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
    )
    :effect (and
      (not (ready-road ?v ?r))
      (ready-road ?v ?next)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-road_0569-road_0457-tw_00060
    :parameters (?v - vehicle ?r - road_type_road_0569 ?next - road_type_road_0457 ?w - window_type_tw_00060)
    :precondition (and
      (ready-road ?v ?r)
      (road-next ?r ?next)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
    )
    :effect (and
      (not (ready-road ?v ?r))
      (ready-road ?v ?next)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-road_0569-road_0457-tw_00090
    :parameters (?v - vehicle ?r - road_type_road_0569 ?next - road_type_road_0457 ?w - window_type_tw_00090)
    :precondition (and
      (ready-road ?v ?r)
      (road-next ?r ?next)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
    )
    :effect (and
      (not (ready-road ?v ?r))
      (ready-road ?v ?next)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-road_0569-road_0457-tw_00120
    :parameters (?v - vehicle ?r - road_type_road_0569 ?next - road_type_road_0457 ?w - window_type_tw_00120)
    :precondition (and
      (ready-road ?v ?r)
      (road-next ?r ?next)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
    )
    :effect (and
      (not (ready-road ?v ?r))
      (ready-road ?v ?next)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-road_0569-road_0457-tw_00150
    :parameters (?v - vehicle ?r - road_type_road_0569 ?next - road_type_road_0457 ?w - window_type_tw_00150)
    :precondition (and
      (ready-road ?v ?r)
      (road-next ?r ?next)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
    )
    :effect (and
      (not (ready-road ?v ?r))
      (ready-road ?v ?next)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-road_0569-road_0457-tw_00180
    :parameters (?v - vehicle ?r - road_type_road_0569 ?next - road_type_road_0457 ?w - window_type_tw_00180)
    :precondition (and
      (ready-road ?v ?r)
      (road-next ?r ?next)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
    )
    :effect (and
      (not (ready-road ?v ?r))
      (ready-road ?v ?next)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-road_0569-road_0457-tw_00210
    :parameters (?v - vehicle ?r - road_type_road_0569 ?next - road_type_road_0457 ?w - window_type_tw_00210)
    :precondition (and
      (ready-road ?v ?r)
      (road-next ?r ?next)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
    )
    :effect (and
      (not (ready-road ?v ?r))
      (ready-road ?v ?next)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-road_0569-road_0457-tw_00240
    :parameters (?v - vehicle ?r - road_type_road_0569 ?next - road_type_road_0457 ?w - window_type_tw_00240)
    :precondition (and
      (ready-road ?v ?r)
      (road-next ?r ?next)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
    )
    :effect (and
      (not (ready-road ?v ?r))
      (ready-road ?v ?next)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-road_0569-road_0457-tw_00270
    :parameters (?v - vehicle ?r - road_type_road_0569 ?next - road_type_road_0457 ?w - window_type_tw_00270)
    :precondition (and
      (ready-road ?v ?r)
      (road-next ?r ?next)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
    )
    :effect (and
      (not (ready-road ?v ?r))
      (ready-road ?v ?next)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-road_0569-road_0457-tw_00300
    :parameters (?v - vehicle ?r - road_type_road_0569 ?next - road_type_road_0457 ?w - window_type_tw_00300)
    :precondition (and
      (ready-road ?v ?r)
      (road-next ?r ?next)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
    )
    :effect (and
      (not (ready-road ?v ?r))
      (ready-road ?v ?next)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-road_0569-road_0457-tw_00330
    :parameters (?v - vehicle ?r - road_type_road_0569 ?next - road_type_road_0457 ?w - window_type_tw_00330)
    :precondition (and
      (ready-road ?v ?r)
      (road-next ?r ?next)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
    )
    :effect (and
      (not (ready-road ?v ?r))
      (ready-road ?v ?next)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-road_0569-road_0457-tw_00360
    :parameters (?v - vehicle ?r - road_type_road_0569 ?next - road_type_road_0457 ?w - window_type_tw_00360)
    :precondition (and
      (ready-road ?v ?r)
      (road-next ?r ?next)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
    )
    :effect (and
      (not (ready-road ?v ?r))
      (ready-road ?v ?next)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-road_0569-road_0457-tw_00390
    :parameters (?v - vehicle ?r - road_type_road_0569 ?next - road_type_road_0457 ?w - window_type_tw_00390)
    :precondition (and
      (ready-road ?v ?r)
      (road-next ?r ?next)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
    )
    :effect (and
      (not (ready-road ?v ?r))
      (ready-road ?v ?next)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-road_0569-road_0457-tw_00420
    :parameters (?v - vehicle ?r - road_type_road_0569 ?next - road_type_road_0457 ?w - window_type_tw_00420)
    :precondition (and
      (ready-road ?v ?r)
      (road-next ?r ?next)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
    )
    :effect (and
      (not (ready-road ?v ?r))
      (ready-road ?v ?next)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-road_0569-road_0457-tw_00450
    :parameters (?v - vehicle ?r - road_type_road_0569 ?next - road_type_road_0457 ?w - window_type_tw_00450)
    :precondition (and
      (ready-road ?v ?r)
      (road-next ?r ?next)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
    )
    :effect (and
      (not (ready-road ?v ?r))
      (ready-road ?v ?next)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-road_0569-road_0457-tw_00480
    :parameters (?v - vehicle ?r - road_type_road_0569 ?next - road_type_road_0457 ?w - window_type_tw_00480)
    :precondition (and
      (ready-road ?v ?r)
      (road-next ?r ?next)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
    )
    :effect (and
      (not (ready-road ?v ?r))
      (ready-road ?v ?next)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-road_0569-road_0457-tw_00510
    :parameters (?v - vehicle ?r - road_type_road_0569 ?next - road_type_road_0457 ?w - window_type_tw_00510)
    :precondition (and
      (ready-road ?v ?r)
      (road-next ?r ?next)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
    )
    :effect (and
      (not (ready-road ?v ?r))
      (ready-road ?v ?next)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-road_0569-road_0457-tw_00540
    :parameters (?v - vehicle ?r - road_type_road_0569 ?next - road_type_road_0457 ?w - window_type_tw_00540)
    :precondition (and
      (ready-road ?v ?r)
      (road-next ?r ?next)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
    )
    :effect (and
      (not (ready-road ?v ?r))
      (ready-road ?v ?next)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-road_0569-road_0457-tw_00570
    :parameters (?v - vehicle ?r - road_type_road_0569 ?next - road_type_road_0457 ?w - window_type_tw_00570)
    :precondition (and
      (ready-road ?v ?r)
      (road-next ?r ?next)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
    )
    :effect (and
      (not (ready-road ?v ?r))
      (ready-road ?v ?next)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-road_0569-road_0457-tw_00600
    :parameters (?v - vehicle ?r - road_type_road_0569 ?next - road_type_road_0457 ?w - window_type_tw_00600)
    :precondition (and
      (ready-road ?v ?r)
      (road-next ?r ?next)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
    )
    :effect (and
      (not (ready-road ?v ?r))
      (ready-road ?v ?next)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-road_0569-road_0458-tw_00000
    :parameters (?v - vehicle ?r - road_type_road_0569 ?next - road_type_road_0458 ?w - window_type_tw_00000)
    :precondition (and
      (ready-road ?v ?r)
      (road-next ?r ?next)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
    )
    :effect (and
      (not (ready-road ?v ?r))
      (ready-road ?v ?next)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-road_0569-road_0458-tw_00030
    :parameters (?v - vehicle ?r - road_type_road_0569 ?next - road_type_road_0458 ?w - window_type_tw_00030)
    :precondition (and
      (ready-road ?v ?r)
      (road-next ?r ?next)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
    )
    :effect (and
      (not (ready-road ?v ?r))
      (ready-road ?v ?next)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-road_0569-road_0458-tw_00060
    :parameters (?v - vehicle ?r - road_type_road_0569 ?next - road_type_road_0458 ?w - window_type_tw_00060)
    :precondition (and
      (ready-road ?v ?r)
      (road-next ?r ?next)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
    )
    :effect (and
      (not (ready-road ?v ?r))
      (ready-road ?v ?next)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-road_0569-road_0458-tw_00090
    :parameters (?v - vehicle ?r - road_type_road_0569 ?next - road_type_road_0458 ?w - window_type_tw_00090)
    :precondition (and
      (ready-road ?v ?r)
      (road-next ?r ?next)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
    )
    :effect (and
      (not (ready-road ?v ?r))
      (ready-road ?v ?next)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-road_0569-road_0458-tw_00120
    :parameters (?v - vehicle ?r - road_type_road_0569 ?next - road_type_road_0458 ?w - window_type_tw_00120)
    :precondition (and
      (ready-road ?v ?r)
      (road-next ?r ?next)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
    )
    :effect (and
      (not (ready-road ?v ?r))
      (ready-road ?v ?next)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-road_0569-road_0458-tw_00150
    :parameters (?v - vehicle ?r - road_type_road_0569 ?next - road_type_road_0458 ?w - window_type_tw_00150)
    :precondition (and
      (ready-road ?v ?r)
      (road-next ?r ?next)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
    )
    :effect (and
      (not (ready-road ?v ?r))
      (ready-road ?v ?next)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-road_0569-road_0458-tw_00180
    :parameters (?v - vehicle ?r - road_type_road_0569 ?next - road_type_road_0458 ?w - window_type_tw_00180)
    :precondition (and
      (ready-road ?v ?r)
      (road-next ?r ?next)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
    )
    :effect (and
      (not (ready-road ?v ?r))
      (ready-road ?v ?next)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-road_0569-road_0458-tw_00210
    :parameters (?v - vehicle ?r - road_type_road_0569 ?next - road_type_road_0458 ?w - window_type_tw_00210)
    :precondition (and
      (ready-road ?v ?r)
      (road-next ?r ?next)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
    )
    :effect (and
      (not (ready-road ?v ?r))
      (ready-road ?v ?next)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-road_0569-road_0458-tw_00240
    :parameters (?v - vehicle ?r - road_type_road_0569 ?next - road_type_road_0458 ?w - window_type_tw_00240)
    :precondition (and
      (ready-road ?v ?r)
      (road-next ?r ?next)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
    )
    :effect (and
      (not (ready-road ?v ?r))
      (ready-road ?v ?next)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-road_0569-road_0458-tw_00270
    :parameters (?v - vehicle ?r - road_type_road_0569 ?next - road_type_road_0458 ?w - window_type_tw_00270)
    :precondition (and
      (ready-road ?v ?r)
      (road-next ?r ?next)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
    )
    :effect (and
      (not (ready-road ?v ?r))
      (ready-road ?v ?next)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-road_0569-road_0458-tw_00300
    :parameters (?v - vehicle ?r - road_type_road_0569 ?next - road_type_road_0458 ?w - window_type_tw_00300)
    :precondition (and
      (ready-road ?v ?r)
      (road-next ?r ?next)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
    )
    :effect (and
      (not (ready-road ?v ?r))
      (ready-road ?v ?next)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-road_0569-road_0458-tw_00330
    :parameters (?v - vehicle ?r - road_type_road_0569 ?next - road_type_road_0458 ?w - window_type_tw_00330)
    :precondition (and
      (ready-road ?v ?r)
      (road-next ?r ?next)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
    )
    :effect (and
      (not (ready-road ?v ?r))
      (ready-road ?v ?next)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-road_0569-road_0458-tw_00360
    :parameters (?v - vehicle ?r - road_type_road_0569 ?next - road_type_road_0458 ?w - window_type_tw_00360)
    :precondition (and
      (ready-road ?v ?r)
      (road-next ?r ?next)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
    )
    :effect (and
      (not (ready-road ?v ?r))
      (ready-road ?v ?next)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-road_0569-road_0458-tw_00390
    :parameters (?v - vehicle ?r - road_type_road_0569 ?next - road_type_road_0458 ?w - window_type_tw_00390)
    :precondition (and
      (ready-road ?v ?r)
      (road-next ?r ?next)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
    )
    :effect (and
      (not (ready-road ?v ?r))
      (ready-road ?v ?next)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-road_0569-road_0458-tw_00420
    :parameters (?v - vehicle ?r - road_type_road_0569 ?next - road_type_road_0458 ?w - window_type_tw_00420)
    :precondition (and
      (ready-road ?v ?r)
      (road-next ?r ?next)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
    )
    :effect (and
      (not (ready-road ?v ?r))
      (ready-road ?v ?next)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-road_0569-road_0458-tw_00450
    :parameters (?v - vehicle ?r - road_type_road_0569 ?next - road_type_road_0458 ?w - window_type_tw_00450)
    :precondition (and
      (ready-road ?v ?r)
      (road-next ?r ?next)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
    )
    :effect (and
      (not (ready-road ?v ?r))
      (ready-road ?v ?next)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-road_0569-road_0458-tw_00480
    :parameters (?v - vehicle ?r - road_type_road_0569 ?next - road_type_road_0458 ?w - window_type_tw_00480)
    :precondition (and
      (ready-road ?v ?r)
      (road-next ?r ?next)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
    )
    :effect (and
      (not (ready-road ?v ?r))
      (ready-road ?v ?next)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-road_0569-road_0458-tw_00510
    :parameters (?v - vehicle ?r - road_type_road_0569 ?next - road_type_road_0458 ?w - window_type_tw_00510)
    :precondition (and
      (ready-road ?v ?r)
      (road-next ?r ?next)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
    )
    :effect (and
      (not (ready-road ?v ?r))
      (ready-road ?v ?next)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-road_0569-road_0458-tw_00540
    :parameters (?v - vehicle ?r - road_type_road_0569 ?next - road_type_road_0458 ?w - window_type_tw_00540)
    :precondition (and
      (ready-road ?v ?r)
      (road-next ?r ?next)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
    )
    :effect (and
      (not (ready-road ?v ?r))
      (ready-road ?v ?next)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-road_0569-road_0458-tw_00570
    :parameters (?v - vehicle ?r - road_type_road_0569 ?next - road_type_road_0458 ?w - window_type_tw_00570)
    :precondition (and
      (ready-road ?v ?r)
      (road-next ?r ?next)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
    )
    :effect (and
      (not (ready-road ?v ?r))
      (ready-road ?v ?next)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-road_0569-road_0458-tw_00600
    :parameters (?v - vehicle ?r - road_type_road_0569 ?next - road_type_road_0458 ?w - window_type_tw_00600)
    :precondition (and
      (ready-road ?v ?r)
      (road-next ?r ?next)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
    )
    :effect (and
      (not (ready-road ?v ?r))
      (ready-road ?v ?next)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-static-road_0572-road_0574
    :parameters (?v - vehicle ?r - road_type_road_0572 ?next - road_type_road_0574)
    :precondition (and
      (ready-road ?v ?r)
      (road-next ?r ?next)
      (road-open ?r)
      (static-road ?r)
      (not (road-blocked ?r))
    )
    :effect (and
      (not (ready-road ?v ?r))
      (ready-road ?v ?next)
      (increase (travel-time ?v) (travel-duration ?r))
      (increase (sim-time) (travel-duration ?r))
    )
  )

  (:action traverse-road-static-road_0572-road_0575
    :parameters (?v - vehicle ?r - road_type_road_0572 ?next - road_type_road_0575)
    :precondition (and
      (ready-road ?v ?r)
      (road-next ?r ?next)
      (road-open ?r)
      (static-road ?r)
      (not (road-blocked ?r))
    )
    :effect (and
      (not (ready-road ?v ?r))
      (ready-road ?v ?next)
      (increase (travel-time ?v) (travel-duration ?r))
      (increase (sim-time) (travel-duration ?r))
    )
  )

  (:action traverse-road-static-road_0572-road_0576
    :parameters (?v - vehicle ?r - road_type_road_0572 ?next - road_type_road_0576)
    :precondition (and
      (ready-road ?v ?r)
      (road-next ?r ?next)
      (road-open ?r)
      (static-road ?r)
      (not (road-blocked ?r))
    )
    :effect (and
      (not (ready-road ?v ?r))
      (ready-road ?v ?next)
      (increase (travel-time ?v) (travel-duration ?r))
      (increase (sim-time) (travel-duration ?r))
    )
  )

  (:action traverse-road-static-road_0573-road_0490
    :parameters (?v - vehicle ?r - road_type_road_0573 ?next - road_type_road_0490)
    :precondition (and
      (ready-road ?v ?r)
      (road-next ?r ?next)
      (road-open ?r)
      (static-road ?r)
      (not (road-blocked ?r))
    )
    :effect (and
      (not (ready-road ?v ?r))
      (ready-road ?v ?next)
      (increase (travel-time ?v) (travel-duration ?r))
      (increase (sim-time) (travel-duration ?r))
    )
  )

  (:action traverse-road-static-road_0573-road_0491
    :parameters (?v - vehicle ?r - road_type_road_0573 ?next - road_type_road_0491)
    :precondition (and
      (ready-road ?v ?r)
      (road-next ?r ?next)
      (road-open ?r)
      (static-road ?r)
      (not (road-blocked ?r))
    )
    :effect (and
      (not (ready-road ?v ?r))
      (ready-road ?v ?next)
      (increase (travel-time ?v) (travel-duration ?r))
      (increase (sim-time) (travel-duration ?r))
    )
  )

  (:action traverse-road-static-road_0573-road_0492
    :parameters (?v - vehicle ?r - road_type_road_0573 ?next - road_type_road_0492)
    :precondition (and
      (ready-road ?v ?r)
      (road-next ?r ?next)
      (road-open ?r)
      (static-road ?r)
      (not (road-blocked ?r))
    )
    :effect (and
      (not (ready-road ?v ?r))
      (ready-road ?v ?next)
      (increase (travel-time ?v) (travel-duration ?r))
      (increase (sim-time) (travel-duration ?r))
    )
  )

  (:action traverse-road-static-road_0574-road_0845
    :parameters (?v - vehicle ?r - road_type_road_0574 ?next - road_type_road_0845)
    :precondition (and
      (ready-road ?v ?r)
      (road-next ?r ?next)
      (road-open ?r)
      (static-road ?r)
      (not (road-blocked ?r))
    )
    :effect (and
      (not (ready-road ?v ?r))
      (ready-road ?v ?next)
      (increase (travel-time ?v) (travel-duration ?r))
      (increase (sim-time) (travel-duration ?r))
    )
  )

  (:action traverse-road-static-road_0575-macro_0040
    :parameters (?v - vehicle ?r - road_type_road_0575 ?next - road_type_macro_0040)
    :precondition (and
      (ready-road ?v ?r)
      (road-next ?r ?next)
      (road-open ?r)
      (static-road ?r)
      (not (road-blocked ?r))
    )
    :effect (and
      (not (ready-road ?v ?r))
      (ready-road ?v ?next)
      (increase (travel-time ?v) (travel-duration ?r))
      (increase (sim-time) (travel-duration ?r))
    )
  )

  (:action traverse-road-static-road_0575-macro_0041
    :parameters (?v - vehicle ?r - road_type_road_0575 ?next - road_type_macro_0041)
    :precondition (and
      (ready-road ?v ?r)
      (road-next ?r ?next)
      (road-open ?r)
      (static-road ?r)
      (not (road-blocked ?r))
    )
    :effect (and
      (not (ready-road ?v ?r))
      (ready-road ?v ?next)
      (increase (travel-time ?v) (travel-duration ?r))
      (increase (sim-time) (travel-duration ?r))
    )
  )

  (:action traverse-road-static-road_0575-road_0572
    :parameters (?v - vehicle ?r - road_type_road_0575 ?next - road_type_road_0572)
    :precondition (and
      (ready-road ?v ?r)
      (road-next ?r ?next)
      (road-open ?r)
      (static-road ?r)
      (not (road-blocked ?r))
    )
    :effect (and
      (not (ready-road ?v ?r))
      (ready-road ?v ?next)
      (increase (travel-time ?v) (travel-duration ?r))
      (increase (sim-time) (travel-duration ?r))
    )
  )

  (:action traverse-road-static-road_0575-road_0573
    :parameters (?v - vehicle ?r - road_type_road_0575 ?next - road_type_road_0573)
    :precondition (and
      (ready-road ?v ?r)
      (road-next ?r ?next)
      (road-open ?r)
      (static-road ?r)
      (not (road-blocked ?r))
    )
    :effect (and
      (not (ready-road ?v ?r))
      (ready-road ?v ?next)
      (increase (travel-time ?v) (travel-duration ?r))
      (increase (sim-time) (travel-duration ?r))
    )
  )

  (:action traverse-road-static-road_0577-road_0594
    :parameters (?v - vehicle ?r - road_type_road_0577 ?next - road_type_road_0594)
    :precondition (and
      (ready-road ?v ?r)
      (road-next ?r ?next)
      (road-open ?r)
      (static-road ?r)
      (not (road-blocked ?r))
    )
    :effect (and
      (not (ready-road ?v ?r))
      (ready-road ?v ?next)
      (increase (travel-time ?v) (travel-duration ?r))
      (increase (sim-time) (travel-duration ?r))
    )
  )

  (:action traverse-road-static-road_0577-road_0595
    :parameters (?v - vehicle ?r - road_type_road_0577 ?next - road_type_road_0595)
    :precondition (and
      (ready-road ?v ?r)
      (road-next ?r ?next)
      (road-open ?r)
      (static-road ?r)
      (not (road-blocked ?r))
    )
    :effect (and
      (not (ready-road ?v ?r))
      (ready-road ?v ?next)
      (increase (travel-time ?v) (travel-duration ?r))
      (increase (sim-time) (travel-duration ?r))
    )
  )

  (:action traverse-road-static-road_0578-road_0163
    :parameters (?v - vehicle ?r - road_type_road_0578 ?next - road_type_road_0163)
    :precondition (and
      (ready-road ?v ?r)
      (road-next ?r ?next)
      (road-open ?r)
      (static-road ?r)
      (not (road-blocked ?r))
    )
    :effect (and
      (not (ready-road ?v ?r))
      (ready-road ?v ?next)
      (increase (travel-time ?v) (travel-duration ?r))
      (increase (sim-time) (travel-duration ?r))
    )
  )

  (:action traverse-road-static-road_0578-road_0164
    :parameters (?v - vehicle ?r - road_type_road_0578 ?next - road_type_road_0164)
    :precondition (and
      (ready-road ?v ?r)
      (road-next ?r ?next)
      (road-open ?r)
      (static-road ?r)
      (not (road-blocked ?r))
    )
    :effect (and
      (not (ready-road ?v ?r))
      (ready-road ?v ?next)
      (increase (travel-time ?v) (travel-duration ?r))
      (increase (sim-time) (travel-duration ?r))
    )
  )

  (:action traverse-road-static-road_0579-road_0586
    :parameters (?v - vehicle ?r - road_type_road_0579 ?next - road_type_road_0586)
    :precondition (and
      (ready-road ?v ?r)
      (road-next ?r ?next)
      (road-open ?r)
      (static-road ?r)
      (not (road-blocked ?r))
    )
    :effect (and
      (not (ready-road ?v ?r))
      (ready-road ?v ?next)
      (increase (travel-time ?v) (travel-duration ?r))
      (increase (sim-time) (travel-duration ?r))
    )
  )

  (:action traverse-road-static-road_0579-road_0587
    :parameters (?v - vehicle ?r - road_type_road_0579 ?next - road_type_road_0587)
    :precondition (and
      (ready-road ?v ?r)
      (road-next ?r ?next)
      (road-open ?r)
      (static-road ?r)
      (not (road-blocked ?r))
    )
    :effect (and
      (not (ready-road ?v ?r))
      (ready-road ?v ?next)
      (increase (travel-time ?v) (travel-duration ?r))
      (increase (sim-time) (travel-duration ?r))
    )
  )

  (:action traverse-road-static-road_0579-road_0588
    :parameters (?v - vehicle ?r - road_type_road_0579 ?next - road_type_road_0588)
    :precondition (and
      (ready-road ?v ?r)
      (road-next ?r ?next)
      (road-open ?r)
      (static-road ?r)
      (not (road-blocked ?r))
    )
    :effect (and
      (not (ready-road ?v ?r))
      (ready-road ?v ?next)
      (increase (travel-time ?v) (travel-duration ?r))
      (increase (sim-time) (travel-duration ?r))
    )
  )

  (:action traverse-road-static-road_0580-road_0391
    :parameters (?v - vehicle ?r - road_type_road_0580 ?next - road_type_road_0391)
    :precondition (and
      (ready-road ?v ?r)
      (road-next ?r ?next)
      (road-open ?r)
      (static-road ?r)
      (not (road-blocked ?r))
    )
    :effect (and
      (not (ready-road ?v ?r))
      (ready-road ?v ?next)
      (increase (travel-time ?v) (travel-duration ?r))
      (increase (sim-time) (travel-duration ?r))
    )
  )

  (:action traverse-road-static-road_0580-road_0392
    :parameters (?v - vehicle ?r - road_type_road_0580 ?next - road_type_road_0392)
    :precondition (and
      (ready-road ?v ?r)
      (road-next ?r ?next)
      (road-open ?r)
      (static-road ?r)
      (not (road-blocked ?r))
    )
    :effect (and
      (not (ready-road ?v ?r))
      (ready-road ?v ?next)
      (increase (travel-time ?v) (travel-duration ?r))
      (increase (sim-time) (travel-duration ?r))
    )
  )

  (:action traverse-road-dynamic-road_0581-road_0550-tw_00000
    :parameters (?v - vehicle ?r - road_type_road_0581 ?next - road_type_road_0550 ?w - window_type_tw_00000)
    :precondition (and
      (ready-road ?v ?r)
      (road-next ?r ?next)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
    )
    :effect (and
      (not (ready-road ?v ?r))
      (ready-road ?v ?next)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-road_0581-road_0550-tw_00030
    :parameters (?v - vehicle ?r - road_type_road_0581 ?next - road_type_road_0550 ?w - window_type_tw_00030)
    :precondition (and
      (ready-road ?v ?r)
      (road-next ?r ?next)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
    )
    :effect (and
      (not (ready-road ?v ?r))
      (ready-road ?v ?next)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-road_0581-road_0550-tw_00060
    :parameters (?v - vehicle ?r - road_type_road_0581 ?next - road_type_road_0550 ?w - window_type_tw_00060)
    :precondition (and
      (ready-road ?v ?r)
      (road-next ?r ?next)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
    )
    :effect (and
      (not (ready-road ?v ?r))
      (ready-road ?v ?next)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-road_0581-road_0550-tw_00090
    :parameters (?v - vehicle ?r - road_type_road_0581 ?next - road_type_road_0550 ?w - window_type_tw_00090)
    :precondition (and
      (ready-road ?v ?r)
      (road-next ?r ?next)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
    )
    :effect (and
      (not (ready-road ?v ?r))
      (ready-road ?v ?next)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-road_0581-road_0550-tw_00120
    :parameters (?v - vehicle ?r - road_type_road_0581 ?next - road_type_road_0550 ?w - window_type_tw_00120)
    :precondition (and
      (ready-road ?v ?r)
      (road-next ?r ?next)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
    )
    :effect (and
      (not (ready-road ?v ?r))
      (ready-road ?v ?next)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-road_0581-road_0550-tw_00150
    :parameters (?v - vehicle ?r - road_type_road_0581 ?next - road_type_road_0550 ?w - window_type_tw_00150)
    :precondition (and
      (ready-road ?v ?r)
      (road-next ?r ?next)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
    )
    :effect (and
      (not (ready-road ?v ?r))
      (ready-road ?v ?next)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-road_0581-road_0550-tw_00180
    :parameters (?v - vehicle ?r - road_type_road_0581 ?next - road_type_road_0550 ?w - window_type_tw_00180)
    :precondition (and
      (ready-road ?v ?r)
      (road-next ?r ?next)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
    )
    :effect (and
      (not (ready-road ?v ?r))
      (ready-road ?v ?next)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-road_0581-road_0550-tw_00210
    :parameters (?v - vehicle ?r - road_type_road_0581 ?next - road_type_road_0550 ?w - window_type_tw_00210)
    :precondition (and
      (ready-road ?v ?r)
      (road-next ?r ?next)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
    )
    :effect (and
      (not (ready-road ?v ?r))
      (ready-road ?v ?next)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-road_0581-road_0550-tw_00240
    :parameters (?v - vehicle ?r - road_type_road_0581 ?next - road_type_road_0550 ?w - window_type_tw_00240)
    :precondition (and
      (ready-road ?v ?r)
      (road-next ?r ?next)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
    )
    :effect (and
      (not (ready-road ?v ?r))
      (ready-road ?v ?next)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-road_0581-road_0550-tw_00270
    :parameters (?v - vehicle ?r - road_type_road_0581 ?next - road_type_road_0550 ?w - window_type_tw_00270)
    :precondition (and
      (ready-road ?v ?r)
      (road-next ?r ?next)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
    )
    :effect (and
      (not (ready-road ?v ?r))
      (ready-road ?v ?next)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-road_0581-road_0550-tw_00300
    :parameters (?v - vehicle ?r - road_type_road_0581 ?next - road_type_road_0550 ?w - window_type_tw_00300)
    :precondition (and
      (ready-road ?v ?r)
      (road-next ?r ?next)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
    )
    :effect (and
      (not (ready-road ?v ?r))
      (ready-road ?v ?next)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-road_0581-road_0550-tw_00330
    :parameters (?v - vehicle ?r - road_type_road_0581 ?next - road_type_road_0550 ?w - window_type_tw_00330)
    :precondition (and
      (ready-road ?v ?r)
      (road-next ?r ?next)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
    )
    :effect (and
      (not (ready-road ?v ?r))
      (ready-road ?v ?next)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-road_0581-road_0550-tw_00360
    :parameters (?v - vehicle ?r - road_type_road_0581 ?next - road_type_road_0550 ?w - window_type_tw_00360)
    :precondition (and
      (ready-road ?v ?r)
      (road-next ?r ?next)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
    )
    :effect (and
      (not (ready-road ?v ?r))
      (ready-road ?v ?next)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-road_0581-road_0550-tw_00390
    :parameters (?v - vehicle ?r - road_type_road_0581 ?next - road_type_road_0550 ?w - window_type_tw_00390)
    :precondition (and
      (ready-road ?v ?r)
      (road-next ?r ?next)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
    )
    :effect (and
      (not (ready-road ?v ?r))
      (ready-road ?v ?next)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-road_0581-road_0550-tw_00420
    :parameters (?v - vehicle ?r - road_type_road_0581 ?next - road_type_road_0550 ?w - window_type_tw_00420)
    :precondition (and
      (ready-road ?v ?r)
      (road-next ?r ?next)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
    )
    :effect (and
      (not (ready-road ?v ?r))
      (ready-road ?v ?next)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-road_0581-road_0550-tw_00450
    :parameters (?v - vehicle ?r - road_type_road_0581 ?next - road_type_road_0550 ?w - window_type_tw_00450)
    :precondition (and
      (ready-road ?v ?r)
      (road-next ?r ?next)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
    )
    :effect (and
      (not (ready-road ?v ?r))
      (ready-road ?v ?next)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-road_0581-road_0550-tw_00480
    :parameters (?v - vehicle ?r - road_type_road_0581 ?next - road_type_road_0550 ?w - window_type_tw_00480)
    :precondition (and
      (ready-road ?v ?r)
      (road-next ?r ?next)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
    )
    :effect (and
      (not (ready-road ?v ?r))
      (ready-road ?v ?next)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-road_0581-road_0550-tw_00510
    :parameters (?v - vehicle ?r - road_type_road_0581 ?next - road_type_road_0550 ?w - window_type_tw_00510)
    :precondition (and
      (ready-road ?v ?r)
      (road-next ?r ?next)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
    )
    :effect (and
      (not (ready-road ?v ?r))
      (ready-road ?v ?next)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-road_0581-road_0550-tw_00540
    :parameters (?v - vehicle ?r - road_type_road_0581 ?next - road_type_road_0550 ?w - window_type_tw_00540)
    :precondition (and
      (ready-road ?v ?r)
      (road-next ?r ?next)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
    )
    :effect (and
      (not (ready-road ?v ?r))
      (ready-road ?v ?next)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-road_0581-road_0550-tw_00570
    :parameters (?v - vehicle ?r - road_type_road_0581 ?next - road_type_road_0550 ?w - window_type_tw_00570)
    :precondition (and
      (ready-road ?v ?r)
      (road-next ?r ?next)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
    )
    :effect (and
      (not (ready-road ?v ?r))
      (ready-road ?v ?next)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-road_0581-road_0550-tw_00600
    :parameters (?v - vehicle ?r - road_type_road_0581 ?next - road_type_road_0550 ?w - window_type_tw_00600)
    :precondition (and
      (ready-road ?v ?r)
      (road-next ?r ?next)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
    )
    :effect (and
      (not (ready-road ?v ?r))
      (ready-road ?v ?next)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-road_0581-road_0551-tw_00000
    :parameters (?v - vehicle ?r - road_type_road_0581 ?next - road_type_road_0551 ?w - window_type_tw_00000)
    :precondition (and
      (ready-road ?v ?r)
      (road-next ?r ?next)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
    )
    :effect (and
      (not (ready-road ?v ?r))
      (ready-road ?v ?next)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-road_0581-road_0551-tw_00030
    :parameters (?v - vehicle ?r - road_type_road_0581 ?next - road_type_road_0551 ?w - window_type_tw_00030)
    :precondition (and
      (ready-road ?v ?r)
      (road-next ?r ?next)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
    )
    :effect (and
      (not (ready-road ?v ?r))
      (ready-road ?v ?next)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-road_0581-road_0551-tw_00060
    :parameters (?v - vehicle ?r - road_type_road_0581 ?next - road_type_road_0551 ?w - window_type_tw_00060)
    :precondition (and
      (ready-road ?v ?r)
      (road-next ?r ?next)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
    )
    :effect (and
      (not (ready-road ?v ?r))
      (ready-road ?v ?next)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-road_0581-road_0551-tw_00090
    :parameters (?v - vehicle ?r - road_type_road_0581 ?next - road_type_road_0551 ?w - window_type_tw_00090)
    :precondition (and
      (ready-road ?v ?r)
      (road-next ?r ?next)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
    )
    :effect (and
      (not (ready-road ?v ?r))
      (ready-road ?v ?next)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-road_0581-road_0551-tw_00120
    :parameters (?v - vehicle ?r - road_type_road_0581 ?next - road_type_road_0551 ?w - window_type_tw_00120)
    :precondition (and
      (ready-road ?v ?r)
      (road-next ?r ?next)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
    )
    :effect (and
      (not (ready-road ?v ?r))
      (ready-road ?v ?next)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-road_0581-road_0551-tw_00150
    :parameters (?v - vehicle ?r - road_type_road_0581 ?next - road_type_road_0551 ?w - window_type_tw_00150)
    :precondition (and
      (ready-road ?v ?r)
      (road-next ?r ?next)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
    )
    :effect (and
      (not (ready-road ?v ?r))
      (ready-road ?v ?next)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-road_0581-road_0551-tw_00180
    :parameters (?v - vehicle ?r - road_type_road_0581 ?next - road_type_road_0551 ?w - window_type_tw_00180)
    :precondition (and
      (ready-road ?v ?r)
      (road-next ?r ?next)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
    )
    :effect (and
      (not (ready-road ?v ?r))
      (ready-road ?v ?next)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-road_0581-road_0551-tw_00210
    :parameters (?v - vehicle ?r - road_type_road_0581 ?next - road_type_road_0551 ?w - window_type_tw_00210)
    :precondition (and
      (ready-road ?v ?r)
      (road-next ?r ?next)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
    )
    :effect (and
      (not (ready-road ?v ?r))
      (ready-road ?v ?next)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-road_0581-road_0551-tw_00240
    :parameters (?v - vehicle ?r - road_type_road_0581 ?next - road_type_road_0551 ?w - window_type_tw_00240)
    :precondition (and
      (ready-road ?v ?r)
      (road-next ?r ?next)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
    )
    :effect (and
      (not (ready-road ?v ?r))
      (ready-road ?v ?next)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-road_0581-road_0551-tw_00270
    :parameters (?v - vehicle ?r - road_type_road_0581 ?next - road_type_road_0551 ?w - window_type_tw_00270)
    :precondition (and
      (ready-road ?v ?r)
      (road-next ?r ?next)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
    )
    :effect (and
      (not (ready-road ?v ?r))
      (ready-road ?v ?next)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-road_0581-road_0551-tw_00300
    :parameters (?v - vehicle ?r - road_type_road_0581 ?next - road_type_road_0551 ?w - window_type_tw_00300)
    :precondition (and
      (ready-road ?v ?r)
      (road-next ?r ?next)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
    )
    :effect (and
      (not (ready-road ?v ?r))
      (ready-road ?v ?next)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-road_0581-road_0551-tw_00330
    :parameters (?v - vehicle ?r - road_type_road_0581 ?next - road_type_road_0551 ?w - window_type_tw_00330)
    :precondition (and
      (ready-road ?v ?r)
      (road-next ?r ?next)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
    )
    :effect (and
      (not (ready-road ?v ?r))
      (ready-road ?v ?next)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-road_0581-road_0551-tw_00360
    :parameters (?v - vehicle ?r - road_type_road_0581 ?next - road_type_road_0551 ?w - window_type_tw_00360)
    :precondition (and
      (ready-road ?v ?r)
      (road-next ?r ?next)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
    )
    :effect (and
      (not (ready-road ?v ?r))
      (ready-road ?v ?next)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-road_0581-road_0551-tw_00390
    :parameters (?v - vehicle ?r - road_type_road_0581 ?next - road_type_road_0551 ?w - window_type_tw_00390)
    :precondition (and
      (ready-road ?v ?r)
      (road-next ?r ?next)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
    )
    :effect (and
      (not (ready-road ?v ?r))
      (ready-road ?v ?next)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-road_0581-road_0551-tw_00420
    :parameters (?v - vehicle ?r - road_type_road_0581 ?next - road_type_road_0551 ?w - window_type_tw_00420)
    :precondition (and
      (ready-road ?v ?r)
      (road-next ?r ?next)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
    )
    :effect (and
      (not (ready-road ?v ?r))
      (ready-road ?v ?next)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-road_0581-road_0551-tw_00450
    :parameters (?v - vehicle ?r - road_type_road_0581 ?next - road_type_road_0551 ?w - window_type_tw_00450)
    :precondition (and
      (ready-road ?v ?r)
      (road-next ?r ?next)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
    )
    :effect (and
      (not (ready-road ?v ?r))
      (ready-road ?v ?next)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-road_0581-road_0551-tw_00480
    :parameters (?v - vehicle ?r - road_type_road_0581 ?next - road_type_road_0551 ?w - window_type_tw_00480)
    :precondition (and
      (ready-road ?v ?r)
      (road-next ?r ?next)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
    )
    :effect (and
      (not (ready-road ?v ?r))
      (ready-road ?v ?next)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-road_0581-road_0551-tw_00510
    :parameters (?v - vehicle ?r - road_type_road_0581 ?next - road_type_road_0551 ?w - window_type_tw_00510)
    :precondition (and
      (ready-road ?v ?r)
      (road-next ?r ?next)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
    )
    :effect (and
      (not (ready-road ?v ?r))
      (ready-road ?v ?next)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-road_0581-road_0551-tw_00540
    :parameters (?v - vehicle ?r - road_type_road_0581 ?next - road_type_road_0551 ?w - window_type_tw_00540)
    :precondition (and
      (ready-road ?v ?r)
      (road-next ?r ?next)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
    )
    :effect (and
      (not (ready-road ?v ?r))
      (ready-road ?v ?next)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-road_0581-road_0551-tw_00570
    :parameters (?v - vehicle ?r - road_type_road_0581 ?next - road_type_road_0551 ?w - window_type_tw_00570)
    :precondition (and
      (ready-road ?v ?r)
      (road-next ?r ?next)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
    )
    :effect (and
      (not (ready-road ?v ?r))
      (ready-road ?v ?next)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-road_0581-road_0551-tw_00600
    :parameters (?v - vehicle ?r - road_type_road_0581 ?next - road_type_road_0551 ?w - window_type_tw_00600)
    :precondition (and
      (ready-road ?v ?r)
      (road-next ?r ?next)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
    )
    :effect (and
      (not (ready-road ?v ?r))
      (ready-road ?v ?next)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-road_0581-road_0552-tw_00000
    :parameters (?v - vehicle ?r - road_type_road_0581 ?next - road_type_road_0552 ?w - window_type_tw_00000)
    :precondition (and
      (ready-road ?v ?r)
      (road-next ?r ?next)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
    )
    :effect (and
      (not (ready-road ?v ?r))
      (ready-road ?v ?next)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-road_0581-road_0552-tw_00030
    :parameters (?v - vehicle ?r - road_type_road_0581 ?next - road_type_road_0552 ?w - window_type_tw_00030)
    :precondition (and
      (ready-road ?v ?r)
      (road-next ?r ?next)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
    )
    :effect (and
      (not (ready-road ?v ?r))
      (ready-road ?v ?next)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-road_0581-road_0552-tw_00060
    :parameters (?v - vehicle ?r - road_type_road_0581 ?next - road_type_road_0552 ?w - window_type_tw_00060)
    :precondition (and
      (ready-road ?v ?r)
      (road-next ?r ?next)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
    )
    :effect (and
      (not (ready-road ?v ?r))
      (ready-road ?v ?next)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-road_0581-road_0552-tw_00090
    :parameters (?v - vehicle ?r - road_type_road_0581 ?next - road_type_road_0552 ?w - window_type_tw_00090)
    :precondition (and
      (ready-road ?v ?r)
      (road-next ?r ?next)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
    )
    :effect (and
      (not (ready-road ?v ?r))
      (ready-road ?v ?next)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-road_0581-road_0552-tw_00120
    :parameters (?v - vehicle ?r - road_type_road_0581 ?next - road_type_road_0552 ?w - window_type_tw_00120)
    :precondition (and
      (ready-road ?v ?r)
      (road-next ?r ?next)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
    )
    :effect (and
      (not (ready-road ?v ?r))
      (ready-road ?v ?next)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-road_0581-road_0552-tw_00150
    :parameters (?v - vehicle ?r - road_type_road_0581 ?next - road_type_road_0552 ?w - window_type_tw_00150)
    :precondition (and
      (ready-road ?v ?r)
      (road-next ?r ?next)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
    )
    :effect (and
      (not (ready-road ?v ?r))
      (ready-road ?v ?next)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-road_0581-road_0552-tw_00180
    :parameters (?v - vehicle ?r - road_type_road_0581 ?next - road_type_road_0552 ?w - window_type_tw_00180)
    :precondition (and
      (ready-road ?v ?r)
      (road-next ?r ?next)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
    )
    :effect (and
      (not (ready-road ?v ?r))
      (ready-road ?v ?next)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-road_0581-road_0552-tw_00210
    :parameters (?v - vehicle ?r - road_type_road_0581 ?next - road_type_road_0552 ?w - window_type_tw_00210)
    :precondition (and
      (ready-road ?v ?r)
      (road-next ?r ?next)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
    )
    :effect (and
      (not (ready-road ?v ?r))
      (ready-road ?v ?next)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-road_0581-road_0552-tw_00240
    :parameters (?v - vehicle ?r - road_type_road_0581 ?next - road_type_road_0552 ?w - window_type_tw_00240)
    :precondition (and
      (ready-road ?v ?r)
      (road-next ?r ?next)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
    )
    :effect (and
      (not (ready-road ?v ?r))
      (ready-road ?v ?next)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-road_0581-road_0552-tw_00270
    :parameters (?v - vehicle ?r - road_type_road_0581 ?next - road_type_road_0552 ?w - window_type_tw_00270)
    :precondition (and
      (ready-road ?v ?r)
      (road-next ?r ?next)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
    )
    :effect (and
      (not (ready-road ?v ?r))
      (ready-road ?v ?next)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-road_0581-road_0552-tw_00300
    :parameters (?v - vehicle ?r - road_type_road_0581 ?next - road_type_road_0552 ?w - window_type_tw_00300)
    :precondition (and
      (ready-road ?v ?r)
      (road-next ?r ?next)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
    )
    :effect (and
      (not (ready-road ?v ?r))
      (ready-road ?v ?next)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-road_0581-road_0552-tw_00330
    :parameters (?v - vehicle ?r - road_type_road_0581 ?next - road_type_road_0552 ?w - window_type_tw_00330)
    :precondition (and
      (ready-road ?v ?r)
      (road-next ?r ?next)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
    )
    :effect (and
      (not (ready-road ?v ?r))
      (ready-road ?v ?next)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-road_0581-road_0552-tw_00360
    :parameters (?v - vehicle ?r - road_type_road_0581 ?next - road_type_road_0552 ?w - window_type_tw_00360)
    :precondition (and
      (ready-road ?v ?r)
      (road-next ?r ?next)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
    )
    :effect (and
      (not (ready-road ?v ?r))
      (ready-road ?v ?next)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-road_0581-road_0552-tw_00390
    :parameters (?v - vehicle ?r - road_type_road_0581 ?next - road_type_road_0552 ?w - window_type_tw_00390)
    :precondition (and
      (ready-road ?v ?r)
      (road-next ?r ?next)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
    )
    :effect (and
      (not (ready-road ?v ?r))
      (ready-road ?v ?next)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-road_0581-road_0552-tw_00420
    :parameters (?v - vehicle ?r - road_type_road_0581 ?next - road_type_road_0552 ?w - window_type_tw_00420)
    :precondition (and
      (ready-road ?v ?r)
      (road-next ?r ?next)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
    )
    :effect (and
      (not (ready-road ?v ?r))
      (ready-road ?v ?next)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-road_0581-road_0552-tw_00450
    :parameters (?v - vehicle ?r - road_type_road_0581 ?next - road_type_road_0552 ?w - window_type_tw_00450)
    :precondition (and
      (ready-road ?v ?r)
      (road-next ?r ?next)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
    )
    :effect (and
      (not (ready-road ?v ?r))
      (ready-road ?v ?next)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-road_0581-road_0552-tw_00480
    :parameters (?v - vehicle ?r - road_type_road_0581 ?next - road_type_road_0552 ?w - window_type_tw_00480)
    :precondition (and
      (ready-road ?v ?r)
      (road-next ?r ?next)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
    )
    :effect (and
      (not (ready-road ?v ?r))
      (ready-road ?v ?next)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-road_0581-road_0552-tw_00510
    :parameters (?v - vehicle ?r - road_type_road_0581 ?next - road_type_road_0552 ?w - window_type_tw_00510)
    :precondition (and
      (ready-road ?v ?r)
      (road-next ?r ?next)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
    )
    :effect (and
      (not (ready-road ?v ?r))
      (ready-road ?v ?next)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-road_0581-road_0552-tw_00540
    :parameters (?v - vehicle ?r - road_type_road_0581 ?next - road_type_road_0552 ?w - window_type_tw_00540)
    :precondition (and
      (ready-road ?v ?r)
      (road-next ?r ?next)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
    )
    :effect (and
      (not (ready-road ?v ?r))
      (ready-road ?v ?next)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-road_0581-road_0552-tw_00570
    :parameters (?v - vehicle ?r - road_type_road_0581 ?next - road_type_road_0552 ?w - window_type_tw_00570)
    :precondition (and
      (ready-road ?v ?r)
      (road-next ?r ?next)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
    )
    :effect (and
      (not (ready-road ?v ?r))
      (ready-road ?v ?next)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-road_0581-road_0552-tw_00600
    :parameters (?v - vehicle ?r - road_type_road_0581 ?next - road_type_road_0552 ?w - window_type_tw_00600)
    :precondition (and
      (ready-road ?v ?r)
      (road-next ?r ?next)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
    )
    :effect (and
      (not (ready-road ?v ?r))
      (ready-road ?v ?next)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-static-road_0582-road_0718
    :parameters (?v - vehicle ?r - road_type_road_0582 ?next - road_type_road_0718)
    :precondition (and
      (ready-road ?v ?r)
      (road-next ?r ?next)
      (road-open ?r)
      (static-road ?r)
      (not (road-blocked ?r))
    )
    :effect (and
      (not (ready-road ?v ?r))
      (ready-road ?v ?next)
      (increase (travel-time ?v) (travel-duration ?r))
      (increase (sim-time) (travel-duration ?r))
    )
  )

  (:action traverse-road-static-road_0582-road_0719
    :parameters (?v - vehicle ?r - road_type_road_0582 ?next - road_type_road_0719)
    :precondition (and
      (ready-road ?v ?r)
      (road-next ?r ?next)
      (road-open ?r)
      (static-road ?r)
      (not (road-blocked ?r))
    )
    :effect (and
      (not (ready-road ?v ?r))
      (ready-road ?v ?next)
      (increase (travel-time ?v) (travel-duration ?r))
      (increase (sim-time) (travel-duration ?r))
    )
  )

  (:action traverse-road-static-road_0583-road_0860
    :parameters (?v - vehicle ?r - road_type_road_0583 ?next - road_type_road_0860)
    :precondition (and
      (ready-road ?v ?r)
      (road-next ?r ?next)
      (road-open ?r)
      (static-road ?r)
      (not (road-blocked ?r))
    )
    :effect (and
      (not (ready-road ?v ?r))
      (ready-road ?v ?next)
      (increase (travel-time ?v) (travel-duration ?r))
      (increase (sim-time) (travel-duration ?r))
    )
  )

  (:action traverse-road-static-road_0583-road_0861
    :parameters (?v - vehicle ?r - road_type_road_0583 ?next - road_type_road_0861)
    :precondition (and
      (ready-road ?v ?r)
      (road-next ?r ?next)
      (road-open ?r)
      (static-road ?r)
      (not (road-blocked ?r))
    )
    :effect (and
      (not (ready-road ?v ?r))
      (ready-road ?v ?next)
      (increase (travel-time ?v) (travel-duration ?r))
      (increase (sim-time) (travel-duration ?r))
    )
  )

  (:action traverse-road-static-road_0584-road_0779
    :parameters (?v - vehicle ?r - road_type_road_0584 ?next - road_type_road_0779)
    :precondition (and
      (ready-road ?v ?r)
      (road-next ?r ?next)
      (road-open ?r)
      (static-road ?r)
      (not (road-blocked ?r))
    )
    :effect (and
      (not (ready-road ?v ?r))
      (ready-road ?v ?next)
      (increase (travel-time ?v) (travel-duration ?r))
      (increase (sim-time) (travel-duration ?r))
    )
  )

  (:action traverse-road-static-road_0584-road_0780
    :parameters (?v - vehicle ?r - road_type_road_0584 ?next - road_type_road_0780)
    :precondition (and
      (ready-road ?v ?r)
      (road-next ?r ?next)
      (road-open ?r)
      (static-road ?r)
      (not (road-blocked ?r))
    )
    :effect (and
      (not (ready-road ?v ?r))
      (ready-road ?v ?next)
      (increase (travel-time ?v) (travel-duration ?r))
      (increase (sim-time) (travel-duration ?r))
    )
  )

  (:action traverse-road-static-road_0586-road_0579
    :parameters (?v - vehicle ?r - road_type_road_0586 ?next - road_type_road_0579)
    :precondition (and
      (ready-road ?v ?r)
      (road-next ?r ?next)
      (road-open ?r)
      (static-road ?r)
      (not (road-blocked ?r))
    )
    :effect (and
      (not (ready-road ?v ?r))
      (ready-road ?v ?next)
      (increase (travel-time ?v) (travel-duration ?r))
      (increase (sim-time) (travel-duration ?r))
    )
  )

  (:action traverse-road-static-road_0586-road_0580
    :parameters (?v - vehicle ?r - road_type_road_0586 ?next - road_type_road_0580)
    :precondition (and
      (ready-road ?v ?r)
      (road-next ?r ?next)
      (road-open ?r)
      (static-road ?r)
      (not (road-blocked ?r))
    )
    :effect (and
      (not (ready-road ?v ?r))
      (ready-road ?v ?next)
      (increase (travel-time ?v) (travel-duration ?r))
      (increase (sim-time) (travel-duration ?r))
    )
  )

  (:action traverse-road-static-road_0587-road_0054
    :parameters (?v - vehicle ?r - road_type_road_0587 ?next - road_type_road_0054)
    :precondition (and
      (ready-road ?v ?r)
      (road-next ?r ?next)
      (road-open ?r)
      (static-road ?r)
      (not (road-blocked ?r))
    )
    :effect (and
      (not (ready-road ?v ?r))
      (ready-road ?v ?next)
      (increase (travel-time ?v) (travel-duration ?r))
      (increase (sim-time) (travel-duration ?r))
    )
  )

  (:action traverse-road-static-road_0588-road_0709
    :parameters (?v - vehicle ?r - road_type_road_0588 ?next - road_type_road_0709)
    :precondition (and
      (ready-road ?v ?r)
      (road-next ?r ?next)
      (road-open ?r)
      (static-road ?r)
      (not (road-blocked ?r))
    )
    :effect (and
      (not (ready-road ?v ?r))
      (ready-road ?v ?next)
      (increase (travel-time ?v) (travel-duration ?r))
      (increase (sim-time) (travel-duration ?r))
    )
  )

  (:action traverse-road-static-road_0589-road_0247
    :parameters (?v - vehicle ?r - road_type_road_0589 ?next - road_type_road_0247)
    :precondition (and
      (ready-road ?v ?r)
      (road-next ?r ?next)
      (road-open ?r)
      (static-road ?r)
      (not (road-blocked ?r))
    )
    :effect (and
      (not (ready-road ?v ?r))
      (ready-road ?v ?next)
      (increase (travel-time ?v) (travel-duration ?r))
      (increase (sim-time) (travel-duration ?r))
    )
  )

  (:action traverse-road-static-road_0589-road_0248
    :parameters (?v - vehicle ?r - road_type_road_0589 ?next - road_type_road_0248)
    :precondition (and
      (ready-road ?v ?r)
      (road-next ?r ?next)
      (road-open ?r)
      (static-road ?r)
      (not (road-blocked ?r))
    )
    :effect (and
      (not (ready-road ?v ?r))
      (ready-road ?v ?next)
      (increase (travel-time ?v) (travel-duration ?r))
      (increase (sim-time) (travel-duration ?r))
    )
  )

  (:action traverse-road-static-road_0590-road_0020
    :parameters (?v - vehicle ?r - road_type_road_0590 ?next - road_type_road_0020)
    :precondition (and
      (ready-road ?v ?r)
      (road-next ?r ?next)
      (road-open ?r)
      (static-road ?r)
      (not (road-blocked ?r))
    )
    :effect (and
      (not (ready-road ?v ?r))
      (ready-road ?v ?next)
      (increase (travel-time ?v) (travel-duration ?r))
      (increase (sim-time) (travel-duration ?r))
    )
  )

  (:action traverse-road-static-road_0590-road_0021
    :parameters (?v - vehicle ?r - road_type_road_0590 ?next - road_type_road_0021)
    :precondition (and
      (ready-road ?v ?r)
      (road-next ?r ?next)
      (road-open ?r)
      (static-road ?r)
      (not (road-blocked ?r))
    )
    :effect (and
      (not (ready-road ?v ?r))
      (ready-road ?v ?next)
      (increase (travel-time ?v) (travel-duration ?r))
      (increase (sim-time) (travel-duration ?r))
    )
  )

  (:action traverse-road-dynamic-road_0591-road_0590-tw_00000
    :parameters (?v - vehicle ?r - road_type_road_0591 ?next - road_type_road_0590 ?w - window_type_tw_00000)
    :precondition (and
      (ready-road ?v ?r)
      (road-next ?r ?next)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
    )
    :effect (and
      (not (ready-road ?v ?r))
      (ready-road ?v ?next)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-road_0591-road_0590-tw_00030
    :parameters (?v - vehicle ?r - road_type_road_0591 ?next - road_type_road_0590 ?w - window_type_tw_00030)
    :precondition (and
      (ready-road ?v ?r)
      (road-next ?r ?next)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
    )
    :effect (and
      (not (ready-road ?v ?r))
      (ready-road ?v ?next)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-road_0591-road_0590-tw_00060
    :parameters (?v - vehicle ?r - road_type_road_0591 ?next - road_type_road_0590 ?w - window_type_tw_00060)
    :precondition (and
      (ready-road ?v ?r)
      (road-next ?r ?next)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
    )
    :effect (and
      (not (ready-road ?v ?r))
      (ready-road ?v ?next)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-road_0591-road_0590-tw_00090
    :parameters (?v - vehicle ?r - road_type_road_0591 ?next - road_type_road_0590 ?w - window_type_tw_00090)
    :precondition (and
      (ready-road ?v ?r)
      (road-next ?r ?next)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
    )
    :effect (and
      (not (ready-road ?v ?r))
      (ready-road ?v ?next)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-road_0591-road_0590-tw_00120
    :parameters (?v - vehicle ?r - road_type_road_0591 ?next - road_type_road_0590 ?w - window_type_tw_00120)
    :precondition (and
      (ready-road ?v ?r)
      (road-next ?r ?next)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
    )
    :effect (and
      (not (ready-road ?v ?r))
      (ready-road ?v ?next)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-road_0591-road_0590-tw_00150
    :parameters (?v - vehicle ?r - road_type_road_0591 ?next - road_type_road_0590 ?w - window_type_tw_00150)
    :precondition (and
      (ready-road ?v ?r)
      (road-next ?r ?next)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
    )
    :effect (and
      (not (ready-road ?v ?r))
      (ready-road ?v ?next)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-road_0591-road_0590-tw_00180
    :parameters (?v - vehicle ?r - road_type_road_0591 ?next - road_type_road_0590 ?w - window_type_tw_00180)
    :precondition (and
      (ready-road ?v ?r)
      (road-next ?r ?next)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
    )
    :effect (and
      (not (ready-road ?v ?r))
      (ready-road ?v ?next)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-road_0591-road_0590-tw_00210
    :parameters (?v - vehicle ?r - road_type_road_0591 ?next - road_type_road_0590 ?w - window_type_tw_00210)
    :precondition (and
      (ready-road ?v ?r)
      (road-next ?r ?next)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
    )
    :effect (and
      (not (ready-road ?v ?r))
      (ready-road ?v ?next)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-road_0591-road_0590-tw_00240
    :parameters (?v - vehicle ?r - road_type_road_0591 ?next - road_type_road_0590 ?w - window_type_tw_00240)
    :precondition (and
      (ready-road ?v ?r)
      (road-next ?r ?next)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
    )
    :effect (and
      (not (ready-road ?v ?r))
      (ready-road ?v ?next)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-road_0591-road_0590-tw_00270
    :parameters (?v - vehicle ?r - road_type_road_0591 ?next - road_type_road_0590 ?w - window_type_tw_00270)
    :precondition (and
      (ready-road ?v ?r)
      (road-next ?r ?next)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
    )
    :effect (and
      (not (ready-road ?v ?r))
      (ready-road ?v ?next)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-road_0591-road_0590-tw_00300
    :parameters (?v - vehicle ?r - road_type_road_0591 ?next - road_type_road_0590 ?w - window_type_tw_00300)
    :precondition (and
      (ready-road ?v ?r)
      (road-next ?r ?next)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
    )
    :effect (and
      (not (ready-road ?v ?r))
      (ready-road ?v ?next)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-road_0591-road_0590-tw_00330
    :parameters (?v - vehicle ?r - road_type_road_0591 ?next - road_type_road_0590 ?w - window_type_tw_00330)
    :precondition (and
      (ready-road ?v ?r)
      (road-next ?r ?next)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
    )
    :effect (and
      (not (ready-road ?v ?r))
      (ready-road ?v ?next)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-road_0591-road_0590-tw_00360
    :parameters (?v - vehicle ?r - road_type_road_0591 ?next - road_type_road_0590 ?w - window_type_tw_00360)
    :precondition (and
      (ready-road ?v ?r)
      (road-next ?r ?next)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
    )
    :effect (and
      (not (ready-road ?v ?r))
      (ready-road ?v ?next)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-road_0591-road_0590-tw_00390
    :parameters (?v - vehicle ?r - road_type_road_0591 ?next - road_type_road_0590 ?w - window_type_tw_00390)
    :precondition (and
      (ready-road ?v ?r)
      (road-next ?r ?next)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
    )
    :effect (and
      (not (ready-road ?v ?r))
      (ready-road ?v ?next)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-road_0591-road_0590-tw_00420
    :parameters (?v - vehicle ?r - road_type_road_0591 ?next - road_type_road_0590 ?w - window_type_tw_00420)
    :precondition (and
      (ready-road ?v ?r)
      (road-next ?r ?next)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
    )
    :effect (and
      (not (ready-road ?v ?r))
      (ready-road ?v ?next)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-road_0591-road_0590-tw_00450
    :parameters (?v - vehicle ?r - road_type_road_0591 ?next - road_type_road_0590 ?w - window_type_tw_00450)
    :precondition (and
      (ready-road ?v ?r)
      (road-next ?r ?next)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
    )
    :effect (and
      (not (ready-road ?v ?r))
      (ready-road ?v ?next)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-road_0591-road_0590-tw_00480
    :parameters (?v - vehicle ?r - road_type_road_0591 ?next - road_type_road_0590 ?w - window_type_tw_00480)
    :precondition (and
      (ready-road ?v ?r)
      (road-next ?r ?next)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
    )
    :effect (and
      (not (ready-road ?v ?r))
      (ready-road ?v ?next)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-road_0591-road_0590-tw_00510
    :parameters (?v - vehicle ?r - road_type_road_0591 ?next - road_type_road_0590 ?w - window_type_tw_00510)
    :precondition (and
      (ready-road ?v ?r)
      (road-next ?r ?next)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
    )
    :effect (and
      (not (ready-road ?v ?r))
      (ready-road ?v ?next)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-road_0591-road_0590-tw_00540
    :parameters (?v - vehicle ?r - road_type_road_0591 ?next - road_type_road_0590 ?w - window_type_tw_00540)
    :precondition (and
      (ready-road ?v ?r)
      (road-next ?r ?next)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
    )
    :effect (and
      (not (ready-road ?v ?r))
      (ready-road ?v ?next)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-road_0591-road_0590-tw_00570
    :parameters (?v - vehicle ?r - road_type_road_0591 ?next - road_type_road_0590 ?w - window_type_tw_00570)
    :precondition (and
      (ready-road ?v ?r)
      (road-next ?r ?next)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
    )
    :effect (and
      (not (ready-road ?v ?r))
      (ready-road ?v ?next)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-road_0591-road_0590-tw_00600
    :parameters (?v - vehicle ?r - road_type_road_0591 ?next - road_type_road_0590 ?w - window_type_tw_00600)
    :precondition (and
      (ready-road ?v ?r)
      (road-next ?r ?next)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
    )
    :effect (and
      (not (ready-road ?v ?r))
      (ready-road ?v ?next)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-static-road_0592-road_0490
    :parameters (?v - vehicle ?r - road_type_road_0592 ?next - road_type_road_0490)
    :precondition (and
      (ready-road ?v ?r)
      (road-next ?r ?next)
      (road-open ?r)
      (static-road ?r)
      (not (road-blocked ?r))
    )
    :effect (and
      (not (ready-road ?v ?r))
      (ready-road ?v ?next)
      (increase (travel-time ?v) (travel-duration ?r))
      (increase (sim-time) (travel-duration ?r))
    )
  )

  (:action traverse-road-static-road_0592-road_0491
    :parameters (?v - vehicle ?r - road_type_road_0592 ?next - road_type_road_0491)
    :precondition (and
      (ready-road ?v ?r)
      (road-next ?r ?next)
      (road-open ?r)
      (static-road ?r)
      (not (road-blocked ?r))
    )
    :effect (and
      (not (ready-road ?v ?r))
      (ready-road ?v ?next)
      (increase (travel-time ?v) (travel-duration ?r))
      (increase (sim-time) (travel-duration ?r))
    )
  )

  (:action traverse-road-static-road_0592-road_0492
    :parameters (?v - vehicle ?r - road_type_road_0592 ?next - road_type_road_0492)
    :precondition (and
      (ready-road ?v ?r)
      (road-next ?r ?next)
      (road-open ?r)
      (static-road ?r)
      (not (road-blocked ?r))
    )
    :effect (and
      (not (ready-road ?v ?r))
      (ready-road ?v ?next)
      (increase (travel-time ?v) (travel-duration ?r))
      (increase (sim-time) (travel-duration ?r))
    )
  )

  (:action traverse-road-static-road_0593-road_0556
    :parameters (?v - vehicle ?r - road_type_road_0593 ?next - road_type_road_0556)
    :precondition (and
      (ready-road ?v ?r)
      (road-next ?r ?next)
      (road-open ?r)
      (static-road ?r)
      (not (road-blocked ?r))
    )
    :effect (and
      (not (ready-road ?v ?r))
      (ready-road ?v ?next)
      (increase (travel-time ?v) (travel-duration ?r))
      (increase (sim-time) (travel-duration ?r))
    )
  )

  (:action traverse-road-static-road_0593-road_0557
    :parameters (?v - vehicle ?r - road_type_road_0593 ?next - road_type_road_0557)
    :precondition (and
      (ready-road ?v ?r)
      (road-next ?r ?next)
      (road-open ?r)
      (static-road ?r)
      (not (road-blocked ?r))
    )
    :effect (and
      (not (ready-road ?v ?r))
      (ready-road ?v ?next)
      (increase (travel-time ?v) (travel-duration ?r))
      (increase (sim-time) (travel-duration ?r))
    )
  )

  (:action traverse-road-static-road_0594-road_0378
    :parameters (?v - vehicle ?r - road_type_road_0594 ?next - road_type_road_0378)
    :precondition (and
      (ready-road ?v ?r)
      (road-next ?r ?next)
      (road-open ?r)
      (static-road ?r)
      (not (road-blocked ?r))
    )
    :effect (and
      (not (ready-road ?v ?r))
      (ready-road ?v ?next)
      (increase (travel-time ?v) (travel-duration ?r))
      (increase (sim-time) (travel-duration ?r))
    )
  )

  (:action traverse-road-static-road_0595-road_0803
    :parameters (?v - vehicle ?r - road_type_road_0595 ?next - road_type_road_0803)
    :precondition (and
      (ready-road ?v ?r)
      (road-next ?r ?next)
      (road-open ?r)
      (static-road ?r)
      (not (road-blocked ?r))
    )
    :effect (and
      (not (ready-road ?v ?r))
      (ready-road ?v ?next)
      (increase (travel-time ?v) (travel-duration ?r))
      (increase (sim-time) (travel-duration ?r))
    )
  )

  (:action traverse-road-static-road_0595-road_0804
    :parameters (?v - vehicle ?r - road_type_road_0595 ?next - road_type_road_0804)
    :precondition (and
      (ready-road ?v ?r)
      (road-next ?r ?next)
      (road-open ?r)
      (static-road ?r)
      (not (road-blocked ?r))
    )
    :effect (and
      (not (ready-road ?v ?r))
      (ready-road ?v ?next)
      (increase (travel-time ?v) (travel-duration ?r))
      (increase (sim-time) (travel-duration ?r))
    )
  )

  (:action traverse-road-static-road_0598-road_0181
    :parameters (?v - vehicle ?r - road_type_road_0598 ?next - road_type_road_0181)
    :precondition (and
      (ready-road ?v ?r)
      (road-next ?r ?next)
      (road-open ?r)
      (static-road ?r)
      (not (road-blocked ?r))
    )
    :effect (and
      (not (ready-road ?v ?r))
      (ready-road ?v ?next)
      (increase (travel-time ?v) (travel-duration ?r))
      (increase (sim-time) (travel-duration ?r))
    )
  )

  (:action traverse-road-static-road_0599-road_0596
    :parameters (?v - vehicle ?r - road_type_road_0599 ?next - road_type_road_0596)
    :precondition (and
      (ready-road ?v ?r)
      (road-next ?r ?next)
      (road-open ?r)
      (static-road ?r)
      (not (road-blocked ?r))
    )
    :effect (and
      (not (ready-road ?v ?r))
      (ready-road ?v ?next)
      (increase (travel-time ?v) (travel-duration ?r))
      (increase (sim-time) (travel-duration ?r))
    )
  )

  (:action traverse-road-static-road_0600-road_0336
    :parameters (?v - vehicle ?r - road_type_road_0600 ?next - road_type_road_0336)
    :precondition (and
      (ready-road ?v ?r)
      (road-next ?r ?next)
      (road-open ?r)
      (static-road ?r)
      (not (road-blocked ?r))
    )
    :effect (and
      (not (ready-road ?v ?r))
      (ready-road ?v ?next)
      (increase (travel-time ?v) (travel-duration ?r))
      (increase (sim-time) (travel-duration ?r))
    )
  )

  (:action traverse-road-static-road_0601-road_0567
    :parameters (?v - vehicle ?r - road_type_road_0601 ?next - road_type_road_0567)
    :precondition (and
      (ready-road ?v ?r)
      (road-next ?r ?next)
      (road-open ?r)
      (static-road ?r)
      (not (road-blocked ?r))
    )
    :effect (and
      (not (ready-road ?v ?r))
      (ready-road ?v ?next)
      (increase (travel-time ?v) (travel-duration ?r))
      (increase (sim-time) (travel-duration ?r))
    )
  )

  (:action traverse-road-static-road_0602-macro_0000
    :parameters (?v - vehicle ?r - road_type_road_0602 ?next - road_type_macro_0000)
    :precondition (and
      (ready-road ?v ?r)
      (road-next ?r ?next)
      (road-open ?r)
      (static-road ?r)
      (not (road-blocked ?r))
    )
    :effect (and
      (not (ready-road ?v ?r))
      (ready-road ?v ?next)
      (increase (travel-time ?v) (travel-duration ?r))
      (increase (sim-time) (travel-duration ?r))
    )
  )

  (:action traverse-road-static-road_0602-road_0003
    :parameters (?v - vehicle ?r - road_type_road_0602 ?next - road_type_road_0003)
    :precondition (and
      (ready-road ?v ?r)
      (road-next ?r ?next)
      (road-open ?r)
      (static-road ?r)
      (not (road-blocked ?r))
    )
    :effect (and
      (not (ready-road ?v ?r))
      (ready-road ?v ?next)
      (increase (travel-time ?v) (travel-duration ?r))
      (increase (sim-time) (travel-duration ?r))
    )
  )

  (:action traverse-road-static-road_0602-road_0004
    :parameters (?v - vehicle ?r - road_type_road_0602 ?next - road_type_road_0004)
    :precondition (and
      (ready-road ?v ?r)
      (road-next ?r ?next)
      (road-open ?r)
      (static-road ?r)
      (not (road-blocked ?r))
    )
    :effect (and
      (not (ready-road ?v ?r))
      (ready-road ?v ?next)
      (increase (travel-time ?v) (travel-duration ?r))
      (increase (sim-time) (travel-duration ?r))
    )
  )

  (:action traverse-road-static-road_0603-road_0793
    :parameters (?v - vehicle ?r - road_type_road_0603 ?next - road_type_road_0793)
    :precondition (and
      (ready-road ?v ?r)
      (road-next ?r ?next)
      (road-open ?r)
      (static-road ?r)
      (not (road-blocked ?r))
    )
    :effect (and
      (not (ready-road ?v ?r))
      (ready-road ?v ?next)
      (increase (travel-time ?v) (travel-duration ?r))
      (increase (sim-time) (travel-duration ?r))
    )
  )

  (:action traverse-road-static-road_0603-road_0794
    :parameters (?v - vehicle ?r - road_type_road_0603 ?next - road_type_road_0794)
    :precondition (and
      (ready-road ?v ?r)
      (road-next ?r ?next)
      (road-open ?r)
      (static-road ?r)
      (not (road-blocked ?r))
    )
    :effect (and
      (not (ready-road ?v ?r))
      (ready-road ?v ?next)
      (increase (travel-time ?v) (travel-duration ?r))
      (increase (sim-time) (travel-duration ?r))
    )
  )

  (:action traverse-road-static-road_0604-road_0600
    :parameters (?v - vehicle ?r - road_type_road_0604 ?next - road_type_road_0600)
    :precondition (and
      (ready-road ?v ?r)
      (road-next ?r ?next)
      (road-open ?r)
      (static-road ?r)
      (not (road-blocked ?r))
    )
    :effect (and
      (not (ready-road ?v ?r))
      (ready-road ?v ?next)
      (increase (travel-time ?v) (travel-duration ?r))
      (increase (sim-time) (travel-duration ?r))
    )
  )

  (:action traverse-road-static-road_0604-road_0601
    :parameters (?v - vehicle ?r - road_type_road_0604 ?next - road_type_road_0601)
    :precondition (and
      (ready-road ?v ?r)
      (road-next ?r ?next)
      (road-open ?r)
      (static-road ?r)
      (not (road-blocked ?r))
    )
    :effect (and
      (not (ready-road ?v ?r))
      (ready-road ?v ?next)
      (increase (travel-time ?v) (travel-duration ?r))
      (increase (sim-time) (travel-duration ?r))
    )
  )

  (:action traverse-road-static-road_0606-road_0596
    :parameters (?v - vehicle ?r - road_type_road_0606 ?next - road_type_road_0596)
    :precondition (and
      (ready-road ?v ?r)
      (road-next ?r ?next)
      (road-open ?r)
      (static-road ?r)
      (not (road-blocked ?r))
    )
    :effect (and
      (not (ready-road ?v ?r))
      (ready-road ?v ?next)
      (increase (travel-time ?v) (travel-duration ?r))
      (increase (sim-time) (travel-duration ?r))
    )
  )

  (:action traverse-road-static-road_0607-road_0677
    :parameters (?v - vehicle ?r - road_type_road_0607 ?next - road_type_road_0677)
    :precondition (and
      (ready-road ?v ?r)
      (road-next ?r ?next)
      (road-open ?r)
      (static-road ?r)
      (not (road-blocked ?r))
    )
    :effect (and
      (not (ready-road ?v ?r))
      (ready-road ?v ?next)
      (increase (travel-time ?v) (travel-duration ?r))
      (increase (sim-time) (travel-duration ?r))
    )
  )

  (:action traverse-road-static-road_0607-road_0678
    :parameters (?v - vehicle ?r - road_type_road_0607 ?next - road_type_road_0678)
    :precondition (and
      (ready-road ?v ?r)
      (road-next ?r ?next)
      (road-open ?r)
      (static-road ?r)
      (not (road-blocked ?r))
    )
    :effect (and
      (not (ready-road ?v ?r))
      (ready-road ?v ?next)
      (increase (travel-time ?v) (travel-duration ?r))
      (increase (sim-time) (travel-duration ?r))
    )
  )

  (:action traverse-road-static-road_0607-road_0679
    :parameters (?v - vehicle ?r - road_type_road_0607 ?next - road_type_road_0679)
    :precondition (and
      (ready-road ?v ?r)
      (road-next ?r ?next)
      (road-open ?r)
      (static-road ?r)
      (not (road-blocked ?r))
    )
    :effect (and
      (not (ready-road ?v ?r))
      (ready-road ?v ?next)
      (increase (travel-time ?v) (travel-duration ?r))
      (increase (sim-time) (travel-duration ?r))
    )
  )

  (:action traverse-road-static-road_0608-road_0297
    :parameters (?v - vehicle ?r - road_type_road_0608 ?next - road_type_road_0297)
    :precondition (and
      (ready-road ?v ?r)
      (road-next ?r ?next)
      (road-open ?r)
      (static-road ?r)
      (not (road-blocked ?r))
    )
    :effect (and
      (not (ready-road ?v ?r))
      (ready-road ?v ?next)
      (increase (travel-time ?v) (travel-duration ?r))
      (increase (sim-time) (travel-duration ?r))
    )
  )

  (:action traverse-road-static-road_0609-road_0689
    :parameters (?v - vehicle ?r - road_type_road_0609 ?next - road_type_road_0689)
    :precondition (and
      (ready-road ?v ?r)
      (road-next ?r ?next)
      (road-open ?r)
      (static-road ?r)
      (not (road-blocked ?r))
    )
    :effect (and
      (not (ready-road ?v ?r))
      (ready-road ?v ?next)
      (increase (travel-time ?v) (travel-duration ?r))
      (increase (sim-time) (travel-duration ?r))
    )
  )

  (:action traverse-road-static-road_0609-road_0690
    :parameters (?v - vehicle ?r - road_type_road_0609 ?next - road_type_road_0690)
    :precondition (and
      (ready-road ?v ?r)
      (road-next ?r ?next)
      (road-open ?r)
      (static-road ?r)
      (not (road-blocked ?r))
    )
    :effect (and
      (not (ready-road ?v ?r))
      (ready-road ?v ?next)
      (increase (travel-time ?v) (travel-duration ?r))
      (increase (sim-time) (travel-duration ?r))
    )
  )

  (:action traverse-road-static-road_0610-road_0781
    :parameters (?v - vehicle ?r - road_type_road_0610 ?next - road_type_road_0781)
    :precondition (and
      (ready-road ?v ?r)
      (road-next ?r ?next)
      (road-open ?r)
      (static-road ?r)
      (not (road-blocked ?r))
    )
    :effect (and
      (not (ready-road ?v ?r))
      (ready-road ?v ?next)
      (increase (travel-time ?v) (travel-duration ?r))
      (increase (sim-time) (travel-duration ?r))
    )
  )

  (:action traverse-road-static-road_0610-road_0782
    :parameters (?v - vehicle ?r - road_type_road_0610 ?next - road_type_road_0782)
    :precondition (and
      (ready-road ?v ?r)
      (road-next ?r ?next)
      (road-open ?r)
      (static-road ?r)
      (not (road-blocked ?r))
    )
    :effect (and
      (not (ready-road ?v ?r))
      (ready-road ?v ?next)
      (increase (travel-time ?v) (travel-duration ?r))
      (increase (sim-time) (travel-duration ?r))
    )
  )

  (:action traverse-road-static-road_0611-macro_0045
    :parameters (?v - vehicle ?r - road_type_road_0611 ?next - road_type_macro_0045)
    :precondition (and
      (ready-road ?v ?r)
      (road-next ?r ?next)
      (road-open ?r)
      (static-road ?r)
      (not (road-blocked ?r))
    )
    :effect (and
      (not (ready-road ?v ?r))
      (ready-road ?v ?next)
      (increase (travel-time ?v) (travel-duration ?r))
      (increase (sim-time) (travel-duration ?r))
    )
  )

  (:action traverse-road-static-road_0611-road_0639
    :parameters (?v - vehicle ?r - road_type_road_0611 ?next - road_type_road_0639)
    :precondition (and
      (ready-road ?v ?r)
      (road-next ?r ?next)
      (road-open ?r)
      (static-road ?r)
      (not (road-blocked ?r))
    )
    :effect (and
      (not (ready-road ?v ?r))
      (ready-road ?v ?next)
      (increase (travel-time ?v) (travel-duration ?r))
      (increase (sim-time) (travel-duration ?r))
    )
  )

  (:action traverse-road-static-road_0612-road_0361
    :parameters (?v - vehicle ?r - road_type_road_0612 ?next - road_type_road_0361)
    :precondition (and
      (ready-road ?v ?r)
      (road-next ?r ?next)
      (road-open ?r)
      (static-road ?r)
      (not (road-blocked ?r))
    )
    :effect (and
      (not (ready-road ?v ?r))
      (ready-road ?v ?next)
      (increase (travel-time ?v) (travel-duration ?r))
      (increase (sim-time) (travel-duration ?r))
    )
  )

  (:action traverse-road-static-road_0614-road_0456
    :parameters (?v - vehicle ?r - road_type_road_0614 ?next - road_type_road_0456)
    :precondition (and
      (ready-road ?v ?r)
      (road-next ?r ?next)
      (road-open ?r)
      (static-road ?r)
      (not (road-blocked ?r))
    )
    :effect (and
      (not (ready-road ?v ?r))
      (ready-road ?v ?next)
      (increase (travel-time ?v) (travel-duration ?r))
      (increase (sim-time) (travel-duration ?r))
    )
  )

  (:action traverse-road-static-road_0614-road_0457
    :parameters (?v - vehicle ?r - road_type_road_0614 ?next - road_type_road_0457)
    :precondition (and
      (ready-road ?v ?r)
      (road-next ?r ?next)
      (road-open ?r)
      (static-road ?r)
      (not (road-blocked ?r))
    )
    :effect (and
      (not (ready-road ?v ?r))
      (ready-road ?v ?next)
      (increase (travel-time ?v) (travel-duration ?r))
      (increase (sim-time) (travel-duration ?r))
    )
  )

  (:action traverse-road-static-road_0614-road_0458
    :parameters (?v - vehicle ?r - road_type_road_0614 ?next - road_type_road_0458)
    :precondition (and
      (ready-road ?v ?r)
      (road-next ?r ?next)
      (road-open ?r)
      (static-road ?r)
      (not (road-blocked ?r))
    )
    :effect (and
      (not (ready-road ?v ?r))
      (ready-road ?v ?next)
      (increase (travel-time ?v) (travel-duration ?r))
      (increase (sim-time) (travel-duration ?r))
    )
  )

  (:action traverse-road-static-road_0615-road_0079
    :parameters (?v - vehicle ?r - road_type_road_0615 ?next - road_type_road_0079)
    :precondition (and
      (ready-road ?v ?r)
      (road-next ?r ?next)
      (road-open ?r)
      (static-road ?r)
      (not (road-blocked ?r))
    )
    :effect (and
      (not (ready-road ?v ?r))
      (ready-road ?v ?next)
      (increase (travel-time ?v) (travel-duration ?r))
      (increase (sim-time) (travel-duration ?r))
    )
  )

  (:action traverse-road-static-road_0616-macro_0043
    :parameters (?v - vehicle ?r - road_type_road_0616 ?next - road_type_macro_0043)
    :precondition (and
      (ready-road ?v ?r)
      (road-next ?r ?next)
      (road-open ?r)
      (static-road ?r)
      (not (road-blocked ?r))
    )
    :effect (and
      (not (ready-road ?v ?r))
      (ready-road ?v ?next)
      (increase (travel-time ?v) (travel-duration ?r))
      (increase (sim-time) (travel-duration ?r))
    )
  )

  (:action traverse-road-static-road_0617-macro_0018
    :parameters (?v - vehicle ?r - road_type_road_0617 ?next - road_type_macro_0018)
    :precondition (and
      (ready-road ?v ?r)
      (road-next ?r ?next)
      (road-open ?r)
      (static-road ?r)
      (not (road-blocked ?r))
    )
    :effect (and
      (not (ready-road ?v ?r))
      (ready-road ?v ?next)
      (increase (travel-time ?v) (travel-duration ?r))
      (increase (sim-time) (travel-duration ?r))
    )
  )

  (:action traverse-road-static-road_0617-road_0232
    :parameters (?v - vehicle ?r - road_type_road_0617 ?next - road_type_road_0232)
    :precondition (and
      (ready-road ?v ?r)
      (road-next ?r ?next)
      (road-open ?r)
      (static-road ?r)
      (not (road-blocked ?r))
    )
    :effect (and
      (not (ready-road ?v ?r))
      (ready-road ?v ?next)
      (increase (travel-time ?v) (travel-duration ?r))
      (increase (sim-time) (travel-duration ?r))
    )
  )

  (:action traverse-road-static-road_0619-road_0418
    :parameters (?v - vehicle ?r - road_type_road_0619 ?next - road_type_road_0418)
    :precondition (and
      (ready-road ?v ?r)
      (road-next ?r ?next)
      (road-open ?r)
      (static-road ?r)
      (not (road-blocked ?r))
    )
    :effect (and
      (not (ready-road ?v ?r))
      (ready-road ?v ?next)
      (increase (travel-time ?v) (travel-duration ?r))
      (increase (sim-time) (travel-duration ?r))
    )
  )

  (:action traverse-road-static-road_0620-road_0364
    :parameters (?v - vehicle ?r - road_type_road_0620 ?next - road_type_road_0364)
    :precondition (and
      (ready-road ?v ?r)
      (road-next ?r ?next)
      (road-open ?r)
      (static-road ?r)
      (not (road-blocked ?r))
    )
    :effect (and
      (not (ready-road ?v ?r))
      (ready-road ?v ?next)
      (increase (travel-time ?v) (travel-duration ?r))
      (increase (sim-time) (travel-duration ?r))
    )
  )

  (:action traverse-road-static-road_0620-road_0365
    :parameters (?v - vehicle ?r - road_type_road_0620 ?next - road_type_road_0365)
    :precondition (and
      (ready-road ?v ?r)
      (road-next ?r ?next)
      (road-open ?r)
      (static-road ?r)
      (not (road-blocked ?r))
    )
    :effect (and
      (not (ready-road ?v ?r))
      (ready-road ?v ?next)
      (increase (travel-time ?v) (travel-duration ?r))
      (increase (sim-time) (travel-duration ?r))
    )
  )

  (:action traverse-road-dynamic-road_0621-macro_0002-tw_00000
    :parameters (?v - vehicle ?r - road_type_road_0621 ?next - road_type_macro_0002 ?w - window_type_tw_00000)
    :precondition (and
      (ready-road ?v ?r)
      (road-next ?r ?next)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
    )
    :effect (and
      (not (ready-road ?v ?r))
      (ready-road ?v ?next)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-road_0621-macro_0002-tw_00030
    :parameters (?v - vehicle ?r - road_type_road_0621 ?next - road_type_macro_0002 ?w - window_type_tw_00030)
    :precondition (and
      (ready-road ?v ?r)
      (road-next ?r ?next)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
    )
    :effect (and
      (not (ready-road ?v ?r))
      (ready-road ?v ?next)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-road_0621-macro_0002-tw_00060
    :parameters (?v - vehicle ?r - road_type_road_0621 ?next - road_type_macro_0002 ?w - window_type_tw_00060)
    :precondition (and
      (ready-road ?v ?r)
      (road-next ?r ?next)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
    )
    :effect (and
      (not (ready-road ?v ?r))
      (ready-road ?v ?next)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-road_0621-macro_0002-tw_00090
    :parameters (?v - vehicle ?r - road_type_road_0621 ?next - road_type_macro_0002 ?w - window_type_tw_00090)
    :precondition (and
      (ready-road ?v ?r)
      (road-next ?r ?next)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
    )
    :effect (and
      (not (ready-road ?v ?r))
      (ready-road ?v ?next)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-road_0621-macro_0002-tw_00120
    :parameters (?v - vehicle ?r - road_type_road_0621 ?next - road_type_macro_0002 ?w - window_type_tw_00120)
    :precondition (and
      (ready-road ?v ?r)
      (road-next ?r ?next)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
    )
    :effect (and
      (not (ready-road ?v ?r))
      (ready-road ?v ?next)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-road_0621-macro_0002-tw_00150
    :parameters (?v - vehicle ?r - road_type_road_0621 ?next - road_type_macro_0002 ?w - window_type_tw_00150)
    :precondition (and
      (ready-road ?v ?r)
      (road-next ?r ?next)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
    )
    :effect (and
      (not (ready-road ?v ?r))
      (ready-road ?v ?next)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-road_0621-macro_0002-tw_00180
    :parameters (?v - vehicle ?r - road_type_road_0621 ?next - road_type_macro_0002 ?w - window_type_tw_00180)
    :precondition (and
      (ready-road ?v ?r)
      (road-next ?r ?next)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
    )
    :effect (and
      (not (ready-road ?v ?r))
      (ready-road ?v ?next)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-road_0621-macro_0002-tw_00210
    :parameters (?v - vehicle ?r - road_type_road_0621 ?next - road_type_macro_0002 ?w - window_type_tw_00210)
    :precondition (and
      (ready-road ?v ?r)
      (road-next ?r ?next)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
    )
    :effect (and
      (not (ready-road ?v ?r))
      (ready-road ?v ?next)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-road_0621-macro_0002-tw_00240
    :parameters (?v - vehicle ?r - road_type_road_0621 ?next - road_type_macro_0002 ?w - window_type_tw_00240)
    :precondition (and
      (ready-road ?v ?r)
      (road-next ?r ?next)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
    )
    :effect (and
      (not (ready-road ?v ?r))
      (ready-road ?v ?next)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-road_0621-macro_0002-tw_00270
    :parameters (?v - vehicle ?r - road_type_road_0621 ?next - road_type_macro_0002 ?w - window_type_tw_00270)
    :precondition (and
      (ready-road ?v ?r)
      (road-next ?r ?next)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
    )
    :effect (and
      (not (ready-road ?v ?r))
      (ready-road ?v ?next)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-road_0621-macro_0002-tw_00300
    :parameters (?v - vehicle ?r - road_type_road_0621 ?next - road_type_macro_0002 ?w - window_type_tw_00300)
    :precondition (and
      (ready-road ?v ?r)
      (road-next ?r ?next)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
    )
    :effect (and
      (not (ready-road ?v ?r))
      (ready-road ?v ?next)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-road_0621-macro_0002-tw_00330
    :parameters (?v - vehicle ?r - road_type_road_0621 ?next - road_type_macro_0002 ?w - window_type_tw_00330)
    :precondition (and
      (ready-road ?v ?r)
      (road-next ?r ?next)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
    )
    :effect (and
      (not (ready-road ?v ?r))
      (ready-road ?v ?next)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-road_0621-macro_0002-tw_00360
    :parameters (?v - vehicle ?r - road_type_road_0621 ?next - road_type_macro_0002 ?w - window_type_tw_00360)
    :precondition (and
      (ready-road ?v ?r)
      (road-next ?r ?next)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
    )
    :effect (and
      (not (ready-road ?v ?r))
      (ready-road ?v ?next)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-road_0621-macro_0002-tw_00390
    :parameters (?v - vehicle ?r - road_type_road_0621 ?next - road_type_macro_0002 ?w - window_type_tw_00390)
    :precondition (and
      (ready-road ?v ?r)
      (road-next ?r ?next)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
    )
    :effect (and
      (not (ready-road ?v ?r))
      (ready-road ?v ?next)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-road_0621-macro_0002-tw_00420
    :parameters (?v - vehicle ?r - road_type_road_0621 ?next - road_type_macro_0002 ?w - window_type_tw_00420)
    :precondition (and
      (ready-road ?v ?r)
      (road-next ?r ?next)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
    )
    :effect (and
      (not (ready-road ?v ?r))
      (ready-road ?v ?next)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-road_0621-macro_0002-tw_00450
    :parameters (?v - vehicle ?r - road_type_road_0621 ?next - road_type_macro_0002 ?w - window_type_tw_00450)
    :precondition (and
      (ready-road ?v ?r)
      (road-next ?r ?next)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
    )
    :effect (and
      (not (ready-road ?v ?r))
      (ready-road ?v ?next)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-road_0621-macro_0002-tw_00480
    :parameters (?v - vehicle ?r - road_type_road_0621 ?next - road_type_macro_0002 ?w - window_type_tw_00480)
    :precondition (and
      (ready-road ?v ?r)
      (road-next ?r ?next)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
    )
    :effect (and
      (not (ready-road ?v ?r))
      (ready-road ?v ?next)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-road_0621-macro_0002-tw_00510
    :parameters (?v - vehicle ?r - road_type_road_0621 ?next - road_type_macro_0002 ?w - window_type_tw_00510)
    :precondition (and
      (ready-road ?v ?r)
      (road-next ?r ?next)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
    )
    :effect (and
      (not (ready-road ?v ?r))
      (ready-road ?v ?next)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-road_0621-macro_0002-tw_00540
    :parameters (?v - vehicle ?r - road_type_road_0621 ?next - road_type_macro_0002 ?w - window_type_tw_00540)
    :precondition (and
      (ready-road ?v ?r)
      (road-next ?r ?next)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
    )
    :effect (and
      (not (ready-road ?v ?r))
      (ready-road ?v ?next)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-road_0621-macro_0002-tw_00570
    :parameters (?v - vehicle ?r - road_type_road_0621 ?next - road_type_macro_0002 ?w - window_type_tw_00570)
    :precondition (and
      (ready-road ?v ?r)
      (road-next ?r ?next)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
    )
    :effect (and
      (not (ready-road ?v ?r))
      (ready-road ?v ?next)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-road_0621-macro_0002-tw_00600
    :parameters (?v - vehicle ?r - road_type_road_0621 ?next - road_type_macro_0002 ?w - window_type_tw_00600)
    :precondition (and
      (ready-road ?v ?r)
      (road-next ?r ?next)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
    )
    :effect (and
      (not (ready-road ?v ?r))
      (ready-road ?v ?next)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-road_0621-macro_0003-tw_00000
    :parameters (?v - vehicle ?r - road_type_road_0621 ?next - road_type_macro_0003 ?w - window_type_tw_00000)
    :precondition (and
      (ready-road ?v ?r)
      (road-next ?r ?next)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
    )
    :effect (and
      (not (ready-road ?v ?r))
      (ready-road ?v ?next)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-road_0621-macro_0003-tw_00030
    :parameters (?v - vehicle ?r - road_type_road_0621 ?next - road_type_macro_0003 ?w - window_type_tw_00030)
    :precondition (and
      (ready-road ?v ?r)
      (road-next ?r ?next)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
    )
    :effect (and
      (not (ready-road ?v ?r))
      (ready-road ?v ?next)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-road_0621-macro_0003-tw_00060
    :parameters (?v - vehicle ?r - road_type_road_0621 ?next - road_type_macro_0003 ?w - window_type_tw_00060)
    :precondition (and
      (ready-road ?v ?r)
      (road-next ?r ?next)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
    )
    :effect (and
      (not (ready-road ?v ?r))
      (ready-road ?v ?next)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-road_0621-macro_0003-tw_00090
    :parameters (?v - vehicle ?r - road_type_road_0621 ?next - road_type_macro_0003 ?w - window_type_tw_00090)
    :precondition (and
      (ready-road ?v ?r)
      (road-next ?r ?next)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
    )
    :effect (and
      (not (ready-road ?v ?r))
      (ready-road ?v ?next)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-road_0621-macro_0003-tw_00120
    :parameters (?v - vehicle ?r - road_type_road_0621 ?next - road_type_macro_0003 ?w - window_type_tw_00120)
    :precondition (and
      (ready-road ?v ?r)
      (road-next ?r ?next)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
    )
    :effect (and
      (not (ready-road ?v ?r))
      (ready-road ?v ?next)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-road_0621-macro_0003-tw_00150
    :parameters (?v - vehicle ?r - road_type_road_0621 ?next - road_type_macro_0003 ?w - window_type_tw_00150)
    :precondition (and
      (ready-road ?v ?r)
      (road-next ?r ?next)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
    )
    :effect (and
      (not (ready-road ?v ?r))
      (ready-road ?v ?next)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-road_0621-macro_0003-tw_00180
    :parameters (?v - vehicle ?r - road_type_road_0621 ?next - road_type_macro_0003 ?w - window_type_tw_00180)
    :precondition (and
      (ready-road ?v ?r)
      (road-next ?r ?next)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
    )
    :effect (and
      (not (ready-road ?v ?r))
      (ready-road ?v ?next)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-road_0621-macro_0003-tw_00210
    :parameters (?v - vehicle ?r - road_type_road_0621 ?next - road_type_macro_0003 ?w - window_type_tw_00210)
    :precondition (and
      (ready-road ?v ?r)
      (road-next ?r ?next)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
    )
    :effect (and
      (not (ready-road ?v ?r))
      (ready-road ?v ?next)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-road_0621-macro_0003-tw_00240
    :parameters (?v - vehicle ?r - road_type_road_0621 ?next - road_type_macro_0003 ?w - window_type_tw_00240)
    :precondition (and
      (ready-road ?v ?r)
      (road-next ?r ?next)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
    )
    :effect (and
      (not (ready-road ?v ?r))
      (ready-road ?v ?next)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-road_0621-macro_0003-tw_00270
    :parameters (?v - vehicle ?r - road_type_road_0621 ?next - road_type_macro_0003 ?w - window_type_tw_00270)
    :precondition (and
      (ready-road ?v ?r)
      (road-next ?r ?next)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
    )
    :effect (and
      (not (ready-road ?v ?r))
      (ready-road ?v ?next)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-road_0621-macro_0003-tw_00300
    :parameters (?v - vehicle ?r - road_type_road_0621 ?next - road_type_macro_0003 ?w - window_type_tw_00300)
    :precondition (and
      (ready-road ?v ?r)
      (road-next ?r ?next)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
    )
    :effect (and
      (not (ready-road ?v ?r))
      (ready-road ?v ?next)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-road_0621-macro_0003-tw_00330
    :parameters (?v - vehicle ?r - road_type_road_0621 ?next - road_type_macro_0003 ?w - window_type_tw_00330)
    :precondition (and
      (ready-road ?v ?r)
      (road-next ?r ?next)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
    )
    :effect (and
      (not (ready-road ?v ?r))
      (ready-road ?v ?next)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-road_0621-macro_0003-tw_00360
    :parameters (?v - vehicle ?r - road_type_road_0621 ?next - road_type_macro_0003 ?w - window_type_tw_00360)
    :precondition (and
      (ready-road ?v ?r)
      (road-next ?r ?next)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
    )
    :effect (and
      (not (ready-road ?v ?r))
      (ready-road ?v ?next)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-road_0621-macro_0003-tw_00390
    :parameters (?v - vehicle ?r - road_type_road_0621 ?next - road_type_macro_0003 ?w - window_type_tw_00390)
    :precondition (and
      (ready-road ?v ?r)
      (road-next ?r ?next)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
    )
    :effect (and
      (not (ready-road ?v ?r))
      (ready-road ?v ?next)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-road_0621-macro_0003-tw_00420
    :parameters (?v - vehicle ?r - road_type_road_0621 ?next - road_type_macro_0003 ?w - window_type_tw_00420)
    :precondition (and
      (ready-road ?v ?r)
      (road-next ?r ?next)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
    )
    :effect (and
      (not (ready-road ?v ?r))
      (ready-road ?v ?next)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-road_0621-macro_0003-tw_00450
    :parameters (?v - vehicle ?r - road_type_road_0621 ?next - road_type_macro_0003 ?w - window_type_tw_00450)
    :precondition (and
      (ready-road ?v ?r)
      (road-next ?r ?next)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
    )
    :effect (and
      (not (ready-road ?v ?r))
      (ready-road ?v ?next)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-road_0621-macro_0003-tw_00480
    :parameters (?v - vehicle ?r - road_type_road_0621 ?next - road_type_macro_0003 ?w - window_type_tw_00480)
    :precondition (and
      (ready-road ?v ?r)
      (road-next ?r ?next)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
    )
    :effect (and
      (not (ready-road ?v ?r))
      (ready-road ?v ?next)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-road_0621-macro_0003-tw_00510
    :parameters (?v - vehicle ?r - road_type_road_0621 ?next - road_type_macro_0003 ?w - window_type_tw_00510)
    :precondition (and
      (ready-road ?v ?r)
      (road-next ?r ?next)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
    )
    :effect (and
      (not (ready-road ?v ?r))
      (ready-road ?v ?next)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-road_0621-macro_0003-tw_00540
    :parameters (?v - vehicle ?r - road_type_road_0621 ?next - road_type_macro_0003 ?w - window_type_tw_00540)
    :precondition (and
      (ready-road ?v ?r)
      (road-next ?r ?next)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
    )
    :effect (and
      (not (ready-road ?v ?r))
      (ready-road ?v ?next)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-road_0621-macro_0003-tw_00570
    :parameters (?v - vehicle ?r - road_type_road_0621 ?next - road_type_macro_0003 ?w - window_type_tw_00570)
    :precondition (and
      (ready-road ?v ?r)
      (road-next ?r ?next)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
    )
    :effect (and
      (not (ready-road ?v ?r))
      (ready-road ?v ?next)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-road_0621-macro_0003-tw_00600
    :parameters (?v - vehicle ?r - road_type_road_0621 ?next - road_type_macro_0003 ?w - window_type_tw_00600)
    :precondition (and
      (ready-road ?v ?r)
      (road-next ?r ?next)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
    )
    :effect (and
      (not (ready-road ?v ?r))
      (ready-road ?v ?next)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-static-road_0622-road_0412
    :parameters (?v - vehicle ?r - road_type_road_0622 ?next - road_type_road_0412)
    :precondition (and
      (ready-road ?v ?r)
      (road-next ?r ?next)
      (road-open ?r)
      (static-road ?r)
      (not (road-blocked ?r))
    )
    :effect (and
      (not (ready-road ?v ?r))
      (ready-road ?v ?next)
      (increase (travel-time ?v) (travel-duration ?r))
      (increase (sim-time) (travel-duration ?r))
    )
  )

  (:action traverse-road-static-road_0622-road_0413
    :parameters (?v - vehicle ?r - road_type_road_0622 ?next - road_type_road_0413)
    :precondition (and
      (ready-road ?v ?r)
      (road-next ?r ?next)
      (road-open ?r)
      (static-road ?r)
      (not (road-blocked ?r))
    )
    :effect (and
      (not (ready-road ?v ?r))
      (ready-road ?v ?next)
      (increase (travel-time ?v) (travel-duration ?r))
      (increase (sim-time) (travel-duration ?r))
    )
  )

  (:action traverse-road-static-road_0623-road_0461
    :parameters (?v - vehicle ?r - road_type_road_0623 ?next - road_type_road_0461)
    :precondition (and
      (ready-road ?v ?r)
      (road-next ?r ?next)
      (road-open ?r)
      (static-road ?r)
      (not (road-blocked ?r))
    )
    :effect (and
      (not (ready-road ?v ?r))
      (ready-road ?v ?next)
      (increase (travel-time ?v) (travel-duration ?r))
      (increase (sim-time) (travel-duration ?r))
    )
  )

  (:action traverse-road-static-road_0623-road_0462
    :parameters (?v - vehicle ?r - road_type_road_0623 ?next - road_type_road_0462)
    :precondition (and
      (ready-road ?v ?r)
      (road-next ?r ?next)
      (road-open ?r)
      (static-road ?r)
      (not (road-blocked ?r))
    )
    :effect (and
      (not (ready-road ?v ?r))
      (ready-road ?v ?next)
      (increase (travel-time ?v) (travel-duration ?r))
      (increase (sim-time) (travel-duration ?r))
    )
  )

  (:action traverse-road-static-road_0624-road_0414
    :parameters (?v - vehicle ?r - road_type_road_0624 ?next - road_type_road_0414)
    :precondition (and
      (ready-road ?v ?r)
      (road-next ?r ?next)
      (road-open ?r)
      (static-road ?r)
      (not (road-blocked ?r))
    )
    :effect (and
      (not (ready-road ?v ?r))
      (ready-road ?v ?next)
      (increase (travel-time ?v) (travel-duration ?r))
      (increase (sim-time) (travel-duration ?r))
    )
  )

  (:action traverse-road-static-road_0625-road_0447
    :parameters (?v - vehicle ?r - road_type_road_0625 ?next - road_type_road_0447)
    :precondition (and
      (ready-road ?v ?r)
      (road-next ?r ?next)
      (road-open ?r)
      (static-road ?r)
      (not (road-blocked ?r))
    )
    :effect (and
      (not (ready-road ?v ?r))
      (ready-road ?v ?next)
      (increase (travel-time ?v) (travel-duration ?r))
      (increase (sim-time) (travel-duration ?r))
    )
  )

  (:action traverse-road-static-road_0625-road_0448
    :parameters (?v - vehicle ?r - road_type_road_0625 ?next - road_type_road_0448)
    :precondition (and
      (ready-road ?v ?r)
      (road-next ?r ?next)
      (road-open ?r)
      (static-road ?r)
      (not (road-blocked ?r))
    )
    :effect (and
      (not (ready-road ?v ?r))
      (ready-road ?v ?next)
      (increase (travel-time ?v) (travel-duration ?r))
      (increase (sim-time) (travel-duration ?r))
    )
  )

  (:action traverse-road-static-road_0626-road_0327
    :parameters (?v - vehicle ?r - road_type_road_0626 ?next - road_type_road_0327)
    :precondition (and
      (ready-road ?v ?r)
      (road-next ?r ?next)
      (road-open ?r)
      (static-road ?r)
      (not (road-blocked ?r))
    )
    :effect (and
      (not (ready-road ?v ?r))
      (ready-road ?v ?next)
      (increase (travel-time ?v) (travel-duration ?r))
      (increase (sim-time) (travel-duration ?r))
    )
  )

  (:action traverse-road-static-road_0626-road_0328
    :parameters (?v - vehicle ?r - road_type_road_0626 ?next - road_type_road_0328)
    :precondition (and
      (ready-road ?v ?r)
      (road-next ?r ?next)
      (road-open ?r)
      (static-road ?r)
      (not (road-blocked ?r))
    )
    :effect (and
      (not (ready-road ?v ?r))
      (ready-road ?v ?next)
      (increase (travel-time ?v) (travel-duration ?r))
      (increase (sim-time) (travel-duration ?r))
    )
  )

  (:action traverse-road-static-road_0627-road_0744
    :parameters (?v - vehicle ?r - road_type_road_0627 ?next - road_type_road_0744)
    :precondition (and
      (ready-road ?v ?r)
      (road-next ?r ?next)
      (road-open ?r)
      (static-road ?r)
      (not (road-blocked ?r))
    )
    :effect (and
      (not (ready-road ?v ?r))
      (ready-road ?v ?next)
      (increase (travel-time ?v) (travel-duration ?r))
      (increase (sim-time) (travel-duration ?r))
    )
  )

  (:action traverse-road-static-road_0628-road_0816
    :parameters (?v - vehicle ?r - road_type_road_0628 ?next - road_type_road_0816)
    :precondition (and
      (ready-road ?v ?r)
      (road-next ?r ?next)
      (road-open ?r)
      (static-road ?r)
      (not (road-blocked ?r))
    )
    :effect (and
      (not (ready-road ?v ?r))
      (ready-road ?v ?next)
      (increase (travel-time ?v) (travel-duration ?r))
      (increase (sim-time) (travel-duration ?r))
    )
  )

  (:action traverse-road-static-road_0628-road_0817
    :parameters (?v - vehicle ?r - road_type_road_0628 ?next - road_type_road_0817)
    :precondition (and
      (ready-road ?v ?r)
      (road-next ?r ?next)
      (road-open ?r)
      (static-road ?r)
      (not (road-blocked ?r))
    )
    :effect (and
      (not (ready-road ?v ?r))
      (ready-road ?v ?next)
      (increase (travel-time ?v) (travel-duration ?r))
      (increase (sim-time) (travel-duration ?r))
    )
  )

  (:action traverse-road-static-road_0628-road_0818
    :parameters (?v - vehicle ?r - road_type_road_0628 ?next - road_type_road_0818)
    :precondition (and
      (ready-road ?v ?r)
      (road-next ?r ?next)
      (road-open ?r)
      (static-road ?r)
      (not (road-blocked ?r))
    )
    :effect (and
      (not (ready-road ?v ?r))
      (ready-road ?v ?next)
      (increase (travel-time ?v) (travel-duration ?r))
      (increase (sim-time) (travel-duration ?r))
    )
  )

  (:action traverse-road-static-road_0629-road_0787
    :parameters (?v - vehicle ?r - road_type_road_0629 ?next - road_type_road_0787)
    :precondition (and
      (ready-road ?v ?r)
      (road-next ?r ?next)
      (road-open ?r)
      (static-road ?r)
      (not (road-blocked ?r))
    )
    :effect (and
      (not (ready-road ?v ?r))
      (ready-road ?v ?next)
      (increase (travel-time ?v) (travel-duration ?r))
      (increase (sim-time) (travel-duration ?r))
    )
  )

  (:action traverse-road-static-road_0630-road_0196
    :parameters (?v - vehicle ?r - road_type_road_0630 ?next - road_type_road_0196)
    :precondition (and
      (ready-road ?v ?r)
      (road-next ?r ?next)
      (road-open ?r)
      (static-road ?r)
      (not (road-blocked ?r))
    )
    :effect (and
      (not (ready-road ?v ?r))
      (ready-road ?v ?next)
      (increase (travel-time ?v) (travel-duration ?r))
      (increase (sim-time) (travel-duration ?r))
    )
  )

  (:action traverse-road-static-road_0632-road_0633
    :parameters (?v - vehicle ?r - road_type_road_0632 ?next - road_type_road_0633)
    :precondition (and
      (ready-road ?v ?r)
      (road-next ?r ?next)
      (road-open ?r)
      (static-road ?r)
      (not (road-blocked ?r))
    )
    :effect (and
      (not (ready-road ?v ?r))
      (ready-road ?v ?next)
      (increase (travel-time ?v) (travel-duration ?r))
      (increase (sim-time) (travel-duration ?r))
    )
  )

  (:action traverse-road-static-road_0633-road_0630
    :parameters (?v - vehicle ?r - road_type_road_0633 ?next - road_type_road_0630)
    :precondition (and
      (ready-road ?v ?r)
      (road-next ?r ?next)
      (road-open ?r)
      (static-road ?r)
      (not (road-blocked ?r))
    )
    :effect (and
      (not (ready-road ?v ?r))
      (ready-road ?v ?next)
      (increase (travel-time ?v) (travel-duration ?r))
      (increase (sim-time) (travel-duration ?r))
    )
  )

  (:action traverse-road-static-road_0634-macro_0031
    :parameters (?v - vehicle ?r - road_type_road_0634 ?next - road_type_macro_0031)
    :precondition (and
      (ready-road ?v ?r)
      (road-next ?r ?next)
      (road-open ?r)
      (static-road ?r)
      (not (road-blocked ?r))
    )
    :effect (and
      (not (ready-road ?v ?r))
      (ready-road ?v ?next)
      (increase (travel-time ?v) (travel-duration ?r))
      (increase (sim-time) (travel-duration ?r))
    )
  )

  (:action traverse-road-static-road_0635-road_0349
    :parameters (?v - vehicle ?r - road_type_road_0635 ?next - road_type_road_0349)
    :precondition (and
      (ready-road ?v ?r)
      (road-next ?r ?next)
      (road-open ?r)
      (static-road ?r)
      (not (road-blocked ?r))
    )
    :effect (and
      (not (ready-road ?v ?r))
      (ready-road ?v ?next)
      (increase (travel-time ?v) (travel-duration ?r))
      (increase (sim-time) (travel-duration ?r))
    )
  )

  (:action traverse-road-static-road_0635-road_0350
    :parameters (?v - vehicle ?r - road_type_road_0635 ?next - road_type_road_0350)
    :precondition (and
      (ready-road ?v ?r)
      (road-next ?r ?next)
      (road-open ?r)
      (static-road ?r)
      (not (road-blocked ?r))
    )
    :effect (and
      (not (ready-road ?v ?r))
      (ready-road ?v ?next)
      (increase (travel-time ?v) (travel-duration ?r))
      (increase (sim-time) (travel-duration ?r))
    )
  )

  (:action traverse-road-static-road_0636-road_0759
    :parameters (?v - vehicle ?r - road_type_road_0636 ?next - road_type_road_0759)
    :precondition (and
      (ready-road ?v ?r)
      (road-next ?r ?next)
      (road-open ?r)
      (static-road ?r)
      (not (road-blocked ?r))
    )
    :effect (and
      (not (ready-road ?v ?r))
      (ready-road ?v ?next)
      (increase (travel-time ?v) (travel-duration ?r))
      (increase (sim-time) (travel-duration ?r))
    )
  )

  (:action traverse-road-static-road_0637-macro_0044
    :parameters (?v - vehicle ?r - road_type_road_0637 ?next - road_type_macro_0044)
    :precondition (and
      (ready-road ?v ?r)
      (road-next ?r ?next)
      (road-open ?r)
      (static-road ?r)
      (not (road-blocked ?r))
    )
    :effect (and
      (not (ready-road ?v ?r))
      (ready-road ?v ?next)
      (increase (travel-time ?v) (travel-duration ?r))
      (increase (sim-time) (travel-duration ?r))
    )
  )

  (:action traverse-road-static-road_0637-road_0632
    :parameters (?v - vehicle ?r - road_type_road_0637 ?next - road_type_road_0632)
    :precondition (and
      (ready-road ?v ?r)
      (road-next ?r ?next)
      (road-open ?r)
      (static-road ?r)
      (not (road-blocked ?r))
    )
    :effect (and
      (not (ready-road ?v ?r))
      (ready-road ?v ?next)
      (increase (travel-time ?v) (travel-duration ?r))
      (increase (sim-time) (travel-duration ?r))
    )
  )

  (:action traverse-road-static-road_0639-road_0611
    :parameters (?v - vehicle ?r - road_type_road_0639 ?next - road_type_road_0611)
    :precondition (and
      (ready-road ?v ?r)
      (road-next ?r ?next)
      (road-open ?r)
      (static-road ?r)
      (not (road-blocked ?r))
    )
    :effect (and
      (not (ready-road ?v ?r))
      (ready-road ?v ?next)
      (increase (travel-time ?v) (travel-duration ?r))
      (increase (sim-time) (travel-duration ?r))
    )
  )

  (:action traverse-road-static-road_0639-road_0612
    :parameters (?v - vehicle ?r - road_type_road_0639 ?next - road_type_road_0612)
    :precondition (and
      (ready-road ?v ?r)
      (road-next ?r ?next)
      (road-open ?r)
      (static-road ?r)
      (not (road-blocked ?r))
    )
    :effect (and
      (not (ready-road ?v ?r))
      (ready-road ?v ?next)
      (increase (travel-time ?v) (travel-duration ?r))
      (increase (sim-time) (travel-duration ?r))
    )
  )

  (:action traverse-road-static-road_0640-macro_0033
    :parameters (?v - vehicle ?r - road_type_road_0640 ?next - road_type_macro_0033)
    :precondition (and
      (ready-road ?v ?r)
      (road-next ?r ?next)
      (road-open ?r)
      (static-road ?r)
      (not (road-blocked ?r))
    )
    :effect (and
      (not (ready-road ?v ?r))
      (ready-road ?v ?next)
      (increase (travel-time ?v) (travel-duration ?r))
      (increase (sim-time) (travel-duration ?r))
    )
  )

  (:action traverse-road-static-road_0640-road_0428
    :parameters (?v - vehicle ?r - road_type_road_0640 ?next - road_type_road_0428)
    :precondition (and
      (ready-road ?v ?r)
      (road-next ?r ?next)
      (road-open ?r)
      (static-road ?r)
      (not (road-blocked ?r))
    )
    :effect (and
      (not (ready-road ?v ?r))
      (ready-road ?v ?next)
      (increase (travel-time ?v) (travel-duration ?r))
      (increase (sim-time) (travel-duration ?r))
    )
  )

  (:action traverse-road-static-road_0640-road_0430
    :parameters (?v - vehicle ?r - road_type_road_0640 ?next - road_type_road_0430)
    :precondition (and
      (ready-road ?v ?r)
      (road-next ?r ?next)
      (road-open ?r)
      (static-road ?r)
      (not (road-blocked ?r))
    )
    :effect (and
      (not (ready-road ?v ?r))
      (ready-road ?v ?next)
      (increase (travel-time ?v) (travel-duration ?r))
      (increase (sim-time) (travel-duration ?r))
    )
  )

  (:action traverse-road-static-road_0641-road_0420
    :parameters (?v - vehicle ?r - road_type_road_0641 ?next - road_type_road_0420)
    :precondition (and
      (ready-road ?v ?r)
      (road-next ?r ?next)
      (road-open ?r)
      (static-road ?r)
      (not (road-blocked ?r))
    )
    :effect (and
      (not (ready-road ?v ?r))
      (ready-road ?v ?next)
      (increase (travel-time ?v) (travel-duration ?r))
      (increase (sim-time) (travel-duration ?r))
    )
  )

  (:action traverse-road-static-road_0641-road_0421
    :parameters (?v - vehicle ?r - road_type_road_0641 ?next - road_type_road_0421)
    :precondition (and
      (ready-road ?v ?r)
      (road-next ?r ?next)
      (road-open ?r)
      (static-road ?r)
      (not (road-blocked ?r))
    )
    :effect (and
      (not (ready-road ?v ?r))
      (ready-road ?v ?next)
      (increase (travel-time ?v) (travel-duration ?r))
      (increase (sim-time) (travel-duration ?r))
    )
  )

  (:action traverse-road-static-road_0641-road_0422
    :parameters (?v - vehicle ?r - road_type_road_0641 ?next - road_type_road_0422)
    :precondition (and
      (ready-road ?v ?r)
      (road-next ?r ?next)
      (road-open ?r)
      (static-road ?r)
      (not (road-blocked ?r))
    )
    :effect (and
      (not (ready-road ?v ?r))
      (ready-road ?v ?next)
      (increase (travel-time ?v) (travel-duration ?r))
      (increase (sim-time) (travel-duration ?r))
    )
  )

  (:action traverse-road-static-road_0642-road_0707
    :parameters (?v - vehicle ?r - road_type_road_0642 ?next - road_type_road_0707)
    :precondition (and
      (ready-road ?v ?r)
      (road-next ?r ?next)
      (road-open ?r)
      (static-road ?r)
      (not (road-blocked ?r))
    )
    :effect (and
      (not (ready-road ?v ?r))
      (ready-road ?v ?next)
      (increase (travel-time ?v) (travel-duration ?r))
      (increase (sim-time) (travel-duration ?r))
    )
  )

  (:action traverse-road-static-road_0642-road_0708
    :parameters (?v - vehicle ?r - road_type_road_0642 ?next - road_type_road_0708)
    :precondition (and
      (ready-road ?v ?r)
      (road-next ?r ?next)
      (road-open ?r)
      (static-road ?r)
      (not (road-blocked ?r))
    )
    :effect (and
      (not (ready-road ?v ?r))
      (ready-road ?v ?next)
      (increase (travel-time ?v) (travel-duration ?r))
      (increase (sim-time) (travel-duration ?r))
    )
  )

  (:action traverse-road-static-road_0643-macro_0051
    :parameters (?v - vehicle ?r - road_type_road_0643 ?next - road_type_macro_0051)
    :precondition (and
      (ready-road ?v ?r)
      (road-next ?r ?next)
      (road-open ?r)
      (static-road ?r)
      (not (road-blocked ?r))
    )
    :effect (and
      (not (ready-road ?v ?r))
      (ready-road ?v ?next)
      (increase (travel-time ?v) (travel-duration ?r))
      (increase (sim-time) (travel-duration ?r))
    )
  )

  (:action traverse-road-static-road_0643-road_0789
    :parameters (?v - vehicle ?r - road_type_road_0643 ?next - road_type_road_0789)
    :precondition (and
      (ready-road ?v ?r)
      (road-next ?r ?next)
      (road-open ?r)
      (static-road ?r)
      (not (road-blocked ?r))
    )
    :effect (and
      (not (ready-road ?v ?r))
      (ready-road ?v ?next)
      (increase (travel-time ?v) (travel-duration ?r))
      (increase (sim-time) (travel-duration ?r))
    )
  )

  (:action traverse-road-static-road_0643-road_0790
    :parameters (?v - vehicle ?r - road_type_road_0643 ?next - road_type_road_0790)
    :precondition (and
      (ready-road ?v ?r)
      (road-next ?r ?next)
      (road-open ?r)
      (static-road ?r)
      (not (road-blocked ?r))
    )
    :effect (and
      (not (ready-road ?v ?r))
      (ready-road ?v ?next)
      (increase (travel-time ?v) (travel-duration ?r))
      (increase (sim-time) (travel-duration ?r))
    )
  )

  (:action traverse-road-static-road_0644-road_0200
    :parameters (?v - vehicle ?r - road_type_road_0644 ?next - road_type_road_0200)
    :precondition (and
      (ready-road ?v ?r)
      (road-next ?r ?next)
      (road-open ?r)
      (static-road ?r)
      (not (road-blocked ?r))
    )
    :effect (and
      (not (ready-road ?v ?r))
      (ready-road ?v ?next)
      (increase (travel-time ?v) (travel-duration ?r))
      (increase (sim-time) (travel-duration ?r))
    )
  )

  (:action traverse-road-static-road_0645-road_0020
    :parameters (?v - vehicle ?r - road_type_road_0645 ?next - road_type_road_0020)
    :precondition (and
      (ready-road ?v ?r)
      (road-next ?r ?next)
      (road-open ?r)
      (static-road ?r)
      (not (road-blocked ?r))
    )
    :effect (and
      (not (ready-road ?v ?r))
      (ready-road ?v ?next)
      (increase (travel-time ?v) (travel-duration ?r))
      (increase (sim-time) (travel-duration ?r))
    )
  )

  (:action traverse-road-static-road_0645-road_0021
    :parameters (?v - vehicle ?r - road_type_road_0645 ?next - road_type_road_0021)
    :precondition (and
      (ready-road ?v ?r)
      (road-next ?r ?next)
      (road-open ?r)
      (static-road ?r)
      (not (road-blocked ?r))
    )
    :effect (and
      (not (ready-road ?v ?r))
      (ready-road ?v ?next)
      (increase (travel-time ?v) (travel-duration ?r))
      (increase (sim-time) (travel-duration ?r))
    )
  )

  (:action traverse-road-static-road_0647-road_0014
    :parameters (?v - vehicle ?r - road_type_road_0647 ?next - road_type_road_0014)
    :precondition (and
      (ready-road ?v ?r)
      (road-next ?r ?next)
      (road-open ?r)
      (static-road ?r)
      (not (road-blocked ?r))
    )
    :effect (and
      (not (ready-road ?v ?r))
      (ready-road ?v ?next)
      (increase (travel-time ?v) (travel-duration ?r))
      (increase (sim-time) (travel-duration ?r))
    )
  )

  (:action traverse-road-dynamic-road_0648-road_0470-tw_00000
    :parameters (?v - vehicle ?r - road_type_road_0648 ?next - road_type_road_0470 ?w - window_type_tw_00000)
    :precondition (and
      (ready-road ?v ?r)
      (road-next ?r ?next)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
    )
    :effect (and
      (not (ready-road ?v ?r))
      (ready-road ?v ?next)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-road_0648-road_0470-tw_00030
    :parameters (?v - vehicle ?r - road_type_road_0648 ?next - road_type_road_0470 ?w - window_type_tw_00030)
    :precondition (and
      (ready-road ?v ?r)
      (road-next ?r ?next)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
    )
    :effect (and
      (not (ready-road ?v ?r))
      (ready-road ?v ?next)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-road_0648-road_0470-tw_00060
    :parameters (?v - vehicle ?r - road_type_road_0648 ?next - road_type_road_0470 ?w - window_type_tw_00060)
    :precondition (and
      (ready-road ?v ?r)
      (road-next ?r ?next)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
    )
    :effect (and
      (not (ready-road ?v ?r))
      (ready-road ?v ?next)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-road_0648-road_0470-tw_00090
    :parameters (?v - vehicle ?r - road_type_road_0648 ?next - road_type_road_0470 ?w - window_type_tw_00090)
    :precondition (and
      (ready-road ?v ?r)
      (road-next ?r ?next)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
    )
    :effect (and
      (not (ready-road ?v ?r))
      (ready-road ?v ?next)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-road_0648-road_0470-tw_00120
    :parameters (?v - vehicle ?r - road_type_road_0648 ?next - road_type_road_0470 ?w - window_type_tw_00120)
    :precondition (and
      (ready-road ?v ?r)
      (road-next ?r ?next)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
    )
    :effect (and
      (not (ready-road ?v ?r))
      (ready-road ?v ?next)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-road_0648-road_0470-tw_00150
    :parameters (?v - vehicle ?r - road_type_road_0648 ?next - road_type_road_0470 ?w - window_type_tw_00150)
    :precondition (and
      (ready-road ?v ?r)
      (road-next ?r ?next)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
    )
    :effect (and
      (not (ready-road ?v ?r))
      (ready-road ?v ?next)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-road_0648-road_0470-tw_00180
    :parameters (?v - vehicle ?r - road_type_road_0648 ?next - road_type_road_0470 ?w - window_type_tw_00180)
    :precondition (and
      (ready-road ?v ?r)
      (road-next ?r ?next)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
    )
    :effect (and
      (not (ready-road ?v ?r))
      (ready-road ?v ?next)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-road_0648-road_0470-tw_00210
    :parameters (?v - vehicle ?r - road_type_road_0648 ?next - road_type_road_0470 ?w - window_type_tw_00210)
    :precondition (and
      (ready-road ?v ?r)
      (road-next ?r ?next)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
    )
    :effect (and
      (not (ready-road ?v ?r))
      (ready-road ?v ?next)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-road_0648-road_0470-tw_00240
    :parameters (?v - vehicle ?r - road_type_road_0648 ?next - road_type_road_0470 ?w - window_type_tw_00240)
    :precondition (and
      (ready-road ?v ?r)
      (road-next ?r ?next)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
    )
    :effect (and
      (not (ready-road ?v ?r))
      (ready-road ?v ?next)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-road_0648-road_0470-tw_00270
    :parameters (?v - vehicle ?r - road_type_road_0648 ?next - road_type_road_0470 ?w - window_type_tw_00270)
    :precondition (and
      (ready-road ?v ?r)
      (road-next ?r ?next)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
    )
    :effect (and
      (not (ready-road ?v ?r))
      (ready-road ?v ?next)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-road_0648-road_0470-tw_00300
    :parameters (?v - vehicle ?r - road_type_road_0648 ?next - road_type_road_0470 ?w - window_type_tw_00300)
    :precondition (and
      (ready-road ?v ?r)
      (road-next ?r ?next)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
    )
    :effect (and
      (not (ready-road ?v ?r))
      (ready-road ?v ?next)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-road_0648-road_0470-tw_00330
    :parameters (?v - vehicle ?r - road_type_road_0648 ?next - road_type_road_0470 ?w - window_type_tw_00330)
    :precondition (and
      (ready-road ?v ?r)
      (road-next ?r ?next)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
    )
    :effect (and
      (not (ready-road ?v ?r))
      (ready-road ?v ?next)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-road_0648-road_0470-tw_00360
    :parameters (?v - vehicle ?r - road_type_road_0648 ?next - road_type_road_0470 ?w - window_type_tw_00360)
    :precondition (and
      (ready-road ?v ?r)
      (road-next ?r ?next)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
    )
    :effect (and
      (not (ready-road ?v ?r))
      (ready-road ?v ?next)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-road_0648-road_0470-tw_00390
    :parameters (?v - vehicle ?r - road_type_road_0648 ?next - road_type_road_0470 ?w - window_type_tw_00390)
    :precondition (and
      (ready-road ?v ?r)
      (road-next ?r ?next)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
    )
    :effect (and
      (not (ready-road ?v ?r))
      (ready-road ?v ?next)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-road_0648-road_0470-tw_00420
    :parameters (?v - vehicle ?r - road_type_road_0648 ?next - road_type_road_0470 ?w - window_type_tw_00420)
    :precondition (and
      (ready-road ?v ?r)
      (road-next ?r ?next)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
    )
    :effect (and
      (not (ready-road ?v ?r))
      (ready-road ?v ?next)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-road_0648-road_0470-tw_00450
    :parameters (?v - vehicle ?r - road_type_road_0648 ?next - road_type_road_0470 ?w - window_type_tw_00450)
    :precondition (and
      (ready-road ?v ?r)
      (road-next ?r ?next)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
    )
    :effect (and
      (not (ready-road ?v ?r))
      (ready-road ?v ?next)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-road_0648-road_0470-tw_00480
    :parameters (?v - vehicle ?r - road_type_road_0648 ?next - road_type_road_0470 ?w - window_type_tw_00480)
    :precondition (and
      (ready-road ?v ?r)
      (road-next ?r ?next)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
    )
    :effect (and
      (not (ready-road ?v ?r))
      (ready-road ?v ?next)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-road_0648-road_0470-tw_00510
    :parameters (?v - vehicle ?r - road_type_road_0648 ?next - road_type_road_0470 ?w - window_type_tw_00510)
    :precondition (and
      (ready-road ?v ?r)
      (road-next ?r ?next)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
    )
    :effect (and
      (not (ready-road ?v ?r))
      (ready-road ?v ?next)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-road_0648-road_0470-tw_00540
    :parameters (?v - vehicle ?r - road_type_road_0648 ?next - road_type_road_0470 ?w - window_type_tw_00540)
    :precondition (and
      (ready-road ?v ?r)
      (road-next ?r ?next)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
    )
    :effect (and
      (not (ready-road ?v ?r))
      (ready-road ?v ?next)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-road_0648-road_0470-tw_00570
    :parameters (?v - vehicle ?r - road_type_road_0648 ?next - road_type_road_0470 ?w - window_type_tw_00570)
    :precondition (and
      (ready-road ?v ?r)
      (road-next ?r ?next)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
    )
    :effect (and
      (not (ready-road ?v ?r))
      (ready-road ?v ?next)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-road_0648-road_0470-tw_00600
    :parameters (?v - vehicle ?r - road_type_road_0648 ?next - road_type_road_0470 ?w - window_type_tw_00600)
    :precondition (and
      (ready-road ?v ?r)
      (road-next ?r ?next)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
    )
    :effect (and
      (not (ready-road ?v ?r))
      (ready-road ?v ?next)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-road_0649-road_0373-tw_00000
    :parameters (?v - vehicle ?r - road_type_road_0649 ?next - road_type_road_0373 ?w - window_type_tw_00000)
    :precondition (and
      (ready-road ?v ?r)
      (road-next ?r ?next)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
    )
    :effect (and
      (not (ready-road ?v ?r))
      (ready-road ?v ?next)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-road_0649-road_0373-tw_00030
    :parameters (?v - vehicle ?r - road_type_road_0649 ?next - road_type_road_0373 ?w - window_type_tw_00030)
    :precondition (and
      (ready-road ?v ?r)
      (road-next ?r ?next)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
    )
    :effect (and
      (not (ready-road ?v ?r))
      (ready-road ?v ?next)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-road_0649-road_0373-tw_00060
    :parameters (?v - vehicle ?r - road_type_road_0649 ?next - road_type_road_0373 ?w - window_type_tw_00060)
    :precondition (and
      (ready-road ?v ?r)
      (road-next ?r ?next)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
    )
    :effect (and
      (not (ready-road ?v ?r))
      (ready-road ?v ?next)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-road_0649-road_0373-tw_00090
    :parameters (?v - vehicle ?r - road_type_road_0649 ?next - road_type_road_0373 ?w - window_type_tw_00090)
    :precondition (and
      (ready-road ?v ?r)
      (road-next ?r ?next)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
    )
    :effect (and
      (not (ready-road ?v ?r))
      (ready-road ?v ?next)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-road_0649-road_0373-tw_00120
    :parameters (?v - vehicle ?r - road_type_road_0649 ?next - road_type_road_0373 ?w - window_type_tw_00120)
    :precondition (and
      (ready-road ?v ?r)
      (road-next ?r ?next)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
    )
    :effect (and
      (not (ready-road ?v ?r))
      (ready-road ?v ?next)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-road_0649-road_0373-tw_00150
    :parameters (?v - vehicle ?r - road_type_road_0649 ?next - road_type_road_0373 ?w - window_type_tw_00150)
    :precondition (and
      (ready-road ?v ?r)
      (road-next ?r ?next)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
    )
    :effect (and
      (not (ready-road ?v ?r))
      (ready-road ?v ?next)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-road_0649-road_0373-tw_00180
    :parameters (?v - vehicle ?r - road_type_road_0649 ?next - road_type_road_0373 ?w - window_type_tw_00180)
    :precondition (and
      (ready-road ?v ?r)
      (road-next ?r ?next)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
    )
    :effect (and
      (not (ready-road ?v ?r))
      (ready-road ?v ?next)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-road_0649-road_0373-tw_00210
    :parameters (?v - vehicle ?r - road_type_road_0649 ?next - road_type_road_0373 ?w - window_type_tw_00210)
    :precondition (and
      (ready-road ?v ?r)
      (road-next ?r ?next)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
    )
    :effect (and
      (not (ready-road ?v ?r))
      (ready-road ?v ?next)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-road_0649-road_0373-tw_00240
    :parameters (?v - vehicle ?r - road_type_road_0649 ?next - road_type_road_0373 ?w - window_type_tw_00240)
    :precondition (and
      (ready-road ?v ?r)
      (road-next ?r ?next)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
    )
    :effect (and
      (not (ready-road ?v ?r))
      (ready-road ?v ?next)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-road_0649-road_0373-tw_00270
    :parameters (?v - vehicle ?r - road_type_road_0649 ?next - road_type_road_0373 ?w - window_type_tw_00270)
    :precondition (and
      (ready-road ?v ?r)
      (road-next ?r ?next)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
    )
    :effect (and
      (not (ready-road ?v ?r))
      (ready-road ?v ?next)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-road_0649-road_0373-tw_00300
    :parameters (?v - vehicle ?r - road_type_road_0649 ?next - road_type_road_0373 ?w - window_type_tw_00300)
    :precondition (and
      (ready-road ?v ?r)
      (road-next ?r ?next)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
    )
    :effect (and
      (not (ready-road ?v ?r))
      (ready-road ?v ?next)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-road_0649-road_0373-tw_00330
    :parameters (?v - vehicle ?r - road_type_road_0649 ?next - road_type_road_0373 ?w - window_type_tw_00330)
    :precondition (and
      (ready-road ?v ?r)
      (road-next ?r ?next)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
    )
    :effect (and
      (not (ready-road ?v ?r))
      (ready-road ?v ?next)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-road_0649-road_0373-tw_00360
    :parameters (?v - vehicle ?r - road_type_road_0649 ?next - road_type_road_0373 ?w - window_type_tw_00360)
    :precondition (and
      (ready-road ?v ?r)
      (road-next ?r ?next)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
    )
    :effect (and
      (not (ready-road ?v ?r))
      (ready-road ?v ?next)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-road_0649-road_0373-tw_00390
    :parameters (?v - vehicle ?r - road_type_road_0649 ?next - road_type_road_0373 ?w - window_type_tw_00390)
    :precondition (and
      (ready-road ?v ?r)
      (road-next ?r ?next)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
    )
    :effect (and
      (not (ready-road ?v ?r))
      (ready-road ?v ?next)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-road_0649-road_0373-tw_00420
    :parameters (?v - vehicle ?r - road_type_road_0649 ?next - road_type_road_0373 ?w - window_type_tw_00420)
    :precondition (and
      (ready-road ?v ?r)
      (road-next ?r ?next)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
    )
    :effect (and
      (not (ready-road ?v ?r))
      (ready-road ?v ?next)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-road_0649-road_0373-tw_00450
    :parameters (?v - vehicle ?r - road_type_road_0649 ?next - road_type_road_0373 ?w - window_type_tw_00450)
    :precondition (and
      (ready-road ?v ?r)
      (road-next ?r ?next)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
    )
    :effect (and
      (not (ready-road ?v ?r))
      (ready-road ?v ?next)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-road_0649-road_0373-tw_00480
    :parameters (?v - vehicle ?r - road_type_road_0649 ?next - road_type_road_0373 ?w - window_type_tw_00480)
    :precondition (and
      (ready-road ?v ?r)
      (road-next ?r ?next)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
    )
    :effect (and
      (not (ready-road ?v ?r))
      (ready-road ?v ?next)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-road_0649-road_0373-tw_00510
    :parameters (?v - vehicle ?r - road_type_road_0649 ?next - road_type_road_0373 ?w - window_type_tw_00510)
    :precondition (and
      (ready-road ?v ?r)
      (road-next ?r ?next)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
    )
    :effect (and
      (not (ready-road ?v ?r))
      (ready-road ?v ?next)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-road_0649-road_0373-tw_00540
    :parameters (?v - vehicle ?r - road_type_road_0649 ?next - road_type_road_0373 ?w - window_type_tw_00540)
    :precondition (and
      (ready-road ?v ?r)
      (road-next ?r ?next)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
    )
    :effect (and
      (not (ready-road ?v ?r))
      (ready-road ?v ?next)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-road_0649-road_0373-tw_00570
    :parameters (?v - vehicle ?r - road_type_road_0649 ?next - road_type_road_0373 ?w - window_type_tw_00570)
    :precondition (and
      (ready-road ?v ?r)
      (road-next ?r ?next)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
    )
    :effect (and
      (not (ready-road ?v ?r))
      (ready-road ?v ?next)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-road_0649-road_0373-tw_00600
    :parameters (?v - vehicle ?r - road_type_road_0649 ?next - road_type_road_0373 ?w - window_type_tw_00600)
    :precondition (and
      (ready-road ?v ?r)
      (road-next ?r ?next)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
    )
    :effect (and
      (not (ready-road ?v ?r))
      (ready-road ?v ?next)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-road_0649-road_0374-tw_00000
    :parameters (?v - vehicle ?r - road_type_road_0649 ?next - road_type_road_0374 ?w - window_type_tw_00000)
    :precondition (and
      (ready-road ?v ?r)
      (road-next ?r ?next)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
    )
    :effect (and
      (not (ready-road ?v ?r))
      (ready-road ?v ?next)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-road_0649-road_0374-tw_00030
    :parameters (?v - vehicle ?r - road_type_road_0649 ?next - road_type_road_0374 ?w - window_type_tw_00030)
    :precondition (and
      (ready-road ?v ?r)
      (road-next ?r ?next)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
    )
    :effect (and
      (not (ready-road ?v ?r))
      (ready-road ?v ?next)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-road_0649-road_0374-tw_00060
    :parameters (?v - vehicle ?r - road_type_road_0649 ?next - road_type_road_0374 ?w - window_type_tw_00060)
    :precondition (and
      (ready-road ?v ?r)
      (road-next ?r ?next)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
    )
    :effect (and
      (not (ready-road ?v ?r))
      (ready-road ?v ?next)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-road_0649-road_0374-tw_00090
    :parameters (?v - vehicle ?r - road_type_road_0649 ?next - road_type_road_0374 ?w - window_type_tw_00090)
    :precondition (and
      (ready-road ?v ?r)
      (road-next ?r ?next)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
    )
    :effect (and
      (not (ready-road ?v ?r))
      (ready-road ?v ?next)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-road_0649-road_0374-tw_00120
    :parameters (?v - vehicle ?r - road_type_road_0649 ?next - road_type_road_0374 ?w - window_type_tw_00120)
    :precondition (and
      (ready-road ?v ?r)
      (road-next ?r ?next)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
    )
    :effect (and
      (not (ready-road ?v ?r))
      (ready-road ?v ?next)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-road_0649-road_0374-tw_00150
    :parameters (?v - vehicle ?r - road_type_road_0649 ?next - road_type_road_0374 ?w - window_type_tw_00150)
    :precondition (and
      (ready-road ?v ?r)
      (road-next ?r ?next)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
    )
    :effect (and
      (not (ready-road ?v ?r))
      (ready-road ?v ?next)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-road_0649-road_0374-tw_00180
    :parameters (?v - vehicle ?r - road_type_road_0649 ?next - road_type_road_0374 ?w - window_type_tw_00180)
    :precondition (and
      (ready-road ?v ?r)
      (road-next ?r ?next)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
    )
    :effect (and
      (not (ready-road ?v ?r))
      (ready-road ?v ?next)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-road_0649-road_0374-tw_00210
    :parameters (?v - vehicle ?r - road_type_road_0649 ?next - road_type_road_0374 ?w - window_type_tw_00210)
    :precondition (and
      (ready-road ?v ?r)
      (road-next ?r ?next)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
    )
    :effect (and
      (not (ready-road ?v ?r))
      (ready-road ?v ?next)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-road_0649-road_0374-tw_00240
    :parameters (?v - vehicle ?r - road_type_road_0649 ?next - road_type_road_0374 ?w - window_type_tw_00240)
    :precondition (and
      (ready-road ?v ?r)
      (road-next ?r ?next)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
    )
    :effect (and
      (not (ready-road ?v ?r))
      (ready-road ?v ?next)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-road_0649-road_0374-tw_00270
    :parameters (?v - vehicle ?r - road_type_road_0649 ?next - road_type_road_0374 ?w - window_type_tw_00270)
    :precondition (and
      (ready-road ?v ?r)
      (road-next ?r ?next)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
    )
    :effect (and
      (not (ready-road ?v ?r))
      (ready-road ?v ?next)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-road_0649-road_0374-tw_00300
    :parameters (?v - vehicle ?r - road_type_road_0649 ?next - road_type_road_0374 ?w - window_type_tw_00300)
    :precondition (and
      (ready-road ?v ?r)
      (road-next ?r ?next)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
    )
    :effect (and
      (not (ready-road ?v ?r))
      (ready-road ?v ?next)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-road_0649-road_0374-tw_00330
    :parameters (?v - vehicle ?r - road_type_road_0649 ?next - road_type_road_0374 ?w - window_type_tw_00330)
    :precondition (and
      (ready-road ?v ?r)
      (road-next ?r ?next)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
    )
    :effect (and
      (not (ready-road ?v ?r))
      (ready-road ?v ?next)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-road_0649-road_0374-tw_00360
    :parameters (?v - vehicle ?r - road_type_road_0649 ?next - road_type_road_0374 ?w - window_type_tw_00360)
    :precondition (and
      (ready-road ?v ?r)
      (road-next ?r ?next)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
    )
    :effect (and
      (not (ready-road ?v ?r))
      (ready-road ?v ?next)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-road_0649-road_0374-tw_00390
    :parameters (?v - vehicle ?r - road_type_road_0649 ?next - road_type_road_0374 ?w - window_type_tw_00390)
    :precondition (and
      (ready-road ?v ?r)
      (road-next ?r ?next)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
    )
    :effect (and
      (not (ready-road ?v ?r))
      (ready-road ?v ?next)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-road_0649-road_0374-tw_00420
    :parameters (?v - vehicle ?r - road_type_road_0649 ?next - road_type_road_0374 ?w - window_type_tw_00420)
    :precondition (and
      (ready-road ?v ?r)
      (road-next ?r ?next)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
    )
    :effect (and
      (not (ready-road ?v ?r))
      (ready-road ?v ?next)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-road_0649-road_0374-tw_00450
    :parameters (?v - vehicle ?r - road_type_road_0649 ?next - road_type_road_0374 ?w - window_type_tw_00450)
    :precondition (and
      (ready-road ?v ?r)
      (road-next ?r ?next)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
    )
    :effect (and
      (not (ready-road ?v ?r))
      (ready-road ?v ?next)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-road_0649-road_0374-tw_00480
    :parameters (?v - vehicle ?r - road_type_road_0649 ?next - road_type_road_0374 ?w - window_type_tw_00480)
    :precondition (and
      (ready-road ?v ?r)
      (road-next ?r ?next)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
    )
    :effect (and
      (not (ready-road ?v ?r))
      (ready-road ?v ?next)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-road_0649-road_0374-tw_00510
    :parameters (?v - vehicle ?r - road_type_road_0649 ?next - road_type_road_0374 ?w - window_type_tw_00510)
    :precondition (and
      (ready-road ?v ?r)
      (road-next ?r ?next)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
    )
    :effect (and
      (not (ready-road ?v ?r))
      (ready-road ?v ?next)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-road_0649-road_0374-tw_00540
    :parameters (?v - vehicle ?r - road_type_road_0649 ?next - road_type_road_0374 ?w - window_type_tw_00540)
    :precondition (and
      (ready-road ?v ?r)
      (road-next ?r ?next)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
    )
    :effect (and
      (not (ready-road ?v ?r))
      (ready-road ?v ?next)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-road_0649-road_0374-tw_00570
    :parameters (?v - vehicle ?r - road_type_road_0649 ?next - road_type_road_0374 ?w - window_type_tw_00570)
    :precondition (and
      (ready-road ?v ?r)
      (road-next ?r ?next)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
    )
    :effect (and
      (not (ready-road ?v ?r))
      (ready-road ?v ?next)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-road_0649-road_0374-tw_00600
    :parameters (?v - vehicle ?r - road_type_road_0649 ?next - road_type_road_0374 ?w - window_type_tw_00600)
    :precondition (and
      (ready-road ?v ?r)
      (road-next ?r ?next)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
    )
    :effect (and
      (not (ready-road ?v ?r))
      (ready-road ?v ?next)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-static-road_0650-road_0097
    :parameters (?v - vehicle ?r - road_type_road_0650 ?next - road_type_road_0097)
    :precondition (and
      (ready-road ?v ?r)
      (road-next ?r ?next)
      (road-open ?r)
      (static-road ?r)
      (not (road-blocked ?r))
    )
    :effect (and
      (not (ready-road ?v ?r))
      (ready-road ?v ?next)
      (increase (travel-time ?v) (travel-duration ?r))
      (increase (sim-time) (travel-duration ?r))
    )
  )

  (:action traverse-road-static-road_0651-road_0152
    :parameters (?v - vehicle ?r - road_type_road_0651 ?next - road_type_road_0152)
    :precondition (and
      (ready-road ?v ?r)
      (road-next ?r ?next)
      (road-open ?r)
      (static-road ?r)
      (not (road-blocked ?r))
    )
    :effect (and
      (not (ready-road ?v ?r))
      (ready-road ?v ?next)
      (increase (travel-time ?v) (travel-duration ?r))
      (increase (sim-time) (travel-duration ?r))
    )
  )

  (:action traverse-road-static-road_0651-road_0153
    :parameters (?v - vehicle ?r - road_type_road_0651 ?next - road_type_road_0153)
    :precondition (and
      (ready-road ?v ?r)
      (road-next ?r ?next)
      (road-open ?r)
      (static-road ?r)
      (not (road-blocked ?r))
    )
    :effect (and
      (not (ready-road ?v ?r))
      (ready-road ?v ?next)
      (increase (travel-time ?v) (travel-duration ?r))
      (increase (sim-time) (travel-duration ?r))
    )
  )

  (:action traverse-road-static-road_0651-road_0154
    :parameters (?v - vehicle ?r - road_type_road_0651 ?next - road_type_road_0154)
    :precondition (and
      (ready-road ?v ?r)
      (road-next ?r ?next)
      (road-open ?r)
      (static-road ?r)
      (not (road-blocked ?r))
    )
    :effect (and
      (not (ready-road ?v ?r))
      (ready-road ?v ?next)
      (increase (travel-time ?v) (travel-duration ?r))
      (increase (sim-time) (travel-duration ?r))
    )
  )

  (:action traverse-road-static-road_0651-road_0155
    :parameters (?v - vehicle ?r - road_type_road_0651 ?next - road_type_road_0155)
    :precondition (and
      (ready-road ?v ?r)
      (road-next ?r ?next)
      (road-open ?r)
      (static-road ?r)
      (not (road-blocked ?r))
    )
    :effect (and
      (not (ready-road ?v ?r))
      (ready-road ?v ?next)
      (increase (travel-time ?v) (travel-duration ?r))
      (increase (sim-time) (travel-duration ?r))
    )
  )

  (:action traverse-road-static-road_0652-road_0139
    :parameters (?v - vehicle ?r - road_type_road_0652 ?next - road_type_road_0139)
    :precondition (and
      (ready-road ?v ?r)
      (road-next ?r ?next)
      (road-open ?r)
      (static-road ?r)
      (not (road-blocked ?r))
    )
    :effect (and
      (not (ready-road ?v ?r))
      (ready-road ?v ?next)
      (increase (travel-time ?v) (travel-duration ?r))
      (increase (sim-time) (travel-duration ?r))
    )
  )

  (:action traverse-road-static-road_0652-road_0140
    :parameters (?v - vehicle ?r - road_type_road_0652 ?next - road_type_road_0140)
    :precondition (and
      (ready-road ?v ?r)
      (road-next ?r ?next)
      (road-open ?r)
      (static-road ?r)
      (not (road-blocked ?r))
    )
    :effect (and
      (not (ready-road ?v ?r))
      (ready-road ?v ?next)
      (increase (travel-time ?v) (travel-duration ?r))
      (increase (sim-time) (travel-duration ?r))
    )
  )

  (:action traverse-road-static-road_0652-road_0141
    :parameters (?v - vehicle ?r - road_type_road_0652 ?next - road_type_road_0141)
    :precondition (and
      (ready-road ?v ?r)
      (road-next ?r ?next)
      (road-open ?r)
      (static-road ?r)
      (not (road-blocked ?r))
    )
    :effect (and
      (not (ready-road ?v ?r))
      (ready-road ?v ?next)
      (increase (travel-time ?v) (travel-duration ?r))
      (increase (sim-time) (travel-duration ?r))
    )
  )

  (:action traverse-road-static-road_0653-road_0673
    :parameters (?v - vehicle ?r - road_type_road_0653 ?next - road_type_road_0673)
    :precondition (and
      (ready-road ?v ?r)
      (road-next ?r ?next)
      (road-open ?r)
      (static-road ?r)
      (not (road-blocked ?r))
    )
    :effect (and
      (not (ready-road ?v ?r))
      (ready-road ?v ?next)
      (increase (travel-time ?v) (travel-duration ?r))
      (increase (sim-time) (travel-duration ?r))
    )
  )

  (:action traverse-road-static-road_0653-road_0674
    :parameters (?v - vehicle ?r - road_type_road_0653 ?next - road_type_road_0674)
    :precondition (and
      (ready-road ?v ?r)
      (road-next ?r ?next)
      (road-open ?r)
      (static-road ?r)
      (not (road-blocked ?r))
    )
    :effect (and
      (not (ready-road ?v ?r))
      (ready-road ?v ?next)
      (increase (travel-time ?v) (travel-duration ?r))
      (increase (sim-time) (travel-duration ?r))
    )
  )

  (:action traverse-road-static-road_0654-road_0150
    :parameters (?v - vehicle ?r - road_type_road_0654 ?next - road_type_road_0150)
    :precondition (and
      (ready-road ?v ?r)
      (road-next ?r ?next)
      (road-open ?r)
      (static-road ?r)
      (not (road-blocked ?r))
    )
    :effect (and
      (not (ready-road ?v ?r))
      (ready-road ?v ?next)
      (increase (travel-time ?v) (travel-duration ?r))
      (increase (sim-time) (travel-duration ?r))
    )
  )

  (:action traverse-road-static-road_0654-road_0151
    :parameters (?v - vehicle ?r - road_type_road_0654 ?next - road_type_road_0151)
    :precondition (and
      (ready-road ?v ?r)
      (road-next ?r ?next)
      (road-open ?r)
      (static-road ?r)
      (not (road-blocked ?r))
    )
    :effect (and
      (not (ready-road ?v ?r))
      (ready-road ?v ?next)
      (increase (travel-time ?v) (travel-duration ?r))
      (increase (sim-time) (travel-duration ?r))
    )
  )

  (:action traverse-road-static-road_0655-road_0642
    :parameters (?v - vehicle ?r - road_type_road_0655 ?next - road_type_road_0642)
    :precondition (and
      (ready-road ?v ?r)
      (road-next ?r ?next)
      (road-open ?r)
      (static-road ?r)
      (not (road-blocked ?r))
    )
    :effect (and
      (not (ready-road ?v ?r))
      (ready-road ?v ?next)
      (increase (travel-time ?v) (travel-duration ?r))
      (increase (sim-time) (travel-duration ?r))
    )
  )

  (:action traverse-road-static-road_0656-road_0254
    :parameters (?v - vehicle ?r - road_type_road_0656 ?next - road_type_road_0254)
    :precondition (and
      (ready-road ?v ?r)
      (road-next ?r ?next)
      (road-open ?r)
      (static-road ?r)
      (not (road-blocked ?r))
    )
    :effect (and
      (not (ready-road ?v ?r))
      (ready-road ?v ?next)
      (increase (travel-time ?v) (travel-duration ?r))
      (increase (sim-time) (travel-duration ?r))
    )
  )

  (:action traverse-road-static-road_0657-road_0760
    :parameters (?v - vehicle ?r - road_type_road_0657 ?next - road_type_road_0760)
    :precondition (and
      (ready-road ?v ?r)
      (road-next ?r ?next)
      (road-open ?r)
      (static-road ?r)
      (not (road-blocked ?r))
    )
    :effect (and
      (not (ready-road ?v ?r))
      (ready-road ?v ?next)
      (increase (travel-time ?v) (travel-duration ?r))
      (increase (sim-time) (travel-duration ?r))
    )
  )

  (:action traverse-road-static-road_0657-road_0761
    :parameters (?v - vehicle ?r - road_type_road_0657 ?next - road_type_road_0761)
    :precondition (and
      (ready-road ?v ?r)
      (road-next ?r ?next)
      (road-open ?r)
      (static-road ?r)
      (not (road-blocked ?r))
    )
    :effect (and
      (not (ready-road ?v ?r))
      (ready-road ?v ?next)
      (increase (travel-time ?v) (travel-duration ?r))
      (increase (sim-time) (travel-duration ?r))
    )
  )

  (:action traverse-road-static-road_0658-road_0622
    :parameters (?v - vehicle ?r - road_type_road_0658 ?next - road_type_road_0622)
    :precondition (and
      (ready-road ?v ?r)
      (road-next ?r ?next)
      (road-open ?r)
      (static-road ?r)
      (not (road-blocked ?r))
    )
    :effect (and
      (not (ready-road ?v ?r))
      (ready-road ?v ?next)
      (increase (travel-time ?v) (travel-duration ?r))
      (increase (sim-time) (travel-duration ?r))
    )
  )

  (:action traverse-road-static-road_0658-road_0623
    :parameters (?v - vehicle ?r - road_type_road_0658 ?next - road_type_road_0623)
    :precondition (and
      (ready-road ?v ?r)
      (road-next ?r ?next)
      (road-open ?r)
      (static-road ?r)
      (not (road-blocked ?r))
    )
    :effect (and
      (not (ready-road ?v ?r))
      (ready-road ?v ?next)
      (increase (travel-time ?v) (travel-duration ?r))
      (increase (sim-time) (travel-duration ?r))
    )
  )

  (:action traverse-road-static-road_0659-road_0123
    :parameters (?v - vehicle ?r - road_type_road_0659 ?next - road_type_road_0123)
    :precondition (and
      (ready-road ?v ?r)
      (road-next ?r ?next)
      (road-open ?r)
      (static-road ?r)
      (not (road-blocked ?r))
    )
    :effect (and
      (not (ready-road ?v ?r))
      (ready-road ?v ?next)
      (increase (travel-time ?v) (travel-duration ?r))
      (increase (sim-time) (travel-duration ?r))
    )
  )

  (:action traverse-road-static-road_0659-road_0124
    :parameters (?v - vehicle ?r - road_type_road_0659 ?next - road_type_road_0124)
    :precondition (and
      (ready-road ?v ?r)
      (road-next ?r ?next)
      (road-open ?r)
      (static-road ?r)
      (not (road-blocked ?r))
    )
    :effect (and
      (not (ready-road ?v ?r))
      (ready-road ?v ?next)
      (increase (travel-time ?v) (travel-duration ?r))
      (increase (sim-time) (travel-duration ?r))
    )
  )

  (:action traverse-road-static-road_0659-road_0125
    :parameters (?v - vehicle ?r - road_type_road_0659 ?next - road_type_road_0125)
    :precondition (and
      (ready-road ?v ?r)
      (road-next ?r ?next)
      (road-open ?r)
      (static-road ?r)
      (not (road-blocked ?r))
    )
    :effect (and
      (not (ready-road ?v ?r))
      (ready-road ?v ?next)
      (increase (travel-time ?v) (travel-duration ?r))
      (increase (sim-time) (travel-duration ?r))
    )
  )

  (:action traverse-road-static-road_0659-road_0126
    :parameters (?v - vehicle ?r - road_type_road_0659 ?next - road_type_road_0126)
    :precondition (and
      (ready-road ?v ?r)
      (road-next ?r ?next)
      (road-open ?r)
      (static-road ?r)
      (not (road-blocked ?r))
    )
    :effect (and
      (not (ready-road ?v ?r))
      (ready-road ?v ?next)
      (increase (travel-time ?v) (travel-duration ?r))
      (increase (sim-time) (travel-duration ?r))
    )
  )

  (:action traverse-road-static-road_0661-road_0568
    :parameters (?v - vehicle ?r - road_type_road_0661 ?next - road_type_road_0568)
    :precondition (and
      (ready-road ?v ?r)
      (road-next ?r ?next)
      (road-open ?r)
      (static-road ?r)
      (not (road-blocked ?r))
    )
    :effect (and
      (not (ready-road ?v ?r))
      (ready-road ?v ?next)
      (increase (travel-time ?v) (travel-duration ?r))
      (increase (sim-time) (travel-duration ?r))
    )
  )

  (:action traverse-road-static-road_0661-road_0569
    :parameters (?v - vehicle ?r - road_type_road_0661 ?next - road_type_road_0569)
    :precondition (and
      (ready-road ?v ?r)
      (road-next ?r ?next)
      (road-open ?r)
      (static-road ?r)
      (not (road-blocked ?r))
    )
    :effect (and
      (not (ready-road ?v ?r))
      (ready-road ?v ?next)
      (increase (travel-time ?v) (travel-duration ?r))
      (increase (sim-time) (travel-duration ?r))
    )
  )

  (:action traverse-road-static-road_0662-road_0650
    :parameters (?v - vehicle ?r - road_type_road_0662 ?next - road_type_road_0650)
    :precondition (and
      (ready-road ?v ?r)
      (road-next ?r ?next)
      (road-open ?r)
      (static-road ?r)
      (not (road-blocked ?r))
    )
    :effect (and
      (not (ready-road ?v ?r))
      (ready-road ?v ?next)
      (increase (travel-time ?v) (travel-duration ?r))
      (increase (sim-time) (travel-duration ?r))
    )
  )

  (:action traverse-road-dynamic-road_0664-road_0621-tw_00000
    :parameters (?v - vehicle ?r - road_type_road_0664 ?next - road_type_road_0621 ?w - window_type_tw_00000)
    :precondition (and
      (ready-road ?v ?r)
      (road-next ?r ?next)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
    )
    :effect (and
      (not (ready-road ?v ?r))
      (ready-road ?v ?next)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-road_0664-road_0621-tw_00030
    :parameters (?v - vehicle ?r - road_type_road_0664 ?next - road_type_road_0621 ?w - window_type_tw_00030)
    :precondition (and
      (ready-road ?v ?r)
      (road-next ?r ?next)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
    )
    :effect (and
      (not (ready-road ?v ?r))
      (ready-road ?v ?next)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-road_0664-road_0621-tw_00060
    :parameters (?v - vehicle ?r - road_type_road_0664 ?next - road_type_road_0621 ?w - window_type_tw_00060)
    :precondition (and
      (ready-road ?v ?r)
      (road-next ?r ?next)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
    )
    :effect (and
      (not (ready-road ?v ?r))
      (ready-road ?v ?next)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-road_0664-road_0621-tw_00090
    :parameters (?v - vehicle ?r - road_type_road_0664 ?next - road_type_road_0621 ?w - window_type_tw_00090)
    :precondition (and
      (ready-road ?v ?r)
      (road-next ?r ?next)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
    )
    :effect (and
      (not (ready-road ?v ?r))
      (ready-road ?v ?next)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-road_0664-road_0621-tw_00120
    :parameters (?v - vehicle ?r - road_type_road_0664 ?next - road_type_road_0621 ?w - window_type_tw_00120)
    :precondition (and
      (ready-road ?v ?r)
      (road-next ?r ?next)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
    )
    :effect (and
      (not (ready-road ?v ?r))
      (ready-road ?v ?next)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-road_0664-road_0621-tw_00150
    :parameters (?v - vehicle ?r - road_type_road_0664 ?next - road_type_road_0621 ?w - window_type_tw_00150)
    :precondition (and
      (ready-road ?v ?r)
      (road-next ?r ?next)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
    )
    :effect (and
      (not (ready-road ?v ?r))
      (ready-road ?v ?next)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-road_0664-road_0621-tw_00180
    :parameters (?v - vehicle ?r - road_type_road_0664 ?next - road_type_road_0621 ?w - window_type_tw_00180)
    :precondition (and
      (ready-road ?v ?r)
      (road-next ?r ?next)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
    )
    :effect (and
      (not (ready-road ?v ?r))
      (ready-road ?v ?next)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-road_0664-road_0621-tw_00210
    :parameters (?v - vehicle ?r - road_type_road_0664 ?next - road_type_road_0621 ?w - window_type_tw_00210)
    :precondition (and
      (ready-road ?v ?r)
      (road-next ?r ?next)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
    )
    :effect (and
      (not (ready-road ?v ?r))
      (ready-road ?v ?next)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-road_0664-road_0621-tw_00240
    :parameters (?v - vehicle ?r - road_type_road_0664 ?next - road_type_road_0621 ?w - window_type_tw_00240)
    :precondition (and
      (ready-road ?v ?r)
      (road-next ?r ?next)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
    )
    :effect (and
      (not (ready-road ?v ?r))
      (ready-road ?v ?next)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-road_0664-road_0621-tw_00270
    :parameters (?v - vehicle ?r - road_type_road_0664 ?next - road_type_road_0621 ?w - window_type_tw_00270)
    :precondition (and
      (ready-road ?v ?r)
      (road-next ?r ?next)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
    )
    :effect (and
      (not (ready-road ?v ?r))
      (ready-road ?v ?next)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-road_0664-road_0621-tw_00300
    :parameters (?v - vehicle ?r - road_type_road_0664 ?next - road_type_road_0621 ?w - window_type_tw_00300)
    :precondition (and
      (ready-road ?v ?r)
      (road-next ?r ?next)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
    )
    :effect (and
      (not (ready-road ?v ?r))
      (ready-road ?v ?next)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-road_0664-road_0621-tw_00330
    :parameters (?v - vehicle ?r - road_type_road_0664 ?next - road_type_road_0621 ?w - window_type_tw_00330)
    :precondition (and
      (ready-road ?v ?r)
      (road-next ?r ?next)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
    )
    :effect (and
      (not (ready-road ?v ?r))
      (ready-road ?v ?next)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-road_0664-road_0621-tw_00360
    :parameters (?v - vehicle ?r - road_type_road_0664 ?next - road_type_road_0621 ?w - window_type_tw_00360)
    :precondition (and
      (ready-road ?v ?r)
      (road-next ?r ?next)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
    )
    :effect (and
      (not (ready-road ?v ?r))
      (ready-road ?v ?next)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-road_0664-road_0621-tw_00390
    :parameters (?v - vehicle ?r - road_type_road_0664 ?next - road_type_road_0621 ?w - window_type_tw_00390)
    :precondition (and
      (ready-road ?v ?r)
      (road-next ?r ?next)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
    )
    :effect (and
      (not (ready-road ?v ?r))
      (ready-road ?v ?next)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-road_0664-road_0621-tw_00420
    :parameters (?v - vehicle ?r - road_type_road_0664 ?next - road_type_road_0621 ?w - window_type_tw_00420)
    :precondition (and
      (ready-road ?v ?r)
      (road-next ?r ?next)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
    )
    :effect (and
      (not (ready-road ?v ?r))
      (ready-road ?v ?next)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-road_0664-road_0621-tw_00450
    :parameters (?v - vehicle ?r - road_type_road_0664 ?next - road_type_road_0621 ?w - window_type_tw_00450)
    :precondition (and
      (ready-road ?v ?r)
      (road-next ?r ?next)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
    )
    :effect (and
      (not (ready-road ?v ?r))
      (ready-road ?v ?next)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-road_0664-road_0621-tw_00480
    :parameters (?v - vehicle ?r - road_type_road_0664 ?next - road_type_road_0621 ?w - window_type_tw_00480)
    :precondition (and
      (ready-road ?v ?r)
      (road-next ?r ?next)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
    )
    :effect (and
      (not (ready-road ?v ?r))
      (ready-road ?v ?next)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-road_0664-road_0621-tw_00510
    :parameters (?v - vehicle ?r - road_type_road_0664 ?next - road_type_road_0621 ?w - window_type_tw_00510)
    :precondition (and
      (ready-road ?v ?r)
      (road-next ?r ?next)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
    )
    :effect (and
      (not (ready-road ?v ?r))
      (ready-road ?v ?next)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-road_0664-road_0621-tw_00540
    :parameters (?v - vehicle ?r - road_type_road_0664 ?next - road_type_road_0621 ?w - window_type_tw_00540)
    :precondition (and
      (ready-road ?v ?r)
      (road-next ?r ?next)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
    )
    :effect (and
      (not (ready-road ?v ?r))
      (ready-road ?v ?next)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-road_0664-road_0621-tw_00570
    :parameters (?v - vehicle ?r - road_type_road_0664 ?next - road_type_road_0621 ?w - window_type_tw_00570)
    :precondition (and
      (ready-road ?v ?r)
      (road-next ?r ?next)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
    )
    :effect (and
      (not (ready-road ?v ?r))
      (ready-road ?v ?next)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-road_0664-road_0621-tw_00600
    :parameters (?v - vehicle ?r - road_type_road_0664 ?next - road_type_road_0621 ?w - window_type_tw_00600)
    :precondition (and
      (ready-road ?v ?r)
      (road-next ?r ?next)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
    )
    :effect (and
      (not (ready-road ?v ?r))
      (ready-road ?v ?next)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-static-road_0665-road_0819
    :parameters (?v - vehicle ?r - road_type_road_0665 ?next - road_type_road_0819)
    :precondition (and
      (ready-road ?v ?r)
      (road-next ?r ?next)
      (road-open ?r)
      (static-road ?r)
      (not (road-blocked ?r))
    )
    :effect (and
      (not (ready-road ?v ?r))
      (ready-road ?v ?next)
      (increase (travel-time ?v) (travel-duration ?r))
      (increase (sim-time) (travel-duration ?r))
    )
  )

  (:action traverse-road-static-road_0665-road_0820
    :parameters (?v - vehicle ?r - road_type_road_0665 ?next - road_type_road_0820)
    :precondition (and
      (ready-road ?v ?r)
      (road-next ?r ?next)
      (road-open ?r)
      (static-road ?r)
      (not (road-blocked ?r))
    )
    :effect (and
      (not (ready-road ?v ?r))
      (ready-road ?v ?next)
      (increase (travel-time ?v) (travel-duration ?r))
      (increase (sim-time) (travel-duration ?r))
    )
  )

  (:action traverse-road-static-road_0666-road_0670
    :parameters (?v - vehicle ?r - road_type_road_0666 ?next - road_type_road_0670)
    :precondition (and
      (ready-road ?v ?r)
      (road-next ?r ?next)
      (road-open ?r)
      (static-road ?r)
      (not (road-blocked ?r))
    )
    :effect (and
      (not (ready-road ?v ?r))
      (ready-road ?v ?next)
      (increase (travel-time ?v) (travel-duration ?r))
      (increase (sim-time) (travel-duration ?r))
    )
  )

  (:action traverse-road-static-road_0666-road_0671
    :parameters (?v - vehicle ?r - road_type_road_0666 ?next - road_type_road_0671)
    :precondition (and
      (ready-road ?v ?r)
      (road-next ?r ?next)
      (road-open ?r)
      (static-road ?r)
      (not (road-blocked ?r))
    )
    :effect (and
      (not (ready-road ?v ?r))
      (ready-road ?v ?next)
      (increase (travel-time ?v) (travel-duration ?r))
      (increase (sim-time) (travel-duration ?r))
    )
  )

  (:action traverse-road-static-road_0666-road_0672
    :parameters (?v - vehicle ?r - road_type_road_0666 ?next - road_type_road_0672)
    :precondition (and
      (ready-road ?v ?r)
      (road-next ?r ?next)
      (road-open ?r)
      (static-road ?r)
      (not (road-blocked ?r))
    )
    :effect (and
      (not (ready-road ?v ?r))
      (ready-road ?v ?next)
      (increase (travel-time ?v) (travel-duration ?r))
      (increase (sim-time) (travel-duration ?r))
    )
  )

  (:action traverse-road-static-road_0667-road_0682
    :parameters (?v - vehicle ?r - road_type_road_0667 ?next - road_type_road_0682)
    :precondition (and
      (ready-road ?v ?r)
      (road-next ?r ?next)
      (road-open ?r)
      (static-road ?r)
      (not (road-blocked ?r))
    )
    :effect (and
      (not (ready-road ?v ?r))
      (ready-road ?v ?next)
      (increase (travel-time ?v) (travel-duration ?r))
      (increase (sim-time) (travel-duration ?r))
    )
  )

  (:action traverse-road-static-road_0668-road_0139
    :parameters (?v - vehicle ?r - road_type_road_0668 ?next - road_type_road_0139)
    :precondition (and
      (ready-road ?v ?r)
      (road-next ?r ?next)
      (road-open ?r)
      (static-road ?r)
      (not (road-blocked ?r))
    )
    :effect (and
      (not (ready-road ?v ?r))
      (ready-road ?v ?next)
      (increase (travel-time ?v) (travel-duration ?r))
      (increase (sim-time) (travel-duration ?r))
    )
  )

  (:action traverse-road-static-road_0668-road_0140
    :parameters (?v - vehicle ?r - road_type_road_0668 ?next - road_type_road_0140)
    :precondition (and
      (ready-road ?v ?r)
      (road-next ?r ?next)
      (road-open ?r)
      (static-road ?r)
      (not (road-blocked ?r))
    )
    :effect (and
      (not (ready-road ?v ?r))
      (ready-road ?v ?next)
      (increase (travel-time ?v) (travel-duration ?r))
      (increase (sim-time) (travel-duration ?r))
    )
  )

  (:action traverse-road-static-road_0668-road_0141
    :parameters (?v - vehicle ?r - road_type_road_0668 ?next - road_type_road_0141)
    :precondition (and
      (ready-road ?v ?r)
      (road-next ?r ?next)
      (road-open ?r)
      (static-road ?r)
      (not (road-blocked ?r))
    )
    :effect (and
      (not (ready-road ?v ?r))
      (ready-road ?v ?next)
      (increase (travel-time ?v) (travel-duration ?r))
      (increase (sim-time) (travel-duration ?r))
    )
  )

  (:action traverse-road-static-road_0669-road_0098
    :parameters (?v - vehicle ?r - road_type_road_0669 ?next - road_type_road_0098)
    :precondition (and
      (ready-road ?v ?r)
      (road-next ?r ?next)
      (road-open ?r)
      (static-road ?r)
      (not (road-blocked ?r))
    )
    :effect (and
      (not (ready-road ?v ?r))
      (ready-road ?v ?next)
      (increase (travel-time ?v) (travel-duration ?r))
      (increase (sim-time) (travel-duration ?r))
    )
  )

  (:action traverse-road-static-road_0669-road_0099
    :parameters (?v - vehicle ?r - road_type_road_0669 ?next - road_type_road_0099)
    :precondition (and
      (ready-road ?v ?r)
      (road-next ?r ?next)
      (road-open ?r)
      (static-road ?r)
      (not (road-blocked ?r))
    )
    :effect (and
      (not (ready-road ?v ?r))
      (ready-road ?v ?next)
      (increase (travel-time ?v) (travel-duration ?r))
      (increase (sim-time) (travel-duration ?r))
    )
  )

  (:action traverse-road-static-road_0670-road_0499
    :parameters (?v - vehicle ?r - road_type_road_0670 ?next - road_type_road_0499)
    :precondition (and
      (ready-road ?v ?r)
      (road-next ?r ?next)
      (road-open ?r)
      (static-road ?r)
      (not (road-blocked ?r))
    )
    :effect (and
      (not (ready-road ?v ?r))
      (ready-road ?v ?next)
      (increase (travel-time ?v) (travel-duration ?r))
      (increase (sim-time) (travel-duration ?r))
    )
  )

  (:action traverse-road-static-road_0670-road_0500
    :parameters (?v - vehicle ?r - road_type_road_0670 ?next - road_type_road_0500)
    :precondition (and
      (ready-road ?v ?r)
      (road-next ?r ?next)
      (road-open ?r)
      (static-road ?r)
      (not (road-blocked ?r))
    )
    :effect (and
      (not (ready-road ?v ?r))
      (ready-road ?v ?next)
      (increase (travel-time ?v) (travel-duration ?r))
      (increase (sim-time) (travel-duration ?r))
    )
  )

  (:action traverse-road-static-road_0670-road_0501
    :parameters (?v - vehicle ?r - road_type_road_0670 ?next - road_type_road_0501)
    :precondition (and
      (ready-road ?v ?r)
      (road-next ?r ?next)
      (road-open ?r)
      (static-road ?r)
      (not (road-blocked ?r))
    )
    :effect (and
      (not (ready-road ?v ?r))
      (ready-road ?v ?next)
      (increase (travel-time ?v) (travel-duration ?r))
      (increase (sim-time) (travel-duration ?r))
    )
  )

  (:action traverse-road-static-road_0671-road_0665
    :parameters (?v - vehicle ?r - road_type_road_0671 ?next - road_type_road_0665)
    :precondition (and
      (ready-road ?v ?r)
      (road-next ?r ?next)
      (road-open ?r)
      (static-road ?r)
      (not (road-blocked ?r))
    )
    :effect (and
      (not (ready-road ?v ?r))
      (ready-road ?v ?next)
      (increase (travel-time ?v) (travel-duration ?r))
      (increase (sim-time) (travel-duration ?r))
    )
  )

  (:action traverse-road-static-road_0671-road_0666
    :parameters (?v - vehicle ?r - road_type_road_0671 ?next - road_type_road_0666)
    :precondition (and
      (ready-road ?v ?r)
      (road-next ?r ?next)
      (road-open ?r)
      (static-road ?r)
      (not (road-blocked ?r))
    )
    :effect (and
      (not (ready-road ?v ?r))
      (ready-road ?v ?next)
      (increase (travel-time ?v) (travel-duration ?r))
      (increase (sim-time) (travel-duration ?r))
    )
  )

  (:action traverse-road-static-road_0672-road_0628
    :parameters (?v - vehicle ?r - road_type_road_0672 ?next - road_type_road_0628)
    :precondition (and
      (ready-road ?v ?r)
      (road-next ?r ?next)
      (road-open ?r)
      (static-road ?r)
      (not (road-blocked ?r))
    )
    :effect (and
      (not (ready-road ?v ?r))
      (ready-road ?v ?next)
      (increase (travel-time ?v) (travel-duration ?r))
      (increase (sim-time) (travel-duration ?r))
    )
  )

  (:action traverse-road-static-road_0672-road_0629
    :parameters (?v - vehicle ?r - road_type_road_0672 ?next - road_type_road_0629)
    :precondition (and
      (ready-road ?v ?r)
      (road-next ?r ?next)
      (road-open ?r)
      (static-road ?r)
      (not (road-blocked ?r))
    )
    :effect (and
      (not (ready-road ?v ?r))
      (ready-road ?v ?next)
      (increase (travel-time ?v) (travel-duration ?r))
      (increase (sim-time) (travel-duration ?r))
    )
  )

  (:action traverse-road-static-road_0673-road_0318
    :parameters (?v - vehicle ?r - road_type_road_0673 ?next - road_type_road_0318)
    :precondition (and
      (ready-road ?v ?r)
      (road-next ?r ?next)
      (road-open ?r)
      (static-road ?r)
      (not (road-blocked ?r))
    )
    :effect (and
      (not (ready-road ?v ?r))
      (ready-road ?v ?next)
      (increase (travel-time ?v) (travel-duration ?r))
      (increase (sim-time) (travel-duration ?r))
    )
  )

  (:action traverse-road-static-road_0673-road_0319
    :parameters (?v - vehicle ?r - road_type_road_0673 ?next - road_type_road_0319)
    :precondition (and
      (ready-road ?v ?r)
      (road-next ?r ?next)
      (road-open ?r)
      (static-road ?r)
      (not (road-blocked ?r))
    )
    :effect (and
      (not (ready-road ?v ?r))
      (ready-road ?v ?next)
      (increase (travel-time ?v) (travel-duration ?r))
      (increase (sim-time) (travel-duration ?r))
    )
  )

  (:action traverse-road-static-road_0674-road_0152
    :parameters (?v - vehicle ?r - road_type_road_0674 ?next - road_type_road_0152)
    :precondition (and
      (ready-road ?v ?r)
      (road-next ?r ?next)
      (road-open ?r)
      (static-road ?r)
      (not (road-blocked ?r))
    )
    :effect (and
      (not (ready-road ?v ?r))
      (ready-road ?v ?next)
      (increase (travel-time ?v) (travel-duration ?r))
      (increase (sim-time) (travel-duration ?r))
    )
  )

  (:action traverse-road-static-road_0674-road_0153
    :parameters (?v - vehicle ?r - road_type_road_0674 ?next - road_type_road_0153)
    :precondition (and
      (ready-road ?v ?r)
      (road-next ?r ?next)
      (road-open ?r)
      (static-road ?r)
      (not (road-blocked ?r))
    )
    :effect (and
      (not (ready-road ?v ?r))
      (ready-road ?v ?next)
      (increase (travel-time ?v) (travel-duration ?r))
      (increase (sim-time) (travel-duration ?r))
    )
  )

  (:action traverse-road-static-road_0674-road_0154
    :parameters (?v - vehicle ?r - road_type_road_0674 ?next - road_type_road_0154)
    :precondition (and
      (ready-road ?v ?r)
      (road-next ?r ?next)
      (road-open ?r)
      (static-road ?r)
      (not (road-blocked ?r))
    )
    :effect (and
      (not (ready-road ?v ?r))
      (ready-road ?v ?next)
      (increase (travel-time ?v) (travel-duration ?r))
      (increase (sim-time) (travel-duration ?r))
    )
  )

  (:action traverse-road-static-road_0674-road_0155
    :parameters (?v - vehicle ?r - road_type_road_0674 ?next - road_type_road_0155)
    :precondition (and
      (ready-road ?v ?r)
      (road-next ?r ?next)
      (road-open ?r)
      (static-road ?r)
      (not (road-blocked ?r))
    )
    :effect (and
      (not (ready-road ?v ?r))
      (ready-road ?v ?next)
      (increase (travel-time ?v) (travel-duration ?r))
      (increase (sim-time) (travel-duration ?r))
    )
  )

  (:action traverse-road-static-road_0675-road_0252
    :parameters (?v - vehicle ?r - road_type_road_0675 ?next - road_type_road_0252)
    :precondition (and
      (ready-road ?v ?r)
      (road-next ?r ?next)
      (road-open ?r)
      (static-road ?r)
      (not (road-blocked ?r))
    )
    :effect (and
      (not (ready-road ?v ?r))
      (ready-road ?v ?next)
      (increase (travel-time ?v) (travel-duration ?r))
      (increase (sim-time) (travel-duration ?r))
    )
  )

  (:action traverse-road-static-road_0675-road_0253
    :parameters (?v - vehicle ?r - road_type_road_0675 ?next - road_type_road_0253)
    :precondition (and
      (ready-road ?v ?r)
      (road-next ?r ?next)
      (road-open ?r)
      (static-road ?r)
      (not (road-blocked ?r))
    )
    :effect (and
      (not (ready-road ?v ?r))
      (ready-road ?v ?next)
      (increase (travel-time ?v) (travel-duration ?r))
      (increase (sim-time) (travel-duration ?r))
    )
  )

  (:action traverse-road-static-road_0676-road_0260
    :parameters (?v - vehicle ?r - road_type_road_0676 ?next - road_type_road_0260)
    :precondition (and
      (ready-road ?v ?r)
      (road-next ?r ?next)
      (road-open ?r)
      (static-road ?r)
      (not (road-blocked ?r))
    )
    :effect (and
      (not (ready-road ?v ?r))
      (ready-road ?v ?next)
      (increase (travel-time ?v) (travel-duration ?r))
      (increase (sim-time) (travel-duration ?r))
    )
  )

  (:action traverse-road-static-road_0677-road_0686
    :parameters (?v - vehicle ?r - road_type_road_0677 ?next - road_type_road_0686)
    :precondition (and
      (ready-road ?v ?r)
      (road-next ?r ?next)
      (road-open ?r)
      (static-road ?r)
      (not (road-blocked ?r))
    )
    :effect (and
      (not (ready-road ?v ?r))
      (ready-road ?v ?next)
      (increase (travel-time ?v) (travel-duration ?r))
      (increase (sim-time) (travel-duration ?r))
    )
  )

  (:action traverse-road-static-road_0677-road_0687
    :parameters (?v - vehicle ?r - road_type_road_0677 ?next - road_type_road_0687)
    :precondition (and
      (ready-road ?v ?r)
      (road-next ?r ?next)
      (road-open ?r)
      (static-road ?r)
      (not (road-blocked ?r))
    )
    :effect (and
      (not (ready-road ?v ?r))
      (ready-road ?v ?next)
      (increase (travel-time ?v) (travel-duration ?r))
      (increase (sim-time) (travel-duration ?r))
    )
  )

  (:action traverse-road-static-road_0677-road_0688
    :parameters (?v - vehicle ?r - road_type_road_0677 ?next - road_type_road_0688)
    :precondition (and
      (ready-road ?v ?r)
      (road-next ?r ?next)
      (road-open ?r)
      (static-road ?r)
      (not (road-blocked ?r))
    )
    :effect (and
      (not (ready-road ?v ?r))
      (ready-road ?v ?next)
      (increase (travel-time ?v) (travel-duration ?r))
      (increase (sim-time) (travel-duration ?r))
    )
  )

  (:action traverse-road-static-road_0678-road_0606
    :parameters (?v - vehicle ?r - road_type_road_0678 ?next - road_type_road_0606)
    :precondition (and
      (ready-road ?v ?r)
      (road-next ?r ?next)
      (road-open ?r)
      (static-road ?r)
      (not (road-blocked ?r))
    )
    :effect (and
      (not (ready-road ?v ?r))
      (ready-road ?v ?next)
      (increase (travel-time ?v) (travel-duration ?r))
      (increase (sim-time) (travel-duration ?r))
    )
  )

  (:action traverse-road-static-road_0678-road_0607
    :parameters (?v - vehicle ?r - road_type_road_0678 ?next - road_type_road_0607)
    :precondition (and
      (ready-road ?v ?r)
      (road-next ?r ?next)
      (road-open ?r)
      (static-road ?r)
      (not (road-blocked ?r))
    )
    :effect (and
      (not (ready-road ?v ?r))
      (ready-road ?v ?next)
      (increase (travel-time ?v) (travel-duration ?r))
      (increase (sim-time) (travel-duration ?r))
    )
  )

  (:action traverse-road-static-road_0679-road_0043
    :parameters (?v - vehicle ?r - road_type_road_0679 ?next - road_type_road_0043)
    :precondition (and
      (ready-road ?v ?r)
      (road-next ?r ?next)
      (road-open ?r)
      (static-road ?r)
      (not (road-blocked ?r))
    )
    :effect (and
      (not (ready-road ?v ?r))
      (ready-road ?v ?next)
      (increase (travel-time ?v) (travel-duration ?r))
      (increase (sim-time) (travel-duration ?r))
    )
  )

  (:action traverse-road-static-road_0679-road_0044
    :parameters (?v - vehicle ?r - road_type_road_0679 ?next - road_type_road_0044)
    :precondition (and
      (ready-road ?v ?r)
      (road-next ?r ?next)
      (road-open ?r)
      (static-road ?r)
      (not (road-blocked ?r))
    )
    :effect (and
      (not (ready-road ?v ?r))
      (ready-road ?v ?next)
      (increase (travel-time ?v) (travel-duration ?r))
      (increase (sim-time) (travel-duration ?r))
    )
  )

  (:action traverse-road-static-road_0680-macro_0058
    :parameters (?v - vehicle ?r - road_type_road_0680 ?next - road_type_macro_0058)
    :precondition (and
      (ready-road ?v ?r)
      (road-next ?r ?next)
      (road-open ?r)
      (static-road ?r)
      (not (road-blocked ?r))
    )
    :effect (and
      (not (ready-road ?v ?r))
      (ready-road ?v ?next)
      (increase (travel-time ?v) (travel-duration ?r))
      (increase (sim-time) (travel-duration ?r))
    )
  )

  (:action traverse-road-static-road_0680-road_0851
    :parameters (?v - vehicle ?r - road_type_road_0680 ?next - road_type_road_0851)
    :precondition (and
      (ready-road ?v ?r)
      (road-next ?r ?next)
      (road-open ?r)
      (static-road ?r)
      (not (road-blocked ?r))
    )
    :effect (and
      (not (ready-road ?v ?r))
      (ready-road ?v ?next)
      (increase (travel-time ?v) (travel-duration ?r))
      (increase (sim-time) (travel-duration ?r))
    )
  )

  (:action traverse-road-static-road_0681-macro_0055
    :parameters (?v - vehicle ?r - road_type_road_0681 ?next - road_type_macro_0055)
    :precondition (and
      (ready-road ?v ?r)
      (road-next ?r ?next)
      (road-open ?r)
      (static-road ?r)
      (not (road-blocked ?r))
    )
    :effect (and
      (not (ready-road ?v ?r))
      (ready-road ?v ?next)
      (increase (travel-time ?v) (travel-duration ?r))
      (increase (sim-time) (travel-duration ?r))
    )
  )

  (:action traverse-road-static-road_0681-road_0834
    :parameters (?v - vehicle ?r - road_type_road_0681 ?next - road_type_road_0834)
    :precondition (and
      (ready-road ?v ?r)
      (road-next ?r ?next)
      (road-open ?r)
      (static-road ?r)
      (not (road-blocked ?r))
    )
    :effect (and
      (not (ready-road ?v ?r))
      (ready-road ?v ?next)
      (increase (travel-time ?v) (travel-duration ?r))
      (increase (sim-time) (travel-duration ?r))
    )
  )

  (:action traverse-road-static-road_0681-road_0835
    :parameters (?v - vehicle ?r - road_type_road_0681 ?next - road_type_road_0835)
    :precondition (and
      (ready-road ?v ?r)
      (road-next ?r ?next)
      (road-open ?r)
      (static-road ?r)
      (not (road-blocked ?r))
    )
    :effect (and
      (not (ready-road ?v ?r))
      (ready-road ?v ?next)
      (increase (travel-time ?v) (travel-duration ?r))
      (increase (sim-time) (travel-duration ?r))
    )
  )

  (:action traverse-road-static-road_0682-road_0459
    :parameters (?v - vehicle ?r - road_type_road_0682 ?next - road_type_road_0459)
    :precondition (and
      (ready-road ?v ?r)
      (road-next ?r ?next)
      (road-open ?r)
      (static-road ?r)
      (not (road-blocked ?r))
    )
    :effect (and
      (not (ready-road ?v ?r))
      (ready-road ?v ?next)
      (increase (travel-time ?v) (travel-duration ?r))
      (increase (sim-time) (travel-duration ?r))
    )
  )

  (:action traverse-road-static-road_0682-road_0460
    :parameters (?v - vehicle ?r - road_type_road_0682 ?next - road_type_road_0460)
    :precondition (and
      (ready-road ?v ?r)
      (road-next ?r ?next)
      (road-open ?r)
      (static-road ?r)
      (not (road-blocked ?r))
    )
    :effect (and
      (not (ready-road ?v ?r))
      (ready-road ?v ?next)
      (increase (travel-time ?v) (travel-duration ?r))
      (increase (sim-time) (travel-duration ?r))
    )
  )

  (:action traverse-road-static-road_0683-road_0470
    :parameters (?v - vehicle ?r - road_type_road_0683 ?next - road_type_road_0470)
    :precondition (and
      (ready-road ?v ?r)
      (road-next ?r ?next)
      (road-open ?r)
      (static-road ?r)
      (not (road-blocked ?r))
    )
    :effect (and
      (not (ready-road ?v ?r))
      (ready-road ?v ?next)
      (increase (travel-time ?v) (travel-duration ?r))
      (increase (sim-time) (travel-duration ?r))
    )
  )

  (:action traverse-road-static-road_0684-road_0149
    :parameters (?v - vehicle ?r - road_type_road_0684 ?next - road_type_road_0149)
    :precondition (and
      (ready-road ?v ?r)
      (road-next ?r ?next)
      (road-open ?r)
      (static-road ?r)
      (not (road-blocked ?r))
    )
    :effect (and
      (not (ready-road ?v ?r))
      (ready-road ?v ?next)
      (increase (travel-time ?v) (travel-duration ?r))
      (increase (sim-time) (travel-duration ?r))
    )
  )

  (:action traverse-road-static-road_0685-road_0825
    :parameters (?v - vehicle ?r - road_type_road_0685 ?next - road_type_road_0825)
    :precondition (and
      (ready-road ?v ?r)
      (road-next ?r ?next)
      (road-open ?r)
      (static-road ?r)
      (not (road-blocked ?r))
    )
    :effect (and
      (not (ready-road ?v ?r))
      (ready-road ?v ?next)
      (increase (travel-time ?v) (travel-duration ?r))
      (increase (sim-time) (travel-duration ?r))
    )
  )

  (:action traverse-road-static-road_0685-road_0826
    :parameters (?v - vehicle ?r - road_type_road_0685 ?next - road_type_road_0826)
    :precondition (and
      (ready-road ?v ?r)
      (road-next ?r ?next)
      (road-open ?r)
      (static-road ?r)
      (not (road-blocked ?r))
    )
    :effect (and
      (not (ready-road ?v ?r))
      (ready-road ?v ?next)
      (increase (travel-time ?v) (travel-duration ?r))
      (increase (sim-time) (travel-duration ?r))
    )
  )

  (:action traverse-road-static-road_0686-road_0677
    :parameters (?v - vehicle ?r - road_type_road_0686 ?next - road_type_road_0677)
    :precondition (and
      (ready-road ?v ?r)
      (road-next ?r ?next)
      (road-open ?r)
      (static-road ?r)
      (not (road-blocked ?r))
    )
    :effect (and
      (not (ready-road ?v ?r))
      (ready-road ?v ?next)
      (increase (travel-time ?v) (travel-duration ?r))
      (increase (sim-time) (travel-duration ?r))
    )
  )

  (:action traverse-road-static-road_0686-road_0678
    :parameters (?v - vehicle ?r - road_type_road_0686 ?next - road_type_road_0678)
    :precondition (and
      (ready-road ?v ?r)
      (road-next ?r ?next)
      (road-open ?r)
      (static-road ?r)
      (not (road-blocked ?r))
    )
    :effect (and
      (not (ready-road ?v ?r))
      (ready-road ?v ?next)
      (increase (travel-time ?v) (travel-duration ?r))
      (increase (sim-time) (travel-duration ?r))
    )
  )

  (:action traverse-road-static-road_0686-road_0679
    :parameters (?v - vehicle ?r - road_type_road_0686 ?next - road_type_road_0679)
    :precondition (and
      (ready-road ?v ?r)
      (road-next ?r ?next)
      (road-open ?r)
      (static-road ?r)
      (not (road-blocked ?r))
    )
    :effect (and
      (not (ready-road ?v ?r))
      (ready-road ?v ?next)
      (increase (travel-time ?v) (travel-duration ?r))
      (increase (sim-time) (travel-duration ?r))
    )
  )

  (:action traverse-road-static-road_0687-road_0523
    :parameters (?v - vehicle ?r - road_type_road_0687 ?next - road_type_road_0523)
    :precondition (and
      (ready-road ?v ?r)
      (road-next ?r ?next)
      (road-open ?r)
      (static-road ?r)
      (not (road-blocked ?r))
    )
    :effect (and
      (not (ready-road ?v ?r))
      (ready-road ?v ?next)
      (increase (travel-time ?v) (travel-duration ?r))
      (increase (sim-time) (travel-duration ?r))
    )
  )

  (:action traverse-road-static-road_0687-road_0524
    :parameters (?v - vehicle ?r - road_type_road_0687 ?next - road_type_road_0524)
    :precondition (and
      (ready-road ?v ?r)
      (road-next ?r ?next)
      (road-open ?r)
      (static-road ?r)
      (not (road-blocked ?r))
    )
    :effect (and
      (not (ready-road ?v ?r))
      (ready-road ?v ?next)
      (increase (travel-time ?v) (travel-duration ?r))
      (increase (sim-time) (travel-duration ?r))
    )
  )

  (:action traverse-road-static-road_0687-road_0525
    :parameters (?v - vehicle ?r - road_type_road_0687 ?next - road_type_road_0525)
    :precondition (and
      (ready-road ?v ?r)
      (road-next ?r ?next)
      (road-open ?r)
      (static-road ?r)
      (not (road-blocked ?r))
    )
    :effect (and
      (not (ready-road ?v ?r))
      (ready-road ?v ?next)
      (increase (travel-time ?v) (travel-duration ?r))
      (increase (sim-time) (travel-duration ?r))
    )
  )

  (:action traverse-road-static-road_0687-road_0526
    :parameters (?v - vehicle ?r - road_type_road_0687 ?next - road_type_road_0526)
    :precondition (and
      (ready-road ?v ?r)
      (road-next ?r ?next)
      (road-open ?r)
      (static-road ?r)
      (not (road-blocked ?r))
    )
    :effect (and
      (not (ready-road ?v ?r))
      (ready-road ?v ?next)
      (increase (travel-time ?v) (travel-duration ?r))
      (increase (sim-time) (travel-duration ?r))
    )
  )

  (:action traverse-road-static-road_0688-road_0689
    :parameters (?v - vehicle ?r - road_type_road_0688 ?next - road_type_road_0689)
    :precondition (and
      (ready-road ?v ?r)
      (road-next ?r ?next)
      (road-open ?r)
      (static-road ?r)
      (not (road-blocked ?r))
    )
    :effect (and
      (not (ready-road ?v ?r))
      (ready-road ?v ?next)
      (increase (travel-time ?v) (travel-duration ?r))
      (increase (sim-time) (travel-duration ?r))
    )
  )

  (:action traverse-road-static-road_0688-road_0690
    :parameters (?v - vehicle ?r - road_type_road_0688 ?next - road_type_road_0690)
    :precondition (and
      (ready-road ?v ?r)
      (road-next ?r ?next)
      (road-open ?r)
      (static-road ?r)
      (not (road-blocked ?r))
    )
    :effect (and
      (not (ready-road ?v ?r))
      (ready-road ?v ?next)
      (increase (travel-time ?v) (travel-duration ?r))
      (increase (sim-time) (travel-duration ?r))
    )
  )

  (:action traverse-road-static-road_0689-road_0686
    :parameters (?v - vehicle ?r - road_type_road_0689 ?next - road_type_road_0686)
    :precondition (and
      (ready-road ?v ?r)
      (road-next ?r ?next)
      (road-open ?r)
      (static-road ?r)
      (not (road-blocked ?r))
    )
    :effect (and
      (not (ready-road ?v ?r))
      (ready-road ?v ?next)
      (increase (travel-time ?v) (travel-duration ?r))
      (increase (sim-time) (travel-duration ?r))
    )
  )

  (:action traverse-road-static-road_0689-road_0687
    :parameters (?v - vehicle ?r - road_type_road_0689 ?next - road_type_road_0687)
    :precondition (and
      (ready-road ?v ?r)
      (road-next ?r ?next)
      (road-open ?r)
      (static-road ?r)
      (not (road-blocked ?r))
    )
    :effect (and
      (not (ready-road ?v ?r))
      (ready-road ?v ?next)
      (increase (travel-time ?v) (travel-duration ?r))
      (increase (sim-time) (travel-duration ?r))
    )
  )

  (:action traverse-road-static-road_0689-road_0688
    :parameters (?v - vehicle ?r - road_type_road_0689 ?next - road_type_road_0688)
    :precondition (and
      (ready-road ?v ?r)
      (road-next ?r ?next)
      (road-open ?r)
      (static-road ?r)
      (not (road-blocked ?r))
    )
    :effect (and
      (not (ready-road ?v ?r))
      (ready-road ?v ?next)
      (increase (travel-time ?v) (travel-duration ?r))
      (increase (sim-time) (travel-duration ?r))
    )
  )

  (:action traverse-road-static-road_0690-road_0297
    :parameters (?v - vehicle ?r - road_type_road_0690 ?next - road_type_road_0297)
    :precondition (and
      (ready-road ?v ?r)
      (road-next ?r ?next)
      (road-open ?r)
      (static-road ?r)
      (not (road-blocked ?r))
    )
    :effect (and
      (not (ready-road ?v ?r))
      (ready-road ?v ?next)
      (increase (travel-time ?v) (travel-duration ?r))
      (increase (sim-time) (travel-duration ?r))
    )
  )

  (:action traverse-road-dynamic-road_0691-road_0615-tw_00000
    :parameters (?v - vehicle ?r - road_type_road_0691 ?next - road_type_road_0615 ?w - window_type_tw_00000)
    :precondition (and
      (ready-road ?v ?r)
      (road-next ?r ?next)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
    )
    :effect (and
      (not (ready-road ?v ?r))
      (ready-road ?v ?next)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-road_0691-road_0615-tw_00030
    :parameters (?v - vehicle ?r - road_type_road_0691 ?next - road_type_road_0615 ?w - window_type_tw_00030)
    :precondition (and
      (ready-road ?v ?r)
      (road-next ?r ?next)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
    )
    :effect (and
      (not (ready-road ?v ?r))
      (ready-road ?v ?next)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-road_0691-road_0615-tw_00060
    :parameters (?v - vehicle ?r - road_type_road_0691 ?next - road_type_road_0615 ?w - window_type_tw_00060)
    :precondition (and
      (ready-road ?v ?r)
      (road-next ?r ?next)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
    )
    :effect (and
      (not (ready-road ?v ?r))
      (ready-road ?v ?next)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-road_0691-road_0615-tw_00090
    :parameters (?v - vehicle ?r - road_type_road_0691 ?next - road_type_road_0615 ?w - window_type_tw_00090)
    :precondition (and
      (ready-road ?v ?r)
      (road-next ?r ?next)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
    )
    :effect (and
      (not (ready-road ?v ?r))
      (ready-road ?v ?next)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-road_0691-road_0615-tw_00120
    :parameters (?v - vehicle ?r - road_type_road_0691 ?next - road_type_road_0615 ?w - window_type_tw_00120)
    :precondition (and
      (ready-road ?v ?r)
      (road-next ?r ?next)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
    )
    :effect (and
      (not (ready-road ?v ?r))
      (ready-road ?v ?next)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-road_0691-road_0615-tw_00150
    :parameters (?v - vehicle ?r - road_type_road_0691 ?next - road_type_road_0615 ?w - window_type_tw_00150)
    :precondition (and
      (ready-road ?v ?r)
      (road-next ?r ?next)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
    )
    :effect (and
      (not (ready-road ?v ?r))
      (ready-road ?v ?next)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-road_0691-road_0615-tw_00180
    :parameters (?v - vehicle ?r - road_type_road_0691 ?next - road_type_road_0615 ?w - window_type_tw_00180)
    :precondition (and
      (ready-road ?v ?r)
      (road-next ?r ?next)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
    )
    :effect (and
      (not (ready-road ?v ?r))
      (ready-road ?v ?next)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-road_0691-road_0615-tw_00210
    :parameters (?v - vehicle ?r - road_type_road_0691 ?next - road_type_road_0615 ?w - window_type_tw_00210)
    :precondition (and
      (ready-road ?v ?r)
      (road-next ?r ?next)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
    )
    :effect (and
      (not (ready-road ?v ?r))
      (ready-road ?v ?next)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-road_0691-road_0615-tw_00240
    :parameters (?v - vehicle ?r - road_type_road_0691 ?next - road_type_road_0615 ?w - window_type_tw_00240)
    :precondition (and
      (ready-road ?v ?r)
      (road-next ?r ?next)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
    )
    :effect (and
      (not (ready-road ?v ?r))
      (ready-road ?v ?next)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-road_0691-road_0615-tw_00270
    :parameters (?v - vehicle ?r - road_type_road_0691 ?next - road_type_road_0615 ?w - window_type_tw_00270)
    :precondition (and
      (ready-road ?v ?r)
      (road-next ?r ?next)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
    )
    :effect (and
      (not (ready-road ?v ?r))
      (ready-road ?v ?next)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-road_0691-road_0615-tw_00300
    :parameters (?v - vehicle ?r - road_type_road_0691 ?next - road_type_road_0615 ?w - window_type_tw_00300)
    :precondition (and
      (ready-road ?v ?r)
      (road-next ?r ?next)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
    )
    :effect (and
      (not (ready-road ?v ?r))
      (ready-road ?v ?next)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-road_0691-road_0615-tw_00330
    :parameters (?v - vehicle ?r - road_type_road_0691 ?next - road_type_road_0615 ?w - window_type_tw_00330)
    :precondition (and
      (ready-road ?v ?r)
      (road-next ?r ?next)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
    )
    :effect (and
      (not (ready-road ?v ?r))
      (ready-road ?v ?next)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-road_0691-road_0615-tw_00360
    :parameters (?v - vehicle ?r - road_type_road_0691 ?next - road_type_road_0615 ?w - window_type_tw_00360)
    :precondition (and
      (ready-road ?v ?r)
      (road-next ?r ?next)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
    )
    :effect (and
      (not (ready-road ?v ?r))
      (ready-road ?v ?next)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-road_0691-road_0615-tw_00390
    :parameters (?v - vehicle ?r - road_type_road_0691 ?next - road_type_road_0615 ?w - window_type_tw_00390)
    :precondition (and
      (ready-road ?v ?r)
      (road-next ?r ?next)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
    )
    :effect (and
      (not (ready-road ?v ?r))
      (ready-road ?v ?next)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-road_0691-road_0615-tw_00420
    :parameters (?v - vehicle ?r - road_type_road_0691 ?next - road_type_road_0615 ?w - window_type_tw_00420)
    :precondition (and
      (ready-road ?v ?r)
      (road-next ?r ?next)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
    )
    :effect (and
      (not (ready-road ?v ?r))
      (ready-road ?v ?next)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-road_0691-road_0615-tw_00450
    :parameters (?v - vehicle ?r - road_type_road_0691 ?next - road_type_road_0615 ?w - window_type_tw_00450)
    :precondition (and
      (ready-road ?v ?r)
      (road-next ?r ?next)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
    )
    :effect (and
      (not (ready-road ?v ?r))
      (ready-road ?v ?next)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-road_0691-road_0615-tw_00480
    :parameters (?v - vehicle ?r - road_type_road_0691 ?next - road_type_road_0615 ?w - window_type_tw_00480)
    :precondition (and
      (ready-road ?v ?r)
      (road-next ?r ?next)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
    )
    :effect (and
      (not (ready-road ?v ?r))
      (ready-road ?v ?next)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-road_0691-road_0615-tw_00510
    :parameters (?v - vehicle ?r - road_type_road_0691 ?next - road_type_road_0615 ?w - window_type_tw_00510)
    :precondition (and
      (ready-road ?v ?r)
      (road-next ?r ?next)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
    )
    :effect (and
      (not (ready-road ?v ?r))
      (ready-road ?v ?next)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-road_0691-road_0615-tw_00540
    :parameters (?v - vehicle ?r - road_type_road_0691 ?next - road_type_road_0615 ?w - window_type_tw_00540)
    :precondition (and
      (ready-road ?v ?r)
      (road-next ?r ?next)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
    )
    :effect (and
      (not (ready-road ?v ?r))
      (ready-road ?v ?next)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-road_0691-road_0615-tw_00570
    :parameters (?v - vehicle ?r - road_type_road_0691 ?next - road_type_road_0615 ?w - window_type_tw_00570)
    :precondition (and
      (ready-road ?v ?r)
      (road-next ?r ?next)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
    )
    :effect (and
      (not (ready-road ?v ?r))
      (ready-road ?v ?next)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-road_0691-road_0615-tw_00600
    :parameters (?v - vehicle ?r - road_type_road_0691 ?next - road_type_road_0615 ?w - window_type_tw_00600)
    :precondition (and
      (ready-road ?v ?r)
      (road-next ?r ?next)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
    )
    :effect (and
      (not (ready-road ?v ?r))
      (ready-road ?v ?next)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-road_0691-road_0616-tw_00000
    :parameters (?v - vehicle ?r - road_type_road_0691 ?next - road_type_road_0616 ?w - window_type_tw_00000)
    :precondition (and
      (ready-road ?v ?r)
      (road-next ?r ?next)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
    )
    :effect (and
      (not (ready-road ?v ?r))
      (ready-road ?v ?next)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-road_0691-road_0616-tw_00030
    :parameters (?v - vehicle ?r - road_type_road_0691 ?next - road_type_road_0616 ?w - window_type_tw_00030)
    :precondition (and
      (ready-road ?v ?r)
      (road-next ?r ?next)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
    )
    :effect (and
      (not (ready-road ?v ?r))
      (ready-road ?v ?next)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-road_0691-road_0616-tw_00060
    :parameters (?v - vehicle ?r - road_type_road_0691 ?next - road_type_road_0616 ?w - window_type_tw_00060)
    :precondition (and
      (ready-road ?v ?r)
      (road-next ?r ?next)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
    )
    :effect (and
      (not (ready-road ?v ?r))
      (ready-road ?v ?next)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-road_0691-road_0616-tw_00090
    :parameters (?v - vehicle ?r - road_type_road_0691 ?next - road_type_road_0616 ?w - window_type_tw_00090)
    :precondition (and
      (ready-road ?v ?r)
      (road-next ?r ?next)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
    )
    :effect (and
      (not (ready-road ?v ?r))
      (ready-road ?v ?next)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-road_0691-road_0616-tw_00120
    :parameters (?v - vehicle ?r - road_type_road_0691 ?next - road_type_road_0616 ?w - window_type_tw_00120)
    :precondition (and
      (ready-road ?v ?r)
      (road-next ?r ?next)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
    )
    :effect (and
      (not (ready-road ?v ?r))
      (ready-road ?v ?next)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-road_0691-road_0616-tw_00150
    :parameters (?v - vehicle ?r - road_type_road_0691 ?next - road_type_road_0616 ?w - window_type_tw_00150)
    :precondition (and
      (ready-road ?v ?r)
      (road-next ?r ?next)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
    )
    :effect (and
      (not (ready-road ?v ?r))
      (ready-road ?v ?next)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-road_0691-road_0616-tw_00180
    :parameters (?v - vehicle ?r - road_type_road_0691 ?next - road_type_road_0616 ?w - window_type_tw_00180)
    :precondition (and
      (ready-road ?v ?r)
      (road-next ?r ?next)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
    )
    :effect (and
      (not (ready-road ?v ?r))
      (ready-road ?v ?next)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-road_0691-road_0616-tw_00210
    :parameters (?v - vehicle ?r - road_type_road_0691 ?next - road_type_road_0616 ?w - window_type_tw_00210)
    :precondition (and
      (ready-road ?v ?r)
      (road-next ?r ?next)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
    )
    :effect (and
      (not (ready-road ?v ?r))
      (ready-road ?v ?next)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-road_0691-road_0616-tw_00240
    :parameters (?v - vehicle ?r - road_type_road_0691 ?next - road_type_road_0616 ?w - window_type_tw_00240)
    :precondition (and
      (ready-road ?v ?r)
      (road-next ?r ?next)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
    )
    :effect (and
      (not (ready-road ?v ?r))
      (ready-road ?v ?next)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-road_0691-road_0616-tw_00270
    :parameters (?v - vehicle ?r - road_type_road_0691 ?next - road_type_road_0616 ?w - window_type_tw_00270)
    :precondition (and
      (ready-road ?v ?r)
      (road-next ?r ?next)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
    )
    :effect (and
      (not (ready-road ?v ?r))
      (ready-road ?v ?next)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-road_0691-road_0616-tw_00300
    :parameters (?v - vehicle ?r - road_type_road_0691 ?next - road_type_road_0616 ?w - window_type_tw_00300)
    :precondition (and
      (ready-road ?v ?r)
      (road-next ?r ?next)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
    )
    :effect (and
      (not (ready-road ?v ?r))
      (ready-road ?v ?next)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-road_0691-road_0616-tw_00330
    :parameters (?v - vehicle ?r - road_type_road_0691 ?next - road_type_road_0616 ?w - window_type_tw_00330)
    :precondition (and
      (ready-road ?v ?r)
      (road-next ?r ?next)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
    )
    :effect (and
      (not (ready-road ?v ?r))
      (ready-road ?v ?next)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-road_0691-road_0616-tw_00360
    :parameters (?v - vehicle ?r - road_type_road_0691 ?next - road_type_road_0616 ?w - window_type_tw_00360)
    :precondition (and
      (ready-road ?v ?r)
      (road-next ?r ?next)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
    )
    :effect (and
      (not (ready-road ?v ?r))
      (ready-road ?v ?next)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-road_0691-road_0616-tw_00390
    :parameters (?v - vehicle ?r - road_type_road_0691 ?next - road_type_road_0616 ?w - window_type_tw_00390)
    :precondition (and
      (ready-road ?v ?r)
      (road-next ?r ?next)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
    )
    :effect (and
      (not (ready-road ?v ?r))
      (ready-road ?v ?next)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-road_0691-road_0616-tw_00420
    :parameters (?v - vehicle ?r - road_type_road_0691 ?next - road_type_road_0616 ?w - window_type_tw_00420)
    :precondition (and
      (ready-road ?v ?r)
      (road-next ?r ?next)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
    )
    :effect (and
      (not (ready-road ?v ?r))
      (ready-road ?v ?next)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-road_0691-road_0616-tw_00450
    :parameters (?v - vehicle ?r - road_type_road_0691 ?next - road_type_road_0616 ?w - window_type_tw_00450)
    :precondition (and
      (ready-road ?v ?r)
      (road-next ?r ?next)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
    )
    :effect (and
      (not (ready-road ?v ?r))
      (ready-road ?v ?next)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-road_0691-road_0616-tw_00480
    :parameters (?v - vehicle ?r - road_type_road_0691 ?next - road_type_road_0616 ?w - window_type_tw_00480)
    :precondition (and
      (ready-road ?v ?r)
      (road-next ?r ?next)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
    )
    :effect (and
      (not (ready-road ?v ?r))
      (ready-road ?v ?next)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-road_0691-road_0616-tw_00510
    :parameters (?v - vehicle ?r - road_type_road_0691 ?next - road_type_road_0616 ?w - window_type_tw_00510)
    :precondition (and
      (ready-road ?v ?r)
      (road-next ?r ?next)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
    )
    :effect (and
      (not (ready-road ?v ?r))
      (ready-road ?v ?next)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-road_0691-road_0616-tw_00540
    :parameters (?v - vehicle ?r - road_type_road_0691 ?next - road_type_road_0616 ?w - window_type_tw_00540)
    :precondition (and
      (ready-road ?v ?r)
      (road-next ?r ?next)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
    )
    :effect (and
      (not (ready-road ?v ?r))
      (ready-road ?v ?next)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-road_0691-road_0616-tw_00570
    :parameters (?v - vehicle ?r - road_type_road_0691 ?next - road_type_road_0616 ?w - window_type_tw_00570)
    :precondition (and
      (ready-road ?v ?r)
      (road-next ?r ?next)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
    )
    :effect (and
      (not (ready-road ?v ?r))
      (ready-road ?v ?next)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-road_0691-road_0616-tw_00600
    :parameters (?v - vehicle ?r - road_type_road_0691 ?next - road_type_road_0616 ?w - window_type_tw_00600)
    :precondition (and
      (ready-road ?v ?r)
      (road-next ?r ?next)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
    )
    :effect (and
      (not (ready-road ?v ?r))
      (ready-road ?v ?next)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-static-road_0692-road_0344
    :parameters (?v - vehicle ?r - road_type_road_0692 ?next - road_type_road_0344)
    :precondition (and
      (ready-road ?v ?r)
      (road-next ?r ?next)
      (road-open ?r)
      (static-road ?r)
      (not (road-blocked ?r))
    )
    :effect (and
      (not (ready-road ?v ?r))
      (ready-road ?v ?next)
      (increase (travel-time ?v) (travel-duration ?r))
      (increase (sim-time) (travel-duration ?r))
    )
  )

  (:action traverse-road-static-road_0692-road_0345
    :parameters (?v - vehicle ?r - road_type_road_0692 ?next - road_type_road_0345)
    :precondition (and
      (ready-road ?v ?r)
      (road-next ?r ?next)
      (road-open ?r)
      (static-road ?r)
      (not (road-blocked ?r))
    )
    :effect (and
      (not (ready-road ?v ?r))
      (ready-road ?v ?next)
      (increase (travel-time ?v) (travel-duration ?r))
      (increase (sim-time) (travel-duration ?r))
    )
  )

  (:action traverse-road-static-road_0693-road_0207
    :parameters (?v - vehicle ?r - road_type_road_0693 ?next - road_type_road_0207)
    :precondition (and
      (ready-road ?v ?r)
      (road-next ?r ?next)
      (road-open ?r)
      (static-road ?r)
      (not (road-blocked ?r))
    )
    :effect (and
      (not (ready-road ?v ?r))
      (ready-road ?v ?next)
      (increase (travel-time ?v) (travel-duration ?r))
      (increase (sim-time) (travel-duration ?r))
    )
  )

  (:action traverse-road-static-road_0693-road_0208
    :parameters (?v - vehicle ?r - road_type_road_0693 ?next - road_type_road_0208)
    :precondition (and
      (ready-road ?v ?r)
      (road-next ?r ?next)
      (road-open ?r)
      (static-road ?r)
      (not (road-blocked ?r))
    )
    :effect (and
      (not (ready-road ?v ?r))
      (ready-road ?v ?next)
      (increase (travel-time ?v) (travel-duration ?r))
      (increase (sim-time) (travel-duration ?r))
    )
  )

  (:action traverse-road-static-road_0693-road_0209
    :parameters (?v - vehicle ?r - road_type_road_0693 ?next - road_type_road_0209)
    :precondition (and
      (ready-road ?v ?r)
      (road-next ?r ?next)
      (road-open ?r)
      (static-road ?r)
      (not (road-blocked ?r))
    )
    :effect (and
      (not (ready-road ?v ?r))
      (ready-road ?v ?next)
      (increase (travel-time ?v) (travel-duration ?r))
      (increase (sim-time) (travel-duration ?r))
    )
  )

  (:action traverse-road-static-road_0694-road_0329
    :parameters (?v - vehicle ?r - road_type_road_0694 ?next - road_type_road_0329)
    :precondition (and
      (ready-road ?v ?r)
      (road-next ?r ?next)
      (road-open ?r)
      (static-road ?r)
      (not (road-blocked ?r))
    )
    :effect (and
      (not (ready-road ?v ?r))
      (ready-road ?v ?next)
      (increase (travel-time ?v) (travel-duration ?r))
      (increase (sim-time) (travel-duration ?r))
    )
  )

  (:action traverse-road-static-road_0694-road_0330
    :parameters (?v - vehicle ?r - road_type_road_0694 ?next - road_type_road_0330)
    :precondition (and
      (ready-road ?v ?r)
      (road-next ?r ?next)
      (road-open ?r)
      (static-road ?r)
      (not (road-blocked ?r))
    )
    :effect (and
      (not (ready-road ?v ?r))
      (ready-road ?v ?next)
      (increase (travel-time ?v) (travel-duration ?r))
      (increase (sim-time) (travel-duration ?r))
    )
  )

  (:action traverse-road-static-road_0696-macro_0033
    :parameters (?v - vehicle ?r - road_type_road_0696 ?next - road_type_macro_0033)
    :precondition (and
      (ready-road ?v ?r)
      (road-next ?r ?next)
      (road-open ?r)
      (static-road ?r)
      (not (road-blocked ?r))
    )
    :effect (and
      (not (ready-road ?v ?r))
      (ready-road ?v ?next)
      (increase (travel-time ?v) (travel-duration ?r))
      (increase (sim-time) (travel-duration ?r))
    )
  )

  (:action traverse-road-static-road_0696-road_0428
    :parameters (?v - vehicle ?r - road_type_road_0696 ?next - road_type_road_0428)
    :precondition (and
      (ready-road ?v ?r)
      (road-next ?r ?next)
      (road-open ?r)
      (static-road ?r)
      (not (road-blocked ?r))
    )
    :effect (and
      (not (ready-road ?v ?r))
      (ready-road ?v ?next)
      (increase (travel-time ?v) (travel-duration ?r))
      (increase (sim-time) (travel-duration ?r))
    )
  )

  (:action traverse-road-static-road_0696-road_0430
    :parameters (?v - vehicle ?r - road_type_road_0696 ?next - road_type_road_0430)
    :precondition (and
      (ready-road ?v ?r)
      (road-next ?r ?next)
      (road-open ?r)
      (static-road ?r)
      (not (road-blocked ?r))
    )
    :effect (and
      (not (ready-road ?v ?r))
      (ready-road ?v ?next)
      (increase (travel-time ?v) (travel-duration ?r))
      (increase (sim-time) (travel-duration ?r))
    )
  )

  (:action traverse-road-static-road_0697-road_0713
    :parameters (?v - vehicle ?r - road_type_road_0697 ?next - road_type_road_0713)
    :precondition (and
      (ready-road ?v ?r)
      (road-next ?r ?next)
      (road-open ?r)
      (static-road ?r)
      (not (road-blocked ?r))
    )
    :effect (and
      (not (ready-road ?v ?r))
      (ready-road ?v ?next)
      (increase (travel-time ?v) (travel-duration ?r))
      (increase (sim-time) (travel-duration ?r))
    )
  )

  (:action traverse-road-static-road_0698-road_0399
    :parameters (?v - vehicle ?r - road_type_road_0698 ?next - road_type_road_0399)
    :precondition (and
      (ready-road ?v ?r)
      (road-next ?r ?next)
      (road-open ?r)
      (static-road ?r)
      (not (road-blocked ?r))
    )
    :effect (and
      (not (ready-road ?v ?r))
      (ready-road ?v ?next)
      (increase (travel-time ?v) (travel-duration ?r))
      (increase (sim-time) (travel-duration ?r))
    )
  )

  (:action traverse-road-static-road_0700-macro_0037
    :parameters (?v - vehicle ?r - road_type_road_0700 ?next - road_type_macro_0037)
    :precondition (and
      (ready-road ?v ?r)
      (road-next ?r ?next)
      (road-open ?r)
      (static-road ?r)
      (not (road-blocked ?r))
    )
    :effect (and
      (not (ready-road ?v ?r))
      (ready-road ?v ?next)
      (increase (travel-time ?v) (travel-duration ?r))
      (increase (sim-time) (travel-duration ?r))
    )
  )

  (:action traverse-road-static-road_0701-road_0476
    :parameters (?v - vehicle ?r - road_type_road_0701 ?next - road_type_road_0476)
    :precondition (and
      (ready-road ?v ?r)
      (road-next ?r ?next)
      (road-open ?r)
      (static-road ?r)
      (not (road-blocked ?r))
    )
    :effect (and
      (not (ready-road ?v ?r))
      (ready-road ?v ?next)
      (increase (travel-time ?v) (travel-duration ?r))
      (increase (sim-time) (travel-duration ?r))
    )
  )

  (:action traverse-road-static-road_0702-road_0805
    :parameters (?v - vehicle ?r - road_type_road_0702 ?next - road_type_road_0805)
    :precondition (and
      (ready-road ?v ?r)
      (road-next ?r ?next)
      (road-open ?r)
      (static-road ?r)
      (not (road-blocked ?r))
    )
    :effect (and
      (not (ready-road ?v ?r))
      (ready-road ?v ?next)
      (increase (travel-time ?v) (travel-duration ?r))
      (increase (sim-time) (travel-duration ?r))
    )
  )

  (:action traverse-road-static-road_0702-road_0806
    :parameters (?v - vehicle ?r - road_type_road_0702 ?next - road_type_road_0806)
    :precondition (and
      (ready-road ?v ?r)
      (road-next ?r ?next)
      (road-open ?r)
      (static-road ?r)
      (not (road-blocked ?r))
    )
    :effect (and
      (not (ready-road ?v ?r))
      (ready-road ?v ?next)
      (increase (travel-time ?v) (travel-duration ?r))
      (increase (sim-time) (travel-duration ?r))
    )
  )

  (:action traverse-road-static-road_0703-road_0485
    :parameters (?v - vehicle ?r - road_type_road_0703 ?next - road_type_road_0485)
    :precondition (and
      (ready-road ?v ?r)
      (road-next ?r ?next)
      (road-open ?r)
      (static-road ?r)
      (not (road-blocked ?r))
    )
    :effect (and
      (not (ready-road ?v ?r))
      (ready-road ?v ?next)
      (increase (travel-time ?v) (travel-duration ?r))
      (increase (sim-time) (travel-duration ?r))
    )
  )

  (:action traverse-road-static-road_0703-road_0486
    :parameters (?v - vehicle ?r - road_type_road_0703 ?next - road_type_road_0486)
    :precondition (and
      (ready-road ?v ?r)
      (road-next ?r ?next)
      (road-open ?r)
      (static-road ?r)
      (not (road-blocked ?r))
    )
    :effect (and
      (not (ready-road ?v ?r))
      (ready-road ?v ?next)
      (increase (travel-time ?v) (travel-duration ?r))
      (increase (sim-time) (travel-duration ?r))
    )
  )

  (:action traverse-road-static-road_0704-road_0223
    :parameters (?v - vehicle ?r - road_type_road_0704 ?next - road_type_road_0223)
    :precondition (and
      (ready-road ?v ?r)
      (road-next ?r ?next)
      (road-open ?r)
      (static-road ?r)
      (not (road-blocked ?r))
    )
    :effect (and
      (not (ready-road ?v ?r))
      (ready-road ?v ?next)
      (increase (travel-time ?v) (travel-duration ?r))
      (increase (sim-time) (travel-duration ?r))
    )
  )

  (:action traverse-road-static-road_0704-road_0224
    :parameters (?v - vehicle ?r - road_type_road_0704 ?next - road_type_road_0224)
    :precondition (and
      (ready-road ?v ?r)
      (road-next ?r ?next)
      (road-open ?r)
      (static-road ?r)
      (not (road-blocked ?r))
    )
    :effect (and
      (not (ready-road ?v ?r))
      (ready-road ?v ?next)
      (increase (travel-time ?v) (travel-duration ?r))
      (increase (sim-time) (travel-duration ?r))
    )
  )

  (:action traverse-road-static-road_0705-macro_0030
    :parameters (?v - vehicle ?r - road_type_road_0705 ?next - road_type_macro_0030)
    :precondition (and
      (ready-road ?v ?r)
      (road-next ?r ?next)
      (road-open ?r)
      (static-road ?r)
      (not (road-blocked ?r))
    )
    :effect (and
      (not (ready-road ?v ?r))
      (ready-road ?v ?next)
      (increase (travel-time ?v) (travel-duration ?r))
      (increase (sim-time) (travel-duration ?r))
    )
  )

  (:action traverse-road-static-road_0705-road_0401
    :parameters (?v - vehicle ?r - road_type_road_0705 ?next - road_type_road_0401)
    :precondition (and
      (ready-road ?v ?r)
      (road-next ?r ?next)
      (road-open ?r)
      (static-road ?r)
      (not (road-blocked ?r))
    )
    :effect (and
      (not (ready-road ?v ?r))
      (ready-road ?v ?next)
      (increase (travel-time ?v) (travel-duration ?r))
      (increase (sim-time) (travel-duration ?r))
    )
  )

  (:action traverse-road-static-road_0705-road_0403
    :parameters (?v - vehicle ?r - road_type_road_0705 ?next - road_type_road_0403)
    :precondition (and
      (ready-road ?v ?r)
      (road-next ?r ?next)
      (road-open ?r)
      (static-road ?r)
      (not (road-blocked ?r))
    )
    :effect (and
      (not (ready-road ?v ?r))
      (ready-road ?v ?next)
      (increase (travel-time ?v) (travel-duration ?r))
      (increase (sim-time) (travel-duration ?r))
    )
  )

  (:action traverse-road-static-road_0706-road_0621
    :parameters (?v - vehicle ?r - road_type_road_0706 ?next - road_type_road_0621)
    :precondition (and
      (ready-road ?v ?r)
      (road-next ?r ?next)
      (road-open ?r)
      (static-road ?r)
      (not (road-blocked ?r))
    )
    :effect (and
      (not (ready-road ?v ?r))
      (ready-road ?v ?next)
      (increase (travel-time ?v) (travel-duration ?r))
      (increase (sim-time) (travel-duration ?r))
    )
  )

  (:action traverse-road-static-road_0707-road_0418
    :parameters (?v - vehicle ?r - road_type_road_0707 ?next - road_type_road_0418)
    :precondition (and
      (ready-road ?v ?r)
      (road-next ?r ?next)
      (road-open ?r)
      (static-road ?r)
      (not (road-blocked ?r))
    )
    :effect (and
      (not (ready-road ?v ?r))
      (ready-road ?v ?next)
      (increase (travel-time ?v) (travel-duration ?r))
      (increase (sim-time) (travel-duration ?r))
    )
  )

  (:action traverse-road-static-road_0708-macro_0009
    :parameters (?v - vehicle ?r - road_type_road_0708 ?next - road_type_macro_0009)
    :precondition (and
      (ready-road ?v ?r)
      (road-next ?r ?next)
      (road-open ?r)
      (static-road ?r)
      (not (road-blocked ?r))
    )
    :effect (and
      (not (ready-road ?v ?r))
      (ready-road ?v ?next)
      (increase (travel-time ?v) (travel-duration ?r))
      (increase (sim-time) (travel-duration ?r))
    )
  )

  (:action traverse-road-static-road_0708-road_0095
    :parameters (?v - vehicle ?r - road_type_road_0708 ?next - road_type_road_0095)
    :precondition (and
      (ready-road ?v ?r)
      (road-next ?r ?next)
      (road-open ?r)
      (static-road ?r)
      (not (road-blocked ?r))
    )
    :effect (and
      (not (ready-road ?v ?r))
      (ready-road ?v ?next)
      (increase (travel-time ?v) (travel-duration ?r))
      (increase (sim-time) (travel-duration ?r))
    )
  )

  (:action traverse-road-static-road_0709-road_0586
    :parameters (?v - vehicle ?r - road_type_road_0709 ?next - road_type_road_0586)
    :precondition (and
      (ready-road ?v ?r)
      (road-next ?r ?next)
      (road-open ?r)
      (static-road ?r)
      (not (road-blocked ?r))
    )
    :effect (and
      (not (ready-road ?v ?r))
      (ready-road ?v ?next)
      (increase (travel-time ?v) (travel-duration ?r))
      (increase (sim-time) (travel-duration ?r))
    )
  )

  (:action traverse-road-static-road_0709-road_0587
    :parameters (?v - vehicle ?r - road_type_road_0709 ?next - road_type_road_0587)
    :precondition (and
      (ready-road ?v ?r)
      (road-next ?r ?next)
      (road-open ?r)
      (static-road ?r)
      (not (road-blocked ?r))
    )
    :effect (and
      (not (ready-road ?v ?r))
      (ready-road ?v ?next)
      (increase (travel-time ?v) (travel-duration ?r))
      (increase (sim-time) (travel-duration ?r))
    )
  )

  (:action traverse-road-static-road_0709-road_0588
    :parameters (?v - vehicle ?r - road_type_road_0709 ?next - road_type_road_0588)
    :precondition (and
      (ready-road ?v ?r)
      (road-next ?r ?next)
      (road-open ?r)
      (static-road ?r)
      (not (road-blocked ?r))
    )
    :effect (and
      (not (ready-road ?v ?r))
      (ready-road ?v ?next)
      (increase (travel-time ?v) (travel-duration ?r))
      (increase (sim-time) (travel-duration ?r))
    )
  )

  (:action traverse-road-static-road_0711-road_0343
    :parameters (?v - vehicle ?r - road_type_road_0711 ?next - road_type_road_0343)
    :precondition (and
      (ready-road ?v ?r)
      (road-next ?r ?next)
      (road-open ?r)
      (static-road ?r)
      (not (road-blocked ?r))
    )
    :effect (and
      (not (ready-road ?v ?r))
      (ready-road ?v ?next)
      (increase (travel-time ?v) (travel-duration ?r))
      (increase (sim-time) (travel-duration ?r))
    )
  )

  (:action traverse-road-static-road_0712-road_0122
    :parameters (?v - vehicle ?r - road_type_road_0712 ?next - road_type_road_0122)
    :precondition (and
      (ready-road ?v ?r)
      (road-next ?r ?next)
      (road-open ?r)
      (static-road ?r)
      (not (road-blocked ?r))
    )
    :effect (and
      (not (ready-road ?v ?r))
      (ready-road ?v ?next)
      (increase (travel-time ?v) (travel-duration ?r))
      (increase (sim-time) (travel-duration ?r))
    )
  )

  (:action traverse-road-static-road_0713-road_0696
    :parameters (?v - vehicle ?r - road_type_road_0713 ?next - road_type_road_0696)
    :precondition (and
      (ready-road ?v ?r)
      (road-next ?r ?next)
      (road-open ?r)
      (static-road ?r)
      (not (road-blocked ?r))
    )
    :effect (and
      (not (ready-road ?v ?r))
      (ready-road ?v ?next)
      (increase (travel-time ?v) (travel-duration ?r))
      (increase (sim-time) (travel-duration ?r))
    )
  )

  (:action traverse-road-static-road_0713-road_0697
    :parameters (?v - vehicle ?r - road_type_road_0713 ?next - road_type_road_0697)
    :precondition (and
      (ready-road ?v ?r)
      (road-next ?r ?next)
      (road-open ?r)
      (static-road ?r)
      (not (road-blocked ?r))
    )
    :effect (and
      (not (ready-road ?v ?r))
      (ready-road ?v ?next)
      (increase (travel-time ?v) (travel-duration ?r))
      (increase (sim-time) (travel-duration ?r))
    )
  )

  (:action traverse-road-static-road_0713-road_0698
    :parameters (?v - vehicle ?r - road_type_road_0713 ?next - road_type_road_0698)
    :precondition (and
      (ready-road ?v ?r)
      (road-next ?r ?next)
      (road-open ?r)
      (static-road ?r)
      (not (road-blocked ?r))
    )
    :effect (and
      (not (ready-road ?v ?r))
      (ready-road ?v ?next)
      (increase (travel-time ?v) (travel-duration ?r))
      (increase (sim-time) (travel-duration ?r))
    )
  )

  (:action traverse-road-static-road_0714-road_0261
    :parameters (?v - vehicle ?r - road_type_road_0714 ?next - road_type_road_0261)
    :precondition (and
      (ready-road ?v ?r)
      (road-next ?r ?next)
      (road-open ?r)
      (static-road ?r)
      (not (road-blocked ?r))
    )
    :effect (and
      (not (ready-road ?v ?r))
      (ready-road ?v ?next)
      (increase (travel-time ?v) (travel-duration ?r))
      (increase (sim-time) (travel-duration ?r))
    )
  )

  (:action traverse-road-static-road_0715-road_0657
    :parameters (?v - vehicle ?r - road_type_road_0715 ?next - road_type_road_0657)
    :precondition (and
      (ready-road ?v ?r)
      (road-next ?r ?next)
      (road-open ?r)
      (static-road ?r)
      (not (road-blocked ?r))
    )
    :effect (and
      (not (ready-road ?v ?r))
      (ready-road ?v ?next)
      (increase (travel-time ?v) (travel-duration ?r))
      (increase (sim-time) (travel-duration ?r))
    )
  )

  (:action traverse-road-static-road_0716-road_0213
    :parameters (?v - vehicle ?r - road_type_road_0716 ?next - road_type_road_0213)
    :precondition (and
      (ready-road ?v ?r)
      (road-next ?r ?next)
      (road-open ?r)
      (static-road ?r)
      (not (road-blocked ?r))
    )
    :effect (and
      (not (ready-road ?v ?r))
      (ready-road ?v ?next)
      (increase (travel-time ?v) (travel-duration ?r))
      (increase (sim-time) (travel-duration ?r))
    )
  )

  (:action traverse-road-static-road_0716-road_0214
    :parameters (?v - vehicle ?r - road_type_road_0716 ?next - road_type_road_0214)
    :precondition (and
      (ready-road ?v ?r)
      (road-next ?r ?next)
      (road-open ?r)
      (static-road ?r)
      (not (road-blocked ?r))
    )
    :effect (and
      (not (ready-road ?v ?r))
      (ready-road ?v ?next)
      (increase (travel-time ?v) (travel-duration ?r))
      (increase (sim-time) (travel-duration ?r))
    )
  )

  (:action traverse-road-static-road_0716-road_0215
    :parameters (?v - vehicle ?r - road_type_road_0716 ?next - road_type_road_0215)
    :precondition (and
      (ready-road ?v ?r)
      (road-next ?r ?next)
      (road-open ?r)
      (static-road ?r)
      (not (road-blocked ?r))
    )
    :effect (and
      (not (ready-road ?v ?r))
      (ready-road ?v ?next)
      (increase (travel-time ?v) (travel-duration ?r))
      (increase (sim-time) (travel-duration ?r))
    )
  )

  (:action traverse-road-static-road_0717-road_0216
    :parameters (?v - vehicle ?r - road_type_road_0717 ?next - road_type_road_0216)
    :precondition (and
      (ready-road ?v ?r)
      (road-next ?r ?next)
      (road-open ?r)
      (static-road ?r)
      (not (road-blocked ?r))
    )
    :effect (and
      (not (ready-road ?v ?r))
      (ready-road ?v ?next)
      (increase (travel-time ?v) (travel-duration ?r))
      (increase (sim-time) (travel-duration ?r))
    )
  )

  (:action traverse-road-static-road_0717-road_0217
    :parameters (?v - vehicle ?r - road_type_road_0717 ?next - road_type_road_0217)
    :precondition (and
      (ready-road ?v ?r)
      (road-next ?r ?next)
      (road-open ?r)
      (static-road ?r)
      (not (road-blocked ?r))
    )
    :effect (and
      (not (ready-road ?v ?r))
      (ready-road ?v ?next)
      (increase (travel-time ?v) (travel-duration ?r))
      (increase (sim-time) (travel-duration ?r))
    )
  )

  (:action traverse-road-static-road_0718-road_0581
    :parameters (?v - vehicle ?r - road_type_road_0718 ?next - road_type_road_0581)
    :precondition (and
      (ready-road ?v ?r)
      (road-next ?r ?next)
      (road-open ?r)
      (static-road ?r)
      (not (road-blocked ?r))
    )
    :effect (and
      (not (ready-road ?v ?r))
      (ready-road ?v ?next)
      (increase (travel-time ?v) (travel-duration ?r))
      (increase (sim-time) (travel-duration ?r))
    )
  )

  (:action traverse-road-static-road_0718-road_0582
    :parameters (?v - vehicle ?r - road_type_road_0718 ?next - road_type_road_0582)
    :precondition (and
      (ready-road ?v ?r)
      (road-next ?r ?next)
      (road-open ?r)
      (static-road ?r)
      (not (road-blocked ?r))
    )
    :effect (and
      (not (ready-road ?v ?r))
      (ready-road ?v ?next)
      (increase (travel-time ?v) (travel-duration ?r))
      (increase (sim-time) (travel-duration ?r))
    )
  )

  (:action traverse-road-static-road_0719-road_0550
    :parameters (?v - vehicle ?r - road_type_road_0719 ?next - road_type_road_0550)
    :precondition (and
      (ready-road ?v ?r)
      (road-next ?r ?next)
      (road-open ?r)
      (static-road ?r)
      (not (road-blocked ?r))
    )
    :effect (and
      (not (ready-road ?v ?r))
      (ready-road ?v ?next)
      (increase (travel-time ?v) (travel-duration ?r))
      (increase (sim-time) (travel-duration ?r))
    )
  )

  (:action traverse-road-static-road_0719-road_0551
    :parameters (?v - vehicle ?r - road_type_road_0719 ?next - road_type_road_0551)
    :precondition (and
      (ready-road ?v ?r)
      (road-next ?r ?next)
      (road-open ?r)
      (static-road ?r)
      (not (road-blocked ?r))
    )
    :effect (and
      (not (ready-road ?v ?r))
      (ready-road ?v ?next)
      (increase (travel-time ?v) (travel-duration ?r))
      (increase (sim-time) (travel-duration ?r))
    )
  )

  (:action traverse-road-static-road_0719-road_0552
    :parameters (?v - vehicle ?r - road_type_road_0719 ?next - road_type_road_0552)
    :precondition (and
      (ready-road ?v ?r)
      (road-next ?r ?next)
      (road-open ?r)
      (static-road ?r)
      (not (road-blocked ?r))
    )
    :effect (and
      (not (ready-road ?v ?r))
      (ready-road ?v ?next)
      (increase (travel-time ?v) (travel-duration ?r))
      (increase (sim-time) (travel-duration ?r))
    )
  )

  (:action traverse-road-static-road_0720-road_0544
    :parameters (?v - vehicle ?r - road_type_road_0720 ?next - road_type_road_0544)
    :precondition (and
      (ready-road ?v ?r)
      (road-next ?r ?next)
      (road-open ?r)
      (static-road ?r)
      (not (road-blocked ?r))
    )
    :effect (and
      (not (ready-road ?v ?r))
      (ready-road ?v ?next)
      (increase (travel-time ?v) (travel-duration ?r))
      (increase (sim-time) (travel-duration ?r))
    )
  )

  (:action traverse-road-static-road_0720-road_0545
    :parameters (?v - vehicle ?r - road_type_road_0720 ?next - road_type_road_0545)
    :precondition (and
      (ready-road ?v ?r)
      (road-next ?r ?next)
      (road-open ?r)
      (static-road ?r)
      (not (road-blocked ?r))
    )
    :effect (and
      (not (ready-road ?v ?r))
      (ready-road ?v ?next)
      (increase (travel-time ?v) (travel-duration ?r))
      (increase (sim-time) (travel-duration ?r))
    )
  )

  (:action traverse-road-static-road_0722-road_0456
    :parameters (?v - vehicle ?r - road_type_road_0722 ?next - road_type_road_0456)
    :precondition (and
      (ready-road ?v ?r)
      (road-next ?r ?next)
      (road-open ?r)
      (static-road ?r)
      (not (road-blocked ?r))
    )
    :effect (and
      (not (ready-road ?v ?r))
      (ready-road ?v ?next)
      (increase (travel-time ?v) (travel-duration ?r))
      (increase (sim-time) (travel-duration ?r))
    )
  )

  (:action traverse-road-static-road_0722-road_0457
    :parameters (?v - vehicle ?r - road_type_road_0722 ?next - road_type_road_0457)
    :precondition (and
      (ready-road ?v ?r)
      (road-next ?r ?next)
      (road-open ?r)
      (static-road ?r)
      (not (road-blocked ?r))
    )
    :effect (and
      (not (ready-road ?v ?r))
      (ready-road ?v ?next)
      (increase (travel-time ?v) (travel-duration ?r))
      (increase (sim-time) (travel-duration ?r))
    )
  )

  (:action traverse-road-static-road_0722-road_0458
    :parameters (?v - vehicle ?r - road_type_road_0722 ?next - road_type_road_0458)
    :precondition (and
      (ready-road ?v ?r)
      (road-next ?r ?next)
      (road-open ?r)
      (static-road ?r)
      (not (road-blocked ?r))
    )
    :effect (and
      (not (ready-road ?v ?r))
      (ready-road ?v ?next)
      (increase (travel-time ?v) (travel-duration ?r))
      (increase (sim-time) (travel-duration ?r))
    )
  )

  (:action traverse-road-static-road_0723-road_0426
    :parameters (?v - vehicle ?r - road_type_road_0723 ?next - road_type_road_0426)
    :precondition (and
      (ready-road ?v ?r)
      (road-next ?r ?next)
      (road-open ?r)
      (static-road ?r)
      (not (road-blocked ?r))
    )
    :effect (and
      (not (ready-road ?v ?r))
      (ready-road ?v ?next)
      (increase (travel-time ?v) (travel-duration ?r))
      (increase (sim-time) (travel-duration ?r))
    )
  )

  (:action traverse-road-static-road_0723-road_0427
    :parameters (?v - vehicle ?r - road_type_road_0723 ?next - road_type_road_0427)
    :precondition (and
      (ready-road ?v ?r)
      (road-next ?r ?next)
      (road-open ?r)
      (static-road ?r)
      (not (road-blocked ?r))
    )
    :effect (and
      (not (ready-road ?v ?r))
      (ready-road ?v ?next)
      (increase (travel-time ?v) (travel-duration ?r))
      (increase (sim-time) (travel-duration ?r))
    )
  )

  (:action traverse-road-static-road_0724-road_0485
    :parameters (?v - vehicle ?r - road_type_road_0724 ?next - road_type_road_0485)
    :precondition (and
      (ready-road ?v ?r)
      (road-next ?r ?next)
      (road-open ?r)
      (static-road ?r)
      (not (road-blocked ?r))
    )
    :effect (and
      (not (ready-road ?v ?r))
      (ready-road ?v ?next)
      (increase (travel-time ?v) (travel-duration ?r))
      (increase (sim-time) (travel-duration ?r))
    )
  )

  (:action traverse-road-static-road_0724-road_0486
    :parameters (?v - vehicle ?r - road_type_road_0724 ?next - road_type_road_0486)
    :precondition (and
      (ready-road ?v ?r)
      (road-next ?r ?next)
      (road-open ?r)
      (static-road ?r)
      (not (road-blocked ?r))
    )
    :effect (and
      (not (ready-road ?v ?r))
      (ready-road ?v ?next)
      (increase (travel-time ?v) (travel-duration ?r))
      (increase (sim-time) (travel-duration ?r))
    )
  )

  (:action traverse-road-static-road_0725-road_0759
    :parameters (?v - vehicle ?r - road_type_road_0725 ?next - road_type_road_0759)
    :precondition (and
      (ready-road ?v ?r)
      (road-next ?r ?next)
      (road-open ?r)
      (static-road ?r)
      (not (road-blocked ?r))
    )
    :effect (and
      (not (ready-road ?v ?r))
      (ready-road ?v ?next)
      (increase (travel-time ?v) (travel-duration ?r))
      (increase (sim-time) (travel-duration ?r))
    )
  )

  (:action traverse-road-static-road_0726-road_0110
    :parameters (?v - vehicle ?r - road_type_road_0726 ?next - road_type_road_0110)
    :precondition (and
      (ready-road ?v ?r)
      (road-next ?r ?next)
      (road-open ?r)
      (static-road ?r)
      (not (road-blocked ?r))
    )
    :effect (and
      (not (ready-road ?v ?r))
      (ready-road ?v ?next)
      (increase (travel-time ?v) (travel-duration ?r))
      (increase (sim-time) (travel-duration ?r))
    )
  )

  (:action traverse-road-static-road_0728-road_0075
    :parameters (?v - vehicle ?r - road_type_road_0728 ?next - road_type_road_0075)
    :precondition (and
      (ready-road ?v ?r)
      (road-next ?r ?next)
      (road-open ?r)
      (static-road ?r)
      (not (road-blocked ?r))
    )
    :effect (and
      (not (ready-road ?v ?r))
      (ready-road ?v ?next)
      (increase (travel-time ?v) (travel-duration ?r))
      (increase (sim-time) (travel-duration ?r))
    )
  )

  (:action traverse-road-static-road_0728-road_0076
    :parameters (?v - vehicle ?r - road_type_road_0728 ?next - road_type_road_0076)
    :precondition (and
      (ready-road ?v ?r)
      (road-next ?r ?next)
      (road-open ?r)
      (static-road ?r)
      (not (road-blocked ?r))
    )
    :effect (and
      (not (ready-road ?v ?r))
      (ready-road ?v ?next)
      (increase (travel-time ?v) (travel-duration ?r))
      (increase (sim-time) (travel-duration ?r))
    )
  )

  (:action traverse-road-static-road_0729-road_0042
    :parameters (?v - vehicle ?r - road_type_road_0729 ?next - road_type_road_0042)
    :precondition (and
      (ready-road ?v ?r)
      (road-next ?r ?next)
      (road-open ?r)
      (static-road ?r)
      (not (road-blocked ?r))
    )
    :effect (and
      (not (ready-road ?v ?r))
      (ready-road ?v ?next)
      (increase (travel-time ?v) (travel-duration ?r))
      (increase (sim-time) (travel-duration ?r))
    )
  )

  (:action traverse-road-static-road_0730-road_0728
    :parameters (?v - vehicle ?r - road_type_road_0730 ?next - road_type_road_0728)
    :precondition (and
      (ready-road ?v ?r)
      (road-next ?r ?next)
      (road-open ?r)
      (static-road ?r)
      (not (road-blocked ?r))
    )
    :effect (and
      (not (ready-road ?v ?r))
      (ready-road ?v ?next)
      (increase (travel-time ?v) (travel-duration ?r))
      (increase (sim-time) (travel-duration ?r))
    )
  )

  (:action traverse-road-static-road_0730-road_0729
    :parameters (?v - vehicle ?r - road_type_road_0730 ?next - road_type_road_0729)
    :precondition (and
      (ready-road ?v ?r)
      (road-next ?r ?next)
      (road-open ?r)
      (static-road ?r)
      (not (road-blocked ?r))
    )
    :effect (and
      (not (ready-road ?v ?r))
      (ready-road ?v ?next)
      (increase (travel-time ?v) (travel-duration ?r))
      (increase (sim-time) (travel-duration ?r))
    )
  )

  (:action traverse-road-static-road_0732-macro_0048
    :parameters (?v - vehicle ?r - road_type_road_0732 ?next - road_type_macro_0048)
    :precondition (and
      (ready-road ?v ?r)
      (road-next ?r ?next)
      (road-open ?r)
      (static-road ?r)
      (not (road-blocked ?r))
    )
    :effect (and
      (not (ready-road ?v ?r))
      (ready-road ?v ?next)
      (increase (travel-time ?v) (travel-duration ?r))
      (increase (sim-time) (travel-duration ?r))
    )
  )

  (:action traverse-road-static-road_0733-road_0043
    :parameters (?v - vehicle ?r - road_type_road_0733 ?next - road_type_road_0043)
    :precondition (and
      (ready-road ?v ?r)
      (road-next ?r ?next)
      (road-open ?r)
      (static-road ?r)
      (not (road-blocked ?r))
    )
    :effect (and
      (not (ready-road ?v ?r))
      (ready-road ?v ?next)
      (increase (travel-time ?v) (travel-duration ?r))
      (increase (sim-time) (travel-duration ?r))
    )
  )

  (:action traverse-road-static-road_0733-road_0044
    :parameters (?v - vehicle ?r - road_type_road_0733 ?next - road_type_road_0044)
    :precondition (and
      (ready-road ?v ?r)
      (road-next ?r ?next)
      (road-open ?r)
      (static-road ?r)
      (not (road-blocked ?r))
    )
    :effect (and
      (not (ready-road ?v ?r))
      (ready-road ?v ?next)
      (increase (travel-time ?v) (travel-duration ?r))
      (increase (sim-time) (travel-duration ?r))
    )
  )

  (:action traverse-road-static-road_0734-macro_0051
    :parameters (?v - vehicle ?r - road_type_road_0734 ?next - road_type_macro_0051)
    :precondition (and
      (ready-road ?v ?r)
      (road-next ?r ?next)
      (road-open ?r)
      (static-road ?r)
      (not (road-blocked ?r))
    )
    :effect (and
      (not (ready-road ?v ?r))
      (ready-road ?v ?next)
      (increase (travel-time ?v) (travel-duration ?r))
      (increase (sim-time) (travel-duration ?r))
    )
  )

  (:action traverse-road-static-road_0734-road_0789
    :parameters (?v - vehicle ?r - road_type_road_0734 ?next - road_type_road_0789)
    :precondition (and
      (ready-road ?v ?r)
      (road-next ?r ?next)
      (road-open ?r)
      (static-road ?r)
      (not (road-blocked ?r))
    )
    :effect (and
      (not (ready-road ?v ?r))
      (ready-road ?v ?next)
      (increase (travel-time ?v) (travel-duration ?r))
      (increase (sim-time) (travel-duration ?r))
    )
  )

  (:action traverse-road-static-road_0734-road_0790
    :parameters (?v - vehicle ?r - road_type_road_0734 ?next - road_type_road_0790)
    :precondition (and
      (ready-road ?v ?r)
      (road-next ?r ?next)
      (road-open ?r)
      (static-road ?r)
      (not (road-blocked ?r))
    )
    :effect (and
      (not (ready-road ?v ?r))
      (ready-road ?v ?next)
      (increase (travel-time ?v) (travel-duration ?r))
      (increase (sim-time) (travel-duration ?r))
    )
  )

  (:action traverse-road-static-road_0735-road_0338
    :parameters (?v - vehicle ?r - road_type_road_0735 ?next - road_type_road_0338)
    :precondition (and
      (ready-road ?v ?r)
      (road-next ?r ?next)
      (road-open ?r)
      (static-road ?r)
      (not (road-blocked ?r))
    )
    :effect (and
      (not (ready-road ?v ?r))
      (ready-road ?v ?next)
      (increase (travel-time ?v) (travel-duration ?r))
      (increase (sim-time) (travel-duration ?r))
    )
  )

  (:action traverse-road-static-road_0735-road_0339
    :parameters (?v - vehicle ?r - road_type_road_0735 ?next - road_type_road_0339)
    :precondition (and
      (ready-road ?v ?r)
      (road-next ?r ?next)
      (road-open ?r)
      (static-road ?r)
      (not (road-blocked ?r))
    )
    :effect (and
      (not (ready-road ?v ?r))
      (ready-road ?v ?next)
      (increase (travel-time ?v) (travel-duration ?r))
      (increase (sim-time) (travel-duration ?r))
    )
  )

  (:action traverse-road-dynamic-road_0736-road_0329-tw_00000
    :parameters (?v - vehicle ?r - road_type_road_0736 ?next - road_type_road_0329 ?w - window_type_tw_00000)
    :precondition (and
      (ready-road ?v ?r)
      (road-next ?r ?next)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
    )
    :effect (and
      (not (ready-road ?v ?r))
      (ready-road ?v ?next)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-road_0736-road_0329-tw_00030
    :parameters (?v - vehicle ?r - road_type_road_0736 ?next - road_type_road_0329 ?w - window_type_tw_00030)
    :precondition (and
      (ready-road ?v ?r)
      (road-next ?r ?next)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
    )
    :effect (and
      (not (ready-road ?v ?r))
      (ready-road ?v ?next)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-road_0736-road_0329-tw_00060
    :parameters (?v - vehicle ?r - road_type_road_0736 ?next - road_type_road_0329 ?w - window_type_tw_00060)
    :precondition (and
      (ready-road ?v ?r)
      (road-next ?r ?next)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
    )
    :effect (and
      (not (ready-road ?v ?r))
      (ready-road ?v ?next)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-road_0736-road_0329-tw_00090
    :parameters (?v - vehicle ?r - road_type_road_0736 ?next - road_type_road_0329 ?w - window_type_tw_00090)
    :precondition (and
      (ready-road ?v ?r)
      (road-next ?r ?next)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
    )
    :effect (and
      (not (ready-road ?v ?r))
      (ready-road ?v ?next)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-road_0736-road_0329-tw_00120
    :parameters (?v - vehicle ?r - road_type_road_0736 ?next - road_type_road_0329 ?w - window_type_tw_00120)
    :precondition (and
      (ready-road ?v ?r)
      (road-next ?r ?next)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
    )
    :effect (and
      (not (ready-road ?v ?r))
      (ready-road ?v ?next)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-road_0736-road_0329-tw_00150
    :parameters (?v - vehicle ?r - road_type_road_0736 ?next - road_type_road_0329 ?w - window_type_tw_00150)
    :precondition (and
      (ready-road ?v ?r)
      (road-next ?r ?next)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
    )
    :effect (and
      (not (ready-road ?v ?r))
      (ready-road ?v ?next)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-road_0736-road_0329-tw_00180
    :parameters (?v - vehicle ?r - road_type_road_0736 ?next - road_type_road_0329 ?w - window_type_tw_00180)
    :precondition (and
      (ready-road ?v ?r)
      (road-next ?r ?next)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
    )
    :effect (and
      (not (ready-road ?v ?r))
      (ready-road ?v ?next)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-road_0736-road_0329-tw_00210
    :parameters (?v - vehicle ?r - road_type_road_0736 ?next - road_type_road_0329 ?w - window_type_tw_00210)
    :precondition (and
      (ready-road ?v ?r)
      (road-next ?r ?next)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
    )
    :effect (and
      (not (ready-road ?v ?r))
      (ready-road ?v ?next)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-road_0736-road_0329-tw_00240
    :parameters (?v - vehicle ?r - road_type_road_0736 ?next - road_type_road_0329 ?w - window_type_tw_00240)
    :precondition (and
      (ready-road ?v ?r)
      (road-next ?r ?next)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
    )
    :effect (and
      (not (ready-road ?v ?r))
      (ready-road ?v ?next)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-road_0736-road_0329-tw_00270
    :parameters (?v - vehicle ?r - road_type_road_0736 ?next - road_type_road_0329 ?w - window_type_tw_00270)
    :precondition (and
      (ready-road ?v ?r)
      (road-next ?r ?next)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
    )
    :effect (and
      (not (ready-road ?v ?r))
      (ready-road ?v ?next)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-road_0736-road_0329-tw_00300
    :parameters (?v - vehicle ?r - road_type_road_0736 ?next - road_type_road_0329 ?w - window_type_tw_00300)
    :precondition (and
      (ready-road ?v ?r)
      (road-next ?r ?next)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
    )
    :effect (and
      (not (ready-road ?v ?r))
      (ready-road ?v ?next)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-road_0736-road_0329-tw_00330
    :parameters (?v - vehicle ?r - road_type_road_0736 ?next - road_type_road_0329 ?w - window_type_tw_00330)
    :precondition (and
      (ready-road ?v ?r)
      (road-next ?r ?next)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
    )
    :effect (and
      (not (ready-road ?v ?r))
      (ready-road ?v ?next)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-road_0736-road_0329-tw_00360
    :parameters (?v - vehicle ?r - road_type_road_0736 ?next - road_type_road_0329 ?w - window_type_tw_00360)
    :precondition (and
      (ready-road ?v ?r)
      (road-next ?r ?next)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
    )
    :effect (and
      (not (ready-road ?v ?r))
      (ready-road ?v ?next)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-road_0736-road_0329-tw_00390
    :parameters (?v - vehicle ?r - road_type_road_0736 ?next - road_type_road_0329 ?w - window_type_tw_00390)
    :precondition (and
      (ready-road ?v ?r)
      (road-next ?r ?next)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
    )
    :effect (and
      (not (ready-road ?v ?r))
      (ready-road ?v ?next)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-road_0736-road_0329-tw_00420
    :parameters (?v - vehicle ?r - road_type_road_0736 ?next - road_type_road_0329 ?w - window_type_tw_00420)
    :precondition (and
      (ready-road ?v ?r)
      (road-next ?r ?next)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
    )
    :effect (and
      (not (ready-road ?v ?r))
      (ready-road ?v ?next)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-road_0736-road_0329-tw_00450
    :parameters (?v - vehicle ?r - road_type_road_0736 ?next - road_type_road_0329 ?w - window_type_tw_00450)
    :precondition (and
      (ready-road ?v ?r)
      (road-next ?r ?next)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
    )
    :effect (and
      (not (ready-road ?v ?r))
      (ready-road ?v ?next)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-road_0736-road_0329-tw_00480
    :parameters (?v - vehicle ?r - road_type_road_0736 ?next - road_type_road_0329 ?w - window_type_tw_00480)
    :precondition (and
      (ready-road ?v ?r)
      (road-next ?r ?next)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
    )
    :effect (and
      (not (ready-road ?v ?r))
      (ready-road ?v ?next)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-road_0736-road_0329-tw_00510
    :parameters (?v - vehicle ?r - road_type_road_0736 ?next - road_type_road_0329 ?w - window_type_tw_00510)
    :precondition (and
      (ready-road ?v ?r)
      (road-next ?r ?next)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
    )
    :effect (and
      (not (ready-road ?v ?r))
      (ready-road ?v ?next)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-road_0736-road_0329-tw_00540
    :parameters (?v - vehicle ?r - road_type_road_0736 ?next - road_type_road_0329 ?w - window_type_tw_00540)
    :precondition (and
      (ready-road ?v ?r)
      (road-next ?r ?next)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
    )
    :effect (and
      (not (ready-road ?v ?r))
      (ready-road ?v ?next)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-road_0736-road_0329-tw_00570
    :parameters (?v - vehicle ?r - road_type_road_0736 ?next - road_type_road_0329 ?w - window_type_tw_00570)
    :precondition (and
      (ready-road ?v ?r)
      (road-next ?r ?next)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
    )
    :effect (and
      (not (ready-road ?v ?r))
      (ready-road ?v ?next)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-road_0736-road_0329-tw_00600
    :parameters (?v - vehicle ?r - road_type_road_0736 ?next - road_type_road_0329 ?w - window_type_tw_00600)
    :precondition (and
      (ready-road ?v ?r)
      (road-next ?r ?next)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
    )
    :effect (and
      (not (ready-road ?v ?r))
      (ready-road ?v ?next)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-road_0736-road_0330-tw_00000
    :parameters (?v - vehicle ?r - road_type_road_0736 ?next - road_type_road_0330 ?w - window_type_tw_00000)
    :precondition (and
      (ready-road ?v ?r)
      (road-next ?r ?next)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
    )
    :effect (and
      (not (ready-road ?v ?r))
      (ready-road ?v ?next)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-road_0736-road_0330-tw_00030
    :parameters (?v - vehicle ?r - road_type_road_0736 ?next - road_type_road_0330 ?w - window_type_tw_00030)
    :precondition (and
      (ready-road ?v ?r)
      (road-next ?r ?next)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
    )
    :effect (and
      (not (ready-road ?v ?r))
      (ready-road ?v ?next)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-road_0736-road_0330-tw_00060
    :parameters (?v - vehicle ?r - road_type_road_0736 ?next - road_type_road_0330 ?w - window_type_tw_00060)
    :precondition (and
      (ready-road ?v ?r)
      (road-next ?r ?next)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
    )
    :effect (and
      (not (ready-road ?v ?r))
      (ready-road ?v ?next)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-road_0736-road_0330-tw_00090
    :parameters (?v - vehicle ?r - road_type_road_0736 ?next - road_type_road_0330 ?w - window_type_tw_00090)
    :precondition (and
      (ready-road ?v ?r)
      (road-next ?r ?next)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
    )
    :effect (and
      (not (ready-road ?v ?r))
      (ready-road ?v ?next)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-road_0736-road_0330-tw_00120
    :parameters (?v - vehicle ?r - road_type_road_0736 ?next - road_type_road_0330 ?w - window_type_tw_00120)
    :precondition (and
      (ready-road ?v ?r)
      (road-next ?r ?next)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
    )
    :effect (and
      (not (ready-road ?v ?r))
      (ready-road ?v ?next)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-road_0736-road_0330-tw_00150
    :parameters (?v - vehicle ?r - road_type_road_0736 ?next - road_type_road_0330 ?w - window_type_tw_00150)
    :precondition (and
      (ready-road ?v ?r)
      (road-next ?r ?next)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
    )
    :effect (and
      (not (ready-road ?v ?r))
      (ready-road ?v ?next)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-road_0736-road_0330-tw_00180
    :parameters (?v - vehicle ?r - road_type_road_0736 ?next - road_type_road_0330 ?w - window_type_tw_00180)
    :precondition (and
      (ready-road ?v ?r)
      (road-next ?r ?next)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
    )
    :effect (and
      (not (ready-road ?v ?r))
      (ready-road ?v ?next)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-road_0736-road_0330-tw_00210
    :parameters (?v - vehicle ?r - road_type_road_0736 ?next - road_type_road_0330 ?w - window_type_tw_00210)
    :precondition (and
      (ready-road ?v ?r)
      (road-next ?r ?next)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
    )
    :effect (and
      (not (ready-road ?v ?r))
      (ready-road ?v ?next)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-road_0736-road_0330-tw_00240
    :parameters (?v - vehicle ?r - road_type_road_0736 ?next - road_type_road_0330 ?w - window_type_tw_00240)
    :precondition (and
      (ready-road ?v ?r)
      (road-next ?r ?next)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
    )
    :effect (and
      (not (ready-road ?v ?r))
      (ready-road ?v ?next)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-road_0736-road_0330-tw_00270
    :parameters (?v - vehicle ?r - road_type_road_0736 ?next - road_type_road_0330 ?w - window_type_tw_00270)
    :precondition (and
      (ready-road ?v ?r)
      (road-next ?r ?next)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
    )
    :effect (and
      (not (ready-road ?v ?r))
      (ready-road ?v ?next)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-road_0736-road_0330-tw_00300
    :parameters (?v - vehicle ?r - road_type_road_0736 ?next - road_type_road_0330 ?w - window_type_tw_00300)
    :precondition (and
      (ready-road ?v ?r)
      (road-next ?r ?next)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
    )
    :effect (and
      (not (ready-road ?v ?r))
      (ready-road ?v ?next)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-road_0736-road_0330-tw_00330
    :parameters (?v - vehicle ?r - road_type_road_0736 ?next - road_type_road_0330 ?w - window_type_tw_00330)
    :precondition (and
      (ready-road ?v ?r)
      (road-next ?r ?next)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
    )
    :effect (and
      (not (ready-road ?v ?r))
      (ready-road ?v ?next)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-road_0736-road_0330-tw_00360
    :parameters (?v - vehicle ?r - road_type_road_0736 ?next - road_type_road_0330 ?w - window_type_tw_00360)
    :precondition (and
      (ready-road ?v ?r)
      (road-next ?r ?next)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
    )
    :effect (and
      (not (ready-road ?v ?r))
      (ready-road ?v ?next)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-road_0736-road_0330-tw_00390
    :parameters (?v - vehicle ?r - road_type_road_0736 ?next - road_type_road_0330 ?w - window_type_tw_00390)
    :precondition (and
      (ready-road ?v ?r)
      (road-next ?r ?next)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
    )
    :effect (and
      (not (ready-road ?v ?r))
      (ready-road ?v ?next)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-road_0736-road_0330-tw_00420
    :parameters (?v - vehicle ?r - road_type_road_0736 ?next - road_type_road_0330 ?w - window_type_tw_00420)
    :precondition (and
      (ready-road ?v ?r)
      (road-next ?r ?next)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
    )
    :effect (and
      (not (ready-road ?v ?r))
      (ready-road ?v ?next)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-road_0736-road_0330-tw_00450
    :parameters (?v - vehicle ?r - road_type_road_0736 ?next - road_type_road_0330 ?w - window_type_tw_00450)
    :precondition (and
      (ready-road ?v ?r)
      (road-next ?r ?next)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
    )
    :effect (and
      (not (ready-road ?v ?r))
      (ready-road ?v ?next)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-road_0736-road_0330-tw_00480
    :parameters (?v - vehicle ?r - road_type_road_0736 ?next - road_type_road_0330 ?w - window_type_tw_00480)
    :precondition (and
      (ready-road ?v ?r)
      (road-next ?r ?next)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
    )
    :effect (and
      (not (ready-road ?v ?r))
      (ready-road ?v ?next)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-road_0736-road_0330-tw_00510
    :parameters (?v - vehicle ?r - road_type_road_0736 ?next - road_type_road_0330 ?w - window_type_tw_00510)
    :precondition (and
      (ready-road ?v ?r)
      (road-next ?r ?next)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
    )
    :effect (and
      (not (ready-road ?v ?r))
      (ready-road ?v ?next)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-road_0736-road_0330-tw_00540
    :parameters (?v - vehicle ?r - road_type_road_0736 ?next - road_type_road_0330 ?w - window_type_tw_00540)
    :precondition (and
      (ready-road ?v ?r)
      (road-next ?r ?next)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
    )
    :effect (and
      (not (ready-road ?v ?r))
      (ready-road ?v ?next)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-road_0736-road_0330-tw_00570
    :parameters (?v - vehicle ?r - road_type_road_0736 ?next - road_type_road_0330 ?w - window_type_tw_00570)
    :precondition (and
      (ready-road ?v ?r)
      (road-next ?r ?next)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
    )
    :effect (and
      (not (ready-road ?v ?r))
      (ready-road ?v ?next)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-road_0736-road_0330-tw_00600
    :parameters (?v - vehicle ?r - road_type_road_0736 ?next - road_type_road_0330 ?w - window_type_tw_00600)
    :precondition (and
      (ready-road ?v ?r)
      (road-next ?r ?next)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
    )
    :effect (and
      (not (ready-road ?v ?r))
      (ready-road ?v ?next)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-static-road_0738-road_0052
    :parameters (?v - vehicle ?r - road_type_road_0738 ?next - road_type_road_0052)
    :precondition (and
      (ready-road ?v ?r)
      (road-next ?r ?next)
      (road-open ?r)
      (static-road ?r)
      (not (road-blocked ?r))
    )
    :effect (and
      (not (ready-road ?v ?r))
      (ready-road ?v ?next)
      (increase (travel-time ?v) (travel-duration ?r))
      (increase (sim-time) (travel-duration ?r))
    )
  )

  (:action traverse-road-static-road_0739-road_0444
    :parameters (?v - vehicle ?r - road_type_road_0739 ?next - road_type_road_0444)
    :precondition (and
      (ready-road ?v ?r)
      (road-next ?r ?next)
      (road-open ?r)
      (static-road ?r)
      (not (road-blocked ?r))
    )
    :effect (and
      (not (ready-road ?v ?r))
      (ready-road ?v ?next)
      (increase (travel-time ?v) (travel-duration ?r))
      (increase (sim-time) (travel-duration ?r))
    )
  )

  (:action traverse-road-static-road_0739-road_0445
    :parameters (?v - vehicle ?r - road_type_road_0739 ?next - road_type_road_0445)
    :precondition (and
      (ready-road ?v ?r)
      (road-next ?r ?next)
      (road-open ?r)
      (static-road ?r)
      (not (road-blocked ?r))
    )
    :effect (and
      (not (ready-road ?v ?r))
      (ready-road ?v ?next)
      (increase (travel-time ?v) (travel-duration ?r))
      (increase (sim-time) (travel-duration ?r))
    )
  )

  (:action traverse-road-static-road_0739-road_0446
    :parameters (?v - vehicle ?r - road_type_road_0739 ?next - road_type_road_0446)
    :precondition (and
      (ready-road ?v ?r)
      (road-next ?r ?next)
      (road-open ?r)
      (static-road ?r)
      (not (road-blocked ?r))
    )
    :effect (and
      (not (ready-road ?v ?r))
      (ready-road ?v ?next)
      (increase (travel-time ?v) (travel-duration ?r))
      (increase (sim-time) (travel-duration ?r))
    )
  )

  (:action traverse-road-static-road_0740-road_0082
    :parameters (?v - vehicle ?r - road_type_road_0740 ?next - road_type_road_0082)
    :precondition (and
      (ready-road ?v ?r)
      (road-next ?r ?next)
      (road-open ?r)
      (static-road ?r)
      (not (road-blocked ?r))
    )
    :effect (and
      (not (ready-road ?v ?r))
      (ready-road ?v ?next)
      (increase (travel-time ?v) (travel-duration ?r))
      (increase (sim-time) (travel-duration ?r))
    )
  )

  (:action traverse-road-static-road_0741-road_0106
    :parameters (?v - vehicle ?r - road_type_road_0741 ?next - road_type_road_0106)
    :precondition (and
      (ready-road ?v ?r)
      (road-next ?r ?next)
      (road-open ?r)
      (static-road ?r)
      (not (road-blocked ?r))
    )
    :effect (and
      (not (ready-road ?v ?r))
      (ready-road ?v ?next)
      (increase (travel-time ?v) (travel-duration ?r))
      (increase (sim-time) (travel-duration ?r))
    )
  )

  (:action traverse-road-static-road_0741-road_0107
    :parameters (?v - vehicle ?r - road_type_road_0741 ?next - road_type_road_0107)
    :precondition (and
      (ready-road ?v ?r)
      (road-next ?r ?next)
      (road-open ?r)
      (static-road ?r)
      (not (road-blocked ?r))
    )
    :effect (and
      (not (ready-road ?v ?r))
      (ready-road ?v ?next)
      (increase (travel-time ?v) (travel-duration ?r))
      (increase (sim-time) (travel-duration ?r))
    )
  )

  (:action traverse-road-static-road_0742-road_0711
    :parameters (?v - vehicle ?r - road_type_road_0742 ?next - road_type_road_0711)
    :precondition (and
      (ready-road ?v ?r)
      (road-next ?r ?next)
      (road-open ?r)
      (static-road ?r)
      (not (road-blocked ?r))
    )
    :effect (and
      (not (ready-road ?v ?r))
      (ready-road ?v ?next)
      (increase (travel-time ?v) (travel-duration ?r))
      (increase (sim-time) (travel-duration ?r))
    )
  )

  (:action traverse-road-static-road_0742-road_0712
    :parameters (?v - vehicle ?r - road_type_road_0742 ?next - road_type_road_0712)
    :precondition (and
      (ready-road ?v ?r)
      (road-next ?r ?next)
      (road-open ?r)
      (static-road ?r)
      (not (road-blocked ?r))
    )
    :effect (and
      (not (ready-road ?v ?r))
      (ready-road ?v ?next)
      (increase (travel-time ?v) (travel-duration ?r))
      (increase (sim-time) (travel-duration ?r))
    )
  )

  (:action traverse-road-dynamic-road_0743-road_0758-tw_00000
    :parameters (?v - vehicle ?r - road_type_road_0743 ?next - road_type_road_0758 ?w - window_type_tw_00000)
    :precondition (and
      (ready-road ?v ?r)
      (road-next ?r ?next)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
    )
    :effect (and
      (not (ready-road ?v ?r))
      (ready-road ?v ?next)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-road_0743-road_0758-tw_00030
    :parameters (?v - vehicle ?r - road_type_road_0743 ?next - road_type_road_0758 ?w - window_type_tw_00030)
    :precondition (and
      (ready-road ?v ?r)
      (road-next ?r ?next)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
    )
    :effect (and
      (not (ready-road ?v ?r))
      (ready-road ?v ?next)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-road_0743-road_0758-tw_00060
    :parameters (?v - vehicle ?r - road_type_road_0743 ?next - road_type_road_0758 ?w - window_type_tw_00060)
    :precondition (and
      (ready-road ?v ?r)
      (road-next ?r ?next)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
    )
    :effect (and
      (not (ready-road ?v ?r))
      (ready-road ?v ?next)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-road_0743-road_0758-tw_00090
    :parameters (?v - vehicle ?r - road_type_road_0743 ?next - road_type_road_0758 ?w - window_type_tw_00090)
    :precondition (and
      (ready-road ?v ?r)
      (road-next ?r ?next)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
    )
    :effect (and
      (not (ready-road ?v ?r))
      (ready-road ?v ?next)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-road_0743-road_0758-tw_00120
    :parameters (?v - vehicle ?r - road_type_road_0743 ?next - road_type_road_0758 ?w - window_type_tw_00120)
    :precondition (and
      (ready-road ?v ?r)
      (road-next ?r ?next)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
    )
    :effect (and
      (not (ready-road ?v ?r))
      (ready-road ?v ?next)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-road_0743-road_0758-tw_00150
    :parameters (?v - vehicle ?r - road_type_road_0743 ?next - road_type_road_0758 ?w - window_type_tw_00150)
    :precondition (and
      (ready-road ?v ?r)
      (road-next ?r ?next)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
    )
    :effect (and
      (not (ready-road ?v ?r))
      (ready-road ?v ?next)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-road_0743-road_0758-tw_00180
    :parameters (?v - vehicle ?r - road_type_road_0743 ?next - road_type_road_0758 ?w - window_type_tw_00180)
    :precondition (and
      (ready-road ?v ?r)
      (road-next ?r ?next)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
    )
    :effect (and
      (not (ready-road ?v ?r))
      (ready-road ?v ?next)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-road_0743-road_0758-tw_00210
    :parameters (?v - vehicle ?r - road_type_road_0743 ?next - road_type_road_0758 ?w - window_type_tw_00210)
    :precondition (and
      (ready-road ?v ?r)
      (road-next ?r ?next)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
    )
    :effect (and
      (not (ready-road ?v ?r))
      (ready-road ?v ?next)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-road_0743-road_0758-tw_00240
    :parameters (?v - vehicle ?r - road_type_road_0743 ?next - road_type_road_0758 ?w - window_type_tw_00240)
    :precondition (and
      (ready-road ?v ?r)
      (road-next ?r ?next)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
    )
    :effect (and
      (not (ready-road ?v ?r))
      (ready-road ?v ?next)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-road_0743-road_0758-tw_00270
    :parameters (?v - vehicle ?r - road_type_road_0743 ?next - road_type_road_0758 ?w - window_type_tw_00270)
    :precondition (and
      (ready-road ?v ?r)
      (road-next ?r ?next)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
    )
    :effect (and
      (not (ready-road ?v ?r))
      (ready-road ?v ?next)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-road_0743-road_0758-tw_00300
    :parameters (?v - vehicle ?r - road_type_road_0743 ?next - road_type_road_0758 ?w - window_type_tw_00300)
    :precondition (and
      (ready-road ?v ?r)
      (road-next ?r ?next)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
    )
    :effect (and
      (not (ready-road ?v ?r))
      (ready-road ?v ?next)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-road_0743-road_0758-tw_00330
    :parameters (?v - vehicle ?r - road_type_road_0743 ?next - road_type_road_0758 ?w - window_type_tw_00330)
    :precondition (and
      (ready-road ?v ?r)
      (road-next ?r ?next)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
    )
    :effect (and
      (not (ready-road ?v ?r))
      (ready-road ?v ?next)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-road_0743-road_0758-tw_00360
    :parameters (?v - vehicle ?r - road_type_road_0743 ?next - road_type_road_0758 ?w - window_type_tw_00360)
    :precondition (and
      (ready-road ?v ?r)
      (road-next ?r ?next)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
    )
    :effect (and
      (not (ready-road ?v ?r))
      (ready-road ?v ?next)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-road_0743-road_0758-tw_00390
    :parameters (?v - vehicle ?r - road_type_road_0743 ?next - road_type_road_0758 ?w - window_type_tw_00390)
    :precondition (and
      (ready-road ?v ?r)
      (road-next ?r ?next)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
    )
    :effect (and
      (not (ready-road ?v ?r))
      (ready-road ?v ?next)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-road_0743-road_0758-tw_00420
    :parameters (?v - vehicle ?r - road_type_road_0743 ?next - road_type_road_0758 ?w - window_type_tw_00420)
    :precondition (and
      (ready-road ?v ?r)
      (road-next ?r ?next)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
    )
    :effect (and
      (not (ready-road ?v ?r))
      (ready-road ?v ?next)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-road_0743-road_0758-tw_00450
    :parameters (?v - vehicle ?r - road_type_road_0743 ?next - road_type_road_0758 ?w - window_type_tw_00450)
    :precondition (and
      (ready-road ?v ?r)
      (road-next ?r ?next)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
    )
    :effect (and
      (not (ready-road ?v ?r))
      (ready-road ?v ?next)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-road_0743-road_0758-tw_00480
    :parameters (?v - vehicle ?r - road_type_road_0743 ?next - road_type_road_0758 ?w - window_type_tw_00480)
    :precondition (and
      (ready-road ?v ?r)
      (road-next ?r ?next)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
    )
    :effect (and
      (not (ready-road ?v ?r))
      (ready-road ?v ?next)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-road_0743-road_0758-tw_00510
    :parameters (?v - vehicle ?r - road_type_road_0743 ?next - road_type_road_0758 ?w - window_type_tw_00510)
    :precondition (and
      (ready-road ?v ?r)
      (road-next ?r ?next)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
    )
    :effect (and
      (not (ready-road ?v ?r))
      (ready-road ?v ?next)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-road_0743-road_0758-tw_00540
    :parameters (?v - vehicle ?r - road_type_road_0743 ?next - road_type_road_0758 ?w - window_type_tw_00540)
    :precondition (and
      (ready-road ?v ?r)
      (road-next ?r ?next)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
    )
    :effect (and
      (not (ready-road ?v ?r))
      (ready-road ?v ?next)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-road_0743-road_0758-tw_00570
    :parameters (?v - vehicle ?r - road_type_road_0743 ?next - road_type_road_0758 ?w - window_type_tw_00570)
    :precondition (and
      (ready-road ?v ?r)
      (road-next ?r ?next)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
    )
    :effect (and
      (not (ready-road ?v ?r))
      (ready-road ?v ?next)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-road_0743-road_0758-tw_00600
    :parameters (?v - vehicle ?r - road_type_road_0743 ?next - road_type_road_0758 ?w - window_type_tw_00600)
    :precondition (and
      (ready-road ?v ?r)
      (road-next ?r ?next)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
    )
    :effect (and
      (not (ready-road ?v ?r))
      (ready-road ?v ?next)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-road_0744-road_0812-tw_00000
    :parameters (?v - vehicle ?r - road_type_road_0744 ?next - road_type_road_0812 ?w - window_type_tw_00000)
    :precondition (and
      (ready-road ?v ?r)
      (road-next ?r ?next)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
    )
    :effect (and
      (not (ready-road ?v ?r))
      (ready-road ?v ?next)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-road_0744-road_0812-tw_00030
    :parameters (?v - vehicle ?r - road_type_road_0744 ?next - road_type_road_0812 ?w - window_type_tw_00030)
    :precondition (and
      (ready-road ?v ?r)
      (road-next ?r ?next)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
    )
    :effect (and
      (not (ready-road ?v ?r))
      (ready-road ?v ?next)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-road_0744-road_0812-tw_00060
    :parameters (?v - vehicle ?r - road_type_road_0744 ?next - road_type_road_0812 ?w - window_type_tw_00060)
    :precondition (and
      (ready-road ?v ?r)
      (road-next ?r ?next)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
    )
    :effect (and
      (not (ready-road ?v ?r))
      (ready-road ?v ?next)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-road_0744-road_0812-tw_00090
    :parameters (?v - vehicle ?r - road_type_road_0744 ?next - road_type_road_0812 ?w - window_type_tw_00090)
    :precondition (and
      (ready-road ?v ?r)
      (road-next ?r ?next)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
    )
    :effect (and
      (not (ready-road ?v ?r))
      (ready-road ?v ?next)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-road_0744-road_0812-tw_00120
    :parameters (?v - vehicle ?r - road_type_road_0744 ?next - road_type_road_0812 ?w - window_type_tw_00120)
    :precondition (and
      (ready-road ?v ?r)
      (road-next ?r ?next)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
    )
    :effect (and
      (not (ready-road ?v ?r))
      (ready-road ?v ?next)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-road_0744-road_0812-tw_00150
    :parameters (?v - vehicle ?r - road_type_road_0744 ?next - road_type_road_0812 ?w - window_type_tw_00150)
    :precondition (and
      (ready-road ?v ?r)
      (road-next ?r ?next)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
    )
    :effect (and
      (not (ready-road ?v ?r))
      (ready-road ?v ?next)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-road_0744-road_0812-tw_00180
    :parameters (?v - vehicle ?r - road_type_road_0744 ?next - road_type_road_0812 ?w - window_type_tw_00180)
    :precondition (and
      (ready-road ?v ?r)
      (road-next ?r ?next)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
    )
    :effect (and
      (not (ready-road ?v ?r))
      (ready-road ?v ?next)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-road_0744-road_0812-tw_00210
    :parameters (?v - vehicle ?r - road_type_road_0744 ?next - road_type_road_0812 ?w - window_type_tw_00210)
    :precondition (and
      (ready-road ?v ?r)
      (road-next ?r ?next)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
    )
    :effect (and
      (not (ready-road ?v ?r))
      (ready-road ?v ?next)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-road_0744-road_0812-tw_00240
    :parameters (?v - vehicle ?r - road_type_road_0744 ?next - road_type_road_0812 ?w - window_type_tw_00240)
    :precondition (and
      (ready-road ?v ?r)
      (road-next ?r ?next)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
    )
    :effect (and
      (not (ready-road ?v ?r))
      (ready-road ?v ?next)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-road_0744-road_0812-tw_00270
    :parameters (?v - vehicle ?r - road_type_road_0744 ?next - road_type_road_0812 ?w - window_type_tw_00270)
    :precondition (and
      (ready-road ?v ?r)
      (road-next ?r ?next)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
    )
    :effect (and
      (not (ready-road ?v ?r))
      (ready-road ?v ?next)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-road_0744-road_0812-tw_00300
    :parameters (?v - vehicle ?r - road_type_road_0744 ?next - road_type_road_0812 ?w - window_type_tw_00300)
    :precondition (and
      (ready-road ?v ?r)
      (road-next ?r ?next)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
    )
    :effect (and
      (not (ready-road ?v ?r))
      (ready-road ?v ?next)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-road_0744-road_0812-tw_00330
    :parameters (?v - vehicle ?r - road_type_road_0744 ?next - road_type_road_0812 ?w - window_type_tw_00330)
    :precondition (and
      (ready-road ?v ?r)
      (road-next ?r ?next)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
    )
    :effect (and
      (not (ready-road ?v ?r))
      (ready-road ?v ?next)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-road_0744-road_0812-tw_00360
    :parameters (?v - vehicle ?r - road_type_road_0744 ?next - road_type_road_0812 ?w - window_type_tw_00360)
    :precondition (and
      (ready-road ?v ?r)
      (road-next ?r ?next)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
    )
    :effect (and
      (not (ready-road ?v ?r))
      (ready-road ?v ?next)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-road_0744-road_0812-tw_00390
    :parameters (?v - vehicle ?r - road_type_road_0744 ?next - road_type_road_0812 ?w - window_type_tw_00390)
    :precondition (and
      (ready-road ?v ?r)
      (road-next ?r ?next)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
    )
    :effect (and
      (not (ready-road ?v ?r))
      (ready-road ?v ?next)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-road_0744-road_0812-tw_00420
    :parameters (?v - vehicle ?r - road_type_road_0744 ?next - road_type_road_0812 ?w - window_type_tw_00420)
    :precondition (and
      (ready-road ?v ?r)
      (road-next ?r ?next)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
    )
    :effect (and
      (not (ready-road ?v ?r))
      (ready-road ?v ?next)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-road_0744-road_0812-tw_00450
    :parameters (?v - vehicle ?r - road_type_road_0744 ?next - road_type_road_0812 ?w - window_type_tw_00450)
    :precondition (and
      (ready-road ?v ?r)
      (road-next ?r ?next)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
    )
    :effect (and
      (not (ready-road ?v ?r))
      (ready-road ?v ?next)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-road_0744-road_0812-tw_00480
    :parameters (?v - vehicle ?r - road_type_road_0744 ?next - road_type_road_0812 ?w - window_type_tw_00480)
    :precondition (and
      (ready-road ?v ?r)
      (road-next ?r ?next)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
    )
    :effect (and
      (not (ready-road ?v ?r))
      (ready-road ?v ?next)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-road_0744-road_0812-tw_00510
    :parameters (?v - vehicle ?r - road_type_road_0744 ?next - road_type_road_0812 ?w - window_type_tw_00510)
    :precondition (and
      (ready-road ?v ?r)
      (road-next ?r ?next)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
    )
    :effect (and
      (not (ready-road ?v ?r))
      (ready-road ?v ?next)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-road_0744-road_0812-tw_00540
    :parameters (?v - vehicle ?r - road_type_road_0744 ?next - road_type_road_0812 ?w - window_type_tw_00540)
    :precondition (and
      (ready-road ?v ?r)
      (road-next ?r ?next)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
    )
    :effect (and
      (not (ready-road ?v ?r))
      (ready-road ?v ?next)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-road_0744-road_0812-tw_00570
    :parameters (?v - vehicle ?r - road_type_road_0744 ?next - road_type_road_0812 ?w - window_type_tw_00570)
    :precondition (and
      (ready-road ?v ?r)
      (road-next ?r ?next)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
    )
    :effect (and
      (not (ready-road ?v ?r))
      (ready-road ?v ?next)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-road_0744-road_0812-tw_00600
    :parameters (?v - vehicle ?r - road_type_road_0744 ?next - road_type_road_0812 ?w - window_type_tw_00600)
    :precondition (and
      (ready-road ?v ?r)
      (road-next ?r ?next)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
    )
    :effect (and
      (not (ready-road ?v ?r))
      (ready-road ?v ?next)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-road_0744-road_0813-tw_00000
    :parameters (?v - vehicle ?r - road_type_road_0744 ?next - road_type_road_0813 ?w - window_type_tw_00000)
    :precondition (and
      (ready-road ?v ?r)
      (road-next ?r ?next)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
    )
    :effect (and
      (not (ready-road ?v ?r))
      (ready-road ?v ?next)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-road_0744-road_0813-tw_00030
    :parameters (?v - vehicle ?r - road_type_road_0744 ?next - road_type_road_0813 ?w - window_type_tw_00030)
    :precondition (and
      (ready-road ?v ?r)
      (road-next ?r ?next)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
    )
    :effect (and
      (not (ready-road ?v ?r))
      (ready-road ?v ?next)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-road_0744-road_0813-tw_00060
    :parameters (?v - vehicle ?r - road_type_road_0744 ?next - road_type_road_0813 ?w - window_type_tw_00060)
    :precondition (and
      (ready-road ?v ?r)
      (road-next ?r ?next)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
    )
    :effect (and
      (not (ready-road ?v ?r))
      (ready-road ?v ?next)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-road_0744-road_0813-tw_00090
    :parameters (?v - vehicle ?r - road_type_road_0744 ?next - road_type_road_0813 ?w - window_type_tw_00090)
    :precondition (and
      (ready-road ?v ?r)
      (road-next ?r ?next)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
    )
    :effect (and
      (not (ready-road ?v ?r))
      (ready-road ?v ?next)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-road_0744-road_0813-tw_00120
    :parameters (?v - vehicle ?r - road_type_road_0744 ?next - road_type_road_0813 ?w - window_type_tw_00120)
    :precondition (and
      (ready-road ?v ?r)
      (road-next ?r ?next)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
    )
    :effect (and
      (not (ready-road ?v ?r))
      (ready-road ?v ?next)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-road_0744-road_0813-tw_00150
    :parameters (?v - vehicle ?r - road_type_road_0744 ?next - road_type_road_0813 ?w - window_type_tw_00150)
    :precondition (and
      (ready-road ?v ?r)
      (road-next ?r ?next)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
    )
    :effect (and
      (not (ready-road ?v ?r))
      (ready-road ?v ?next)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-road_0744-road_0813-tw_00180
    :parameters (?v - vehicle ?r - road_type_road_0744 ?next - road_type_road_0813 ?w - window_type_tw_00180)
    :precondition (and
      (ready-road ?v ?r)
      (road-next ?r ?next)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
    )
    :effect (and
      (not (ready-road ?v ?r))
      (ready-road ?v ?next)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-road_0744-road_0813-tw_00210
    :parameters (?v - vehicle ?r - road_type_road_0744 ?next - road_type_road_0813 ?w - window_type_tw_00210)
    :precondition (and
      (ready-road ?v ?r)
      (road-next ?r ?next)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
    )
    :effect (and
      (not (ready-road ?v ?r))
      (ready-road ?v ?next)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-road_0744-road_0813-tw_00240
    :parameters (?v - vehicle ?r - road_type_road_0744 ?next - road_type_road_0813 ?w - window_type_tw_00240)
    :precondition (and
      (ready-road ?v ?r)
      (road-next ?r ?next)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
    )
    :effect (and
      (not (ready-road ?v ?r))
      (ready-road ?v ?next)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-road_0744-road_0813-tw_00270
    :parameters (?v - vehicle ?r - road_type_road_0744 ?next - road_type_road_0813 ?w - window_type_tw_00270)
    :precondition (and
      (ready-road ?v ?r)
      (road-next ?r ?next)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
    )
    :effect (and
      (not (ready-road ?v ?r))
      (ready-road ?v ?next)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-road_0744-road_0813-tw_00300
    :parameters (?v - vehicle ?r - road_type_road_0744 ?next - road_type_road_0813 ?w - window_type_tw_00300)
    :precondition (and
      (ready-road ?v ?r)
      (road-next ?r ?next)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
    )
    :effect (and
      (not (ready-road ?v ?r))
      (ready-road ?v ?next)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-road_0744-road_0813-tw_00330
    :parameters (?v - vehicle ?r - road_type_road_0744 ?next - road_type_road_0813 ?w - window_type_tw_00330)
    :precondition (and
      (ready-road ?v ?r)
      (road-next ?r ?next)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
    )
    :effect (and
      (not (ready-road ?v ?r))
      (ready-road ?v ?next)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-road_0744-road_0813-tw_00360
    :parameters (?v - vehicle ?r - road_type_road_0744 ?next - road_type_road_0813 ?w - window_type_tw_00360)
    :precondition (and
      (ready-road ?v ?r)
      (road-next ?r ?next)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
    )
    :effect (and
      (not (ready-road ?v ?r))
      (ready-road ?v ?next)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-road_0744-road_0813-tw_00390
    :parameters (?v - vehicle ?r - road_type_road_0744 ?next - road_type_road_0813 ?w - window_type_tw_00390)
    :precondition (and
      (ready-road ?v ?r)
      (road-next ?r ?next)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
    )
    :effect (and
      (not (ready-road ?v ?r))
      (ready-road ?v ?next)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-road_0744-road_0813-tw_00420
    :parameters (?v - vehicle ?r - road_type_road_0744 ?next - road_type_road_0813 ?w - window_type_tw_00420)
    :precondition (and
      (ready-road ?v ?r)
      (road-next ?r ?next)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
    )
    :effect (and
      (not (ready-road ?v ?r))
      (ready-road ?v ?next)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-road_0744-road_0813-tw_00450
    :parameters (?v - vehicle ?r - road_type_road_0744 ?next - road_type_road_0813 ?w - window_type_tw_00450)
    :precondition (and
      (ready-road ?v ?r)
      (road-next ?r ?next)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
    )
    :effect (and
      (not (ready-road ?v ?r))
      (ready-road ?v ?next)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-road_0744-road_0813-tw_00480
    :parameters (?v - vehicle ?r - road_type_road_0744 ?next - road_type_road_0813 ?w - window_type_tw_00480)
    :precondition (and
      (ready-road ?v ?r)
      (road-next ?r ?next)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
    )
    :effect (and
      (not (ready-road ?v ?r))
      (ready-road ?v ?next)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-road_0744-road_0813-tw_00510
    :parameters (?v - vehicle ?r - road_type_road_0744 ?next - road_type_road_0813 ?w - window_type_tw_00510)
    :precondition (and
      (ready-road ?v ?r)
      (road-next ?r ?next)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
    )
    :effect (and
      (not (ready-road ?v ?r))
      (ready-road ?v ?next)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-road_0744-road_0813-tw_00540
    :parameters (?v - vehicle ?r - road_type_road_0744 ?next - road_type_road_0813 ?w - window_type_tw_00540)
    :precondition (and
      (ready-road ?v ?r)
      (road-next ?r ?next)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
    )
    :effect (and
      (not (ready-road ?v ?r))
      (ready-road ?v ?next)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-road_0744-road_0813-tw_00570
    :parameters (?v - vehicle ?r - road_type_road_0744 ?next - road_type_road_0813 ?w - window_type_tw_00570)
    :precondition (and
      (ready-road ?v ?r)
      (road-next ?r ?next)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
    )
    :effect (and
      (not (ready-road ?v ?r))
      (ready-road ?v ?next)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-road_0744-road_0813-tw_00600
    :parameters (?v - vehicle ?r - road_type_road_0744 ?next - road_type_road_0813 ?w - window_type_tw_00600)
    :precondition (and
      (ready-road ?v ?r)
      (road-next ?r ?next)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
    )
    :effect (and
      (not (ready-road ?v ?r))
      (ready-road ?v ?next)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-static-road_0745-road_0000
    :parameters (?v - vehicle ?r - road_type_road_0745 ?next - road_type_road_0000)
    :precondition (and
      (ready-road ?v ?r)
      (road-next ?r ?next)
      (road-open ?r)
      (static-road ?r)
      (not (road-blocked ?r))
    )
    :effect (and
      (not (ready-road ?v ?r))
      (ready-road ?v ?next)
      (increase (travel-time ?v) (travel-duration ?r))
      (increase (sim-time) (travel-duration ?r))
    )
  )

  (:action traverse-road-static-road_0745-road_0001
    :parameters (?v - vehicle ?r - road_type_road_0745 ?next - road_type_road_0001)
    :precondition (and
      (ready-road ?v ?r)
      (road-next ?r ?next)
      (road-open ?r)
      (static-road ?r)
      (not (road-blocked ?r))
    )
    :effect (and
      (not (ready-road ?v ?r))
      (ready-road ?v ?next)
      (increase (travel-time ?v) (travel-duration ?r))
      (increase (sim-time) (travel-duration ?r))
    )
  )

  (:action traverse-road-static-road_0746-road_0233
    :parameters (?v - vehicle ?r - road_type_road_0746 ?next - road_type_road_0233)
    :precondition (and
      (ready-road ?v ?r)
      (road-next ?r ?next)
      (road-open ?r)
      (static-road ?r)
      (not (road-blocked ?r))
    )
    :effect (and
      (not (ready-road ?v ?r))
      (ready-road ?v ?next)
      (increase (travel-time ?v) (travel-duration ?r))
      (increase (sim-time) (travel-duration ?r))
    )
  )

  (:action traverse-road-static-road_0746-road_0234
    :parameters (?v - vehicle ?r - road_type_road_0746 ?next - road_type_road_0234)
    :precondition (and
      (ready-road ?v ?r)
      (road-next ?r ?next)
      (road-open ?r)
      (static-road ?r)
      (not (road-blocked ?r))
    )
    :effect (and
      (not (ready-road ?v ?r))
      (ready-road ?v ?next)
      (increase (travel-time ?v) (travel-duration ?r))
      (increase (sim-time) (travel-duration ?r))
    )
  )

  (:action traverse-road-static-road_0747-road_0346
    :parameters (?v - vehicle ?r - road_type_road_0747 ?next - road_type_road_0346)
    :precondition (and
      (ready-road ?v ?r)
      (road-next ?r ?next)
      (road-open ?r)
      (static-road ?r)
      (not (road-blocked ?r))
    )
    :effect (and
      (not (ready-road ?v ?r))
      (ready-road ?v ?next)
      (increase (travel-time ?v) (travel-duration ?r))
      (increase (sim-time) (travel-duration ?r))
    )
  )

  (:action traverse-road-static-road_0747-road_0347
    :parameters (?v - vehicle ?r - road_type_road_0747 ?next - road_type_road_0347)
    :precondition (and
      (ready-road ?v ?r)
      (road-next ?r ?next)
      (road-open ?r)
      (static-road ?r)
      (not (road-blocked ?r))
    )
    :effect (and
      (not (ready-road ?v ?r))
      (ready-road ?v ?next)
      (increase (travel-time ?v) (travel-duration ?r))
      (increase (sim-time) (travel-duration ?r))
    )
  )

  (:action traverse-road-static-road_0749-road_0779
    :parameters (?v - vehicle ?r - road_type_road_0749 ?next - road_type_road_0779)
    :precondition (and
      (ready-road ?v ?r)
      (road-next ?r ?next)
      (road-open ?r)
      (static-road ?r)
      (not (road-blocked ?r))
    )
    :effect (and
      (not (ready-road ?v ?r))
      (ready-road ?v ?next)
      (increase (travel-time ?v) (travel-duration ?r))
      (increase (sim-time) (travel-duration ?r))
    )
  )

  (:action traverse-road-static-road_0749-road_0780
    :parameters (?v - vehicle ?r - road_type_road_0749 ?next - road_type_road_0780)
    :precondition (and
      (ready-road ?v ?r)
      (road-next ?r ?next)
      (road-open ?r)
      (static-road ?r)
      (not (road-blocked ?r))
    )
    :effect (and
      (not (ready-road ?v ?r))
      (ready-road ?v ?next)
      (increase (travel-time ?v) (travel-duration ?r))
      (increase (sim-time) (travel-duration ?r))
    )
  )

  (:action traverse-road-static-road_0750-road_0860
    :parameters (?v - vehicle ?r - road_type_road_0750 ?next - road_type_road_0860)
    :precondition (and
      (ready-road ?v ?r)
      (road-next ?r ?next)
      (road-open ?r)
      (static-road ?r)
      (not (road-blocked ?r))
    )
    :effect (and
      (not (ready-road ?v ?r))
      (ready-road ?v ?next)
      (increase (travel-time ?v) (travel-duration ?r))
      (increase (sim-time) (travel-duration ?r))
    )
  )

  (:action traverse-road-static-road_0750-road_0861
    :parameters (?v - vehicle ?r - road_type_road_0750 ?next - road_type_road_0861)
    :precondition (and
      (ready-road ?v ?r)
      (road-next ?r ?next)
      (road-open ?r)
      (static-road ?r)
      (not (road-blocked ?r))
    )
    :effect (and
      (not (ready-road ?v ?r))
      (ready-road ?v ?next)
      (increase (travel-time ?v) (travel-duration ?r))
      (increase (sim-time) (travel-duration ?r))
    )
  )

  (:action traverse-road-static-road_0751-road_0342
    :parameters (?v - vehicle ?r - road_type_road_0751 ?next - road_type_road_0342)
    :precondition (and
      (ready-road ?v ?r)
      (road-next ?r ?next)
      (road-open ?r)
      (static-road ?r)
      (not (road-blocked ?r))
    )
    :effect (and
      (not (ready-road ?v ?r))
      (ready-road ?v ?next)
      (increase (travel-time ?v) (travel-duration ?r))
      (increase (sim-time) (travel-duration ?r))
    )
  )

  (:action traverse-road-static-road_0752-road_0772
    :parameters (?v - vehicle ?r - road_type_road_0752 ?next - road_type_road_0772)
    :precondition (and
      (ready-road ?v ?r)
      (road-next ?r ?next)
      (road-open ?r)
      (static-road ?r)
      (not (road-blocked ?r))
    )
    :effect (and
      (not (ready-road ?v ?r))
      (ready-road ?v ?next)
      (increase (travel-time ?v) (travel-duration ?r))
      (increase (sim-time) (travel-duration ?r))
    )
  )

  (:action traverse-road-static-road_0752-road_0773
    :parameters (?v - vehicle ?r - road_type_road_0752 ?next - road_type_road_0773)
    :precondition (and
      (ready-road ?v ?r)
      (road-next ?r ?next)
      (road-open ?r)
      (static-road ?r)
      (not (road-blocked ?r))
    )
    :effect (and
      (not (ready-road ?v ?r))
      (ready-road ?v ?next)
      (increase (travel-time ?v) (travel-duration ?r))
      (increase (sim-time) (travel-duration ?r))
    )
  )

  (:action traverse-road-static-road_0753-road_0783
    :parameters (?v - vehicle ?r - road_type_road_0753 ?next - road_type_road_0783)
    :precondition (and
      (ready-road ?v ?r)
      (road-next ?r ?next)
      (road-open ?r)
      (static-road ?r)
      (not (road-blocked ?r))
    )
    :effect (and
      (not (ready-road ?v ?r))
      (ready-road ?v ?next)
      (increase (travel-time ?v) (travel-duration ?r))
      (increase (sim-time) (travel-duration ?r))
    )
  )

  (:action traverse-road-static-road_0753-road_0784
    :parameters (?v - vehicle ?r - road_type_road_0753 ?next - road_type_road_0784)
    :precondition (and
      (ready-road ?v ?r)
      (road-next ?r ?next)
      (road-open ?r)
      (static-road ?r)
      (not (road-blocked ?r))
    )
    :effect (and
      (not (ready-road ?v ?r))
      (ready-road ?v ?next)
      (increase (travel-time ?v) (travel-duration ?r))
      (increase (sim-time) (travel-duration ?r))
    )
  )

  (:action traverse-road-static-road_0754-macro_0005
    :parameters (?v - vehicle ?r - road_type_road_0754 ?next - road_type_macro_0005)
    :precondition (and
      (ready-road ?v ?r)
      (road-next ?r ?next)
      (road-open ?r)
      (static-road ?r)
      (not (road-blocked ?r))
    )
    :effect (and
      (not (ready-road ?v ?r))
      (ready-road ?v ?next)
      (increase (travel-time ?v) (travel-duration ?r))
      (increase (sim-time) (travel-duration ?r))
    )
  )

  (:action traverse-road-static-road_0754-road_0061
    :parameters (?v - vehicle ?r - road_type_road_0754 ?next - road_type_road_0061)
    :precondition (and
      (ready-road ?v ?r)
      (road-next ?r ?next)
      (road-open ?r)
      (static-road ?r)
      (not (road-blocked ?r))
    )
    :effect (and
      (not (ready-road ?v ?r))
      (ready-road ?v ?next)
      (increase (travel-time ?v) (travel-duration ?r))
      (increase (sim-time) (travel-duration ?r))
    )
  )

  (:action traverse-road-static-road_0756-road_0781
    :parameters (?v - vehicle ?r - road_type_road_0756 ?next - road_type_road_0781)
    :precondition (and
      (ready-road ?v ?r)
      (road-next ?r ?next)
      (road-open ?r)
      (static-road ?r)
      (not (road-blocked ?r))
    )
    :effect (and
      (not (ready-road ?v ?r))
      (ready-road ?v ?next)
      (increase (travel-time ?v) (travel-duration ?r))
      (increase (sim-time) (travel-duration ?r))
    )
  )

  (:action traverse-road-static-road_0756-road_0782
    :parameters (?v - vehicle ?r - road_type_road_0756 ?next - road_type_road_0782)
    :precondition (and
      (ready-road ?v ?r)
      (road-next ?r ?next)
      (road-open ?r)
      (static-road ?r)
      (not (road-blocked ?r))
    )
    :effect (and
      (not (ready-road ?v ?r))
      (ready-road ?v ?next)
      (increase (travel-time ?v) (travel-duration ?r))
      (increase (sim-time) (travel-duration ?r))
    )
  )

  (:action traverse-road-static-road_0757-road_0636
    :parameters (?v - vehicle ?r - road_type_road_0757 ?next - road_type_road_0636)
    :precondition (and
      (ready-road ?v ?r)
      (road-next ?r ?next)
      (road-open ?r)
      (static-road ?r)
      (not (road-blocked ?r))
    )
    :effect (and
      (not (ready-road ?v ?r))
      (ready-road ?v ?next)
      (increase (travel-time ?v) (travel-duration ?r))
      (increase (sim-time) (travel-duration ?r))
    )
  )

  (:action traverse-road-static-road_0757-road_0637
    :parameters (?v - vehicle ?r - road_type_road_0757 ?next - road_type_road_0637)
    :precondition (and
      (ready-road ?v ?r)
      (road-next ?r ?next)
      (road-open ?r)
      (static-road ?r)
      (not (road-blocked ?r))
    )
    :effect (and
      (not (ready-road ?v ?r))
      (ready-road ?v ?next)
      (increase (travel-time ?v) (travel-duration ?r))
      (increase (sim-time) (travel-duration ?r))
    )
  )

  (:action traverse-road-static-road_0758-road_0742
    :parameters (?v - vehicle ?r - road_type_road_0758 ?next - road_type_road_0742)
    :precondition (and
      (ready-road ?v ?r)
      (road-next ?r ?next)
      (road-open ?r)
      (static-road ?r)
      (not (road-blocked ?r))
    )
    :effect (and
      (not (ready-road ?v ?r))
      (ready-road ?v ?next)
      (increase (travel-time ?v) (travel-duration ?r))
      (increase (sim-time) (travel-duration ?r))
    )
  )

  (:action traverse-road-static-road_0758-road_0743
    :parameters (?v - vehicle ?r - road_type_road_0758 ?next - road_type_road_0743)
    :precondition (and
      (ready-road ?v ?r)
      (road-next ?r ?next)
      (road-open ?r)
      (static-road ?r)
      (not (road-blocked ?r))
    )
    :effect (and
      (not (ready-road ?v ?r))
      (ready-road ?v ?next)
      (increase (travel-time ?v) (travel-duration ?r))
      (increase (sim-time) (travel-duration ?r))
    )
  )

  (:action traverse-road-static-road_0759-road_0703
    :parameters (?v - vehicle ?r - road_type_road_0759 ?next - road_type_road_0703)
    :precondition (and
      (ready-road ?v ?r)
      (road-next ?r ?next)
      (road-open ?r)
      (static-road ?r)
      (not (road-blocked ?r))
    )
    :effect (and
      (not (ready-road ?v ?r))
      (ready-road ?v ?next)
      (increase (travel-time ?v) (travel-duration ?r))
      (increase (sim-time) (travel-duration ?r))
    )
  )

  (:action traverse-road-static-road_0759-road_0704
    :parameters (?v - vehicle ?r - road_type_road_0759 ?next - road_type_road_0704)
    :precondition (and
      (ready-road ?v ?r)
      (road-next ?r ?next)
      (road-open ?r)
      (static-road ?r)
      (not (road-blocked ?r))
    )
    :effect (and
      (not (ready-road ?v ?r))
      (ready-road ?v ?next)
      (increase (travel-time ?v) (travel-duration ?r))
      (increase (sim-time) (travel-duration ?r))
    )
  )

  (:action traverse-road-static-road_0760-road_0240
    :parameters (?v - vehicle ?r - road_type_road_0760 ?next - road_type_road_0240)
    :precondition (and
      (ready-road ?v ?r)
      (road-next ?r ?next)
      (road-open ?r)
      (static-road ?r)
      (not (road-blocked ?r))
    )
    :effect (and
      (not (ready-road ?v ?r))
      (ready-road ?v ?next)
      (increase (travel-time ?v) (travel-duration ?r))
      (increase (sim-time) (travel-duration ?r))
    )
  )

  (:action traverse-road-static-road_0760-road_0241
    :parameters (?v - vehicle ?r - road_type_road_0760 ?next - road_type_road_0241)
    :precondition (and
      (ready-road ?v ?r)
      (road-next ?r ?next)
      (road-open ?r)
      (static-road ?r)
      (not (road-blocked ?r))
    )
    :effect (and
      (not (ready-road ?v ?r))
      (ready-road ?v ?next)
      (increase (travel-time ?v) (travel-duration ?r))
      (increase (sim-time) (travel-duration ?r))
    )
  )

  (:action traverse-road-static-road_0761-road_0016
    :parameters (?v - vehicle ?r - road_type_road_0761 ?next - road_type_road_0016)
    :precondition (and
      (ready-road ?v ?r)
      (road-next ?r ?next)
      (road-open ?r)
      (static-road ?r)
      (not (road-blocked ?r))
    )
    :effect (and
      (not (ready-road ?v ?r))
      (ready-road ?v ?next)
      (increase (travel-time ?v) (travel-duration ?r))
      (increase (sim-time) (travel-duration ?r))
    )
  )

  (:action traverse-road-static-road_0761-road_0017
    :parameters (?v - vehicle ?r - road_type_road_0761 ?next - road_type_road_0017)
    :precondition (and
      (ready-road ?v ?r)
      (road-next ?r ?next)
      (road-open ?r)
      (static-road ?r)
      (not (road-blocked ?r))
    )
    :effect (and
      (not (ready-road ?v ?r))
      (ready-road ?v ?next)
      (increase (travel-time ?v) (travel-duration ?r))
      (increase (sim-time) (travel-duration ?r))
    )
  )

  (:action traverse-road-static-road_0762-macro_0050
    :parameters (?v - vehicle ?r - road_type_road_0762 ?next - road_type_macro_0050)
    :precondition (and
      (ready-road ?v ?r)
      (road-next ?r ?next)
      (road-open ?r)
      (static-road ?r)
      (not (road-blocked ?r))
    )
    :effect (and
      (not (ready-road ?v ?r))
      (ready-road ?v ?next)
      (increase (travel-time ?v) (travel-duration ?r))
      (increase (sim-time) (travel-duration ?r))
    )
  )

  (:action traverse-road-static-road_0762-road_0765
    :parameters (?v - vehicle ?r - road_type_road_0762 ?next - road_type_road_0765)
    :precondition (and
      (ready-road ?v ?r)
      (road-next ?r ?next)
      (road-open ?r)
      (static-road ?r)
      (not (road-blocked ?r))
    )
    :effect (and
      (not (ready-road ?v ?r))
      (ready-road ?v ?next)
      (increase (travel-time ?v) (travel-duration ?r))
      (increase (sim-time) (travel-duration ?r))
    )
  )

  (:action traverse-road-static-road_0763-road_0000
    :parameters (?v - vehicle ?r - road_type_road_0763 ?next - road_type_road_0000)
    :precondition (and
      (ready-road ?v ?r)
      (road-next ?r ?next)
      (road-open ?r)
      (static-road ?r)
      (not (road-blocked ?r))
    )
    :effect (and
      (not (ready-road ?v ?r))
      (ready-road ?v ?next)
      (increase (travel-time ?v) (travel-duration ?r))
      (increase (sim-time) (travel-duration ?r))
    )
  )

  (:action traverse-road-static-road_0763-road_0001
    :parameters (?v - vehicle ?r - road_type_road_0763 ?next - road_type_road_0001)
    :precondition (and
      (ready-road ?v ?r)
      (road-next ?r ?next)
      (road-open ?r)
      (static-road ?r)
      (not (road-blocked ?r))
    )
    :effect (and
      (not (ready-road ?v ?r))
      (ready-road ?v ?next)
      (increase (travel-time ?v) (travel-duration ?r))
      (increase (sim-time) (travel-duration ?r))
    )
  )

  (:action traverse-road-static-road_0765-road_0237
    :parameters (?v - vehicle ?r - road_type_road_0765 ?next - road_type_road_0237)
    :precondition (and
      (ready-road ?v ?r)
      (road-next ?r ?next)
      (road-open ?r)
      (static-road ?r)
      (not (road-blocked ?r))
    )
    :effect (and
      (not (ready-road ?v ?r))
      (ready-road ?v ?next)
      (increase (travel-time ?v) (travel-duration ?r))
      (increase (sim-time) (travel-duration ?r))
    )
  )

  (:action traverse-road-static-road_0765-road_0238
    :parameters (?v - vehicle ?r - road_type_road_0765 ?next - road_type_road_0238)
    :precondition (and
      (ready-road ?v ?r)
      (road-next ?r ?next)
      (road-open ?r)
      (static-road ?r)
      (not (road-blocked ?r))
    )
    :effect (and
      (not (ready-road ?v ?r))
      (ready-road ?v ?next)
      (increase (travel-time ?v) (travel-duration ?r))
      (increase (sim-time) (travel-duration ?r))
    )
  )

  (:action traverse-road-static-road_0766-road_0317
    :parameters (?v - vehicle ?r - road_type_road_0766 ?next - road_type_road_0317)
    :precondition (and
      (ready-road ?v ?r)
      (road-next ?r ?next)
      (road-open ?r)
      (static-road ?r)
      (not (road-blocked ?r))
    )
    :effect (and
      (not (ready-road ?v ?r))
      (ready-road ?v ?next)
      (increase (travel-time ?v) (travel-duration ?r))
      (increase (sim-time) (travel-duration ?r))
    )
  )

  (:action traverse-road-static-road_0767-macro_0029
    :parameters (?v - vehicle ?r - road_type_road_0767 ?next - road_type_macro_0029)
    :precondition (and
      (ready-road ?v ?r)
      (road-next ?r ?next)
      (road-open ?r)
      (static-road ?r)
      (not (road-blocked ?r))
    )
    :effect (and
      (not (ready-road ?v ?r))
      (ready-road ?v ?next)
      (increase (travel-time ?v) (travel-duration ?r))
      (increase (sim-time) (travel-duration ?r))
    )
  )

  (:action traverse-road-static-road_0767-road_0369
    :parameters (?v - vehicle ?r - road_type_road_0767 ?next - road_type_road_0369)
    :precondition (and
      (ready-road ?v ?r)
      (road-next ?r ?next)
      (road-open ?r)
      (static-road ?r)
      (not (road-blocked ?r))
    )
    :effect (and
      (not (ready-road ?v ?r))
      (ready-road ?v ?next)
      (increase (travel-time ?v) (travel-duration ?r))
      (increase (sim-time) (travel-duration ?r))
    )
  )

  (:action traverse-road-static-road_0768-road_0383
    :parameters (?v - vehicle ?r - road_type_road_0768 ?next - road_type_road_0383)
    :precondition (and
      (ready-road ?v ?r)
      (road-next ?r ?next)
      (road-open ?r)
      (static-road ?r)
      (not (road-blocked ?r))
    )
    :effect (and
      (not (ready-road ?v ?r))
      (ready-road ?v ?next)
      (increase (travel-time ?v) (travel-duration ?r))
      (increase (sim-time) (travel-duration ?r))
    )
  )

  (:action traverse-road-static-road_0768-road_0384
    :parameters (?v - vehicle ?r - road_type_road_0768 ?next - road_type_road_0384)
    :precondition (and
      (ready-road ?v ?r)
      (road-next ?r ?next)
      (road-open ?r)
      (static-road ?r)
      (not (road-blocked ?r))
    )
    :effect (and
      (not (ready-road ?v ?r))
      (ready-road ?v ?next)
      (increase (travel-time ?v) (travel-duration ?r))
      (increase (sim-time) (travel-duration ?r))
    )
  )

  (:action traverse-road-static-road_0769-road_0228
    :parameters (?v - vehicle ?r - road_type_road_0769 ?next - road_type_road_0228)
    :precondition (and
      (ready-road ?v ?r)
      (road-next ?r ?next)
      (road-open ?r)
      (static-road ?r)
      (not (road-blocked ?r))
    )
    :effect (and
      (not (ready-road ?v ?r))
      (ready-road ?v ?next)
      (increase (travel-time ?v) (travel-duration ?r))
      (increase (sim-time) (travel-duration ?r))
    )
  )

  (:action traverse-road-static-road_0770-road_0313
    :parameters (?v - vehicle ?r - road_type_road_0770 ?next - road_type_road_0313)
    :precondition (and
      (ready-road ?v ?r)
      (road-next ?r ?next)
      (road-open ?r)
      (static-road ?r)
      (not (road-blocked ?r))
    )
    :effect (and
      (not (ready-road ?v ?r))
      (ready-road ?v ?next)
      (increase (travel-time ?v) (travel-duration ?r))
      (increase (sim-time) (travel-duration ?r))
    )
  )

  (:action traverse-road-static-road_0770-road_0314
    :parameters (?v - vehicle ?r - road_type_road_0770 ?next - road_type_road_0314)
    :precondition (and
      (ready-road ?v ?r)
      (road-next ?r ?next)
      (road-open ?r)
      (static-road ?r)
      (not (road-blocked ?r))
    )
    :effect (and
      (not (ready-road ?v ?r))
      (ready-road ?v ?next)
      (increase (travel-time ?v) (travel-duration ?r))
      (increase (sim-time) (travel-duration ?r))
    )
  )

  (:action traverse-road-static-road_0771-road_0443
    :parameters (?v - vehicle ?r - road_type_road_0771 ?next - road_type_road_0443)
    :precondition (and
      (ready-road ?v ?r)
      (road-next ?r ?next)
      (road-open ?r)
      (static-road ?r)
      (not (road-blocked ?r))
    )
    :effect (and
      (not (ready-road ?v ?r))
      (ready-road ?v ?next)
      (increase (travel-time ?v) (travel-duration ?r))
      (increase (sim-time) (travel-duration ?r))
    )
  )

  (:action traverse-road-static-road_0772-road_0420
    :parameters (?v - vehicle ?r - road_type_road_0772 ?next - road_type_road_0420)
    :precondition (and
      (ready-road ?v ?r)
      (road-next ?r ?next)
      (road-open ?r)
      (static-road ?r)
      (not (road-blocked ?r))
    )
    :effect (and
      (not (ready-road ?v ?r))
      (ready-road ?v ?next)
      (increase (travel-time ?v) (travel-duration ?r))
      (increase (sim-time) (travel-duration ?r))
    )
  )

  (:action traverse-road-static-road_0772-road_0421
    :parameters (?v - vehicle ?r - road_type_road_0772 ?next - road_type_road_0421)
    :precondition (and
      (ready-road ?v ?r)
      (road-next ?r ?next)
      (road-open ?r)
      (static-road ?r)
      (not (road-blocked ?r))
    )
    :effect (and
      (not (ready-road ?v ?r))
      (ready-road ?v ?next)
      (increase (travel-time ?v) (travel-duration ?r))
      (increase (sim-time) (travel-duration ?r))
    )
  )

  (:action traverse-road-static-road_0772-road_0422
    :parameters (?v - vehicle ?r - road_type_road_0772 ?next - road_type_road_0422)
    :precondition (and
      (ready-road ?v ?r)
      (road-next ?r ?next)
      (road-open ?r)
      (static-road ?r)
      (not (road-blocked ?r))
    )
    :effect (and
      (not (ready-road ?v ?r))
      (ready-road ?v ?next)
      (increase (travel-time ?v) (travel-duration ?r))
      (increase (sim-time) (travel-duration ?r))
    )
  )

  (:action traverse-road-static-road_0773-road_0795
    :parameters (?v - vehicle ?r - road_type_road_0773 ?next - road_type_road_0795)
    :precondition (and
      (ready-road ?v ?r)
      (road-next ?r ?next)
      (road-open ?r)
      (static-road ?r)
      (not (road-blocked ?r))
    )
    :effect (and
      (not (ready-road ?v ?r))
      (ready-road ?v ?next)
      (increase (travel-time ?v) (travel-duration ?r))
      (increase (sim-time) (travel-duration ?r))
    )
  )

  (:action traverse-road-static-road_0773-road_0796
    :parameters (?v - vehicle ?r - road_type_road_0773 ?next - road_type_road_0796)
    :precondition (and
      (ready-road ?v ?r)
      (road-next ?r ?next)
      (road-open ?r)
      (static-road ?r)
      (not (road-blocked ?r))
    )
    :effect (and
      (not (ready-road ?v ?r))
      (ready-road ?v ?next)
      (increase (travel-time ?v) (travel-duration ?r))
      (increase (sim-time) (travel-duration ?r))
    )
  )

  (:action traverse-road-static-road_0773-road_0797
    :parameters (?v - vehicle ?r - road_type_road_0773 ?next - road_type_road_0797)
    :precondition (and
      (ready-road ?v ?r)
      (road-next ?r ?next)
      (road-open ?r)
      (static-road ?r)
      (not (road-blocked ?r))
    )
    :effect (and
      (not (ready-road ?v ?r))
      (ready-road ?v ?next)
      (increase (travel-time ?v) (travel-duration ?r))
      (increase (sim-time) (travel-duration ?r))
    )
  )

  (:action traverse-road-static-road_0774-road_0343
    :parameters (?v - vehicle ?r - road_type_road_0774 ?next - road_type_road_0343)
    :precondition (and
      (ready-road ?v ?r)
      (road-next ?r ?next)
      (road-open ?r)
      (static-road ?r)
      (not (road-blocked ?r))
    )
    :effect (and
      (not (ready-road ?v ?r))
      (ready-road ?v ?next)
      (increase (travel-time ?v) (travel-duration ?r))
      (increase (sim-time) (travel-duration ?r))
    )
  )

  (:action traverse-road-static-road_0775-road_0122
    :parameters (?v - vehicle ?r - road_type_road_0775 ?next - road_type_road_0122)
    :precondition (and
      (ready-road ?v ?r)
      (road-next ?r ?next)
      (road-open ?r)
      (static-road ?r)
      (not (road-blocked ?r))
    )
    :effect (and
      (not (ready-road ?v ?r))
      (ready-road ?v ?next)
      (increase (travel-time ?v) (travel-duration ?r))
      (increase (sim-time) (travel-duration ?r))
    )
  )

  (:action traverse-road-static-road_0776-macro_0014
    :parameters (?v - vehicle ?r - road_type_road_0776 ?next - road_type_macro_0014)
    :precondition (and
      (ready-road ?v ?r)
      (road-next ?r ?next)
      (road-open ?r)
      (static-road ?r)
      (not (road-blocked ?r))
    )
    :effect (and
      (not (ready-road ?v ?r))
      (ready-road ?v ?next)
      (increase (travel-time ?v) (travel-duration ?r))
      (increase (sim-time) (travel-duration ?r))
    )
  )

  (:action traverse-road-static-road_0776-macro_0015
    :parameters (?v - vehicle ?r - road_type_road_0776 ?next - road_type_macro_0015)
    :precondition (and
      (ready-road ?v ?r)
      (road-next ?r ?next)
      (road-open ?r)
      (static-road ?r)
      (not (road-blocked ?r))
    )
    :effect (and
      (not (ready-road ?v ?r))
      (ready-road ?v ?next)
      (increase (travel-time ?v) (travel-duration ?r))
      (increase (sim-time) (travel-duration ?r))
    )
  )

  (:action traverse-road-static-road_0777-road_0206
    :parameters (?v - vehicle ?r - road_type_road_0777 ?next - road_type_road_0206)
    :precondition (and
      (ready-road ?v ?r)
      (road-next ?r ?next)
      (road-open ?r)
      (static-road ?r)
      (not (road-blocked ?r))
    )
    :effect (and
      (not (ready-road ?v ?r))
      (ready-road ?v ?next)
      (increase (travel-time ?v) (travel-duration ?r))
      (increase (sim-time) (travel-duration ?r))
    )
  )

  (:action traverse-road-static-road_0778-road_0779
    :parameters (?v - vehicle ?r - road_type_road_0778 ?next - road_type_road_0779)
    :precondition (and
      (ready-road ?v ?r)
      (road-next ?r ?next)
      (road-open ?r)
      (static-road ?r)
      (not (road-blocked ?r))
    )
    :effect (and
      (not (ready-road ?v ?r))
      (ready-road ?v ?next)
      (increase (travel-time ?v) (travel-duration ?r))
      (increase (sim-time) (travel-duration ?r))
    )
  )

  (:action traverse-road-static-road_0778-road_0780
    :parameters (?v - vehicle ?r - road_type_road_0778 ?next - road_type_road_0780)
    :precondition (and
      (ready-road ?v ?r)
      (road-next ?r ?next)
      (road-open ?r)
      (static-road ?r)
      (not (road-blocked ?r))
    )
    :effect (and
      (not (ready-road ?v ?r))
      (ready-road ?v ?next)
      (increase (travel-time ?v) (travel-duration ?r))
      (increase (sim-time) (travel-duration ?r))
    )
  )

  (:action traverse-road-static-road_0779-road_0749
    :parameters (?v - vehicle ?r - road_type_road_0779 ?next - road_type_road_0749)
    :precondition (and
      (ready-road ?v ?r)
      (road-next ?r ?next)
      (road-open ?r)
      (static-road ?r)
      (not (road-blocked ?r))
    )
    :effect (and
      (not (ready-road ?v ?r))
      (ready-road ?v ?next)
      (increase (travel-time ?v) (travel-duration ?r))
      (increase (sim-time) (travel-duration ?r))
    )
  )

  (:action traverse-road-static-road_0779-road_0750
    :parameters (?v - vehicle ?r - road_type_road_0779 ?next - road_type_road_0750)
    :precondition (and
      (ready-road ?v ?r)
      (road-next ?r ?next)
      (road-open ?r)
      (static-road ?r)
      (not (road-blocked ?r))
    )
    :effect (and
      (not (ready-road ?v ?r))
      (ready-road ?v ?next)
      (increase (travel-time ?v) (travel-duration ?r))
      (increase (sim-time) (travel-duration ?r))
    )
  )

  (:action traverse-road-static-road_0779-road_0751
    :parameters (?v - vehicle ?r - road_type_road_0779 ?next - road_type_road_0751)
    :precondition (and
      (ready-road ?v ?r)
      (road-next ?r ?next)
      (road-open ?r)
      (static-road ?r)
      (not (road-blocked ?r))
    )
    :effect (and
      (not (ready-road ?v ?r))
      (ready-road ?v ?next)
      (increase (travel-time ?v) (travel-duration ?r))
      (increase (sim-time) (travel-duration ?r))
    )
  )

  (:action traverse-road-dynamic-road_0780-road_0235-tw_00000
    :parameters (?v - vehicle ?r - road_type_road_0780 ?next - road_type_road_0235 ?w - window_type_tw_00000)
    :precondition (and
      (ready-road ?v ?r)
      (road-next ?r ?next)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
    )
    :effect (and
      (not (ready-road ?v ?r))
      (ready-road ?v ?next)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-road_0780-road_0235-tw_00030
    :parameters (?v - vehicle ?r - road_type_road_0780 ?next - road_type_road_0235 ?w - window_type_tw_00030)
    :precondition (and
      (ready-road ?v ?r)
      (road-next ?r ?next)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
    )
    :effect (and
      (not (ready-road ?v ?r))
      (ready-road ?v ?next)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-road_0780-road_0235-tw_00060
    :parameters (?v - vehicle ?r - road_type_road_0780 ?next - road_type_road_0235 ?w - window_type_tw_00060)
    :precondition (and
      (ready-road ?v ?r)
      (road-next ?r ?next)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
    )
    :effect (and
      (not (ready-road ?v ?r))
      (ready-road ?v ?next)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-road_0780-road_0235-tw_00090
    :parameters (?v - vehicle ?r - road_type_road_0780 ?next - road_type_road_0235 ?w - window_type_tw_00090)
    :precondition (and
      (ready-road ?v ?r)
      (road-next ?r ?next)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
    )
    :effect (and
      (not (ready-road ?v ?r))
      (ready-road ?v ?next)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-road_0780-road_0235-tw_00120
    :parameters (?v - vehicle ?r - road_type_road_0780 ?next - road_type_road_0235 ?w - window_type_tw_00120)
    :precondition (and
      (ready-road ?v ?r)
      (road-next ?r ?next)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
    )
    :effect (and
      (not (ready-road ?v ?r))
      (ready-road ?v ?next)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-road_0780-road_0235-tw_00150
    :parameters (?v - vehicle ?r - road_type_road_0780 ?next - road_type_road_0235 ?w - window_type_tw_00150)
    :precondition (and
      (ready-road ?v ?r)
      (road-next ?r ?next)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
    )
    :effect (and
      (not (ready-road ?v ?r))
      (ready-road ?v ?next)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-road_0780-road_0235-tw_00180
    :parameters (?v - vehicle ?r - road_type_road_0780 ?next - road_type_road_0235 ?w - window_type_tw_00180)
    :precondition (and
      (ready-road ?v ?r)
      (road-next ?r ?next)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
    )
    :effect (and
      (not (ready-road ?v ?r))
      (ready-road ?v ?next)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-road_0780-road_0235-tw_00210
    :parameters (?v - vehicle ?r - road_type_road_0780 ?next - road_type_road_0235 ?w - window_type_tw_00210)
    :precondition (and
      (ready-road ?v ?r)
      (road-next ?r ?next)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
    )
    :effect (and
      (not (ready-road ?v ?r))
      (ready-road ?v ?next)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-road_0780-road_0235-tw_00240
    :parameters (?v - vehicle ?r - road_type_road_0780 ?next - road_type_road_0235 ?w - window_type_tw_00240)
    :precondition (and
      (ready-road ?v ?r)
      (road-next ?r ?next)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
    )
    :effect (and
      (not (ready-road ?v ?r))
      (ready-road ?v ?next)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-road_0780-road_0235-tw_00270
    :parameters (?v - vehicle ?r - road_type_road_0780 ?next - road_type_road_0235 ?w - window_type_tw_00270)
    :precondition (and
      (ready-road ?v ?r)
      (road-next ?r ?next)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
    )
    :effect (and
      (not (ready-road ?v ?r))
      (ready-road ?v ?next)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-road_0780-road_0235-tw_00300
    :parameters (?v - vehicle ?r - road_type_road_0780 ?next - road_type_road_0235 ?w - window_type_tw_00300)
    :precondition (and
      (ready-road ?v ?r)
      (road-next ?r ?next)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
    )
    :effect (and
      (not (ready-road ?v ?r))
      (ready-road ?v ?next)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-road_0780-road_0235-tw_00330
    :parameters (?v - vehicle ?r - road_type_road_0780 ?next - road_type_road_0235 ?w - window_type_tw_00330)
    :precondition (and
      (ready-road ?v ?r)
      (road-next ?r ?next)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
    )
    :effect (and
      (not (ready-road ?v ?r))
      (ready-road ?v ?next)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-road_0780-road_0235-tw_00360
    :parameters (?v - vehicle ?r - road_type_road_0780 ?next - road_type_road_0235 ?w - window_type_tw_00360)
    :precondition (and
      (ready-road ?v ?r)
      (road-next ?r ?next)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
    )
    :effect (and
      (not (ready-road ?v ?r))
      (ready-road ?v ?next)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-road_0780-road_0235-tw_00390
    :parameters (?v - vehicle ?r - road_type_road_0780 ?next - road_type_road_0235 ?w - window_type_tw_00390)
    :precondition (and
      (ready-road ?v ?r)
      (road-next ?r ?next)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
    )
    :effect (and
      (not (ready-road ?v ?r))
      (ready-road ?v ?next)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-road_0780-road_0235-tw_00420
    :parameters (?v - vehicle ?r - road_type_road_0780 ?next - road_type_road_0235 ?w - window_type_tw_00420)
    :precondition (and
      (ready-road ?v ?r)
      (road-next ?r ?next)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
    )
    :effect (and
      (not (ready-road ?v ?r))
      (ready-road ?v ?next)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-road_0780-road_0235-tw_00450
    :parameters (?v - vehicle ?r - road_type_road_0780 ?next - road_type_road_0235 ?w - window_type_tw_00450)
    :precondition (and
      (ready-road ?v ?r)
      (road-next ?r ?next)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
    )
    :effect (and
      (not (ready-road ?v ?r))
      (ready-road ?v ?next)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-road_0780-road_0235-tw_00480
    :parameters (?v - vehicle ?r - road_type_road_0780 ?next - road_type_road_0235 ?w - window_type_tw_00480)
    :precondition (and
      (ready-road ?v ?r)
      (road-next ?r ?next)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
    )
    :effect (and
      (not (ready-road ?v ?r))
      (ready-road ?v ?next)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-road_0780-road_0235-tw_00510
    :parameters (?v - vehicle ?r - road_type_road_0780 ?next - road_type_road_0235 ?w - window_type_tw_00510)
    :precondition (and
      (ready-road ?v ?r)
      (road-next ?r ?next)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
    )
    :effect (and
      (not (ready-road ?v ?r))
      (ready-road ?v ?next)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-road_0780-road_0235-tw_00540
    :parameters (?v - vehicle ?r - road_type_road_0780 ?next - road_type_road_0235 ?w - window_type_tw_00540)
    :precondition (and
      (ready-road ?v ?r)
      (road-next ?r ?next)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
    )
    :effect (and
      (not (ready-road ?v ?r))
      (ready-road ?v ?next)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-road_0780-road_0235-tw_00570
    :parameters (?v - vehicle ?r - road_type_road_0780 ?next - road_type_road_0235 ?w - window_type_tw_00570)
    :precondition (and
      (ready-road ?v ?r)
      (road-next ?r ?next)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
    )
    :effect (and
      (not (ready-road ?v ?r))
      (ready-road ?v ?next)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-road_0780-road_0235-tw_00600
    :parameters (?v - vehicle ?r - road_type_road_0780 ?next - road_type_road_0235 ?w - window_type_tw_00600)
    :precondition (and
      (ready-road ?v ?r)
      (road-next ?r ?next)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
    )
    :effect (and
      (not (ready-road ?v ?r))
      (ready-road ?v ?next)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-road_0780-road_0236-tw_00000
    :parameters (?v - vehicle ?r - road_type_road_0780 ?next - road_type_road_0236 ?w - window_type_tw_00000)
    :precondition (and
      (ready-road ?v ?r)
      (road-next ?r ?next)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
    )
    :effect (and
      (not (ready-road ?v ?r))
      (ready-road ?v ?next)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-road_0780-road_0236-tw_00030
    :parameters (?v - vehicle ?r - road_type_road_0780 ?next - road_type_road_0236 ?w - window_type_tw_00030)
    :precondition (and
      (ready-road ?v ?r)
      (road-next ?r ?next)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
    )
    :effect (and
      (not (ready-road ?v ?r))
      (ready-road ?v ?next)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-road_0780-road_0236-tw_00060
    :parameters (?v - vehicle ?r - road_type_road_0780 ?next - road_type_road_0236 ?w - window_type_tw_00060)
    :precondition (and
      (ready-road ?v ?r)
      (road-next ?r ?next)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
    )
    :effect (and
      (not (ready-road ?v ?r))
      (ready-road ?v ?next)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-road_0780-road_0236-tw_00090
    :parameters (?v - vehicle ?r - road_type_road_0780 ?next - road_type_road_0236 ?w - window_type_tw_00090)
    :precondition (and
      (ready-road ?v ?r)
      (road-next ?r ?next)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
    )
    :effect (and
      (not (ready-road ?v ?r))
      (ready-road ?v ?next)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-road_0780-road_0236-tw_00120
    :parameters (?v - vehicle ?r - road_type_road_0780 ?next - road_type_road_0236 ?w - window_type_tw_00120)
    :precondition (and
      (ready-road ?v ?r)
      (road-next ?r ?next)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
    )
    :effect (and
      (not (ready-road ?v ?r))
      (ready-road ?v ?next)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-road_0780-road_0236-tw_00150
    :parameters (?v - vehicle ?r - road_type_road_0780 ?next - road_type_road_0236 ?w - window_type_tw_00150)
    :precondition (and
      (ready-road ?v ?r)
      (road-next ?r ?next)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
    )
    :effect (and
      (not (ready-road ?v ?r))
      (ready-road ?v ?next)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-road_0780-road_0236-tw_00180
    :parameters (?v - vehicle ?r - road_type_road_0780 ?next - road_type_road_0236 ?w - window_type_tw_00180)
    :precondition (and
      (ready-road ?v ?r)
      (road-next ?r ?next)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
    )
    :effect (and
      (not (ready-road ?v ?r))
      (ready-road ?v ?next)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-road_0780-road_0236-tw_00210
    :parameters (?v - vehicle ?r - road_type_road_0780 ?next - road_type_road_0236 ?w - window_type_tw_00210)
    :precondition (and
      (ready-road ?v ?r)
      (road-next ?r ?next)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
    )
    :effect (and
      (not (ready-road ?v ?r))
      (ready-road ?v ?next)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-road_0780-road_0236-tw_00240
    :parameters (?v - vehicle ?r - road_type_road_0780 ?next - road_type_road_0236 ?w - window_type_tw_00240)
    :precondition (and
      (ready-road ?v ?r)
      (road-next ?r ?next)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
    )
    :effect (and
      (not (ready-road ?v ?r))
      (ready-road ?v ?next)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-road_0780-road_0236-tw_00270
    :parameters (?v - vehicle ?r - road_type_road_0780 ?next - road_type_road_0236 ?w - window_type_tw_00270)
    :precondition (and
      (ready-road ?v ?r)
      (road-next ?r ?next)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
    )
    :effect (and
      (not (ready-road ?v ?r))
      (ready-road ?v ?next)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-road_0780-road_0236-tw_00300
    :parameters (?v - vehicle ?r - road_type_road_0780 ?next - road_type_road_0236 ?w - window_type_tw_00300)
    :precondition (and
      (ready-road ?v ?r)
      (road-next ?r ?next)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
    )
    :effect (and
      (not (ready-road ?v ?r))
      (ready-road ?v ?next)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-road_0780-road_0236-tw_00330
    :parameters (?v - vehicle ?r - road_type_road_0780 ?next - road_type_road_0236 ?w - window_type_tw_00330)
    :precondition (and
      (ready-road ?v ?r)
      (road-next ?r ?next)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
    )
    :effect (and
      (not (ready-road ?v ?r))
      (ready-road ?v ?next)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-road_0780-road_0236-tw_00360
    :parameters (?v - vehicle ?r - road_type_road_0780 ?next - road_type_road_0236 ?w - window_type_tw_00360)
    :precondition (and
      (ready-road ?v ?r)
      (road-next ?r ?next)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
    )
    :effect (and
      (not (ready-road ?v ?r))
      (ready-road ?v ?next)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-road_0780-road_0236-tw_00390
    :parameters (?v - vehicle ?r - road_type_road_0780 ?next - road_type_road_0236 ?w - window_type_tw_00390)
    :precondition (and
      (ready-road ?v ?r)
      (road-next ?r ?next)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
    )
    :effect (and
      (not (ready-road ?v ?r))
      (ready-road ?v ?next)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-road_0780-road_0236-tw_00420
    :parameters (?v - vehicle ?r - road_type_road_0780 ?next - road_type_road_0236 ?w - window_type_tw_00420)
    :precondition (and
      (ready-road ?v ?r)
      (road-next ?r ?next)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
    )
    :effect (and
      (not (ready-road ?v ?r))
      (ready-road ?v ?next)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-road_0780-road_0236-tw_00450
    :parameters (?v - vehicle ?r - road_type_road_0780 ?next - road_type_road_0236 ?w - window_type_tw_00450)
    :precondition (and
      (ready-road ?v ?r)
      (road-next ?r ?next)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
    )
    :effect (and
      (not (ready-road ?v ?r))
      (ready-road ?v ?next)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-road_0780-road_0236-tw_00480
    :parameters (?v - vehicle ?r - road_type_road_0780 ?next - road_type_road_0236 ?w - window_type_tw_00480)
    :precondition (and
      (ready-road ?v ?r)
      (road-next ?r ?next)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
    )
    :effect (and
      (not (ready-road ?v ?r))
      (ready-road ?v ?next)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-road_0780-road_0236-tw_00510
    :parameters (?v - vehicle ?r - road_type_road_0780 ?next - road_type_road_0236 ?w - window_type_tw_00510)
    :precondition (and
      (ready-road ?v ?r)
      (road-next ?r ?next)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
    )
    :effect (and
      (not (ready-road ?v ?r))
      (ready-road ?v ?next)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-road_0780-road_0236-tw_00540
    :parameters (?v - vehicle ?r - road_type_road_0780 ?next - road_type_road_0236 ?w - window_type_tw_00540)
    :precondition (and
      (ready-road ?v ?r)
      (road-next ?r ?next)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
    )
    :effect (and
      (not (ready-road ?v ?r))
      (ready-road ?v ?next)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-road_0780-road_0236-tw_00570
    :parameters (?v - vehicle ?r - road_type_road_0780 ?next - road_type_road_0236 ?w - window_type_tw_00570)
    :precondition (and
      (ready-road ?v ?r)
      (road-next ?r ?next)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
    )
    :effect (and
      (not (ready-road ?v ?r))
      (ready-road ?v ?next)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-road_0780-road_0236-tw_00600
    :parameters (?v - vehicle ?r - road_type_road_0780 ?next - road_type_road_0236 ?w - window_type_tw_00600)
    :precondition (and
      (ready-road ?v ?r)
      (road-next ?r ?next)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
    )
    :effect (and
      (not (ready-road ?v ?r))
      (ready-road ?v ?next)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-static-road_0781-road_0723
    :parameters (?v - vehicle ?r - road_type_road_0781 ?next - road_type_road_0723)
    :precondition (and
      (ready-road ?v ?r)
      (road-next ?r ?next)
      (road-open ?r)
      (static-road ?r)
      (not (road-blocked ?r))
    )
    :effect (and
      (not (ready-road ?v ?r))
      (ready-road ?v ?next)
      (increase (travel-time ?v) (travel-duration ?r))
      (increase (sim-time) (travel-duration ?r))
    )
  )

  (:action traverse-road-static-road_0781-road_0724
    :parameters (?v - vehicle ?r - road_type_road_0781 ?next - road_type_road_0724)
    :precondition (and
      (ready-road ?v ?r)
      (road-next ?r ?next)
      (road-open ?r)
      (static-road ?r)
      (not (road-blocked ?r))
    )
    :effect (and
      (not (ready-road ?v ?r))
      (ready-road ?v ?next)
      (increase (travel-time ?v) (travel-duration ?r))
      (increase (sim-time) (travel-duration ?r))
    )
  )

  (:action traverse-road-static-road_0782-road_0029
    :parameters (?v - vehicle ?r - road_type_road_0782 ?next - road_type_road_0029)
    :precondition (and
      (ready-road ?v ?r)
      (road-next ?r ?next)
      (road-open ?r)
      (static-road ?r)
      (not (road-blocked ?r))
    )
    :effect (and
      (not (ready-road ?v ?r))
      (ready-road ?v ?next)
      (increase (travel-time ?v) (travel-duration ?r))
      (increase (sim-time) (travel-duration ?r))
    )
  )

  (:action traverse-road-static-road_0782-road_0030
    :parameters (?v - vehicle ?r - road_type_road_0782 ?next - road_type_road_0030)
    :precondition (and
      (ready-road ?v ?r)
      (road-next ?r ?next)
      (road-open ?r)
      (static-road ?r)
      (not (road-blocked ?r))
    )
    :effect (and
      (not (ready-road ?v ?r))
      (ready-road ?v ?next)
      (increase (travel-time ?v) (travel-duration ?r))
      (increase (sim-time) (travel-duration ?r))
    )
  )

  (:action traverse-road-static-road_0783-road_0798
    :parameters (?v - vehicle ?r - road_type_road_0783 ?next - road_type_road_0798)
    :precondition (and
      (ready-road ?v ?r)
      (road-next ?r ?next)
      (road-open ?r)
      (static-road ?r)
      (not (road-blocked ?r))
    )
    :effect (and
      (not (ready-road ?v ?r))
      (ready-road ?v ?next)
      (increase (travel-time ?v) (travel-duration ?r))
      (increase (sim-time) (travel-duration ?r))
    )
  )

  (:action traverse-road-static-road_0783-road_0799
    :parameters (?v - vehicle ?r - road_type_road_0783 ?next - road_type_road_0799)
    :precondition (and
      (ready-road ?v ?r)
      (road-next ?r ?next)
      (road-open ?r)
      (static-road ?r)
      (not (road-blocked ?r))
    )
    :effect (and
      (not (ready-road ?v ?r))
      (ready-road ?v ?next)
      (increase (travel-time ?v) (travel-duration ?r))
      (increase (sim-time) (travel-duration ?r))
    )
  )

  (:action traverse-road-static-road_0784-road_0348
    :parameters (?v - vehicle ?r - road_type_road_0784 ?next - road_type_road_0348)
    :precondition (and
      (ready-road ?v ?r)
      (road-next ?r ?next)
      (road-open ?r)
      (static-road ?r)
      (not (road-blocked ?r))
    )
    :effect (and
      (not (ready-road ?v ?r))
      (ready-road ?v ?next)
      (increase (travel-time ?v) (travel-duration ?r))
      (increase (sim-time) (travel-duration ?r))
    )
  )

  (:action traverse-road-static-road_0785-road_0756
    :parameters (?v - vehicle ?r - road_type_road_0785 ?next - road_type_road_0756)
    :precondition (and
      (ready-road ?v ?r)
      (road-next ?r ?next)
      (road-open ?r)
      (static-road ?r)
      (not (road-blocked ?r))
    )
    :effect (and
      (not (ready-road ?v ?r))
      (ready-road ?v ?next)
      (increase (travel-time ?v) (travel-duration ?r))
      (increase (sim-time) (travel-duration ?r))
    )
  )

  (:action traverse-road-static-road_0785-road_0757
    :parameters (?v - vehicle ?r - road_type_road_0785 ?next - road_type_road_0757)
    :precondition (and
      (ready-road ?v ?r)
      (road-next ?r ?next)
      (road-open ?r)
      (static-road ?r)
      (not (road-blocked ?r))
    )
    :effect (and
      (not (ready-road ?v ?r))
      (ready-road ?v ?next)
      (increase (travel-time ?v) (travel-duration ?r))
      (increase (sim-time) (travel-duration ?r))
    )
  )

  (:action traverse-road-static-road_0786-road_0471
    :parameters (?v - vehicle ?r - road_type_road_0786 ?next - road_type_road_0471)
    :precondition (and
      (ready-road ?v ?r)
      (road-next ?r ?next)
      (road-open ?r)
      (static-road ?r)
      (not (road-blocked ?r))
    )
    :effect (and
      (not (ready-road ?v ?r))
      (ready-road ?v ?next)
      (increase (travel-time ?v) (travel-duration ?r))
      (increase (sim-time) (travel-duration ?r))
    )
  )

  (:action traverse-road-static-road_0786-road_0472
    :parameters (?v - vehicle ?r - road_type_road_0786 ?next - road_type_road_0472)
    :precondition (and
      (ready-road ?v ?r)
      (road-next ?r ?next)
      (road-open ?r)
      (static-road ?r)
      (not (road-blocked ?r))
    )
    :effect (and
      (not (ready-road ?v ?r))
      (ready-road ?v ?next)
      (increase (travel-time ?v) (travel-duration ?r))
      (increase (sim-time) (travel-duration ?r))
    )
  )

  (:action traverse-road-static-road_0786-road_0473
    :parameters (?v - vehicle ?r - road_type_road_0786 ?next - road_type_road_0473)
    :precondition (and
      (ready-road ?v ?r)
      (road-next ?r ?next)
      (road-open ?r)
      (static-road ?r)
      (not (road-blocked ?r))
    )
    :effect (and
      (not (ready-road ?v ?r))
      (ready-road ?v ?next)
      (increase (travel-time ?v) (travel-duration ?r))
      (increase (sim-time) (travel-duration ?r))
    )
  )

  (:action traverse-road-static-road_0787-road_0024
    :parameters (?v - vehicle ?r - road_type_road_0787 ?next - road_type_road_0024)
    :precondition (and
      (ready-road ?v ?r)
      (road-next ?r ?next)
      (road-open ?r)
      (static-road ?r)
      (not (road-blocked ?r))
    )
    :effect (and
      (not (ready-road ?v ?r))
      (ready-road ?v ?next)
      (increase (travel-time ?v) (travel-duration ?r))
      (increase (sim-time) (travel-duration ?r))
    )
  )

  (:action traverse-road-static-road_0787-road_0025
    :parameters (?v - vehicle ?r - road_type_road_0787 ?next - road_type_road_0025)
    :precondition (and
      (ready-road ?v ?r)
      (road-next ?r ?next)
      (road-open ?r)
      (static-road ?r)
      (not (road-blocked ?r))
    )
    :effect (and
      (not (ready-road ?v ?r))
      (ready-road ?v ?next)
      (increase (travel-time ?v) (travel-duration ?r))
      (increase (sim-time) (travel-duration ?r))
    )
  )

  (:action traverse-road-static-road_0787-road_0026
    :parameters (?v - vehicle ?r - road_type_road_0787 ?next - road_type_road_0026)
    :precondition (and
      (ready-road ?v ?r)
      (road-next ?r ?next)
      (road-open ?r)
      (static-road ?r)
      (not (road-blocked ?r))
    )
    :effect (and
      (not (ready-road ?v ?r))
      (ready-road ?v ?next)
      (increase (travel-time ?v) (travel-duration ?r))
      (increase (sim-time) (travel-duration ?r))
    )
  )

  (:action traverse-road-static-road_0789-road_0643
    :parameters (?v - vehicle ?r - road_type_road_0789 ?next - road_type_road_0643)
    :precondition (and
      (ready-road ?v ?r)
      (road-next ?r ?next)
      (road-open ?r)
      (static-road ?r)
      (not (road-blocked ?r))
    )
    :effect (and
      (not (ready-road ?v ?r))
      (ready-road ?v ?next)
      (increase (travel-time ?v) (travel-duration ?r))
      (increase (sim-time) (travel-duration ?r))
    )
  )

  (:action traverse-road-static-road_0789-road_0644
    :parameters (?v - vehicle ?r - road_type_road_0789 ?next - road_type_road_0644)
    :precondition (and
      (ready-road ?v ?r)
      (road-next ?r ?next)
      (road-open ?r)
      (static-road ?r)
      (not (road-blocked ?r))
    )
    :effect (and
      (not (ready-road ?v ?r))
      (ready-road ?v ?next)
      (increase (travel-time ?v) (travel-duration ?r))
      (increase (sim-time) (travel-duration ?r))
    )
  )

  (:action traverse-road-static-road_0789-road_0645
    :parameters (?v - vehicle ?r - road_type_road_0789 ?next - road_type_road_0645)
    :precondition (and
      (ready-road ?v ?r)
      (road-next ?r ?next)
      (road-open ?r)
      (static-road ?r)
      (not (road-blocked ?r))
    )
    :effect (and
      (not (ready-road ?v ?r))
      (ready-road ?v ?next)
      (increase (travel-time ?v) (travel-duration ?r))
      (increase (sim-time) (travel-duration ?r))
    )
  )

  (:action traverse-road-static-road_0790-road_0791
    :parameters (?v - vehicle ?r - road_type_road_0790 ?next - road_type_road_0791)
    :precondition (and
      (ready-road ?v ?r)
      (road-next ?r ?next)
      (road-open ?r)
      (static-road ?r)
      (not (road-blocked ?r))
    )
    :effect (and
      (not (ready-road ?v ?r))
      (ready-road ?v ?next)
      (increase (travel-time ?v) (travel-duration ?r))
      (increase (sim-time) (travel-duration ?r))
    )
  )

  (:action traverse-road-static-road_0791-macro_0051
    :parameters (?v - vehicle ?r - road_type_road_0791 ?next - road_type_macro_0051)
    :precondition (and
      (ready-road ?v ?r)
      (road-next ?r ?next)
      (road-open ?r)
      (static-road ?r)
      (not (road-blocked ?r))
    )
    :effect (and
      (not (ready-road ?v ?r))
      (ready-road ?v ?next)
      (increase (travel-time ?v) (travel-duration ?r))
      (increase (sim-time) (travel-duration ?r))
    )
  )

  (:action traverse-road-static-road_0791-road_0789
    :parameters (?v - vehicle ?r - road_type_road_0791 ?next - road_type_road_0789)
    :precondition (and
      (ready-road ?v ?r)
      (road-next ?r ?next)
      (road-open ?r)
      (static-road ?r)
      (not (road-blocked ?r))
    )
    :effect (and
      (not (ready-road ?v ?r))
      (ready-road ?v ?next)
      (increase (travel-time ?v) (travel-duration ?r))
      (increase (sim-time) (travel-duration ?r))
    )
  )

  (:action traverse-road-static-road_0791-road_0790
    :parameters (?v - vehicle ?r - road_type_road_0791 ?next - road_type_road_0790)
    :precondition (and
      (ready-road ?v ?r)
      (road-next ?r ?next)
      (road-open ?r)
      (static-road ?r)
      (not (road-blocked ?r))
    )
    :effect (and
      (not (ready-road ?v ?r))
      (ready-road ?v ?next)
      (increase (travel-time ?v) (travel-duration ?r))
      (increase (sim-time) (travel-duration ?r))
    )
  )

  (:action traverse-road-static-road_0793-macro_0012
    :parameters (?v - vehicle ?r - road_type_road_0793 ?next - road_type_macro_0012)
    :precondition (and
      (ready-road ?v ?r)
      (road-next ?r ?next)
      (road-open ?r)
      (static-road ?r)
      (not (road-blocked ?r))
    )
    :effect (and
      (not (ready-road ?v ?r))
      (ready-road ?v ?next)
      (increase (travel-time ?v) (travel-duration ?r))
      (increase (sim-time) (travel-duration ?r))
    )
  )

  (:action traverse-road-static-road_0793-road_0129
    :parameters (?v - vehicle ?r - road_type_road_0793 ?next - road_type_road_0129)
    :precondition (and
      (ready-road ?v ?r)
      (road-next ?r ?next)
      (road-open ?r)
      (static-road ?r)
      (not (road-blocked ?r))
    )
    :effect (and
      (not (ready-road ?v ?r))
      (ready-road ?v ?next)
      (increase (travel-time ?v) (travel-duration ?r))
      (increase (sim-time) (travel-duration ?r))
    )
  )

  (:action traverse-road-static-road_0794-road_0602
    :parameters (?v - vehicle ?r - road_type_road_0794 ?next - road_type_road_0602)
    :precondition (and
      (ready-road ?v ?r)
      (road-next ?r ?next)
      (road-open ?r)
      (static-road ?r)
      (not (road-blocked ?r))
    )
    :effect (and
      (not (ready-road ?v ?r))
      (ready-road ?v ?next)
      (increase (travel-time ?v) (travel-duration ?r))
      (increase (sim-time) (travel-duration ?r))
    )
  )

  (:action traverse-road-static-road_0794-road_0603
    :parameters (?v - vehicle ?r - road_type_road_0794 ?next - road_type_road_0603)
    :precondition (and
      (ready-road ?v ?r)
      (road-next ?r ?next)
      (road-open ?r)
      (static-road ?r)
      (not (road-blocked ?r))
    )
    :effect (and
      (not (ready-road ?v ?r))
      (ready-road ?v ?next)
      (increase (travel-time ?v) (travel-duration ?r))
      (increase (sim-time) (travel-duration ?r))
    )
  )

  (:action traverse-road-static-road_0794-road_0604
    :parameters (?v - vehicle ?r - road_type_road_0794 ?next - road_type_road_0604)
    :precondition (and
      (ready-road ?v ?r)
      (road-next ?r ?next)
      (road-open ?r)
      (static-road ?r)
      (not (road-blocked ?r))
    )
    :effect (and
      (not (ready-road ?v ?r))
      (ready-road ?v ?next)
      (increase (travel-time ?v) (travel-duration ?r))
      (increase (sim-time) (travel-duration ?r))
    )
  )

  (:action traverse-road-static-road_0795-road_0193
    :parameters (?v - vehicle ?r - road_type_road_0795 ?next - road_type_road_0193)
    :precondition (and
      (ready-road ?v ?r)
      (road-next ?r ?next)
      (road-open ?r)
      (static-road ?r)
      (not (road-blocked ?r))
    )
    :effect (and
      (not (ready-road ?v ?r))
      (ready-road ?v ?next)
      (increase (travel-time ?v) (travel-duration ?r))
      (increase (sim-time) (travel-duration ?r))
    )
  )

  (:action traverse-road-static-road_0796-road_0752
    :parameters (?v - vehicle ?r - road_type_road_0796 ?next - road_type_road_0752)
    :precondition (and
      (ready-road ?v ?r)
      (road-next ?r ?next)
      (road-open ?r)
      (static-road ?r)
      (not (road-blocked ?r))
    )
    :effect (and
      (not (ready-road ?v ?r))
      (ready-road ?v ?next)
      (increase (travel-time ?v) (travel-duration ?r))
      (increase (sim-time) (travel-duration ?r))
    )
  )

  (:action traverse-road-static-road_0796-road_0753
    :parameters (?v - vehicle ?r - road_type_road_0796 ?next - road_type_road_0753)
    :precondition (and
      (ready-road ?v ?r)
      (road-next ?r ?next)
      (road-open ?r)
      (static-road ?r)
      (not (road-blocked ?r))
    )
    :effect (and
      (not (ready-road ?v ?r))
      (ready-road ?v ?next)
      (increase (travel-time ?v) (travel-duration ?r))
      (increase (sim-time) (travel-duration ?r))
    )
  )

  (:action traverse-road-static-road_0797-road_0772
    :parameters (?v - vehicle ?r - road_type_road_0797 ?next - road_type_road_0772)
    :precondition (and
      (ready-road ?v ?r)
      (road-next ?r ?next)
      (road-open ?r)
      (static-road ?r)
      (not (road-blocked ?r))
    )
    :effect (and
      (not (ready-road ?v ?r))
      (ready-road ?v ?next)
      (increase (travel-time ?v) (travel-duration ?r))
      (increase (sim-time) (travel-duration ?r))
    )
  )

  (:action traverse-road-static-road_0797-road_0773
    :parameters (?v - vehicle ?r - road_type_road_0797 ?next - road_type_road_0773)
    :precondition (and
      (ready-road ?v ?r)
      (road-next ?r ?next)
      (road-open ?r)
      (static-road ?r)
      (not (road-blocked ?r))
    )
    :effect (and
      (not (ready-road ?v ?r))
      (ready-road ?v ?next)
      (increase (travel-time ?v) (travel-duration ?r))
      (increase (sim-time) (travel-duration ?r))
    )
  )

  (:action traverse-road-static-road_0798-road_0370
    :parameters (?v - vehicle ?r - road_type_road_0798 ?next - road_type_road_0370)
    :precondition (and
      (ready-road ?v ?r)
      (road-next ?r ?next)
      (road-open ?r)
      (static-road ?r)
      (not (road-blocked ?r))
    )
    :effect (and
      (not (ready-road ?v ?r))
      (ready-road ?v ?next)
      (increase (travel-time ?v) (travel-duration ?r))
      (increase (sim-time) (travel-duration ?r))
    )
  )

  (:action traverse-road-static-road_0799-road_0210
    :parameters (?v - vehicle ?r - road_type_road_0799 ?next - road_type_road_0210)
    :precondition (and
      (ready-road ?v ?r)
      (road-next ?r ?next)
      (road-open ?r)
      (static-road ?r)
      (not (road-blocked ?r))
    )
    :effect (and
      (not (ready-road ?v ?r))
      (ready-road ?v ?next)
      (increase (travel-time ?v) (travel-duration ?r))
      (increase (sim-time) (travel-duration ?r))
    )
  )

  (:action traverse-road-static-road_0803-road_0378
    :parameters (?v - vehicle ?r - road_type_road_0803 ?next - road_type_road_0378)
    :precondition (and
      (ready-road ?v ?r)
      (road-next ?r ?next)
      (road-open ?r)
      (static-road ?r)
      (not (road-blocked ?r))
    )
    :effect (and
      (not (ready-road ?v ?r))
      (ready-road ?v ?next)
      (increase (travel-time ?v) (travel-duration ?r))
      (increase (sim-time) (travel-duration ?r))
    )
  )

  (:action traverse-road-static-road_0804-road_0361
    :parameters (?v - vehicle ?r - road_type_road_0804 ?next - road_type_road_0361)
    :precondition (and
      (ready-road ?v ?r)
      (road-next ?r ?next)
      (road-open ?r)
      (static-road ?r)
      (not (road-blocked ?r))
    )
    :effect (and
      (not (ready-road ?v ?r))
      (ready-road ?v ?next)
      (increase (travel-time ?v) (travel-duration ?r))
      (increase (sim-time) (travel-duration ?r))
    )
  )

  (:action traverse-road-static-road_0805-road_0178
    :parameters (?v - vehicle ?r - road_type_road_0805 ?next - road_type_road_0178)
    :precondition (and
      (ready-road ?v ?r)
      (road-next ?r ?next)
      (road-open ?r)
      (static-road ?r)
      (not (road-blocked ?r))
    )
    :effect (and
      (not (ready-road ?v ?r))
      (ready-road ?v ?next)
      (increase (travel-time ?v) (travel-duration ?r))
      (increase (sim-time) (travel-duration ?r))
    )
  )

  (:action traverse-road-static-road_0805-road_0179
    :parameters (?v - vehicle ?r - road_type_road_0805 ?next - road_type_road_0179)
    :precondition (and
      (ready-road ?v ?r)
      (road-next ?r ?next)
      (road-open ?r)
      (static-road ?r)
      (not (road-blocked ?r))
    )
    :effect (and
      (not (ready-road ?v ?r))
      (ready-road ?v ?next)
      (increase (travel-time ?v) (travel-duration ?r))
      (increase (sim-time) (travel-duration ?r))
    )
  )

  (:action traverse-road-static-road_0806-road_0700
    :parameters (?v - vehicle ?r - road_type_road_0806 ?next - road_type_road_0700)
    :precondition (and
      (ready-road ?v ?r)
      (road-next ?r ?next)
      (road-open ?r)
      (static-road ?r)
      (not (road-blocked ?r))
    )
    :effect (and
      (not (ready-road ?v ?r))
      (ready-road ?v ?next)
      (increase (travel-time ?v) (travel-duration ?r))
      (increase (sim-time) (travel-duration ?r))
    )
  )

  (:action traverse-road-static-road_0806-road_0701
    :parameters (?v - vehicle ?r - road_type_road_0806 ?next - road_type_road_0701)
    :precondition (and
      (ready-road ?v ?r)
      (road-next ?r ?next)
      (road-open ?r)
      (static-road ?r)
      (not (road-blocked ?r))
    )
    :effect (and
      (not (ready-road ?v ?r))
      (ready-road ?v ?next)
      (increase (travel-time ?v) (travel-duration ?r))
      (increase (sim-time) (travel-duration ?r))
    )
  )

  (:action traverse-road-static-road_0806-road_0702
    :parameters (?v - vehicle ?r - road_type_road_0806 ?next - road_type_road_0702)
    :precondition (and
      (ready-road ?v ?r)
      (road-next ?r ?next)
      (road-open ?r)
      (static-road ?r)
      (not (road-blocked ?r))
    )
    :effect (and
      (not (ready-road ?v ?r))
      (ready-road ?v ?next)
      (increase (travel-time ?v) (travel-duration ?r))
      (increase (sim-time) (travel-duration ?r))
    )
  )

  (:action traverse-road-static-road_0807-road_0320
    :parameters (?v - vehicle ?r - road_type_road_0807 ?next - road_type_road_0320)
    :precondition (and
      (ready-road ?v ?r)
      (road-next ?r ?next)
      (road-open ?r)
      (static-road ?r)
      (not (road-blocked ?r))
    )
    :effect (and
      (not (ready-road ?v ?r))
      (ready-road ?v ?next)
      (increase (travel-time ?v) (travel-duration ?r))
      (increase (sim-time) (travel-duration ?r))
    )
  )

  (:action traverse-road-static-road_0808-road_0290
    :parameters (?v - vehicle ?r - road_type_road_0808 ?next - road_type_road_0290)
    :precondition (and
      (ready-road ?v ?r)
      (road-next ?r ?next)
      (road-open ?r)
      (static-road ?r)
      (not (road-blocked ?r))
    )
    :effect (and
      (not (ready-road ?v ?r))
      (ready-road ?v ?next)
      (increase (travel-time ?v) (travel-duration ?r))
      (increase (sim-time) (travel-duration ?r))
    )
  )

  (:action traverse-road-static-road_0809-road_0354
    :parameters (?v - vehicle ?r - road_type_road_0809 ?next - road_type_road_0354)
    :precondition (and
      (ready-road ?v ?r)
      (road-next ?r ?next)
      (road-open ?r)
      (static-road ?r)
      (not (road-blocked ?r))
    )
    :effect (and
      (not (ready-road ?v ?r))
      (ready-road ?v ?next)
      (increase (travel-time ?v) (travel-duration ?r))
      (increase (sim-time) (travel-duration ?r))
    )
  )

  (:action traverse-road-static-road_0809-road_0355
    :parameters (?v - vehicle ?r - road_type_road_0809 ?next - road_type_road_0355)
    :precondition (and
      (ready-road ?v ?r)
      (road-next ?r ?next)
      (road-open ?r)
      (static-road ?r)
      (not (road-blocked ?r))
    )
    :effect (and
      (not (ready-road ?v ?r))
      (ready-road ?v ?next)
      (increase (travel-time ?v) (travel-duration ?r))
      (increase (sim-time) (travel-duration ?r))
    )
  )

  (:action traverse-road-static-road_0809-road_0356
    :parameters (?v - vehicle ?r - road_type_road_0809 ?next - road_type_road_0356)
    :precondition (and
      (ready-road ?v ?r)
      (road-next ?r ?next)
      (road-open ?r)
      (static-road ?r)
      (not (road-blocked ?r))
    )
    :effect (and
      (not (ready-road ?v ?r))
      (ready-road ?v ?next)
      (increase (travel-time ?v) (travel-duration ?r))
      (increase (sim-time) (travel-duration ?r))
    )
  )

  (:action traverse-road-static-road_0810-road_0361
    :parameters (?v - vehicle ?r - road_type_road_0810 ?next - road_type_road_0361)
    :precondition (and
      (ready-road ?v ?r)
      (road-next ?r ?next)
      (road-open ?r)
      (static-road ?r)
      (not (road-blocked ?r))
    )
    :effect (and
      (not (ready-road ?v ?r))
      (ready-road ?v ?next)
      (increase (travel-time ?v) (travel-duration ?r))
      (increase (sim-time) (travel-duration ?r))
    )
  )

  (:action traverse-road-static-road_0811-road_0385
    :parameters (?v - vehicle ?r - road_type_road_0811 ?next - road_type_road_0385)
    :precondition (and
      (ready-road ?v ?r)
      (road-next ?r ?next)
      (road-open ?r)
      (static-road ?r)
      (not (road-blocked ?r))
    )
    :effect (and
      (not (ready-road ?v ?r))
      (ready-road ?v ?next)
      (increase (travel-time ?v) (travel-duration ?r))
      (increase (sim-time) (travel-duration ?r))
    )
  )

  (:action traverse-road-static-road_0811-road_0386
    :parameters (?v - vehicle ?r - road_type_road_0811 ?next - road_type_road_0386)
    :precondition (and
      (ready-road ?v ?r)
      (road-next ?r ?next)
      (road-open ?r)
      (static-road ?r)
      (not (road-blocked ?r))
    )
    :effect (and
      (not (ready-road ?v ?r))
      (ready-road ?v ?next)
      (increase (travel-time ?v) (travel-duration ?r))
      (increase (sim-time) (travel-duration ?r))
    )
  )

  (:action traverse-road-dynamic-road_0812-macro_0027-tw_00000
    :parameters (?v - vehicle ?r - road_type_road_0812 ?next - road_type_macro_0027 ?w - window_type_tw_00000)
    :precondition (and
      (ready-road ?v ?r)
      (road-next ?r ?next)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
    )
    :effect (and
      (not (ready-road ?v ?r))
      (ready-road ?v ?next)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-road_0812-macro_0027-tw_00030
    :parameters (?v - vehicle ?r - road_type_road_0812 ?next - road_type_macro_0027 ?w - window_type_tw_00030)
    :precondition (and
      (ready-road ?v ?r)
      (road-next ?r ?next)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
    )
    :effect (and
      (not (ready-road ?v ?r))
      (ready-road ?v ?next)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-road_0812-macro_0027-tw_00060
    :parameters (?v - vehicle ?r - road_type_road_0812 ?next - road_type_macro_0027 ?w - window_type_tw_00060)
    :precondition (and
      (ready-road ?v ?r)
      (road-next ?r ?next)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
    )
    :effect (and
      (not (ready-road ?v ?r))
      (ready-road ?v ?next)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-road_0812-macro_0027-tw_00090
    :parameters (?v - vehicle ?r - road_type_road_0812 ?next - road_type_macro_0027 ?w - window_type_tw_00090)
    :precondition (and
      (ready-road ?v ?r)
      (road-next ?r ?next)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
    )
    :effect (and
      (not (ready-road ?v ?r))
      (ready-road ?v ?next)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-road_0812-macro_0027-tw_00120
    :parameters (?v - vehicle ?r - road_type_road_0812 ?next - road_type_macro_0027 ?w - window_type_tw_00120)
    :precondition (and
      (ready-road ?v ?r)
      (road-next ?r ?next)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
    )
    :effect (and
      (not (ready-road ?v ?r))
      (ready-road ?v ?next)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-road_0812-macro_0027-tw_00150
    :parameters (?v - vehicle ?r - road_type_road_0812 ?next - road_type_macro_0027 ?w - window_type_tw_00150)
    :precondition (and
      (ready-road ?v ?r)
      (road-next ?r ?next)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
    )
    :effect (and
      (not (ready-road ?v ?r))
      (ready-road ?v ?next)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-road_0812-macro_0027-tw_00180
    :parameters (?v - vehicle ?r - road_type_road_0812 ?next - road_type_macro_0027 ?w - window_type_tw_00180)
    :precondition (and
      (ready-road ?v ?r)
      (road-next ?r ?next)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
    )
    :effect (and
      (not (ready-road ?v ?r))
      (ready-road ?v ?next)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-road_0812-macro_0027-tw_00210
    :parameters (?v - vehicle ?r - road_type_road_0812 ?next - road_type_macro_0027 ?w - window_type_tw_00210)
    :precondition (and
      (ready-road ?v ?r)
      (road-next ?r ?next)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
    )
    :effect (and
      (not (ready-road ?v ?r))
      (ready-road ?v ?next)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-road_0812-macro_0027-tw_00240
    :parameters (?v - vehicle ?r - road_type_road_0812 ?next - road_type_macro_0027 ?w - window_type_tw_00240)
    :precondition (and
      (ready-road ?v ?r)
      (road-next ?r ?next)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
    )
    :effect (and
      (not (ready-road ?v ?r))
      (ready-road ?v ?next)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-road_0812-macro_0027-tw_00270
    :parameters (?v - vehicle ?r - road_type_road_0812 ?next - road_type_macro_0027 ?w - window_type_tw_00270)
    :precondition (and
      (ready-road ?v ?r)
      (road-next ?r ?next)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
    )
    :effect (and
      (not (ready-road ?v ?r))
      (ready-road ?v ?next)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-road_0812-macro_0027-tw_00300
    :parameters (?v - vehicle ?r - road_type_road_0812 ?next - road_type_macro_0027 ?w - window_type_tw_00300)
    :precondition (and
      (ready-road ?v ?r)
      (road-next ?r ?next)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
    )
    :effect (and
      (not (ready-road ?v ?r))
      (ready-road ?v ?next)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-road_0812-macro_0027-tw_00330
    :parameters (?v - vehicle ?r - road_type_road_0812 ?next - road_type_macro_0027 ?w - window_type_tw_00330)
    :precondition (and
      (ready-road ?v ?r)
      (road-next ?r ?next)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
    )
    :effect (and
      (not (ready-road ?v ?r))
      (ready-road ?v ?next)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-road_0812-macro_0027-tw_00360
    :parameters (?v - vehicle ?r - road_type_road_0812 ?next - road_type_macro_0027 ?w - window_type_tw_00360)
    :precondition (and
      (ready-road ?v ?r)
      (road-next ?r ?next)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
    )
    :effect (and
      (not (ready-road ?v ?r))
      (ready-road ?v ?next)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-road_0812-macro_0027-tw_00390
    :parameters (?v - vehicle ?r - road_type_road_0812 ?next - road_type_macro_0027 ?w - window_type_tw_00390)
    :precondition (and
      (ready-road ?v ?r)
      (road-next ?r ?next)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
    )
    :effect (and
      (not (ready-road ?v ?r))
      (ready-road ?v ?next)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-road_0812-macro_0027-tw_00420
    :parameters (?v - vehicle ?r - road_type_road_0812 ?next - road_type_macro_0027 ?w - window_type_tw_00420)
    :precondition (and
      (ready-road ?v ?r)
      (road-next ?r ?next)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
    )
    :effect (and
      (not (ready-road ?v ?r))
      (ready-road ?v ?next)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-road_0812-macro_0027-tw_00450
    :parameters (?v - vehicle ?r - road_type_road_0812 ?next - road_type_macro_0027 ?w - window_type_tw_00450)
    :precondition (and
      (ready-road ?v ?r)
      (road-next ?r ?next)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
    )
    :effect (and
      (not (ready-road ?v ?r))
      (ready-road ?v ?next)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-road_0812-macro_0027-tw_00480
    :parameters (?v - vehicle ?r - road_type_road_0812 ?next - road_type_macro_0027 ?w - window_type_tw_00480)
    :precondition (and
      (ready-road ?v ?r)
      (road-next ?r ?next)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
    )
    :effect (and
      (not (ready-road ?v ?r))
      (ready-road ?v ?next)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-road_0812-macro_0027-tw_00510
    :parameters (?v - vehicle ?r - road_type_road_0812 ?next - road_type_macro_0027 ?w - window_type_tw_00510)
    :precondition (and
      (ready-road ?v ?r)
      (road-next ?r ?next)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
    )
    :effect (and
      (not (ready-road ?v ?r))
      (ready-road ?v ?next)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-road_0812-macro_0027-tw_00540
    :parameters (?v - vehicle ?r - road_type_road_0812 ?next - road_type_macro_0027 ?w - window_type_tw_00540)
    :precondition (and
      (ready-road ?v ?r)
      (road-next ?r ?next)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
    )
    :effect (and
      (not (ready-road ?v ?r))
      (ready-road ?v ?next)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-road_0812-macro_0027-tw_00570
    :parameters (?v - vehicle ?r - road_type_road_0812 ?next - road_type_macro_0027 ?w - window_type_tw_00570)
    :precondition (and
      (ready-road ?v ?r)
      (road-next ?r ?next)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
    )
    :effect (and
      (not (ready-road ?v ?r))
      (ready-road ?v ?next)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-road_0812-macro_0027-tw_00600
    :parameters (?v - vehicle ?r - road_type_road_0812 ?next - road_type_macro_0027 ?w - window_type_tw_00600)
    :precondition (and
      (ready-road ?v ?r)
      (road-next ?r ?next)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
    )
    :effect (and
      (not (ready-road ?v ?r))
      (ready-road ?v ?next)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-road_0812-road_0332-tw_00000
    :parameters (?v - vehicle ?r - road_type_road_0812 ?next - road_type_road_0332 ?w - window_type_tw_00000)
    :precondition (and
      (ready-road ?v ?r)
      (road-next ?r ?next)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
    )
    :effect (and
      (not (ready-road ?v ?r))
      (ready-road ?v ?next)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-road_0812-road_0332-tw_00030
    :parameters (?v - vehicle ?r - road_type_road_0812 ?next - road_type_road_0332 ?w - window_type_tw_00030)
    :precondition (and
      (ready-road ?v ?r)
      (road-next ?r ?next)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
    )
    :effect (and
      (not (ready-road ?v ?r))
      (ready-road ?v ?next)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-road_0812-road_0332-tw_00060
    :parameters (?v - vehicle ?r - road_type_road_0812 ?next - road_type_road_0332 ?w - window_type_tw_00060)
    :precondition (and
      (ready-road ?v ?r)
      (road-next ?r ?next)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
    )
    :effect (and
      (not (ready-road ?v ?r))
      (ready-road ?v ?next)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-road_0812-road_0332-tw_00090
    :parameters (?v - vehicle ?r - road_type_road_0812 ?next - road_type_road_0332 ?w - window_type_tw_00090)
    :precondition (and
      (ready-road ?v ?r)
      (road-next ?r ?next)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
    )
    :effect (and
      (not (ready-road ?v ?r))
      (ready-road ?v ?next)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-road_0812-road_0332-tw_00120
    :parameters (?v - vehicle ?r - road_type_road_0812 ?next - road_type_road_0332 ?w - window_type_tw_00120)
    :precondition (and
      (ready-road ?v ?r)
      (road-next ?r ?next)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
    )
    :effect (and
      (not (ready-road ?v ?r))
      (ready-road ?v ?next)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-road_0812-road_0332-tw_00150
    :parameters (?v - vehicle ?r - road_type_road_0812 ?next - road_type_road_0332 ?w - window_type_tw_00150)
    :precondition (and
      (ready-road ?v ?r)
      (road-next ?r ?next)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
    )
    :effect (and
      (not (ready-road ?v ?r))
      (ready-road ?v ?next)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-road_0812-road_0332-tw_00180
    :parameters (?v - vehicle ?r - road_type_road_0812 ?next - road_type_road_0332 ?w - window_type_tw_00180)
    :precondition (and
      (ready-road ?v ?r)
      (road-next ?r ?next)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
    )
    :effect (and
      (not (ready-road ?v ?r))
      (ready-road ?v ?next)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-road_0812-road_0332-tw_00210
    :parameters (?v - vehicle ?r - road_type_road_0812 ?next - road_type_road_0332 ?w - window_type_tw_00210)
    :precondition (and
      (ready-road ?v ?r)
      (road-next ?r ?next)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
    )
    :effect (and
      (not (ready-road ?v ?r))
      (ready-road ?v ?next)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-road_0812-road_0332-tw_00240
    :parameters (?v - vehicle ?r - road_type_road_0812 ?next - road_type_road_0332 ?w - window_type_tw_00240)
    :precondition (and
      (ready-road ?v ?r)
      (road-next ?r ?next)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
    )
    :effect (and
      (not (ready-road ?v ?r))
      (ready-road ?v ?next)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-road_0812-road_0332-tw_00270
    :parameters (?v - vehicle ?r - road_type_road_0812 ?next - road_type_road_0332 ?w - window_type_tw_00270)
    :precondition (and
      (ready-road ?v ?r)
      (road-next ?r ?next)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
    )
    :effect (and
      (not (ready-road ?v ?r))
      (ready-road ?v ?next)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-road_0812-road_0332-tw_00300
    :parameters (?v - vehicle ?r - road_type_road_0812 ?next - road_type_road_0332 ?w - window_type_tw_00300)
    :precondition (and
      (ready-road ?v ?r)
      (road-next ?r ?next)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
    )
    :effect (and
      (not (ready-road ?v ?r))
      (ready-road ?v ?next)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-road_0812-road_0332-tw_00330
    :parameters (?v - vehicle ?r - road_type_road_0812 ?next - road_type_road_0332 ?w - window_type_tw_00330)
    :precondition (and
      (ready-road ?v ?r)
      (road-next ?r ?next)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
    )
    :effect (and
      (not (ready-road ?v ?r))
      (ready-road ?v ?next)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-road_0812-road_0332-tw_00360
    :parameters (?v - vehicle ?r - road_type_road_0812 ?next - road_type_road_0332 ?w - window_type_tw_00360)
    :precondition (and
      (ready-road ?v ?r)
      (road-next ?r ?next)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
    )
    :effect (and
      (not (ready-road ?v ?r))
      (ready-road ?v ?next)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-road_0812-road_0332-tw_00390
    :parameters (?v - vehicle ?r - road_type_road_0812 ?next - road_type_road_0332 ?w - window_type_tw_00390)
    :precondition (and
      (ready-road ?v ?r)
      (road-next ?r ?next)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
    )
    :effect (and
      (not (ready-road ?v ?r))
      (ready-road ?v ?next)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-road_0812-road_0332-tw_00420
    :parameters (?v - vehicle ?r - road_type_road_0812 ?next - road_type_road_0332 ?w - window_type_tw_00420)
    :precondition (and
      (ready-road ?v ?r)
      (road-next ?r ?next)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
    )
    :effect (and
      (not (ready-road ?v ?r))
      (ready-road ?v ?next)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-road_0812-road_0332-tw_00450
    :parameters (?v - vehicle ?r - road_type_road_0812 ?next - road_type_road_0332 ?w - window_type_tw_00450)
    :precondition (and
      (ready-road ?v ?r)
      (road-next ?r ?next)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
    )
    :effect (and
      (not (ready-road ?v ?r))
      (ready-road ?v ?next)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-road_0812-road_0332-tw_00480
    :parameters (?v - vehicle ?r - road_type_road_0812 ?next - road_type_road_0332 ?w - window_type_tw_00480)
    :precondition (and
      (ready-road ?v ?r)
      (road-next ?r ?next)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
    )
    :effect (and
      (not (ready-road ?v ?r))
      (ready-road ?v ?next)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-road_0812-road_0332-tw_00510
    :parameters (?v - vehicle ?r - road_type_road_0812 ?next - road_type_road_0332 ?w - window_type_tw_00510)
    :precondition (and
      (ready-road ?v ?r)
      (road-next ?r ?next)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
    )
    :effect (and
      (not (ready-road ?v ?r))
      (ready-road ?v ?next)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-road_0812-road_0332-tw_00540
    :parameters (?v - vehicle ?r - road_type_road_0812 ?next - road_type_road_0332 ?w - window_type_tw_00540)
    :precondition (and
      (ready-road ?v ?r)
      (road-next ?r ?next)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
    )
    :effect (and
      (not (ready-road ?v ?r))
      (ready-road ?v ?next)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-road_0812-road_0332-tw_00570
    :parameters (?v - vehicle ?r - road_type_road_0812 ?next - road_type_road_0332 ?w - window_type_tw_00570)
    :precondition (and
      (ready-road ?v ?r)
      (road-next ?r ?next)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
    )
    :effect (and
      (not (ready-road ?v ?r))
      (ready-road ?v ?next)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-road_0812-road_0332-tw_00600
    :parameters (?v - vehicle ?r - road_type_road_0812 ?next - road_type_road_0332 ?w - window_type_tw_00600)
    :precondition (and
      (ready-road ?v ?r)
      (road-next ?r ?next)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
    )
    :effect (and
      (not (ready-road ?v ?r))
      (ready-road ?v ?next)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-static-road_0813-road_0814
    :parameters (?v - vehicle ?r - road_type_road_0813 ?next - road_type_road_0814)
    :precondition (and
      (ready-road ?v ?r)
      (road-next ?r ?next)
      (road-open ?r)
      (static-road ?r)
      (not (road-blocked ?r))
    )
    :effect (and
      (not (ready-road ?v ?r))
      (ready-road ?v ?next)
      (increase (travel-time ?v) (travel-duration ?r))
      (increase (sim-time) (travel-duration ?r))
    )
  )

  (:action traverse-road-static-road_0813-road_0815
    :parameters (?v - vehicle ?r - road_type_road_0813 ?next - road_type_road_0815)
    :precondition (and
      (ready-road ?v ?r)
      (road-next ?r ?next)
      (road-open ?r)
      (static-road ?r)
      (not (road-blocked ?r))
    )
    :effect (and
      (not (ready-road ?v ?r))
      (ready-road ?v ?next)
      (increase (travel-time ?v) (travel-duration ?r))
      (increase (sim-time) (travel-duration ?r))
    )
  )

  (:action traverse-road-static-road_0814-macro_0037
    :parameters (?v - vehicle ?r - road_type_road_0814 ?next - road_type_macro_0037)
    :precondition (and
      (ready-road ?v ?r)
      (road-next ?r ?next)
      (road-open ?r)
      (static-road ?r)
      (not (road-blocked ?r))
    )
    :effect (and
      (not (ready-road ?v ?r))
      (ready-road ?v ?next)
      (increase (travel-time ?v) (travel-duration ?r))
      (increase (sim-time) (travel-duration ?r))
    )
  )

  (:action traverse-road-static-road_0815-road_0805
    :parameters (?v - vehicle ?r - road_type_road_0815 ?next - road_type_road_0805)
    :precondition (and
      (ready-road ?v ?r)
      (road-next ?r ?next)
      (road-open ?r)
      (static-road ?r)
      (not (road-blocked ?r))
    )
    :effect (and
      (not (ready-road ?v ?r))
      (ready-road ?v ?next)
      (increase (travel-time ?v) (travel-duration ?r))
      (increase (sim-time) (travel-duration ?r))
    )
  )

  (:action traverse-road-static-road_0815-road_0806
    :parameters (?v - vehicle ?r - road_type_road_0815 ?next - road_type_road_0806)
    :precondition (and
      (ready-road ?v ?r)
      (road-next ?r ?next)
      (road-open ?r)
      (static-road ?r)
      (not (road-blocked ?r))
    )
    :effect (and
      (not (ready-road ?v ?r))
      (ready-road ?v ?next)
      (increase (travel-time ?v) (travel-duration ?r))
      (increase (sim-time) (travel-duration ?r))
    )
  )

  (:action traverse-road-static-road_0816-road_0665
    :parameters (?v - vehicle ?r - road_type_road_0816 ?next - road_type_road_0665)
    :precondition (and
      (ready-road ?v ?r)
      (road-next ?r ?next)
      (road-open ?r)
      (static-road ?r)
      (not (road-blocked ?r))
    )
    :effect (and
      (not (ready-road ?v ?r))
      (ready-road ?v ?next)
      (increase (travel-time ?v) (travel-duration ?r))
      (increase (sim-time) (travel-duration ?r))
    )
  )

  (:action traverse-road-static-road_0816-road_0666
    :parameters (?v - vehicle ?r - road_type_road_0816 ?next - road_type_road_0666)
    :precondition (and
      (ready-road ?v ?r)
      (road-next ?r ?next)
      (road-open ?r)
      (static-road ?r)
      (not (road-blocked ?r))
    )
    :effect (and
      (not (ready-road ?v ?r))
      (ready-road ?v ?next)
      (increase (travel-time ?v) (travel-duration ?r))
      (increase (sim-time) (travel-duration ?r))
    )
  )

  (:action traverse-road-static-road_0817-road_0628
    :parameters (?v - vehicle ?r - road_type_road_0817 ?next - road_type_road_0628)
    :precondition (and
      (ready-road ?v ?r)
      (road-next ?r ?next)
      (road-open ?r)
      (static-road ?r)
      (not (road-blocked ?r))
    )
    :effect (and
      (not (ready-road ?v ?r))
      (ready-road ?v ?next)
      (increase (travel-time ?v) (travel-duration ?r))
      (increase (sim-time) (travel-duration ?r))
    )
  )

  (:action traverse-road-static-road_0817-road_0629
    :parameters (?v - vehicle ?r - road_type_road_0817 ?next - road_type_road_0629)
    :precondition (and
      (ready-road ?v ?r)
      (road-next ?r ?next)
      (road-open ?r)
      (static-road ?r)
      (not (road-blocked ?r))
    )
    :effect (and
      (not (ready-road ?v ?r))
      (ready-road ?v ?next)
      (increase (travel-time ?v) (travel-duration ?r))
      (increase (sim-time) (travel-duration ?r))
    )
  )

  (:action traverse-road-static-road_0818-road_0268
    :parameters (?v - vehicle ?r - road_type_road_0818 ?next - road_type_road_0268)
    :precondition (and
      (ready-road ?v ?r)
      (road-next ?r ?next)
      (road-open ?r)
      (static-road ?r)
      (not (road-blocked ?r))
    )
    :effect (and
      (not (ready-road ?v ?r))
      (ready-road ?v ?next)
      (increase (travel-time ?v) (travel-duration ?r))
      (increase (sim-time) (travel-duration ?r))
    )
  )

  (:action traverse-road-static-road_0818-road_0269
    :parameters (?v - vehicle ?r - road_type_road_0818 ?next - road_type_road_0269)
    :precondition (and
      (ready-road ?v ?r)
      (road-next ?r ?next)
      (road-open ?r)
      (static-road ?r)
      (not (road-blocked ?r))
    )
    :effect (and
      (not (ready-road ?v ?r))
      (ready-road ?v ?next)
      (increase (travel-time ?v) (travel-duration ?r))
      (increase (sim-time) (travel-duration ?r))
    )
  )

  (:action traverse-road-static-road_0819-road_0556
    :parameters (?v - vehicle ?r - road_type_road_0819 ?next - road_type_road_0556)
    :precondition (and
      (ready-road ?v ?r)
      (road-next ?r ?next)
      (road-open ?r)
      (static-road ?r)
      (not (road-blocked ?r))
    )
    :effect (and
      (not (ready-road ?v ?r))
      (ready-road ?v ?next)
      (increase (travel-time ?v) (travel-duration ?r))
      (increase (sim-time) (travel-duration ?r))
    )
  )

  (:action traverse-road-static-road_0819-road_0557
    :parameters (?v - vehicle ?r - road_type_road_0819 ?next - road_type_road_0557)
    :precondition (and
      (ready-road ?v ?r)
      (road-next ?r ?next)
      (road-open ?r)
      (static-road ?r)
      (not (road-blocked ?r))
    )
    :effect (and
      (not (ready-road ?v ?r))
      (ready-road ?v ?next)
      (increase (travel-time ?v) (travel-duration ?r))
      (increase (sim-time) (travel-duration ?r))
    )
  )

  (:action traverse-road-static-road_0820-road_0643
    :parameters (?v - vehicle ?r - road_type_road_0820 ?next - road_type_road_0643)
    :precondition (and
      (ready-road ?v ?r)
      (road-next ?r ?next)
      (road-open ?r)
      (static-road ?r)
      (not (road-blocked ?r))
    )
    :effect (and
      (not (ready-road ?v ?r))
      (ready-road ?v ?next)
      (increase (travel-time ?v) (travel-duration ?r))
      (increase (sim-time) (travel-duration ?r))
    )
  )

  (:action traverse-road-static-road_0820-road_0644
    :parameters (?v - vehicle ?r - road_type_road_0820 ?next - road_type_road_0644)
    :precondition (and
      (ready-road ?v ?r)
      (road-next ?r ?next)
      (road-open ?r)
      (static-road ?r)
      (not (road-blocked ?r))
    )
    :effect (and
      (not (ready-road ?v ?r))
      (ready-road ?v ?next)
      (increase (travel-time ?v) (travel-duration ?r))
      (increase (sim-time) (travel-duration ?r))
    )
  )

  (:action traverse-road-static-road_0820-road_0645
    :parameters (?v - vehicle ?r - road_type_road_0820 ?next - road_type_road_0645)
    :precondition (and
      (ready-road ?v ?r)
      (road-next ?r ?next)
      (road-open ?r)
      (static-road ?r)
      (not (road-blocked ?r))
    )
    :effect (and
      (not (ready-road ?v ?r))
      (ready-road ?v ?next)
      (increase (travel-time ?v) (travel-duration ?r))
      (increase (sim-time) (travel-duration ?r))
    )
  )

  (:action traverse-road-static-road_0821-macro_0010
    :parameters (?v - vehicle ?r - road_type_road_0821 ?next - road_type_macro_0010)
    :precondition (and
      (ready-road ?v ?r)
      (road-next ?r ?next)
      (road-open ?r)
      (static-road ?r)
      (not (road-blocked ?r))
    )
    :effect (and
      (not (ready-road ?v ?r))
      (ready-road ?v ?next)
      (increase (travel-time ?v) (travel-duration ?r))
      (increase (sim-time) (travel-duration ?r))
    )
  )

  (:action traverse-road-static-road_0821-road_0102
    :parameters (?v - vehicle ?r - road_type_road_0821 ?next - road_type_road_0102)
    :precondition (and
      (ready-road ?v ?r)
      (road-next ?r ?next)
      (road-open ?r)
      (static-road ?r)
      (not (road-blocked ?r))
    )
    :effect (and
      (not (ready-road ?v ?r))
      (ready-road ?v ?next)
      (increase (travel-time ?v) (travel-duration ?r))
      (increase (sim-time) (travel-duration ?r))
    )
  )

  (:action traverse-road-static-road_0821-road_0104
    :parameters (?v - vehicle ?r - road_type_road_0821 ?next - road_type_road_0104)
    :precondition (and
      (ready-road ?v ?r)
      (road-next ?r ?next)
      (road-open ?r)
      (static-road ?r)
      (not (road-blocked ?r))
    )
    :effect (and
      (not (ready-road ?v ?r))
      (ready-road ?v ?next)
      (increase (travel-time ?v) (travel-duration ?r))
      (increase (sim-time) (travel-duration ?r))
    )
  )

  (:action traverse-road-static-road_0822-road_0162
    :parameters (?v - vehicle ?r - road_type_road_0822 ?next - road_type_road_0162)
    :precondition (and
      (ready-road ?v ?r)
      (road-next ?r ?next)
      (road-open ?r)
      (static-road ?r)
      (not (road-blocked ?r))
    )
    :effect (and
      (not (ready-road ?v ?r))
      (ready-road ?v ?next)
      (increase (travel-time ?v) (travel-duration ?r))
      (increase (sim-time) (travel-duration ?r))
    )
  )

  (:action traverse-road-static-road_0823-road_0821
    :parameters (?v - vehicle ?r - road_type_road_0823 ?next - road_type_road_0821)
    :precondition (and
      (ready-road ?v ?r)
      (road-next ?r ?next)
      (road-open ?r)
      (static-road ?r)
      (not (road-blocked ?r))
    )
    :effect (and
      (not (ready-road ?v ?r))
      (ready-road ?v ?next)
      (increase (travel-time ?v) (travel-duration ?r))
      (increase (sim-time) (travel-duration ?r))
    )
  )

  (:action traverse-road-static-road_0823-road_0822
    :parameters (?v - vehicle ?r - road_type_road_0823 ?next - road_type_road_0822)
    :precondition (and
      (ready-road ?v ?r)
      (road-next ?r ?next)
      (road-open ?r)
      (static-road ?r)
      (not (road-blocked ?r))
    )
    :effect (and
      (not (ready-road ?v ?r))
      (ready-road ?v ?next)
      (increase (travel-time ?v) (travel-duration ?r))
      (increase (sim-time) (travel-duration ?r))
    )
  )

  (:action traverse-road-static-road_0824-road_0162
    :parameters (?v - vehicle ?r - road_type_road_0824 ?next - road_type_road_0162)
    :precondition (and
      (ready-road ?v ?r)
      (road-next ?r ?next)
      (road-open ?r)
      (static-road ?r)
      (not (road-blocked ?r))
    )
    :effect (and
      (not (ready-road ?v ?r))
      (ready-road ?v ?next)
      (increase (travel-time ?v) (travel-duration ?r))
      (increase (sim-time) (travel-duration ?r))
    )
  )

  (:action traverse-road-static-road_0825-road_0827
    :parameters (?v - vehicle ?r - road_type_road_0825 ?next - road_type_road_0827)
    :precondition (and
      (ready-road ?v ?r)
      (road-next ?r ?next)
      (road-open ?r)
      (static-road ?r)
      (not (road-blocked ?r))
    )
    :effect (and
      (not (ready-road ?v ?r))
      (ready-road ?v ?next)
      (increase (travel-time ?v) (travel-duration ?r))
      (increase (sim-time) (travel-duration ?r))
    )
  )

  (:action traverse-road-static-road_0826-road_0685
    :parameters (?v - vehicle ?r - road_type_road_0826 ?next - road_type_road_0685)
    :precondition (and
      (ready-road ?v ?r)
      (road-next ?r ?next)
      (road-open ?r)
      (static-road ?r)
      (not (road-blocked ?r))
    )
    :effect (and
      (not (ready-road ?v ?r))
      (ready-road ?v ?next)
      (increase (travel-time ?v) (travel-duration ?r))
      (increase (sim-time) (travel-duration ?r))
    )
  )

  (:action traverse-road-static-road_0827-macro_0011
    :parameters (?v - vehicle ?r - road_type_road_0827 ?next - road_type_macro_0011)
    :precondition (and
      (ready-road ?v ?r)
      (road-next ?r ?next)
      (road-open ?r)
      (static-road ?r)
      (not (road-blocked ?r))
    )
    :effect (and
      (not (ready-road ?v ?r))
      (ready-road ?v ?next)
      (increase (travel-time ?v) (travel-duration ?r))
      (increase (sim-time) (travel-duration ?r))
    )
  )

  (:action traverse-road-static-road_0827-road_0114
    :parameters (?v - vehicle ?r - road_type_road_0827 ?next - road_type_road_0114)
    :precondition (and
      (ready-road ?v ?r)
      (road-next ?r ?next)
      (road-open ?r)
      (static-road ?r)
      (not (road-blocked ?r))
    )
    :effect (and
      (not (ready-road ?v ?r))
      (ready-road ?v ?next)
      (increase (travel-time ?v) (travel-duration ?r))
      (increase (sim-time) (travel-duration ?r))
    )
  )

  (:action traverse-road-static-road_0827-road_0115
    :parameters (?v - vehicle ?r - road_type_road_0827 ?next - road_type_road_0115)
    :precondition (and
      (ready-road ?v ?r)
      (road-next ?r ?next)
      (road-open ?r)
      (static-road ?r)
      (not (road-blocked ?r))
    )
    :effect (and
      (not (ready-road ?v ?r))
      (ready-road ?v ?next)
      (increase (travel-time ?v) (travel-duration ?r))
      (increase (sim-time) (travel-duration ?r))
    )
  )

  (:action traverse-road-static-road_0828-road_0827
    :parameters (?v - vehicle ?r - road_type_road_0828 ?next - road_type_road_0827)
    :precondition (and
      (ready-road ?v ?r)
      (road-next ?r ?next)
      (road-open ?r)
      (static-road ?r)
      (not (road-blocked ?r))
    )
    :effect (and
      (not (ready-road ?v ?r))
      (ready-road ?v ?next)
      (increase (travel-time ?v) (travel-duration ?r))
      (increase (sim-time) (travel-duration ?r))
    )
  )

  (:action traverse-road-static-road_0834-road_0020
    :parameters (?v - vehicle ?r - road_type_road_0834 ?next - road_type_road_0020)
    :precondition (and
      (ready-road ?v ?r)
      (road-next ?r ?next)
      (road-open ?r)
      (static-road ?r)
      (not (road-blocked ?r))
    )
    :effect (and
      (not (ready-road ?v ?r))
      (ready-road ?v ?next)
      (increase (travel-time ?v) (travel-duration ?r))
      (increase (sim-time) (travel-duration ?r))
    )
  )

  (:action traverse-road-static-road_0834-road_0021
    :parameters (?v - vehicle ?r - road_type_road_0834 ?next - road_type_road_0021)
    :precondition (and
      (ready-road ?v ?r)
      (road-next ?r ?next)
      (road-open ?r)
      (static-road ?r)
      (not (road-blocked ?r))
    )
    :effect (and
      (not (ready-road ?v ?r))
      (ready-road ?v ?next)
      (increase (travel-time ?v) (travel-duration ?r))
      (increase (sim-time) (travel-duration ?r))
    )
  )

  (:action traverse-road-static-road_0835-road_0680
    :parameters (?v - vehicle ?r - road_type_road_0835 ?next - road_type_road_0680)
    :precondition (and
      (ready-road ?v ?r)
      (road-next ?r ?next)
      (road-open ?r)
      (static-road ?r)
      (not (road-blocked ?r))
    )
    :effect (and
      (not (ready-road ?v ?r))
      (ready-road ?v ?next)
      (increase (travel-time ?v) (travel-duration ?r))
      (increase (sim-time) (travel-duration ?r))
    )
  )

  (:action traverse-road-static-road_0835-road_0681
    :parameters (?v - vehicle ?r - road_type_road_0835 ?next - road_type_road_0681)
    :precondition (and
      (ready-road ?v ?r)
      (road-next ?r ?next)
      (road-open ?r)
      (static-road ?r)
      (not (road-blocked ?r))
    )
    :effect (and
      (not (ready-road ?v ?r))
      (ready-road ?v ?next)
      (increase (travel-time ?v) (travel-duration ?r))
      (increase (sim-time) (travel-duration ?r))
    )
  )

  (:action traverse-road-static-road_0836-road_0166
    :parameters (?v - vehicle ?r - road_type_road_0836 ?next - road_type_road_0166)
    :precondition (and
      (ready-road ?v ?r)
      (road-next ?r ?next)
      (road-open ?r)
      (static-road ?r)
      (not (road-blocked ?r))
    )
    :effect (and
      (not (ready-road ?v ?r))
      (ready-road ?v ?next)
      (increase (travel-time ?v) (travel-duration ?r))
      (increase (sim-time) (travel-duration ?r))
    )
  )

  (:action traverse-road-static-road_0836-road_0167
    :parameters (?v - vehicle ?r - road_type_road_0836 ?next - road_type_road_0167)
    :precondition (and
      (ready-road ?v ?r)
      (road-next ?r ?next)
      (road-open ?r)
      (static-road ?r)
      (not (road-blocked ?r))
    )
    :effect (and
      (not (ready-road ?v ?r))
      (ready-road ?v ?next)
      (increase (travel-time ?v) (travel-duration ?r))
      (increase (sim-time) (travel-duration ?r))
    )
  )

  (:action traverse-road-static-road_0838-road_0098
    :parameters (?v - vehicle ?r - road_type_road_0838 ?next - road_type_road_0098)
    :precondition (and
      (ready-road ?v ?r)
      (road-next ?r ?next)
      (road-open ?r)
      (static-road ?r)
      (not (road-blocked ?r))
    )
    :effect (and
      (not (ready-road ?v ?r))
      (ready-road ?v ?next)
      (increase (travel-time ?v) (travel-duration ?r))
      (increase (sim-time) (travel-duration ?r))
    )
  )

  (:action traverse-road-static-road_0838-road_0099
    :parameters (?v - vehicle ?r - road_type_road_0838 ?next - road_type_road_0099)
    :precondition (and
      (ready-road ?v ?r)
      (road-next ?r ?next)
      (road-open ?r)
      (static-road ?r)
      (not (road-blocked ?r))
    )
    :effect (and
      (not (ready-road ?v ?r))
      (ready-road ?v ?next)
      (increase (travel-time ?v) (travel-duration ?r))
      (increase (sim-time) (travel-duration ?r))
    )
  )

  (:action traverse-road-static-road_0839-road_0161
    :parameters (?v - vehicle ?r - road_type_road_0839 ?next - road_type_road_0161)
    :precondition (and
      (ready-road ?v ?r)
      (road-next ?r ?next)
      (road-open ?r)
      (static-road ?r)
      (not (road-blocked ?r))
    )
    :effect (and
      (not (ready-road ?v ?r))
      (ready-road ?v ?next)
      (increase (travel-time ?v) (travel-duration ?r))
      (increase (sim-time) (travel-duration ?r))
    )
  )

  (:action traverse-road-static-road_0843-road_0161
    :parameters (?v - vehicle ?r - road_type_road_0843 ?next - road_type_road_0161)
    :precondition (and
      (ready-road ?v ?r)
      (road-next ?r ?next)
      (road-open ?r)
      (static-road ?r)
      (not (road-blocked ?r))
    )
    :effect (and
      (not (ready-road ?v ?r))
      (ready-road ?v ?next)
      (increase (travel-time ?v) (travel-duration ?r))
      (increase (sim-time) (travel-duration ?r))
    )
  )

  (:action traverse-road-static-road_0844-road_0838
    :parameters (?v - vehicle ?r - road_type_road_0844 ?next - road_type_road_0838)
    :precondition (and
      (ready-road ?v ?r)
      (road-next ?r ?next)
      (road-open ?r)
      (static-road ?r)
      (not (road-blocked ?r))
    )
    :effect (and
      (not (ready-road ?v ?r))
      (ready-road ?v ?next)
      (increase (travel-time ?v) (travel-duration ?r))
      (increase (sim-time) (travel-duration ?r))
    )
  )

  (:action traverse-road-static-road_0844-road_0839
    :parameters (?v - vehicle ?r - road_type_road_0844 ?next - road_type_road_0839)
    :precondition (and
      (ready-road ?v ?r)
      (road-next ?r ?next)
      (road-open ?r)
      (static-road ?r)
      (not (road-blocked ?r))
    )
    :effect (and
      (not (ready-road ?v ?r))
      (ready-road ?v ?next)
      (increase (travel-time ?v) (travel-duration ?r))
      (increase (sim-time) (travel-duration ?r))
    )
  )

  (:action traverse-road-static-road_0845-road_0574
    :parameters (?v - vehicle ?r - road_type_road_0845 ?next - road_type_road_0574)
    :precondition (and
      (ready-road ?v ?r)
      (road-next ?r ?next)
      (road-open ?r)
      (static-road ?r)
      (not (road-blocked ?r))
    )
    :effect (and
      (not (ready-road ?v ?r))
      (ready-road ?v ?next)
      (increase (travel-time ?v) (travel-duration ?r))
      (increase (sim-time) (travel-duration ?r))
    )
  )

  (:action traverse-road-static-road_0845-road_0575
    :parameters (?v - vehicle ?r - road_type_road_0845 ?next - road_type_road_0575)
    :precondition (and
      (ready-road ?v ?r)
      (road-next ?r ?next)
      (road-open ?r)
      (static-road ?r)
      (not (road-blocked ?r))
    )
    :effect (and
      (not (ready-road ?v ?r))
      (ready-road ?v ?next)
      (increase (travel-time ?v) (travel-duration ?r))
      (increase (sim-time) (travel-duration ?r))
    )
  )

  (:action traverse-road-static-road_0845-road_0576
    :parameters (?v - vehicle ?r - road_type_road_0845 ?next - road_type_road_0576)
    :precondition (and
      (ready-road ?v ?r)
      (road-next ?r ?next)
      (road-open ?r)
      (static-road ?r)
      (not (road-blocked ?r))
    )
    :effect (and
      (not (ready-road ?v ?r))
      (ready-road ?v ?next)
      (increase (travel-time ?v) (travel-duration ?r))
      (increase (sim-time) (travel-duration ?r))
    )
  )

  (:action traverse-road-static-road_0846-road_0170
    :parameters (?v - vehicle ?r - road_type_road_0846 ?next - road_type_road_0170)
    :precondition (and
      (ready-road ?v ?r)
      (road-next ?r ?next)
      (road-open ?r)
      (static-road ?r)
      (not (road-blocked ?r))
    )
    :effect (and
      (not (ready-road ?v ?r))
      (ready-road ?v ?next)
      (increase (travel-time ?v) (travel-duration ?r))
      (increase (sim-time) (travel-duration ?r))
    )
  )

  (:action traverse-road-static-road_0847-macro_0008
    :parameters (?v - vehicle ?r - road_type_road_0847 ?next - road_type_macro_0008)
    :precondition (and
      (ready-road ?v ?r)
      (road-next ?r ?next)
      (road-open ?r)
      (static-road ?r)
      (not (road-blocked ?r))
    )
    :effect (and
      (not (ready-road ?v ?r))
      (ready-road ?v ?next)
      (increase (travel-time ?v) (travel-duration ?r))
      (increase (sim-time) (travel-duration ?r))
    )
  )

  (:action traverse-road-static-road_0847-road_0086
    :parameters (?v - vehicle ?r - road_type_road_0847 ?next - road_type_road_0086)
    :precondition (and
      (ready-road ?v ?r)
      (road-next ?r ?next)
      (road-open ?r)
      (static-road ?r)
      (not (road-blocked ?r))
    )
    :effect (and
      (not (ready-road ?v ?r))
      (ready-road ?v ?next)
      (increase (travel-time ?v) (travel-duration ?r))
      (increase (sim-time) (travel-duration ?r))
    )
  )

  (:action traverse-road-static-road_0847-road_0087
    :parameters (?v - vehicle ?r - road_type_road_0847 ?next - road_type_road_0087)
    :precondition (and
      (ready-road ?v ?r)
      (road-next ?r ?next)
      (road-open ?r)
      (static-road ?r)
      (not (road-blocked ?r))
    )
    :effect (and
      (not (ready-road ?v ?r))
      (ready-road ?v ?next)
      (increase (travel-time ?v) (travel-duration ?r))
      (increase (sim-time) (travel-duration ?r))
    )
  )

  (:action traverse-road-static-road_0847-road_0088
    :parameters (?v - vehicle ?r - road_type_road_0847 ?next - road_type_road_0088)
    :precondition (and
      (ready-road ?v ?r)
      (road-next ?r ?next)
      (road-open ?r)
      (static-road ?r)
      (not (road-blocked ?r))
    )
    :effect (and
      (not (ready-road ?v ?r))
      (ready-road ?v ?next)
      (increase (travel-time ?v) (travel-duration ?r))
      (increase (sim-time) (travel-duration ?r))
    )
  )

  (:action traverse-road-static-road_0848-road_0196
    :parameters (?v - vehicle ?r - road_type_road_0848 ?next - road_type_road_0196)
    :precondition (and
      (ready-road ?v ?r)
      (road-next ?r ?next)
      (road-open ?r)
      (static-road ?r)
      (not (road-blocked ?r))
    )
    :effect (and
      (not (ready-road ?v ?r))
      (ready-road ?v ?next)
      (increase (travel-time ?v) (travel-duration ?r))
      (increase (sim-time) (travel-duration ?r))
    )
  )

  (:action traverse-road-static-road_0849-road_0633
    :parameters (?v - vehicle ?r - road_type_road_0849 ?next - road_type_road_0633)
    :precondition (and
      (ready-road ?v ?r)
      (road-next ?r ?next)
      (road-open ?r)
      (static-road ?r)
      (not (road-blocked ?r))
    )
    :effect (and
      (not (ready-road ?v ?r))
      (ready-road ?v ?next)
      (increase (travel-time ?v) (travel-duration ?r))
      (increase (sim-time) (travel-duration ?r))
    )
  )

  (:action traverse-road-static-road_0851-road_0393
    :parameters (?v - vehicle ?r - road_type_road_0851 ?next - road_type_road_0393)
    :precondition (and
      (ready-road ?v ?r)
      (road-next ?r ?next)
      (road-open ?r)
      (static-road ?r)
      (not (road-blocked ?r))
    )
    :effect (and
      (not (ready-road ?v ?r))
      (ready-road ?v ?next)
      (increase (travel-time ?v) (travel-duration ?r))
      (increase (sim-time) (travel-duration ?r))
    )
  )

  (:action traverse-road-static-road_0851-road_0394
    :parameters (?v - vehicle ?r - road_type_road_0851 ?next - road_type_road_0394)
    :precondition (and
      (ready-road ?v ?r)
      (road-next ?r ?next)
      (road-open ?r)
      (static-road ?r)
      (not (road-blocked ?r))
    )
    :effect (and
      (not (ready-road ?v ?r))
      (ready-road ?v ?next)
      (increase (travel-time ?v) (travel-duration ?r))
      (increase (sim-time) (travel-duration ?r))
    )
  )

  (:action traverse-road-static-road_0852-road_0197
    :parameters (?v - vehicle ?r - road_type_road_0852 ?next - road_type_road_0197)
    :precondition (and
      (ready-road ?v ?r)
      (road-next ?r ?next)
      (road-open ?r)
      (static-road ?r)
      (not (road-blocked ?r))
    )
    :effect (and
      (not (ready-road ?v ?r))
      (ready-road ?v ?next)
      (increase (travel-time ?v) (travel-duration ?r))
      (increase (sim-time) (travel-duration ?r))
    )
  )

  (:action traverse-road-static-road_0852-road_0198
    :parameters (?v - vehicle ?r - road_type_road_0852 ?next - road_type_road_0198)
    :precondition (and
      (ready-road ?v ?r)
      (road-next ?r ?next)
      (road-open ?r)
      (static-road ?r)
      (not (road-blocked ?r))
    )
    :effect (and
      (not (ready-road ?v ?r))
      (ready-road ?v ?next)
      (increase (travel-time ?v) (travel-duration ?r))
      (increase (sim-time) (travel-duration ?r))
    )
  )

  (:action traverse-road-static-road_0852-road_0199
    :parameters (?v - vehicle ?r - road_type_road_0852 ?next - road_type_road_0199)
    :precondition (and
      (ready-road ?v ?r)
      (road-next ?r ?next)
      (road-open ?r)
      (static-road ?r)
      (not (road-blocked ?r))
    )
    :effect (and
      (not (ready-road ?v ?r))
      (ready-road ?v ?next)
      (increase (travel-time ?v) (travel-duration ?r))
      (increase (sim-time) (travel-duration ?r))
    )
  )

  (:action traverse-road-static-road_0853-road_0397
    :parameters (?v - vehicle ?r - road_type_road_0853 ?next - road_type_road_0397)
    :precondition (and
      (ready-road ?v ?r)
      (road-next ?r ?next)
      (road-open ?r)
      (static-road ?r)
      (not (road-blocked ?r))
    )
    :effect (and
      (not (ready-road ?v ?r))
      (ready-road ?v ?next)
      (increase (travel-time ?v) (travel-duration ?r))
      (increase (sim-time) (travel-duration ?r))
    )
  )

  (:action traverse-road-static-road_0853-road_0398
    :parameters (?v - vehicle ?r - road_type_road_0853 ?next - road_type_road_0398)
    :precondition (and
      (ready-road ?v ?r)
      (road-next ?r ?next)
      (road-open ?r)
      (static-road ?r)
      (not (road-blocked ?r))
    )
    :effect (and
      (not (ready-road ?v ?r))
      (ready-road ?v ?next)
      (increase (travel-time ?v) (travel-duration ?r))
      (increase (sim-time) (travel-duration ?r))
    )
  )

  (:action traverse-road-static-road_0854-road_0216
    :parameters (?v - vehicle ?r - road_type_road_0854 ?next - road_type_road_0216)
    :precondition (and
      (ready-road ?v ?r)
      (road-next ?r ?next)
      (road-open ?r)
      (static-road ?r)
      (not (road-blocked ?r))
    )
    :effect (and
      (not (ready-road ?v ?r))
      (ready-road ?v ?next)
      (increase (travel-time ?v) (travel-duration ?r))
      (increase (sim-time) (travel-duration ?r))
    )
  )

  (:action traverse-road-static-road_0854-road_0217
    :parameters (?v - vehicle ?r - road_type_road_0854 ?next - road_type_road_0217)
    :precondition (and
      (ready-road ?v ?r)
      (road-next ?r ?next)
      (road-open ?r)
      (static-road ?r)
      (not (road-blocked ?r))
    )
    :effect (and
      (not (ready-road ?v ?r))
      (ready-road ?v ?next)
      (increase (travel-time ?v) (travel-duration ?r))
      (increase (sim-time) (travel-duration ?r))
    )
  )

  (:action traverse-road-static-road_0855-road_0010
    :parameters (?v - vehicle ?r - road_type_road_0855 ?next - road_type_road_0010)
    :precondition (and
      (ready-road ?v ?r)
      (road-next ?r ?next)
      (road-open ?r)
      (static-road ?r)
      (not (road-blocked ?r))
    )
    :effect (and
      (not (ready-road ?v ?r))
      (ready-road ?v ?next)
      (increase (travel-time ?v) (travel-duration ?r))
      (increase (sim-time) (travel-duration ?r))
    )
  )

  (:action traverse-road-static-road_0856-road_0142
    :parameters (?v - vehicle ?r - road_type_road_0856 ?next - road_type_road_0142)
    :precondition (and
      (ready-road ?v ?r)
      (road-next ?r ?next)
      (road-open ?r)
      (static-road ?r)
      (not (road-blocked ?r))
    )
    :effect (and
      (not (ready-road ?v ?r))
      (ready-road ?v ?next)
      (increase (travel-time ?v) (travel-duration ?r))
      (increase (sim-time) (travel-duration ?r))
    )
  )

  (:action traverse-road-static-road_0856-road_0143
    :parameters (?v - vehicle ?r - road_type_road_0856 ?next - road_type_road_0143)
    :precondition (and
      (ready-road ?v ?r)
      (road-next ?r ?next)
      (road-open ?r)
      (static-road ?r)
      (not (road-blocked ?r))
    )
    :effect (and
      (not (ready-road ?v ?r))
      (ready-road ?v ?next)
      (increase (travel-time ?v) (travel-duration ?r))
      (increase (sim-time) (travel-duration ?r))
    )
  )

  (:action traverse-road-static-road_0857-road_0518
    :parameters (?v - vehicle ?r - road_type_road_0857 ?next - road_type_road_0518)
    :precondition (and
      (ready-road ?v ?r)
      (road-next ?r ?next)
      (road-open ?r)
      (static-road ?r)
      (not (road-blocked ?r))
    )
    :effect (and
      (not (ready-road ?v ?r))
      (ready-road ?v ?next)
      (increase (travel-time ?v) (travel-duration ?r))
      (increase (sim-time) (travel-duration ?r))
    )
  )

  (:action traverse-road-static-road_0857-road_0519
    :parameters (?v - vehicle ?r - road_type_road_0857 ?next - road_type_road_0519)
    :precondition (and
      (ready-road ?v ?r)
      (road-next ?r ?next)
      (road-open ?r)
      (static-road ?r)
      (not (road-blocked ?r))
    )
    :effect (and
      (not (ready-road ?v ?r))
      (ready-road ?v ?next)
      (increase (travel-time ?v) (travel-duration ?r))
      (increase (sim-time) (travel-duration ?r))
    )
  )

  (:action traverse-road-static-road_0859-road_0301
    :parameters (?v - vehicle ?r - road_type_road_0859 ?next - road_type_road_0301)
    :precondition (and
      (ready-road ?v ?r)
      (road-next ?r ?next)
      (road-open ?r)
      (static-road ?r)
      (not (road-blocked ?r))
    )
    :effect (and
      (not (ready-road ?v ?r))
      (ready-road ?v ?next)
      (increase (travel-time ?v) (travel-duration ?r))
      (increase (sim-time) (travel-duration ?r))
    )
  )

  (:action traverse-road-static-road_0859-road_0302
    :parameters (?v - vehicle ?r - road_type_road_0859 ?next - road_type_road_0302)
    :precondition (and
      (ready-road ?v ?r)
      (road-next ?r ?next)
      (road-open ?r)
      (static-road ?r)
      (not (road-blocked ?r))
    )
    :effect (and
      (not (ready-road ?v ?r))
      (ready-road ?v ?next)
      (increase (travel-time ?v) (travel-duration ?r))
      (increase (sim-time) (travel-duration ?r))
    )
  )

  (:action traverse-road-static-road_0860-macro_0042
    :parameters (?v - vehicle ?r - road_type_road_0860 ?next - road_type_macro_0042)
    :precondition (and
      (ready-road ?v ?r)
      (road-next ?r ?next)
      (road-open ?r)
      (static-road ?r)
      (not (road-blocked ?r))
    )
    :effect (and
      (not (ready-road ?v ?r))
      (ready-road ?v ?next)
      (increase (travel-time ?v) (travel-duration ?r))
      (increase (sim-time) (travel-duration ?r))
    )
  )

  (:action traverse-road-static-road_0860-road_0583
    :parameters (?v - vehicle ?r - road_type_road_0860 ?next - road_type_road_0583)
    :precondition (and
      (ready-road ?v ?r)
      (road-next ?r ?next)
      (road-open ?r)
      (static-road ?r)
      (not (road-blocked ?r))
    )
    :effect (and
      (not (ready-road ?v ?r))
      (ready-road ?v ?next)
      (increase (travel-time ?v) (travel-duration ?r))
      (increase (sim-time) (travel-duration ?r))
    )
  )

  (:action traverse-road-static-road_0860-road_0584
    :parameters (?v - vehicle ?r - road_type_road_0860 ?next - road_type_road_0584)
    :precondition (and
      (ready-road ?v ?r)
      (road-next ?r ?next)
      (road-open ?r)
      (static-road ?r)
      (not (road-blocked ?r))
    )
    :effect (and
      (not (ready-road ?v ?r))
      (ready-road ?v ?next)
      (increase (travel-time ?v) (travel-duration ?r))
      (increase (sim-time) (travel-duration ?r))
    )
  )

  (:action traverse-road-dynamic-road_0861-road_0283-tw_00000
    :parameters (?v - vehicle ?r - road_type_road_0861 ?next - road_type_road_0283 ?w - window_type_tw_00000)
    :precondition (and
      (ready-road ?v ?r)
      (road-next ?r ?next)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
    )
    :effect (and
      (not (ready-road ?v ?r))
      (ready-road ?v ?next)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-road_0861-road_0283-tw_00030
    :parameters (?v - vehicle ?r - road_type_road_0861 ?next - road_type_road_0283 ?w - window_type_tw_00030)
    :precondition (and
      (ready-road ?v ?r)
      (road-next ?r ?next)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
    )
    :effect (and
      (not (ready-road ?v ?r))
      (ready-road ?v ?next)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-road_0861-road_0283-tw_00060
    :parameters (?v - vehicle ?r - road_type_road_0861 ?next - road_type_road_0283 ?w - window_type_tw_00060)
    :precondition (and
      (ready-road ?v ?r)
      (road-next ?r ?next)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
    )
    :effect (and
      (not (ready-road ?v ?r))
      (ready-road ?v ?next)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-road_0861-road_0283-tw_00090
    :parameters (?v - vehicle ?r - road_type_road_0861 ?next - road_type_road_0283 ?w - window_type_tw_00090)
    :precondition (and
      (ready-road ?v ?r)
      (road-next ?r ?next)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
    )
    :effect (and
      (not (ready-road ?v ?r))
      (ready-road ?v ?next)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-road_0861-road_0283-tw_00120
    :parameters (?v - vehicle ?r - road_type_road_0861 ?next - road_type_road_0283 ?w - window_type_tw_00120)
    :precondition (and
      (ready-road ?v ?r)
      (road-next ?r ?next)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
    )
    :effect (and
      (not (ready-road ?v ?r))
      (ready-road ?v ?next)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-road_0861-road_0283-tw_00150
    :parameters (?v - vehicle ?r - road_type_road_0861 ?next - road_type_road_0283 ?w - window_type_tw_00150)
    :precondition (and
      (ready-road ?v ?r)
      (road-next ?r ?next)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
    )
    :effect (and
      (not (ready-road ?v ?r))
      (ready-road ?v ?next)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-road_0861-road_0283-tw_00180
    :parameters (?v - vehicle ?r - road_type_road_0861 ?next - road_type_road_0283 ?w - window_type_tw_00180)
    :precondition (and
      (ready-road ?v ?r)
      (road-next ?r ?next)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
    )
    :effect (and
      (not (ready-road ?v ?r))
      (ready-road ?v ?next)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-road_0861-road_0283-tw_00210
    :parameters (?v - vehicle ?r - road_type_road_0861 ?next - road_type_road_0283 ?w - window_type_tw_00210)
    :precondition (and
      (ready-road ?v ?r)
      (road-next ?r ?next)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
    )
    :effect (and
      (not (ready-road ?v ?r))
      (ready-road ?v ?next)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-road_0861-road_0283-tw_00240
    :parameters (?v - vehicle ?r - road_type_road_0861 ?next - road_type_road_0283 ?w - window_type_tw_00240)
    :precondition (and
      (ready-road ?v ?r)
      (road-next ?r ?next)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
    )
    :effect (and
      (not (ready-road ?v ?r))
      (ready-road ?v ?next)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-road_0861-road_0283-tw_00270
    :parameters (?v - vehicle ?r - road_type_road_0861 ?next - road_type_road_0283 ?w - window_type_tw_00270)
    :precondition (and
      (ready-road ?v ?r)
      (road-next ?r ?next)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
    )
    :effect (and
      (not (ready-road ?v ?r))
      (ready-road ?v ?next)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-road_0861-road_0283-tw_00300
    :parameters (?v - vehicle ?r - road_type_road_0861 ?next - road_type_road_0283 ?w - window_type_tw_00300)
    :precondition (and
      (ready-road ?v ?r)
      (road-next ?r ?next)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
    )
    :effect (and
      (not (ready-road ?v ?r))
      (ready-road ?v ?next)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-road_0861-road_0283-tw_00330
    :parameters (?v - vehicle ?r - road_type_road_0861 ?next - road_type_road_0283 ?w - window_type_tw_00330)
    :precondition (and
      (ready-road ?v ?r)
      (road-next ?r ?next)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
    )
    :effect (and
      (not (ready-road ?v ?r))
      (ready-road ?v ?next)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-road_0861-road_0283-tw_00360
    :parameters (?v - vehicle ?r - road_type_road_0861 ?next - road_type_road_0283 ?w - window_type_tw_00360)
    :precondition (and
      (ready-road ?v ?r)
      (road-next ?r ?next)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
    )
    :effect (and
      (not (ready-road ?v ?r))
      (ready-road ?v ?next)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-road_0861-road_0283-tw_00390
    :parameters (?v - vehicle ?r - road_type_road_0861 ?next - road_type_road_0283 ?w - window_type_tw_00390)
    :precondition (and
      (ready-road ?v ?r)
      (road-next ?r ?next)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
    )
    :effect (and
      (not (ready-road ?v ?r))
      (ready-road ?v ?next)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-road_0861-road_0283-tw_00420
    :parameters (?v - vehicle ?r - road_type_road_0861 ?next - road_type_road_0283 ?w - window_type_tw_00420)
    :precondition (and
      (ready-road ?v ?r)
      (road-next ?r ?next)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
    )
    :effect (and
      (not (ready-road ?v ?r))
      (ready-road ?v ?next)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-road_0861-road_0283-tw_00450
    :parameters (?v - vehicle ?r - road_type_road_0861 ?next - road_type_road_0283 ?w - window_type_tw_00450)
    :precondition (and
      (ready-road ?v ?r)
      (road-next ?r ?next)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
    )
    :effect (and
      (not (ready-road ?v ?r))
      (ready-road ?v ?next)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-road_0861-road_0283-tw_00480
    :parameters (?v - vehicle ?r - road_type_road_0861 ?next - road_type_road_0283 ?w - window_type_tw_00480)
    :precondition (and
      (ready-road ?v ?r)
      (road-next ?r ?next)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
    )
    :effect (and
      (not (ready-road ?v ?r))
      (ready-road ?v ?next)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-road_0861-road_0283-tw_00510
    :parameters (?v - vehicle ?r - road_type_road_0861 ?next - road_type_road_0283 ?w - window_type_tw_00510)
    :precondition (and
      (ready-road ?v ?r)
      (road-next ?r ?next)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
    )
    :effect (and
      (not (ready-road ?v ?r))
      (ready-road ?v ?next)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-road_0861-road_0283-tw_00540
    :parameters (?v - vehicle ?r - road_type_road_0861 ?next - road_type_road_0283 ?w - window_type_tw_00540)
    :precondition (and
      (ready-road ?v ?r)
      (road-next ?r ?next)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
    )
    :effect (and
      (not (ready-road ?v ?r))
      (ready-road ?v ?next)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-road_0861-road_0283-tw_00570
    :parameters (?v - vehicle ?r - road_type_road_0861 ?next - road_type_road_0283 ?w - window_type_tw_00570)
    :precondition (and
      (ready-road ?v ?r)
      (road-next ?r ?next)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
    )
    :effect (and
      (not (ready-road ?v ?r))
      (ready-road ?v ?next)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  (:action traverse-road-dynamic-road_0861-road_0283-tw_00600
    :parameters (?v - vehicle ?r - road_type_road_0861 ?next - road_type_road_0283 ?w - window_type_tw_00600)
    :precondition (and
      (ready-road ?v ?r)
      (road-next ?r ?next)
      (road-open ?r)
      (dynamic-road ?r)
      (current-window ?w)
      (not (road-blocked ?r))
    )
    :effect (and
      (not (ready-road ?v ?r))
      (ready-road ?v ?next)
      (increase (travel-time ?v) (travel-duration-window ?r ?w))
      (increase (sim-time) (travel-duration-window ?r ?w))
    )
  )

  ;; Advances the single global congestion window.
  (:event advance-window
    :parameters (?from - time-window ?to - time-window)
    :precondition (and
      (current-window ?from)
      (next-window ?from ?to)
      (>= (sim-time) (window-start ?to))
    )
    :effect (and
      (not (current-window ?from))
      (current-window ?to)
    )
  )
)
