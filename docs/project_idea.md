# Project idea

The project follows the professor's idea: start from a simplified OpenStreetMap representation of a real city and map the road structure into PDDL.

The selected city is Bologna city center because it has main roads, many small intersections, and realistic routing constraints.

The main objective is not to model the entire city perfectly. The objective is to build a simplified but coherent planning model.
